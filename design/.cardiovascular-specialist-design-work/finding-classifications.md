# Phase 4 — Finding Classifications (cardiovascular-specialist design doc)

Orchestrator-PERSONAL classification of every Phase-3 red-team finding per the **PF-S3-01 guard**: each finding was source-read against its cited location in `design/cardiovascular-specialist-design.md` (and the substrate / contract pack) before classification — no auto-accept, no auto-reject. REJECTED rows carry source-of-truth attestation. Verdicts: **LEGITIMATE** (incorporated as-stated), **LEGITIMATE-MODIFIED** (incorporated with a modification), **REJECTED** (premise empirically wrong; attested). Inputs: Role 3 coverage report (`red-team-role3-coverage.md`, BLOCK_WITH_FINDINGS, 5 findings incl. 1 clean verdict) + Role 4 adversarial report (`red-team-role4-safety.md`, BLOCK/CRITICAL, 6 findings). Composed `final_harm_class = max(Role3.nominal, Role4.worst_case_reachable)` resolvable now that BOTH reports exist (Role 4's CV-SF-03 "Role 3 absent" was a parallel-dispatch timing artifact — see below).

| ID | Source | Verdict | Personal source-read + disposition |
|---|---|---|---|
| CV-COV-001 | R3 | **LEGITIMATE-MODIFIED** | I wrote §15.2 with 11 role-specific criteria; `DESIGN_DOC_TEMPLATE.md` §15 caps at "5–10 binary pass/fail criteria" (verified L483 of template). 11 > 10 = real overshoot. Per the budget-overage discipline (classify load-bearing vs reducible): criterion 11 (no-compounds-write) is LOAD-BEARING (the key ownership decision, CV-COV-004-confirmed), criteria 2 (refusal-class enumeration) + 3 (mode-floor string) are both deploy-gate-enumeration checks and REDUCIBLE-by-merge. **Fix:** merge §15.2 criteria 2+3 into one → 10 criteria. |
| CV-COV-002 | R3 | **LEGITIMATE-MODIFIED** | Verified §13 tags `audit-cardiac-emergency-floor.sh` + `audit-device-screening-refusal.sh` PROPOSED, surfaced in §18 OQ-1/OQ-2, named as mitigations in §17.1 risks 1/2. Role 3 itself classifies this P2-annotate (NOT P0) because the behavior IS encoded (§5 r1, §6 step2 evaluated-first, §7 short-circuit, §14 two red-flag stimuli) and the design correctly tags PROPOSED + surfaces in §18 per template F-010 (a PROPOSED row does NOT gate the agent.md). **Disposition:** template-compliant; no structural design change. The two PROPOSED audits become **beads-to-create** for the integrator (the highest-value follow-ups). Strengthen §13 note + §17.1 to state the compensating LIVE controls (refusal-class presence + section-count + role-inlining are LIVE; the floor is a fail-safe binary in §5/§6/§7) so the residual is the *defense-in-depth audit*, not the behavior. |
| CV-COV-003 | R3 | **LEGITIMATE-MODIFIED** | Verified `grep BASIS_NOT_REVIEWABLE` = §2.2 (enumerated) + §6 step5 (operational), absent from §11.2/§12 — thin vs the other 7 classes. BUT the ≥4-class floor is met (8 classes), and the BASIS_NOT_REVIEWABLE recognition surface IS present in §11.2 Anti-Pattern 7 ("write a value/citation I can't ground to a whitelisted primary") + Ask-vs-Proceed §5. **Fix:** name `BASIS_NOT_REVIEWABLE` explicitly in Anti-Pattern 7's cue (one phrase) so the class is exercised, not just enumerated. No new AP (budget at 8). |
| CV-COV-004 | R3 | **NOT-A-DEFECT (clean verdict)** | Role 3's load-bearing consistency check on the no-compounds-write decision returned CONSISTENT across §2.2/§8/§9.1/§15.2/§17.2/§18-OQ-4 with cited locators. I re-verified: the `target-class=compound` dispatch lands on name-and-route reasoning + owned biomarker/protocol/parameter updates (§8), R7 is re-scoped to a personalized-reasoning precondition (§6 step4), risk-floor-readiness fields are runtime Communication props (§9.1 field5). No orphaned dispatch target, no dangling write surface. **No action** — confirms the architect-reading reconciliation was correct. |
| CV-COV-005 | R3 | **LEGITIMATE-MODIFIED** | Verified §5 rule 5 carries the inflammation "carry-the-harm-with-the-benefit" discipline (CANTOS fatal-infection / LoDoCo2 non-CV-death), but no §11.2 cue / §12 pair instantiates it, unlike device/association/class-label/Rx (each has rule + cue/example). Real thinness. **Fix:** fold an inflammation-harm-omission recognition cue into §11.2 Anti-Pattern 4 (the efficacy-claim AP) — one phrase. Keeps §11.2 at 8, §12 at 4. |
| CV-SF-01 | R4 | **LEGITIMATE-MODIFIED** | Same root as CV-COV-002 (the two highest-harm audits are PROPOSED). Role 4 correctly BLOCKs as a default-to-BLOCK safety reviewer (it cannot affirm a zero-finding pass when H1 behaviors lack mechanical enforcement). I verified the behaviors ARE encoded (§5 r1/r10, §6 step2, §7, §14, §12.1) and the audits ARE honestly PROPOSED + §18-surfaced (template F-010). **Disposition:** beads-to-create (integrator) — same as CV-COV-002. The Role-4 BLOCK is the design-doc-stage adversarial posture, not a structural design defect; the design-doc-protocol resolves it by (a) incorporating LEGITIMATE findings + (b) routing the audit-build to a bead. |
| CV-SF-02 | R4 | **LEGITIMATE-MODIFIED** | Verified the LIVE `audit-specialist-profile.sh` checks refusal-class *presence* (`--check refusal-classes`/`authority-framing`), not stimulus→class *behavioral mapping* — that mapping is gated by the PROPOSED device-screening audit. Same deferred-defense-in-depth class as CV-SF-01. **Disposition:** same bead (the device-screening-refusal audit covers the ECG-trace→`IMAGE_OR_SIGNAL_INPUT` mapping). No structural design change; the §5 r10/r11 + §6 step3 behavioral contract is present and correct. |
| CV-SF-03 | R4 | **REJECTED (premise empirically wrong; attested)** | Role 4's premise "Role 3 coverage report ABSENT" was true only at Role 4's dispatch instant — Role 3 and Role 4 ran in PARALLEL (single orchestrator batch), and Role 3 had not yet written its file when Role 4 checked. **Source-of-truth attestation:** `design/.cardiovascular-specialist-design-work/red-team-role3-coverage.md` EXISTS (Role 3 returned `coverage_verdict: BLOCK_WITH_FINDINGS`, 5 findings, all 8 classes [covered], AFB PRESENT). The entry-condition is satisfied. The composed `final_harm_class = max(Role3.nominal=H2 [CV-COV-002], Role4.worst_case_reachable=H1 [CV-SF-01/05]) = H1` is now computable and does not change any verdict. Role 4 itself flagged this as a meta-finding it did NOT block on, so no design action follows. REJECTED as a standing defect; the parallel-dispatch sequencing is the cause, not a missing artifact. |
| CV-SF-04 | R4 | **LEGITIMATE-MODIFIED** | Two parts. (a) "Role-7 deployment state open (§18 OQ-3)": Role 7 (medical-liaison) **IS deployed** — confirmed by the deployed `medical-safety-reviewer` agent.md ("medical-liaison (Role 7 DEPLOYED 2026-05-29 — the live adjudicator)") + the BOARD ("14/15 specialists deployed" incl. medical-liaison at the pinned base `94496b4`). So OQ-3's main concern is resolved. **Fix:** update §18 OQ-3 + §17.2#3 to state Role 7 is deployed (2026-05-29). (b) "degraded-mode logic sound but no LIVE audit": Role 4 explicitly affirms the degraded-mode LOGIC is sound; the missing liaison-route audit is the same deferred-defense-in-depth class as CV-SF-01 → bead. No structural logic change. |
| CV-SF-05 | R4 | **LEGITIMATE (highest-value; incorporated)** | I personally searched the design doc: NO clause covers a chemically-correct-but-contextually-unsafe substitution. §5 r8 covers *prescription* compounds; §5 r2 covers causal-vs-associational markers; neither catches an OTC/dietary substitution that is chemically equivalent but dangerous in a renal/ACEi-ARB context (KCl salt-substitute + ACEi/ARB or CKD → hyperkalemia → fatal arrhythmia). This is a REAL gap and the deployed **gi-specialist carried exactly this discipline** (gi Core Rule 10: "no sustained-use-dangerous substitution — bromism-class: a betaine-HCl/acidifier or electrolyte-substitute 'equivalent' that harms over time"), which my synthesis dropped. **Grounding for the fix (it IS groundable, not invented):** (i) the inherited bromism-class concept from Role 1's refusal taxonomy + the gi-specialist precedent; (ii) the substrate Section B antihypertensive risk-tier table DOES document ACEi/ARB hyperkalemia as a contraindication-class harm — the NEW vector is the dietary/salt-substitute substitution path + the sustained-use-dangerous-substitution refusal. **Fix:** add a sustained-use-dangerous / chemically-correct-but-contextually-unsafe-substitution clause to Core Rule 11 (folded, NOT a 13th rule), + an Ask-vs-Proceed §3 clause, + a §14 edge case (test stimulus: KCl salt-substitute ask while on an ACEi), + a §17.1 risk, + an Anti-Pattern 3 cue. **Substrate note for the integrator:** Role 4 correctly observes the gap also exists upstream in `domain-research.md` (no electrolyte/hyperkalemia-substitution primary) — flag as a **beads-to-create** item for a future substrate re-rotation; the design-doc clause ships now (grounded as above), the substrate primary is a follow-up. |
| CV-SF-06 | R4 | **LEGITIMATE-MODIFIED** | Verified §5 r8 IS already content-triggered ("A request to start/dose/titrate ... maps to `PRESCRIPTIVE_DIRECTIVE`") — Role 4 itself confirms "the content-triggered PRESCRIPTIVE_DIRECTIVE floor is a real backstop (so H2 not H1)." The ask is to make the content-trigger EXPLICIT so a future edit can't regress it into framing-dependence. **Fix:** add an explicit clause to Core Rule 8 + Ask-vs-Proceed §3: the directive gate is *content-triggered* (the dose/diagnosis request itself fires it), independent of framing — authority/educational/obscure framing neither triggers nor relaxes it (`AUTHORITY_FRAMING_BYPASS` is the framing-bypass guard; the content floor stands on its own). |

## Verdict tally
- **LEGITIMATE (incorporated as-stated):** 1 — CV-SF-05 (bromism-class clause).
- **LEGITIMATE-MODIFIED (incorporated with modification):** 7 — CV-COV-001, CV-COV-002, CV-COV-003, CV-COV-005, CV-SF-01, CV-SF-02, CV-SF-04, CV-SF-06. (Note: CV-COV-002/CV-SF-01/CV-SF-02/CV-SF-04 share the PROPOSED-audit-deferral root → 2 beads-to-create; no structural design change beyond framing.)
- **REJECTED (attested):** 1 — CV-SF-03 (Role 3 report exists; parallel-dispatch timing artifact).
- **NOT-A-DEFECT (clean verdict):** 1 — CV-COV-004 (no-compounds-write consistency confirmed).

## Design-doc changes applied at Phase 5 (incorporating LEGITIMATE/MODIFIED)
1. **CV-SF-05:** add the sustained-use-dangerous / chemically-correct-but-contextually-unsafe-substitution clause (Core Rule 11 fold + Ask-vs-Proceed §3 + §14 edge case + §17.1 risk 9 + Anti-Pattern 3 cue + §15.2 criterion).
2. **CV-SF-06:** Core Rule 8 + Ask-vs-Proceed §3 — directive gate is content-triggered, explicit.
3. **CV-COV-001:** merge §15.2 criteria 2+3 → 10 criteria.
4. **CV-COV-003:** name `BASIS_NOT_REVIEWABLE` in Anti-Pattern 7 cue.
5. **CV-COV-005:** fold inflammation-harm-omission cue into Anti-Pattern 4.
6. **CV-SF-04:** update §18 OQ-3 + §17.2#3 — Role 7 deployed 2026-05-29.
7. **CV-COV-002 / CV-SF-01 / CV-SF-02 / CV-SF-04(b):** strengthen §13 + §17.1 framing (compensating LIVE controls); route the two PROPOSED audits + the liaison-route audit to **beads-to-create** (integrator).

## Beads-to-create (integrator — builder does NOT write .beads/)
1. `scripts/audit-cardiac-emergency-floor.sh` — assert a red-flag stimulus emits the `TIME_CRITICAL` card before any triage/self-management content, floor persists across turns + resists a device-normal override (fixture). [CV-COV-002 / CV-SF-01]
2. `scripts/audit-device-screening-refusal.sh` — assert an ECG-trace / cuffless-BP / PPG stimulus maps to `IMAGE_OR_SIGNAL_INPUT` or `DEVICE_FUNCTION` + a non-clearing statement, never an interpretation (fixture). [CV-SF-02]
3. (optional) liaison-route + degraded-mode-fail-safe audit — assert a medium+/H1–H2 surface routes to the live medical-liaison and never falls back to operator-self-override. [CV-SF-04]
4. (substrate follow-up) a future `aplus-research` re-rotation of the cardiovascular substrate to add an electrolyte/hyperkalemia-substitution (KCl salt-substitute + ACEi/ARB/renal) primary — the bromism-class clause ships design-doc-grounded now; the substrate primary is the follow-up. [CV-SF-05 substrate note]
