# /review-pr Self-Review — genetics-specialist (Phase-7 design-doc-protocol)

Scope: local commits vs `origin/main` (no PR open yet) — 3 commits, 35 files / +3614, all additions (22 md + 13 json). The deliverable is documentation + a deployed agent profile (no executable code), so the document-appropriate review lenses were applied (contracts / logic / doc-quality / historical-context) by a fresh independent reviewer, NOT the source-code security/test agents. The deployed `agent.md` already cleared `audit-specialist-profile.sh` 0-BLOCK + a Phase-6 adversarial review (DEPLOY-READY); this is the closing independent pass.

## Reviewer verdict: PASS. 2 findings, **0 LEGITIMATE-to-fix** after triage (PF-S3-01 personal source-read of each).

| ID | Conf | Title | Triage | Evidence |
|---|---|---|---|---|
| REV-01 | 78 | library-index cites risk_class slug `genetic-pgx-high` that "resolves nowhere / fabricated" | **NOT_A_BUG** (fails legitimacy cond. 2 — factually wrong) | Verified: `genetic-pgx-high` IS the real `risk_class` value in the integrator's main-checkout risk table (`templates/specialist-risk-class.yaml` line 100: `risk_class: genetic-pgx-high`). The reviewer used `git show origin/main:` (committed main lacks the genetics row — same worktree-vs-integration drift as bda / INV-PROVENANCE-DISJOINT / the row itself), not the integrator's working-tree table where the slug lives. The library-index's "genetics-specialist = genetic-pgx-high, mode_floor deep, target_class reference" is accurate and idiomatic (the sibling cardiovascular likewise names its real slug `compound-medium`). Reject-but-don't-adopt: the suggested fix (drop the slug) would lose accurate info; the slug is correct. No change. |
| REV-02 | 35 | OQ-3 "genetics row present in main" claim not reproducible against committed `origin/main` | **DEFERRED** (already disclosed; integrator-side) | This is the worktree-vs-integration drift the design doc exhaustively discloses (header builder-attestation framing, §13/§16, §17.2 A5, §17.3 BC-1, §18 OQ-1/2/3). The reviewer itself notes it is "disclosed, not hidden; harness-neutral to agent behavior," and the deployed agent.md double-protects the floor via the prose-hardcoded `--mode=deep --target-class=reference` dispatch string (AC-9). `design/genetics-specialist-design.md` is Status:Final → PROTOCOL forbids inline-editing it → routed to the integrator as a rebase-merge precondition (outbox beads-to-create). No agent.md change. |

## What the reviewer independently confirmed clean (the deployed agent.md honors every spec contract)
All four safety floors as Core Rules (r1 DTC, r2 risk≠disease, r3 disease-risk-route + EMERGENCY gene-CLASS, r4 PGx, r10 privacy); the three Phase-3 red-team safety fixes (EMERGENCY gene-CLASS trigger by symbol; confirmation-status artifact-binding; out-of-scope recognize-and-refer Core Rule 12); the 8 refusal classes incl. mandatory AUTHORITY_FRAMING_BYPASS; the six-field PGx-flag spec + "NOT a dose directive"; the R8 six-field `vault/dna/` metadata contract; the deep/reference dispatch floor with no bare `deep-research`; ≥3 resolving PF ids; the intentional Context-Loading divergence from the sibling (operator binds at dispatch, not step-1 auto-load); the Anti-Patterns merge of the spec's 8 into 7 (deployed AP6 covers F3/F4/F5/F7 with all citations preserved). No internal contradictions, no boundary crossings, no broken inheritance.

## Frozen-design-doc / integrator follow-ups (posted to outbox beads-to-create; NOT fixed here per PROTOCOL DO-NOT on a Status:Final design doc)
1. **AR-003 (Phase-6, Minor):** design §5 preamble "the four substrate safety floors are rules 1–4" disagrees with §15.2 AC-2 (the privacy floor is rule 10). The **deployed agent.md is already correct** (omits the brittle numbering; all four floors present as Core Rules, each audited). Zero functional impact; spec-hygiene only.
2. **REV-02 / OQ-3 / AR-009 (rebase precondition):** integrator confirms the `genetics-specialist` row (`genetic-pgx-high` / deep / reference) + `scripts/audit-research-provenance.sh` + `INV-RESEARCH-PROVENANCE-DISJOINT` resolve in the rebased tree at merge, and re-runs bda + `--check mode-floor-correctness` against it (per design BC-1). If still absent, downgrade §13/§16 LIVE-at-integration rows to PROPOSED.
3. **dna-metadata-contract audit (OQ-4) + six-reader conformance (OQ-5):** deferred defense-in-depth beads (the R8 contract ships behavior-encoded in agent.md §5 r9).

**Net: /review-pr self-review PASS — 0 LEGITIMATE findings requiring a code/profile fix; both findings triaged with cited evidence (NOT_A_BUG + DEFERRED). The 3 integrator follow-ups are documentation/integration preconditions, carried in the outbox.**
