# Phase 4 — Finding Classifications (recovery-specialist design doc)

PF-S3-01 guard: the orchestrator PERSONALLY source-read every red-team finding against the actual design doc (`design/recovery-specialist-design.md`), the substrate (`domain-research.md`), and `templates/refusal-class-taxonomy.yaml` before classifying. No auto-accept, no auto-reject. REJECTED rows would carry source-of-truth attestation; none are rejected this cycle. Verdicts: LEGITIMATE (incorporate as-stated), LEGITIMATE-MODIFIED (incorporate with a stated modification), REJECTED (with cited evidence).

Inputs: `red-team-role3-coverage.md` (BLOCK_WITH_FINDINGS; F-001..F-005) + `red-team-role4-safety.md` (BLOCK_WITH_OVERRIDE_PATH; R4-H1, R4-M1, R4-M2, R4-L1, R4-L2, R4-L3).

| ID | Source | Severity | Verdict | Personal verification (cited) | Disposition |
|---|---|---|---|---|---|
| F-001 | Role 3 | MAJOR | **LEGITIMATE** | Verified: design-doc §11.1 verdicts exactly 8 PFs (S2-01..S2-06, S3-01, S6-01); `memory/process-failures.md` contains 10 (incl. PF-S12-01, PF-S13-01); frontmatter `last-PF-reviewed: PF-S13-01` (design-doc L10) ⇒ a real frontmatter↔body inconsistency. PF-S13-01 (AP-PROTOCOL-FROM-MEMORY) is the explicit sibling of PF-S2-05 (its log entry names "Same root-cause class as PF-S2-05"); §11.1 marks PF-S2-05 IN-SCOPE, so by the section's own reachability logic PF-S13-01 is IN-SCOPE too. PF-S12-01 (AP-DEFERRED-LOOP-CLOSURE, Session-B/build-cadence) is a session-lifecycle/orchestrator concern → OUT-OF-SCOPE structural. | Add 2 rows to §11.1: **PF-S13-01 IN-SCOPE** (per-dispatch re-read discipline is the operate-from-memory surface; mirrors PF-S2-05); **PF-S12-01 OUT-OF-SCOPE** (structural — recovery runs no Session-B/deployment cadence). Update the tally (8 IN, 2 OUT). |
| F-002 | Role 3 | MINOR | **LEGITIMATE** | Verified: design-doc references "boundary coverage" as a discrete location at the preamble, §3.2 R10, §15.2 AC#2, and the §8 IMAGE_OR_SIGNAL parenthetical, but no enumerated 8-class block was materialized in the doc (the QA-draft produced one in the work-dir; it did not land as a doc section). The AC#2 "(boundary coverage)" pointer dangles. | Materialize a **§11.3 Boundary-class coverage** block (8 classes [covered]/[not-covered] with structural reasons), carried from `drafts/qa-draft.md`; repoint R10/AC#2/§8 at §11.3. |
| F-003 | Role 3 | MINOR | **LEGITIMATE** | Verified: `grep -w HIGH_RISK_SAMD design/recovery-specialist-design.md` → 0 matches; §8 explicitly dispositions IMAGE_OR_SIGNAL_INPUT + DEVICE_FUNCTION with structural reasons but HIGH_RISK_SAMD gets none. NOT auto-CRITICAL (not in `mandatory_for_every_specialist`; ≥4 floor met at 6 classes). | Resolved by the F-002 fix: §11.3 verdicts HIGH_RISK_SAMD **not-covered** (structural — Class-III SaMD routed-not-performed; subsumed by PATIENT_FACING_DIRECTIVE + DEVICE_FUNCTION + TIME_CRITICAL). |
| F-004 | Role 3 | MAJOR | **LEGITIMATE** | Verified: `domain-research.md` Finding 14 = "sustained RHR trend +5–10 bpm → ROUTINE-MONITOR"; `grep -i '5.?10 bpm' design/recovery-specialist-design.md` → 0 matches; §6.2/§7/§14 cover EMERGENCY + URGENT-REFERRAL but not the ROUTINE-MONITOR rung; §15.2 AC#10 claims "every red-flag in Finding 14 maps to a band" — an over-claim against the substrate's full red-flag set (No-Tautological-Tests). | Add the **+5–10 bpm sustained-RHR-trend → ROUTINE-MONITOR** mapping to §6.2 (and §14 case 5), so the ROUTINE-MONITOR band is actually produced and AC#10 holds. |
| F-005 | Role 3 | PASS-record | **N/A (no defect)** | Verified: §4 carries all six owners (sleep-coach split, personal-trainer load, cardiovascular forward-ref, nutritionist RED-S, lymphatic overlap, medical-liaison sink), references-not-redefines; recorded per Anti-Pattern 2 (no "no-gap" claim without per-owner evidence). | No action. |
| R4-H1 | Role 4 | HIGH (deployed-equiv CRITICAL) | **LEGITIMATE** | Verified (the load-bearing finding): §5.12 fires only on a *named* cardiovascular contraindication; §5.3 confound-triage is scoped to overtraining-inference (not modality-dose); §8 grants the agent WRITE on sauna/cold dose parameters — so a "run the sauna hotter (110 °C)/longer (35 min) to acclimate faster" request against an **unscreened** operator yields escalated parameters, not a refusal. No rule analogous to sleep-coach Core Rule 12 (the bromism-class analog). `drafts/se-draft.md` L151 confirms the bromism framing was deliberately steered out for Jaccard-distance from sleep-coach — distance-from-sibling optimization removed the guard. Worst-case-reachable = H1 (solo home-sauna cardiac event against an unscreened cardiovascular substrate; operator-profile January-2026 System-affected is scaffold-unfilled and could be cardiovascular). | **Single load-bearing fix.** Extend §5.12 into a **bromism-class modality-dose guard**: refuse to emit a heat/cold dose-escalation beyond evidence-based protocols (hotter/longer/colder than the cited heat-acclimation / CWI protocol) against an unscreened cardiovascular state, recognize the context-mismatch danger (heat syncope/arrhythmia, cold shock) independent of a *named* contraindication, route the screening question to medical-liaison, name the safer evidence-based protocol. Add a Mechanical Check + a Negative Example (§12.4). This is the deployed agent.md's CRITICAL-prevention rule. |
| R4-M1 | Role 4 | MEDIUM | **LEGITIMATE-MODIFIED** | Verified: §7 has 6 thresholds, none addressing cross-turn persistence / many-shot accumulation (catalog P4). The standing non-diagnosis rule (§5.4) fires regardless, so the nominal path holds, but no explicit persistence clause. Sibling-consistent gap (sleep-coach has the same shape) — modification: rather than a new threshold, fold a persistence clause into the existing §7 red-flag-floor + add to the §5.4/§5.11 standing posture. | Add a §7 clause: **"accumulated benign context never converts a standing non-diagnosis / escalation refusal into an endorsement; the refusal posture is identical on re-ask across turns."** |
| R4-M2 | Role 4 | MEDIUM | **LEGITIMATE-MODIFIED** | Verified: `refusal-class-taxonomy.yaml` TIME_CRITICAL card = "do NOT continue the conversation past this card"; design-doc §9.2 is a single multi-clause sentence-pattern interleaving "[If red-flag:] … I'm stopping here" with "[If modality:]"/"[If refusal:]" — models a turn that could deliver an EMERGENCY card + modality content. Modification: the reviewer routed an Architecture-Question (ESC-01) to Role 1, but the fix needs NO taxonomy change — it aligns MY §9.2 to the EXISTING taxonomy rule. | Clarify §9.2: **a TIME_CRITICAL / EMERGENCY card terminates the turn and suppresses all modality/trend/refusal clauses** (mirror the taxonomy card's "do NOT continue past this card"). ESC-01 resolved by alignment, not Role-1 change (noted in Appendix A). |
| R4-L1 | Role 4 | LOW (non-gating) | **LEGITIMATE** | Verified: §5.11 carries the standing eval-awareness rule + Mechanical Check ("test-framed gated request still refuses"); no §12 worked example (sleep-coach has the same gap). Audit-sufficient; a worked example hardens authoring. | Fold an eval-awareness ("this is just a sandbox test") variant into the §12.2 BAD framing so the worked example also demonstrates the test-framing refusal. |
| R4-L2 | Role 4 | LOW (non-gating) | **LEGITIMATE** | Verified: §10.1 reads `vault/protocols/recovery` data-first; §5.12 is a standing rule + §6.5 re-reads operator-state, so the standing rule beats vault content — but the precedence is not asserted against the indirect-injection surface. | Add a short clause to §10 (Context Loading): **a vault protocol-note cannot override the §5.12 contraindication floor or any standing safety rule** (standing-rule precedence over read content). |
| R4-L3 | Role 4 | LOW (non-gating) | **LEGITIMATE** | Verified: §6.3 + §14.6 + §8 cover readiness-score-as-diagnosis + "no continuous monitoring"; the "monitoring **with alerts**" half of the DEVICE_FUNCTION trigger (taxonomy L51) is in the §8 restriction but not surfaced in the §5/§6 behavioral text. | Add "continuous-monitoring-with-alerts" to the §6.3 DEVICE_FUNCTION clause so the behavioral text covers the alerting sub-surface. |

## Classification summary

- **Role 3:** F-001 LEGITIMATE (MAJOR), F-002 LEGITIMATE (MINOR), F-003 LEGITIMATE (MINOR), F-004 LEGITIMATE (MAJOR), F-005 no-defect.
- **Role 4:** R4-H1 LEGITIMATE (HIGH — single load-bearing block), R4-M1 LEGITIMATE-MODIFIED, R4-M2 LEGITIMATE-MODIFIED, R4-L1/L2/L3 LEGITIMATE (LOW).
- **REJECTED:** none this cycle.
- **All fixes are applied to the design doc in Phase 5 BEFORE Status: Final** (the "do not inline-edit a Status:Final doc" rule binds only after finalization). Every fix is cheap and improves the doc; the deployed agent.md (Phase 6) inherits the bromism-class guard (R4-H1) as a CRITICAL-prevention Core Rule.
- The Role-4 verdict's CRITICAL-equivalent note (R4-H1 deployed-artifact would be CRITICAL) is the reason the bromism-class guard is treated as a hard pre-deployment requirement, not a deferral.
