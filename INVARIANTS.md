---
title: Project Invariants Register
type: reference
status: active
created: 2026-05-25
last_reviewed: 2026-05-25
review_cadence: every-5-sessions
permalink: a-plus-maxing/invariants
---

# Project Invariants Register

Rules that MUST remain true across all sessions and dispatches. Distinct from guidelines, conventions, or stylistic preferences. The defining property is binary: at any moment, the rule is either satisfied or violated, and violation requires explicit user approval BEFORE the work proceeds.

> **Mechanical enforcement principle:** Invariants get scripts. If a rule is important enough to be called an invariant, it earns a script (or schema, or hook) that audits it. Rules that AREN'T mechanically enforceable get classified differently — as guidelines, anti-patterns, or decision records — but NOT as invariants. See Rigor Framework Discipline 5.

## Change discipline

Invariants change only by this four-step ritual:
1. Cite the invariant being violated.
2. Present new evidence (PF entry, downstream consumer report, etc.).
3. Explicit user approval.
4. Append a row to the Change Log table below with date, old, new, evidence, session ID.

This prevents soft erosion via small exceptions.

---

## Register

| ID | Scope | Property | One-line statement | Mechanical Verification | Auth |
|---|---|---|---|---|---|
| INV-HO-ROTATION | HANDOFF.md | 6-clause rotation | Volatile sections contain only current-session content + at most one Historical pointer line | `scripts/handoff-audit.sh` (clauses 2 + 5 enforced; clauses 1 + 6 require prior-version diff — v2); smoke tests `scripts/tests/test_handoff_audit.sh` (12/12 pass) | S2 |
| INV-HO-NO-STALE-HASH | HANDOFF.md | no SHA prefixes in narrative | HANDOFF prose does not cite sha256/commit hash prefixes; provenance lives in artifact citations | `scripts/handoff-audit.sh` content-pattern check (lenient: section-header date OR same-line date satisfies clause 4); smoke tests `scripts/tests/test_handoff_audit.sh` (12/12 pass) | S2 |
| INV-RESEARCH-ATTESTATION | aplus-research | gate JSON attestation_chain required | Gate JSONs for phases 3.5/4.75/6/7.5/8.5 carry `attestation_chain` matching agent-source sha256 + mtime > iter_start_ts | `lib/gate_attest.py` + schema validation; smoke tests `tests/test_gate_attest.py` (9/9 pass) | S3 |
| INV-RESEARCH-POPULATION-MISMATCH | aplus-research | animal/in-vitro numerical claims tagged | Every animal/in-vitro numerical claim carries `[population-mismatch: <species>]` in same sentence or has species as subject within 100 chars | aplus-research IC-7 verifier (Phase 4.75) | S2 |
| INV-RESEARCH-CONCENTRATION-SURFACED | aplus-research | first-class concentration section | When single-cluster share ≥70%, draft has first-class concentration section before any indication subsection | aplus-research IC-9 verifier (Phase 4.75) | S2 |
| INV-RESEARCH-NO-VENDOR-NUMERICAL | aplus-research | vendor/anecdote cites never ground numerical | `vendor_label` and `anecdote_aggregate` tags never appear in same sentence as dose/effect-size/AE-rate/n claim | aplus-research IC-3 + IC-4 verifier (Phase 4.75) | S2 |
| INV-RESEARCH-IC13-CORPUS | aplus-research | per-citation corpus scoping | Deep mode requires ≥80% (min 20) of numerical/quoted claims grep-verified against retrieved source corpus | aplus-research IC-13 verifier (Phase 4.75) | S2 |
| INV-RESEARCH-CROSS-SECTION-ID | aplus-research | shared-entity reconciliation | Citations, institutions, compound identifiers, regulatory dates, and trial registrations appearing in 2+ section drafts must agree; mismatches HALT before outline refinement | Phase 4.25 ID-Reconcile gate; schema `schemas/gate-4.25.schema.json`; gate-attest tests T13-T16 (4/4 pass) | S6 |
| INV-ROLE-INLINING | Task dispatch | full 11-section role profile | Agent dispatches matching role-context (H1=`# {Role Name}` or `roles/<slug>/agent.md` ref) inline the full 11-section profile verbatim; 9th section is an operational slot accepting `## Modes` \| `## Audit Protocol` \| `## Task Routing` (hook v2.5) | `.claude/hooks/enforce-role-inlining.sh` PreToolUse hook; smoke tests `hooks/tests/test_enforce_role_inlining.sh` (11/11 pass) | S3 |
| INV-SCOPE-CONTRACT | Session lifecycle | binary AC + WILL/NOT lists | Every session has a written scope contract before any work: Goal, binary ACs, Files I WILL touch, Files I will NOT touch, NOT doing, Invariants at risk | `scripts/scope-contract-audit.sh` (validates 6 required subfields + ≥1 binary checkbox); smoke tests `scripts/tests/test_scope_contract_audit.sh` (12/12 pass) | S3 |
| INV-BRANCH-NOT-MAIN | git | no commits on main | Working commits land on `feature/*` or `fix/*` branches, never `main` | `.claude/hooks/block-push-main.sh` (push) + `.claude/hooks/block-commit-main.sh` (commit) — both PreToolUse Bash hooks; smoke tests `.claude/hooks/tests/test_block_commit_main.sh` (21/21 pass) | S2 |
| INV-PF-ATTESTATION | Session close | mandatory "No PF" attestation | Every session close either appends a PF entry OR explicitly attests "No new PF-class entries this session" with rationale | `scripts/pf-attestation-audit.sh` (validates `S<N> close (YYYY-MM-DD):` line presence); smoke tests `scripts/tests/test_pf_attestation_audit.sh` (12/12 pass) | S3 |
| INV-RESEARCH-PROVENANCE-DISJOINT | aplus-research / specialist merge | mode-required attested gates PRESENT before verify-chain | A specialist's `design/.<slug>-design-work/` research provenance must be produced by the gated `/aplus-research` path: the mode-required attested gates (per the risk table + SKILL.md gate-by-mode matrix) are PRESENT in the canonical `gates/` dir with attestation chains BEFORE `verify-chain` runs — closing the vacuous-pass hole where verify-chain `continue`s past ABSENT gates | `scripts/audit-research-provenance.sh` (bda) — presence-then-integrity; rejects non-canonical layouts; delegates chain-integrity to `gate_attest.py verify-chain`; smoke tests `scripts/tests/test_audit_research_provenance.sh` (8/8 pass) | S19 |
| INV-TRUNK-COMPLETENESS | git / branch topology | trunk holds complete roster + governance | The trunk (`main`) carries the complete deployed roster AND the governance/tooling layer; no deployed agent or governance artifact lives only on a branch the operator cannot run from | `scripts/branch-completeness-audit.sh` — asserts the checkout (HEAD) contains every deployed `agent.md` on a reference branch (default `origin/main`) + bda + the registered provenance invariant; smoke tests `scripts/tests/test_branch_completeness_audit.sh` (3/3 pass); run at session open + close (step 8.5) | S22 |
| INV-WIKI-INGESTION-GATED | wiki ingestion | gated entity pages pass the accuracy battery before commit | A `vault/{compounds,biomarkers,library}/` entity page cannot be committed unless it passes the deterministic per-page battery — research provenance (bda + `verify-chain` via a `provenance_dir`/`provenance_slug` frontmatter pointer), structural conformance, frontmatter/enum validity, index sync; the periodic whole-vault lint additionally enforces dead-namespace-link + open-contradiction integrity. Pre-gate pages are provenance-exempt only, listed in `vault/library/_ingest-grandfather.txt` | `scripts/wiki-ingest-lint.sh` (commit-time, blocking) via PreToolUse hook `.claude/hooks/block-ungated-vault-write.sh`, plus `scripts/wiki-lint.sh` (periodic, every 5 sessions); shared `scripts/lib/wiki-helpers.sh`; smoke tests `scripts/tests/test_wiki_ingest_lint.sh` (14/14), `.claude/hooks/tests/test_block_ungated_vault_write.sh` (6/6), `scripts/tests/test_wiki_lint.sh` (9/9) | S23 |
| INV-SKILL-TRACE | Session close | per-PR gated-skill invocation table | Every session close whose session ran PR lifecycles records the per-PR `/review-pr`+`/merge` invoked-fresh table in the PF log's `## Session <N>` section; NO cells carry an explicit violation marker; zero-PR sessions declare the exact escape sentence | `scripts/skill-trace-audit.sh --session <N>` (RECORD-shape audit; honest limits in the script header); smoke tests `scripts/tests/test_skill_trace_audit.sh` (21/21 pass) | S52 |
| INV-CLOSE-AUDIT | Session close | fail-closed meta-gate over the close roster | Every session close runs `scripts/close-audit.sh --session <N>` to exit 0 before the close is attested clean: it runs the negative-test FLOOR first (proving each audit can still go RED — F-007), then the per-close roster, mapping exit 0→PASS / 1→FAIL / ≥2-or-missing→FATAL (a check that could not RUN must not pass — F-008) | `scripts/close-audit.sh` (toolkit pattern, a-plus roster); negative test `scripts/tests/test_close_audit.sh` (15/15 pass); wired CLAUDE.md close step 8.5 | S65 |
| INV-HARVEST-CAPTURE | Session close / self-improvement | every session failure captured in 3 layers | Every process failure logged in a session is captured in ALL of {`memory/process-failures.md`, `harvest.jsonl`, a bead} or the close fails — the self-improvement loop (Loop A) cannot silently lose a failure | vendored `toolkit/scripts/harvest-gate.sh` + its toolkit negative test (`toolkit/tests/`); wired CLAUDE.md close step 8.6 | S65 |
| INV-CORE-CAPABILITY | Session close / core capability | the core plan-generation path stays wired | The core plan-generation path is wired end-to-end: `assemble` has a production caller (`scripts/plan/generate_plan.py`) that records the plan via `record_plan`, and the author→assemble→record_plan→rendered-plan path RUNS (the `--self-test` E2E) — so the PF-S63-02 class (the core capability unbuilt while secondary work proceeds, each component green in isolation) cannot silently recur | `scripts/core-capability-audit.sh` (structural: a production caller calls `assemble`+`record_plan`; behavioral: the `--self-test` wired-path E2E runs under `.venv`); negative test `scripts/tests/test_core_capability_audit.sh` (in the floor); wired CLAUDE.md close step 8.5 | S70 |

## Category breakdown

- **Format / Document:** INV-HO-ROTATION, INV-HO-NO-STALE-HASH
- **Process:** INV-SCOPE-CONTRACT, INV-PF-ATTESTATION, INV-SKILL-TRACE, INV-CLOSE-AUDIT, INV-HARVEST-CAPTURE, INV-CORE-CAPABILITY
- **Git / branch topology:** INV-BRANCH-NOT-MAIN, INV-TRUNK-COMPLETENESS
- **Wiki / ingestion:** INV-WIKI-INGESTION-GATED
- **Role-discipline:** INV-ROLE-INLINING
- **Research-domain (aplus-research):** INV-RESEARCH-ATTESTATION, INV-RESEARCH-POPULATION-MISMATCH, INV-RESEARCH-CONCENTRATION-SURFACED, INV-RESEARCH-NO-VENDOR-NUMERICAL, INV-RESEARCH-IC13-CORPUS, INV-RESEARCH-CROSS-SECTION-ID, INV-RESEARCH-PROVENANCE-DISJOINT

## Change Log

| Date | Invariant | Change | Evidence | Session |
|---|---|---|---|---|
| 2026-05-25 | (all initial entries) | Created register | PF-S3-01 demonstrated gap; Rigor Framework Discipline 5 adopted | S3 |
| 2026-05-25 | INV-HO-ROTATION, INV-HO-NO-STALE-HASH, INV-SCOPE-CONTRACT, INV-PF-ATTESTATION, INV-BRANCH-NOT-MAIN | Mechanical verification promoted from TODO to live scripts/hooks | Audit scripts + commit-block hook built and tested (57/57 across 4 suites); wired into close protocol step 8.5 | S5 |
| 2026-05-25 | INV-RESEARCH-CROSS-SECTION-ID | Added | v2 calibration AC1: S3/S4 BPC-157 iter-3 surfaced cross-section metadata mismatches that iter-2 missed (PMID inversion in Section A; Xue 2004 institution narrative drift in Section C). Phase 4.25 ID-Reconcile gate prevents this class upstream of integrity verifier. | S6 |
| 2026-05-28 | INV-ROLE-INLINING | Hook v2.5: 9th section is operational-slot synonym set (`## Modes` \| `## Audit Protocol` \| `## Task Routing`) rather than literal `## Modes` | E1 recurrence_count=3 across S9/S10/S11 — required structural fix per Rigor Framework Discipline 8. Survey of `~/Documents/Projects/skills_library/roles/*/agent.md` confirms the 9th section varies by role (security uses Audit Protocol, orchestrator uses Task Routing). Smoke tests extended 8→11; existing 8 still pass. | S12 |
| 2026-05-30 | INV-RESEARCH-PROVENANCE-DISJOINT | Added to register | bda (`scripts/audit-research-provenance.sh`) + tests built S18 and wired into CLAUDE.md close-8.5 + INTEGRATION-CHECKLIST 1a, but the register row was never added (S18 "register +1" attestation did not match the committed file — bead `3lb`). PF-S17-01 recurrence_count=2 is the evidence: verify-chain alone vacuously passes ABSENT gates; bda asserts presence-then-integrity. Mechanical verification live at promotion time (not TODO). Walter approved the change-discipline ritual S19. | S19 |
| 2026-06-02 | INV-RESEARCH-PROVENANCE-DISJOINT | bda keys the 7.5 (risk-floor) + 8.5 (layers) requirement on the per-dispatch `gate-2.75.json` `target.type=compound` (an actual compound ENTRY), NOT the slug's coarse risk-table `target_class=compound`; fail-closed to the old `target_class` behavior when `target.type` is unreadable. SKILL.md gate-by-mode matrix footnoted to match. Smoke tests 6→8 (added reference-landscape PASS-without-7.5/8.5 + compound-entry-still-FAIL, both non-tautological). | bead `mhg`: 3 of 7 deployed specialists (cardiovascular/dermatologist/gi-specialist) were false-blocked on 7.5/8.5 for goal-agnostic Phase-0 reference research that never produces a compound entry; gi merged+accepted with this exact gap; all 7 design-work dirs are `target.type=reference`; the bda header (lines 33-38) explicitly flagged the revisit as change-discipline. Production-path validated: the 7.5/8.5 false-block clears on all three (remaining EXIT=1 is the orthogonal gate-3.5 grandfather, ADR 2026-06-01), genetics control still EXIT 0. Walter approved at S21 contract confirmation. | S21 |
| 2026-06-02 | INV-WIKI-INGESTION-GATED | Added to register | bead `bte` (expanded from "gate provenance" to the full ingestion accuracy battery at Walter's direction) / `hfm` (the "no library page ships without passing provenance" rule was documented discipline, not mechanical — the same soft-pass shape as the gate-3.5 issue that bit batch-4) / PF-S17-01 (ungated research routing around the gates scales into the wiki). Two mechanical controls built + **29/29 smoke across 3 suites**, live at promotion (not TODO): commit-time blocking gate (`wiki-ingest-lint.sh` + the `block-ungated-vault-write.sh` PreToolUse hook) keying on a `provenance_dir`/`provenance_slug` frontmatter pointer (grandfather-aware via `vault/library/_ingest-grandfather.txt`), and the periodic whole-vault `wiki-lint.sh` (WIKI.md's 6-check Lint operation, finally scripted). Link-integrity is advisory at commit (forward-refs are legitimate mid-buildout), blocking in the periodic lint. WIKI.md Ingest/Lint/Conventions updated to match. Walter approved the change-discipline registration at the S23 contract confirmation. | S23 |
| 2026-06-02 | INV-TRUNK-COMPLETENESS | Added to register | bead `gdw` / PF-S22-01 (`AP-BRANCH-WRITE-FRAGMENTATION`): the 16 specialists lived on `main` while the governance/tooling layer lived only on the `feature` carrier for ~15 sessions (merge-base froze S5/S6) — neither branch was the complete runnable system. Mechanical verification `scripts/branch-completeness-audit.sh` built + 3/3 non-tautological smoke tests, live at promotion (not TODO); wired into close step 8.5. Resolution = single-trunk reconciliation (ADR 2026-06-02-single-trunk-reconciliation). Walter approved the change-discipline registration at the S22 contract confirmation. | S22 |
| 2026-06-12 | INV-SKILL-TRACE | Added to register | PF-S39/S40 read-before-invoke family past N=3 with the S51 merge-surface recurrence (PF-S39-01); structural fix mandated by bead `lz01`; mechanical verification live at promotion (script + 21 smoke tests in this same PR, per the S22/S23 same-merge precedent). Walter approved the change-discipline ritual at S52 (recorded in-session, incl. the scope-contract amendment for this row). | S52 |
| 2026-06-15 | INV-CLOSE-AUDIT | Added to register | PF-S63-02 (agent-advised rigor relaxation → a week of secondary work while the core deliverable stayed unbuilt). Adopted S64 (Rigor Framework v1.0.0 Wave A) as the single fail-closed close gate; `close-audit.sh` already emits this id internally, so registration aligns the register with the running code. Mechanical verification live at promotion: `scripts/close-audit.sh` + `scripts/tests/test_close_audit.sh` (15/15); the full negative-test floor 13/13 green this session. Walter approved the change-discipline ritual at S65. | S65 |
| 2026-06-15 | INV-HARVEST-CAPTURE | Added to register | PF-S63-02 — the drift class recurred partly because failures had no standing 3-layer capture forcing them into the self-improvement loop (Loop A). Adopted S64 Wave A (self-improvement loop / Loop A); `harvest-gate` makes capture fail-closed at close. Mechanical verification at promotion: vendored `toolkit/scripts/harvest-gate.sh` + its toolkit negative test; wired CLAUDE.md close 8.6. Vendored (not forked) per the adoption decision to keep the toolkit as the pull-able enforcement floor. Walter approved the change-discipline ritual at S65. | S65 |
| 2026-06-18 | INV-CORE-CAPABILITY | Added to register | PF-S63-02 — the core deliverable (a followable plan, end-to-end) stayed unbuilt across ~a week of secondary work while every component passed its own unit tests. CLAUDE.md carried the core-capability-first discipline as prose; its MECHANICAL form was deferred until the path's shape existed. S70 wired the minimal plan-generation slice (author→`assemble`→`record_plan`→dashboard) AND `scripts/core-capability-audit.sh` (the gate's checks depend on the path's shape, so they land together). Registering makes "is the core capability wired?" a checked answer at every close, not a remembered one. Mechanical verification live at promotion: `scripts/core-capability-audit.sh` + `scripts/tests/test_core_capability_audit.sh` (in the negative-test floor); added to the close-audit roster. Walter approved the change-discipline ritual at S70. | S70 |

## Audit cadence

- Session close: every invariant whose mechanical verification is automated runs.
- Every 5 sessions: full register reviewed for entries whose mechanical verification is still TODO.
- On PF entry: cross-reference whether any invariant was violated. If yes, log under "documented failure" rationale in the relevant invariant row.

## Pending mechanical-enforcement gaps (TODO)

All S4-vintage TODOs were resolved in S5. Remaining gaps for future sessions:

- INV-HO-ROTATION clauses 1 (replace-don't-accrue) + 6 (session-close diff check) require diffing HANDOFF.md against the prior-session-N snapshot. Deferred to v2 once a session-versioned snapshot system exists.
- `audit-helpers.sh` does not yet emit machine-parseable JSON (only human-readable stderr). Promote to JSON line-protocol if/when a close-protocol orchestrator wants to aggregate results programmatically.
