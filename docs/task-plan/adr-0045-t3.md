---
task-id: ADR-0045-T3
source-spec: docs/spec/adr-0041-0046-comprehensive-plan-spec.md
source-build-plan: docs/build-plan/build-plan-adr-0041-0046.md
wave: 6
assigned-agent: SE
reviewers: [Security, QA]
created: 2026-07-13
status: approved
---

# Recipe: The Daily Deterministic Pass Hosted in the Built ADR-0039 Runner (TERMINAL, disabled-by-default)

> Wave-6 task (build-plan Wave 6 = {ADR-0045-T3}, the TERMINAL wave). **Entry: Wave 5 checkpoint
> passed (MERGED to `main` @ `ee6dbfd1`).** CREATE `scripts/runner/daily_monitor.py` — the daily
> deterministic pass that HOSTS ADR-0045-T2's `tiered_executor.execute_monitoring_day` in the built
> ADR-0039 runner, **disabled by default** (mirroring `scripts/runner/`'s activation pattern); MODIFY
> `scripts/runner/schedule/activate.py` with an ADDITIVE, disabled-by-default daily cadence entry
> alongside the existing weekly entry. It is the terminal node on the monitoring loop
> (`0045-T1 → 0045-T2 → 0045-T3`) — no downstream consumer.
>
> The load-bearing property is **DETERMINISM ON A NO-EVENT DAY**: when no observed `monitoring_signal`
> crosses a material threshold, the daily pass makes **0 model calls + 0 metered de-id-IN calls**
> (AC-2, AC-5) — measured by call-counting an injected fixture `dispatch` + fixture de-id client, never
> inferred — AND the daily cadence ships **disabled by default** (0 active daily-monitor schedule
> entries before an explicit operator enable, AC-3).
>
> **The runner is where 0045-T2's deferred seams get WIRED.** The executor entry
> (`execute_monitoring_day(config, observations, raw_intake, deid_client, dispatch, store_read, root,
> *, plan_date, gate_dispatch=None, reauthor=None, adjudicator=None)`) exposes three optional seams
> that the executor's own tests fed as `None`/stubs; the runner is the TERMINAL consumer that OWNS
> supplying the REAL ones (the three Wave-6-blocking beads):
> - **0n3t (PF-S134-02, THE critical fail-closed one-way door):** the runner MUST inject a REAL
>   `gate_dispatch` (via `gate_dispatch.compose_gate_dispatch(judge_client, review_dispatch,
>   lenses=…)`) when hosting `execute_monitoring_day`, so a Tier-2 re-plan routes THROUGH the composed
>   gate (`plan_orchestrator.run_orchestrated`(loop_enabled) → `plan_driver.drive` →
>   `gate_dispatch.compose_disposition` → the fail-closed `safety_passed is True` surface gate), NOT
>   the `gate_dispatch=None` legacy non-loop path. The executor's WAVE5-01 guard fail-closes a Tier-2
>   to a Tier-4 hold when `gate_dispatch is None`, so a runner that hosts the executor WITHOUT
>   injecting the composed gate never runs the composition on a material-event day (a silent fail-safe
>   that MASKS a fail-open wiring bug). The runner's composed-gate injection is UNCONDITIONAL.
> - **u20d:** the runner supplies `orchestrate.reconcile`'s REAL per-domain candidates (read from the
>   ADR-0044 `plan_model` standing version's `domain_programs`) and CONSUMES the report's
>   `holds`/`conflict_held`/`rx_bpmh_held` to enforce cross-domain holds — NOT the executor's internal
>   `{"plan": None}` degenerate stub (which yields EMPTY holds because `reconcile` gates every hold on
>   `cand.get("plan") is not None`). **Candidate SHAPE (S4):** `reconcile` reads seams via
>   `_cross_domain_seams` from `candidate["plan"][PROGRAM_KEY][CROSS_DOMAIN_SEAMS]` (`PROGRAM_KEY =
>   "domain_program"` from `assemble`; `CROSS_DOMAIN_SEAMS = "cross_domain_seams"` from `domain_program`)
>   — NOT the bare periodized prescription. So each candidate is `{"domain": d, "plan": <plan-dict
>   carrying PROGRAM_KEY → the standing version's domain program (which carries `cross_domain_seams`)>}`;
>   a degenerate `{"plan": <prescription blocks>}` fixture (no `PROGRAM_KEY`) reads 0 seams and enforces
>   NO hold — the exact fail-open the u20d gate must forbid.
> - **evvs:** the runner binds `plan_date` to the STANDING comprehensive version's date (the
>   `VERSION_DATE` / resolved `plan_date` `plan_model.read_plan_version` returns), so the Tier-4
>   `mark_pending(domain, plan_date, root)` pointer lands on the timepoint `_standing_versions`
>   resolves by date-equality — a naive tick-date binding lands the pointer on the wrong timepoint and
>   the held version STANDS (fail-open).
>
> **PF-S130-01 (the CRUX):** every load-bearing property is a PRODUCTION-PATH test driving the REAL
> runner (not the executor in isolation) whose input DISTINGUISHES the hosted behavior AND that REDs
> when the runner's wiring is reverted — the 0n3t composed-path E2E is the canonical instance (a test
> that fires `compose_disposition` in the hosted runner on a material-event day and goes RED when the
> gate injection is removed). **PF-S131-01:** every `monitoring_signal`/`adjustment_rule` fixture is
> `monitoring_compiler.compile_config`'s REAL compiled output, stored as the `MONITORING_CONFIG` field
> of a canonical periodized `plan_model` version (never a hand-built stand-in). **PF-S132-02 +
> PF-S134-01:** every Step-2.5 mutation/revert falsifier runs in an ISOLATED git worktree (or
> reverts-in-place) under `PYTHONDONTWRITEBYTECODE=1 python -B` with `scripts/**/__pycache__` cleared
> before each run. Every fixture is SYNTHETIC — 0 live-API spend, 0 real operator PII (PUBLIC repo).

## Entry State

**Prerequisites** (ADR-0045-T3 ← ADR-0045-T2 per the spec Dependency Map `:439`; all Wave ≤ 5, all
merged to `main` @ `ee6dbfd1`):

- [ ] **Wave 5 checkpoint passed / merged** — the checkout is post-merge `main` at the durable
  fork-point: `git rev-parse HEAD` == `ee6dbfd1554b203941cc6cb9817bcba1788ad288`. **[VERIFIED]**
  (HEAD == `ee6dbfd1`; the Wave-5 executor + threshold re-base are on the trunk.)
- [ ] Predecessor **ADR-0045-T2** present (the tiered executor the daily pass hosts): `test -e
  scripts/plan/tiered_executor.py && grep -q "def execute_monitoring_day" scripts/plan/tiered_executor.py
  && echo OK`. **[VERIFIED]** (`execute_monitoring_day(config, observations, raw_intake, deid_client,
  dispatch, store_read, root, *, plan_date, gate_dispatch=None, reauthor=None, adjudicator=None)` — the
  `gate_dispatch=None` default + the WAVE5-01 fail-closed guard + the `_tier3_reconcile`
  `{"plan": None}` stub all present.)
- [ ] Predecessor **ADR-0045-T1** present (the compiled config the executor reads): `grep -q "def
  compile_config" scripts/plan/monitoring_compiler.py && grep -q "def classification_of"
  scripts/plan/monitoring_compiler.py && echo OK`. **[VERIFIED]**
- [ ] The composed-gate composition present (the 0n3t seam the runner injects): `grep -q "def
  compose_gate_dispatch" scripts/plan/gate_dispatch.py && grep -q "def compose_disposition"
  scripts/plan/gate_dispatch.py && grep -q "def run_orchestrated" scripts/plan/plan_orchestrator.py
  && echo OK`. **[VERIFIED]** (`compose_gate_dispatch(judge_client, review_dispatch, *, lenses=…)` →
  `gate_producer`; `compose_disposition(verdicts, assembled_plan)` — the fail-closed `safety_passed is
  True` surface.)
- [ ] The reconciler + the comprehensive model present (the u20d + evvs seams): `grep -q "def
  reconcile" scripts/plan/orchestrate.py && grep -q "def read_plan_version" scripts/store/plan_model.py
  && grep -q "def record_plan_version" scripts/store/plan_model.py && grep -q "def mark_pending"
  scripts/store/plan_confirm.py && echo OK`. **[VERIFIED]** (`reconcile(candidates, *,
  operator_rx_classes=…)` returns `{report, holds, conflict_held, rx_bpmh_held}`; `plan_model` carries
  the `MONITORING_CONFIG` / `DOMAIN_PROGRAMS` / `VERSION_DATE` version fields + `_standing_versions`
  date-equality resolution; `plan_confirm.mark_pending`/`decision_for`/`DECISION_PENDING` present.)
- [ ] The built ADR-0039 runner host present (the `daily_monitor` lives in it + MODIFY target): `test
  -e scripts/runner/cadence_runner.py && test -e scripts/runner/schedule/activate.py && grep -q "def
  active_entry_count" scripts/runner/schedule/activate.py && echo OK`. **[VERIFIED]** (the runner
  package + the label-parameterized `active_entry_count(label)` + the disabled-by-default posture +
  the `store_lock`/`subscription_dispatch` seams present.)
- [ ] Full test suite at the Wave-5-merged floor: `.venv/bin/python -m pytest -q 2>&1 | tail -1` —
  capture the ACTUAL entry count. The gate is **no-NEW-failure**; record the passed/skipped numbers at
  execution (env- and wave-sensitive — do NOT hardcode). Two PRE-EXISTING env-sensitive failures may
  persist (`tests/serve/test_bind.py::test_port_collision_exits_nonzero_fail_loud`,
  `tests/serve/test_server.py::test_main_reads_no_stdin` — OS-port/stdin cases, the documented `2
  failed, 2489 passed, 7 skipped` baseline, NOT this task's); a 3rd distinct failure at entry → fix
  that first, it is not this task's to introduce.
- [ ] Working tree "clean" for the task scope — **daemon caveat:** a bare `git status` is NOT empty
  (the basic-memory daemon perpetually re-churns `vault/**/*.md` frontmatter; parked `??` files
  exist). "Clean" here = (a) no STAGED changes (`git diff --cached --name-only` prints nothing); (b)
  the two Create targets ABSENT (below). Do NOT `git restore vault/` (the daemon re-churns within ~1s
  — CLAUDE.md vault hygiene).
- [ ] The two Create targets ABSENT (Create-only, RGC-4, no stale/duplicate artifact): `test -e
  scripts/runner/daily_monitor.py -o -e tests/runner/test_daily_monitor.py && { echo "PRESENT
  (stop)"; exit 1; } || echo "ABSENT (Create OK)"`. **[VERIFIED]** (both ABSENT.)
- [ ] On a feature branch cut off `main`: `feature/comprehensive-plan-wave-6` (or equivalent
  `feature/*`) — never `main`.
- [ ] **Rollback target = the DURABLE FORK-POINT SHA** `ee6dbfd1554b203941cc6cb9817bcba1788ad288` —
  the `{commit-before-task}` the Rollback Protocol reverts to AND the base the frozen-spine numstat
  probe diffs against. **Use this permanent-`main`-ancestor SHA, NOT the dynamic `git merge-base` and
  NOT an intermediate SHA (PF-S133-03** — a per-task/merge-base SHA orphans on a fresh clone
  post-squash; the durable fork-point is pre-commit-runnable and stable). It is the CURRENT `main` tip
  at entry; re-verify it is an ancestor of `main` post-merge.
- [ ] The `<always-frozen>` six byte-frozen at entry (numstat==0 vs the durable SHA): `git diff
  --numstat ee6dbfd1554b203941cc6cb9817bcba1788ad288 -- scripts/store/store.py scripts/store/keying.py
  scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/router.py`
  prints nothing. **[VERIFIED]** (empty — this task creates a new runner module + edits `activate.py`
  only; it touches none of the six, and its store writes ride the EXISTING `plan_confirm.mark_pending`
  + `plan_model.record_plan_version` sinks, which CALL `store.append`, they do not EDIT `store.py`.)

## Context Load List

**Files to read** (the 2-file manifest + the seams the runner WIRES + the test precedents to MIRROR):

- `scripts/runner/daily_monitor.py` — **the Create target** (absent at entry).
- `tests/runner/test_daily_monitor.py` — **the Create target** (absent at entry; the Wave-6 → Done
  checkpoint names this module + it is the one-pass integration host).
- `scripts/plan/tiered_executor.py` — **the executor the daily pass HOSTS**. Read
  `execute_monitoring_day(...)`'s full signature + the three optional seams (`gate_dispatch=None`,
  `reauthor=None`, `adjudicator=None`), the WAVE5-01 Tier-2 fail-closed guard (`gate_dispatch is None`
  → Tier-4 hold), the `_tier3_reconcile` `{"plan": None}` internal stub, and the observation contract
  (`observations` = `{signal_name: observed_value}`). Do NOT re-implement its routing — the runner
  CALLS it.
- `scripts/plan/gate_dispatch.py` — the composed gate the runner INJECTS (0n3t). Read
  `compose_gate_dispatch(judge_client, review_dispatch, *, lenses=DEFAULT_LENSES)` → the single-arg
  `gate_producer(assembled_plan) -> {judge, review}` raw-verdict producer, and `compose_disposition(
  verdicts, assembled_plan) -> {accept, safety_passed, revise_domains}` — the ONE composition site +
  the fail-closed `safety_passed is True` surface (the 0n3t E2E spy target).
- `scripts/plan/plan_orchestrator.py` — `run_orchestrated(raw_intake, deid_client, dispatch,
  store_read, root, *, plan_date, domains=…, gate_dispatch=…, …)`; read ONLY to confirm `loop_enabled
  = gate_dispatch is not None` (the property the 0n3t injection carries) — the executor's `_tier2_replan`
  drives it; the runner does not call it directly.
- `scripts/plan/orchestrate.py` — the u20d reconciler. Read `reconcile(candidates, *,
  operator_rx_classes=frozenset()) -> {report, holds, conflict_held, rx_bpmh_held}` + the hold gates
  (`workout_has_plan = … cand.get("plan") is not None`; the `cross_domain_seams` pass's `cand.get(
  "plan") is not None`) — the reason the `{"plan": None}` stub is degenerate and the runner must supply
  real candidates carrying a real `plan`.
- `scripts/store/plan_model.py` — the ADR-0044 model the runner READS (config + candidates + date) +
  RECORDS the Tier-1 bounded adjustment into (AC-4). Read `read_plan_version(on_date, root) ->
  {state, version, plan_date}` (applies the `_standing_versions` confirm-hold), the version fields
  `MONITORING_CONFIG` / `DOMAIN_PROGRAMS` / `VERSION_DATE`, `record_plan_version(version, root)` (the
  EXISTING record sink — no new key), and `_standing_versions`' date-equality resolution
  (`decision_for(domain, reading["timepoint"], root)`) — the evvs alignment target.
- `scripts/store/plan_confirm.py` — the Tier-4 + enforced-hold sink. Read `mark_pending(domain,
  plan_date, root)`, `decision_for(domain, plan_date, root) -> token | None`, `DECISION_PENDING` — the
  EXISTING pointer stream (`plan-confirm::`), no new key.
- `scripts/runner/cadence_runner.py` — the **host-module precedent to MIRROR**: the `run(root, *,
  <injected seams>, plan_date=None)` driver shape, the seam-injection (`dispatch_factory` /
  `deid_client`), the `store_lock.cadence_lock(root)` critical-section serialization, the disabled-by-
  default `main()`/`__main__` posture (importing arms nothing).
- `scripts/runner/schedule/activate.py` — **the MODIFY target**. Read the `RUNNER_LABEL` +
  `enable`/`disable`/`status`/`active_entry_count(label=RUNNER_LABEL)` (ALREADY label-parameterized) +
  the ADR-0039-T3 AC-8 anti-implicit-activation guard (`_scan_provisioning_for_activation`) — the
  additive daily cadence entry mirrors this shape under a NEW `DAILY_MONITOR_LABEL`.
- `tests/runner/test_cadence_runner.py` — the **call-counting fixture `dispatch` + fixture
  `deid_client` to MIRROR** (the no-event-day 0-call determinism measurement).
- `tests/runner/test_store_concurrency.py` — the **lock-busy-defer precedent to MIRROR (S1)**:
  `test_busy_tick_defers_no_corruption` (a tick that finds `store_lock.cadence_lock(root)` held DEFERS
  — 0 writes, a `{"regenerated": False, ..., "reason": "lock-busy"}` deferred receipt) + its
  `_store_digest(root)` byte-unchanged witness. (The lock-busy defer is NOT in `test_cadence_runner.py`
  — this is the correct precedent.)
- `tests/runner/test_activation_surface.py` + `tests/runner/test_no_implicit_activation.py` — the
  **`active_entry_count`/`status`/disabled-by-default + label-handling + `_scan_provisioning_for_activation`
  assertions to MIRROR** (AC-3 + the QA-4 positive control). (These, NOT `test_cadence_runner.py`, are
  where the activation surface is exercised.)
- `tests/plan/test_tiered_executor.py` — the compiled-config / observation / composed-gate spy fixture
  conventions to MIRROR (PF-S131-01 canonical compiled shape; the `compose_disposition` spy).
- `tests/plan/test_orchestrate.py` — the `reconcile` candidate + `cross_domain_seams` conflict
  fixtures to MIRROR for the u20d real-reconcile-hold test.

**Do NOT load:**
- The ADRs (`docs/adr/ADR-0045-*.md`, `docs/adr/ADR-0039-*.md` …) — the spec already distilled them;
  re-reading re-opens resolved trade-offs (TP-02 drift).
- `scripts/plan/adjust.py` (`adjust.adjust_plan`) — the bare re-author leg the Tier-2 path must NOT
  reach; the executor already routes THROUGH the composition, and the daily pass does not touch it
  (numstat check only, `<always-frozen>` member).
- The `<always-frozen>` six + the other ADR-0041..0046 task blocks as EDIT surfaces — numstat-check
  surfaces, not edits (this task creates a runner module + edits `activate.py`; it re-hosts none of the
  loop/executor/model internals — EXTEND-NOT-REBUILD, SEC-03).
- `scripts/serve/plan_loop.py` — the weekly front-door composition; the daily pass is a SEPARATE
  cadence hosting the executor, it does not edit or re-enter `plan_loop.signal` (AC-1: the weekly
  cadence is unchanged, both coexist).
- `monitoring_compiler.py` grammar internals beyond `compile_config`'s output shape +
  `classification_of` — the runner READS the compiled config from the model; it never re-runs the rule
  grammar.

## Risk Pre-Check

**BLOCKING GATE — a runner that hosts a FAIL-CLOSED safety-critical executor (SE + Security review;
0n3t is a one-way door).** Do NOT proceed to GREEN until a real (non-`assert True`), RED-capable
PRODUCTION-PATH test exists for every risk below. A placeholder test, or a test that passes with OR
without the correct wiring (tautological), FAILS this gate. **PF-S132-02 + PF-S134-01:** every
Step-2.5 mutation/revert falsifier runs in an ISOLATED git worktree (or reverts-in-place) under
`PYTHONDONTWRITEBYTECODE=1 python -B` with `scripts/**/__pycache__` cleared before each run (stale
`.pyc` → false regressions).

- **Risk (PF-S134-02 / 0n3t — THE critical fail-open: a runner that hosts `execute_monitoring_day`
  WITHOUT injecting a REAL composed gate never routes a Tier-2 re-plan through the composition; the
  executor's WAVE5-01 guard SILENTLY fail-closes the un-wired Tier-2 to a Tier-4 hold, MASKING the
  wiring bug):**
  - Gate (0n3t, load-bearing): a PRODUCTION-PATH test drives the REAL hosted runner over a compiled
    config carrying a MATERIAL in-domain event and asserts the composed path is taken END-TO-END — a
    spy on `gate_dispatch.compose_disposition` (a reliable module-attribute
    `monkeypatch.setattr(gate_dispatch, "compose_disposition", spy)`) FIRES ≥1, AND the routing result
    carries the `replan-through-composition` Tier-2 action (NOT a `hold-composed-gate-unwired` Tier-4
    hold). The runner built the real `gate_dispatch` via `compose_gate_dispatch(...)` and injected it.
    RED-capable: reverting the injection (host the executor with `gate_dispatch=None`) drives the
    Tier-2 event to the WAVE5-01 `hold-composed-gate-unwired` Tier-4 hold → `compose_disposition` fires
    0 → RED (Step-2.5 mutation #1). The runner's composed-gate injection is UNCONDITIONAL — never
    relying on the executor's `None` default.
  - **Fixture reachability (S6 — the `compose_disposition ≥1` assertion must be genuinely reachable,
    never silently downgraded to the tautological routing-only check):** `replan-through-composition`
    is emitted by the executor whenever `tier == TIER_2` and `gate_dispatch is not None` — REGARDLESS
    of whether the inner `run_orchestrated` actually reached `compose_disposition`. So the routing-action
    assertion ALONE is tautological w.r.t. "did the composition fire". For the load-bearing spy to fire
    ≥1, the Tier-2 `run_orchestrated → plan_driver.drive → compose_disposition` path must be REACHED,
    which requires the material-event fixture's `deid_client` to return a NON-sentinel de-identified
    summary — `run_orchestrated` HALTS at the `deid_in` crown-jewel boundary on the honest-no-plan
    sentinel (`{"deidentified": False, …}` → `DEID_HALTED`, driver never entered) and would fire
    `compose_disposition` 0 — AND fixture authors (the injected `dispatch`) that let
    `pipeline.run_generation` assemble a plan to gate. Pin this in the 0n3t / PF-S131-01 fixture note so
    the `compose_disposition ≥1` spy is a real reach, not a vacuous pass masked by the always-emitted
    routing token.
- **Risk (ADR-0045 Consequences-Negative-2 — the deterministic daily tier must be provably safe
  un-modelled; the no-event-day 0-call determinism is the whole safety + spend claim, AC-2/AC-5):**
  - Gate (AC-2 + AC-5): a PRODUCTION-PATH test drives the hosted runner over a REAL compiled config on
    a day where NO observed signal crosses its `materiality_threshold`, with an injected call-counting
    fixture `dispatch` + fixture `deid_client`, and asserts `dispatch.call_count == 0` AND
    `deid_client.call_count == 0` (the model-call + metered de-id-IN counts). Building the real
    `gate_dispatch` closure is 0-spend and is NOT invoked on a no-event day. RED-capable: reverting the
    no-event short-circuit (the daily pass re-enters the front door / dispatches / de-ids on a no-event
    day) drives any count > 0 → RED (Step-2.5 mutation #2).
  - Counterfactual control (proves the `== 0` is a real measurement, not a dead fixture): the SAME
    `dispatch`/`deid_client` fixture INSTANCES, run over the MATERIAL-event config (the 0n3t E2E),
    reach `call_count > 0` (Tier-2 routes through the injected composition, which consumes them).
- **Risk (ADR-0039 disabled-by-default posture / AC-3 — a daily cadence armed on install is an
  implicit-activation / unsanctioned-spend surface):**
  - Gate (AC-3): a test asserts `activate.active_entry_count(DAILY_MONITOR_LABEL) == 0` on a fresh
    install (no `enable(DAILY_MONITOR_LABEL)` called by any build/provision step) AND the ADR-0039-T3
    AC-8 anti-implicit-activation scan (`_scan_provisioning_for_activation`) stays clean for the daily
    surface. RED-capable: an implicit `enable(DAILY_MONITOR_LABEL)` at import/provision → the count
    reads ≥1 → RED (Step-2.5 mutation #3).
- **Risk (disposition #22 / AC-4 — the daily-pass Tier-1 recording must resolve STANDING; a bounded
  deterministic adjustment must NOT be human-gate-held, which would defeat the daily tier's point):**
  - Gate (AC-4, REFRAMED to the REAL pointer-based hold): a test drives a Tier-1 auto-apply (a
    certified `TIER1_AUTO_APPLY_SAFE` in-bounds rule; the executor's Tier-1 route emits an
    `auto-apply` routing and writes NOTHING to the store — the RECORDING is THIS task's job) and
    asserts the runner (a) RECORDS the bounded adjustment into the ADR-0044 model via the EXISTING
    `plan_model.record_plan_version` (it round-trips through `read_plan_version`), AND (b) does NOT
    call `plan_confirm.mark_pending` for the recorded version's covered domain(s) — so 0 pending
    pointers exist and `plan_model.read_plan_version` resolves the recorded version STANDING. The
    `_standing_versions` hold is PURELY POINTER-BASED: a version stands IFF every covered domain reads
    `None`/`DECISION_CONFIRMED` at its timepoint — there is NO magnitude evaluation on this path. The
    daily-pass Tier-1 route BYPASSES the weekly `plan_loop.regenerate` magnitude gate
    (`_is_renderable_majority`, which lives in `regenerate` and is never reached here) entirely (spec
    disposition #22); `LARGE_CHANGE_THRESHOLD` is not a live symbol at all. So AC-4 asserts
    pointer-based STANDING (round-trip + 0 pending pointer), NOT a magnitude-threshold-passed check.
    RED-capable: the runner `mark_pending`-ing the Tier-1 recording (or routing it through a
    magnitude/human gate) → a pending pointer lands → `read_plan_version` resolves it NOT-standing →
    RED (Step-2.5 mutation #4).
- **Risk (S1, HIGH — a concurrent weekly `store.append` lost-update clobbers the daily pass's
  safety-critical Tier-4 `plan-confirm::` pending pointer, so the human-gate hold silently drops,
  fail-OPEN):** `store.append` is a read-modify-write (`_read_lines` → dedupe check → `_write_atomic`
  full-file `os.replace`), so two concurrent ticks appending to the SAME item file lost-update — the
  second `os.replace` wins and drops the first's line. The daily pass WRITES the safety-critical
  `plan-confirm::` (Tier-4 `mark_pending`) + `plan-model::` (Tier-1 `record_plan_version`) streams;
  with the weekly + daily cadences BOTH armed, a weekly `plan_loop.signal` `store.append` racing the
  daily Tier-4 `mark_pending` on the same store can clobber the pending pointer → the hold vanishes.
  - Gate (S1): the daily pass's store-writing critical section runs inside
    `store_lock.cadence_lock(root)` (the ADR-0039-T4 runner-to-runner advisory lock) and DEFERS on a
    busy tick — 0 store writes, a `{"regenerated": False, "deferred": True, …, "reason": "lock-busy"}`
    deferred receipt (mirroring `cadence_runner.run`), catching up next tick. A PRODUCTION-PATH test
    (mirroring `tests/runner/test_store_concurrency.py::test_busy_tick_defers_no_corruption`) holds
    `store_lock.cadence_lock(root)`, fires ONE daily tick, and asserts the tick returns the
    lock-busy/deferred receipt WITHOUT appending to `plan-confirm::` / `plan-model::` — witnessed by a
    `_store_digest(root)` byte-unchanged check (0 new pending pointer, 0 new recorded version).
    RED-capable: dropping the lock acquisition (the daily pass writes unconditionally) → the busy tick
    writes → the digest changes / a `plan-confirm::` line appears → RED (Step-2.5 mutation #7). The
    lock releases in a `finally` on every exit (crash-safe), so a crashed daily tick frees the lock.
- **Risk (u20d — the runner relying on the executor's `{"plan": None}` reconcile stub silently
  enforces NO cross-domain hold; a declared cross-domain conflict ships un-adjudicated, fail-open):**
  - Gate (u20d): a test seeds a standing `plan_model` version whose two `domain_programs` declare a
    `cross_domain_seams` `SEAM_CONFLICT`, drives a cross-domain-seam event to Tier-3, and asserts the
    runner reads the REAL candidates from the standing version's `domain_programs`, calls
    `orchestrate.reconcile` with candidates of the SHAPE `reconcile` actually reads (S4) — `{"domain":
    d, "plan": {PROGRAM_KEY: <the domain program carrying `cross_domain_seams`>, …}}` — a spy confirms
    each candidate's `plan[PROGRAM_KEY][CROSS_DOMAIN_SEAMS]` is present and non-empty (NOT merely a
    non-`None` `plan`, and NOT a bare-prescription `{"plan": <blocks>}` that reads 0 seams), and
    ENFORCES the resulting `conflict_held` — the held domain reads `plan_confirm.decision_for ==
    DECISION_PENDING` and resolves NOT-standing. RED-capable: reverting to the executor's degenerate
    `{"plan": None}` stub — OR building a degenerate `{"plan": <bare prescription>}` candidate that
    omits `PROGRAM_KEY` — makes `_cross_domain_seams` read 0 seams → 0 real holds → the conflicting
    domain STANDS → RED (Step-2.5 mutation #5).
- **Risk (evvs — a naive `plan_date` (the tick's calendar date) lands the Tier-4 `mark_pending`
  pointer on a timepoint that is NOT the standing version's date, so `_standing_versions`'
  date-equality resolution never drops the held version → fail-open):**
  - Gate (evvs): a test seeds a standing version dated `D` (a fixed non-today date, `D` != the tick's
    naive date), drives a safety-threshold event to Tier-4, and asserts the runner bound `plan_date` to
    `D` (the standing version's `VERSION_DATE` / the resolved `plan_date` from `read_plan_version`) —
    `plan_confirm.mark_pending` was called with `plan_date == D`, so `read_plan_version` resolves the
    version NOT-standing. RED-capable: binding `plan_date` to the naive tick date → the pointer lands
    on the wrong timepoint → `_standing_versions` keeps the version → it STANDS → RED (Step-2.5
    mutation #6).
  - Gate (S3 — Tier-4 POSITIVE human-gate assertion, checkpoint parity with the Wave-6 → Done
    human-gate gate): alongside the NOT-standing assertion above, the SAME Tier-4 safety-gate event
    asserts POSITIVELY that `plan_confirm.decision_for(domain, D, root) == plan_confirm.DECISION_PENDING`
    (the pointer positively exists as `pending`, not merely absent) AND the held version does NOT
    auto-advance past the gate (0 auto-advance — `read_plan_version` returns a non-standing state for
    the held domain, never silently the held version). This mirrors the executor's own Tier-4 contract
    (`mark_pending` → `decision_for == DECISION_PENDING`, 0 auto-advance) exercised END-TO-END through
    the hosted runner, so a runner that recorded the safety event but dropped the pending-marking would
    RED on the positive `decision_for` assertion (not just the NOT-standing one).
- **Risk (PF-S130-01 — tautological hosted-path tests):** each load-bearing test drives the REAL
  runner (not the executor in isolation) and REDs on a revert of the runner's wiring. A test that
  passes with OR without the correct wiring (e.g. one that never counts calls, or asserts a routing the
  executor would produce regardless of the runner's seam) is a gate gap — the Step-2.5 battery (SEVEN
  named falsifiers) reverts each wiring decision per row and REDs that row's test.
- **Risk (PF-S131-01 — non-canonical fixtures):** every `monitoring_signal`/`adjustment_rule` fixture
  is `monitoring_compiler.compile_config`'s REAL compiled output stored as the `MONITORING_CONFIG`
  field of a canonical periodized `plan_model` version (the `{"blocks":[{date, load}]}` prescription
  shape); a u20d candidate is `{"domain": d, "plan": {PROGRAM_KEY: <the canonical DOMAIN PROGRAM
  carrying `cross_domain_seams`>, …}}` — the shape `orchestrate._cross_domain_seams` reads, NOT a bare
  `{"plan": <prescription blocks>}` (S4). A flat stand-in, a hand-built config dict, or a
  `PROGRAM_KEY`-less candidate FAILS this gate.

## TDD Steps

**Single cycle.** All six ACs + the three bead seams + the seven load-bearing falsifiers (six wiring
seams + the S1 lock defer) are properties
of ONE component — the daily deterministic pass HOSTING the executor — exercised over the SAME seeded
synthetic fixtures (a canonical periodized `plan_model` standing version carrying a REAL compiled
`MONITORING_CONFIG` + real `domain_programs`, and per-day observation sets: a no-event set, a
material-in-domain event, a cross-domain-seam conflict, a safety-gate event, and a Tier-1-in-bounds
event), so they are not split (task-planning: "all criteria test the same component"). RED writes every
AC + bead + falsifier as a specific failing test BEFORE any GREEN; the mutation-RED falsifications run
POST-GREEN (Step 2.5) — the runner wiring being reverted does not exist until GREEN. Sizing is within
the template envelope (1 source file created + 1 additive file modified + 1 test; the multiple ACs test
one component).

### Step 1: RED — Write Failing Tests

Call `/write-tests` for `scripts/runner/daily_monitor.py` (and the additive `activate.py` daily
cadence entry), creating `tests/runner/test_daily_monitor.py`, targeting each spec AC + the three bead
seams + the PF-S130-01 falsifiers (SEVEN — the six wiring seams + the S1 lock defer). Mirror the fixture
conventions in: `tests/runner/test_cadence_runner.py` (the call-counting `dispatch`/`deid_client`);
`tests/runner/test_store_concurrency.py` (the `store_lock.cadence_lock` lock-busy defer + `_store_digest`
byte-witness — S1); `tests/runner/test_activation_surface.py` + `tests/runner/test_no_implicit_activation.py`
(the `active_entry_count`/`status`/disabled-by-default + label handling + `_scan_provisioning_for_activation`
— AC-3 + QA-4); `tests/plan/test_tiered_executor.py` (the REAL compiled-config + observation shape + the
`compose_disposition` spy — PF-S131-01); and `tests/plan/test_orchestrate.py` (the `cross_domain_seams`
conflict candidates carrying `PROGRAM_KEY` — S4). ALL fixtures SYNTHETIC (0 real operator PII, 0
live-API); the injected `dispatch` + `deid_client` are fixtures whose CALL COUNTS are the no-event-day
0-call determinism measurement.

**Shared fixtures:** a canonical periodized `plan_model` version = a composite with `VERSION_DATE = D`
(a fixed non-today date), `DOMAIN_PROGRAMS` = ≥2 canonical DOMAIN PROGRAMs (each a real `{"blocks":
[{date, load}]}` prescription + `monitoring_signals` + `adjustment_rules` + `cross_domain_seams`), and
`MONITORING_CONFIG` = `monitoring_compiler.compile_config({domain: program})` over those programs —
recorded into a scratch store via `plan_model.record_plan_version` so it resolves STANDING. Distinct
per-day observation sets seed: (no-event) all observations below `materiality_threshold`;
(material-in-domain) an observation ≥ `materiality_threshold` on an in-domain certified rule — REUSED
as the AC-2/AC-5 counterfactual control (the same call-counting instances driven `> 0`); (cross-domain
conflict) an observation firing a `MUST_ESCALATE` cross-domain rule whose two programs declare a
`SEAM_CONFLICT` `cross_domain_seams`; (safety) an observation firing a `safety == SAFETY_GATE` rule;
(Tier-1-in-bounds) an in-bounds certified rule reading under materiality. The injected seams mirror
`cadence_runner`'s (a fixture `dispatch` + fixture `deid_client` + a fixture `judge_client` + fixture
`review_dispatch` for the composed gate + a scratch `root`/`store_read`).

- **AC-1 — Daily pass hosts in the runner (host + coexistence):**
  `test_daily_tick_invokes_executor_in_runner_host_weekly_unchanged` — a fixture daily tick invokes the
  daily-pass driver over the seeded standing version's compiled config in the built runner; assert the
  pass runs `execute_monitoring_day` and returns a receipt, AND the weekly cadence (`cadence_runner` /
  the `RUNNER_LABEL` schedule) is untouched (both cadences coexist — the daily entry is a NEW label,
  not a rewrite of the weekly one).
- **AC-2 + AC-5 — No-event-day determinism (load-bearing):**
  `test_no_event_day_zero_model_zero_deid` — over the no-event observation set, assert
  `dispatch.call_count == 0` AND `deid_client.call_count == 0` (0 model calls + 0 metered de-id-IN
  calls). RED-capable: Step-2.5 mutation #2.
- **0n3t — Composed-path E2E (bead / falsification #1, PF-S134-02, THE critical one):**
  `test_material_event_routes_through_composed_gate_in_hosted_runner` — over the material-in-domain
  config in the hosted runner, a spy on `gate_dispatch.compose_disposition` FIRES ≥1 and the routing
  result carries the `replan-through-composition` Tier-2 action (NOT `hold-composed-gate-unwired`); the
  runner injected the real `gate_dispatch` via `compose_gate_dispatch(...)`. Doubles as the AC-2/AC-5
  counterfactual control (the SAME `dispatch`/`deid_client` instances reach `> 0`). **Fixture
  reachability (S6):** the `compose_disposition ≥1` spy is the load-bearing assertion — the
  `replan-through-composition` routing token is emitted by the executor for ANY Tier-2 with a non-`None`
  gate, whether or not the composition actually ran, so the routing check ALONE is tautological. To make
  the spy genuinely reachable, the material-event fixture's `deid_client` MUST return a NON-sentinel
  de-identified summary (a `{"deidentified": False, …}` sentinel HALTS `run_orchestrated` at `deid_in`
  → `DEID_HALTED`, driver never entered, `compose_disposition` fires 0) AND the injected `dispatch`
  authors must let `pipeline.run_generation` assemble a plan to gate. RED-capable: Step-2.5 mutation #1
  (remove the injection → `gate_dispatch=None` → WAVE5-01 Tier-4 hold → `compose_disposition` fires 0).
- **AC-3 — Disabled by default:** `test_daily_cadence_disabled_by_default_zero_active_entries` — on a
  fresh install (no `enable`), `activate.active_entry_count(DAILY_MONITOR_LABEL) == 0` AND the
  anti-implicit-activation scan is clean for the daily surface. RED-capable: Step-2.5 mutation #3.
  **Positive control (QA-4 — the 0 is a real per-label measurement, not a silently-ignored label):** in
  the SAME test, `activate.status(DAILY_MONITOR_LABEL)` returns `{"state": "DISABLED", "active_entries":
  0, "label": DAILY_MONITOR_LABEL}` — the status echoes the DAILY label (distinct from the weekly
  `RUNNER_LABEL`, so a counter that ignored the argument and reported the weekly label's state would
  RED), and the count is a real DISABLED-but-registered read of a working per-label counter (mirroring
  `test_activation_surface.py`'s label handling). Reconciled with the S2 scope-out: there is NO
  `enable(DAILY_MONITOR_LABEL) → 1` arm (the daily enable/render is scoped out, Deviation #2) — the
  positive control asserts the disabled-but-known-label state, not an armed count.
- **AC-4 — Tier-1 bounded-adjustment recording resolves STANDING (pointer-based, REFRAMED per QA-1):**
  `test_tier1_auto_apply_records_bounded_adjustment_resolves_standing` — a Tier-1 auto-apply records the
  bounded adjustment into the ADR-0044 model (via `plan_model.record_plan_version`, it round-trips
  through `read_plan_version`) AND the runner does NOT call `plan_confirm.mark_pending` for the recorded
  version's domain(s) — so 0 pending pointers exist and `read_plan_version` resolves the recorded
  version STANDING (the `_standing_versions` hold is purely pointer-based; the daily Tier-1 route
  bypasses the weekly `regenerate` magnitude gate entirely). RED-capable: Step-2.5 mutation #4.
- **u20d — Real cross-domain reconciliation hold (bead):**
  `test_cross_domain_seam_event_yields_real_reconciliation_hold` — a cross-domain-seam event drives the
  runner to read the standing version's REAL `domain_programs`, call `orchestrate.reconcile` with
  candidates of the shape `reconcile` reads (S4) — `{"domain": d, "plan": {PROGRAM_KEY: <the domain
  program carrying `cross_domain_seams`>, …}}` — a spy confirms each candidate's
  `plan[PROGRAM_KEY][CROSS_DOMAIN_SEAMS]` is present + non-empty (NOT a bare-prescription `{"plan":
  <blocks>}` that reads 0 seams), and ENFORCE the `conflict_held` — the held domain reads
  `plan_confirm.decision_for == DECISION_PENDING` and resolves NOT-standing. RED-capable: Step-2.5
  mutation #5 (fall back to the executor's `{"plan": None}` stub — or a bare-prescription candidate that
  omits `PROGRAM_KEY` — reads 0 seams).
- **evvs + S3 — plan_date bound to the standing version's date + Tier-4 POSITIVE human-gate (bead):**
  `test_plan_date_binds_to_standing_version_date_not_naive` — with a standing version dated `D` (`D` !=
  the tick's naive date), a safety-threshold event's Tier-4 hold calls `plan_confirm.mark_pending` with
  `plan_date == D` (the standing version's `VERSION_DATE`), so `read_plan_version` resolves the version
  NOT-standing. **S3 positive assertion (same test or a dedicated `test_tier4_safety_event_holds_pending`):**
  POSITIVELY `plan_confirm.decision_for(domain, D, root) == plan_confirm.DECISION_PENDING` (the pending
  pointer exists, not merely the version's absence) AND 0 auto-advance past the gate — checkpoint parity
  with the Wave-6 → Done human-gate gate. RED-capable: Step-2.5 mutation #6 (bind `plan_date` to the
  naive tick date → the pointer misaligns → the version STANDS + `decision_for(domain, D)` reads `None`).
- **S1 — Lock-busy defer (store-write serialization, HIGH):**
  `test_daily_tick_defers_when_cadence_lock_held` — mirroring
  `tests/runner/test_store_concurrency.py::test_busy_tick_defers_no_corruption`: hold
  `store_lock.cadence_lock(root)` in the test, fire ONE daily tick over a MATERIAL/safety fixture (a
  tick that WOULD write `plan-confirm::`/`plan-model::` if it ran), and assert the tick returns the
  `{"regenerated": False, "deferred": True, …, "reason": "lock-busy"}` deferred receipt WITHOUT
  appending — witnessed by a `_store_digest(root)` byte-unchanged check (0 new `plan-confirm::` pending
  pointer, 0 new `plan-model::` version). RED-capable: Step-2.5 mutation #7 (drop the lock acquisition →
  the busy tick writes → the digest changes → RED).
- **AC-6 — Module green + 0 spend / 0 PII:** `.venv/bin/python -m pytest
  tests/runner/test_daily_monitor.py` passes against fixtures; an import grep confirms 0 live-client
  import and no real operator-identity literal in the test tree.

Run: `.venv/bin/python -m pytest tests/runner/test_daily_monitor.py -q`
Expected: FAIL — `daily_monitor.py` does not exist / the daily cadence entry is absent. (If a hosted-
path test PASSES at this stage it is TAUTOLOGICAL or wrong — investigate before GREEN.)

### Step 1 (continued): RED-capability falsification pre-check (NON-SKIPPABLE)

The runner wiring being reverted does not exist until GREEN, so the per-seam mutation-RED proofs run
POST-GREEN (Step 2.5). At Step 1, confirm ONLY that each named test FAILS for the right reason (the
module/cadence-entry is absent — an `ImportError`/`AttributeError`, not a passing tautology).

### Step 2: GREEN — Implement

Changes to make (behavioral descriptions, NOT literal code):

- `scripts/runner/daily_monitor.py` (**Create**) — a NEW runner-host module (mirroring
  `cadence_runner`'s shape: a public `run(root, *, <injected seams>, plan_date=None)` driver + a
  disabled-by-default `main()`/`__main__` entry that arms nothing on import) that performs the daily
  deterministic pass:
  1. **Read the standing comprehensive version** from the ADR-0044 model
     (`plan_model.read_plan_version`) — the source of the compiled config, the real per-domain
     candidates, and the version date. (satisfies AC-1's "reads the compiled config from the
     comprehensive model".)
  2. **Bind `plan_date` to the standing version's date** (`VERSION_DATE` / the resolved `plan_date`),
     NOT the tick's naive calendar date — so the Tier-4 `mark_pending` pointer aligns with
     `_standing_versions`' date-equality resolution. (satisfies **evvs**.)
  3. **Build the REAL composed gate** via `gate_dispatch.compose_gate_dispatch(judge_client,
     review_dispatch, lenses=…)` and inject it as `gate_dispatch` into `execute_monitoring_day` — the
     injection is UNCONDITIONAL (never the executor's `None` default), so a Tier-2 re-plan routes
     THROUGH the composition (`run_orchestrated`(loop_enabled) → `drive` → `compose_disposition` → the
     fail-closed `safety_passed is True` surface). (satisfies **0n3t** / PF-S134-02.)
  4. **Serialize the store-writing section under `store_lock.cadence_lock(root)` (S1).** Steps 4-6 (the
     executor host + the Tier-4/enforced `mark_pending` holds + the Tier-1 `record_plan_version`) are
     the store-writing critical section — enter it INSIDE `store_lock.cadence_lock(root)` (the ADR-0039-T4
     runner-to-runner advisory lock, the SAME lock the weekly `cadence_runner` uses), and on a BUSY
     acquire (a concurrent weekly/daily tick holds it) DEFER: write nothing and return a
     `{"regenerated": False, "deferred": True, …, "reason": "lock-busy"}` receipt (mirroring
     `cadence_runner.run`), catching up next tick — so a concurrent weekly `store.append` can never
     lost-update-clobber the daily Tier-4 pending pointer. The lock releases in a `finally` on every
     exit (crash-safe). **Host `execute_monitoring_day`** over the standing version's `MONITORING_CONFIG`
     + the day's observations + the injected seams (`raw_intake`/`deid_client`/`dispatch`/`store_read`/
     `root`/`plan_date`/`gate_dispatch`/`reauthor`/`adjudicator`), collecting the per-event tier-routing
     result. On a no-event day the executor makes 0 model + 0 de-id-IN calls (the daily pass adds no
     model/de-id call of its own — reading the model + building the gate closure are 0-spend).
     (satisfies AC-2 + AC-5 + **S1**.)
  5. **For each Tier-1 auto-apply routing**, RECORD the bounded adjustment into the ADR-0044 model via
     the EXISTING `plan_model.record_plan_version` (a bounded, single-in-domain change) and do NOT
     `plan_confirm.mark_pending` it — so the recorded version carries 0 pending pointer and
     `read_plan_version` resolves it STANDING (`_standing_versions` is purely pointer-based; the daily
     Tier-1 route does not touch — and is never gated by — the weekly `regenerate` magnitude check,
     spec disposition #22). This is a store write through the EXISTING record sink — NO new store
     key/stream. (satisfies AC-4 / disposition #22.)
  6. **For each Tier-3 cross-domain-seam routing**, read the standing version's REAL `domain_programs`
     for the involved domains (+ cross-targets), build candidates of the SHAPE `reconcile` reads (S4) —
     `{"domain": d, "plan": {PROGRAM_KEY: <the standing domain program, which carries
     `cross_domain_seams`>, …}}`, NOT a bare-prescription `{"plan": <blocks>}` (which reads 0 seams) and
     NOT the executor's internal `{"plan": None}` stub — call `orchestrate.reconcile`, and ENFORCE the
     report's `holds`/`conflict_held`/`rx_bpmh_held` by `plan_confirm.mark_pending`-ing each held
     domain. (satisfies **u20d**.)
  7. Return a receipt (the routings + what was recorded + what was held) for the runner's log. The
     daily pass's ONLY store writes are the EXISTING `plan_confirm.mark_pending` (Tier-4 + enforced
     holds) + `plan_model.record_plan_version` (Tier-1 recording) sinks — it defines NO new store
     key/stream and calls no other `store.append`/`store.correct`.
  8. Ship **disabled by default**: importing arms nothing (0 tick / model / de-id on import — every
     effect lives inside a function; the `__main__` guard keeps `main()` off the import path). No
     build/provision/`init_instance` step calls the daily enable. (satisfies AC-3's posture.)
- `scripts/runner/schedule/activate.py` (**Modify — additive, LABEL-REGISTRATION ONLY; the daily
  enable/render is SCOPED OUT, S2 option (b)**) — add ONLY a `DAILY_MONITOR_LABEL` (a distinct label,
  e.g. `com.aplusmaxing.daily-monitor`) and read the daily cadence's disabled-by-default state through
  the ALREADY-label-parameterized `active_entry_count(DAILY_MONITOR_LABEL)` / `status(DAILY_MONITOR_LABEL)`
  (both already take a `label` argument — no new enable/render code is needed for AC-3). **Do NOT add a
  daily `enable`/`disable`/render/install path.** `enable(label)` renders the WEEKLY
  `cadence-runner.plist.template` (whose `ProgramArguments` = `scripts.runner.cadence_runner`, the
  model-SPENDING weekly runner) and the weekly-hardcoded crontab line (`0 3 * * 1 … cadence_runner`),
  so an `enable(DAILY_MONITOR_LABEL)` would arm the WEEKLY spending runner under a daily label — a
  mis-target. The CORRECT daily-interval-targeting enable (a NEW daily template → `scripts.runner.daily_monitor`)
  is deferred to the operator-gated LIVE-wiring follow-up (Deviation #2 + a bead-worthy note). The
  change is PURELY ADDITIVE: the weekly `RUNNER_LABEL` entry + its enable/disable/status/count are
  untouched (AC-1 coexistence); no build/provision step calls any daily enable (the ADR-0039-T3 AC-8
  anti-implicit-activation guard is preserved). A fresh install reads
  `active_entry_count(DAILY_MONITOR_LABEL) == 0`. (satisfies AC-3; see Deviation #2.)

Run: `.venv/bin/python -m pytest tests/runner/test_daily_monitor.py -q`
Expected: PASS.

### Step 2.5: Falsify the load-bearing wiring (POST-GREEN mutation-RED — isolated worktree, PF-S132-02 + PF-S134-01)

The wiring the falsifiers target now EXISTS. For EACH row: apply the mutation IN AN ISOLATED git
worktree (skill `using-git-worktrees` — never mutate-and-restore the shared checkout) under
`PYTHONDONTWRITEBYTECODE=1 python -B` with `scripts/**/__pycache__` cleared first, run ONLY that row's
named test, RECORD the observed RED (the failing assertion + the pytest one-line summary), then DISCARD
the worktree and confirm GREEN again before the next row. A test green under its mutation is
tautological and FAILS its property.

| # | Mutation | Target test (property) | Observed RED (record the failing assertion + summary) |
|---|----------|------------------------|-------------------------------------------------------|
| 1 | host the executor with `gate_dispatch=None` (drop the composed-gate injection) | `test_material_event_routes_through_composed_gate_in_hosted_runner` (0n3t / PF-S134-02) | _record_ |
| 2 | re-enter the front door / dispatch / de-id on a no-event day | `test_no_event_day_zero_model_zero_deid` (AC-2 + AC-5) | _record_ |
| 3 | implicitly `enable(DAILY_MONITOR_LABEL)` at import/provision | `test_daily_cadence_disabled_by_default_zero_active_entries` (AC-3) | _record_ |
| 4 | `mark_pending` the Tier-1 recording (or route it through a magnitude/human gate) so a pending pointer lands | `test_tier1_auto_apply_records_bounded_adjustment_resolves_standing` (AC-4) | _record_ |
| 5 | reconcile with the executor's `{"plan": None}` stub (or a bare-prescription candidate omitting `PROGRAM_KEY`) instead of the standing version's real `PROGRAM_KEY`-carrying candidates | `test_cross_domain_seam_event_yields_real_reconciliation_hold` (u20d) | _record_ |
| 6 | bind `plan_date` to the tick's naive calendar date instead of the standing version's date | `test_plan_date_binds_to_standing_version_date_not_naive` (evvs) | _record_ |
| 7 | drop the `store_lock.cadence_lock(root)` acquisition (the daily pass writes unconditionally on a busy tick) | `test_daily_tick_defers_when_cadence_lock_held` (S1 lock defer) | _record_ |

Run after discarding each worktree mutation: `.venv/bin/python -m pytest tests/runner/test_daily_monitor.py -q`
Expected: PASS (every mutation discarded; the suite green again). Confirm NO mutation edit remains in
the working tree before Step 3.

### Step 3: REFACTOR

Review for:
- The daily pass CALLS the executor + the model/reconcile/confirm seams — no re-implementation of the
  executor's routing, the compiler's grammar, the driver's revise loop, or the reconciler's holds
  (EXTEND-NOT-REBUILD, SEC-03). No second copy of the tier vocabulary.
- The composed-gate injection is a SINGLE unconditional wiring point (`compose_gate_dispatch(...)` →
  `execute_monitoring_day(gate_dispatch=…)`), not a per-call-site patchwork; the `plan_date` binding is
  read ONCE from the standing version.
- NO new store key/stream; NO `store.append`/`store.correct` except the EXISTING
  `plan_confirm.mark_pending` (holds) + `plan_model.record_plan_version` (Tier-1 recording); the
  `<always-frozen>` six are untouched; the `activate.py` change is additive (the weekly entry
  byte-unchanged in intent — only new daily symbols added). Remove any Step-1/Step-2.5 temporary
  stub/mutation residue.

Run: `.venv/bin/python -m pytest tests/runner/test_daily_monitor.py tests/runner/test_cadence_runner.py tests/plan/test_tiered_executor.py -q`
Expected: PASS (the new daily pass + the weekly runner + the executor suites green — the daily pass is a
pure additive host).

### Step 4: REGRESSION

Run: `.venv/bin/python -m pytest -q`
Expected: PASS at the entry floor — the SAME pre-existing env-sensitive failures (the documented `2
failed, 2489 passed, 7 skipped` baseline), the entry skipped count, PLUS the new
`test_daily_monitor.py` tests passing, with NO new failure. **The gate is no-NEW-failure.** A 3rd
unexpected failure means the pass reached outside its manifest — do NOT fix unrelated tests;
investigate whether the change reached outside the manifest.

Then re-check the frozen spine + scope (using the DURABLE FORK-POINT SHA, PF-S133-03):
- `git diff --numstat ee6dbfd1554b203941cc6cb9817bcba1788ad288 -- scripts/store/store.py
  scripts/store/keying.py scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/plan/adjust.py
  scripts/plan/router.py` → **EMPTY** (the `<always-frozen>` six — the daily pass's store writes ride
  the EXISTING `mark_pending`/`record_plan_version` sinks, which CALL `store.append`, they do not EDIT
  `store.py`).
- `git diff --name-only ee6dbfd1554b203941cc6cb9817bcba1788ad288 -- scripts/ tests/` → exactly
  `scripts/runner/daily_monitor.py`, `scripts/runner/schedule/activate.py`,
  `tests/runner/test_daily_monitor.py` (no scope creep — no `plan_loop.py`/executor/model edit; no
  `vault/` daemon-churn file staged).

## Interface Contracts

This task CREATES one new runner-host module (the TERMINAL consumer — no downstream) + modifies one
existing file additively; it changes no existing signature.

- **`daily_monitor` public driver entry (NEW — the terminal host):** a public `run(root, *, <injected
  seams>, plan_date=None)` driver mirroring `cadence_runner.run`'s seam-injection shape. It reads the
  standing comprehensive version from `plan_model`, binds `plan_date` to that version's date (evvs),
  builds + injects the real composed `gate_dispatch` (0n3t), and — INSIDE `store_lock.cadence_lock(root)`
  (S1, the ADR-0039-T4 runner-to-runner advisory lock) — hosts `execute_monitoring_day`, records Tier-1
  auto-applies (AC-4), and enforces real cross-domain reconciliation holds (u20d), returning a receipt.
  **On a BUSY lock (a concurrent weekly/daily tick holds it) the driver DEFERS: 0 store writes, a
  `{"regenerated": False, "deferred": True, …, "reason": "lock-busy"}` receipt (mirroring
  `cadence_runner.run`), catching up next tick** — so a concurrent weekly `store.append` can never
  lost-update-clobber the daily Tier-4 pending pointer. **Assumption (SE Ask-vs-Proceed rule 3 — same
  public intent, simpler internal shape):** the day's `observations` (`{signal_name: observed_value}`,
  the executor's contract) reach the driver through an injected seam (a fixture set in tests; the
  runner's store-derived readings in production); the exact seam container + the receipt shape are
  internal choices, stated here and pinned by the fixtures. **No downstream consumer** — this is the
  terminal monitoring-loop node (spec `:439`).
- **The composed-gate injection contract (the runner OWNS wiring the real gate):** the runner builds
  `gate_dispatch = compose_gate_dispatch(judge_client, review_dispatch, lenses=…)` and passes it to
  `execute_monitoring_day(..., gate_dispatch=<real>)` UNCONDITIONALLY — the executor's `gate_dispatch`
  default (`None`) is a test convenience, NOT a production path; the runner never relies on it (the
  WAVE5-01 guard would silently fail-close a Tier-2 to a Tier-4 hold). This is the PF-S134-02 property.
- **The additive daily cadence registration (`activate.py`, LABEL-ONLY per S2):** a new
  `DAILY_MONITOR_LABEL` plus the disabled-by-default state read through the ALREADY-label-parameterized
  `active_entry_count(DAILY_MONITOR_LABEL)` / `status(DAILY_MONITOR_LABEL)`. **NO daily
  `enable`/`disable`/render/install path is added** (the existing `enable(label)` renders the WEEKLY
  `cadence-runner.plist.template` → `scripts.runner.cadence_runner`, so a daily-labeled enable would arm
  the weekly spending runner — the correct daily-interval enable is deferred to the operator-gated
  LIVE-wiring follow-up, Deviation #2). DISABLED BY DEFAULT (no build/provision/`init_instance` step
  arms it; the ADR-0039-T3 AC-8 anti-implicit-activation guard is preserved). The weekly `RUNNER_LABEL`
  cadence is untouched (AC-1 coexistence).
- **Consumed frozen/existing seams (change control — this task MUST NOT change them):**
  `tiered_executor.execute_monitoring_day` (0045-T2, Security-review-gated fail-closed one-way door);
  `gate_dispatch.{compose_gate_dispatch, compose_disposition}` + `plan_orchestrator.run_orchestrated`
  (ADR-0028 no-fork composition); `orchestrate.reconcile` (0043-T2); `plan_model.{read_plan_version,
  record_plan_version, MONITORING_CONFIG, DOMAIN_PROGRAMS, VERSION_DATE, _standing_versions}`
  (0044-T1/T2/T4); `plan_confirm.{mark_pending, decision_for, DECISION_PENDING}` (ADR-0040);
  `store_lock.cadence_lock` (0039-T4, the runner-to-runner advisory lock — S1). This task
  CALLS these seams; it changes none of their signatures. **0045-T3 MUST NOT change the executor's
  tier-routing semantics or the executor entry** (the fail-closed one-way door) — it only HOSTS it.

## Verification Checklist

- [ ] AC-1: daily pass hosts in the runner — a fixture daily tick invokes the daily-pass driver over
  the seeded standing version's compiled config in the built runner + the weekly cadence is untouched
  (both coexist) — `test_daily_tick_invokes_executor_in_runner_host_weekly_unchanged`.
- [ ] AC-2: no-event-day 0-model-call — `dispatch.call_count == 0` over the no-event set —
  `test_no_event_day_zero_model_zero_deid`; Step-2.5 mutation #2 REDs.
- [ ] AC-3: disabled by default — `active_entry_count(DAILY_MONITOR_LABEL) == 0` on a fresh install +
  the anti-implicit-activation scan clean for the daily surface + **the QA-4 positive control**
  (`status(DAILY_MONITOR_LABEL)` echoes the DAILY label with `state == "DISABLED"`, `active_entries ==
  0` — a real per-label read, not an ignored label; no `enable→1` arm, S2 scope-out) —
  `test_daily_cadence_disabled_by_default_zero_active_entries`; Step-2.5 mutation #3 REDs.
- [ ] AC-4 (REFRAMED, QA-1): Tier-1 recording resolves STANDING — a Tier-1 auto-apply records the
  bounded adjustment into the ADR-0044 model via `record_plan_version` (round-trips) AND the runner does
  NOT `mark_pending` it (0 pending pointer), so `read_plan_version` resolves it STANDING (pointer-based;
  the daily Tier-1 route bypasses the weekly `regenerate` magnitude gate — `_is_renderable_majority` is
  never reached, `LARGE_CHANGE_THRESHOLD` is not a live symbol) —
  `test_tier1_auto_apply_records_bounded_adjustment_resolves_standing`; Step-2.5 mutation #4 REDs.
- [ ] AC-5: 0 metered de-id-IN calls on a no-event day — `deid_client.call_count == 0` over the
  no-event set — `test_no_event_day_zero_model_zero_deid` (paired with AC-2); counterfactual control:
  the SAME instances reach `> 0` on the material-event day (the 0n3t E2E).
- [ ] AC-6: `.venv/bin/python -m pytest tests/runner/test_daily_monitor.py` passes (0 live-API, 0 real
  PII — import grep clean).
- [ ] **0n3t (bead / PF-S134-02, load-bearing):** a material-event day in the HOSTED runner fires
  `gate_dispatch.compose_disposition` ≥1 + the routing is `replan-through-composition` (NOT
  `hold-composed-gate-unwired`) — the runner injected the real composed gate; **the fixture `deid_client`
  returns a NON-sentinel summary + authors that let `run_generation` assemble, so the spy is genuinely
  reached, not the always-emitted routing token (S6)** —
  `test_material_event_routes_through_composed_gate_in_hosted_runner`; Step-2.5 mutation #1 (drop the
  injection) REDs.
- [ ] **u20d (bead):** a cross-domain-seam event yields a REAL reconciliation hold — the runner
  reconciles the standing version's real `domain_programs` as candidates of the shape `reconcile` reads
  (`{"domain": d, "plan": {PROGRAM_KEY: <domain program with `cross_domain_seams`>}}`, S4 — a spy
  confirms `plan[PROGRAM_KEY][CROSS_DOMAIN_SEAMS]` non-empty) + enforces `conflict_held` (`decision_for
  == DECISION_PENDING`, NOT-standing) — `test_cross_domain_seam_event_yields_real_reconciliation_hold`;
  Step-2.5 mutation #5 (the `{"plan": None}` stub / a bare-prescription candidate) REDs.
- [ ] **evvs (bead) + S3 Tier-4 positive:** `plan_date` binds to the standing version's date (not a
  naive value) — the Tier-4 `mark_pending` pointer lands on the version's timepoint, so
  `read_plan_version` resolves NOT-standing — AND POSITIVELY `decision_for(domain, D, root) ==
  DECISION_PENDING` + 0 auto-advance (S3, checkpoint parity) —
  `test_plan_date_binds_to_standing_version_date_not_naive`; Step-2.5 mutation #6 (naive date) REDs.
- [ ] **S1 (lock defer, HIGH):** a daily tick that finds `store_lock.cadence_lock(root)` held DEFERS —
  returns the `{"regenerated": False, "deferred": True, …, "reason": "lock-busy"}` receipt WITHOUT
  appending to `plan-confirm::`/`plan-model::` (a `_store_digest` byte-unchanged witness) —
  `test_daily_tick_defers_when_cadence_lock_held`; Step-2.5 mutation #7 (drop the lock) REDs.
- [ ] PF-S130-01: every load-bearing test is a hosted-path test driving the REAL runner that REDs on
  its wiring revert (the 7-row Step-2.5 battery — no tautological pass).
- [ ] PF-S131-01: every `monitoring_signal`/`adjustment_rule` fixture is `compile_config`'s REAL
  compiled output stored as the `MONITORING_CONFIG` field of a canonical periodized `plan_model`
  version; u20d candidates are `{"domain": d, "plan": {PROGRAM_KEY: <canonical DOMAIN PROGRAM with
  `cross_domain_seams`>}}` (the shape `_cross_domain_seams` reads, S4), never a bare-prescription
  candidate.
- [ ] PF-S132-02 + PF-S134-01: every Step-2.5 mutation ran in an isolated worktree under
  `PYTHONDONTWRITEBYTECODE=1 python -B` with `scripts/**/__pycache__` cleared — never a
  mutate-and-restore of the shared checkout.
- [ ] Frozen spine (PF-S133-03 durable SHA): `git diff --numstat
  ee6dbfd1554b203941cc6cb9817bcba1788ad288 -- scripts/store/store.py scripts/store/keying.py
  scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/router.py`
  EMPTY — the daily pass's store writes ride the EXISTING sinks (`mark_pending`/`record_plan_version`
  CALL `store.append`, they do not EDIT it).
- [ ] **Store-adversarial (assessment):** the daily pass's ONLY store writes are the EXISTING
  `plan_confirm.mark_pending` (Tier-4 + enforced holds, `plan-confirm::` stream) +
  `plan_model.record_plan_version` (Tier-1 bounded-adjustment recording, `plan-model::` stream) — BOTH
  ride keying's unchanged `(item, timepoint, source)` identity and are ALREADY battery-tested at
  ADR-0040/0044-T1/T2. It defines NO new store key/stream and calls no other `store.append`/
  `store.correct` (grep-assert 0 new keying + 0 direct `store.append`). **No new store-adversarial
  surface opens — the checklist battery is NOT a new BLOCKING Tier-1 gate for this task** (the
  suite-green gate re-runs the existing 0044-T1/T2 batteries; see Deviation #4).
- [ ] Scope discipline: only `scripts/runner/daily_monitor.py` (created) +
  `scripts/runner/schedule/activate.py` (additive) + `tests/runner/test_daily_monitor.py` (created);
  no executor/model/`plan_loop.py` edit; the weekly cadence untouched.
- [ ] No regression: `.venv/bin/python -m pytest -q` holds the entry floor (no 3rd distinct failure)
  with the new tests passing.
- [ ] Interface contracts documented (the daily driver entry + the composed-gate injection contract +
  the additive cadence registration + the terminal no-downstream note).
- [ ] **TERMINAL whole-spec falsification battery (Wave 6 → Done, build-plan Checkpoint Protocol §
  Wave 6 → Done; QA-3 — option (c), NO invented consolidated/"spine integration" module — that host
  does NOT exist):** the terminal battery = re-running the UNION of the 15 existing per-ADR guarantor
  test modules (each already carrying its slice — the checkpoint protocol enumerates them per wave in
  its "N test modules green" + Artifacts-Present lists) PLUS the numstat probe. **Concrete command:** a
  single `.venv/bin/python -m pytest -q` over the union — e.g. `tests/plan/test_domain_program.py`
  (0041), `tests/plan/test_generate_plan_uniform.py` + `tests/plan/test_assemble.py` (0041-T2),
  `tests/serve/test_orchestrator_synthesize.py` (0042/0043-T1 no-collapse), `tests/serve/test_orchestrator_reconcile.py`
  (0043-T2 always-on floors), `tests/plan/test_activation.py` (0043-T3/0046-T1 roster+activation),
  `tests/store/test_plan_model.py` + `tests/store/test_plan_model_reader.py` (0044-T1/T2 + store-adversarial
  batteries), `tests/plan/test_track.py` (0044-T3), `tests/serve/test_plan_loop_hold.py` +
  `tests/plan/test_confirm_pointer_skip.py` (0044-T4 confirm-hold), `tests/plan/test_monitoring_compiler.py`
  (0045-T1), `tests/plan/test_tiered_executor.py` (0045-T2), `tests/serve/test_large_change_fraction.py`
  (0046-T2), and `tests/runner/test_daily_monitor.py` (0045-T3, THIS task's slice) — followed by the
  **numstat probe** `git diff --numstat origin/main -- <always-frozen>` == EMPTY (+ each superseded
  surface changed ONLY in its owning ADR's commit). NO separate invented consolidated-spine e2e module
  is authored — the union of the existing per-ADR modules + the numstat IS the terminal battery. It
  asserts in one pass: **crown-jewel non-egress**
  (0 identity tokens + 0 raw genotypes across every specialist payload, `genetic-trait-classes`
  present; 0042-T1 AC-4/5); **DOMAIN PROGRAM 7-field conformance** (0041-T1 + 0043-T3 grown roster +
  0046-T1 activation/58z0); **no-collapse** (assembled record reaches the brief, not a band; 0042-T1 +
  0043-T1); **one integrated plan + milestones round-trips + resolves standing** (0043-T3 + 0044-T1);
  **always-on safety floors** (0043-T2 AC-3); **daily pass 0-model-call on a no-event day + human-gate
  hold + fail-closed-by-direction** (0045-T2/T3, THIS task's AC-2 + the executor's Tier-4/fail-closed);
  **store-adversarial batteries re-green** (0044-T1/T2); **the UNION per-ADR frozen-spine numstat** —
  `git diff --numstat origin/main -- <always-frozen>` == EMPTY over the whole build AND each superseded
  surface changed ONLY in its owning ADR's commit; and the full suite GREEN with NO new failure vs `2
  failed, 2489 passed, 7 skipped` — all at 0 live spend, 0 real PII. (The six operator-present LIVE
  runs remain the downstream operator-gated attestations AFTER this build, ADR-0039-gated — out of plan
  scope.)

## Deviations

| # | Spec Says | Recipe Says | Reason | Impact |
|---|-----------|-------------|--------|--------|
| 1 | ADR-0045-T3 AC-1/AC-2: "reads the compiled config … runs the tiered executor's Tier-1 + escalation classification". The day's OBSERVATIONS input the pass classifies is unspecified. | The daily driver reads the compiled config + the real candidates + the version date from the standing `plan_model` version, and the day's `observations` (`{signal_name: value}`, the executor's contract) reach it through an injected seam (a fixture set in tests; the runner's store-derived readings in production). The seam container + receipt shape are internal choices, pinned by the fixtures. | SE Ask-vs-Proceed rule 3 (same public intent — a hosted daily pass over a compiled config on a day's observations — pick the simpler internal shape, state the assumption); no AC tests a competing observation-source contract. | No downstream contract change (terminal node); the executor's observation contract is honored verbatim. |
| 2 | ADR-0045-T3 file manifest: `activate.py` (Modify) "registers the daily deterministic-pass cadence (disabled by default) alongside the existing weekly cadence entry"; AC-3 "0 active daily-monitor schedule entries pre-enable". | **S2 option (b): the daily enable/render/install is SCOPED OUT.** The additive `activate.py` edit adds ONLY `DAILY_MONITOR_LABEL` + reads its disabled-by-default state via the ALREADY-label-parameterized `active_entry_count(DAILY_MONITOR_LABEL)` / `status(DAILY_MONITOR_LABEL)` (all AC-3 needs); it adds NO daily `enable`/`disable`/render/install. The existing `enable(label)` renders the WEEKLY `cadence-runner.plist.template` (`ProgramArguments = scripts.runner.cadence_runner`) + the weekly-hardcoded crontab line (`0 3 * * 1 … cadence_runner`), so an `enable(DAILY_MONITOR_LABEL)` would arm the model-SPENDING WEEKLY runner under a daily label — a mis-target. The CORRECT daily enable (a NEW daily template + interval → `scripts.runner.daily_monitor`) is deferred to the operator-gated LIVE-wiring follow-up. | AC-3's disabled-by-default posture is the load-bearing property and is fully satisfiable label-only (the count reads real state per-label; the daily label reads 0 with no install). Adding a daily-labeled enable that renders the weekly template would be actively WRONG (it arms the wrong, spending runner) — shipping the label-only registration is the correct minimal edit; the real daily interval is exercised only at the operator-gated LIVE enablement, explicitly out of build scope. | **NON-BLOCKING + deferred to bead `a-plus-maxing-glzi`** ("0045-T3 S2: daily-cadence enable path targeting daily_monitor (operator-gated LIVE-wiring)", P2). The LIVE-wiring follow-up: a daily-interval template (`StartCalendarInterval` per-day / a daily cron RRULE) whose `ProgramArguments` target `scripts.runner.daily_monitor`, plus the daily `enable`/`disable` path keyed to it. The build stays in-manifest (label registration only). |
| 3 | Build-plan Agent Assignment Matrix (0045-T3): reviewer = `QA` ("Implementation: hosts 0045-T2's executor's Tier-1 in the built runner"). | Reviewers = `[Security, QA]` — a Security lens is added for the 0n3t composed-gate injection (PF-S134-02): the runner wires the FAIL-CLOSED executor + owns the UNCONDITIONAL real-gate injection, a fail-closed-safety one-way door (a missing injection silently fail-closes a Tier-2 to a Tier-4 hold, masking a fail-open). | The build plan classifies T3 as pure implementation, but the three deferred beads (0n3t especially) make the runner the safety-wiring site — mirroring the Wave-5 Security\* addition on 0045-T2 (the fail-closed executor). Flagged for orchestrator adjudication. | Adds a Tier-2 Security review lens over the 0n3t/u20d/evvs wiring; no scope change. |
| 4 | CLAUDE.md store-surface mandate: "any task writing to or reading from `scripts/store/` MUST satisfy `docs/checklists/store-adversarial-tests.md`". | The daily pass WRITES the store only via the EXISTING `plan_confirm.mark_pending` + `plan_model.record_plan_version` sinks (no new key/stream); the store-adversarial battery is NOT re-scoped as a new BLOCKING Tier-1 gate here — a grep asserts 0 new keying + 0 direct `store.append`, and the suite-green gate re-runs the already-passing 0044-T1/T2 batteries. | The battery mandate targets NEW store read/write SURFACES (new keys/streams); this task opens none — it delegates to the frozen record/pointer spine already battery-tested at ADR-0040/0044-T1/T2 (the same treatment 0045-T2 applied to its `mark_pending`-only write). | No new store-adversarial surface; the checklist is satisfied by the no-new-key grep + the existing batteries re-green (part of the terminal battery). |

## Commit

```
feat(runner): daily deterministic monitoring pass
```
Stage only: `scripts/runner/daily_monitor.py`, `scripts/runner/schedule/activate.py`,
`tests/runner/test_daily_monitor.py` (this task CREATES the daily-pass module + its test and modifies
`activate.py` ADDITIVELY — NO frozen-guard carve, NO executor/model/`plan_loop.py` edit. The daily
pass's store writes are RUNTIME CALLS to the existing `mark_pending`/`record_plan_version` sinks, not
file edits — no `plan_confirm.py`/`plan_model.py`/`store.py` edit).

(Subject is 46 chars — within the ≤50 convention. Stage with `git add <the three paths>` as its OWN
command, then `git commit` separately — the PII commit hook denies single-call stage+commit, `git
commit -a`, and pathspec `git commit <path>`. Confirm NO Step-2.5 worktree mutation leaked into the
checkout, the `<always-frozen>` six are byte-unchanged vs
`ee6dbfd1554b203941cc6cb9817bcba1788ad288`, and NO `vault/` daemon-churn / parked `??` file is staged,
before committing.)

## Rollback

If verification fails after 3 fix cycles:
1. `git stash -m "failed-ADR-0045-T3-attempt-{N}"` — preserve the work for diagnosis (also stashes any
   un-discarded Step-2.5 mutation, though those should live in discarded worktrees — PF-S132-02).
2. `git checkout ee6dbfd1554b203941cc6cb9817bcba1788ad288` — revert to the entry-state HEAD (the
   durable fork-point recorded in Entry State; NOT a dynamic merge-base — PF-S133-03). Because this
   task is Create-plus-additive, the revert removes the new module + test and restores `activate.py`.
3. Update the issue tracker: flag ADR-0045-T3 as BLOCKED with the failure log.
4. Escalate: "Task ADR-0045-T3 failed verification after 3 attempts. Stash ref: {ref}. Failure log:
   {summary}." ADR-0045-T3 is the TERMINAL Wave-6 node AND the site that wires the FAIL-CLOSED
   executor's 0n3t composed-gate injection (a one-way door) — a BLOCK halts the Wave 6 → Done boundary;
   surface it immediately, do not declare the build complete.
5. Stash cleanup: after abandonment or re-planning, drop all `failed-ADR-0045-T3` entries from `git
   stash list`.

Do NOT: silently skip a failing acceptance criterion; weaken a test to make it pass (ESPECIALLY the
PF-S130-01 per-seam revert-REDs — the 0n3t composed-path E2E, the no-event-day 0-model/0-de-id
determinism, the u20d real-reconcile-hold, the evvs plan_date-binding + Tier-4 pending-marking, and the
S1 lock-busy defer are the fail-closed-safety guards); host the executor WITHOUT injecting the real
composed gate (the 0n3t fail-open), fall back to the `{"plan": None}` reconcile stub (or a
bare-prescription candidate omitting `PROGRAM_KEY`), bind `plan_date` to a naive value, or drop the
`store_lock.cadence_lock(root)` serialization (a concurrent-clobber fail-open); define a NEW store
key/stream or call any `store.append`/`store.correct` other than the existing `mark_pending` /
`record_plan_version` sinks; add a daily `enable`/render path that renders the WEEKLY template (S2 —
scoped out); edit any frozen `<always-frozen>` file, the executor, or `plan_loop.py`;
or declare the build COMPLETE with the daily pass in a broken state.
