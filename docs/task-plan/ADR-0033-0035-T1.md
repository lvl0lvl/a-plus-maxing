---
task-id: ADR-0033-0035-T1
source-spec: docs/spec/adr-0033-0035-intake-onboarding-spec.md
source-build-plan: docs/build-plan/build-plan-adr-0033-0035-intake.md
wave: 1
assigned-agent: SE
reviewers: [QA, Security, plan-integrity]
created: 2026-06-30
status: approved
depends-on: []
---

# Recipe: Comprehensive Intake Field→Destination Capture Contract — PIN the Four Default-Fed Tokens to Real Signal (the OQ-1 select-value→deriver-bucket table), the Sensitive Fields + Raw Meds Record-Only, Editable Re-Capture No-Bypass; Mock/Fixture-Tested, 0 Live Spend

A Wave-1 entry task (one of two parallel primitives — T1 ∥ T4, disjoint manifests). It establishes the COMPREHENSIVE 9-step-wizard intake field→destination contract in `scripts/serve/capture.py`: every collected field routes to exactly one of a de-identified token (via the EXISTING `_CHAT_RAW_SOURCE_FIELDS` raw-source→deriver path so the four default-fed tokens get real signal) / a record-only operator-record value (raw meds + race/occupation/sleep/stress/smoker/alcohol) / a directly-wired token. T1 touches ONLY `scripts/serve/capture.py` (Modify) + `tests/serve/test_capture.py` (Modify).

**Load-bearing grounding finding (the SE flags this up-front, then builds to it).** The capture ROUTING MACHINERY already exists and is correct for the T1 contract — ADR-0019-T1 wired the four rich-domain fields (`_CHAT_RAW_SOURCE_FIELDS` [capture.py:94-99] → `router._RAW_TO_FIELD` [router.py:116-124] → the four `_ALWAYS_SET_DERIVED` derivers [router.py:434-552, 578-582]), `rx-interaction-classes` is deliberately ABSENT from `WIRED_TOKENS` so its form field routes record-only [capture.py:65-72], and the default `else` branch [capture.py:291-295] already routes any non-wired field (smoker/alcohol/race/occupation/sleep/stress) record-only. So **T1 is a comprehensive-contract-PINNING + sensitive-field-COVERAGE task, not a routing-code task.** Its `capture.py` Modify is a behaviorally-inert module-docstring update making the full 9-step field→destination contract explicit; the substantive deliverable is the new test contract (the OQ-1 select-value→bucket table, the sensitive-field record-only + no-cannabis coverage, the editable re-capture no-bypass proof). Because the contract pre-exists, each new test pins EXISTING behavior — so the TDD RED is the per-test FAILING-CAPABLE MUTATION-PROOF (the project's pervasive convention — every existing `test_capture.py` test carries a "Failing-capable: …" note), not a literal pre-implementation failure. This is recorded in the Risk Pre-Check + the Report; it is NOT a spec defect (the task is buildable as specified), and the SE adds NO unasked-for routing/validation code (Core Rule 4 + NFR-5 select-vocab-discretionary).

T1 edits NO python production routing logic and NO router.py / store / frozen-engine file: the four derivers, `_RAW_TO_FIELD`, `EXCLUDED_RAW_PII`, `SUMMARY_FIELD_SET`, and `dispatch` are byte-unchanged (NFR-2 EXTEND-NOT-REBUILD); `bodyweight-band` stays a live `WIRED_TOKEN` (its retirement is T2's, W2 — T1 must NOT touch it). The only `capture.py` write surface is the UNCHANGED `store.append` (no new store item, no new write path — the store-adversarial battery is satisfied by construction; see Verification Checklist).

## Entry State

**Prerequisites:**
- [ ] On feature branch `feature/intake-build` (the ADR-0033/0034/0035 build branch; carries the ADR/spec/build-plan docs commits on top of `main` — those touch no code [VERIFIED `git branch --show-current` = `feature/intake-build`]). The fork-point `<wave-base>` is `git merge-base HEAD origin/main` = `12f769f` ([VERIFIED 2026-06-30]); `git diff --numstat 12f769f -- scripts/plan/router.py scripts/serve/capture.py` returns EMPTY (both byte-clean from the base) [VERIFIED].
- [ ] Working tree clean of code changes: `git status` shows only the basic-memory daemon's `vault/**` frontmatter churn (cosmetic daemon noise — leave it uncommitted, never `git add vault/` blindly; CLAUDE.md § Vault hygiene). None of the churn touches T1's manifest (`scripts/serve/capture.py`, `tests/serve/test_capture.py`).
- [ ] Full test suite green at the behavior-preservation baseline: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest -q` with loopback port 8765 FREE. With the operator's intake server holding 8765, EXACTLY the two named env tests fail (`tests/serve/test_bind.py::test_port_collision_exits_nonzero_fail_loud` + `tests/serve/test_server.py::test_main_reads_no_stdin`) — the documented environmental delta, NOT a regression. Free it first: `! lsof -nP -iTCP:8765 -sTCP:LISTEN >/dev/null 2>&1 && echo "8765 FREE"`.
- [ ] `scripts/serve/capture.py` importable + the routing anchors present: `.venv/bin/python -c 'from scripts.serve import capture; assert capture._CHAT_RAW_SOURCE_FIELDS and "rx-interaction-classes" not in capture.WIRED_TOKENS and set(capture.WIRED_TOKENS) <= set(__import__("scripts.plan.router", fromlist=["SUMMARY_FIELD_SET"]).SUMMARY_FIELD_SET)'` → OK ([VERIFIED]; the load-time tripwire [capture.py:193-196] holds; `bodyweight-band` IS in `WIRED_TOKENS` [capture.py:62] — T1 leaves it).
- [ ] `scripts/plan/router.py` derivers + maps present in the grounded shape: `_dietary_pattern_class`/`_supplement_stack_class`/`_peptide_use_class`/`_training_volume_band` [router.py:434-552], `_RAW_TO_FIELD` mapping the four raw sources → the four derived tokens [router.py:116-124], `EXCLUDED_RAW_PII` carrying the four raw sources [router.py:61, 92-95], `_NOT_DISCUSSED = "not-discussed"` [router.py:321], the four tokens in `_ALWAYS_SET_DERIVED` [router.py:578-582] AND in `SUMMARY_FIELD_SET` [router.py:46-49], `summarize(store_read, identity_config=…)` [router.py:630], `dispatch(summary, …)` [router.py:742] [all VERIFIED]. Spot-check the OQ-1 bucket behavior the AC2 table pins: `.venv/bin/python -c 'from scripts.plan.router import _dietary_pattern_class as d,_training_volume_band as t,_supplement_stack_class as s,_peptide_use_class as p; R=lambda v:[{"value":v}]; print(d(R("vegan")),t(R("5 days/week")),s(R("none")),p(R("BPC-157")))'` → `plant-based high none peptide-in-use` ([VERIFIED]). NOTE the OQ-1 crux (Interface Contracts): a value `"5 or more days"` derives `moderate`, NOT `high` — `_training_volume_band`'s regex needs the digit ADJACENT to the unit; the select's submitted VALUE must carry a deriver-matching keyword (`"5 days/week"`/`"5x"`/`"high volume"`).
- [ ] No cannabis field exists anywhere in the capture surface: `! grep -rni cannabis scripts/serve/capture.py tests/serve/test_capture.py && echo "NO CANNABIS (good)"` ([VERIFIED] — AC4's no-cannabis is a NEGATIVE assertion + a forward-constraint on T4, not a removal).
- [ ] `docs/checklists/store-adversarial-tests.md` present (bead `pka`) [VERIFIED] — the store-surface reference (capture.py is a `store.append` WRITER; see Verification Checklist for why T1 adds no novel store-write surface).
- [ ] `plan-integrity` has grounded the plan (Wave-1 entry criterion): every Wave-1 task's inputs confirmed against the LIVE tree (`stat`/`grep`, not plan prose) — INCLUDING the pre-existing-routing finding above (T1's `capture.py` Modify is contract-documentation + tests, the routing machinery pre-dates this task); the dependency map confirmed an acyclic DAG with artifact-named edges (T1 is an entry point, depends-on []; T1→{T2,T3,T6,T8} are the outbound edges); each wave's checkpoint confirmed runnable; the prior-wave checkpoint attested green (N/A — Wave 1 is the entry point, no prior wave).

This entry state doubles as the rollback target — the entry-state commit is the branch HEAD this task is built on top of.

## Context Load List

**Files to read:**
- `scripts/serve/capture.py` — the module docstring [:1-30] (the contract-doc target T1 extends to the full 9-step roster); `WIRED_TOKENS` [:50-64] (note `bodyweight-band` [:62] stays — NOT T1's to touch; `training-age-band` deliberately absent); the `rx-interaction-classes`-record-only rationale [:65-72]; `_TRAIN_AROUND_FIELD`/`_RAW_SYMPTOM_ITEM` [:77-78]; `_DOB_FIELD` [:84]; `_CHAT_RAW_SOURCE_FIELDS` [:94-99] (the four rich-domain field→raw-source keys); `_BOUNDED_ENUMS`/`EQUIPMENT_ACCESS_CLASSES`/`SEX_OPTIONS`/`BODYWEIGHT_BANDS` [:117-126]; `_bounded_value_ok` [:166-188]; the load-time tripwire [:193-196]; `_reading`/`_write_scaffold` [:204-227]; the `persist_capture` routing branch [:254-295] (CRITICAL: the default `else` record-only path [:291-295] + the `_CHAT_RAW_SOURCE_FIELDS` arm [:282-290]); the receipt return `{"store":…,"scaffold":…}` [:300]. MATCH the seam's read-only-routing posture — route by data class, never re-implement the store write.
- `scripts/plan/router.py` — the four derivers `_dietary_pattern_class` [:434-463] / `_supplement_stack_class` [:466-487] / `_peptide_use_class` [:490-510] / `_training_volume_band` [:513-552] (READ to pin the EXACT OQ-1 bucket keywords the AC2 table asserts — precedence: diet checks restricted/allergy FIRST [:454-456]); `_RAW_TO_FIELD` [:116-124]; `EXCLUDED_RAW_PII` [:61, 92-95]; `_NOT_DISCUSSED` [:321]; `_FIELD_DERIVATION` [:560-568]; `_ALWAYS_SET_DERIVED` [:578-582]; `summarize` [:630]; `dispatch` [:742]. READ to pin the contract + to call; T1 makes 0 router.py edits.
- `tests/serve/test_capture.py` — the `_ABSENT_IDENTITY` fixture [:36], `_bound`/`_summary` helpers [:39-46], `_WIRED_VALUES` [:58-64]; the EXISTING coverage T1 EXTENDS-not-duplicates: `test_chat_field_writes_raw_source_summarize_derives_band` + `_CHAT_CAPTURE_CASES` [:819-869] (the AC1 rich-domain→derived-band round-trip, all four fields), `test_rx_field_drug_name_lands_in_scaffold_never_a_field_set_item` [:234-271] + `test_rx_form_field_never_writes_the_model_bound_store_item` [:132-147] (the AC3 raw-meds record-only), `test_record_only_field_lands_in_scaffold_not_in_any_field_set_item` [:535-568] (the AC4 record-only-placement shape to MIRROR), `test_chat_fields_are_not_wired_tokens` [:903-913]. The new cases MATCH this fixture-driven + failing-capable shape (tmp `store_root`/`scaffold_root`, `_ABSENT_IDENTITY`, the assert-NOT-in-the-wrong-place mandate).

**Do NOT load:** the ADRs (`docs/adr/ADR-003{3,4,5}-*.md`) + the dispositions (`docs/spec/.pipeline/adr-0033-0035/`) — the spec/build-plan distilled them; `vault/design/templates/app_view.html` + `app_shell.py` (T4's surface — T1 makes 0 markup edits; the select OPTION VALUES are T4's, built to T1's pinned OQ-1 contract — a format coupling, not a code dep); every `scripts/plan/{orchestrate,pipeline,assemble,generate_plan,adjudicate,adjust,track}.py` + `scripts/store/*.py` body — byte-frozen, not read-to-modify (NFR-2); `scripts/serve/care_review.py`/`referral.py` (Create targets of T8/T3, absent); any other `vault/**` content file — daemon-churned.

## Risk Pre-Check

This task carries the spec's ADR-0033-0035-T1 Risk Mitigations field + the build plan's Wave-1 hard constraints. Before proceeding to GREEN, verify a test exists (or the cycle adds one) for EACH; a placeholder test (`assert True`) fails this gate. Each gate references a live test/command, not a subjective assessment. Because the routing pre-exists, the FAILING-CAPABLE proof for each new test is the NAMED MUTATION (apply → see RED → revert), per the project convention — NOT a literal pre-implementation failure.

- **PRE-EXISTING-ROUTING / NO-UNASKED-CODE (the load-bearing honesty gate, Core Rule 4 + NFR-5).** T1's routing is already built (ADR-0014/0018/0019). The ONLY `capture.py` production change is the module-docstring contract update (behaviorally inert). Gate: `git diff <wave-base>..HEAD -- scripts/serve/capture.py` shows ONLY docstring/comment hunks (no change to `persist_capture`'s branch logic, `WIRED_TOKENS`, `_CHAT_RAW_SOURCE_FIELDS`, `_BOUNDED_ENUMS`, or any routing predicate). FAILING-CAPABLE-of-the-INVARIANT: if the SE adds a routing/validation branch the ACs do not require, this gate (a human diff-hunk read + `grep`) reds — HALT, confine to docstring + tests. (Adding code to manufacture a "real RED" is the rejected over-engineering path.)
- **CROWN-JEWEL raw meds record-only — 0 `rx-interaction-classes` writes from `persist_capture` (NFR-1(b), AC3 / the serve-auto-classification probe).** A submitted medication field must produce 0 `rx-interaction-classes` store writes (the rejected ADR-0034 Alternative A — serve-layer auto-classification). Gate: a seeded raw drug name → `store.read("rx-interaction-classes", root=store_root) == []` after `persist_capture`; the raw name lands in the gitignored scaffold receipt, in NO `SUMMARY_FIELD_SET` store item. FAILING-CAPABLE: re-adding `rx-interaction-classes` to `capture.WIRED_TOKENS` reds the negative assertion (the existing `test_rx_field_drug_name_lands_in_scaffold…` [:246-247] documents exactly this mutation). T1's AC3 coverage EXTENDS the existing probe to the comprehensive-roster capture (a full 9-step submission writes 0 band/class token directly).
- **CROWN-JEWEL sensitive fields record-only — race/occupation/sleep/stress/smoker/alcohol never a `SUMMARY_FIELD_SET` item (NFR-1 sensitive-data surface, AC4).** Each routes to the gitignored scaffold receipt + writes 0 store tokens; NO cannabis field is captured. Gate: each sensitive field name appears in `persist_capture(...)["scaffold"]` and in NO field-set `store.read(token)`; a submitted `"cannabis"` field is asserted ABSENT from any special routing (it falls to the default record-only `else`, but the contract asserts no cannabis-specific handling exists). FAILING-CAPABLE: wiring any sensitive field into `WIRED_TOKENS`/`_CHAT_RAW_SOURCE_FIELDS` reds the 0-store-token assertion.
- **OQ-1 SELECT-VALUE→DERIVER-BUCKET — the four default-fed tokens carry real signal (AC1 + AC2 / ADR-0034 OQ-1).** Each bounded select's submitted VALUE must land on a router-deriver keyword bucket, NOT the no-signal default. Gate: a table-driven test asserts a representative VALUE per token → its EXPECTED bucket (`"vegan"`→`plant-based`, `"5 days/week"`→`high`, `"none"`→`none`, `"BPC-157"`→`peptide-in-use`) through `persist_capture`→`summarize`, AND a fresh store → the `not-discussed` no-signal default (the "real signal" contrast). FAILING-CAPABLE: removing the `nutrition-detail` key from `_CHAT_RAW_SOURCE_FIELDS` (so it falls to record-only) reds the non-default assertion; a deriver-keyword regression reds the specific bucket row. The VALUE→bucket map is the PINNED T1↔T4 contract (Interface Contracts) — the select OPTION VALUE carries the keyword, NOT the display text.
- **EDITABLE RE-CAPTURE no-bypass — re-submit re-runs the SAME path (AC5 / ADR-0034 Consequence-Positive#5).** An edited rich-domain field re-submitted through `persist_capture` re-routes through its raw-source item (the same derivation), with 0 new write surfaces and NO path that writes a band token directly. Gate: re-capture an edited `nutrition-detail` value → the raw source carries BOTH readings (append-only), `summarize` re-derives from `readings[-1]` (the latest), and `store.read("dietary-pattern-class")` stays `[]` (never written directly). FAILING-CAPABLE: a hypothetical direct-band-write path would land a `dietary-pattern-class` store item, reddening the 0-direct-write assertion. (The single `/upload`→`persist_capture` boundary is the structural backstop — the editable My-Info edit re-uses it, T7; T1 proves the capture leg.)
- **EXTEND-NOT-REBUILD — capture.py + test only, router/store/engine byte-frozen (NFR-2).** Gate (a SHELL gate, run in REGRESSION; `<wave-base>` is a `git merge-base`): `git diff --numstat $(git merge-base HEAD origin/main) -- scripts/plan/orchestrate.py scripts/plan/pipeline.py scripts/plan/assemble.py scripts/plan/generate_plan.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/track.py scripts/plan/router.py` → 0 rows; `git diff --numstat $(git merge-base HEAD origin/main) -- 'scripts/store/*.py'` → empty; `git diff --name-only $(git merge-base HEAD origin/main)..HEAD` lists ONLY `scripts/serve/capture.py` + `tests/serve/test_capture.py`. No new `WIRED_TOKENS` member, no new `_CHAT_RAW_SOURCE_FIELDS` key, no new store item.
- **0 LIVE SPEND — fixture/mock-tested (NFR-3).** Gate: every test runs against fixture form-field dicts + tmp `store_root`/`scaffold_root` with `identity_config=_ABSENT_IDENTITY`; no `key_source.resolve()`-gated call, no `converse`/curation call, no network, no real key. `summarize` runs locally with the absent identity config (the fresh-clone posture).

## TDD Steps

Two TDD cycles over `tests/serve/test_capture.py`. Cycle 1 pins the four default-fed tokens to real signal — AC1 (rich-domain→non-default, the real-signal contrast) + AC2 (the OQ-1 select-value→bucket table) — and lands the `capture.py` contract-doc. Cycle 2 pins the crown-jewel record-only contract — AC3 (raw meds record-only, comprehensive) + AC4 (sensitive record-only + no-cannabis) + AC5 (editable re-capture no-bypass) — and runs the EXTEND-NOT-REBUILD + full-suite regression (AC6). All cases are fixture-driven in tmp roots; 0 live spend, 0 network.

**Every Python command is prefixed `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES`** (the macOS fork-safety setting the suite runs under) and uses `.venv/bin/python -m pytest`; port 8765 must be FREE for the full-suite gate.

**RED convention for this task:** because the routing PRE-EXISTS, each new test pins existing behavior — so each RED step (a) writes the test, (b) runs it (it PASSES against the pre-existing-correct routing), and (c) PROVES the test is failing-capable by applying the NAMED MUTATION, observing RED, then reverting. The mutation-proof IS the RED. Do NOT add unasked-for production code to force a literal pre-implementation RED (the rejected over-engineering path; Risk Pre-Check gate 1).

### Cycle 1: Steps 1-4 — the four default-fed tokens carry real signal (AC1 + AC2 OQ-1 table) + the capture.py contract-doc

#### Step 1: RED — Write the pinning tests + prove failing-capable
Edit `tests/serve/test_capture.py`: add an `## ADR-0033-0035-T1` section with two tests, fixture-driven over tmp roots with `_ABSENT_IDENTITY` (mirroring `test_chat_field_writes_raw_source_summarize_derives_band` [:838-869]).

- **Test `test_rich_domain_field_carries_real_signal_vs_not_discussed_default` (AC1).** For each of the four rich-domain fields, capture a bucket-keyword value into a tmp store; assert `summarize(...)[token]` is NON-DEFAULT (`!= "not-discussed"` AND `!= the deriver's no-signal default` — `"general-diet"`/`"moderate"`), AND assert a FRESH store (no capture) yields the `"not-discussed"` sentinel for that token (the real-signal contrast the existing test omits). Pin the contrast pairs: `nutrition-detail`/`dietary-pattern-class`, `supplement-stack`/`supplement-stack-class`, `peptide-stack`/`peptide-use-class`, `training-detail`/`training-volume-band`. Failing-capable mutation: drop a key from `_CHAT_RAW_SOURCE_FIELDS` → that field routes record-only → the token stays `not-discussed` → the non-default assertion reds.
- **Test `test_oq1_select_value_resolves_to_expected_deriver_bucket` (AC2, table-driven).** A `@pytest.mark.parametrize` table of `(field, value, expected_bucket)` rows pinning ONE representative VALUE per token to its EXPECTED bucket through `persist_capture`→`summarize`: `("nutrition-detail","vegan","plant-based")`, `("nutrition-detail","keto","restricted")`, `("training-detail","5 days/week","high")`, `("training-detail","2x","low")`, `("supplement-stack","none","none")`, `("supplement-stack","creatine, whey","multi-supplement")`, `("peptide-stack","none","none")`, `("peptide-stack","BPC-157","peptide-in-use")`. Assert `summarize(...)[token] == expected_bucket` and the value is NOT the no-signal default for that row. (These VALUES carry the deriver keywords — the OQ-1 pinned contract; the `"5 days/week"`→`high` row encodes the crux that the display "5 or more days" must submit a digit-adjacent-unit VALUE.) Failing-capable mutation: a deriver-keyword change (or a `_RAW_TO_FIELD` repoint) reds the specific row.

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/serve/test_capture.py -q`
Expected: the two new tests PASS against the pre-existing-correct routing (the contract pre-exists). Then PROVE failing-capable: temporarily delete the `"nutrition-detail"` key from `capture._CHAT_RAW_SOURCE_FIELDS`, re-run → the AC1 `nutrition-detail` row + the AC2 `vegan`/`keto` rows go RED (the field now records-only, token stays `not-discussed`); REVERT the deletion → green. Record the mutation-proof in the SE self-check.

#### Step 2: GREEN — Land the contract-doc (no routing change)
Changes to make (describe; the SE writes the markdown/docstring):
- `scripts/serve/capture.py` — extend the module docstring [:1-30] to document the COMPREHENSIVE 9-step intake field→destination contract explicitly: the four rich-domain fields (`nutrition-detail`/`supplement-stack`/`peptide-stack`/`training-detail`) → their named-excluded raw sources → the four `_ALWAYS_SET_DERIVED` coarse tokens (the OQ-1 select-value carries the deriver keyword); the directly-wired tokens (goals, demographics); the record-only class (raw meds + race/occupation/sleep/stress/smoker/alcohol → the gitignored scaffold, NEVER a field-set item); and the no-cannabis-field constraint. This is behaviorally INERT (a docstring) — it makes the pre-existing contract legible for the T2/T3 capture regions that extend distinct regions of this same file. Make NO change to `persist_capture`'s branch logic, `WIRED_TOKENS`, `_CHAT_RAW_SOURCE_FIELDS`, `_BOUNDED_ENUMS`, or any routing predicate.

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/serve/test_capture.py -q`
Expected: PASS — the two new tests green (the docstring change is inert; the routing already satisfied them).

#### Step 3: REFACTOR
Review for:
- The two new tests REUSE the existing fixtures (`_ABSENT_IDENTITY`, `_bound`/`_summary`, the tmp-root pattern) — no bespoke store harness; no duplication of `test_chat_field_writes_raw_source_summarize_derives_band` (the new AC1 test adds the NOT-DISCUSSED CONTRAST the existing one lacks; the AC2 table pins the OQ-1 REPRESENTATIVE VALUES the existing realistic-value test does not).
- The AC2 table rows each assert a NON-DEFAULT bucket (or the determinate `"none"`/confirmed-none, which is non-`not-discussed` real signal) so no row silently passes on the no-signal default.
- The `capture.py` docstring names the same field tokens the code uses (no drift between the doc and `_CHAT_RAW_SOURCE_FIELDS`/`router._RAW_TO_FIELD`).

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/serve/test_capture.py -q`
Expected: PASS (no behavior change).

#### Step 4: REGRESSION
Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest -q` (port 8765 FREE → 0 failed; or EXACTLY the 2 named env failures with the port held). Confirm router.py byte-clean:
```
WAVE_BASE=$(git merge-base HEAD origin/main)
git diff --numstat "$WAVE_BASE"..HEAD -- scripts/plan/router.py
```
→ 0 rows (T1 makes 0 router.py edits).

### Cycle 2: Steps 5-7 — the crown-jewel record-only contract (AC3 + AC4 + AC5) + the EXTEND-NOT-REBUILD regression

#### Step 5: RED — Write the record-only + no-bypass tests + prove failing-capable
Edit `tests/serve/test_capture.py`: add three tests under the T1 section.

- **Test `test_comprehensive_capture_records_raw_meds_zero_rx_class_writes` (AC3).** Submit a comprehensive 9-step-shaped dict (goals + demographics + the four rich-domain fields + a raw `rx-interaction-classes` med field with a raw drug name) in ONE `persist_capture`; assert `store.read("rx-interaction-classes", root=store_root) == []`, the raw drug name appears in the scaffold receipt + in NO `SUMMARY_FIELD_SET` store item, and the four rich-domain tokens DID derive non-default (real signal coexists with record-only meds). Failing-capable mutation: re-adding `rx-interaction-classes` to `WIRED_TOKENS` reds the empty-read assertion.
- **Test `test_sensitive_fields_route_record_only_no_cannabis` (AC4).** Submit `race`/`ethnicity`, `occupation`, `sleep`, `stress`, `smoker`, `alcohol` (+ a probe `cannabis` field) values; assert EACH sensitive field name appears in `persist_capture(...)["scaffold"]`, writes 0 field-set store tokens (iterate `SUMMARY_FIELD_SET`, assert the value is in none), AND assert the contract carries NO cannabis-specific routing (the `cannabis` probe lands record-only like any unknown field, and `grep` confirms no `cannabis` token/key in `capture.py`). Failing-capable mutation: wiring `smoker` into `WIRED_TOKENS` reds the 0-store-token assertion.
- **Test `test_editable_re_capture_re_runs_same_path_no_direct_band_write` (AC5).** Capture `nutrition-detail="omnivore"`, then re-capture an edited `nutrition-detail="vegan"` into the SAME store; assert `raw-nutrition-free-text` carries BOTH readings (append-only), `summarize(...)["dietary-pattern-class"] == "plant-based"` (re-derived from `readings[-1]`, the edited latest), and `store.read("dietary-pattern-class", root=store_root) == []` (NEVER written directly — 0 new write surface). Failing-capable mutation: a direct-band-write path would land a `dietary-pattern-class` store item, reddening the 0-direct-write assertion.

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/serve/test_capture.py -q`
Expected: the three new tests PASS against the pre-existing routing. Then PROVE failing-capable per test by the named mutation (apply → RED → revert): for AC4, temporarily add `"smoker"` to `WIRED_TOKENS` (+ a `_BOUNDED_ENUMS` skip) → the 0-store-token assertion reds; revert. Record the mutation-proofs in the SE self-check.

#### Step 6: GREEN — (no production change)
No `capture.py` change is required — the default record-only `else` path [capture.py:291-295] + the `rx`-absent-from-`WIRED_TOKENS` design [capture.py:65-72] already satisfy AC3/AC4/AC5. Confirm GREEN:

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/serve/test_capture.py -q`
Expected: PASS — all T1 cases (Cycle 1 + Cycle 2) green; the pre-existing routing satisfies the comprehensive contract. (If any AC3/AC4/AC5 test is RED here, that is a real routing gap — STOP and re-ground; do NOT weaken the test.)

#### Step 7: REFACTOR + REGRESSION
Review for:
- The three new tests mirror the existing assert-NOT-in-the-wrong-place mandate (`test_record_only_field_lands_in_scaffold_not_in_any_field_set_item` [:535-568]) and the comprehensive-roster shape; no duplication of `test_rx_field_drug_name_lands_in_scaffold…` (the new AC3 test adds the COMPREHENSIVE multi-field submission; the existing one is the single-field probe).
- Each test carries a one-line `Failing-capable:` docstring naming its mutation (the project convention).
- The `cannabis` probe assertion is a NEGATIVE contract (no cannabis-specific handling) + a forward-constraint note for T4 (no cannabis control in the wizard) — not a removal of existing code.

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest -q` (full suite; port 8765 FREE → 0 failed) and the EXTEND-NOT-REBUILD gate:
```
WAVE_BASE=$(git merge-base HEAD origin/main)
git diff --numstat "$WAVE_BASE"..HEAD -- scripts/plan/orchestrate.py scripts/plan/pipeline.py scripts/plan/assemble.py scripts/plan/generate_plan.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/track.py scripts/plan/router.py
git diff --numstat "$WAVE_BASE"..HEAD -- 'scripts/store/*.py'
git diff --name-only "$WAVE_BASE"..HEAD
```
→ full suite green; 0 frozen-file rows; `scripts/store/*.py` empty; `git diff --name-only` lists ONLY `scripts/serve/capture.py` + `tests/serve/test_capture.py`. Confirm the `capture.py` diff is docstring-only: `git diff "$WAVE_BASE"..HEAD -- scripts/serve/capture.py` shows no change to `persist_capture`'s branch logic / `WIRED_TOKENS` / `_CHAT_RAW_SOURCE_FIELDS` / `_BOUNDED_ENUMS` (Risk Pre-Check gate 1).

**TDD step count: 7 (2 cycles).** Within the ≤10-step guidance. T1's 6 ACs (1, 2, 3, 4, 5, 6) span the four-token-real-signal pin (AC1), the OQ-1 select-value→bucket table (AC2), the comprehensive raw-meds record-only (AC3), the sensitive-field record-only + no-cannabis (AC4), the editable re-capture no-bypass (AC5), and the pytest gate (AC6). Two Modify files; 0 new python production routing code (the only `capture.py` change is the inert contract-doc docstring — the routing pre-exists per ADR-0014/0018/0019).

## Interface Contracts

This task PINS the comprehensive intake field→destination contract consumed by ADR-0033-0035-T2 (the DOB/weight capture region over this file), T3 (the safety region over this file), T6 (the gate predicate reads the wired/derived tokens), and T8 (the curation reads the record-only raw meds). It edits NO python routing surface (EXTEND-NOT-REBUILD).

- **The 9-step field→destination contract (PINNED, T1 owns the comprehensive doc).**
  - **Rich-domain → derived token:** `nutrition-detail`→`raw-nutrition-free-text`→`dietary-pattern-class`; `supplement-stack`→`raw-supplement-free-text`→`supplement-stack-class`; `peptide-stack`→`raw-peptide-free-text`→`peptide-use-class`; `training-detail`→`raw-training-detail-free-text`→`training-volume-band` (the EXISTING `_CHAT_RAW_SOURCE_FIELDS` [capture.py:94-99] + `router._RAW_TO_FIELD` [router.py:116-124]). The raw source is named-excluded PII (`EXCLUDED_RAW_PII`); the token is the coarse de-id derivation. NEVER the band token written directly.
  - **Record-only (the gitignored scaffold, NEVER a field-set item):** the raw `rx-interaction-classes` med field (`rx-interaction-classes` deliberately ABSENT from `WIRED_TOKENS` [capture.py:65-72]) + race/ethnicity, occupation, sleep, stress, smoker, alcohol (the default `else` path [capture.py:291-295]). NO cannabis field exists or is captured.
  - **Directly-wired (own-name store item):** goals (`goal-domains`/`goal-targets`/`goal-priority-order`/`hard-limits`) + demographics (`sex-for-dosing`/`bodyweight-band`/`equipment-access-class`) + `date-of-birth`→raw→`training-age-band`. `bodyweight-band` STAYS wired at T1 (its retirement to a `bodyweight-kg` series is T2/W2 — out of T1's scope).
- **OQ-1 select-value→deriver-bucket map (PINNED, the T1↔T4 format coupling — a coupling, NOT a code dependency).** The select OPTION VALUE (not the display text) carries a deriver-matching keyword: diet `vegan`→`plant-based` / `keto`|`allergic`→`restricted` / `vegetarian`→`plant-forward` / `chicken`|`beef`|`omnivore`→`omnivore`; training-frequency `5 days/week`|`5x`|`high volume`→`high` / `2x`|`twice`|`minimal`→`low` / (none)→`moderate`; supplement `none`→`none` / single item→`single-supplement` / `a, b`→`multi-supplement`; peptide `none`→`none` / a compound→`peptide-in-use`. **CRUX flagged for T4 + reviewers:** a display value `"5 or more days"` derives `moderate`, NOT `high` — `_training_volume_band` [router.py:537-541] needs the digit ADJACENT to the unit; T4's high-frequency option MUST submit a digit-adjacent-unit VALUE (`"5 days/week"`/`"5x"`) or a `"high volume"` keyword. T1's AC2 table is the executable pin of this contract; a change to the deriver keywords or the select values is a contract change requiring re-grounding at the T1↔T4 seam.
- **Frozen surface (PRESERVED).** Every `scripts/plan/*.py` (incl. `router.py` — the four derivers, `_RAW_TO_FIELD`, `SUMMARY_FIELD_SET`, `dispatch`), `scripts/store/*.py`, and `capture.py`'s routing logic (`persist_capture` branch, `WIRED_TOKENS`, `_CHAT_RAW_SOURCE_FIELDS`, `_BOUNDED_ENUMS`) are byte-unchanged (NFR-2). T1's only production edit is the inert `capture.py` contract-doc docstring; the substantive deliverable is the test contract.

## Verification Checklist

- [ ] AC-1 (the four default-fed tokens carry real signal) — verified by `test_rich_domain_field_carries_real_signal_vs_not_discussed_default`: a bucket-keyword value → a NON-DEFAULT token (`!= "not-discussed"` and `!= the no-signal default`) AND a fresh store → `"not-discussed"` (the real-signal contrast). Existing `test_chat_field_writes_raw_source_summarize_derives_band` [:838-869] covers the round-trip; the new test adds the not-discussed contrast (failing-capable: drop a `_CHAT_RAW_SOURCE_FIELDS` key → token stays `not-discussed` → reds).
- [ ] AC-2 (select-value→deriver-bucket, OQ-1) — verified by `test_oq1_select_value_resolves_to_expected_deriver_bucket`: a parametrized table asserting `"vegan"`→`plant-based`, `"5 days/week"`→`high`, `"none"`→`none`, `"BPC-157"`→`peptide-in-use` (and the low/restricted/multi rows) through `persist_capture`→`summarize`, NOT the no-signal default (failing-capable: a deriver-keyword/`_RAW_TO_FIELD` regression reds the specific row).
- [ ] AC-3 (raw meds record-only — 0 `rx-interaction-classes` writes from `persist_capture`) — verified by `test_comprehensive_capture_records_raw_meds_zero_rx_class_writes`: a comprehensive submission with a raw drug name → `store.read("rx-interaction-classes") == []`, the name in the scaffold + in NO field-set item (failing-capable: re-add `rx-interaction-classes` to `WIRED_TOKENS` → reds; mirrors the existing `test_rx_field_drug_name_lands_in_scaffold…` [:234-271]).
- [ ] AC-4 (race/occupation/sleep/stress/smoker/alcohol record-only + no cannabis) — verified by `test_sensitive_fields_route_record_only_no_cannabis`: each sensitive field in the scaffold receipt + 0 field-set store tokens; the `cannabis` probe records-only with NO cannabis-specific routing (a `grep` confirms no `cannabis` key in `capture.py`) (failing-capable: wire any sensitive field into `WIRED_TOKENS` → reds).
- [ ] AC-5 (editable re-capture re-runs the same path) — verified by `test_editable_re_capture_re_runs_same_path_no_direct_band_write`: a re-submitted edited `nutrition-detail` → both readings in `raw-nutrition-free-text`, `summarize` re-derives `plant-based` from the latest, `store.read("dietary-pattern-class") == []` (0 direct-band write, 0 new write surface) (failing-capable: a direct-band-write path reds the 0-direct-write assertion).
- [ ] AC-6 (pytest passes) — verified by `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/serve/test_capture.py -q` → 0 failed, AND the full suite `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest -q` green (port 8765 FREE → 0 failed; or EXACTLY the 2 named env failures with the port held).
- [ ] NFR-2 (EXTEND-NOT-REBUILD frozen engine + router/store byte-clean) — verified by the SHELL gate: `WAVE_BASE=$(git merge-base HEAD origin/main); git diff --numstat "$WAVE_BASE"..HEAD -- scripts/plan/orchestrate.py scripts/plan/pipeline.py scripts/plan/assemble.py scripts/plan/generate_plan.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/track.py scripts/plan/router.py` → 0 rows; `git diff --numstat "$WAVE_BASE"..HEAD -- 'scripts/store/*.py'` → empty; `git diff --name-only "$WAVE_BASE"..HEAD` lists ONLY `scripts/serve/capture.py` + `tests/serve/test_capture.py`; the `capture.py` diff is docstring-only (Risk Pre-Check gate 1).
- [ ] Store-surface obligation (`docs/checklists/store-adversarial-tests.md`, bead `pka`) — T1 writes to the store via the UNCHANGED `store.append` and adds NO new store item / no new write path / no new keying. The adversarial battery (cross-stream + dedupe-collision + mutation) is satisfied BY CONSTRUCTION: capture reuses the existing `store.append` sink the battery already protects; the new tests assert NO record-only value cross-streams into any `SUMMARY_FIELD_SET` item (the cross-stream-leak negative assertion, iterating every field-set token). No new store-write surface is introduced for the battery to re-cover.
- [ ] NFR-3 (0 live spend) — every test runs against fixture form-field dicts + tmp roots with `identity_config=_ABSENT_IDENTITY`; no `key_source.resolve()`-gated call, no `converse`/curation call, no network, no real key.
- [ ] Wave-1 checkpoint Go/No-Go gates carried (build plan § Wave 1 → Wave 2 Boundary): the four-default-fed-tokens-carry-signal [AC1], the select-option→deriver-bucket [AC2], raw meds record-only [AC3], sensitive-data record-only + no-cannabis [AC4], editable re-capture no-bypass [AC5], `test_capture.py` green [AC6]; the frozen-engine numstat 0 + router.py numstat 0; 0 live spend — all green.
- [ ] Scope-discipline check: `git diff --name-only "$WAVE_BASE"..HEAD` lists ONLY the 2 manifest files; any other file needs a written justification (the daemon `vault/**` churn is NOT staged).

## Commit

```
feat(capture): T1 comprehensive intake contract
```
(`feat` per the build directive: T1 pins the comprehensive intake field→destination CONTRACT — documented in `capture.py` + executed by the test contract — even though the routing code pre-existed. The body, if any, notes the routing pre-dates T1 and the production change is the contract-doc docstring.)

Stage only:
- `scripts/serve/capture.py`
- `tests/serve/test_capture.py`

Stage by explicit path: run `git add <path>` as its OWN command, then `git commit` SEPARATELY — the `block-pii-commit` PreToolUse hook denies single-call stage+commit, `git commit -a/--all`, and pathspec `git commit <path>` (their content is invisible to the pre-command staged-set scan). Do NOT `git add vault/` blindly (the daemon frontmatter churn re-appears every sync). Do NOT stage `.beads/issues.jsonl` / `harvest.jsonl` / `memory/process-failures.md` — those are close-protocol artifacts. Commit on `feature/intake-build` (never `main`).

## Rollback

If verification fails after 3 fix cycles:
1. `git stash -m "failed-ADR-0033-0035-T1-attempt-{N}"` — preserve work for diagnosis.
2. `git checkout {commit-before-task}` — revert to the entry-state commit (the branch HEAD this task was built on top of).
3. Update the bead tracker: flag ADR-0033-0035-T1 as BLOCKED with the failure log.
4. Escalate: "Task ADR-0033-0035-T1 failed verification after 3 attempts. Stash ref: {ref}. Failure log: {summary}."
5. After abandonment or re-planning, drop all `failed-ADR-0033-0035-T1` entries from `git stash list`.

Do NOT: silently skip a failing acceptance criterion; weaken a test to make it pass (especially the AC3 raw-meds-record-only / AC4 sensitive-record-only crown-jewel negatives — a falsification that cannot go RED is worthless; prove each new test's failing-capability by its named mutation); add a routing/validation branch the ACs do not require to manufacture a "real RED" (the rejected over-engineering path — HALT, confine to docstring + tests, Risk Pre-Check gate 1); wire any record-only field (raw meds / smoker / alcohol / race / occupation / sleep / stress) into `WIRED_TOKENS` or `_CHAT_RAW_SOURCE_FIELDS` (the crown-jewel record-only breach — HALT); touch `bodyweight-band`'s wiring (that retirement is T2/W2 — HALT, out of scope); edit `scripts/plan/router.py` / `scripts/store/*` / any frozen `scripts/plan/*.py` (the EXTEND-NOT-REBUILD breach NFR-2 rejects — HALT, confine to `capture.py` docstring + `test_capture.py`).

## Report

- Recipe: `docs/task-plan/.pipeline/adr-0033-0035/draft-ADR-0033-0035-T1.md` (status: draft).
- Every ADR-0033-0035-T1 acceptance criterion (AC-1..6) maps to a named Verification-Checklist test/command + the Wave-1 checkpoint gate; each new test is failing-capable by a named mutation (drop a `_CHAT_RAW_SOURCE_FIELDS` key → AC1/AC2 red; re-add `rx-interaction-classes` to `WIRED_TOKENS` → AC3 red; wire a sensitive field → AC4 red; a direct-band-write → AC5 red), never tautological.
- GROUNDED against the live tree (`feature/intake-build`, fork-point `git merge-base HEAD origin/main` = `12f769f`, 2026-06-30): `_CHAT_RAW_SOURCE_FIELDS` [capture.py:94-99] + `router._RAW_TO_FIELD` [router.py:116-124] + the four derivers [router.py:434-552] + `_NOT_DISCUSSED` [:321] + `_ALWAYS_SET_DERIVED` [:578-582] wire the four default-fed tokens; `rx-interaction-classes` ABSENT from `WIRED_TOKENS` [capture.py:65-72] + the default `else` path [capture.py:291-295] route raw meds + sensitive fields record-only; the existing `test_chat_field_writes_raw_source_summarize_derives_band` [:838-869] + the FIX-A rx tests [:234-271] already cover AC1/AC3; no cannabis field exists [VERIFIED grep]; `router.py`/`capture.py` numstat 0 from the base [VERIFIED]; the OQ-1 buckets spot-checked live (`vegan`→`plant-based`, `5 days/week`→`high`, `none`→`none`, `BPC-157`→`peptide-in-use`) [VERIFIED].
- ZERO literal implementation code in the recipe — every change is described as behavior + contract + the convention/exemplar to match, with cited `file:line` seams; the SE authors the markdown docstring + the tests under TDD.
- **PRE-EXISTING-ROUTING finding flagged for the wave reviewers (the load-bearing honesty note).** T1's capture ROUTING already exists and is correct (ADR-0014/0018/0019 built it); T1 is a comprehensive-contract-PINNING + sensitive-field-COVERAGE task, NOT a routing-code task. The `capture.py` Modify is a behaviorally-inert contract-doc docstring; the substantive deliverable is the new test contract (the OQ-1 select-value→bucket table + the sensitive-field record-only + the editable re-capture no-bypass). Because the contract pre-exists, the TDD RED is the per-test failing-capable MUTATION-PROOF (the project's pervasive convention), and the SE adds NO unasked-for routing/validation code (Core Rule 4 + NFR-5). This is NOT a spec defect — the task is buildable exactly as specified; the framing is documented so a reviewer expecting a large `persist_capture` diff understands the diff is docstring + tests by design.
- **OQ-1 CRUX flagged for T4 + reviewers (the T1↔T4 coupling).** A select display of "5 or more days" must submit a VALUE that carries a deriver-matching keyword (`"5 days/week"`/`"5x"`/`"high volume"`) — `_training_volume_band` [router.py:537-541] requires the digit ADJACENT to the unit, so the literal `"5 or more days"` derives `moderate`, not `high`. T1's AC2 table is the executable pin; T4 must build the high-frequency option VALUE to match (a format coupling, not a code dependency). Recorded in Interface Contracts.
- No HALT condition met: no untestable AC, no file needed outside the manifest, no provenance/security blocker. The recipe is complete and self-consistent.
