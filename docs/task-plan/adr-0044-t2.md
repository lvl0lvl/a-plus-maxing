---
task-id: ADR-0044-T2
source-spec: docs/spec/adr-0041-0046-comprehensive-plan-spec.md
source-build-plan: docs/build-plan/build-plan-adr-0041-0046.md
wave: 3
assigned-agent: SE
reviewers: [QA, Security]
created: 2026-07-12
status: approved
---

# Recipe: Mixed-History-Tolerant Reader + Consumer Re-point (STORE SURFACE + FROZEN-SPINE)

> Wave-3 Tier-2 task (Dependencies: ADR-0044-T1, merged — the comprehensive model + `resolve_comprehensive`
> the reader EXTENDS). It ADDS a mixed-history-tolerant READER to `scripts/store/plan_model.py` — reads BOTH
> the pre-existing thin `plan::<domain>` readings AND the comprehensive `plan-model::` versions, and resolves
> the latest comprehensive plan as STANDING while the thin history coexists ("comprehensive-wins": a
> comprehensive version dated on/after the latest thin reading wins) — and RE-POINTS the consumer
> `track.resolve_plan_progress` (`track.py:81`) off the superseded thin `plan_schema.read_plan`/`resolve_plan`
> onto this reader. The append-only store is NEVER rewritten (read-side only, 0 thin readings mutated). A
> store with ONLY thin `plan::<domain>` readings still resolves without error via a thin-only fallback (the
> thin resolver's result unchanged), so a pre-migration history keeps working.
>
> THE READER RIDES THE FROZEN PRIMITIVE. It reads BOTH streams through the UNCHANGED `store.read` and resolves
> in memory — it WRITES nothing, REWRITES nothing, and defines NO store key. The HARD-frozen four
> (`store.py`/`keying.py`/`pipeline.py`/`adjudicate.py`) stay byte-frozen (numstat=0), and the `<always-frozen>`
> 6-file glob (adds `adjust.py`/`router.py`) stays numstat=0 vs `origin/main`. `track.py` is a Wave-3 SUPERSEDED
> surface: its per-commit delta is the ONLY sanctioned `scripts/` change besides the `plan_model.py` reader
> addition (a per-ADR numstat probe bounds it).
>
> STORE SURFACE — the four-part store-adversarial battery (bead `pka`, `docs/checklists/store-adversarial-tests.md`)
> is a BLOCKING Tier-1 gate over the READER surface; category (d) runs RED-then-revert under the NAMED
> comprehensive-precedence mutation (the spec's AC-4d falsifier). A placeholder / `assert True` test fails the
> gate. The S37–S41 five-wave escape pattern (cross-stream, same-timepoint dedupe) is the exact failure class
> this battery moves upstream of Tier-3.
>
> PF-S130-01 (the Wave-1 tautological-swap catch). The consumer re-point is a call-site SWAP inside
> `resolve_plan_progress`, so AC-3 is a PRODUCTION-PATH integration test on the REAL `track.resolve_plan_progress`
> over a MIXED history (a thin `plan::<domain>` reading + a later comprehensive version, both dated the render
> date) that resolves comprehensive-wins AND REDs on a revert of the re-point (the reverted thin resolver
> returns the THIN plan, not the comprehensive prescription — Step 2.5). A re-point test that passes whether or
> not the swap happened is tautological and does NOT satisfy the AC.
>
> PF-S131-01 (fixture shape). Every fixture carrying a `domain_program` prescription uses the CANONICAL
> PERIODIZED shape (`{"blocks":[{date, ...}]}`, `domain_program.validate`-conformant), NOT a flat payload; the
> comprehensive-version fixtures use the real `plan_model` composite shape (reuse the `_comprehensive_version` /
> `_training_program` builders proven in `tests/store/test_plan_model.py`), so the comprehensive prescription is
> trivially distinguishable from the thin `plan` shape (`{"exercises":[...]}`) — the input that makes AC-1/AC-3
> non-tautological.
>
> FROZEN-GUARD RECONCILIATION (binding — performed by the SHARED Wave-3 frozen-guard reconciliation step, NOT
> this task's manifest). This is the FIRST task to supersede `scripts/plan/track.py`, so it trips the two
> prior-feature byte-guards that freeze `track.py` at 0 lines: `tests/plan/test_confirm_pointer_skip.py::test_frozen_spine_numstat_and_read_plan_only_hunk`
> (`FROZEN_GLOB` :35-42) and `tests/runner/test_cadence_runner.py::test_frozen_glob_numstat_empty` (:289-315).
> Per the standing per-wave carve-out rule (`build-plan-adr-0041-0046.md` line 21 AMENDED + the Architect
> Option-(A) ruling `docs/adr/.pipeline/frozen-guard-reconciliation-ruling.md` §4/§5), the SHARED Wave-3
> reconciliation commit (owned by NO Wave-3 task) carves `track.py` out of BOTH globs — removing ONLY the
> `"scripts/plan/track.py"` path token and leaving every other frozen member (above all the `<always-frozen>`
> HARD set) intact — alongside its sibling-superseded Wave-3 surfaces in the SAME globs (`orchestrate.py` for
> 0043-T2; `plan_driver.py` for 0046-T1). The behavioral replacement (ruling §4) THIS task DELIVERS is
> `test_plan_model_reader.py` (the reader + re-point + store-adversarial battery) + `tests/plan/test_track.py`
> staying GREEN (the thin-behavior regression proof) + the per-ADR numstat (bounds `track.py`'s delta). This
> task stages NO guard file; its Step-4 regression runs AFTER the reconciliation. A carve-out WITHOUT a
> behavioral replacement is a gate gap — HALT.

## Entry State

**Prerequisites** (ADR-0044-T2 depends on ADR-0044-T1 — the comprehensive model + `resolve_comprehensive` the reader extends; per the Dependency Map `ADR-0044-T1 --> ADR-0044-T2`):

- [ ] Task ADR-0044-T1 complete: `scripts/store/plan_model.py` present and exports the comprehensive model — `grep -q "^def record_plan_version" scripts/store/plan_model.py && grep -q "^def resolve_comprehensive" scripts/store/plan_model.py && grep -q "^def read_plan_version" scripts/store/plan_model.py && grep -q '_PREFIX_MODEL = "plan-model::"' scripts/store/plan_model.py && echo OK` (the comprehensive resolver + `plan-model::` stream the reader extends).
- [ ] The thin-side surfaces the reader CONSUMES are retained (ADR-0044-T1 retained `record_plan`/`resolve_plan`/`read_plan` "for the pre-model history the ADR-0044-T2 mixed-history reader still consumes"): `grep -q "^def read_plan" scripts/store/plan_schema.py && grep -q "^def resolve_plan" scripts/store/plan_schema.py && echo OK`. (`plan_schema` does NOT import `plan_model` — so `plan_model` importing `plan_schema` for the thin-side read is acyclic; confirm with `grep -q "import plan_model" scripts/store/plan_schema.py && echo CYCLE || echo "no cycle"`.)
- [ ] Full test suite at the entry baseline floor: `.venv/bin/python -m pytest -q` shows ONLY the two PRE-EXISTING env-sensitive failures — `tests/serve/test_bind.py::test_port_collision_exits_nonzero_fail_loud` and `tests/serve/test_server.py::test_main_reads_no_stdin` (OS-port/stdin cases, NOT this task's to fix). The gate is **no-NEW-failure**; a 3rd distinct failure is a regression → HALT. Record the exact passed/skipped counts at entry (they are env- and wave-sensitive — Wave-2's merged `test_plan_model.py` + others raised the count; do NOT hardcode a number).
- [ ] Working tree clean for the task scope: `git status --short` shows no STAGED changes and no uncommitted changes to `scripts/store/` / `scripts/plan/track.py` / `tests/store/`. ("Clean" = no STAGED path + the Create target absent + the frozen numstat=0 below. The basic-memory daemon perpetually re-churns `vault/**/*.md` frontmatter — S90; that churn + any parked `??` files are EXPECTED noise, NOT this task's changes. Do NOT stage them.)
- [ ] On a feature branch cut off `main`: `feature/comprehensive-plan-wave-3` (or equivalent `feature/*`) — never `main`.
- [ ] Rollback target + per-ADR numstat base recorded: `PRE_TASK_HEAD = git rev-parse HEAD` at task entry — BOTH the `{commit-before-task}` the Rollback Protocol reverts to AND the base the per-ADR numstat probe diffs against (QA-06: mirror `tests/store/test_plan_model.py:27`'s hardcoded `PRE_TASK_HEAD` constant — diffing against the FIXED entry HEAD is PRE-COMMIT-runnable + in-flow RED-capability-demonstrable, unlike `HEAD^`). Because ADR-0044-T1 + the SHARED Wave-3 frozen-guard reconciliation (if already on the branch) are IN this entry HEAD, the working-tree-vs-`PRE_TASK_HEAD` diff isolates ONLY 0044-T2's own delta over the frozen four (none of which they touch).
- [ ] The Create target is ABSENT (Create-only, no stale/duplicate artifact): `test -e tests/store/test_plan_model_reader.py && { echo "PRESENT (stop)"; exit 1; } || echo "ABSENT (Create OK)"`.
- [ ] The mixed-history reader is ABSENT from `plan_model.py` at entry (this task adds it): `grep -qE "^def (read_standing_plan|resolve_mixed)" scripts/store/plan_model.py && { echo "PRESENT (stop — reader already exists)"; exit 1; } || echo "ABSENT (add OK)"`. **QA-W3-01 — anchor to the function DEF (`^def …`), NOT a loose substring:** the ADR-0044-T1 module docstring at `plan_model.py:9` already contains the phrase "mixed-history reader", so the old loose `mixed.history` regex false-positives PRESENT and HALTs the task at entry though the reader FUNCTION (`read_standing_plan`) is genuinely absent (grounded: `grep -cE "^def (read_standing_plan|resolve_mixed)" scripts/store/plan_model.py` → 0 at entry).
- [ ] The consumer surface to re-point exists in its pre-swap shape: `grep -q "plan = plan_schema.read_plan(domain, on_date, root)" scripts/plan/track.py && echo OK` (`resolve_plan_progress` currently reads the THIN `plan_schema.read_plan` — the call site the swap re-points).
- [ ] The `<always-frozen>` HARD set byte-frozen at entry (numstat==0 vs `origin/main`): `git diff --numstat origin/main -- scripts/store/store.py scripts/store/keying.py scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/router.py` prints nothing. The reader rides `store.read` unchanged (it writes nothing).
- [ ] The two `track.py`-freezing byte-guards are present at their pre-carve shape (the frozen globs the SHARED Wave-3 reconciliation narrows — NOT this task): `grep -q '"scripts/plan/track.py"' tests/plan/test_confirm_pointer_skip.py && grep -q '"scripts/plan/track.py"' tests/runner/test_cadence_runner.py && echo OK` — both currently pin `scripts/plan/track.py` byte-frozen vs `origin/main`; the SHARED Wave-3 frozen-guard reconciliation step (owned by neither Wave-3 task) removes the `track.py` token from both before this task's commit. This task stages NO guard file.

## Context Load List

**Files to read (this task's file manifest + the frozen sinks it reads through + the guards it carves + the store-adversarial precedent):**

- `scripts/store/plan_model.py` — the ADR-0044-T1 module this task EXTENDS. `resolve_comprehensive(readings, on_date)` (`:207`, the reversed-scan latest-wins-per-`on_date` resolver over the composite value + the `NO_PLAN`/`NO_PLAN_TODAY` state vocabulary the mixed-history reader inherits); `read_plan_version(on_date, root)` (`:235`, the public `store.read(_PREFIX_MODEL)` + resolve wrapper the reader reuses for the COMPREHENSIVE side); `_PREFIX_MODEL = "plan-model::"` (`:58`, the disjoint stream); `VERSION_DATE`/`DOMAIN_PROGRAMS`/`NARRATIVE`/`MILESTONES`/`MONITORING_CONFIG` composite field constants (`:67-71`); `NO_PLAN`/`NO_PLAN_TODAY` (`:61-62`). The reader is ADDITIVE (a new function) — it does NOT edit `record_plan_version` / `resolve_comprehensive` / `read_plan_version` (0044-T4 later adds the confirm-hold to the resolver in a DISJOINT region).
- `scripts/store/plan_schema.py` — the THIN-side surfaces the reader consumes for the fallback (READ-only reuse, NOT superseded here — 0044-T1 already retained them for exactly this reader): `read_plan(domain, on_date, root)` (`:514`, `store.read` + `filter_confirmed` + `resolve_plan`, the thin resolve the reader delegates to for the thin fallback AND for a domain the comprehensive plan does not cover); `resolve_plan(readings, on_date)` (`:469`, the thin three-state resolution the fallback preserves); `PLAN_DOMAINS`/`TRACKED_DOMAINS` (`:49-50`); `_PREFIX_PLAN = "plan::"` (`:56`, the thin per-domain stream). Do NOT edit `plan_schema.py` (this task adds NO line here — it is an AC-of-the-numstat forbidden surface, sanctioned-superseded by 0044-T1 only).
- `scripts/plan/track.py` — the consumer this task RE-POINTS. `resolve_plan_progress(domain, on_date, root)` (`:81`) currently reads `plan = plan_schema.read_plan(domain, on_date, root)` (`:113`) then maps `has_plan = plan["state"] is None` (`:115`) → the return dict `{domain, plan, specialist, plan_date, tracking, has_plan, has_tracking}` (`:116-126`). The swap re-points ONLY the plan read (`:113`) onto the new `plan_model` reader; the tracking read (`plan_schema.read_plan_tracking`, `:114`), the `TRACKED_DOMAINS` guard (`:109`), and the return-dict KEYS stay UNCHANGED (so `adjust.py` / `maintained.py` / `plan_loop.py` consumers read the same keys transitively — see Interface Contracts).
- `scripts/store/store.py` — the FROZEN read primitive the reader rides UNCHANGED: `read(item, root)` latest-wins-per-`(item, timepoint, source)` identity + timepoint-sorted (`:214`). Do NOT edit (byte-frozen — entry-state numstat only). The reader `store.read`s both streams, never a new sink, never a write.
- `scripts/store/keying.py` — the dedupe identity `(item, timepoint, source)` with `value` EXCLUDED (`:11-16`). The reader introduces NO new keying (it does not append). Do NOT edit (byte-frozen). READ only to assert (category-c grep) that `plan_model.py`'s reader addition defines no `DEDUPE_FIELDS`/`dedupe_key`/`LINE_FIELDS`.
- `scripts/plan/domain_program.py` — the seven-field DOMAIN PROGRAM contract each comprehensive per-domain program conforms to: `validate(program)` (`:134`), `PRESCRIPTION` (`:68`, the periodized `{"blocks":[...]}` field the comprehensive-wins branch returns as the `plan`), `KIND_FIELD`/`DOMAIN_KINDS` (`:81,89`), `REQUIRED_FIELDS`/`CONDITIONAL_FIELDS` (`:76-77`). READ-only reuse for the fixture builders (do NOT edit — the frozen schema seam).
- `tests/store/test_plan_model.py` — the ADR-0044-T1 test to MIRROR: the `_training_program()` / `_compound_program()` / `_comprehensive_version(...)` synthetic fixture builders (`:36-115`, the canonical periodized `{"blocks":[{date, phase, detail}]}` prescription — PF-S131-01); the `tmp_path`-root scratch-store pattern; the store-adversarial shapes `test_cross_stream_disjoint` (`:408`, incl. the SEC-01 item-`""` decoy), `test_same_timepoint_two_versions_persist` (`:439`), `test_dedupe_key_boundary_and_no_new_keying` (`:455`, the `token not in source` grep); the `PRE_TASK_HEAD` constant + `_numstat`/`_func_src` helpers (`:27,345-359`); `test_no_operator_identity_literal_in_test_tree` (`:481`, the 0-real-PII grep). Reuse these fixtures — do NOT re-invent the composite shape.
- `tests/plan/test_track.py` — the THIN-behavior behavioral guard to PRESERVE (the `track.py` carve-out's ruling-§4 replacement): every existing `resolve_plan_progress` case (`test_progress_pairs_plan_and_tracking` `:81`, `test_progress_no_plan_is_honest_absence` `:92`, `test_progress_no_plan_today_has_plan_false_no_contradiction` `:198`, the CONTRACTS-TRACK-1 `plan_date` agreement `:239`) asserts the THIN resolve semantics that the thin-only fallback MUST preserve byte-for-behavior. These stay GREEN post-re-point (the regression proof the re-point did not loosen thin behavior). Also the `_workout_plan()` / `_seed_plan(...)` fixture convention to mirror.
- `docs/checklists/store-adversarial-tests.md` — the four required categories AC-4 embeds (cross-stream namespace collision, same-timepoint dedupe, dedupe-key boundary, mutation-RED), the enforcement points, and the S37–S41 escape pattern this battery moves upstream.
- `tests/plan/test_confirm_pointer_skip.py` (READ-ONLY — carved by the SHARED Wave-3 reconciliation, NOT this task) — `FROZEN_GLOB` (`:35-42`) currently lists `"scripts/plan/track.py"` (`:40`); `test_frozen_spine_numstat_and_read_plan_only_hunk` (`:212`) asserts it byte-frozen vs `origin/main`. The reconciliation removes the `track.py` token; this task's Step-4 regression re-runs it GREEN — it does NOT edit it.
- `tests/runner/test_cadence_runner.py` (READ-ONLY — carved by the SHARED Wave-3 reconciliation, NOT this task) — `test_frozen_glob_numstat_empty` (`:289`) lists `"scripts/plan/track.py"` (`:297`) in its broader frozen tuple (also a recursive `scripts/store/**/*.py`-minus-`plan_model.py`/`plan_schema.py` glob, `:303-306`, which already excludes `plan_model.py` so the reader addition is unfrozen there). The reconciliation removes the `track.py` token; Step-4 re-runs it GREEN.
- `docs/adr/.pipeline/frozen-guard-reconciliation-ruling.md` — §4 (behavioral-replacement sufficiency) + §5 (the standing per-wave carve-out rule) — the BINDING authority for the two guard edits; read §4's replacement-guard obligation.
- `docs/build-plan/build-plan-adr-0041-0046.md` — line 21 (the AMENDED standing per-wave frozen-guard-reconciliation rule) + the Wave-3 schedule (`:99-113`) + the Wave 3 → Wave 4 checkpoint (`:224-231`) that gate THIS task; the Agent Matrix row (`:180`, `Security, QA` reviewers for the store surface).

**Do NOT load:**
- The ADRs (`docs/adr/ADR-0044-*.md` … `ADR-0046-*.md`) — the spec already distilled them; re-reading re-opens resolved trade-offs (TP-02 drift).
- The other ADR-0044/0043/0045/0046 task blocks + their manifests (the confirm-hold skip on the resolver = 0044-T4; the `horizons.py` re-base = 0044-T3; `activation.py`/`plan_driver.py`/`orchestrate.py`/`monitoring_compiler.py` = other Wave-3/later tasks) — out of THIS task's scope. This recipe covers ONLY the `### ADR-0044-T2` block (the reader + the `track.resolve_plan_progress` re-point).
- The frozen primitives beyond the entry-state numstat check: `scripts/store/store.py` / `scripts/store/keying.py` are READ above for the read/dedupe CONTRACT the reader rides, never edited; `pipeline.py` / `adjudicate.py` / `adjust.py` / `router.py` are not read at all (byte-frozen, not this task's surface — note `adjust.py` is a downstream reader of `resolve_plan_progress` AND a `FROZEN_GLOB` member, so it is transitively re-pointed with 0 edits).

## Risk Pre-Check

**BLOCKING GATE — STORE SURFACE (bead `pka`) + the FROZEN-SPINE `track.py` supersession + the PF-S130-01 tautological-consumer-swap failure class.** Do NOT proceed to GREEN until a real (non-`assert True`), RED-capable test exists for every risk below. The task is NOT complete until the AC-4 category-(d) precedence mutation + the PF-S130-01 revert falsifier are proven RED against the GREEN implementation (Step 2.5 — the comprehensive-precedence / re-point logic being mutated does not exist until GREEN). A placeholder test fails this gate.

- **Risk (S37–S41 store-keying escape pattern, bead `pka`):** the builder + Tier-2 systematically under-test store cross-stream / same-timepoint-dedupe / dedupe-key cases; five consecutive waves shipped a store-keying escape caught only at Tier-3. The reader reads TWO streams (`plan-model::` + `plan::<domain>`) and merges them — a cross-stream leak (returning another domain's or another stream's plan) is the exact S41 `read_panel` fabricated-value class.
  - Gate condition: RED must include all four adversarial categories as real tests over the READER surface — (a) cross-stream namespace collision (the reader for domain X never returns domain Y's thin plan, AND the comprehensive read never returns a thin `plan::<domain>` value NOR an item-`""` decoy — seed thin decoys for a second domain + the SEC-01 item-`""` decoy), (b) same-timepoint dedupe (two DISTINCT comprehensive versions sharing a timepoint both readable, the reversed-scan latest resolves — inherited from `resolve_comprehensive`, re-asserted over the reader), (c) dedupe-key boundary + no-new-keying (a grep asserts `plan_model.py`'s reader addition defines 0 new dedupe key / keying logic — the reader is READ-only, identity stays `keying`'s frozen `(item, timepoint, source)` reached only through `store.read`), (d) **mutation — the NAMED comprehensive-precedence removal drives the mixed-history test RED, then revert** (Step 2.5): remove/invert the comprehensive-wins branch in the reader (make it always return the thin result) → `test_mixed_history_comprehensive_wins` (AC-1) goes RED (it returns the thin plan, not the comprehensive prescription); re-run, confirm RED, revert. A battery green under this mutation is tautological and FAILS this gate.
- **Risk (PF-S130-01 — tautological consumer re-point; the Wave-1 gap Tier-3 caught):** the re-point is a call-site SWAP inside `resolve_plan_progress`; a test that passes whether or not the swap happened encodes broken behavior.
  - Gate condition: AC-3 MUST run the REAL `track.resolve_plan_progress(domain, on_date, root)` over a scratch store carrying a MIXED history — a thin `plan::<domain>` reading dated the render date AND a later comprehensive version dated the render date whose `domain_programs[domain]` carries a DISTINGUISHABLE periodized prescription. It resolves comprehensive-wins: `has_plan is True` AND `plan` carries the comprehensive prescription (`{"blocks":[...]}`), NOT the thin plan (`{"exercises":[...]}`) and NOT a `NO_PLAN` absence. The **PF-S130-01 revert falsifier** (Step 2.5): revert the swap (`resolve_plan_progress` reads `plan_schema.read_plan` again) → over the SAME mixed history it returns the THIN plan → `test_resolve_plan_progress_comprehensive_wins_production_path` (AC-3) goes RED (the reverted thin resolver returns the thin result). A test green under this revert FAILS the AC.
- **Risk (PF-S131-01 — flat-prescription / non-composite fixtures):** a fixture carrying a flat `domain_program` prescription or a hand-built (non-`plan_model`) composite would not distinguish the comprehensive path from the thin path, re-opening the PF-S130-01 tautology on the fixture side.
  - Gate condition: every fixture carrying a `domain_program` prescription uses the CANONICAL PERIODIZED shape (`{"blocks":[{date, ...}]}`, `domain_program.validate`-conformant); the comprehensive-version fixtures are the REAL `plan_model` composite (reuse `tests/store/test_plan_model.py`'s `_comprehensive_version` / `_training_program` builders, recorded via `plan_model.record_plan_version`), so the comprehensive prescription is structurally distinct from the thin `plan` shape. A flat-payload prescription or a hand-built composite dict fed to the reader FAILS this gate.
- **Risk (append-only preserved — the store contract; ADR-0044 Consequences-Negative-3):** the reader must respect append-only immutable history — it reads and resolves, it NEVER rewrites a thin reading NOR a comprehensive version (a forward migration that rewrote history would corrupt the audit trail).
  - Gate condition (SEC-W3-01, symmetric on BOTH streams): AC-1 asserts BOTH streams are BYTE-UNCHANGED after the mixed-history read — the thin `plan::<domain>` readings AND the comprehensive `plan-model::` versions have identical `.ndjson` line counts pre/post (`store.read` of each item returns the same readings) — **0 readings rewritten on EITHER stream** — AND AC-4(c) greps the reader addition for 0 write-primitive calls (`store.append`/`store.correct`/`record_plan_version`). RED-capable: a reader that mutated/re-recorded either stream on read would change its line count / carry a write call → RED.
- **Risk (FROZEN-SPINE `track.py` supersession — the frozen four must stay byte-frozen; the reader rides the primitive):** an un-bounded edit could bleed into the frozen engine or leave `track.py`'s delta unbounded.
  - Gate condition: RED must include the **per-ADR numstat probe** (the FROZEN-SPINE constraint the task carries; maps to the build-plan Wave-3 checkpoint `<always-frozen> numstat=0`, `:113`): `git diff --numstat PRE_TASK_HEAD -- scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/store/store.py scripts/store/keying.py scripts/plan/assemble.py scripts/plan/adjust.py scripts/plan/router.py` == EMPTY (the HARD-frozen four + `assemble.py` — no Wave-3 editor — + `adjust.py`/`router.py`, untouched in THIS commit). **QA-W3-02 narrowing:** the Wave-3 sibling-superseded surfaces `scripts/store/plan_schema.py` + `scripts/plan/orchestrate.py` + `scripts/plan/generate_plan.py` are DROPPED from the persisted forbidden-EMPTY list — 0046-T1 edits `plan_schema.py`+`generate_plan.py`, 0043-T2 edits `orchestrate.py`, so a persisted `PRE_TASK_HEAD`-based probe forbidding them false-REDs at a later Wave-3 checkpoint once a sibling commits (the Wave-2 addendum-(iv)(b) precedent, mirrored by `draft-adr-0043-t2.md:127` + Deviation #1). AND `git diff --numstat origin/main -- scripts/store/store.py scripts/store/keying.py scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/router.py` == EMPTY (the `<always-frozen>` 6-file glob vs the pre-build trunk) AND — as a Step-4 commit-time MANUAL check, NOT a persisted assertion — `git diff --name-only PRE_TASK_HEAD -- scripts/` == exactly `scripts/store/plan_model.py` + `scripts/plan/track.py` (the ONLY sanctioned `scripts/` deltas at THIS task's commit; the name-only set grows past two once a sibling commits, so it cannot be a re-run gate). RED-capable: a throwaway edit to any forbidden file in the persisted probe makes it RED (observe RED, revert).
- **Risk (frozen-guard reconciliation — a gate gap, ruling §4):** editing `track.py` REDs the two prior byte-guards that freeze it (`test_confirm_pointer_skip.py`'s `FROZEN_GLOB` + `test_cadence_runner.py::test_frozen_glob_numstat_empty`); carving them without a behavioral replacement opens the exact regression class they guard.
  - Gate condition: the SHARED Wave-3 reconciliation removes ONLY the `"scripts/plan/track.py"` token from each glob (leaving the `<always-frozen>` HARD set + every non-superseded member frozen) AND enumerates the FULL `track.py`-freezing set by EXECUTION (F-011 — the Wave-2 boundary surfaced 2 guards a static grep missed; do NOT trust a grep-only set). The behavioral replacement THIS task delivers is `tests/store/test_plan_model_reader.py` (the reader + re-point + store-adversarial battery) + `tests/plan/test_track.py` staying GREEN (the thin-behavior regression) + the per-ADR numstat (bounds `track.py`'s delta). This task's Step-4 REGRESSION re-runs the carved guards GREEN. A reconciled guard that also un-freezes any store/engine primitive, or a missing behavioral replacement, FAILS this gate → HALT.
- **Risk (ADR-0044 Consequences-Negative-3, tail — downstream thin-schema consumers of `resolve_plan_progress`):** NON-blocking, documented (Deviation #2). The re-point preserves `resolve_plan_progress`'s return-dict KEYS, so `adjust.py`/`maintained.py`/`plan_loop.py` do not crash; the comprehensive-prescription `plan` shape only reaches them once comprehensive plans are actually authored (a future live-run state — thin-only stores are 100% backward-compatible via the thin fallback). The full consumer-shape migration is the spec's dispositioned "migration of every thin-schema consumer" cost, tracked as a downstream follow-on, NOT this task's scope.

## TDD Steps

Single cycle. All acceptance criteria are properties of ONE coherent unit — the `plan_model` mixed-history reader + the `track.resolve_plan_progress` re-point — exercised over the same seeded scratch-store fixtures, so they are not split (task-planning: "all criteria test the same component"). RED writes every AC as a specific failing test BEFORE any GREEN; the category-(d) precedence mutation + the PF-S130-01 revert falsifier run POST-GREEN (Step 2.5), because the comprehensive-precedence / re-point logic being mutated does not exist until GREEN.

### Step 1: RED — Write Failing Tests

Call `/write-tests` for `scripts/store/plan_model.py` (the reader) + `scripts/plan/track.py` (the re-point), creating `tests/store/test_plan_model_reader.py`, targeting each spec AC. Mirror `tests/store/test_plan_model.py` (reuse its `_training_program` / `_compound_program` / `_comprehensive_version` builders + `PRE_TASK_HEAD`/`_numstat` helpers) and `tests/plan/test_track.py` (the thin `_workout_plan` / `_seed_plan` convention): seed via the `plan_model` / `plan_schema` APIs (and direct `store.append`/`plan_schema.record_plan` for adversarial decoys) under a `tmp_path` root; run each assertion over the REAL read path. ALL fixtures are SYNTHETIC (0 real operator PII, 0 live-API), on scratch stores.

**Shared fixtures:** reuse `_comprehensive_version(...)` (the real `plan_model` composite spanning ≥2 domains, each a periodized-prescription seven-field program — PF-S131-01) recorded via `plan_model.record_plan_version`; a thin `plan::<domain>` reading via `plan_schema.record_plan(domain, {"exercises":[...]}/…, date, specialist, root)`. The comprehensive prescription (`{"blocks":[...]}`) vs the thin plan (`{"exercises":[...]}`) is the distinguishing signal that makes AC-1/AC-3 non-tautological.

- **AC-1 — Mixed-history read, comprehensive-wins (disposition #16, load-bearing):** `test_mixed_history_comprehensive_wins` — seed a store carrying BOTH a thin `plan::workout` reading dated the render date AND a comprehensive version dated the render date whose `domain_programs["workout"]` carries a distinguishable periodized prescription; the REAL mixed-history reader resolves the LATEST comprehensive plan as **standing** (state None, `plan`/`version` carrying the comprehensive prescription), the comprehensive plan WINS over the coexisting thin reading, AND — append-only preserved on BOTH streams (SEC-W3-01, symmetric) — the thin `plan::workout` readings are BYTE-UNCHANGED after the read (`store.read("plan::workout")` line count + values identical pre/post — **0 thin readings rewritten**) AND the comprehensive `plan-model::` stream is BYTE-UNCHANGED (`store.read(plan_model._PREFIX_MODEL)` line count identical pre/post — **0 comprehensive versions rewritten on read**). RED-capable: the (d) precedence mutation (Step 2.5) turns it RED; a reader that appended/re-recorded on read would change EITHER stream's line count → RED.
- **AC-2 — Thin-only fallback (no comprehensive version yet):** `test_thin_only_fallback_resolves` — a store with ONLY thin `plan::<domain>` readings (no `plan-model::` version) resolves WITHOUT error via the reader — a thin plan dated the render date resolves standing carrying the thin plan + its specialist attribution (identical to today's `plan_schema.read_plan` result); a thin store with no plan for the date resolves the `NO_PLAN`/`NO_PLAN_TODAY` absence carrying the latest on-file date (0 crashes on a pre-migration thin-only store).
- **AC-2b — Thin-behavior regression (the `track.py` carve-out's behavioral replacement, MUST):** confirm `tests/plan/test_track.py` in FULL stays GREEN after the re-point — the thin-only fallback preserves the EXACT thin resolve semantics `resolve_plan_progress` pins (`has_plan`, `plan`, `specialist`, the CONTRACTS-TRACK-1 `plan_date` = "the plan FOR on_date else None", the `NO_PLAN_TODAY` `has_plan False` non-contradiction). RED-capability: a re-point that dropped/reshaped the thin result (e.g. returned a comprehensive shape on a thin-only store, or lost `specialist`) breaks `test_track.py::test_progress_pairs_plan_and_tracking` → RED. This is the proof the re-point did not loosen thin behavior.
- **AC-3 — Consumer re-point, PRODUCTION-PATH (PF-S130-01 load-bearing):** `test_resolve_plan_progress_comprehensive_wins_production_path` — seed the SAME mixed history (thin `plan::workout` + a later comprehensive version, both dated the render date, distinguishable prescriptions) under a `tmp_path` root; call the REAL `track.resolve_plan_progress("workout", on_date, root)`; assert `result["has_plan"] is True` AND `result["plan"]` carries the COMPREHENSIVE prescription (`"blocks" in result["plan"]` / equals the comprehensive workout prescription), NOT the thin plan and NOT a `NO_PLAN` absence on a stored plan; AND the return dict still carries every existing KEY (`domain`, `plan`, `specialist`, `plan_date`, `tracking`, `has_plan`, `has_tracking`) so the downstream consumers read the same shape. The path is the real `resolve_plan_progress` → the `plan_model` reader (not a hand-built dict). Step-2.5 revert-to-thin turns it RED (the reverted thin resolver returns the thin plan).
- **AC-3b — Comprehensive standing, thin-empty-for-domain (AC-3 "not a `NO_PLAN` on a stored plan", load-bearing):** `test_resolve_plan_progress_comprehensive_only_not_no_plan` — seed a comprehensive version dated the render date covering `workout` but with NO thin `plan::workout` reading at all; `track.resolve_plan_progress("workout", on_date, root)` resolves `has_plan is True` with the comprehensive prescription — NOT `has_plan False`/`plan None`. RED-capability is the PF-S130-01 revert: the reverted thin resolver over an EMPTY `plan::workout` returns `NO_PLAN` → `has_plan False` → RED (this is the exact "not a `NO_PLAN` on a stored plan" AC-3 clause).
- **AC-4 — Store-adversarial battery (all four, per `docs/checklists/store-adversarial-tests.md`, over the READER surface):**
  - (a) **cross-stream namespace collision:** `test_reader_cross_stream_disjoint` — the reader for domain X never returns domain Y's thin plan (seed distinct thin `plan::workout` + `plan::nutrition` plans + a comprehensive version covering only `workout`; assert the reader for `nutrition` returns nutrition's thin plan via fallback and never workout's, and the reader for `workout` returns the comprehensive workout prescription and never nutrition's) AND the comprehensive read never returns a thin `plan::<domain>` value NOR an item-`""` decoy (mirror `test_plan_model.py::test_cross_stream_disjoint`'s SEC-01 item-`""` decoy seeded dated the render date — the comprehensive resolution returns ONLY the real composite, never the foreign item-`""` value). The distinct item files → disjoint streams.
  - (b) **same-timepoint dedupe:** `test_reader_same_timepoint_latest_comprehensive_resolves` — two DISTINCT comprehensive versions recorded at ONE timepoint (distinct content-tag sources, per `record_plan_version`) both persist (`store.read(_PREFIX_MODEL)` returns 2); the reader's reversed-scan resolves the LAST-appended version dated `on_date` as standing (inherits `resolve_comprehensive`'s tie-break); an identical re-record stays an idempotent no-op (append count unchanged).
  - (c) **dedupe-key boundary + no-new-keying + no-write-primitive (SEC-W3-01):** `test_reader_no_new_keying` — a grep/read over `scripts/store/plan_model.py`'s **reader addition** (the new function body, scoped — NOT the whole module, which legitimately contains the existing `record_plan_version`/`store.append` record path) asserts (i) **0 new dedupe-key / keying logic** (no `DEDUPE_FIELDS`/`dedupe_key`/`LINE_FIELDS`/`_DEDUPE_EXCLUDED` redefinition; identity stays `keying`'s frozen `(item, timepoint, source)`, reached only through `store.read`), AND (ii) **0 write-primitive CALLS** — the reader body invokes NO `store.append(`/`store.correct(`/`.append(`/`.correct(`/`record_plan_version(` (it is read-only over BOTH the thin `plan::` AND the comprehensive `plan-model::` streams; it composes `read_plan_version` + `plan_schema.read_plan`, both read-only). Both counts == 0 — the reader introduces no key and writes to neither stream.
  - (d) **mutation-style (run RED, observe, revert — Step 2.5):** name the mutation + the battery test it turns RED — remove/invert the comprehensive-precedence branch in the reader (`_PREFIX_MODEL` resolution ignored → always return the thin result) → `test_mixed_history_comprehensive_wins` (AC-1) goes RED (the read returns the thin plan, not the comprehensive prescription); re-run, CONFIRM RED, revert. A battery green under this mutation is tautological and FAILS AC-4.
- **AC-FS — Per-ADR freeze-break numstat (the FROZEN-SPINE constraint the task carries — an ADDED gate beyond the spec's five ACs; maps to build-plan Wave-3 `<always-frozen> numstat=0`, `:113`):** `test_frozen_spine_numstat_and_scoped_delta` (subprocess `git diff --numstat`, mirror `test_plan_model.py:345`'s `_numstat` helper + `PRE_TASK_HEAD` constant): (a) `git diff --numstat PRE_TASK_HEAD -- scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/store/store.py scripts/store/keying.py scripts/plan/assemble.py scripts/plan/adjust.py scripts/plan/router.py` is EMPTY (the HARD-frozen four + `assemble.py` — no Wave-3 editor — + `adjust.py`/`router.py`, untouched by THIS task). **QA-W3-02 narrowing:** `scripts/store/plan_schema.py` + `scripts/plan/orchestrate.py` + `scripts/plan/generate_plan.py` are DROPPED from this persisted forbidden-EMPTY list — they are Wave-3 SIBLING-superseded surfaces (0046-T1 edits `plan_schema.py`+`generate_plan.py`; 0043-T2 edits `orchestrate.py`), so a persisted `PRE_TASK_HEAD`-based probe forbidding them false-REDs at a later Wave-3 checkpoint once a sibling commits (the Wave-2 addendum-(iv)(b) precedent, mirrored by `draft-adr-0043-t2.md:127` + its Deviation #1); (b) `git diff --numstat origin/main -- scripts/store/store.py scripts/store/keying.py scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/router.py` is EMPTY (the `<always-frozen>` 6-file HARD glob vs the pre-build trunk); (c) the "exactly `scripts/store/plan_model.py` + `scripts/plan/track.py`" scope is verified as a **Step-4 commit-time MANUAL check** (`git diff --name-only PRE_TASK_HEAD -- scripts/` at THIS task's commit), NOT a persisted pytest assertion — the name-only set grows past two files once a Wave-3 sibling commits, so it cannot be a re-run gate. RED-capable: a throwaway edit to any forbidden file in (a)/(b) makes the probe RED (proven in Step 1-continued by a throwaway edit, observed, reverted).
- **AC-5 — Suite-local green + 0 real PII (spec AC-5):** `test_plan_model_reader.py` passes against scratch stores — 0 live-API calls, 0 real operator PII (verified by the Step-1→Step-2 RED→GREEN transition + a `grep` that the test tree carries no real operator-identity literal, mirror `test_plan_model.py::test_no_operator_identity_literal_in_test_tree`).

Run: `.venv/bin/python -m pytest tests/store/test_plan_model_reader.py -q`
Expected: FAIL — the mixed-history reader does not exist yet, so the reader-import/attribute fails and the re-point tests return the thin result. (If a NON-numstat behavioral test PASSES at this stage, the test is wrong — investigate before GREEN. The AC-5 freeze-numstat probes are the exception: based on `PRE_TASK_HEAD` they are pre-commit-runnable and GREEN whenever the frozen surfaces are untouched — their RED-capability is demonstrated by a throwaway frozen-file edit, not the reader RED→GREEN transition; re-confirmed at Step 4.)

### Step 1 (continued): RED-capability falsification (NON-SKIPPABLE)

Before AND after implementing, prove per-assertion RED-capability (a module-present coincidence can make a test pass for the wrong reason):

1. **AC-3 revert-REDs (PF-S130-01, the load-bearing proof):** after GREEN, revert the `resolve_plan_progress` swap (read `plan_schema.read_plan` again); confirm `test_resolve_plan_progress_comprehensive_wins_production_path` + `test_resolve_plan_progress_comprehensive_only_not_no_plan` go RED (the thin resolver returns the thin plan / `NO_PLAN`); restore the swap (Step 2.5).
2. **AC-4(d) precedence mutation:** after GREEN, remove/invert the comprehensive-precedence branch; confirm `test_mixed_history_comprehensive_wins` goes RED; revert (Step 2.5).
3. **AC-FS forbidden-file mutation:** make a throwaway one-line edit to a forbidden file (e.g. a comment in `scripts/plan/pipeline.py`), confirm `test_frozen_spine_numstat_and_scoped_delta` goes RED, `git checkout` the file. Proves the numstat probe bites.

This is a mandatory gate of THIS recipe — do not silently delegate RED-capability to `/write-tests`.

### Step 2: GREEN — Implement

Changes to make (behavioral descriptions, NOT literal code):

- `scripts/store/plan_model.py` (Modify — ADDITIVE, a new reader function; do NOT edit `record_plan_version`/`resolve_comprehensive`/`read_plan_version`):
  - **The mixed-history-tolerant reader (satisfies AC-1/AC-2/AC-3):** add a `read_standing_plan(domain, on_date, root)`-style public reader (the name is executor discretion but is the stable seam `track.py` imports — name it consistently with `read_plan_version`). It reads BOTH streams through the UNCHANGED `store.read`: the COMPREHENSIVE side via the existing `read_plan_version(on_date, root)` (`resolve_comprehensive` over the `plan-model::` stream), the THIN side via `plan_schema.read_plan(domain, on_date, root)` (the retained thin resolver + its `filter_confirmed`). Apply **comprehensive-wins precedence**: when a comprehensive version resolves STANDING for `on_date` (state None) AND its `domain_programs` covers `domain`, return the comprehensive result — state None, `plan` = that domain's PRESCRIPTION (`version["domain_programs"][domain][domain_program.PRESCRIPTION]`, the periodized `{"blocks":[...]}`), `plan_date` = `on_date`; OTHERWISE return the THIN result verbatim (the thin-only fallback AND the comprehensive-standing-but-domain-not-covered case both fall here — comprehensive wins only for domains it actually covers, so a pre-migration thin store and an uncovered domain keep their exact thin behavior). **Return the SAME shape as `plan_schema.read_plan`** — a dict carrying `state` (None | `NO_PLAN` | `NO_PLAN_TODAY`), `plan`, `specialist`, `plan_date` — so the consumer re-point is a pure call-site swap (see the assumption in Deviation #1 for `specialist` on the comprehensive branch). The reader WRITES nothing, REWRITES nothing (append-only preserved), and defines NO store key (identity reached only through `store.read`). Import surface adds `scripts.store.plan_schema` (thin-side read) — acyclic (`plan_schema` does not import `plan_model`); reuse `domain_program.PRESCRIPTION` for the field name.
- `scripts/plan/track.py` (Modify — the single call-site swap in `resolve_plan_progress`, `:113`; the rest of the function UNCHANGED):
  - **Re-point the plan read (satisfies AC-3):** swap `plan = plan_schema.read_plan(domain, on_date, root)` at **`:113` (inside `resolve_plan_progress`, def `:81`) ONLY** → `plan = plan_model.<read_standing_plan>(domain, on_date, root)` (the new reader). Add the `from scripts.store import plan_model` import.
  - **QA-W3-05 — the swap-target string is NON-UNIQUE:** the byte-identical line `plan = plan_schema.read_plan(domain, on_date, root)` ALSO appears at **`track.py:66` inside `record_tracking` (def `:34`)** (grounded: `grep -n "plan = plan_schema.read_plan(domain, on_date, root)" scripts/plan/track.py` → both `:66` and `:113`); a naive string-replace would re-point BOTH. Target the swap by line/enclosing-function (`resolve_plan_progress`), NOT a bare string-replace. **`record_tracking`'s `:66` thin read is DELIBERATELY left unchanged** (it reads the thin plan to decide plan-to-track presence + `plan_date`): re-pointing it is a known downstream-consumer follow-on (consistent with Deviation #2's thin-schema-consumer migration), NOT this task's scope — so post-swap `resolve_plan_progress` reports `has_plan=True` on a comprehensive-only store while `record_tracking` still records against only the thin plan (an intentional, documented transitional asymmetry, Call Chain Review Rule). Confirm `tests/plan/test_track.py` exercises `record_tracking` so an accidental BOTH-swap REDs.
  - The tracking read (`plan_schema.read_plan_tracking`, `:114`), the `TRACKED_DOMAINS` guard (`:109`, keep `plan_schema.TRACKED_DOMAINS`), the `has_plan = plan["state"] is None` mapping (`:115`), and the RETURN-DICT keys (`:116-126`) stay BYTE-UNCHANGED — the reader returns the `read_plan` shape, so the downstream mapping is untouched and `adjust.py`/`maintained.py`/`plan_loop.py` read the same keys transitively (0 edits to those consumers — `adjust.py` is a `FROZEN_GLOB` member and MUST NOT be touched).
- The `tests/plan/*` + `tests/runner/*` frozen-guard carve-outs are NOT this task's manifest — the two `track.py`-freezing globs (`test_confirm_pointer_skip.py::FROZEN_GLOB` :40, `test_cadence_runner.py::test_frozen_glob_numstat_empty` :297) are carved by the **SHARED Wave-3 frozen-guard reconciliation step** (build plan Wave 3, owned by neither Wave-3 task — Architect Option-(A) arbitration + `docs/adr/.pipeline/frozen-guard-reconciliation-ruling.md`), which runs BEFORE this task's commit and removes ONLY the `"scripts/plan/track.py"` token (alongside the sibling `orchestrate.py`/`plan_driver.py` tokens for 0043-T2/0046-T1). This task edits ONLY `plan_model.py` (reader) + `track.py` (swap) + `test_plan_model_reader.py` (Create); its Step-4 regression re-runs the carved guards GREEN.

Run: `.venv/bin/python -m pytest tests/store/test_plan_model_reader.py -q`
Expected: PASS (the AC-5 `PRE_TASK_HEAD`-based freeze-numstat probes are pre-commit-runnable and GREEN here — the frozen primitives are untouched; re-confirmed at Step 4).

### Step 2.5: Falsify the store-adversarial battery + PF-S130-01 (POST-GREEN mutation-RED / revert)

The comprehensive-precedence + re-point logic the falsifiers target now EXISTS (GREEN landed), so they are meaningful here (at Step 1 every behavioral test fails on the missing reader — nothing to mutate). For EACH: apply, run the named test, OBSERVE RED, then REVERT (never committed). A test that stays green under its falsifier is tautological and FAILS its AC.

- **(d) — remove the comprehensive-precedence rule:** in the reader, drop/invert the comprehensive-wins branch (always return the thin result) → `test_mixed_history_comprehensive_wins` (AC-1) goes RED (it returns the thin plan, not the comprehensive prescription). Re-run, CONFIRM RED, revert.
- **(pf-s130) — revert the consumer re-point:** in `resolve_plan_progress`, restore `plan = plan_schema.read_plan(domain, on_date, root)` (the pre-swap thin read) → `test_resolve_plan_progress_comprehensive_wins_production_path` (AC-3: returns the thin plan, not the comprehensive prescription) AND `test_resolve_plan_progress_comprehensive_only_not_no_plan` (AC-3b: returns `NO_PLAN`/`has_plan False` on a comprehensive-only store) both go RED — proving the re-point is non-tautological (both distinguish the mixed-history reader from the thin path). Revert.

Run after each revert: `.venv/bin/python -m pytest tests/store/test_plan_model_reader.py -q`
Expected: PASS (every falsifier reverted; the suite is green again). Confirm NO falsifier edit remains in the working tree before Step 3.

### Step 3: REFACTOR

Review for:
- Duplication between the mixed-history reader and `resolve_comprehensive`/`plan_schema.resolve_plan` — the reader COMBINES the two existing resolvers (`read_plan_version` for the comprehensive side, `plan_schema.read_plan` for the thin side); it does NOT re-implement the reversed-scan resolution or the store I/O (append/read reached only through `store`/the existing resolvers). No second copy of the resolution logic.
- The reader returns the `plan_schema.read_plan` shape (`state`/`plan`/`specialist`/`plan_date`) so the `track.py` swap is a single call-site change — confirm no incidental reshape leaked into `resolve_plan_progress`'s downstream mapping or return dict.
- No local `keying` redefinition; the reader appends nothing and defines no key (the store-keying escape vector).
- The import direction stays acyclic (`plan_model` imports `plan_schema`; `plan_schema` does not import `plan_model`).
- Remove any Step-1/Step-2.5 temporary stub/mutation residue.

Run: `.venv/bin/python -m pytest tests/store/test_plan_model_reader.py tests/plan/test_track.py tests/store/test_plan_model.py -q`
Expected: PASS (the reader module + the thin-behavior guard + the T1 model suite all green; no behavior change).

### Step 4: REGRESSION

Run: `.venv/bin/python -m pytest -q`
Expected: PASS at the entry baseline floor — the SAME two pre-existing env-sensitive failures (`test_bind`/`test_server`) + the entry skipped count, PLUS the new `test_plan_model_reader.py` tests passing, with NO new failure. Specifically confirm the reconciled guards are GREEN (the behavioral replacement holds — they are carved by the SHARED Wave-3 reconciliation step, NOT this task): `.venv/bin/python -m pytest tests/plan/test_confirm_pointer_skip.py::test_frozen_spine_numstat_and_read_plan_only_hunk tests/runner/test_cadence_runner.py::test_frozen_glob_numstat_empty tests/plan/test_track.py -q` passes (the two globs now exclude `scripts/plan/track.py`; `test_track.py`'s thin behavior is preserved). A 3rd unexpected failure means the reader/swap reached outside the manifest or a shared-fixture touch had a side effect — do NOT fix unrelated tests; investigate whether the change reached outside the manifest and flag per the feedback protocol.

Then re-check the frozen spine + the per-ADR numstat (based on `PRE_TASK_HEAD`, the fixed entry HEAD — pre-commit-runnable, re-confirmed here against the final working tree/commit):
- `git diff --numstat PRE_TASK_HEAD -- scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/store/store.py scripts/store/keying.py scripts/plan/assemble.py scripts/plan/adjust.py scripts/plan/router.py` → empty (the frozen four + `assemble.py` + `adjust.py`/`router.py` untouched; AC-FS(a) — the sibling-superseded `plan_schema.py`/`orchestrate.py`/`generate_plan.py` are EXCLUDED from the persisted probe per QA-W3-02; their being untouched-by-THIS-commit is covered by the name-only scope check below).
- `git diff --numstat origin/main -- scripts/store/store.py scripts/store/keying.py scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/router.py` → empty (the `<always-frozen>` 6-file HARD glob; AC-FS(b)).
- `git diff --name-only PRE_TASK_HEAD -- scripts/ tests/` → exactly `scripts/store/plan_model.py`, `scripts/plan/track.py`, `tests/store/test_plan_model_reader.py` (no scope creep — the guard carve-outs are the SHARED Wave-3 reconciliation commit's, not this task's).

## Interface Contracts

This task ADDS the mixed-history reader seam to `plan_model` and re-points one consumer; it does NOT change any existing signature.

- `plan_model.<read_standing_plan>(domain, on_date, root) -> dict` — the mixed-history-tolerant reader. Reads the comprehensive `plan-model::` stream (via `read_plan_version`) AND the thin `plan::<domain>` stream (via `plan_schema.read_plan`), applies comprehensive-wins precedence (a comprehensive version standing for `on_date` and covering `domain` wins; otherwise the thin result verbatim), and returns the `plan_schema.read_plan` shape — `{"state": None | NO_PLAN | NO_PLAN_TODAY, "plan": <comprehensive prescription | thin plan | None>, "specialist": str | None, "plan_date": str | None}`. Read-only: it writes nothing, rewrites no thin reading (append-only preserved), and defines no store key. Consumed by **ADR-0044-T3** (`horizons.window_block` resolves a pre-migration thin-only store through this reader — the AC-3 back-compat edge) and, transitively, every `resolve_plan_progress` consumer.
- `plan_model.resolve_comprehensive` / `read_plan_version` — UNCHANGED by this task; the reader COMPOSES `read_plan_version`. **ADR-0044-T4** adds the ADR-0040 confirm-hold skip to the comprehensive RESOLVER in a DISJOINT region (an unconfirmed large-change version resolves not-standing) — it must not change the reader's comprehensive-wins precedence or the `read_standing_plan` signature.
- `track.resolve_plan_progress(domain, on_date, root) -> dict` — SIGNATURE + RETURN-DICT KEYS UNCHANGED (`domain`, `plan`, `specialist`, `plan_date`, `tracking`, `has_plan`, `has_tracking`). The ONLY change is the internal plan-read source (`plan_schema.read_plan` → the `plan_model` reader). Its consumers read through the unchanged keys: **`scripts/plan/adjust.py`** (`adjust_plan` → `adjusted_from` provenance + `has_plan`/`has_tracking` gates — `adjust.py` is `FROZEN_GLOB`-frozen, re-pointed transitively with 0 edits), **`scripts/generate/maintained.py`** (`_fold_tracking_section` str()s `progress["plan"]` — shape-agnostic), **`scripts/serve/plan_loop.py`** (`_adherence_inputs` threads the whole dict). None is edited by this task.
- **Behavioral invariants (the store-surface + frozen-spine guarantees):** the reader rides the UNCHANGED `store.read` (it never writes — 0 thin readings AND 0 comprehensive `plan-model::` versions rewritten on read, append-only preserved SYMMETRICALLY on both streams, SEC-W3-01); comprehensive-wins is the standing-resolution precedence; the thin-only fallback preserves the exact thin resolve semantics (`test_track.py` green); `plan_model.py`'s reader defines NO second dedupe key and calls NO write primitive; the store WRITE primitive + inner engine (`store.py`/`keying.py`/`pipeline.py`/`adjudicate.py`) stay byte-frozen; `track.py`'s delta (the `:113` swap in `resolve_plan_progress` only; `:66` in `record_tracking` deliberately unchanged, QA-W3-05) is the ONLY sanctioned `scripts/` change besides the reader addition.
- **Change control:** ADR-0044-T3/T4 MUST NOT change the `read_standing_plan` signature, the comprehensive-wins precedence, or the `resolve_plan_progress` return-dict keys without Architect + Security review (this task carries QA + Security — the store read surface + the append-only-preserving read contract).

## Verification Checklist

- [ ] AC-1: mixed-history read resolves the latest comprehensive as standing with the comprehensive plan winning over a coexisting thin reading AND 0 thin readings rewritten AND 0 comprehensive `plan-model::` versions rewritten (append-only preserved on BOTH streams, SEC-W3-01) — `test_mixed_history_comprehensive_wins`; Step-2.5 (d) precedence mutation turns it RED, reverted.
- [ ] AC-2: a thin-only store resolves without error via the thin fallback (standing thin plan + absence states, 0 crashes) — `test_thin_only_fallback_resolves`.
- [ ] AC-2b (MUST): `tests/plan/test_track.py` in FULL stays GREEN post-re-point — the thin-behavior regression proof (the thin-only fallback preserves the exact thin resolve semantics + CONTRACTS-TRACK-1 `plan_date`); the behavioral replacement for the `track.py` carve-out.
- [ ] AC-3: `track.resolve_plan_progress` over a MIXED history returns `has_plan True` + the comprehensive prescription (not the thin plan, not `NO_PLAN`), all return-dict keys intact, via the REAL production path — `test_resolve_plan_progress_comprehensive_wins_production_path`; Step-2.5 revert-to-thin turns it RED (PF-S130-01 non-tautology), reverted.
- [ ] AC-3b: a comprehensive-only store (no thin plan for the domain) resolves `has_plan True` with the comprehensive prescription — NOT a `NO_PLAN` on a stored plan — `test_resolve_plan_progress_comprehensive_only_not_no_plan`; RED under the revert-to-thin (returns `NO_PLAN`), reverted.
- [ ] AC-4: four-part store-adversarial battery over the reader — (a) cross-stream disjoint (domain-scoped + the SEC-01 item-`""` decoy), (b) same-timepoint two comprehensive versions readable + latest resolves + idempotent re-record, (c) 0 new keying + 0 write-primitive call in the reader addition (grep, SEC-W3-01), (d) run RED under the NAMED comprehensive-precedence removal then reverted — `test_reader_cross_stream_disjoint` + `test_reader_same_timepoint_latest_comprehensive_resolves` + `test_reader_no_new_keying` + the documented (d) procedure.
- [ ] AC-5 (spec AC-5): `.venv/bin/python -m pytest tests/store/test_plan_model_reader.py` passes (0 live-API, 0 real PII — a `grep` confirms no real operator-identity literal in the test tree).
- [ ] AC-FS (added FROZEN-SPINE gate): per-ADR numstat — the frozen four + `assemble.py` (+ `adjust.py`/`router.py`) == 0 vs `PRE_TASK_HEAD` (the sibling-superseded `plan_schema.py`/`orchestrate.py`/`generate_plan.py` EXCLUDED from the persisted probe per QA-W3-02), the `<always-frozen>` 6-file glob == 0 vs `origin/main`, and (commit-time MANUAL check, NOT persisted) `git diff --name-only PRE_TASK_HEAD -- scripts/` == exactly `plan_model.py` + `track.py` — `test_frozen_spine_numstat_and_scoped_delta` (pre-commit-runnable, QA-06). Maps to the build-plan Wave-3 `<always-frozen> numstat=0` checkpoint.
- [ ] Risk (S37–S41 store-keying escape, bead `pka`) covered by AC-4 (all four, (d) run RED); Risk (PF-S130-01 tautological re-point) covered by AC-3/AC-3b production-path + the revert falsifier; Risk (PF-S131-01 fixture shape) covered by the reused `_comprehensive_version` periodized-prescription builders; Risk (append-only preserved) covered by AC-1's 0-thin-rewritten assertion; Risk (FROZEN-SPINE) covered by AC-FS.
- [ ] Frozen-guard reconciliation (ruling §4/§5 — performed by the SHARED Wave-3 reconciliation step, NOT this task's manifest): `track.py` carved from BOTH `test_confirm_pointer_skip.py::FROZEN_GLOB` + `test_cadence_runner.py::test_frozen_glob_numstat_empty` (ONLY the `track.py` token removed, the `<always-frozen>` HARD set + every non-superseded member intact), all GREEN post-carve; this task's behavioral-replacement deliverable (`test_plan_model_reader.py` + `test_track.py` green + the per-ADR numstat) present; the full set enumerated by EXECUTION (F-011).
- [ ] No regression: `.venv/bin/python -m pytest -q` holds the entry floor (the two known env-reds, no 3rd) with the new tests passing.
- [ ] Files modified match the 3-file manifest (`scripts/store/plan_model.py`, `scripts/plan/track.py`, `tests/store/test_plan_model_reader.py`) — the guard carve-outs are the SHARED Wave-3 reconciliation commit's, NOT this task's; no other scope creep; any Step-2.5 falsifier edit is REVERTED, never committed; `store.py`/`keying.py`/`pipeline.py`/`adjudicate.py`/`adjust.py` byte-unchanged.
- [ ] Interface contract documented (the `read_standing_plan` reader seam + the comprehensive-wins precedence + the `resolve_plan_progress` return-key-preserving swap + the 0044-T3/T4 consumers + the downstream `adjust.py`/`maintained.py`/`plan_loop.py` transitive re-point).

## Deviations

| # | Spec Says | Recipe Says | Reason | Impact |
|---|----------|------------|--------|--------|
| 1 | "`resolve_plan_progress` … re-points … onto the mixed-history reader" — the reader returns "the comprehensive prescription"; the `specialist` attribution + the comprehensive-standing-but-domain-not-covered case are unspecified. | The reader returns the `plan_schema.read_plan` shape (`state`/`plan`/`specialist`/`plan_date`). On the comprehensive-wins branch: `plan` = the covered domain's PRESCRIPTION (`{"blocks":[...]}`), `specialist` = None (a comprehensive plan is orchestrator-synthesized, not single-specialist), `plan_date` = `on_date`. A comprehensive version STANDING but not covering `domain` falls back to the thin result (comprehensive wins only for domains it covers). | SE Ask-vs-Proceed rule 3 (same public interface, pick the simpler internal behavior, state the assumption): the `read_plan`-shape return makes the consumer re-point a pure call-site swap (return-dict keys unchanged); `specialist = None` matches the `str | None` contract; the covered-only precedence keeps a pre-migration thin store + uncovered domains at their exact thin behavior (backward-compatible). None of these is tested against a competing interface by any AC. | No downstream contract change; `resolve_plan_progress`'s keys are preserved; the thin-only fallback is byte-behavior-identical (`test_track.py` green). |
| 2 | ADR-0044-T2 Consequences: "migration of every thin-schema consumer — `track.resolve_plan_progress`, dashboard". | This task re-points ONLY `track.resolve_plan_progress`; the downstream consumers (`adjust.py`, `maintained.py`, `plan_loop.py`) are re-pointed TRANSITIVELY with 0 edits (return-dict keys preserved). The full consumer-SHAPE migration (a consumer reading the comprehensive prescription's `{"blocks":[...]}` instead of the thin `{"exercises":[...]}`) is a documented downstream follow-on, NOT this task's scope. **QUALITY note (Call Chain Review Rule):** once comprehensive plans are actually authored, `adjust.py` would attempt to adjust a comprehensive prescription via the thin `generate_plan` path — semantically incomplete, reachable only in the future comprehensive-plan state (thin-only stores are 100% backward-compatible). | The spec explicitly dispositions the consumer migration as an ADR-0044 known consequence; T2's manifest is the reader + the `track.py` swap only. `adjust.py` is `FROZEN_GLOB`-frozen — editing it here would trip the freeze AND exceed the manifest. | Thin stores keep working unchanged; the comprehensive-consumer-shape work is a tracked follow-on. Flagged so the follow-on is visible, not silently deferred. |

**UPSTREAM FLAG: BUILD-PLAN — incomplete enumeration (NON-BLOCKING)**
- Source: `docs/build-plan/build-plan-adr-0041-0046.md` line 21 (the AMENDED per-wave frozen-guard-reconciliation rule's "Superseded surfaces + their prior guards:" list).
- Issue: the explicit list names `orchestrate.py (Wave 3, 0043-T2)` + `plan_schema.py (Wave 2, 0044-T1)` but OMITS `track.py (Wave 3, 0044-T2)` and `plan_driver.py (Wave 3, 0046-T1)`, though both are frozen by `test_confirm_pointer_skip.py::FROZEN_GLOB` + `test_cadence_runner.py::test_frozen_glob_numstat_empty` and are superseded in Wave 3.
- Impact on recipe: none — the STANDING RULE on line 21 ("for each surface a wave supersedes, extend the carve-out in EVERY prior guard that freezes it") self-evidently covers `track.py`; the recipe relies on the rule + the F-011 execute-to-enumerate procedure, not the illustrative list. The orchestrator has already dispositioned `track.py`'s carve as the SHARED Wave-3 reconciliation (Option-A).
- Recommended action: add `track.py (Wave 3, 0044-T2)` + `plan_driver.py (Wave 3, 0046-T1)` to the line-21 enumeration so the SHARED Wave-3 reconciliation commit's carve set is explicit.
- Blocking: No.

## Commit

```
feat(store): mixed-history reader + track re-point
```
Stage only: `scripts/store/plan_model.py`, `scripts/plan/track.py`, `tests/store/test_plan_model_reader.py` (the `track.py` guard carve-outs — the two frozen globs — are the SHARED Wave-3 frozen-guard reconciliation commit's, owned by neither Wave-3 task; this task stages NO guard file).

(Stage with `git add <the three paths>` as its OWN command, then `git commit` separately — the PII commit hook denies single-call stage+commit, `git commit -a`, and pathspec `git commit <path>`. Confirm NO Step-2.5 falsifier edit — to the comprehensive-precedence branch or the `resolve_plan_progress` swap — and NO daemon vault-churn / parked `??` file remains staged, and `store.py`/`keying.py`/`pipeline.py`/`adjudicate.py`/`adjust.py` are byte-unchanged, before committing. The per-ADR numstat probes (`git diff --numstat PRE_TASK_HEAD …`) are pre-commit-runnable and re-confirmed against the final commit.)

## Rollback

If verification fails after 3 fix cycles:
1. `git stash -m "failed-ADR-0044-T2-attempt-{N}"` — preserve the work for diagnosis.
2. `git checkout {commit-before-task}` — revert to the entry-state HEAD recorded in Entry State (also reverts any un-reverted Step-2.5 falsifier edit — the comprehensive-precedence branch or the `resolve_plan_progress` swap).
3. Update the issue tracker: flag ADR-0044-T2 as BLOCKED with the failure log.
4. Escalate: "Task ADR-0044-T2 failed verification after 3 attempts. Stash ref: {ref}. Failure log: {summary}." ADR-0044-T2 is the zero-slack critical-path task that ADR-0044-T4 (the confirm-hold skip rebases on this reader's resolver) and ADR-0044-T3 (thin-only back-compat resolves via this reader) depend on — a BLOCK halts the comprehensive-model resolver line at the Wave 3 → Wave 4 boundary; surface it immediately, do not start any downstream task.
5. Stash cleanup: after abandonment or re-planning, drop all `failed-ADR-0044-T2` entries from `git stash list`.

Do NOT: silently skip a failing acceptance criterion; weaken a test to make it pass (especially the AC-4 store-adversarial mutation-RED battery, the AC-3/AC-3b production-path revert falsifier, or the AC-5 per-ADR numstat — these ARE the store-surface + frozen-spine deliverable); edit any frozen guard here (the `track.py` carve-outs are the SHARED Wave-3 reconciliation commit's, not this task's — this task stages NO guard file); or proceed to any downstream task with the reader/re-point in a broken state.
