---
task-id: ADR-0052-T2
source-spec: docs/spec/adr-0052-widen-roster-spec.md
source-build-plan: docs/build-plan/build-plan-adr-0052.md
wave: 1
assigned-agent: SE
reviewers: [Architect, QA]
created: 2026-07-19
status: approved
---

# Recipe: Raise the `active_plan_domains` floor to the always-on ten, UNCONDITIONALLY

## Entry State

**Prerequisites:**
- [ ] No predecessor tasks — ADR-0052-T2 is a Wave-1 entry point (Dependencies: None). File-disjoint from and fully parallel to ADR-0052-T1 (which owns `scripts/model/client.py` + `tests/model/test_client.py`). T2 owns `scripts/plan/activation.py`, `scripts/serve/plan_loop.py`, `tests/serve/test_plan_loop.py`, `tests/serve/test_orchestrator_synthesize.py`.
- [ ] Full suite green at the session baseline (record counts; the 2 known env-floor reds are not this task's concern). If any OTHER test fails before you change anything, stop and fix that first.
- [ ] Frozen-six + `track.py` byte-frozen: `git diff --numstat 3ab1c3abb6c995fbaaadcb179735759e4a61d73d -- scripts/store/store.py scripts/store/keying.py scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/router.py scripts/plan/track.py` prints NOTHING.
- [ ] Working tree clean for the four manifest files.
- [ ] On the Wave-1 feature branch (cut from `main`).

Rollback target = this entry state: the four files unmodified, frozen surface empty.

## Context Load List

**Files to read:**
- `scripts/plan/activation.py` — the modify target. Read `CARD_DOMAINS` (@26-40, the §1-§13 frozenset; §1-§10 = the always-on ten, §11-§13 = `dermatology`/`gi`/`lymphatic` progressive), `CROSS_CUTTING_INPUTS` (@45, `{genetics, labs}`), the partition tripwire assert (@51-53, the pattern the new load-time assert mirrors), and `active_domains` (@69-89). This is a LEAF module (pure literals + set ops, NO imports) — keep it so.
- `scripts/serve/plan_loop.py` — the modify target. Read `active_plan_domains` (@222-236): its docstring (describes the renderable-core-four floor — must be rewritten) and the CONDITIONAL floor idiom `if not (active & set(plan_schema.RENDERABLE_DOMAINS)): active |= set(plan_schema.RENDERABLE_DOMAINS)` (@233-235 — the exact lines T2 replaces). Note the module already imports `plan_schema`; it will also need `from scripts.plan import activation` (grep whether `activation` is already imported before adding).
- `tests/serve/test_plan_loop.py` — the modify target. Read the existing `active_plan_domains` tests for the assertion + fixture style (how a summary is built and projected through `derive_operator_surface`).
- `tests/serve/test_orchestrator_synthesize.py` — the modify target (the coupled-test migration). Read `test_rich_only_surface_floors_to_renderable_core` (@382-391) and `test_front_door_derives_surface_then_dispatches_active_subset` (@367-379) — both pin the PRE-T2 conditional-floor behavior and BOTH pass GREEN today; T2 changes the behavior they assert, so both must be migrated to the ten-domain floor (else the suite cannot stay green — spec T2 AC-7).

**Do NOT load:** the ADRs (TP-02 drift), the frozen surface, `scripts/model/client.py` (T1's file), `scripts/serve/server.py` / `scripts/serve/care_chat.py` (NOT modified — the synthesize leg already carries the full active set; spec Stale-Premise-Reconciled).

## Risk Pre-Check

- **Risk (ADR-0052 Consequence-Negative — the floor must be raised WITHOUT zeroing an existing operator's plan, and UNCONDITIONALLY):** gated by AC-1 (signal-less floor never zeroes → ⊇ ten), AC-2 (partial-signal floor ⊇ ten — the AR-006 load-bearing case), AC-4 (the conditional-idiom RED). A conditional floor that fires only on no-renderable-signal returns len-1 for a workout-only surface (the silent regression a signal-less-only probe misses).
- **Risk ("moderate-reversibility domain vocabulary"):** gated by AC-6 — `ALWAYS_ON_DOMAINS` pinned as the single-source ten + the exact progressive-three complement.
- **Risk (honesty-of-activation — §11-§13 stay progressive; the floor fabricates no empty-state derm/gi/lymphatic card, Alternative D rejected):** gated by AC-5 — a signal-less surface floors to EXACTLY the ten, none of `{dermatology, gi, lymphatic}`.
- **Risk (frozen ADR-0032 six + `track.py`):** gated by AC-8 — the change lands only in the four T2 files.

## TDD Steps

### Step 1: RED — Write Failing Tests
Add to `tests/serve/test_plan_loop.py`:

- **AC-1 (signal-less floor ⊇ ten, never zeroes):** a summary whose `derive_operator_surface` yields no active card domain → assert `set(activation.ALWAYS_ON_DOMAINS) <= active_plan_domains(summary)`.
- **AC-2 (partial-signal floor ⊇ ten — the AR-006 probe):** a summary touching only `workout` → assert all ten always-on domains present and `len(active_plan_domains(summary)) >= 10`. This is the load-bearing case the CONDITIONAL idiom fails (`{workout} & RENDERABLE` non-empty ⇒ the conditional floor does not fire ⇒ returns `{workout}`).
- **AC-3 (all-signal floor ⊇ ten):** a summary touching every card domain → the ten always present (progressive §11-§13 may additionally appear).
- **AC-4 (conditional-idiom RED / non-tautological):** assert the CLEAN direction (partial-signal ⊇ ten) truthy, then prove RED-capability — reverting the floor to the conditional `if not (active & RENDERABLE): active |= …` makes the partial-signal (workout-only) case return `{workout}` (len 1). Implement the mutation as a local monkeypatch of the floor logic OR a documented scratch-revert probe whose RED is recorded; the test is invalid unless the conditional idiom REDs it.
- **AC-5 (floor adds no progressive domain):** a signal-less surface → `active_plan_domains(summary) == set(activation.ALWAYS_ON_DOMAINS)` exactly (contains NONE of `{dermatology, gi, lymphatic}`).
- **AC-6 (`ALWAYS_ON_DOMAINS` partition):** assert `activation.ALWAYS_ON_DOMAINS <= activation.CARD_DOMAINS`, `activation.ALWAYS_ON_DOMAINS.isdisjoint(activation.CROSS_CUTTING_INPUTS)`, `activation.CARD_DOMAINS - activation.ALWAYS_ON_DOMAINS == {"dermatology", "gi", "lymphatic"}`, and `len(activation.ALWAYS_ON_DOMAINS) == 10`.

Run: `.venv/bin/python -m pytest tests/serve/test_plan_loop.py -q`
Expected: FAIL — `activation.ALWAYS_ON_DOMAINS` does not exist (`AttributeError`) and the partial-signal case returns len-1 under the current conditional floor.

### Step 2: GREEN — Implement

**`scripts/plan/activation.py`** — add `ALWAYS_ON_DOMAINS` as a named frozenset after `CARD_DOMAINS` (keep the leaf-module no-imports property), with a SUBSTANTIVE load-time assert (NOT the tautological `isdisjoint(CARD_DOMAINS - ALWAYS_ON_DOMAINS)` — any set is disjoint from its own relative complement; it guards nothing):
```
# The §1-§10 ALWAYS-ON card domains: the non-progressive set every comprehensive plan authors
# (ADR-0052). §11-§13 (dermatology/gi/lymphatic) stay progressive — activated only on a surface
# signal (`active_domains`), never floored. The single source of the always-on vocabulary.
ALWAYS_ON_DOMAINS = frozenset({
    "workout", "nutrition", "peptides", "supplements", "endocrine",
    "cardiovascular", "recovery", "sleep", "longevity", "mental-performance",
})
assert ALWAYS_ON_DOMAINS <= CARD_DOMAINS, ...
assert len(ALWAYS_ON_DOMAINS) == 10, ...
assert CARD_DOMAINS - ALWAYS_ON_DOMAINS == frozenset({"dermatology", "gi", "lymphatic"}), (
    "the progressive three (§11-§13) must be exactly CARD_DOMAINS minus the always-on ten"
)
```

**`scripts/serve/plan_loop.py`** — in `active_plan_domains`:
- Add `from scripts.plan import activation` if not already imported (grep first).
- Replace the conditional floor `if not (active & set(plan_schema.RENDERABLE_DOMAINS)): active |= set(plan_schema.RENDERABLE_DOMAINS)` with the UNCONDITIONAL `active |= set(activation.ALWAYS_ON_DOMAINS)`.
- Rewrite the docstring: the floor is now the always-on ten UNCONDITIONALLY (every surface floors to ⊇ the ten — the baseline comprehensive plan), progressive §11-§13 add only on a surface signal; the result never zeroes and never fabricates a progressive card.

Run: `.venv/bin/python -m pytest tests/serve/test_plan_loop.py -q`
Expected: PASS.

### Step 3: Migrate the two coupled pre-T2 floor tests (spec T2 AC-7)
In `tests/serve/test_orchestrator_synthesize.py`:
- **`test_rich_only_surface_floors_to_renderable_core` (@382-391):** the rich-only surface (`{"genetic-trait-classes": "genetics"}`, no active card domain) now floors to the always-on TEN, not the four. Update the assertion `active_plan_domains(summary) == set(plan_schema.RENDERABLE_DOMAINS)` → `== set(activation.ALWAYS_ON_DOMAINS)`, and repoint the stale RED-capability comment (`drop the active |= RENDERABLE_DOMAINS floor …`) to the unconditional idiom. Keep the `activation.active_domains(surface) == frozenset()` pre-assert (still true — the surface carries no card signal; the FLOOR, not the surface, supplies the ten). Consider renaming to `…floors_to_always_on_ten` for honesty (optional).
- **`test_front_door_derives_surface_then_dispatches_active_subset` (@367-379):** under the unconditional floor the `{workout, sleep}` surface floors to the ten, so the front door dispatches the widened set. Update the `dispatched == {"workout", "sleep"}` assertion to the correct post-T2 dispatched set (the always-on ten's renderable + rich author calls, per how the fixture accounts dispatch). Run it, read the actual dispatched set, and assert that superset — do NOT weaken to a bare `>=`-nothing; assert the exact expected widened set. Update the AC-S2 comment to reflect the floor.

Run: `.venv/bin/python -m pytest tests/serve/test_orchestrator_synthesize.py -q`
Expected: PASS (both migrated).

### Step 4: REFACTOR
- `activation.py` stays a leaf module (the new assert uses only literals + set ops). `plan_loop.py`'s new `activation` import forms no cycle (activation imports nothing).
- Confirm no other call site depended on the old conditional-floor semantics: `rg -n 'active_plan_domains' scripts tests` — every consumer must tolerate the widened set.

Run: `.venv/bin/python -m pytest tests/serve/test_plan_loop.py tests/serve/test_orchestrator_synthesize.py -q`
Expected: PASS.

### Step 5: REGRESSION
Run: `.venv/bin/python -m pytest -q`
Expected: PASS — 0 NEW failures vs baseline. Watch specifically for any OTHER test that asserted the pre-T2 floor's narrowness (the `rg` in Step 4 should have found them); if one REDs, it is a coupled test the migration missed — add it to this task's manifest and migrate it (do NOT weaken it).

## Interface Contracts

`active_plan_domains(summary)` keeps its signature (`summary: dict` → `set[str]`); its POSTCONDITION widens: the returned set is now always a superset of the always-on ten (was: ⊇ the four only when no renderable signal). **Breaks-if:** any consumer that assumed a minimal/narrow active set (e.g. `== {one domain}`) breaks — the two `test_orchestrator_synthesize.py` tests are exactly those consumers, migrated here; the Step-4 `rg` sweep is the guard against a missed one. `activation.ALWAYS_ON_DOMAINS` is a new module-public frozenset — the single source of the always-on vocabulary; T3's coherence AC (`set(ALWAYS_ON_DOMAINS) == set(client._AUTHOR_CONTRACT_SECTION)`) reads it. The DOWNSTREAM edge `ADR-0052-T2 → ADR-0052-T3`: T2's floor makes `active` carry the ten so `care_chat.synthesize(active, …)` computes `rich = active − RENDERABLE ⊇ {§5-§10}` and authors them. No `server.py`/`care_chat.py` edit.

## Verification Checklist
- [ ] **AC-1** signal-less floor ⊇ ten (never zeroes).
- [ ] **AC-2** partial-signal (workout-only) floor ⊇ ten, `len >= 10` (AR-006).
- [ ] **AC-3** all-signal floor ⊇ ten.
- [ ] **AC-4** conditional-idiom mutation REDs AC-2 (RED-capability recorded).
- [ ] **AC-5** signal-less floor == the ten exactly (no §11-§13 fabrication).
- [ ] **AC-6** `ALWAYS_ON_DOMAINS` partition asserts (⊆ CARD, disjoint CROSS_CUTTING, complement == the progressive three, len 10).
- [ ] **AC-7** the two `test_orchestrator_synthesize.py` floor tests migrated to the ten-domain floor; `.venv/bin/python -m pytest tests/serve/test_orchestrator_synthesize.py -q` passes.
- [ ] **AC-8** `git diff --numstat 3ab1c3ab -- <six + track.py>` prints nothing; `.venv/bin/python -m pytest tests/serve/test_plan_loop.py tests/serve/test_orchestrator_synthesize.py -q` passes; `.venv/bin/python -m pytest -q` 0 NEW failures.
- [ ] Files modified match the manifest — `git diff --name-only` shows the two production files (`activation.py`, `plan_loop.py`) + this recipe + the coupled test files the Step-5 regression sweep surfaced: `test_plan_loop.py` (T2 floor probes), `test_orchestrator_synthesize.py` (4 migrated floor tests), `test_plan_loop_hold.py` (frozen-spine `changed_defs` carve), `test_server.py` (author reached per always-on domain), `test_cadence_runner.py` (cadence dispatches the floored ten), `test_large_change_fraction.py` (the FLAGGED large-change-hold interaction). Eight test files total — the floor is load-bearing across the plan pipeline; the wide coupled-test surface is expected for a core-behavior change, all migrated (none weakened).
- [ ] **FLAGGED for QA/Architect review** — the ADR-0052-T2 ↔ ADR-0046-T2 large-change-hold interaction: post-T2 `|renderable|` is always 4 (the floor makes all four renderable active), so a narrow operator's 2-domain change is a 2-of-4 minority that no longer trips the confirmation hold (pre-T2 it was a 2-of-2 majority that HELD). Resolved accept-and-document (consistent with the strict-majority rule; the comprehensive plan genuinely carries 4 renderable). Confirm no safety mitigation (e.g. a signalled-renderable denominator) is required, or route an ADR amendment.

## Commit
```
feat(plan): floor the plan roster at the always-on ten
```
Stage only the T2 manifest files + this recipe.
Body (why): the active-domain floor is now the always-on ten UNCONDITIONALLY (a partial-signal operator floors to the full ten, not one) via the single-source `activation.ALWAYS_ON_DOMAINS`; the two orchestrator-synthesize tests that pinned the old conditional four-floor are migrated. The progressive three stay progressive — the floor fabricates no empty-state card. The activation half of the four→ten roster widening.

## Rollback
If verification fails after 3 fix cycles: `git stash -m "failed-ADR-0052-T2"`, `git checkout main -- scripts/plan/activation.py scripts/serve/plan_loop.py tests/serve/test_plan_loop.py tests/serve/test_orchestrator_synthesize.py`, confirm the frozen numstat probe still prints nothing, flag ADR-0052-T2 BLOCKED. Do NOT use the tautological `isdisjoint` assert, weaken the conditional-idiom RED, or add a §11-§13 slug to `ALWAYS_ON_DOMAINS`.

## Deviations
None expected — the floor is a two-line change over a grounded conditional idiom, `ALWAYS_ON_DOMAINS` is a literal frozenset, and the two coupled tests are known + named for migration (spec T2 AC-7).
