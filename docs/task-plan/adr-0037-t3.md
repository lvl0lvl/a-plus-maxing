---
task-id: ADR-0037-T3
source-spec: docs/spec/adr-0036-0038-dynamic-plan-loop-spec.md
source-build-plan: docs/build-plan/build-plan-adr-0036-0038.md
wave: 7
assigned-agent: SE
reviewers: [QA, Security]
created: 2026-07-03
status: draft
---

# Recipe: Load-Time Tripwire + Crown-Jewel Wire-Scan Non-Egress + ADR-0001 Egress Amendment

One-line summary: prove the tailoring pass never leaks — add the import-time `SUMMARY_FIELD_SET`-disjointness tripwire on the tailoring keys, wire-scan that no tailoring content reaches the store or the de-id dashboard, keep tailored sections gitignored-artifact-only, record tailoring as a named egress sibling in ADR-0001, and pre-ship-check the reinserted-name commit/push scan.

## Entry State

**Prerequisites:**
- [ ] Task ADR-0037-T1 complete (committed): `scripts/plan/tailoring.py` exists (the pass + its tailored-section key set), `scripts/generate/maintained.py` accepts tailored sections at the `reemit_maintained` boundary, `scripts/serve/plan_loop.py` fills the post-promote hook, `tests/plan/test_tailoring.py` exists. Verify: `test -e scripts/plan/tailoring.py && test -e tests/plan/test_tailoring.py && echo OK`
- [ ] Task ADR-0037-T2 complete (committed): `scripts/plan/tailoring.py` carries the dosing-token reject + the deterministic fail-closed interaction screen (the full tailoring path the wire-scan runs). Verify: `grep -q "rx_interaction_class_set" scripts/plan/tailoring.py && echo OK` (T2 grounds the screen on `router.rx_interaction_class_set`)
- [ ] Wave 6 → Wave 7 checkpoint RAN green (per `docs/build-plan/build-plan-adr-0036-0038.md` § Wave 6 → Wave 7 Boundary — dosing-reject + paired interaction-screen control + deterministic-split-off-model + fail-closed-never-silent-drop all green). This is the build-plan Wave-7 gate: no Wave-7 task starts until the prior wave's checkpoint Go/No-Go passed.
- [ ] Full test suite passes at the captured floor: `.venv/bin/python -m pytest -q` → `2150 passed, 2 failed, 7 skipped` (the two failures — `tests/serve/test_bind.py::test_port_collision_exits_nonzero_fail_loud` and `tests/serve/test_server.py::test_main_reads_no_stdin` — are PRE-EXISTING and env-sensitive, NOT this task's to fix; a 3rd failure is a regression, HALT). Counts move upward as T1/T2 added tests — the gate is "no NEW failure vs the wave-entry baseline", capture the exact live counts at entry.
- [ ] Working tree clean: `git status` shows no changes (ignore the daemon's `vault/**` frontmatter churn per CLAUDE.md § Vault / daemon hygiene — never `git add vault/` for this task)
- [ ] On feature branch: `feature/dynamic-plan-loop-tailoring` (or the wave's short-lived `feature/*` branch off `main`)

## Context Load List

**Files to read:**
- `scripts/plan/tailoring.py` — the pass this task audits and adds the import-time tripwire to; read its tailored-section key set (the keys the tripwire asserts disjoint from `SUMMARY_FIELD_SET`) and its `reemit_maintained` render path.
- `docs/adr/ADR-0001-pii-trust-boundary-no-train-routing.md` — the egress-list Related-Decisions table (rows for ADR-0016/0032/0035) this task amends with the tailoring carve-out + the `amended-by (from ADR-0037)` edge.
- `tests/plan/test_tailoring.py` — the existing T1/T2 test file this task extends with the tripwire probe, the wire-scan probe, the artifact-only probe, and the reinserted-name pre-ship check.

**Do NOT load:**
- ADR-0037 / ADR-0021 / ADR-0034 / the design doc (`design/dynamic-plan-loop-design.md`) — the spec already distilled the crown-jewel-set clauses and finding E/F; re-loading them re-introduces upstream framing the spec captured.
- `scripts/plan/orchestrate.py`, `scripts/serve/care_chat.py` — the hold sets and raw care-lane read are T1's concern, already built; this task does not modify the pass's emit-gate or presentation.
- `scripts/plan/router.py` / `scripts/serve/capture.py` beyond confirming the `SUMMARY_FIELD_SET` symbol and the `WIRED_TOKENS ⊆ SUMMARY_FIELD_SET` / `router` disjointness-assert precedent the tripwire mirrors — do not modify these owners; the tripwire lives in `tailoring.py`.

Note (not a load): the wire-scan and reinserted-name tests IMPORT existing production code to produce and scan payloads — the de-id dashboard render `scripts/generate/render_views.render_views`, the store read, and the commit/push scan `scripts/guard/pii_scan.scan_scoped`/`scan_text_full` + `DATA_BEARING_PREFIXES`. These are test-only imports of already-built, unmodified modules (the `tests/serve/test_care_chat.py` wire-scan pattern), NOT production changes — no manifest expansion, no TP-06 scope creep.

## Risk Pre-Check

**BLOCKING — this is the terminal crown-jewel egress-surface audit task.** The spec task block carries a non-empty Risk Mitigations field; every mitigation below must be covered by a test that REDs on violation before any production/doc change is written. A placeholder (`assert True`) fails this gate. The four checkpoint invariants (front-door-binding, no-new-store-schema, crown-jewel 0-leak, MOCK/FIXTURE 0-live-spend) are checkpoint invariants on this wave: no test opens a network socket or makes a live model/Agent-tool call; 0 real operator PII in the test tree (synthetic seeds only).

- **Risk — ADR-0037 §5 (a tailoring key masquerading as a planner token), covered by AC-1.** Gate: a test must exist that adds a tailoring-artifact key to a `SUMMARY_FIELD_SET` copy and confirms the disjointness guard RAISES. If the guard is absent, the RED phase adds both the guard (in `tailoring.py`) and the test. The test must go RED when the tailoring key IS a member (the injected-violation case) and GREEN when it is not.
- **Risk — ADR-0037 Consequences-Negative-1 (a SECOND raw-egress surface to audit), covered by AC-2 + AC-3.** Gate: a wire-scan test must exist that seeds a synthetic raw-PII token (legal name + raw drug name) into the raw intake, runs the FULL tailoring path (incl. the T2 interaction screen), dumps BOTH the store streams AND the de-id dashboard payload, and asserts 0 raw-PII hits AND 0 tailoring-artifact-key hits in either. Non-tautology: the probe must RED when a tailoring key is injected into the dumped payload (proves it can still fail).
- **Risk — ADR-0037 finding E/F (no raw store stream; named egress), covered by AC-2 + AC-4.** Gate: (a) the wire-scan (above) proves 0 tailoring content in any store stream (finding E — no `plan-tailor::` stream); (b) a grep test must confirm ADR-0001's egress list carries the tailoring carve-out + the `amended-by (from ADR-0037)` edge (finding F — named egress). The grep test REDs until the doc edit lands.
- **Risk — ADR-0037 OQ-3 / RT-01 (the reinserted-name scan residual), covered by AC-5.** Gate: a pre-ship test must feed a maintained artifact carrying the REINSERTED operator NAME (not only contact tokens) under `vault/artifacts/generated/` to the commit/push PII scan and assert it is DENIED (via the `DATA_BEARING_PREFIXES` coverage, design finding J closed). Paired control: a de-identified artifact under the same prefix PASSES the scan — a scan that denies both or neither fails the gate.

## TDD Steps

### Cycle 1: Load-Time Tripwire (AC-1) — Steps 1-4

#### Step 1: RED — Write Failing Tests
Call `/write-tests` for `tests/plan/test_tailoring.py` targeting:
- AC-1: adding a tailoring-artifact key to a `SUMMARY_FIELD_SET` copy makes the import-time disjointness assert RED — the test constructs the disjointness check over the tailoring key set plus an injected `SUMMARY_FIELD_SET` member and confirms it RAISES; the paired non-violation case (the real, disjoint key set) does NOT raise.

Run: `.venv/bin/python -m pytest tests/plan/test_tailoring.py -q`
Expected: FAIL (the tailoring-artifact key set is not yet exposed as a named constant and no import-time disjointness guard exists to exercise).

#### Step 2: GREEN — Implement
Changes to make:
- `scripts/plan/tailoring.py`: expose the tailored-section keys the pass emits as a named module-level constant (e.g. the tailoring-artifact key set T1 injects at the `reemit_maintained` boundary), and add a module-load-time assert that this set is disjoint from `router.SUMMARY_FIELD_SET`, mirroring the `capture.py` `set(WIRED_TOKENS) <= set(SUMMARY_FIELD_SET)` tripwire (`scripts/serve/capture.py:283`) and the `router` `set(SUMMARY_FIELD_SET).isdisjoint(...)` change-control asserts (`scripts/plan/router.py:667-668`). The assert reds at import if any tailoring key is a `SUMMARY_FIELD_SET` member, so a tailoring key can never masquerade as a planner token `summarize`/`dispatch` would carry across the de-id boundary (satisfies AC-1). No behavior change to the pass's emit-gate, presentation, dosing-reject, or screen — this is a load-time guard only.

Run: `.venv/bin/python -m pytest tests/plan/test_tailoring.py -q`
Expected: PASS.

#### Step 3: REFACTOR
Review implementation for:
- Duplication with the `capture.py:283` and `router.py:667-668` tripwire idiom — match its assert shape and its inline change-control comment style (a future field-set edit that pulls a tailoring key in reds this at load), do not invent a new enforcement pattern.
- Naming consistency: the tailoring-artifact key constant name should read alongside `WIRED_TOKENS` / `SUMMARY_FIELD_SET` conventions.
- No import cycle introduced by importing `SUMMARY_FIELD_SET` from `router` into `tailoring.py`.

Run: `.venv/bin/python -m pytest tests/plan/test_tailoring.py -q`
Expected: PASS (no behavior change).

#### Step 4: REGRESSION
Run: `.venv/bin/python -m pytest -q`
Expected: PASS — no NEW failure vs the wave-entry baseline (still the two known env-sensitive `test_bind`/`test_server` failures; a 3rd = regression, HALT).

### Cycle 2: Crown-Jewel Wire-Scan + Artifact-Only + Egress Amendment + Reinserted-Name Check (AC-2, AC-3, AC-4, AC-5) — Steps 5-8

#### Step 5: RED — Write Failing Tests
Call `/write-tests` for `tests/plan/test_tailoring.py` targeting:
- AC-2 (crown-jewel wire-scan non-egress): seed a synthetic raw-PII token (a legal name + a raw drug name) into the raw intake fixture, run the FULL tailoring path (through the T2 interaction screen), dump BOTH the store streams AND the de-id dashboard payload (importing the unmodified `scripts/generate/render_views.render_views` + the store read, per the `tests/serve/test_care_chat.py` wire-scan pattern), and assert the raw tokens AND every tailoring-artifact key are ABSENT from both — count of raw-PII hits AND tailoring-key hits == 0. Non-tautology guard: assert the probe REDs when a tailoring key is injected into the dumped payload.
- AC-3 (artifact-only): assert the tailored sections appear ONLY in the `maintained` artifact under `vault/artifacts/generated/` (via `reemit_maintained`) — count of tailored sections in any tracked/committed render (the de-id dashboard payload) == 0.
- AC-4 (ADR-0001 egress amendment): a grep-style test over `docs/adr/ADR-0001-pii-trust-boundary-no-train-routing.md` finds the tailoring presentation carve-out entry AND the `amended-by (from ADR-0037)` edge.
- AC-5 (reinserted-name pre-ship check): feed a maintained artifact carrying the REINSERTED operator NAME (not only contact tokens) under `vault/artifacts/generated/` to the commit/push PII scan (`scripts/guard/pii_scan.scan_scoped`/`scan_text_full` + `DATA_BEARING_PREFIXES`) and assert it is DENIED; paired control — a de-identified artifact under the same prefix PASSES.

Run: `.venv/bin/python -m pytest tests/plan/test_tailoring.py -q`
Expected: FAIL — AC-4 fails (the carve-out/edge is not yet in ADR-0001); AC-2/AC-3/AC-5 fail on their injected-violation / paired-control assertions until the probes are wired and confirmed to RED-on-violation, GREEN-on-real-code.

#### Step 6: GREEN — Implement
Changes to make:
- `docs/adr/ADR-0001-pii-trust-boundary-no-train-routing.md`: add a Related-Decisions row for ADR-0037 as `amended-by` — a NEW named raw-egress class (the care-agent plan-tailoring presentation call rendered ONLY to the gitignored `vault/artifacts/generated/` maintained artifact), described as a sibling of the ADR-0016 / ADR-0032 / ADR-0035 named-egress extensions on the SAME de-identification discipline, stating the persisted-side-de-identified invariant is UNAMENDED (nothing raw reaches the store or a tracked render); add the corresponding Revision-History entry (satisfies AC-4). No production-code change in this step — AC-2/AC-3/AC-5 assert properties the already-committed T1/T2 code provides; their tests pass against the real, unmodified tailoring path once the probes (Step 5) are correct.

Run: `.venv/bin/python -m pytest tests/plan/test_tailoring.py -q`
Expected: PASS (AC-2 0-hit wire-scan, AC-3 artifact-only, AC-4 doc grep, AC-5 deny-on-reinserted-name + pass-on-de-identified).

#### Step 7: REFACTOR
Review implementation for:
- Duplication with the `tests/serve/test_care_chat.py` wire-scan helper — reuse the existing dump/scan helper shape rather than re-implementing the payload walk.
- The ADR-0001 row's language matches the ADR-0032 / ADR-0035 sibling rows' structure (relationship label, de-association/containment note, inverse-edge note) — consistency with the established egress-extension convention.
- No test reads a real operator artifact — every seed is synthetic; confirm 0 real PII in the test tree.

Run: `.venv/bin/python -m pytest tests/plan/test_tailoring.py -q`
Expected: PASS (no behavior change).

#### Step 8: REGRESSION
Run: `.venv/bin/python -m pytest -q`
Expected: PASS — no NEW failure vs the wave-entry baseline (the two known env-sensitive failures remain; a 3rd = regression, HALT).

## Interface Contracts

This task does not create or modify shared interfaces. The import-time tripwire added to `scripts/plan/tailoring.py` is an internal module-load guard (no caller consumes it); ADR-0037-T3 is the terminal task in the dependency map (no downstream task consumes its output — the edges `ADR-0037-T1 --> ADR-0037-T3` and `ADR-0037-T2 --> ADR-0037-T3` are inbound only). The ADR-0001 edit is a documentation amendment, not a code interface.

## Verification Checklist

- [ ] AC-1: adding a tailoring key to a `SUMMARY_FIELD_SET` copy REDs the import-time assert — verified by the Cycle-1 tripwire test (`tests/plan/test_tailoring.py`, the raise-on-injected-member case + the no-raise paired case)
- [ ] AC-2: a synthetic raw-PII token + every tailoring-artifact key are ABSENT from both the store streams and the de-id dashboard payload after the full tailoring path (0 hits) — verified by the Cycle-2 wire-scan probe (with the inject-key RED-on-violation guard)
- [ ] AC-3: tailored sections appear ONLY in the gitignored `vault/artifacts/generated/` maintained artifact (0 in any tracked/committed render) — verified by the Cycle-2 artifact-only probe
- [ ] AC-4: ADR-0001 carries the tailoring carve-out entry + the `amended-by (from ADR-0037)` edge — verified by the Cycle-2 grep test over `docs/adr/ADR-0001-pii-trust-boundary-no-train-routing.md`
- [ ] AC-5: a maintained artifact carrying the reinserted operator NAME under `vault/artifacts/generated/` is DENIED by the commit/push PII scan (paired: a de-identified artifact passes) — verified by the Cycle-2 reinserted-name pre-ship test
- [ ] AC-6: `.venv/bin/python -m pytest tests/plan/test_tailoring.py` passes against synthetic PII seeds (0 live calls, 0 real operator PII in the test tree)
- [ ] Risk ADR-0037 §5 mitigation covered (AC-1); Consequences-Negative-1 covered (AC-2 + AC-3); finding E/F covered (AC-2 + AC-4); OQ-3/RT-01 covered (AC-5)
- [ ] No regression in full test suite (no NEW failure vs the wave-entry baseline)
- [ ] Files modified match the spec file manifest (no scope creep): `scripts/plan/tailoring.py`, `docs/adr/ADR-0001-pii-trust-boundary-no-train-routing.md`, `tests/plan/test_tailoring.py`
- [ ] Interface contracts documented (the explicit "no shared interfaces" statement)

## Commit

```
feat(tailoring): load-time tripwire + wire-scan non-egress + ADR-0001 egress amendment
```
Stage only (with `git add` as its own command, then `git commit` separately — per CLAUDE.md § Hooks commit-sequencing rule):
- `scripts/plan/tailoring.py`
- `docs/adr/ADR-0001-pii-trust-boundary-no-train-routing.md`
- `tests/plan/test_tailoring.py`

Do NOT stage `vault/**` (daemon frontmatter churn) or any other file.

## Rollback

If verification fails after 3 fix cycles:
1. `git stash -m "failed-ADR-0037-T3-attempt-{N}"` — preserve work for diagnosis
2. `git checkout {commit-before-task}` — revert to entry state (the ADR-0037-T2 completion commit from the Entry State prerequisites)
3. Update issue tracker: flag ADR-0037-T3 as BLOCKED with the failure log
4. Escalate: "Task ADR-0037-T3 failed verification after 3 attempts. Stash ref: {ref}. Failure log: {summary}"
5. After task abandonment or re-planning, drop all `failed-ADR-0037-T3` entries from `git stash list`.

Do NOT: silently skip failing acceptance criteria, weaken tests to pass (e.g. relax the wire-scan's 0-hit threshold, remove the tripwire's injected-violation case, or drop the reinserted-name paired control), or proceed to the next task with a broken state.

## Deviations

| # | Spec Says | Recipe Says | Reason | Impact |
|---|-----------|-------------|--------|--------|
| 1 | T3 file manifest lists only `tailoring.py`, `ADR-0001...md`, `test_tailoring.py`; AC-2 requires dumping "the de-identified dashboard payload" and AC-5 requires the "commit/push PII scan". | The wire-scan/reinserted-name tests IMPORT existing unmodified production modules (`scripts/generate/render_views.py`, the store read, `scripts/guard/pii_scan.py` + `.claude/hooks/lib/pii-scan-scope.sh`) to produce/scan payloads. | Test-only imports of already-built code (the `tests/serve/test_care_chat.py` wire-scan precedent) are not production changes and expand no manifest — the ACs are testable as written with no additional Modify target. | None on the manifest — no production file outside the three-file manifest is changed; no TP-06 scope creep. Recorded for reviewer transparency only. |
