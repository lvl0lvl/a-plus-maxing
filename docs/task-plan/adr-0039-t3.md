---
task-id: ADR-0039-T3
source-spec: docs/spec/adr-0039-scheduled-agent-runner-spec.md
source-build-plan: docs/build-plan/build-plan-adr-0039.md
wave: 2
assigned-agent: SE
reviewers: [Architect, QA, Security]
created: 2026-07-06
status: approved
---

# Recipe: Scheduler + Operator-Owned Activation Surface + NAMED Anti-Implicit-Activation Guard (ADR-0039-T3)

Build-plan wave: **Wave 2** (one of three independent leaves off the Wave-1 driver: auth-isolation `∥` **this scheduler + activation surface** `∥` store-concurrency). Prior-wave attestation: the **Wave 1 → Wave 2** checkpoint Go/No-Go MUST have RAN green (executed, not reasoned) before this task builds — the single `plan_loop.signal(CADENCE_TRIGGER)` call (T1 AC-1/AC-2), the 0-re-hosting grep + 0 `scripts/serve/` edits (AC-3, SEC-03), the frozen-glob numstat=0 incl. `scripts/serve/plan_loop.py` (AC-4, SEC-03), the crown-jewel non-egress proven-non-empty + de-id-bypass mutation-RED (AC-5, QA-F1), the local-store-only de-id-IN-invoked scan (AC-6, QA-F1), import-inertness (AC-7), and the default-factory refusal (AC-9, SEC-04) ALL green. This task builds the operator-owned enable/disable surface + the launchd/cron templates + the NAMED anti-implicit-activation guard over the driver T1 established; it is the sole writer of `scripts/runner/schedule/*` + `.gitignore` in this spec. It does NOT modify `scripts/runner/cadence_runner.py` (T4's file) or `scripts/runner/subscription_dispatch.py` (T2's file) — it CONSUMES T1's `cadence_runner.run` as the scheduled entry.

## Entry State

**Prerequisites:**
- [ ] Task ADR-0039-T1 complete (committed at `{commit-after-T1}` — the Wave-1 entry point) — `scripts/runner/cadence_runner.py` exists with the driver `run(root, *, dispatch_factory, deid_client, tailor_client=None, plan_date=None)` (or its T1-final signature) that calls `plan_loop.signal(root, trigger=plan_loop.CADENCE_TRIGGER, ...)` EXACTLY ONCE per tick; `scripts/runner/subscription_dispatch.py` exists with the injectable session factory (its DEFAULT factory RAISES pre-T2, T1 AC-9); `scripts/runner/__init__.py` is import-inert (0 `signal`/de-id/dispatch on import, T1 AC-7). T3 SCHEDULES `cadence_runner.run`; it neither imports the loop internals nor re-hosts the debounce.
- [ ] Full test suite passes at the T3-entry floor: `.venv/bin/python -m pytest -q` — the KNOWN-RED floor is `3 failed, 2312 passed, 7 skipped` (captured `feature/adr-0039-runner-build @ 67b8389`, 2026-07-06; `passed` will have grown as T1's tests landed). The three benign failures are NOT this task's to fix: (1) `tests/serve/test_bind.py::test_port_collision_exits_nonzero_fail_loud`, (2) `tests/serve/test_server.py::test_main_reads_no_stdin` (pre-existing env-sensitive serve cases), (3) `tests/serve/test_plan_loop_hold.py::test_frozen_spine_and_only_regenerate_changed` (ADR-0040 post-merge-vacuous merge-artifact). **The gate is NO NEW 4th distinct failure — a 4th failure is a regression, HALT and fix before starting.**
- [ ] Working tree clean: `git status` shows no changes.
- [ ] On the feature branch: `feature/adr-0039-runner-build` (the Wave-2 continuation branch off `main`, holding the merged T1 driver) — never `main`.
- [ ] Rollback target recorded: `git rev-parse HEAD` at task entry (the `{commit-after-T1}` the Rollback Protocol reverts to).
- [ ] The Create targets are ABSENT (Create-only, no stale/duplicate artifact): `for f in scripts/runner/schedule/activate.py scripts/runner/schedule/cadence-runner.plist.template scripts/runner/schedule/cadence-runner.crontab.template tests/runner/test_activation_surface.py tests/runner/test_no_implicit_activation.py; do test -e "$f" && { echo "PRESENT $f"; exit 1; }; done && echo "ABSENT (Create OK)"` (verified 2026-07-06: `scripts/runner/schedule/` + these test files ABSENT).
- [ ] No pre-existing scheduler/plist/cron artifact anywhere in `scripts/`: `[ "$(find scripts -name '*.plist' | wc -l)" -eq 0 ] && [ "$(grep -rlE 'launchctl|StartCalendarInterval|crontab' scripts/ | wc -l)" -eq 0 ] && echo OK` (verified 2026-07-06: 0 plists, 0 scheduler tokens; the unrelated `scripts/ingest/scheduler.py` is a wearable-import batch, no OS timer).
- [ ] The anti-implicit-activation PREMISE holds at entry — `scripts/clone/init_instance.py` present with 0 schedule tokens: `test -e scripts/clone/init_instance.py && [ "$(grep -cE 'launchctl|StartCalendarInterval|crontab|cadence|plist|activate\.enable' scripts/clone/init_instance.py)" -eq 0 ] && echo OK` (verified 2026-07-06: present, `run(clone_root=Path("."))` at `init_instance.py:141`, 0 schedule tokens). This is the state T3 AC-8's guard exists to PRESERVE.
- [ ] `.gitignore` present (Modify target) with the `vault/store/` entry already covering the runner lock file: `test -e .gitignore && grep -q "vault/store/" .gitignore && echo OK` (verified 2026-07-06: present; `vault/store/` covers `<root>/.cadence-runner.lock`).

## Context Load List

**Files to read (this task's manifest + the seams it schedules/scans):**
- `scripts/serve/plan_loop.py` — READ-ONLY cited input for the debounce constants + the derived-marker the catch-up defers to: `CADENCE_TRIGGER = "cadence"` ([plan_loop.py:376]), `MIN_REGEN_INTERVAL_DAYS = 7` ([plan_loop.py:383]), `_should_regenerate(store_read, *, on_date)` ([plan_loop.py:440]), `_last_regen_date(store_read, on_date)` ([plan_loop.py:399]), `signal(root, *, trigger, dispatch=None, deid_client=None, plan_date=None, tailor_client=None)` ([plan_loop.py:457]). T3 drives `plan_loop.signal` ONLY through T1's `cadence_runner.run` (never re-declares `MIN_REGEN_INTERVAL_DAYS`, never calls `_should_regenerate`/`regenerate` directly). NOT modified by T3 — it is in the SEC-03 frozen freeze.
- `scripts/runner/cadence_runner.py` — READ-ONLY cited input (created by T1): the `run(...)` signature the plist ProgramArguments schedule and the AC-5/AC-7 debounce tests drive over a fixture dispatch/`deid_client`. T3 does NOT modify this file (T4 modifies it) — a T3 edit to it is scope creep.
- `scripts/clone/init_instance.py` — the provisioning surface the AC-8 guard SCANS (`run(clone_root=...)` at `init_instance.py:141`, 0 schedule tokens today). T3 does NOT modify it; the guard greps + behaviorally probes it. (The AC-8 mutation-RED injects into a SCRATCH copy / fixture, never the tracked file.)
- `.gitignore` — the Modify target. Read the `vault/store/` + `.env*` entries ([.gitignore:4,12-14]) to mirror the ignore convention for the rendered instance plist + any runner-local token/state artifact.
- `docs/checklists/store-adversarial-tests.md` — NOT loaded (T3 is not a store-surface task; T4 owns that battery).

**Read for convention only (do NOT edit in T3):**
- `scripts/model/key_source.py` — the `_keychain_runner` pattern ([key_source.py:44-62]) + `a-plus-maxing-api-key` service constant, for CONTEXT on how T2 keychain-reads the OAuth token (`a-plus-maxing-oauth-token`) at RUNTIME. AC-10 asserts that token is NEVER rendered into the plist — this file is background for WHY (keychain-at-runtime is the real control, not a plist EnvironmentVariables entry). T3 does not edit or import it.

**Do NOT load:**
- The source ADRs (`docs/adr/ADR-0039/0036/0026/0027/0001/0005-*.md`) — the spec already distilled them; re-reading re-opens resolved trade-offs (TP-02 drift).
- The full `docs/spec/adr-0039-scheduled-agent-runner-spec.md` beyond the ADR-0039-T3 block, and `docs/build-plan/build-plan-adr-0039.md` beyond the Wave-2 checkpoint — already extracted into this recipe.
- `scripts/runner/auth_isolation.py` + `scripts/runner/subscription_dispatch.py` (T2's manifest — the env-scrub / session construction), `scripts/runner/store_lock.py` (T4's manifest — the advisory lock). T3 touches none of them; reading them invites a wrong re-bind.
- The frozen glob `scripts/plan/{orchestrate,pipeline,assemble,generate_plan,adjudicate,adjust,track,plan_orchestrator,plan_driver}.py` + `scripts/store/*` + `scripts/serve/plan_loop.py` — the SEC-03 byte-frozen spine (`plan_orchestrator.py`/`plan_driver.py` hold the crown-jewel de-id sentinel HALT + dispatch fan-out; Security M1); T3 edits none of it.

## Risk Pre-Check

This task HAS risk mitigations in the spec — Risk Pre-Check is REQUIRED and is a **BLOCKING GATE**: T3 builds a scheduler activation surface on a PROVISIONING path (the PF-S63-02 guard-loosening sensitivity) plus a NEW at-rest secret/operator-path surface (the plist, tensions-with ADR-0005). Each gate condition below MUST be satisfied by a concrete, failing-capable test in RED before any GREEN code (an `assert True` placeholder fails this gate):

- **Risk — QA-F2 / SEC-01 (the anti-implicit-activation guard is green BY CONSTRUCTION — nothing proves it CAN go RED):** both guard arms pass today (`grep init_instance.py → 0`; `init_instance.run(<scratch>) → 0 entries`), AND a dotted-literal grep for `activate.enable` MISSES the `from ...schedule.activate import enable; enable()` import-and-call form — so a naïve guard is a tautology that would not catch a real implicit activation. Gate: RED must include the **EXECUTED mutation-RED** (AC-8) — inject a REALISTIC `activate.enable()` call in BOTH the dotted `activate.enable(...)` form AND the `from ...schedule.activate import enable; enable()` bare-call form into a SCRATCH `init_instance`/provision path, run the guard, CONFIRM the behavioral arm's active-entry counter reports **≥1** AND the guard rests **RED**, then revert to GREEN. **The forcing mechanism is PINNED** (BLOCKING): the injected `enable()` is forced down the CRON branch (launchd-availability probe monkeypatched `False`) and the counter reads the REAL `crontab -l` for the unique label — a fixture-HOME counter that reports ≥1 off a fixture entry proves NOTHING about real-state reads and FAILS this gate (it re-introduces the scratch-HOME tautology). If neither `launchctl` nor `crontab` is available the AC FAILS LOUD (never `pytest.skip`). A guard that stays green under this injection — or that reddens only via a fixture-HOME counter — is tautological and FAILS this gate.
- **Risk — PF-S63-02 / OQ-4 (disabled-by-default falsifiable):** a "disabled by default" claim that cannot go RED is vacuous. Gate: RED must include a fresh-install probe (AC-1) whose active-entry counter reads **0** from REAL scheduler state (real `crontab -l`/launchd registration for the unique label, NOT a fixture-HOME dir), that would report **≥1** if `enable()` had been called — proven by the enable→**1**→disable→**0** round-trip (AC-3) over the SAME real-state counter (the round-trip IS the evidence the counter reflects real registrations, not a constant 0). AC-6's "0 signal while disabled" is SUBSUMED by this round-trip (trivially green on its own — see AC-6 note), not a substitute for it. Fail-loud if neither scheduler is available.
- **Risk — SEC-02 (OAuth token at rest in the launchd plist):** `scripts/guard/pii_scan.py` is an operator-PII scanner with NO secret pattern — a `sk-ant-oat…` token rendered into the plist's `EnvironmentVariables` passes both PII hooks CLEAN. Gate: RED must include the token-never-in-plist probe (AC-10) — a grep of the plist TEMPLATE **and** the rendered instance plist (produced by `enable()`) for `CLAUDE_CODE_OAUTH_TOKEN` and the token-VALUE pattern `sk-ant-oat[A-Za-z0-9_-]+` returns **0** on BOTH; FAILING-CAPABLE — if the template rendered the token into `EnvironmentVariables`, the grep would hit ≥1.
- **Risk — ADR-0039 Consequences-Negative-5 (operator-specific path in a tracked plist, tensions-with ADR-0005):** a hardcoded `/Users/<name>` path in the tracked template leaks operator specifics. Gate: RED must include the placeholder-only probe (AC-4) — a grep of `cadence-runner.plist.template` finds **0** absolute operator paths (no `/Users/`, no home-dir literal), only `{{PYTHON}}`/`{{REPO_ROOT}}`/`{{LABEL}}`, AND the rendered instance plist path matches a `.gitignore` entry (`git check-ignore` returns it).

## TDD Steps

Ten testable ACs (1–10) across two test files → split into two TDD cycles (8 steps total, at the 10-step ceiling). Cycle 1 = the activation surface (`scripts/runner/schedule/activate.py` + the two templates + the `.gitignore` modify) — AC-1 (disabled-by-default), AC-2 (import-inert), AC-3 (enable/disable/kill-switch round-trip + status), AC-4 (plist 0-operator-path + rendered gitignored), AC-5 (missed-window catch-up honors the built debounce), AC-6 (activation gate — 0 re-gens while disabled), AC-7 (built-debounce pair once enabled), AC-10 (OAuth token never in the plist). Cycle 2 = the NAMED anti-implicit-activation guard (`tests/runner/test_no_implicit_activation.py`) — AC-8 (grep arm + behavioral arm + the EXECUTED mutation-RED). AC-9 is the two-file pytest run, verified at each REGRESSION. **The REAL-STATE arms (AC-1 fresh-install counter, AC-3 round-trip, AC-8 behavioral arm) MUST read real scheduler state via the CRON branch + real `crontab -l` for a uniquely-labelled test entry (NEVER a fixture-`HOME` counter — that is the scratch-HOME tautology AC-8 kills), with a bullet-proof `try/finally` teardown + crontab-snapshot restore, and FAIL LOUD if neither scheduler is available.** A fixture `HOME`/scratch store is used ONLY by arms that never read the real-state counter (AC-2 import-inert, AC-4 template grep, AC-5/AC-7 driving `plan_loop` over a seeded store, AC-10 plist grep). 0 live spend, 0 real operator PII, 0 scheduled program is ever EXECUTED (activation registers an entry; the test never fires the tick's de-id call).

> **Test-hygiene invariant (load-bearing across BOTH cycles; Security L3):** every test that calls `enable()` — and the AC-8 mutation-RED especially — MUST run against a UNIQUELY-LABELLED test runner label (e.g. a per-test-run `com.aplusmaxing.cadence-runner.test-<uuid>` label). **Prefer the reversible CRON fallback over the GUI launchd domain for every real-state arm** — force `enable()` down the cron branch (monkeypatch the launchd-availability probe to `False`) so the entry is a single, restorable `crontab` line, not a registration in the operator's live launchd GUI domain. **Bullet-proof the teardown:** wrap the `enable()` at the OUTERMOST boundary in `try/finally` (a pytest fixture with yield-then-teardown, or an explicit finally) that unconditionally `disable()`s the label AND restores the pre-test crontab snapshot — a crash BETWEEN `enable()` and `disable()` must not leave a REAL armed entry (an un-torn-down entry is itself the implicit activation the task forbids). Capture `crontab -l` before the arm and restore it in the finally. No test EXECUTES the scheduled program (activation only REGISTERS an entry; the tick's de-id call is never fired). **Fail-loud, never skip:** if NEITHER `launchctl` nor `crontab` is available in the environment, the real-state arms (AC-1/AC-3 counter, AC-8 behavioral arm) FAIL LOUD — they do NOT `pytest.skip` (a skipped non-tautology proof reads as clean under the close-audit FATAL-on-skip posture).

### Cycle 1: The Operator-Owned Activation Surface (disabled-by-default, enable/disable/kill-switch/status, catch-up, no-token-in-plist)

#### Step 1: RED — Write Failing Tests
Call `/write-tests` for `scripts/runner/schedule/activate.py`, creating `tests/runner/test_activation_surface.py` (the real-state arms AC-1/AC-3 read the REAL `crontab -l` via the forced cron branch — NEVER a fixture-`HOME` counter; a fixture `HOME` + seeded scratch store + fixture dispatch/`deid_client` serve only the non-real-state arms; 0 live spend, 0 real PII), targeting each spec T3 AC:

- **AC-1 (disabled by default — fresh install; counter reads REAL state):** with NO entry loaded for the UNIQUELY-LABELLED test runner label, `status()` reports **DISABLED** and the active-entry counter for that label reads **0** BEFORE any `enable()` call. **The counter MUST read REAL scheduler state** — the real `crontab -l` for the label (cron branch, preferred) or the real launchd registration — NOT a fixture-`HOME` dir listing (a fixture-HOME-only counter is the scratch-HOME tautology AC-8 exists to kill; the AC-1 counter is the SAME counter AC-8's behavioral arm reuses, so it must read real state here too). Because the label is unique-per-run, real state for it is legitimately 0 at entry. AND (falsification down-payment): constructing/importing the runner surface without `enable()` leaves the real-state count at **0** — the count goes ≥1 only if an entry is actually loaded (proven by AC-3's round-trip over the same counter). **Fail-loud, never skip:** if neither `crontab` nor `launchctl` is available, this AC FAILS LOUD (does not `pytest.skip`).
- **AC-2 (building/importing triggers 0 de-id + 0 dispatch):** `import scripts.runner.schedule.activate` and constructing the surface WITHOUT calling `enable()`/`run()` fires **0** `plan_loop.signal` calls, **0** de-id calls, and **0** dispatch calls (spies over each seam assert 0). Building the activation surface arms nothing.
- **AC-3 (enable/disable/kill-switch round-trip + status — the counter reads REAL state):** force `enable()` down the CRON branch (monkeypatch the launchd-availability probe to `False`) so it writes EXACTLY **1** restorable `crontab` line for the uniquely-labelled runner label; `status()` now reports **ENABLED** with the active-entry counter reading **1** FROM the real `crontab -l` (not a fixture-HOME dir); `disable()` (the kill-switch) removes the line, post-`disable()` real-state count == **0** and `status()` reports DISABLED. **The 0 → 1 → 0 round-trip over the REAL `crontab -l` counter IS the evidence the counter reflects REAL registrations, not a constant** — this is exactly the property AC-8's mutation-RED depends on, so it MUST read real state here (a fixture-HOME round-trip would give a green that proves nothing about real-state reads). Wrap the arm in a bullet-proof `try/finally` that unconditionally `disable()`s the label AND restores the pre-test crontab snapshot (per the Test-hygiene invariant). **Fail-loud, never skip** if neither scheduler is available.
- **AC-4 (plist template carries 0 operator-specific path; rendered instance gitignored):** a grep over `scripts/runner/schedule/cadence-runner.plist.template` finds **0** absolute operator paths (no `/Users/<name>`, no home-dir literal) — ONLY the `{{PYTHON}}`/`{{REPO_ROOT}}`/`{{LABEL}}` placeholders (count of non-placeholder absolute paths == 0); AND the rendered instance plist path produced by `enable()` matches a `.gitignore` entry — `git check-ignore <rendered-path>` returns the path (the rendered `*.rendered.plist` is untracked). The crontab template likewise placeholder-only. Failing-capable: a template with a literal `/Users/walter/...` path reddens the 0-operator-path assertion.
- **AC-5 (missed-window catch-up honors the built debounce):** the plist template sets `StartCalendarInterval` (grep the template → present); a catch-up tick fired WITHIN `MIN_REGEN_INTERVAL_DAYS` of the last completed re-gen produces **0** new plans — driving `plan_loop.signal` (through T1's `cadence_runner.run`) over a synthetic `plan_date` within the interval, over a seeded store whose `_last_regen_date` is < 7 days prior, returns `regenerated: False`. (This IS a correct read: the within-interval NO-OP/hold path of `plan_loop.signal` DOES carry a `regenerated: False` key — distinct from AC-7's gate-PASS path, which returns `regenerate(...)`'s result with NO `regenerated` key. See AC-7's measure note.) The runner adds NO second debounce — the built `plan_loop._should_regenerate` governs.
- **AC-6 (activation gate — disabled → 0 re-gens):** over a multi-week SYNTHETIC date window with the schedule DISABLED, the runner tick is never invoked — count of `plan_loop.signal` calls == **0** while disabled (the scheduler fires nothing when no entry is loaded). **NOTE (QA SHOULD-FIX — AC-6 is subsumed):** "0 `signal` calls while disabled" is trivially satisfied by simply not calling `run()` — a unit test cannot make the OS launchd/cron fire, and there is NO "disabled → `run()` returns early" code gate to exercise, so this AC is NOT the genuine disabled-by-default falsification. The genuine falsification is the **AC-1 + AC-3 real-state counter round-trip** (0 → 1 → 0 over the REAL `crontab -l`/launchd registration): AC-6 is documented here as SUBSUMED by that round-trip. Keep AC-6 as a stated invariant (the scheduler drives nothing while no entry is loaded), but do NOT treat its trivially-green assertion as the disabled-by-default proof.
- **AC-7 (built debounce pair holds once enabled):** after `enable()`, driving a single new reading IN-window through `cadence_runner.run` produces **0** re-gens, and a sustained signal past `MIN_REGEN_INTERVAL_DAYS` produces EXACTLY **1** re-gen — the (single, sustained) pair maps to (0, 1) via the built `plan_loop._should_regenerate`. **Measure (QA SHOULD-FIX — name a concrete re-gen count, NOT a `regenerated` boolean):** `plan_loop.signal`'s gate-PASS-with-seams path returns `regenerate(...)`'s result, which carries NO `regenerated` key (only the no-op/hold paths carry `regenerated: False`), so `assert result["regenerated"] is True` would `KeyError`. Measure the re-gen via a concrete count — a spy on `plan_loop.regenerate` firing, OR the promoted-`plan::`-row count in the store, OR `dispatch.spec_calls()` (the measure `tests/serve/test_plan_loop_regen.py` uses) — asserting the (single → 0, sustained → 1) pair over THAT measure. The runner adds no second debounce (asserts the pair over the REAL loop, not a re-hosted copy).
- **AC-10 (OAuth token NEVER rendered into the plist — SEC-02):** a scan of `cadence-runner.plist.template` AND the rendered instance plist (produced by `enable()`) for the literal `CLAUDE_CODE_OAUTH_TOKEN` AND the token-VALUE pattern (the `sk-ant-oat` prefix followed by the token-char class `[A-Za-z0-9_-]+`) returns **0** hits on BOTH (count == 0 on template AND rendered). The token is keychain-read by the runner PROCESS at RUNTIME (T2), never injected as a launchd `EnvironmentVariables` value. **Positive control (scan non-vacuity — the scan CAN hit):** assemble a synthetic token-shaped probe string at RUNTIME by CONCATENATING fragments in the test (e.g. `"sk-" + "ant-" + "oat" + <random suffix>`) — NEVER a tracked token literal, since a tracked literal would self-match the tree-wide `git ls-files` token scan — and assert the AC-10 pattern MATCHES that runtime-assembled probe, proving the 0-hit result over the real plist is a real 0, not a broken/inert pattern. Failing-capable: rendering the token into `EnvironmentVariables` reddens both plist scans while the positive control stays green.
- **Risk gates (PF-S63-02 disabled-by-default, SEC-02 token-in-plist, Negative-5 operator-path):** AC-1 + AC-3 (the falsifiable disabled→enabled→disabled counter), AC-10 (the failing-capable token grep), AC-4 (the failing-capable operator-path grep) are the controls.

Run: `.venv/bin/python -m pytest tests/runner/test_activation_surface.py -q`
Expected: FAIL (`scripts/runner/schedule/activate.py` does not yet exist — the module import fails). If any test PASSES at this stage, the test is wrong (likely a scratch-HOME counter that reports 0 regardless) — investigate before GREEN.

#### Step 2: GREEN — Implement
Changes to make (descriptions, not literal code):
- `scripts/runner/schedule/cadence-runner.plist.template` (Create) — a launchd `StartCalendarInterval` plist, PLACEHOLDER-ONLY: `{{PYTHON}}` (the interpreter), `{{REPO_ROOT}}` (the runtime-derived repo root), `{{LABEL}}` (the runner label). The `ProgramArguments` invoke T1's runner entry (`{{PYTHON}}` + the `scripts.runner.cadence_runner` module invocation — the exact entry aligns with T1's driver, not re-declared here). `StartCalendarInterval` sets the weekly cadence. ZERO operator-specific paths, ZERO `EnvironmentVariables` carrying `CLAUDE_CODE_OAUTH_TOKEN` (the token is keychain-read at runtime by the process — AC-10). Operator-agnostic → ADR-0005-safe to track. (Satisfies AC-4, AC-5, AC-10.)
- `scripts/runner/schedule/cadence-runner.crontab.template` (Create) — the cron fallback line, placeholder-only (`{{PYTHON}}`/`{{REPO_ROOT}}`/`{{LABEL}}`), 0 operator-specific paths, 0 token. (Satisfies AC-4 for the fallback path.)
- `scripts/runner/schedule/activate.py` (Create) — the operator-owned surface:
  - `enable()` — render the plist from `cadence-runner.plist.template` substituting `{{PYTHON}}` = `sys.executable`, `{{REPO_ROOT}}` = the runtime-derived repo root, `{{LABEL}}` = the runner label; write the rendered instance to a GITIGNORED path (matching `scripts/runner/schedule/*.rendered.plist`); install it (launchd: write into `~/Library/LaunchAgents` + `launchctl load`; cron fallback: append the rendered crontab line when launchd is unavailable). Renders NO operator-specific path into any tracked file and NO OAuth token into the plist. (Satisfies AC-3, AC-4, AC-10.)
  - `disable()` — the kill-switch: unload (`launchctl unload`) + remove the rendered instance (cron: remove the appended line). Idempotent — disabling an already-disabled label is a no-op. (Satisfies AC-3.)
  - `status()` — report ENABLED/DISABLED by reading the REAL scheduler registration for the runner label (launchd registration / crontab line presence for the label), NOT by listing a fixture-HOME dir. The active-entry COUNTER used by `status()` (and reused by the AC-8 guard) enumerates real registrations keyed on the label. (Satisfies AC-1, AC-3, and underwrites AC-8's real-state read.)
  - Import-inertness: the module fires 0 `signal`/de-id/dispatch at import — no `enable()`/`run()` at module scope. (Satisfies AC-2.)
  - Missed-window policy: catch-up-on-next-wake — the surface DEFERS to the built `plan_loop._should_regenerate` debounce (a catch-up tick within `MIN_REGEN_INTERVAL_DAYS` no-ops). The runner declares NO second debounce constant and does NOT re-declare `MIN_REGEN_INTERVAL_DAYS`. (Satisfies AC-5, AC-7.)
- `.gitignore` (Modify) — add `scripts/runner/schedule/*.rendered.plist` + any runner-local token/state artifact (the lock file is already covered by the `vault/store/` entry). (Satisfies AC-4's gitignore check.)

Run: `.venv/bin/python -m pytest tests/runner/test_activation_surface.py -q`
Expected: PASS.

#### Step 3: REFACTOR
Review implementation for:
- The active-entry COUNTER is a SINGLE resolution path shared by `status()` and (via import) the AC-8 guard — do not fork a second counter idiom (a divergent counter is how a by-construction green slips in).
- The runner label is a SINGLE constant (one home), reused by `enable`/`disable`/`status` + the templates' `{{LABEL}}` substitution — never a second string literal.
- No re-declared `MIN_REGEN_INTERVAL_DAYS` / no re-hosted `_should_regenerate` crept into `activate.py` during GREEN — grep the diff (the runner defers to the built debounce).
- The plist/crontab templates carry 0 operator path + 0 token after GREEN (re-grep) — the placeholder substitution happens only at `enable()` runtime into the gitignored rendered instance.

Run: `.venv/bin/python -m pytest tests/runner/test_activation_surface.py -q`
Expected: PASS (no behavior change).

#### Step 4: REGRESSION
Run: `.venv/bin/python -m pytest -q`
Expected: PASS with no NEW failure vs the T3-entry floor (`3 failed, 2312 passed, 7 skipped` raised by T1's added tests; a 4th distinct failure is a regression — investigate whether the change reached outside the manifest).

### Cycle 2: The NAMED Anti-Implicit-Activation Guard (grep arm + behavioral arm + EXECUTED mutation-RED)

#### Step 5: RED — Write Failing Tests
Call `/write-tests` for the anti-implicit-activation guard, creating `tests/runner/test_no_implicit_activation.py` (drive over a SCRATCH `init_instance`/provision copy + the fixture scheduler state from Cycle 1; 0 live spend), targeting:

- **AC-8 (NAMED anti-implicit-activation guard — grep arm + behavioral arm + EXECUTED mutation-RED, QA-F2 / SEC-01):**
  - **Grep arm:** a scan over the provisioning surface — `scripts/clone/init_instance.py` + any setup/build script — for `launchctl`, `StartCalendarInterval`, `crontab`, the plist install, AND the `activate.enable` symbol IN BOTH forms (the dotted `activate.enable` literal AND the `from ...schedule.activate import enable` import-and-call form) returns **0** hits. (The two-form match is load-bearing — a dotted-only grep MISSES `from ...import enable; enable()`.)
  - **Behavioral arm:** invoking `init_instance.run(clone_root=<scratch>)` against a scratch root installs **0** runner launchd/cron entries — the behavioral arm's active-entry counter (REUSED from `activate.status()`'s real-state counter, the SAME one AC-1/AC-3 exercised) reads **0** after `run()`. The counter reads REAL installed launchd/cron state for the runner label (real `crontab -l` / real launchd registration), NOT a scratch-`HOME`/fixture-dir listing.
  - **EXECUTED mutation-RED (mirrors T4 AC-7(d), the whole point — the BLOCKING QA MUST-FIX):** inject a REALISTIC activation call — `activate.enable(<uniquely-labelled>)` in its ACTUAL imported/aliased form — into a SCRATCH `init_instance`/provision path (or a fixture where `init_instance.run()` invokes the enable), in BOTH:
    (i) the dotted `import scripts.runner.schedule.activate as activate; activate.enable(<uniquely-labelled>)` form, AND
    (ii) the bare `from scripts.runner.schedule.activate import enable; enable(<uniquely-labelled>)` form.
    **Pin the forcing mechanism so the counter reads REAL state (NOT a fixture-HOME — the exact scratch-HOME tautology this AC exists to kill):** force the injected `enable()` down the CRON branch (monkeypatch the launchd-availability probe to `False`) so it writes a real `crontab` line for the unique label, and have the guard's behavioral counter read the REAL `crontab -l` for that label. (An SE who instead monkeypatched `HOME` to a fixture dir and counted a fixture-HOME entry would get a ≥1 that proves NOTHING about real-state reads — do NOT do that; the cron-branch + real-`crontab -l` read is mandatory. The launchd variant is acceptable ONLY if run against the real `~/Library/LaunchAgents` with the unique label + guaranteed teardown, never a fixture HOME.) Run the guard against the mutated scratch path; CONFIRM (a) the grep arm MATCHES the injected call in BOTH forms (grep hit ≥1) AND (b) the behavioral counter reports **≥1** FROM real `crontab -l`/launchd state AND (c) the guard rests **RED**. Then, in an UNCONDITIONAL `try/finally`, `disable()` the injected label AND restore the pre-test crontab snapshot, and revert the scratch path to GREEN (a crash between the injected `enable()` and the teardown must not leave a REAL armed entry). **Fail-loud, never skip:** if NEITHER `launchctl` nor `crontab` is available, this AC FAILS LOUD — it does NOT `pytest.skip` (a skipped non-tautology proof reads as clean under the close-audit FATAL-on-skip posture). This proves (i) the counter reads REAL launchd/cron state and (ii) the grep matches the import-and-call form, not only the dotted literal. **A guard that stays green under this injection — or that "reddens" only via a fixture-HOME counter — is tautological and FAILS the Risk Pre-Check gate.**
- **Risk gate (QA-F2 / SEC-01 anti-implicit-activation):** the EXECUTED mutation-RED (both call-forms, real-state counter ≥1, guard RED, revert + teardown) is the non-tautological control.

Run: `.venv/bin/python -m pytest tests/runner/test_no_implicit_activation.py -q`
Expected: FAIL (the guard helper / the `activate` surface it imports do not yet exist, OR the guard's real-state counter is not yet wired). If the guard passes green at this stage WITHOUT the mutation being able to redden it, the guard is tautological — investigate before GREEN.

#### Step 6: GREEN — Implement
Changes to make (descriptions, not literal code) — NOTE: AC-8's guard is realized primarily as the TEST + a small guard helper; `activate.py` (Cycle 1) supplies the shared real-state counter it reuses. If a reusable guard scanner is factored out, it lives beside `activate.py` (e.g. an internal `_scan_provisioning_for_activation(paths)` + the shared counter), imported by `tests/runner/test_no_implicit_activation.py`. No production provisioning file is edited — `init_instance.py` stays byte-unchanged; the guard SCANS it.
- The guard's grep arm matches the `activate.enable` symbol in BOTH the dotted `activate.enable` and the `from ...import enable` (then bare `enable(`) forms — a token/AST scan broad enough to catch the import-and-call form, not a single dotted-literal substring.
- The guard's behavioral arm reuses `activate.status()`'s real-state active-entry counter (the SAME counter the 0→1→0 round-trip validated), invoked after `init_instance.run(clone_root=<scratch>)`, asserting 0 entries for the runner label.
- No change to `scripts/clone/init_instance.py` (the premise the guard preserves) — the mutation-RED injects into a SCRATCH copy / fixture only, reverted + torn down before the step ends.

Run: `.venv/bin/python -m pytest tests/runner/test_no_implicit_activation.py -q`
Expected: PASS (grep arm 0 hits over the real tracked `init_instance.py`; behavioral arm 0 entries; the mutation-RED sub-case observed RED under injection then reverted GREEN).

#### Step 7: REFACTOR
Review implementation for:
- The behavioral arm's counter is the SAME real-state counter as `activate.status()` — confirm no second scratch-HOME counter was introduced (a scratch-HOME counter is the QA-F2 tautology).
- The grep/scan arm's two-form matcher (dotted + import-and-call) is one coherent scanner, not two divergent substring checks.
- The mutation-RED sub-case has a `try/finally` teardown that `disable()`s the injected label AND reverts the scratch path unconditionally — no injected entry or scratch mutation survives the test (an un-torn-down entry is itself an implicit activation).
- The list of provisioning surfaces the guard scans (`init_instance.py` + setup/build scripts) is defined ONCE (one home), so a future provisioning script is added to the scan set in one place.

Run: `.venv/bin/python -m pytest tests/runner/test_no_implicit_activation.py -q`
Expected: PASS (no behavior change).

#### Step 8: REGRESSION
Run: `.venv/bin/python -m pytest tests/runner/test_activation_surface.py tests/runner/test_no_implicit_activation.py -q` (AC-9) THEN `.venv/bin/python -m pytest -q`
Expected: both PASS with no NEW failure vs the T3-entry floor (`3 failed, ... passed, 7 skipped`; a 4th distinct failure is a regression). Confirm no REAL launchd/cron entry survives the run (`launchctl list | grep <test-label>` and `crontab -l | grep <test-label>` return nothing — teardown held).

## Interface Contracts

This task exposes the operator-owned ACTIVATION API — the documented enable/disable surface the operator runs and the plist/crontab template contract. It has NO in-DAG downstream consumer (T3 is a Wave-2 leaf; T2 and T4 do not import it), so no sibling-task contract gate binds; the contract is documented for the operator + the deferred LIVE run, and for the Architect activation-surface review.

- **`enable() / disable() / status()`** (in `scripts/runner/schedule/activate.py`):
  - **`enable()`** — renders the plist from the template (`{{PYTHON}}` = `sys.executable`, `{{REPO_ROOT}}` = runtime-derived repo root, `{{LABEL}}` = the runner label) into a GITIGNORED `*.rendered.plist`, installs it (launchd `StartCalendarInterval` primary; cron fallback line when launchd unavailable). Renders 0 operator-specific path into any tracked file and 0 `CLAUDE_CODE_OAUTH_TOKEN` into the plist (the token is keychain-read at runtime by the process — T2). Weekly cadence; missed-window policy = catch-up-on-next-wake deferring to the built `plan_loop._should_regenerate` debounce.
  - **`disable()`** — the kill-switch: unloads + removes the entry; idempotent.
  - **`status()`** — reports ENABLED/DISABLED by reading the REAL scheduler registration for the runner label; exposes the active-entry counter reused by the anti-implicit-activation guard.
  - **Behavioral invariants:** disabled by default (fresh install → 0 active entries); import-inert (0 `signal`/de-id/dispatch on import); the runner adds NO second debounce (defers to the built loop); NO build/provision/`init_instance` step loads the entry (the NAMED anti-implicit-activation guard, AC-8).
- **`cadence-runner.plist.template` / `cadence-runner.crontab.template`** — placeholder-only (`{{PYTHON}}`/`{{REPO_ROOT}}`/`{{LABEL}}`), 0 operator-specific paths, 0 OAuth token; the tracked, operator-agnostic (ADR-0005-safe) source the rendered gitignored instance derives from at `enable()` time.
- **Change control:** the deferred LIVE operator-present run consumes this activation surface; a later change to the enable/disable/status signatures or the template placeholders triggers an Architect contract-update notice (per the build plan's Wave-2 Architect activation-surface review assignment).

## Verification Checklist

- [ ] AC-1: disabled by default — fresh install `status()` DISABLED, active-entry count == 0 before any `enable()` — verified by `test_activation_surface.py` (disabled-by-default case).
- [ ] AC-2: building/importing fires 0 de-id + 0 dispatch — `import scripts.runner.schedule.activate` + construct without `enable()`/`run()` → 0 `signal`/de-id/dispatch (spies assert 0) — verified by `test_activation_surface.py` (import-inert case).
- [ ] AC-3: enable/disable/kill-switch round-trip + status — `enable()` writes exactly 1 entry for the label, `status()` ENABLED count 1; `disable()` removes it, count 0, `status()` DISABLED (the 0→1→0 round-trip over the same counter) — verified by `test_activation_surface.py` (round-trip case).
- [ ] AC-4: plist template 0 operator-specific path (only `{{PYTHON}}`/`{{REPO_ROOT}}`/`{{LABEL}}`, count == 0) + rendered instance gitignored (`git check-ignore` returns it) — verified by `test_activation_surface.py` (template + gitignore cases).
- [ ] AC-5: missed-window catch-up honors the built debounce — template sets `StartCalendarInterval`; a catch-up tick within `MIN_REGEN_INTERVAL_DAYS` returns `regenerated: False` (0 new plans) — verified by `test_activation_surface.py` (catch-up case).
- [ ] AC-6: activation gate — over a multi-week synthetic window with the schedule DISABLED, `plan_loop.signal` call-count == 0 — verified by `test_activation_surface.py` (activation-gate case). NOTE: SUBSUMED by the AC-1+AC-3 real-state counter round-trip (trivially green alone — not the disabled-by-default proof).
- [ ] AC-7: built debounce pair once enabled — (single in-window → 0 re-gens; sustained past `MIN_REGEN_INTERVAL_DAYS` → exactly 1) via the built `_should_regenerate` (no second debounce), measured by a concrete count (spy on `plan_loop.regenerate` / promoted-`plan::`-row count / `dispatch.spec_calls()`), NOT a `regenerated` boolean (the gate-pass path carries no `regenerated` key) — verified by `test_activation_surface.py` (debounce-pair case).
- [ ] AC-8: NAMED anti-implicit-activation guard EXECUTED mutation-RED — grep arm (BOTH dotted + import-and-call forms) 0 hits over the real `init_instance.py`; behavioral arm 0 entries after `init_instance.run(<scratch>)`; the mutation injecting `activate.enable()` in BOTH forms into a scratch provision path — forced down the CRON branch — drives the REAL `crontab -l` counter to ≥1 + the guard RED, then reverted + crontab-restored in an unconditional finally; FAILS LOUD if neither `launchctl` nor `crontab` is available (never `pytest.skip`) — verified by `test_no_implicit_activation.py`. (PINNED real-state forcing mechanism, NOT a fixture-HOME counter.)
- [ ] AC-9: `.venv/bin/python -m pytest tests/runner/test_activation_surface.py tests/runner/test_no_implicit_activation.py -q` passes against fixture `HOME`/scratch roots (0 live calls, 0 real PII).
- [ ] AC-10: OAuth token NEVER in the plist (SEC-02) — scan of the template AND the rendered instance plist for the literal `CLAUDE_CODE_OAUTH_TOKEN` + the token-VALUE pattern (`sk-ant-oat` prefix + the `[A-Za-z0-9_-]+` token-char class) → 0 on BOTH, PLUS a runtime-assembled positive control (fragments concatenated at test time, never a tracked literal) proving the scan CAN hit — verified by `test_activation_surface.py` (token-never-in-plist case).
- [ ] Risk PF-S63-02 (disabled-by-default falsifiable) covered — AC-1 + AC-3 (the 0→1→0 round-trip counter).
- [ ] Risk QA-F2 / SEC-01 (anti-implicit-activation guard non-tautological) covered — AC-8 (EXECUTED mutation-RED, both call-forms, real-state counter ≥1, guard RED, revert + teardown).
- [ ] Risk SEC-02 (OAuth token at rest in the plist) covered — AC-10 (failing-capable token grep on template + rendered).
- [ ] Risk ADR-0039 Negative-5 (operator-specific path in a tracked plist) covered — AC-4 (failing-capable 0-operator-path grep + rendered gitignored).
- [ ] Risk ADR-0039 Negative-2 (machine-awake / missed window) covered — AC-5 (catch-up honoring the built debounce).
- [ ] No regression: `.venv/bin/python -m pytest -q` holds the `3 failed, ..., 7 skipped` floor (no 4th distinct failure).
- [ ] Test hygiene held: no REAL launchd/cron entry survives the run (`launchctl list` / `crontab -l` for the test label return nothing — teardown held); `scripts/clone/init_instance.py` byte-unchanged (the guard SCANS it, the mutation injects into a scratch copy only).
- [ ] Scope discipline: `git diff --name-only` shows ONLY `scripts/runner/schedule/activate.py`, `scripts/runner/schedule/cadence-runner.plist.template`, `scripts/runner/schedule/cadence-runner.crontab.template`, `.gitignore`, `tests/runner/test_activation_surface.py`, `tests/runner/test_no_implicit_activation.py` — no touch to `scripts/runner/cadence_runner.py` (T4) / `scripts/runner/subscription_dispatch.py` (T2) / the frozen glob / `scripts/serve/plan_loop.py` / `scripts/clone/init_instance.py`.
- [ ] Frozen glob byte-frozen (canonical superset — 7 plan-engine files + `plan_orchestrator.py` + `plan_driver.py` + `scripts/store/` + `scripts/serve/plan_loop.py`; Security M1 added `plan_orchestrator.py`/`plan_driver.py`, which hold the crown-jewel de-id sentinel HALT + dispatch fan-out and are frozen by T2's set too): `git diff --numstat origin/main -- 'scripts/plan/orchestrate.py' 'scripts/plan/pipeline.py' 'scripts/plan/assemble.py' 'scripts/plan/generate_plan.py' 'scripts/plan/adjudicate.py' 'scripts/plan/adjust.py' 'scripts/plan/track.py' 'scripts/plan/plan_orchestrator.py' 'scripts/plan/plan_driver.py' 'scripts/store/' 'scripts/serve/plan_loop.py'` returns EMPTY.
- [ ] Interface contract (`enable`/`disable`/`status` + the plist/crontab template contract) documented for the operator + the Architect activation-surface review.

## Commit

```
feat(runner): scheduler + operator-owned activation surface
```
Stage only:
- `scripts/runner/schedule/activate.py`
- `scripts/runner/schedule/cadence-runner.plist.template`
- `scripts/runner/schedule/cadence-runner.crontab.template`
- `.gitignore`
- `tests/runner/test_activation_surface.py`
- `tests/runner/test_no_implicit_activation.py`

Stage each path explicitly (`git add <path>` as its own command, then `git commit` separately — the commit-sequencing rule; no `git commit -a`, no pathspec `git commit <path>`, else the PII commit hook denies the single-call stage+commit). Confirm before committing: no rendered `*.rendered.plist` staged, no test-injected scratch mutation to `init_instance.py` remains in the working tree, no REAL launchd/cron test entry survives.

## Rollback

If verification fails after 3 fix cycles:
1. `git stash -m "failed-ADR-0039-T3-attempt-{N}"` — preserve the work for diagnosis.
2. `git checkout {commit-after-T1}` — revert to the entry-state HEAD recorded in Entry State (the T1 commit).
3. Teardown any leaked scheduler entry FIRST: `disable()` (or `launchctl unload` / remove the crontab line) for the test runner label — a failed T3 attempt must not leave a REAL armed entry on the machine (an un-torn-down entry is itself the implicit activation the task forbids).
4. Update the issue tracker: flag ADR-0039-T3 as BLOCKED with the failure log.
5. Escalate: "Task ADR-0039-T3 failed verification after 3 attempts. Stash ref: {ref}. Failure log: {summary}." T3 is a Wave-2 leaf (not a predecessor to T2/T4), so a BLOCK does not halt the sibling leaves — but the terminal Wave-2 → Done checkpoint cannot pass until T3 is green (the disabled-by-default / anti-implicit-activation gate is a terminal-checkpoint invariant).
6. After abandonment or re-planning, drop all `failed-ADR-0039-T3` entries from `git stash list`.

Do NOT: silently skip a failing acceptance criterion; weaken a test to make it pass (especially the AC-8 EXECUTED mutation-RED — a weakened guard re-opens the implicit-activation vector on a provisioning path, the exact PF-S63-02 sensitivity — or the AC-10 token-in-plist grep, which re-opens an at-rest secret surface); leave a REAL launchd/cron test entry armed; or proceed with the schedule in an implicitly-armed state.
