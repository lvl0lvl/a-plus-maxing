---
task-id: ADR-0017-T1
source-spec: docs/spec/adr-0015-0019-conversational-intake-spec.md
source-build-plan: docs/build-plan/build-plan-conversational-intake.md
wave: A
assigned-agent: SE
reviewers: [Security, QA]
created: 2026-06-22
status: complete
---

# Recipe: The Conversation→De-Identified-Store Extractor (Model Proposes, Gate Disposes; M-3; H-2 Wiring)

This task produces CODE: the `scripts/serve/extract.py` conversation→facts extractor and the H-2
`identity_config` wiring fix in `scripts/serve/server.py`. The extractor — `extract_facts(...)
-> ExtractionResult{candidate_facts, declined_domains, dropped}` — validates the model's extraction
proposal (NEVER trusts it) and routes the well-formed `candidate_facts` UNCHANGED to
`capture.persist_capture` (the gate, [capture.py:189-244](../../scripts/serve/capture.py#L189)
[VERIFIED]) BEFORE any `store.append`. It adds NO `store.append` of its own ("model proposes, gate
disposes"). The H-2 fix threads the instance `identity_config` into BOTH the new `/chat` capture call
AND the EXISTING `/upload` `persist_capture` call (today at [server.py:158-159](../../scripts/serve/server.py#L158)
WITHOUT `identity_config` [VERIFIED] — a factory-to-component wiring gap). It handles M-2 (malformed
input → empty `candidate_facts` + `dropped`, writes nothing, never raises into the thread) and M-3
(partial/garbled → keep well-formed, list dropped, receipt reports what ACTUALLY LANDED). It
PUBLISHES the extractor consumed by `ADR-0016-T1`'s per-turn dispatch. **STORE-SURFACE task → the
`docs/checklists/store-adversarial-tests.md` battery is BINDING** (the extractor's only store path is
through `persist_capture` → `store.append`). Built + tested against a MOCK extraction proposal (a
crafted dict, no live model). The TDD Steps follow the standard RED/GREEN/REFACTOR/REGRESSION cycle.

## Entry State

**Prerequisites:**
- [ ] `ADR-0015-T1` committed output exists: the client's `converse(...)` emits the structured extraction proposal this extractor validates. **Verification method:** `test -f scripts/model/client.py && .venv/bin/python -c "from scripts.model import client"` exits 0. **If absent, HALT** — the extraction-proposal source is not built.
- [ ] The capture gate exists, byte-unchanged: `persist_capture` with the `identity_config` param + the value gates. **Verification method:** `.venv/bin/python -c "from scripts.serve.capture import persist_capture; import inspect; assert 'identity_config' in inspect.signature(persist_capture).parameters"` exits 0; the gates `_bounded_value_ok` / `_value_has_pii` / by-data-class routing are at [capture.py:189-244](../../scripts/serve/capture.py#L189) [VERIFIED]. **If `persist_capture` lacks `identity_config`, HALT** — the H-2 wiring target is missing.
- [ ] The existing `/upload` call site DROPS `identity_config` today (the H-2 gap this task closes). **Verification method:** `rg -n "persist_capture" scripts/serve/server.py` shows the call at [server.py:158-159](../../scripts/serve/server.py#L158) WITHOUT `identity_config=` (VERIFIED — the H-2 omission). **If `identity_config=` is already threaded at the `/upload` call, NOTE it** — the gap is partly closed; the `/chat` call still needs threading.
- [ ] The identity-config default + the value-PII scan exist: `pii_scan.DEFAULT_IDENTITY_CONFIG` ([pii_scan.py:149](../../scripts/guard/pii_scan.py#L149) [VERIFIED]) + `scan_text`/`scan_text_full`. **Verification method:** `.venv/bin/python -c "from scripts.guard.pii_scan import DEFAULT_IDENTITY_CONFIG, scan_text"` exits 0.
- [ ] The store write surface exists, byte-unchanged: `store.append(item, reading, root)` + the `(item,timepoint,source)` dedupe. **Verification method:** `.venv/bin/python -c "from scripts.store import store, keying; assert keying.LINE_FIELDS == ('item','timepoint','source','value')"` exits 0 ([store.py:134](../../scripts/store/store.py#L134), [keying.py:11](../../scripts/store/keying.py#L11)). The extractor REUSES this via `persist_capture` — NO re-implementation.
- [ ] `scripts/serve/extract.py`, `tests/serve/test_extract.py` do NOT yet exist (created in this task). **Verification method:** `test ! -f scripts/serve/extract.py && test ! -f tests/serve/test_extract.py` returns 0. **If either exists, HALT and escalate** — a prior partial build is present.
- [ ] Repo-root pytest bootstrap present (`test -f conftest.py`). Full suite green (baseline): `.venv/bin/python -m pytest -q` exits 0. Working tree clean; on the Wave-A feature branch. Capture the pre-task HEAD for the 0-shared-routine-edit `git diff --numstat` baseline.

**Rollback target:** the commit at entry state (HEAD on the Wave-A feature branch before this task's commit), captured as `{commit-before-task}` in the Rollback section.

## Context Load List

**Files to read:**
- `scripts/serve/capture.py` — existing (`ADR-0014-T1` output). Read `persist_capture(fields, *, root, scaffold_root, identity_config)` ([L189-244](../../scripts/serve/capture.py#L189)): the UNCHANGED gate the extractor routes `candidate_facts` through — `WIRED_TOKENS` ([L50-56](../../scripts/serve/capture.py#L50)), the `_bounded_value_ok` / `_value_has_pii` gates ([L98-147](../../scripts/serve/capture.py#L98)), the by-data-class routing (a raw/record-only value → the gitignored scaffold, never a `SUMMARY_FIELD_SET` item). The receipt shape is `{"store": [...], "scaffold": [...]}` ([L244](../../scripts/serve/capture.py#L244)). The extractor adds NO `store.append` — its only store path is THIS gate. Read but DO NOT modify.
- `scripts/serve/server.py` — existing. Read `do_POST` ([L113-178](../../scripts/serve/server.py#L113)): the `/upload` handler calls `capture.persist_capture(fields, root=store_root, scaffold_root=scaffold_root)` at [L158-159](../../scripts/serve/server.py#L158) WITHOUT `identity_config` (the H-2 gap). Read the `store_root`/`dna_root`/`scaffold_root` class-attr test seams ([L103-105](../../scripts/serve/server.py#L103)) — this task threads `identity_config` (an instance-bound config) through the SAME shape. MODIFY only the `persist_capture` call site(s) to add `identity_config=` — DO NOT change the route table or the bind (the `/chat` route is `ADR-0016-T1`'s).
- `scripts/guard/pii_scan.py` — existing. Read `DEFAULT_IDENTITY_CONFIG` ([L149](../../scripts/guard/pii_scan.py#L149)) + `scan_text` ([L276](../../scripts/guard/pii_scan.py#L276)): the operator-identity config + the value-PII scan the gate's `_value_has_pii` uses; the H-2 wiring threads the instance config (`<instance_root>/vault/meta/operator-identity.txt`) so identity-PII detection is non-empty. Read but DO NOT modify.
- `scripts/plan/router.py` — existing. Read `summarize`'s 8j6 fail-closed gate ([L416-430](../../scripts/plan/router.py#L416)) ONLY to confirm the RUNTIME backstop (a raw-PII value in a pass-through field-set item RAISES) — the extractor's CONCERN-3 NOTE: the test asserts the gate catches identity/contact PII (email/phone/postal), NOT a clinical diagnosis in a free-text token (the accepted V1 residual, [capture.py:88-95](../../scripts/serve/capture.py#L88)). Do NOT modify.
- `scripts/store/store.py` + `scripts/store/keying.py` — existing. Read `append` ([store.py:134-162](../../scripts/store/store.py#L134)) + `LINE_FIELDS`/`dedupe_key` ([keying.py:11,19-32](../../scripts/store/keying.py#L11)): the `(item,timepoint,source)` dedupe identity the store-adversarial battery exercises on the extractor's write path. Read but DO NOT modify.
- `scripts/serve/extract.py`, `tests/serve/test_extract.py` — do not yet exist; created in this task. `tests/serve/test_capture.py` — Modify (the H-2 `identity_config` round-trip; reuse the existing `_bound`/`_ABSENT_IDENTITY` patterns at [tests/serve/test_capture.py:36-46](../../tests/serve/test_capture.py#L36)). `tests/serve/test_capture_store_adversarial.py` — the `pka` battery's home (extend with the extractor write path).

**Do NOT load:**
- The ADRs themselves beyond the gate-at-write + M-2/M-3 + H-2 + CONCERN-3 criteria already extracted.
- `scripts/serve/chat.py` (the per-turn dispatch) / the `/chat` route — `ADR-0016-T1`'s; this task builds the extractor + the H-2 wiring the dispatch calls, not the dispatch.
- `scripts/store/store.py`'s NDJSON internals beyond the `append` contract — the extractor REUSES `persist_capture` → `store.append`, never re-implementing the write.
- `scripts/ingest/*` — the shared ingest routines are byte-unchanged (NFR-4); not read or modified.

## Risk Pre-Check

Active. Each gate maps to a concrete `pytest` test, an `rg` assertion, or a `git diff --numstat`. A placeholder (`assert True`) fails each gate. **STORE-SURFACE task → the `docs/checklists/store-adversarial-tests.md` battery is BINDING (all four categories on the extractor's `persist_capture` → `store.append` write path, category 4 observed RED under a deliberately-broken keying/dedupe then reverted).**

- **ADR-0017 Negative-1 (the extractor is the FIRST model-output-trusting surface; a mis-classified fact could route raw detail toward a token).** Mitigation: criteria 1, 2. **Gate (gate-at-write, falsifiable):** a RED test asserts `rg` finds 0 `store.append` in `scripts/serve/extract.py` (the extractor's ONLY store path is through `persist_capture`'s gate), AND a crafted extraction proposal with a RAW value (email/phone/postal) under a wired token name lands in the gitignored scaffold (record-only), NOT in the token's store item. **Falsifiable RED:** the assertion turns RED if a model-emitted raw value reaches a `SUMMARY_FIELD_SET` item. **Negative-control:** temporarily add a direct `store.append` of a candidate fact in `extract.py` (bypassing the gate), confirm the `rg`-0 + record-only assertion FAILS, then revert and confirm the only store path is the gate. **CONCERN-3 NOTE:** the test asserts the gate catches identity/contact PII (email/phone/postal/identity tokens); it does NOT claim the gate catches a clinical diagnosis in a free-text token (the accepted V1 residual, [capture.py:88-95](../../scripts/serve/capture.py#L88)) — a "gate de-identifies all PHI" assertion is tautological/false and is excluded.
- **design M-2 (malformed input) / M-3 (partial/garbled).** Mitigation: criteria 3, 4. **Gate (M-2 fail-soft, falsifiable):** a RED test feeds a non-dict / unparseable `model_extraction_output` and asserts `ExtractionResult{candidate_facts={}, dropped=[...]}`, writes NOTHING, does NOT raise into the request thread, does NOT fabricate a fact. **Gate (M-3 partial, falsifiable):** a RED test feeds a mixed proposal (some well-formed, some malformed) and asserts the well-formed go in `candidate_facts` (each routed INDEPENDENTLY through `persist_capture` — it is per-field), the rest in `dropped`, and the turn receipt reports what ACTUALLY LANDED (the `persist_capture` `{"store":[...], "scaffold":[...]}` receipt) PLUS `dropped` — so the next-turn `missing_fields` is computed against what the STORE holds, not what the model claimed.
- **design H-2 (empty operator-identity detection at today's `/upload` call site).** Mitigation: criterion 5. **Gate (H-2 wiring, falsifiable):** a RED test asserts `rg` finds `identity_config=` at BOTH the new `/chat` capture call AND the existing `/upload` `persist_capture` call (closing the [server.py:158-159](../../scripts/serve/server.py#L158) omission), AND a `test_capture.py` round-trip: an operator-identity token in a free-text value routes record-only WITH `identity_config` threaded (vs the empty-detection baseline WITHOUT it). **Negative-control:** with `identity_config` threaded, the identity token is detected (record-only); without it (the baseline), detection is empty — the round-trip proves the threading changes behavior, so it is not a no-op.
- **ADR-0015 fail-closed (a failed EXTRACTION call yields zero facts, writes nothing).** Mitigation: criterion 6. **Gate:** a RED test distinguishes a FAILED extraction call (zero candidate facts, writes nothing, never a fabricated/guessed fact) from an empty `candidate_facts` from a SUCCESSFUL call (a turn that captured nothing yet — a valid distinct outcome, not a failure).
- **Store-surface battery (`pka` MANDATE).** The extractor writes to `scripts/store/` (via `persist_capture` → `store.append`), so `docs/checklists/store-adversarial-tests.md` is binding — all four categories on the extractor write path, category 4 observed RED under a deliberately-broken keying/dedupe then reverted. **Gate:** `tests/serve/test_capture_store_adversarial.py` (Modify) carries all four categories on the extractor's write path (cross-stream collision, same-timepoint dedupe, dedupe-key boundary with `value` excluded, mutation observed RED); a battery green under the mutation is a no-go.

## TDD Steps

Three cycles. **Cycle 1** builds the `extract.py` validate-and-route-through-the-gate seam + M-2/M-3 (AC-1, AC-3, AC-4, AC-6; Risk Negative-1, M-2/M-3). **Cycle 2** asserts the gate-at-write boundary (a raw-PII proposal routes record-only) + threads the H-2 `identity_config` at BOTH call sites (AC-2, AC-5; Risk Negative-1, H-2). **Cycle 3** adds the dedicated store-adversarial battery on the extractor write path (AC-7; the `pka` mandate), then AC-7 closes as the suite pass. The extraction proposal is a crafted MOCK dict — no live model.

### Cycle 1: `extract.py` Validate-and-Route-Through-the-Gate Seam + M-2/M-3 (AC-1, AC-3, AC-4, AC-6; Risk Negative-1, M-2/M-3)

#### Step 1: RED — Write Failing Tests

Call `/write-tests` for `tests/serve/test_extract.py` targeting:
- AC-1 (gate-at-write, the only store path): assert `rg` finds 0 `store.append` in `scripts/serve/extract.py` AND a test that the extractor's only store path is through `persist_capture` (feed a well-formed proposal, assert the facts land via `persist_capture`'s receipt, not a direct write).
- AC-3 / M-2 (malformed input): a non-dict `model_extraction_output`, or a proposal `extract_facts` cannot parse, yields `ExtractionResult{candidate_facts={}, dropped=[<unparseable field names>]}`, writes NOTHING, does NOT raise into the request thread, does NOT fabricate a fact — assert per malformed case.
- AC-4 / M-3 (partial/garbled): a mixed proposal (some well-formed + some malformed) → the well-formed in `candidate_facts` (each routed INDEPENDENTLY through the gate), the rest in `dropped`; the receipt reports what ACTUALLY LANDED (the `persist_capture` receipt) PLUS `dropped`; assert the next-turn `missing_fields` is computed against the STORE (landed-only), not the model's claim.
- AC-6 (failed vs empty distinction): a FAILED extraction call yields ZERO candidate facts + writes nothing (never a fabricated fact); an empty `candidate_facts` from a SUCCESSFUL call (a turn that captured nothing) is a valid distinct outcome — assert the two cases are distinguished.

Run: `.venv/bin/python -m pytest tests/serve/test_extract.py`
Expected: FAIL — the test imports `extract_facts` from `scripts/serve/extract.py`, which does not yet exist (ImportError).

#### Step 2: GREEN — Implement

Changes to make:
- `scripts/serve/extract.py`: Create the module exposing `extract_facts(model_extraction_output, turn_text) -> ExtractionResult{candidate_facts: dict, declined_domains: set, dropped: list}`. It VALIDATES the model proposal (never trusts it): a non-dict / unparseable proposal yields `candidate_facts={}` + `dropped`, writes nothing, never raises into the thread; a partial proposal keeps the well-formed facts in `candidate_facts` and lists the malformed in `dropped`. The well-formed `candidate_facts` are routed UNCHANGED to `capture.persist_capture` (the gate) — the extractor adds NO `store.append` of its own. A FAILED extraction call yields zero facts + writes nothing; an empty-from-success is a valid distinct outcome. The returned result + the `persist_capture` receipt drive the next-turn gap-set against what LANDED. This is the published surface — see Interface Contracts.

Run: `.venv/bin/python -m pytest tests/serve/test_extract.py`
Expected: PASS.

#### Step 3: REFACTOR

Review the new module for:
- The extractor's ONLY store path is `persist_capture` — 0 `store.append` in `extract.py` (re-confirm AC-1's `rg`=0); it never re-implements the store write.
- The validation is fail-soft (M-2/M-3): malformed input → `dropped`, never a raise into the request thread, never a fabricated fact.
- The receipt reports what ACTUALLY LANDED (the `persist_capture` receipt), so the gap-set is computed against the STORE, not the model's claim.

Run: `.venv/bin/python -m pytest tests/serve/test_extract.py`
Expected: PASS (no behavior change).

#### Step 4: REGRESSION

Run: `.venv/bin/python -m pytest -q`
Expected: PASS. No tests outside this task's scope break (only the new `tests/serve/test_extract.py` is added).

### Cycle 2: Gate-at-Write Boundary (Raw-PII Proposal → Record-Only) + H-2 `identity_config` Wiring at BOTH Call Sites (AC-2, AC-5; Risk Negative-1, H-2)

#### Step 5: RED — Write Failing Tests

Call `/write-tests` for `tests/serve/test_extract.py` + `tests/serve/test_capture.py` (Modify):
- AC-2 (raw-PII proposal routes record-only): feed a crafted extraction proposal with a RAW value (email/phone/postal) under a WIRED token name and assert it lands in the gitignored scaffold (record-only), NOT in the token's store item. **CONCERN-3 NOTE:** the test asserts identity/contact PII (email/phone/postal/identity tokens) is caught; it does NOT claim a clinical diagnosis in a free-text token is caught (the accepted V1 residual — a tautological/false test, excluded).
- **Negative-control RED step for the gate-at-write (one-time):** temporarily add a direct `store.append` of a candidate fact in `extract.py` (bypassing the gate), confirm the AC-1 `rg`-0 + the AC-2 record-only assertion FAILS, then revert and confirm the only store path is the gate. Document the negative control.
- AC-5 / H-2 (`identity_config` threaded at BOTH call sites): assert `rg` finds `identity_config=` at the new `/chat` capture call AND the existing `/upload` `persist_capture` call (closing [server.py:158-159](../../scripts/serve/server.py#L158)); a `test_capture.py` round-trip asserts an operator-identity token in a free-text value routes record-only WITH `identity_config` threaded (vs the empty-detection baseline WITHOUT it).

Run: `.venv/bin/python -m pytest tests/serve/test_extract.py tests/serve/test_capture.py`
Expected: FAIL — the gate-at-write boundary proof + the H-2 `identity_config` threading do not yet exist (the `/upload` call still drops `identity_config`).

#### Step 6: GREEN — Implement

Changes to make:
- `scripts/serve/extract.py`: Confirm the routing already satisfies the boundary (the well-formed `candidate_facts` go through `persist_capture`'s gate; a raw-PII value under a wired token routes record-only by the gate's own logic). If the boundary test reveals a leak, REPAIR the routing — do NOT weaken the test. (The gate is the existing `persist_capture`; the extractor must not bypass it.)
- `scripts/serve/server.py`: Thread the instance `identity_config` into the existing `/upload` `persist_capture` call at [L158-159](../../scripts/serve/server.py#L158) — add `identity_config=` (an instance-bound config, e.g. `<instance_root>/vault/meta/operator-identity.txt`, threaded through a class-attr test seam in the same shape as `store_root`/`scaffold_root`). This closes the H-2 omission. (The `/chat` capture call's `identity_config` is threaded in `ADR-0016-T1`'s dispatch, but THIS task adds the `identity_config` test seam + the `/upload` threading; the `rg`-both-call-sites assertion is satisfied once `ADR-0016-T1` lands the `/chat` call — the test for the `/chat` half is written here and goes green when the dispatch is wired, OR this task adds a minimal `/chat` capture call site stub that `ADR-0016-T1` extends. Pin the threading mechanism here.) Satisfies AC-2, AC-5.

Run: `.venv/bin/python -m pytest tests/serve/test_extract.py tests/serve/test_capture.py`
Expected: PASS.

#### Step 7: REFACTOR

Review for:
- The gate-at-write boundary: a raw-PII proposal routes record-only via the EXISTING `persist_capture` gate — the extractor never bypasses it (re-confirm AC-1's `rg`=0 + AC-2).
- The H-2 `identity_config` is threaded as an instance-bound config (not the cwd default) so identity-PII detection is non-empty; the round-trip proves the threading changes behavior (not a no-op).
- The CONCERN-3 boundary: the tests do NOT claim the gate catches clinical PHI in a free-text token (the accepted V1 residual stands for both form and extractor).

Run: `.venv/bin/python -m pytest tests/serve/test_extract.py tests/serve/test_capture.py`
Expected: PASS (no behavior change).

#### Step 8: REGRESSION

Run: `.venv/bin/python -m pytest -q`
Expected: PASS. No tests outside this task's scope break (the existing `/upload` E2E stays green with `identity_config` now threaded).

### Cycle 3: Store-Adversarial Battery on the Extractor Write Path (AC-7; `pka` mandate)

#### Step 9: RED — Write Failing Tests

Call `/write-tests` for `tests/serve/test_capture_store_adversarial.py` (Modify) — the `pka` battery on the extractor's `persist_capture` → `store.append` write path:
1. **Cross-stream namespace collision:** an extractor write to one token (`goal-domains`) never cross-reads as another token (`hard-limits`); a `store.read` for token X never returns token Y's value.
2. **Same-timepoint dedupe:** two DISTINCT extractor captures legitimately sharing a timepoint both persist (different item/source); an identical re-capture of the same `(item, timepoint, source)` stays idempotent (the second `store.append` is a no-op, [store.py:160-161](../../scripts/store/store.py#L160)).
3. **Dedupe-key boundary (`value` EXCLUDED):** same `(item, timepoint, source)` + a DIFFERENT value collides (the second write dropped, by design, [keying.py:15-16](../../scripts/store/keying.py#L15)); any single field differing does NOT collide.
4. **Mutation-style verification:** at least one test FAILS when the keying/dedupe is deliberately broken (e.g. the dedupe key widened to include `value`, or the `source:"intake"` tag dropped). Run the mutation, observe RED, REVERT. A battery green under the mutation is tautological.

Run: `.venv/bin/python -m pytest tests/serve/test_capture_store_adversarial.py`
Expected: FAIL — the extractor write-path battery's expectations are not yet met (the new write-path cases are absent or the mutation does not turn it RED).

#### Step 10: GREEN — Implement

Changes to make:
- No production change is needed for the store-adversarial battery (AC-7) — it exercises the EXISTING `store.append`/`keying` behavior the extractor write path REUSES (via `persist_capture`); the battery PROVES the extractor write path inherits the store's collision/dedupe guarantees. If category 4's mutation does NOT turn the battery RED, the battery is tautological — STRENGTHEN it (it is testing nothing), do not weaken the production code.

Run: `.venv/bin/python -m pytest tests/serve/test_capture_store_adversarial.py`
Expected: PASS.

#### Step 11: REFACTOR

Review for:
- The store-adversarial battery's category 4 actually ran RED under the mutation (the test file documents the mutation that broke it + that it was reverted) — non-tautological per the `pka` mandate.
- The battery covers the EXTRACTOR write path (the facts routed through `persist_capture`), not only the form-capture path the prior `ADR-0014-T1` battery covered.
- The extractor's write path is the `persist_capture` → `store.append` REUSE — the battery confirms 0 forked store-write logic.

Run: `.venv/bin/python -m pytest tests/serve/test_capture_store_adversarial.py`
Expected: PASS (no behavior change).

#### Step 12: REGRESSION

Run: `.venv/bin/python -m pytest -q`
Expected: PASS — the `tests/serve/` suites green; no tests outside this task's scope broken. Then confirm this task's exit subset passes as a unit: `.venv/bin/python -m pytest tests/serve/test_extract.py tests/serve/test_capture.py` (AC-7), and the 0-shared-routine-edit proof `git diff --numstat <pre-task> -- scripts/ingest/ingest.py scripts/ingest/adapter.py scripts/ingest/scheduler.py` reports 0.

## Interface Contracts

This task PUBLISHES the extractor + closes the H-2 wiring gap, consumed by `ADR-0016-T1`'s per-turn dispatch, so the contract is load-bearing.

- **Function:** `extract_facts(model_extraction_output, turn_text) -> ExtractionResult{candidate_facts: dict, declined_domains: set, dropped: list}` in `scripts/serve/extract.py` — **NAMED CONTRACT (frozen at this task):** validates the model proposal (never trusts it) and routes the well-formed `candidate_facts` UNCHANGED to `capture.persist_capture` (the gate). **Invariants (load-bearing):** (a) the extractor's ONLY store path is `persist_capture` — 0 `store.append` in `extract.py` (AC-1, ADR-0017 Confirmation); (b) a model-emitted raw value under a token name routes record-only / rejected, never silently into a `SUMMARY_FIELD_SET` item (AC-2, ADR-0017 Falsification); (c) M-2/M-3 fail-soft — malformed input → `dropped`, never a raise into the thread, never a fabricated fact (AC-3, AC-4); (d) a failed extraction yields zero facts (AC-6). **Dependent:** `ADR-0016-T1`'s dispatch calls `extract_facts` → `persist_capture` per turn.
- **H-2 wiring:** the instance `identity_config` is threaded into BOTH the `/chat` capture call (`ADR-0016-T1` wires the `/chat` half over this task's seam) AND the existing `/upload` `persist_capture` call (this task closes the [server.py:158-159](../../scripts/serve/server.py#L158) omission). **Invariant (load-bearing):** operator-identity PII detection is non-empty at BOTH capture call sites (the same gate, the same instance config). **Breaks if:** a capture call site drops `identity_config` (the H-2 gap re-opens — empty identity detection).

## Verification Checklist

- [ ] **AC-1** (gate-at-write: 0 `store.append` in `extract.py`; the only store path is `persist_capture`) — verified by the `test_extract.py` `rg` + routing test. Failing-capable per the negative control. ADR-0017 Confirmation.
- [ ] **AC-2** (raw-PII proposal under a wired token routes record-only, NOT the store item; CONCERN-3 NOTE: identity/contact PII, not clinical PHI) — verified by the `test_extract.py` boundary test. Mitigates ADR-0017 Negative-1 / Falsification.
- [ ] **AC-3** (M-2: malformed input → `candidate_facts={}` + `dropped`, writes nothing, no raise into the thread, no fabricated fact) — verified per malformed case.
- [ ] **AC-4** (M-3: partial → well-formed in `candidate_facts` (per-field gate), rest in `dropped`; receipt reports what LANDED + `dropped`; gap-set computed against the STORE) — verified by the mixed-proposal test.
- [ ] **AC-5** (H-2: `identity_config` threaded at BOTH `/chat` + `/upload` call sites; the round-trip proves record-only WITH it vs empty-detection baseline) — verified by the `rg`-both-call-sites + the `test_capture.py` round-trip.
- [ ] **AC-6** (failed extraction → zero facts + writes nothing; empty-from-success is a valid distinct outcome) — verified by the two-case distinction test.
- [ ] **AC-7** (`pytest tests/serve/test_extract.py tests/serve/test_capture.py` passes) — verified by running that exact command.
- [ ] **Store-surface battery (`pka`):** `docs/checklists/store-adversarial-tests.md` satisfied on the extractor write path — all four categories present + non-tautological (category 4 ran RED), in `tests/serve/test_capture_store_adversarial.py`. Checked at Tier-1 self-check AND named in the Tier-2 QA dispatch prompt.
- [ ] No regression in full test suite — `.venv/bin/python -m pytest -q` (Step 12) green.
- [ ] 0-shared-routine-edit — `git diff --numstat <pre-task> -- scripts/ingest/ingest.py scripts/ingest/adapter.py scripts/ingest/scheduler.py` = 0.
- [ ] Files modified match the file manifest — only `scripts/serve/extract.py`, `scripts/serve/server.py`, `tests/serve/test_extract.py`, `tests/serve/test_capture.py`, `tests/serve/test_capture_store_adversarial.py`.
- [ ] Interface contracts documented — `extract_facts` + the H-2 dual-call-site wiring, with the gate-at-write + M-2/M-3 invariants and the dependent task ID `ADR-0016-T1`.

## Commit

```
feat(serve): add conversation->de-identified-store extractor + thread identity_config (H-2)
```
Stage only: `scripts/serve/extract.py`, `scripts/serve/server.py`, `tests/serve/test_extract.py`, `tests/serve/test_capture.py`, `tests/serve/test_capture_store_adversarial.py`. Do NOT stage `conftest.py`, any `scripts/serve/capture.py` / `scripts/plan/router.py` / `scripts/store/store.py` file (the byte-unchanged reused gate/store — staging would risk the 0-edit posture), `scripts/model/` (the `ADR-0015-T1` prerequisite, unchanged), `scripts/serve/chat.py` (the `ADR-0015-T2`/`ADR-0016-T1` dispatch, not this task's), or ANY `vault/scaffold/filled/` value or `vault/store/` file (gitignored operator data — `block-pii-commit` denies them). Stage with `git add` as its OWN command, then `git commit` separately (the commit-sequencing rule). One commit for the task.

## Rollback

If verification fails after 3 fix cycles:
1. `git stash -m "failed-ADR-0017-T1-attempt-N"` — preserve work for diagnosis.
2. `git checkout {commit-before-task}` — revert the tracked tree to the Entry State commit. Remove the leftover `scripts/serve/extract.py` + the new test file if a failed run created them (ONLY if they did not exist at entry). Remove any `vault/scaffold/filled/` value or `vault/store/<token>.ndjson` written into a REAL (non-tmp) instance during a failed run (gitignored, but scrub so a later fresh-clone scan is clean).
3. Flag: mark `ADR-0017-T1` BLOCKED with the failure log (which AC test stayed red + the observed output). Note this task BLOCKS `ADR-0016-T1` (the per-turn dispatch calls the extractor) and `ADR-0017-T2` (the end-to-end runs through it).
4. Escalate: "Task ADR-0017-T1 failed verification after 3 attempts. Stash ref: failed-ADR-0017-T1-attempt-N. Failure log: [which AC test failed and what was tried]. Downstream gated: ADR-0016-T1, ADR-0017-T2."
5. Stash cleanup: after abandonment or re-planning, drop all `failed-ADR-0017-T1` entries from `git stash list`.

Do NOT:
1. Silently skip a failing acceptance criterion (e.g. ship without the AC-1 gate-at-write proof, the AC-5 H-2 wiring, or the AC-7 store-adversarial category-4 mutation).
2. Weaken a test to make it pass (e.g. relax AC-2 to assert "the value exists somewhere" instead of "NOT in the token's store item"; relax AC-5 to drop `identity_config` at one call site; drop the AC-7 category-4 mutation; claim the gate catches clinical PHI in a free-text token — the excluded tautological assertion).
3. Add a `store.append` in `extract.py` — the extractor's only store path is `persist_capture`'s gate; a direct write bypasses the gate (HALT, do not add).
4. Edit `capture.py` / `store.py` / `router.py` to "make the extraction work" — the extractor REUSES the gate + the store unchanged; if routing "needs" a gate change, escalate, do not edit the seam.
5. Hit a LIVE model — the extraction proposal is a crafted mock dict; a live call is a no-go (no-live-API).
6. Drop `identity_config` at the `/upload` call site "because it worked before" — the H-2 gap (empty identity detection) is the exact factory-to-component wiring class the project mandates closing; thread it at BOTH sites.
