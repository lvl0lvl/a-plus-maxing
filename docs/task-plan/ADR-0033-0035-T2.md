---
task-id: ADR-0033-0035-T2
source-spec: docs/spec/adr-0033-0035-intake-onboarding-spec.md
source-build-plan: docs/build-plan/build-plan-adr-0033-0035-intake.md
wave: 2
assigned-agent: SE
reviewers: [Architect, Security, QA, plan-integrity]
created: 2026-06-30
status: approved
depends-on: [ADR-0033-0035-T1]
---

# Recipe: OQ-5 Actual-Age + Actual-Weight De-Association (REPURPOSE) — Full DOB + Weight Series Stay LOCAL, Only the Exact Age + Current-Weight+Trend Cross; SCOPED-Additive `router.py` Deriver Edit + the `capture.py` Band-Consumer De-Wire (RETAIN the `BODYWEIGHT_BANDS` constant per bead `rod1`); `SUMMARY_FIELD_SET` Tuple + `dispatch` BYTE-UNCHANGED; Non-Tautological; Mock/Fixture-Tested; 0 Live Spend

A Wave-2 task (parallel with T5; depends on T1 — T2 adds the DOB/weight region to `capture.py` OVER T1's W1 routing skeleton). It lands ADR-0034's amended OQ-5 mechanism, RESOLVED as **REPURPOSE** (no token added, none removed): the `SUMMARY_FIELD_SET` tuple ([router.py:23-56](../../../scripts/plan/router.py) [VERIFIED]) stays BYTE-IDENTICAL while the `training-age-band` and `bodyweight-band` tokens' DERIVATION changes. `training-age-band`'s deriver `_age_band` ([router.py:324-340](../../../scripts/plan/router.py), today a born-decade bucketer) is repurposed to emit the EXACT AGE (an integer-years scalar) computed at summarize-time from the locally-stored full `date-of-birth` (still a named-excluded raw-PII source [router.py:64](../../../scripts/plan/router.py), never crossing). `bodyweight-band` changes from a pass-through `WIRED_TOKENS`/`_BOUNDED_ENUMS` band select to a `_RAW_TO_FIELD`-derived token sourced from a NEW local `bodyweight-kg` dated time-series (named-excluded raw-PII — the per-day weight log stays local + feeds the dashboard chart), with a deriver emitting the de-associated CURRENT WEIGHT + TREND. The full DOB never appears in `summarize` output or any model request; the exact age + current weight + trend ARE EXPECTED de-associated facts (the crown jewel, NFR-1).

It is a **SCOPED-ADDITIVE** change to `router.py` — the ONE sanctioned plan-path edit this build names (the ADR-0019 / ADR-0032-T3 precedent), verified by a SCOPED-DIFF assertion (NOT numstat=0 on router.py). The sanctioned hunks: (a) `_age_band`'s body repurposed to the exact-age deriver; (b) a NEW current-weight+trend deriver; (c) a NEW `_FIELD_DERIVATION` `"bodyweight-band"` entry ([router.py:560-568](../../../scripts/plan/router.py)); (d) a NEW `_RAW_TO_FIELD` `"bodyweight-kg": "bodyweight-band"` entry ([router.py:116-125](../../../scripts/plan/router.py)); (e) a NEW `EXCLUDED_RAW_PII` `"bodyweight-kg"` member ([router.py:61-96](../../../scripts/plan/router.py)). The `SUMMARY_FIELD_SET` tuple, `dispatch` ([router.py:742-802](../../../scripts/plan/router.py)), the disjointness/tripwire asserts ([router.py:590-591](../../../scripts/plan/router.py)), and every OTHER deriver are BYTE-UNCHANGED (EXTEND-NOT-REBUILD, NFR-2). On the capture side: `bodyweight-band` is removed from `WIRED_TOKENS` ([capture.py:62](../../../scripts/serve/capture.py)) and `_BOUNDED_ENUMS` ([capture.py:124](../../../scripts/serve/capture.py)) — DE-WIRED from the live capture path — while the `BODYWEIGHT_BANDS` tuple constant ([capture.py:118](../../../scripts/serve/capture.py)) is RETAINED (still imported by the superseded non-served legacy `intake.py`; full deletion is bead `rod1`, OUT of this build — see the Adjudicated Scope Note); the actual weight routes to the named-excluded local `bodyweight-kg` time-series (a new capture special-case mirroring the `_DOB_FIELD` raw-source case [capture.py:276-281](../../../scripts/serve/capture.py)); the `assert set(WIRED_TOKENS) <= set(SUMMARY_FIELD_SET)` tripwire ([capture.py:193](../../../scripts/serve/capture.py)) still holds (a removed member keeps the subset).

The frozen plan ENGINE (`scripts/plan/{orchestrate,pipeline,assemble,generate_plan,adjudicate,adjust,track}.py`) + `scripts/store/*.py` stay numstat=0 from `$(git merge-base HEAD origin/main)` (= `12f769f`, verified 2026-06-30 S103 — the executable base is computed DYNAMICALLY per QA-F6, the literal is the as-of annotation). The build is MOCK/FIXTURE-tested at 0 live spend (NFR-3): every test injects a fixture `store_read` (router-side) or a tmp store/scaffold root (capture-side) — 0 live API, 0 key, 0 network, no `key_source.resolve()`-gated call, no `converse`/curation call. The operator-present LIVE run is NOT in T2 (multi-tenancy + the live care-agent run are deferred per the spec disposition).

---

## Adjudicated Scope Note — DE-WIRE, do NOT delete (spec AMENDED 2026-06-30 S103; 7-file manifest)

This recipe surfaced a blast-radius gap (the de-wiring of `bodyweight-band` reaches call sites beyond T2's original 4-file manifest). It is ADJUDICATED + the spec is AMENDED (`docs/spec/adr-0033-0035-intake-onboarding-spec.md`, inline `[AMENDED 2026-06-30 S103]` in the T2 File Manifest rows + AC8). The disposition:

- **DE-WIRE, do NOT delete.** T2 removes `bodyweight-band` from `capture.WIRED_TOKENS` ([capture.py:62](../../../scripts/serve/capture.py)) + `capture._BOUNDED_ENUMS` ([capture.py:124](../../../scripts/serve/capture.py)) — so the actual-weight number routes to the named-excluded `bodyweight-kg` local series, NOT a `bodyweight-band` pass-through write — but **RETAINS the `BODYWEIGHT_BANDS` tuple CONSTANT** ([capture.py:118](../../../scripts/serve/capture.py)). The constant is still imported by the superseded, non-served `vault/design/templates/intake.py` (the old ADR-0004 6-step guided flow; the server renders `generate.run('app')` = app_view.html, never `'intake'`). De-wiring fully achieves the de-id intent (no `bodyweight-band` pass-through write; the planner gets the de-associated current weight + trend derived from `bodyweight-kg`). Fully deleting the constant + retiring `intake.py` is a tracked follow-up bead (`a-plus-maxing-rod1`), OUT of this build.
- **7-file manifest** (the Factory-to-Component Wiring Rule — every STORE-SEED + summarize-driving call site of the de-wired/repurposed token updated in the same PR): the original 4 (`scripts/plan/router.py`, `scripts/serve/capture.py`, `tests/plan/test_router.py`, `tests/serve/test_capture.py`) + **`tests/serve/test_intake_demographics.py`** + **`tests/serve/test_capture_store_adversarial.py`** + **`tests/genetics/test_dna_aware_plan_e2e.py`**.
  - `tests/serve/test_intake_demographics.py` — reconcile the `("bodyweight-band", capture.BODYWEIGHT_BANDS)` WIRED-band assertion + the AC-1 orphan-lit negative control ([:48,:56,:154-162,:218-232,:410](../../../tests/serve/test_intake_demographics.py)) to the NEW behavior: a captured weight writes a `bodyweight-kg` LOCAL reading and `summarize` DERIVES the de-associated current-weight+trend `bodyweight-band` token (NOT a directly-captured WIRED band). (`capture.BODYWEIGHT_BANDS` references no longer ImportError — the constant is retained — but the wired-band SEMANTICS must be reconciled to the derived contract.) Reconcile the born-year→born-decade `training-age-band` cases ([:39,:126,:212](../../../tests/serve/test_intake_demographics.py)) to the full-DOB exact-age contract.
  - `tests/serve/test_capture_store_adversarial.py` — the `pka` Cat-1 demographic cross-stream/dedupe/mutation battery keys on `bodyweight-band` ([:438-523](../../../tests/serve/test_capture_store_adversarial.py)); re-key those cases onto the new `bodyweight-kg` LOCAL series (or another live demographic token), preserving the cross-stream-isolation + dedupe-collision + mutation coverage `docs/checklists/store-adversarial-tests.md` mandates.
  - `tests/genetics/test_dna_aware_plan_e2e.py` — **the 7th call site (judge live-traced).** Its `_CLEAN_RECORDS` seeds `("bodyweight-band", "80-90kg", "intake")` as a STORE record ([:116](../../../tests/genetics/test_dna_aware_plan_e2e.py)) and `_drive_plan_signal` runs the REAL `router.summarize`→`router.dispatch` ([:256-257](../../../tests/genetics/test_dna_aware_plan_e2e.py)). After T2 adds `bodyweight-kg → bodyweight-band` to `_RAW_TO_FIELD`, `summarize`'s `source_items` branch ([router.py:713-719](../../../scripts/plan/router.py)) wins for `bodyweight-band` (now a DERIVED token); the directly-stored `bodyweight-band` item is IGNORED and — with no `bodyweight-kg` source — the token is OMITTED → `dispatch` raises `partial summary, missing ['bodyweight-band']` → the E2E REDs the Wave-2 full-suite gate. **Fix:** re-key the `_CLEAN_RECORDS` seed ([:116]) from `("bodyweight-band", "80-90kg", "intake")` to a `bodyweight-kg` reading (`("bodyweight-kg", "82", "intake")`) so `summarize` DERIVES the `bodyweight-band` token from the source. The E2E asserts on `genetic-trait-classes`, not the weight value — it only needs the token PRESENT so `dispatch` stays complete; the `_bodyweight_trend` deriver's insufficient-series floor (a single reading → current + `flat`) produces the token. (Note: the E2E's `_append` writes all `_CLEAN_RECORDS` under ONE fixed `PLAN_DATE`/source, so a 2-reading series via `_CLEAN_RECORDS` would dedupe-collide; if the implementer's `_bodyweight_trend` instead OMITS on a single reading, add a second earlier-dated `bodyweight-kg` `_append` with a distinct timepoint rather than a 2nd `_CLEAN_RECORDS` tuple.)
- **Do NOT touch** `vault/design/templates/intake.py`, `scripts/generate/generate.py`, `tests/generate/test_intake.py` (they keep working against the RETAINED constant; their retirement is bead `rod1`). `tests/serve/test_app_shell.py` is **T5's (W2)** — adding it to T2's manifest would collide on the same wave; its `training-age-band == "born-1980s"` demographic round-trip markup assertion is fixed IN T4 (T4 asserts the raw-source `date-of-birth` round-trip, NOT the deriver-output value), so it never depends on T2's deriver change — no cross-wave coupling.

The full-suite-green Wave-2 checkpoint stays ACHIEVABLE under this disposition (no production import breaks; the 3 reconciled tests track the new behavior). This is no longer a HALT.

---

## Entry State

**Prerequisites:**
- [ ] Working tree clean: `git status` shows no code changes (the daemon `vault/**` frontmatter churn is daemon noise — leave it uncommitted, do not stage it; CLAUDE.md § Vault hygiene).
- [ ] On feature branch `feature/intake-build` (the ADR-0033/0034/0035 build branch, cut off `main`; `$(git merge-base HEAD origin/main)` = `12f769f` as of 2026-06-30).
- [ ] **The Wave-1 checkpoint Go/No-Go RAN green** (executed, not reasoned) — T1's capture routing skeleton + T4's wizard markup landed, the full suite was green minus the 2 named env failures, the frozen-7 + `scripts/store/*` numstat=0, and `router.py` numstat=0 (T1/T4 do not touch it). T2 is a Wave-2 task — it does NOT proceed until the prior wave's checkpoint passed (V1 Build Execution: the wave is the unit).
- [ ] **T4's W1 demographic rework is present** (the ordering precondition — T4 owns the `bodyweight-band` band-`<select>` markup; T2 does not touch `app_view.html`/`test_app_shell.py`): `bodyweight-band` is removed from `app_shell._DEMOGRAPHIC_SELECTS` ([app_shell.py:40](../../../vault/design/templates/app_shell.py)) and the band-`<select>` markup is reworked to the weight-number input. Verify: `! grep -q "bodyweight-band" vault/design/templates/app_shell.py && echo OK` (if this fails, T4's W1 work is incomplete — route back to T4 before proceeding; the de-wire's effect on the rendered band markup is T4's, not T2's). (`BODYWEIGHT_BANDS` is RETAINED, so any residual `capture.BODYWEIGHT_BANDS` reference in `test_app_shell.py` still resolves — not a T2 break.)
- [ ] Full test suite passes at the behavior-preservation baseline: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest -q` green with port 8765 FREE (or EXACTLY the 2 named env tests failing if the operator's intake server holds 8765 — the documented delta, NOTHING else). Re-baseline only with a recorded count delta.
- [ ] Loopback default port 8765 FREE: `! lsof -nP -iTCP:8765 -sTCP:LISTEN >/dev/null 2>&1 && echo "8765 FREE"`.
- [ ] Infrastructure prerequisite verified — the EXACT router anchors T2 extends are present, in the grounded shape, and carry NO bodyweight-kg / exact-age text yet (so the AC-2/AC-3/AC-4 probes are genuinely RED-first):
      `grep -q "SUMMARY_FIELD_SET = (" scripts/plan/router.py && grep -q "EXCLUDED_RAW_PII = (" scripts/plan/router.py && grep -q "isdisjoint(set(EXCLUDED_RAW_PII))" scripts/plan/router.py && grep -q "set(_RAW_TO_FIELD) <= set(EXCLUDED_RAW_PII)" scripts/plan/router.py && grep -q "_FIELD_DERIVATION = {" scripts/plan/router.py && grep -q "def _age_band" scripts/plan/router.py && grep -q '"date-of-birth": "training-age-band"' scripts/plan/router.py && grep -q "def dispatch(summary" scripts/plan/router.py && ! grep -q "bodyweight-kg" scripts/plan/router.py && echo OK`.
- [ ] Infrastructure prerequisite verified — the capture anchors T2 reworks are present, and `bodyweight-kg` is a clean new item name on the SURFACES T2 OWNS (router.py + capture.py — NOT a repo-wide grep: T4/W1 legitimately introduces `bodyweight-kg` into `tests/serve/test_app_shell.py` (the AC-2 markup input + the AC-7 prefill seed) and the vault markup BEFORE T2 runs at W2, so a `scripts tests` glob would false-fail at W2-entry while T2's RED-first surfaces stay clean):
      `grep -q "WIRED_TOKENS = (" scripts/serve/capture.py && grep -q "BODYWEIGHT_BANDS = (" scripts/serve/capture.py && grep -q '"bodyweight-band":' scripts/serve/capture.py && grep -q 'set(WIRED_TOKENS) <= set(SUMMARY_FIELD_SET)' scripts/serve/capture.py && grep -q "_DOB_FIELD = " scripts/serve/capture.py && ! grep -q "bodyweight-kg" scripts/plan/router.py scripts/serve/capture.py && echo OK`.
- [ ] Infrastructure prerequisite verified — the `pii_scan` fail-closed surface the crown-jewel probe reuses (the canonical scanner the plan invokes at T8/T9):
      `grep -q "from scripts.guard import pii_scan" scripts/plan/router.py && .venv/bin/python -c 'from scripts.guard.pii_scan import scan_text, scan_text_full, DEFAULT_IDENTITY_CONFIG' && echo OK`.
- [ ] Infrastructure prerequisite verified — the router + capture test harnesses the T2 cases reuse VERBATIM are present:
      `grep -q "def _store_read_factory" tests/plan/test_router.py && grep -q "def _clean_records" tests/plan/test_router.py && grep -q "def _reading" tests/plan/test_router.py && grep -q "def _bound" tests/serve/test_capture.py && grep -q "def _summary" tests/serve/test_capture.py && echo OK`.
- [ ] `plan-integrity` has grounded the plan AND attested the Wave-1 checkpoint RAN green; it has confirmed the adjudicated 7-file manifest (DE-WIRE-not-delete; `BODYWEIGHT_BANDS` retained per bead `rod1`) covers EVERY live store-seed + summarize-driving call site of the de-wired/repurposed `bodyweight-band` token (incl. the `tests/genetics/test_dna_aware_plan_e2e.py` `_CLEAN_RECORDS` seed).

This entry state doubles as the rollback target — the entry-state commit is the branch HEAD this task is built on top of.

## Context Load List

**Files to read:**
- `scripts/plan/router.py` — the SCOPED-ADDITIVE edit target. Read the seams: `SUMMARY_FIELD_SET` ([:23-56], the closed allowlist — BYTE-UNCHANGED, `training-age-band` :25 + `bodyweight-band` :27 stay verbatim members; the stale "born-decade band" comment :44-45 is a misnomer the ADR sanctioned — DO NOT edit the tuple members); `EXCLUDED_RAW_PII` ([:61-96], `date-of-birth` :64 — T2 APPENDS one new member `"bodyweight-kg"`); `_RAW_TO_FIELD` ([:116-125], `date-of-birth → training-age-band` :117 — T2 APPENDS `"bodyweight-kg": "bodyweight-band"`); the disjointness tripwires ([:590-591] — `set(_RAW_TO_FIELD) <= set(EXCLUDED_RAW_PII)` :590 REQUIRES `bodyweight-kg` ∈ EXCLUDED once it joins `_RAW_TO_FIELD`; `set(SUMMARY_FIELD_SET).isdisjoint(set(EXCLUDED_RAW_PII))` :591 still holds — `bodyweight-band` ∈ field-set, `bodyweight-kg` ∈ excluded, disjoint); `_age_band` ([:324-340], **the deriver whose BODY T2 repurposes** born-decade → exact age; KEEP the function name `_age_band` so the `_FIELD_DERIVATION` `training-age-band` line stays byte-unchanged — minimal diff); `_FIELD_DERIVATION` ([:560-568], `training-age-band → _age_band` :561 byte-unchanged — T2 APPENDS `"bodyweight-band": <new deriver>`); `_ALWAYS_SET_DERIVED` ([:578-583], byte-unchanged — neither token is always-set; both stay CONDITIONAL, omitted when their source is absent, AC-6); `summarize` ([:630-739] — UNCHANGED; the conditional `source_items` path :712-719 now routes `bodyweight-band` through the new deriver once it is in `_RAW_TO_FIELD`, exactly as `training-age-band` already routes; the signature + body are byte-unchanged); `dispatch` ([:742-802] + the `set(payload) <= set(SUMMARY_FIELD_SET)` whitelist :777 — byte-unchanged; it already REJECTS `bodyweight-kg`/`date-of-birth` as out-of-field-set). READ for the pattern; modify ONLY the 5 sanctioned hunks.
- `scripts/serve/capture.py` — the band-consumer DE-WIRE target. Read: `WIRED_TOKENS` ([:50-64], REMOVE `bodyweight-band` :62); `BODYWEIGHT_BANDS` ([:118], **RETAIN the constant** — legacy `intake.py` importer; bead `rod1`); `_BOUNDED_ENUMS` ([:120-126], REMOVE the `bodyweight-band` entry :124); `_DOB_FIELD` ([:84]) + its capture special-case ([:276-281], the MIRROR — a named-excluded raw-source field written under its own name; the new `bodyweight-kg` capture follows this shape; the full-DOB write is ALREADY this case — only the comment changes); the `assert set(WIRED_TOKENS) <= set(SUMMARY_FIELD_SET)` tripwire ([:193], still holds after the removal); the record-only else-branch ([:291-295]).
- `scripts/guard/pii_scan.py` — `scan_text` / `scan_text_full` + `DEFAULT_IDENTITY_CONFIG` (the canonical operator-PII scanner the crown-jewel DOB probe runs over the `summarize` output, SEC-F2). Read the fail-closed contract; do NOT modify.
- `tests/plan/test_router.py` — the router harness + **the existing tests T2 MUST UPDATE** (they encode the OLD born-decade / pass-through contract): `_store_read_factory` ([:21]), `_clean_records` ([:33], `date-of-birth: "1986-04-12"` :36-37 already a full date; `bodyweight-band: "80-90kg"` :50-51 the pass-through record to REPLACE with a `bodyweight-kg` series), `_reading` ([:142]); `test_summarize_reads_through_store` ([:121-139], `== "born-1980s"`/`"born-1970s"` → exact age); `test_age_band_token_value` ([:148-165], the born-decade parametrize table → exact-age table + malformed→sentinel); `test_summarize_passthrough_accepts_clean_non_str_values` ([:568-584], uses `bodyweight-band` as the pass-through example — repoint to a still-pass-through token); the clone-DOB crown-jewel `test_summarize…` ([:739/:743], `== "born-1980s"` + `"born-1950s" not in str(summary)` → exact age + full-DOB-absent). The new T2 cases APPEND alongside.
- `tests/serve/test_capture.py` — the capture harness: `_ABSENT_IDENTITY` ([:36]), `_bound` ([:39]), `_summary` ([:44]), `_WIRED_VALUES` ([:58]); **the malformed-birth-year test** ([:875-895], `age-band-unknown`) — UPDATE to a malformed full-DOB → the exact-age sentinel.
- `tests/serve/test_intake_demographics.py` (manifest file 5) — read **the existing WIRED-`bodyweight-band` cases** T2 reconciles: the `("bodyweight-band", capture.BODYWEIGHT_BANDS)` markup<->gate no-drift assertion ([:218-232,:410]), the AC-1 orphan-lit round-trip + negative control ([:48,:56,:154-162]), and the born-year→born-decade `training-age-band` cases ([:39,:126,:212]). They encode the OLD demographic contract; reconcile to the derived `bodyweight-kg`→current-weight+trend + full-DOB→exact-age behavior. `capture.BODYWEIGHT_BANDS` stays importable (the constant is RETAINED).
- `tests/serve/test_capture_store_adversarial.py` (manifest file 6) — read the `pka` Cat-1 demographic battery ([:354-523]) that keys cross-stream/dedupe/mutation on the wired `bodyweight-band` token; re-key onto the new `bodyweight-kg` LOCAL series, preserving the coverage.
- `tests/genetics/test_dna_aware_plan_e2e.py` (manifest file 7) — read `_CLEAN_RECORDS` ([:110-123]) carrying the `("bodyweight-band", "80-90kg", "intake")` STORE seed ([:116]), the `_append` helper (one fixed `PLAN_DATE`/source, [:180-188]), and `_drive_plan_signal` running the REAL `summarize`→`dispatch` ([:248-257]). T2 re-keys the seed to `bodyweight-kg` so the now-DERIVED `bodyweight-band` token stays present and `dispatch` stays complete (the E2E asserts on `genetic-trait-classes`, not the weight). Do NOT add `tests/serve/test_app_shell.py` (T5's W2 file — a same-wave collision; its demographic round-trip is T4-reconciled to the raw source).
- `docs/checklists/store-adversarial-tests.md` — the store-surface battery reference (capture writes the `bodyweight-kg` series via the UNCHANGED `store.append`; bead `pka`). Read for the Cycle-2 store-adversarial obligation.

**Do NOT load / Do NOT touch:** `vault/design/templates/intake.py` + `scripts/generate/generate.py` + `tests/generate/test_intake.py` — the superseded non-served legacy wizard; they keep working against the RETAINED `BODYWEIGHT_BANDS` constant, and their retirement is bead `rod1`, OUT of this build; the ADRs (`docs/adr/ADR-0033/0034/0035*.md`) + the dispositions ledger — the spec distilled them; the frozen engine (`scripts/plan/{orchestrate,pipeline,assemble,generate_plan,adjudicate,adjust,track}.py` + `scripts/store/*.py`) — byte-frozen, numstat=0, never modified (NFR-2); the OTHER tasks' surfaces (T3's `referral.py`, T4/T5's `app_view.html` markup, T6-T9) — out of T2; the summary-dict fixtures that construct a summary DIRECTLY (`tests/model/test_client.py`, `tests/plan/test_assemble.py`, `tests/plan/test_deid_in.py`, `tests/plan/test_plan_orchestrator.py`, `scripts/plan/_a_prime_self_test.py`) — they bypass the deriver and `bodyweight-band`/`training-age-band` stay field-set members, so they are UNAFFECTED (do not edit them); any `vault/**` content file — daemon-churned.

## Risk Pre-Check

This task carries the spec's ADR-0033-0035-T2 Risk Mitigations + the build plan's Wave-2 cross-cutting invariants + the QA-F3 negative-control posture + the SEC-F2 format-agnostic DOB scan. Before proceeding to RED, verify a test exists (or RED adds one) for EACH; a placeholder test (`assert True`) fails this gate. The gate conditions reference live tests/commands, not subjective assessments.

- **CROWN-JEWEL — the full DOB + the raw weight SERIES never reach the no-train planner (NFR-1, the load-bearing probe).** The planner sees only the de-associated EXACT AGE (an integer) and the de-associated CURRENT WEIGHT + TREND — NOT the full birthdate, NOT the per-day weight history. Gate (per SEC-F2, the PRIMARY assertion is FORMAT-AGNOSTIC, not the MM/DD regex):
  - **DOB (AC-3):** seed a fixture full DOB; `summarize(...)["training-age-band"]` is the integer age ONLY; the EXACT seeded full-DOB string appears NOWHERE in the serialized summary (a seeded-substring scan over the whole summary — catches ISO `1986-03-12`, long-form `March 12 1986`, any format); AND `pii_scan.scan_text` over the summary output returns 0 identity hits. SUPPLEMENTARY (does NOT stand alone): the enumerated `MM/DD` / 4-digit-year-plus-month-day regex. **NEGATIVE CONTROL (QA-F3, prove-it-can-RED at THIS wave):** with `_age_band` STUBBED to emit the raw DOB, the seeded-substring scan (+ `pii_scan`) goes RED — so the W2 green is non-vacuous (not passed-because-the-summary-was-empty).
  - **WEIGHT (AC-5):** seed a `bodyweight-kg` series with DISTINCT per-day values (e.g. `[85.0, 84.0, 83.0, 82.0]`); the token carries the CURRENT (82) + a trend word, and the NON-current history (`85.0`/`84.0`/`83.0`) appears NOWHERE in the summary (a seeded-substring scan); `dispatch` REJECTS a `bodyweight-kg` payload field (out-of-field-set). **NEGATIVE CONTROL (QA-F3):** with the `bodyweight-band` deriver STUBBED to emit the raw series, the seeded-substring scan (+ the dispatch-reject) goes RED.
  - All FAILING-CAPABLE: a leaked full DOB / per-day history reds the absence assertion; a deriver that echoes the raw value reds the negative control.
- **NON-TAUTOLOGICAL derivation — the planner gets the real computed signal, not a plumbed constant (NFR-5).**
  - **AGE (AC-2):** fixture DOB A → the EXACT integer age A; a DIFFERENT birth-year DOB B → a DIFFERENT age B ≠ A (computed DYNAMICALLY relative to today's UTC date so the test is deterministic across run dates — assert the DIFFERENCE, not a hardcoded literal). A constant return makes `age_a == age_b` red.
  - **WEIGHT+TREND (AC-4):** a down-trending `bodyweight-kg` series → a trend token distinct from a flat series's; the current-weight component traces to the latest reading. A constant return reds the down-vs-flat distinction.
- **EXTEND-NOT-REBUILD — additive/scoped only; `SUMMARY_FIELD_SET` tuple + `dispatch` + the engine stay byte-frozen (NFR-2).** Re-authoring `dispatch`, the `SUMMARY_FIELD_SET` tuple, or any other deriver is the rebuild failure. Gate (SHELL gates run in REGRESSION + at the Wave-2 checkpoint — `$(git merge-base HEAD origin/main)` is a SHA, unresolvable inside pytest):
  - **AC-7 scoped diff:** `git diff $(git merge-base HEAD origin/main) -- scripts/plan/router.py` shows ONLY the 5 sanctioned hunks; the `SUMMARY_FIELD_SET` tuple + `dispatch` + the other derivers byte-unchanged.
  - **AC-7 mechanical guard:** `git diff $(git merge-base HEAD origin/main) -- scripts/plan/router.py | grep '^-' | grep -E 'def dispatch|SUMMARY_FIELD_SET|EXCLUDED_RAW_PII'` returns 0 DELETED lines touching the protected surfaces (the `EXCLUDED_RAW_PII` member is ADDED — a `^+` line; the guard greps `^-` only — so the addition does not trip it; the human diff-hunk read is the backstop).
  - **Frozen-engine numstat:** `git diff --numstat $(git merge-base HEAD origin/main) -- <7 frozen files> 'scripts/store/*.py'` → 0 rows.
- **AC-1 — `SUMMARY_FIELD_SET` BYTE-IDENTICAL + tripwires hold.** Gate: importing `scripts.plan.router` raises no `AssertionError` (the `set(_RAW_TO_FIELD) <= set(EXCLUDED_RAW_PII)` + `isdisjoint` + `WIRED_TOKENS <= SUMMARY_FIELD_SET` tripwires hold after the additive member) AND a tuple-equality assertion against the pre-edit membership (repurpose, not add).
- **AC-6 — ALWAYS-SET/CONDITIONAL coherence (no partial-summary raise).** Gate: a `store_read` with no `date-of-birth` / no `bodyweight-kg` OMITS `training-age-band` / `bodyweight-band` from the summary (both stay CONDITIONAL — NOT in `_ALWAYS_SET_DERIVED`), and `dispatch` over a complete summary does NOT raise (mirrors the existing pass-through conditional contract).
- **AC-8 — CAPTURE band-consumer DE-WIRE (not delete; spec AMENDED).** Gate: `'bodyweight-band' not in capture.WIRED_TOKENS` AND `not in capture._BOUNDED_ENUMS`; the `BODYWEIGHT_BANDS` tuple constant is RETAINED (`hasattr(capture, "BODYWEIGHT_BANDS")` True — still imported by the non-served legacy `intake.py`; full deletion is bead `rod1`); a captured weight writes a `bodyweight-kg` reading + 0 `bodyweight-band` store write; `import scripts.serve.capture` raises no `AssertionError`. **Store-surface (bead `pka`):** the `bodyweight-kg` write goes through the UNCHANGED `store.append` — the store-adversarial battery (cross-stream / dedupe-collision / mutation) must cover the new `bodyweight-kg` stream (`docs/checklists/store-adversarial-tests.md`), re-keyed from the de-wired `bodyweight-band` battery (the adjudicated 7-file manifest).
- **0 LIVE SPEND — mock/fixture-tested (NFR-3).** Gate: every T2 test injects a fixture `store_read` (router) or a tmp store/scaffold root (capture) — 0 live calls, 0 key, 0 network, no `key_source.resolve()`-gated call, no `converse`/curation call.

## TDD Steps

TWO TDD cycles (each RED → GREEN → REFACTOR → REGRESSION). Cycle 1 is the router-side de-association (the crown-jewel + non-tautology — RISKIEST FIRST, fixture-driven, independently testable). Cycle 2 is the capture-side band-consumer DE-WIRE + the 3-test reconciliation (depends on Cycle 1's `_RAW_TO_FIELD`/`EXCLUDED_RAW_PII` change for the full round-trip). Run every pytest with `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES` on the `.venv` runtime.

**RED-first integrity note.** The genuinely RED-on-T2 probes are AC-2 (no exact-age derivation yet → the born-decade `_age_band` returns the wrong value), AC-3 (crown-jewel DOB — the born-decade band already strips the raw DOB, so the seeded-substring half is GREEN today, but the integer-age assertion + the negative control are RED-capable and the negative control is the load-bearing prove-it-can-RED), AC-4/AC-5 (no `bodyweight-band` deriver — `bodyweight-band` is a pass-through today, so deriving current-weight+trend from a `bodyweight-kg` series is absent), AC-8 (`bodyweight-band` still wired). AC-1's tuple-equality is a STANDING-GREEN lock (the tuple does not change); AC-7 (scoped-diff/mechanical-guard) + the frozen-engine numstat are STANDING INVARIANTS run in REGRESSION + at the checkpoint (`$(git merge-base HEAD origin/main)` is a SHA, unresolvable inside pytest). This mirrors the ADR-0032-T3 precedent.

### Cycle 1 — Router-side de-association (exact age + current-weight+trend; crown-jewel; non-tautological)

#### Step 1.1: RED — Write failing tests (`tests/plan/test_router.py`, appended + the OLD-contract tests UPDATED)

Call `/write-tests` for `scripts/plan/router.py`. First update the shared fixture + the OLD-contract tests (they encode the born-decade / pass-through behavior the repurpose supersedes — updating them to the NEW behavior is required by TDD, not a tautology: each must still go RED on a wrong age / a leaked DOB):
- **Fixture update — `_clean_records` ([:50-51]):** REPLACE the `bodyweight-band: "80-90kg"` pass-through record with a `bodyweight-kg` dated series (≥2 readings, distinct values, distinct timepoints) so `_clean_store_read`-backed tests still produce a `bodyweight-band` token (now derived). `date-of-birth: "1986-04-12"` ([:36-37]) stays (already a full ISO date).
- **`test_summarize_reads_through_store` ([:121-139]):** `summary["training-age-band"]` → the EXACT AGE of a `1986-04-12` DOB (computed dynamically); the `other` `1972-08-09` DOB → a DIFFERENT age. Keep the data-flow leg (a hardcoded-return `summarize` reds it).
- **`test_age_band_token_value` ([:148-165]):** the born-decade parametrize table → an exact-age table (full DOBs → computed integer ages) + the malformed cases (`"not-a-date"`, `"86"`, `"198"`, a future-dated DOB) → the new unparseable sentinel (see GREEN).
- **`test_summarize_passthrough_accepts_clean_non_str_values` ([:568-584]):** repoint the pass-through-non-str example OFF `bodyweight-band` (no longer pass-through) onto a still-pass-through token (e.g. `recovery-status-band` / `sex-for-dosing` / `equipment-access-class`), preserving the int/None pass-through intent.
- **the clone-DOB crown-jewel `test_summarize…` ([:739/:743]):** `training-age-band == "born-1980s"` → the exact age of the clone DOB; keep + strengthen `"born-1950s" not in str(summary)` to "the full clone-DOB string + the 1955 plant absent" (the de-association proof).

Then the NEW T2 cases (fixture `store_read` via `_store_read_factory`, 0 live spend):
- **AC-1 — `test_summary_field_set_byte_identical_after_repurpose`** (standing-green lock): `import scripts.plan.router as router` raises no `AssertionError`; `router.SUMMARY_FIELD_SET == <the pinned pre-edit tuple>` (a literal tuple-equality, proving no token added/removed).
- **AC-2 — `test_training_age_band_is_exact_age_non_tautological`** (RED-first): a fixture `date-of-birth` full-date → `summarize(...)["training-age-band"]` is the EXACT AGE in integer years (== the dynamically-computed `today − dob` years); a DIFFERENT birth-year DOB → a DIFFERENT age; assert `age_a != age_b`. RED today: the born-decade `_age_band` returns `"born-1980s"`, not an integer.
- **AC-3 — `test_training_age_band_carries_no_full_dob_crown_jewel`** (RED-capable + the load-bearing negative control): seed a full DOB; assert the token is the integer age, the EXACT seeded full-DOB string is absent from the serialized summary (PRIMARY, format-agnostic), and `pii_scan.scan_text` over the serialized summary returns 0 (SEC-F2); SUPPLEMENTARY MM/DD regex absent. NEGATIVE CONTROL (QA-F3): `monkeypatch` the module's `_age_band` to a stub that returns the raw DOB value verbatim, re-run the assertions, and confirm the seeded-substring + `pii_scan` checks go RED (proves the probe can RED at THIS wave).
- **AC-4 — `test_bodyweight_band_is_current_weight_plus_trend_non_tautological`** (RED-first): a fixture `bodyweight-kg` down-trending series → `summarize(...)["bodyweight-band"]` carries the current weight (traces to the latest reading) + a `down`-class trend; a FLAT series → a distinct (`flat`-class) trend; assert the two trend tokens differ. RED today: `bodyweight-band` is a pass-through (no `bodyweight-kg` deriver).
- **AC-5 — `test_bodyweight_series_stays_local_crown_jewel`** (RED-capable + negative control): seed a `bodyweight-kg` series with distinct per-day values; assert the NON-current history values are absent from `str(summary)` (only the current crosses) AND `dispatch({**complete_summary, "bodyweight-kg": <series>})` RAISES the out-of-field-set error (the whitelist rejects `bodyweight-kg`). NEGATIVE CONTROL (QA-F3): stub the `bodyweight-band` deriver to emit the raw series → the seeded-substring scan goes RED.
- **AC-6 — `test_age_and_weight_conditional_omitted_when_absent`** (RED-first for the weight half): a `store_read` with no `date-of-birth` AND no `bodyweight-kg` → both tokens ABSENT from the summary; build a complete summary (the other field-set members present) and assert `dispatch(summary)` does NOT raise. RED today: `bodyweight-band` (pass-through) is present whenever a `bodyweight-band` store item exists — the conditional-on-`bodyweight-kg` contract is absent.

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/plan/test_router.py -q -k "age or bodyweight or field_set or conditional or reads_through or passthrough_accepts"`
Expected: FAIL — AC-2/AC-4/AC-5/AC-6 + the updated born-decade/pass-through tests red; AC-1 + the standing-green halves pass. Step 1.2 turns them GREEN.

#### Step 1.2: GREEN — Implement the router-side de-association (`scripts/plan/router.py`, the 5 sanctioned hunks)

Changes (ALL additive/scoped — no literal code reproduced here; the SE authors it under TDD within the pinned contracts, NFR-5):
- **`_age_band` body repurpose** ([:324-340]): KEEP the function name `_age_band` (so `_FIELD_DERIVATION`'s `training-age-band` line stays byte-unchanged — minimal diff). Replace the body: parse the full DOB from `readings[-1]["value"]` (the pinned ISO `YYYY-MM-DD` from T4's `<input type=date>`), compute the EXACT AGE in integer years relative to `datetime.now(timezone.utc).date()` (the existing `datetime`/`timezone` import is already present, :14), return the integer (or its `str`). An UNPARSEABLE / future-dated DOB → a defined sentinel (mirroring the old `age-band-unknown` fail-safe — e.g. `"age-unknown"`), NEVER a crash, NEVER an echo of the raw value (the crown-jewel: the sentinel carries no DOB substring). The token is a de-associated scalar.
- **New deriver `_bodyweight_trend(readings)`** (mirroring the deriver shape — takes the `bodyweight-kg` readings SERIES): emit a scalar carrying the CURRENT weight (the latest reading's numeric value, coarsened as the SE sees fit) + a coarse TREND token from a closed set (e.g. `{up, down, flat}` / `{gaining, stable, losing}` — implementer-discretionary, NFR-5), computed from the latest vs an earlier numeric reading. The NON-current per-day history NEVER enters the token (the crown jewel) — only the current value + the trend word. An insufficient series (<2 numeric) → current + `flat` (no false trend), mirroring `_trend_token`'s no-signal floor.
- **`_FIELD_DERIVATION` entry** ([:560-568]): APPEND `"bodyweight-band": _bodyweight_trend` (the `training-age-band` line stays byte-unchanged).
- **`_RAW_TO_FIELD` entry** ([:116-125]): APPEND `"bodyweight-kg": "bodyweight-band"` (so `summarize`'s conditional `source_items` path routes `bodyweight-band` through the new deriver from the `bodyweight-kg` source).
- **`EXCLUDED_RAW_PII` member** ([:61-96]): APPEND `"bodyweight-kg"` (REQUIRED by the `set(_RAW_TO_FIELD) <= set(EXCLUDED_RAW_PII)` tripwire :590, and it keeps `bodyweight-band` ∈ field-set / `bodyweight-kg` ∈ excluded disjoint :591). Add it as a new line INSIDE the existing tuple (a `^+` line — the mechanical guard greps `^-` only).
- **NO change** to `SUMMARY_FIELD_SET`'s tuple members, `dispatch`, `summarize`'s signature/body, `_ALWAYS_SET_DERIVED`, any other deriver, or the tripwire assert lines. (Directly-adjacent stale comments describing the repurposed derivers — e.g. the `_RAW_TO_FIELD`/`_age_band` "born-decade" prose — MAY be updated to the exact-age/weight-series contract within scope, as long as the `SUMMARY_FIELD_SET` tuple members + the protected-name lines `def dispatch`/`SUMMARY_FIELD_SET = (`/`EXCLUDED_RAW_PII = (` stay byte-unchanged; keep it minimal.)

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/plan/test_router.py -q`
Expected: PASS — the new AC-1..AC-6 cases + the updated born-decade/pass-through tests green; the rest of the router suite unaffected.

#### Step 1.3: REFACTOR (router)

Review for:
- `_age_band` emits the exact age + the unparseable sentinel, NEVER echoes the raw DOB (the crown jewel); the function name is unchanged (minimal `_FIELD_DERIVATION` diff).
- `_bodyweight_trend` emits current + a coarse trend ONLY (no per-day history), reuses no duplicate numeric-parsing helper if one fits (`biomarker_meta.to_number` is the existing numeric parser — reuse if apt; do NOT add a dependency).
- `bodyweight-band` + `training-age-band` are both CONDITIONAL (not added to `_ALWAYS_SET_DERIVED`) — confirm the no-source-omit contract (AC-6).
- The `SUMMARY_FIELD_SET` tuple + `dispatch` + the tripwire asserts + every other deriver are byte-unchanged (the scoped diff + the mechanical guard).

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/plan/test_router.py -q` → PASS (no behavior change).

#### Step 1.4: REGRESSION (router)

Run the full router suite + the full suite, then the SHELL gates (STANDING INVARIANTS; `$(git merge-base HEAD origin/main)` unresolvable inside pytest):
```
OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/plan/test_router.py -q
git diff $(git merge-base HEAD origin/main) -- scripts/plan/router.py                       # ONLY the 5 sanctioned hunks
git diff $(git merge-base HEAD origin/main) -- scripts/plan/router.py | grep '^-' | grep -E 'def dispatch|SUMMARY_FIELD_SET|EXCLUDED_RAW_PII'   # → 0 lines (AC-7 mechanical guard)
git diff --numstat $(git merge-base HEAD origin/main) -- scripts/plan/orchestrate.py scripts/plan/pipeline.py scripts/plan/assemble.py scripts/plan/generate_plan.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/track.py 'scripts/store/*.py'   # → 0 rows (frozen engine)
```
Expected: the router suite green; the router diff is the 5 sanctioned hunks; the mechanical guard returns 0; the frozen-engine numstat is 0. (The FULL-suite run is deferred to Cycle 2's REGRESSION, where the capture rework + reconciliation lands — the full suite will still carry the out-of-manifest breakage until Cycle 2.)

### Cycle 2 — Capture-side band-consumer DE-WIRE + the 3-test reconciliation (the adjudicated 7-file manifest)

#### Step 2.1: RED — Write failing tests (`tests/serve/test_capture.py`, appended + the OLD-contract test UPDATED)

- **the malformed-birth-year test ([:875-895]):** UPDATE the input from a malformed birth YEAR to a malformed full DOB and the expectation from `age-band-unknown` to the new exact-age sentinel (`age-unknown`).
- **AC-8 — `test_bodyweight_band_de_wired_weight_routes_to_kg_series`** (RED-first): assert `'bodyweight-band' not in capture.WIRED_TOKENS` AND `'bodyweight-band' not in capture._BOUNDED_ENUMS`; assert `hasattr(capture, "BODYWEIGHT_BANDS")` is True (the constant is RETAINED — the de-wire-not-delete disposition; full deletion is bead `rod1`); `persist_capture({"<weight-field>": "82.0"}, root=<tmp store>, …)` writes a `bodyweight-kg` reading (`store.read("bodyweight-kg", root=…)` non-empty) AND 0 `bodyweight-band` store writes; `import scripts.serve.capture` raises no `AssertionError` (the `WIRED_TOKENS <= SUMMARY_FIELD_SET` tripwire holds). RED today: `bodyweight-band` is wired into `WIRED_TOKENS`/`_BOUNDED_ENUMS`.
- **AC-9 round-trip — `test_weight_capture_round_trips_to_bodyweight_band_token`** (RED-first; cross-checks Cycle 1): capture a weight via `persist_capture` over a tmp root, then `summarize(_bound(root))["bodyweight-band"]` carries the current-weight+trend token (proving the capture→`bodyweight-kg`→deriver round-trip). RED today: no `bodyweight-kg` capture special-case + no deriver.
- **full-DOB-local — `test_full_dob_captures_to_local_date_of_birth_item`**: capture a full ISO DOB via the `_DOB_FIELD` field; assert it writes the `date-of-birth` named-excluded store item (already the live behavior — pins it survives the amendment) and 0 `training-age-band` direct write.

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/serve/test_capture.py -q -k "bodyweight or weight or dob or birth"`
Expected: FAIL — AC-8 / round-trip / DOB-sentinel red. Step 2.2 turns them GREEN.

#### Step 2.2: GREEN — Implement the capture de-wire (`scripts/serve/capture.py`) + the 3-test reconciliation

In `scripts/serve/capture.py`:
- REMOVE `"bodyweight-band"` from `WIRED_TOKENS` ([:62]).
- REMOVE the `"bodyweight-band"` entry from `_BOUNDED_ENUMS` ([:124]).
- **RETAIN the `BODYWEIGHT_BANDS` tuple constant ([:118])** — it is still imported by the superseded non-served `vault/design/templates/intake.py`; do NOT delete it (full deletion + intake.py retirement is bead `rod1`). The descriptive comment ([:114]) may be updated to note it is retained for the legacy wizard only (de-wired from the live capture path).
- ADD a `bodyweight-kg` weight-capture special-case mirroring `_DOB_FIELD` ([:276-281]): a `_WEIGHT_FIELD = "bodyweight-kg"` constant (the form field name == the store item name, the simplest T2↔T4 coupling — mirroring `_DOB_FIELD = "date-of-birth"`), an `elif name == _WEIGHT_FIELD:` branch that `store.append`s a `bodyweight-kg` reading (the named-excluded local time-series; the lbs/kg unit conversion is T4's CLIENT-SIDE affordance per NFR-5 — capture receives the kg-normalized number). KEEP the `_DOB_FIELD` special-case (the full-DOB local write is already this case — update only its stale "born-decade" comment).
- The `assert set(WIRED_TOKENS) <= set(SUMMARY_FIELD_SET)` tripwire ([:193]) is UNCHANGED (still holds — a removed member keeps the subset).

The adjudicated 3-test reconciliation (every store-seed + summarize-driving call site of the de-wired/repurposed `bodyweight-band` token — the Factory-to-Component Wiring Rule):
- `tests/serve/test_intake_demographics.py` — reconcile the `bodyweight-band`-as-WIRED-token cases ([:48,:56,:154-162,:218-232]) + the `("bodyweight-band", capture.BODYWEIGHT_BANDS)` markup<->gate no-drift assertion + the AC-1 orphan-lit negative control to the NEW behavior: a captured weight writes a `bodyweight-kg` LOCAL reading; `summarize` DERIVES the de-associated current-weight+trend `bodyweight-band` token (NOT a directly-captured WIRED band). Reconcile the born-year→born-decade `training-age-band` cases ([:39,:46,:126,:212]) to the full-DOB exact-age contract. (`capture.BODYWEIGHT_BANDS` references at [:232,:410] stay importable — the constant is RETAINED — but the WIRED-band semantics are reconciled to the derived contract; a remaining `capture.BODYWEIGHT_BANDS` reference is acceptable only if it no longer asserts `bodyweight-band` is a captured WIRED token.)
- `tests/serve/test_capture_store_adversarial.py` — re-key the `pka` Cat-1 demographic battery ([:354-523]) from the wired `bodyweight-band` token to the new `bodyweight-kg` LOCAL series (or another live demographic token), preserving the cross-stream-isolation + dedupe-collision + mutation coverage per `docs/checklists/store-adversarial-tests.md`.
- `tests/genetics/test_dna_aware_plan_e2e.py` — **re-key the `_CLEAN_RECORDS` STORE seed ([:116]) from `("bodyweight-band", "80-90kg", "intake")` to `("bodyweight-kg", "82", "intake")`** so the now-DERIVED `bodyweight-band` token is produced (the `_bodyweight_trend` insufficient-series floor → current + `flat`) and `dispatch` stays complete. The E2E asserts on `genetic-trait-classes`, not the weight — it only needs the token present. (If the implementer's `_bodyweight_trend` OMITS on a single reading, seed a 2-reading `bodyweight-kg` series via a second earlier-dated `_append` with a distinct timepoint — the existing `_CLEAN_RECORDS`/`_append` path writes one fixed `PLAN_DATE`/source, so a 2nd `_CLEAN_RECORDS` tuple would dedupe-collide.)
- DO NOT touch `vault/design/templates/intake.py` / `scripts/generate/generate.py` / `tests/generate/test_intake.py` — they keep working against the RETAINED constant (bead `rod1`). DO NOT add `tests/serve/test_app_shell.py` (T5's W2 file — a same-wave collision; its demographic round-trip markup is T4-reconciled to assert the raw-source `date-of-birth`, not T2's deriver output).

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/serve/test_capture.py tests/serve/test_intake_demographics.py tests/serve/test_capture_store_adversarial.py tests/genetics/test_dna_aware_plan_e2e.py -q`
Expected: PASS — AC-8 / round-trip / DOB-sentinel + the 3 reconciled test files green (incl. the E2E `dispatch`-complete after the seed re-key).

#### Step 2.3: REFACTOR (capture)

Review for: the `bodyweight-kg` special-case MIRRORS `_DOB_FIELD` (a named-excluded raw-source field written under its own name via the UNCHANGED `store.append` — not a fork of the store surface); `BODYWEIGHT_BANDS` is RETAINED but no longer referenced by the LIVE capture path (`bodyweight-band` is out of `WIRED_TOKENS`/`_BOUNDED_ENUMS`; the only remaining `BODYWEIGHT_BANDS` consumer is the non-served legacy `intake.py` — `rg -l "BODYWEIGHT_BANDS" scripts vault` shows `capture.py` (def) + `intake.py` (legacy import) ONLY, no live serve/plan consumer); the removals leave the tripwire valid.

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/serve/test_capture.py -q` → PASS.

#### Step 2.4: REGRESSION (full suite + all SHELL gates)

```
OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/plan/test_router.py tests/serve/test_capture.py -q   # AC-9 (the two named test files)
OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest -q                                                         # the PORT-PINNED full suite (0 failed, or the 2 named env tests only)
rg -l "BODYWEIGHT_BANDS" scripts vault   # → capture.py (def, RETAINED) + intake.py (legacy import) ONLY — no live serve/plan consumer
git diff $(git merge-base HEAD origin/main) -- scripts/plan/router.py | grep '^-' | grep -E 'def dispatch|SUMMARY_FIELD_SET|EXCLUDED_RAW_PII'   # → 0 (AC-7)
git diff --numstat $(git merge-base HEAD origin/main) -- scripts/plan/orchestrate.py scripts/plan/pipeline.py scripts/plan/assemble.py scripts/plan/generate_plan.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/track.py 'scripts/store/*.py'   # → 0 rows (frozen engine)
git diff --name-only $(git merge-base HEAD origin/main)   # the changed set = the 7 manifest files (router.py + capture.py + test_router.py + test_capture.py + test_intake_demographics.py + test_capture_store_adversarial.py + tests/genetics/test_dna_aware_plan_e2e.py); NOTHING else (NOT intake.py / generate.py / test_intake.py / test_app_shell.py)
```
Expected: the full suite green (port 8765 free, or the 2 named env tests only — a held-port run NEVER launders a regression); `BODYWEIGHT_BANDS` survives in `capture.py` (def) + `intake.py` (legacy) only; the router mechanical guard 0; the frozen-engine numstat 0; the changed set is exactly the 7 manifest files (NOTE: `tests/serve/test_app_shell.py`'s demographic round-trip is reconciled by T4 to assert the raw-source `date-of-birth`, removing any cross-wave coupling to T2's deriver — it is T5's W2 file, never in T2's diff). Confirm the crown-jewel probes stay green:
`OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/plan/test_router.py -q -k "crown or no_full_dob or stays_local"`.

**TDD step count: 8 (2 cycles).** Within the ≤10-step guidance. Cycle 1 is the crown-jewel router de-association (riskiest first, fixture-driven); Cycle 2 is the capture de-wire + the 3-test reconciliation (depends on Cycle 1's `_RAW_TO_FIELD`/`EXCLUDED_RAW_PII` change for the round-trip). The 9 ACs span the byte-identical field-set + the non-tautological exact age + the crown-jewel DOB-absent/fail-safe + the non-tautological weight+trend + the crown-jewel weight-series-local + the conditional contract + the scoped-additive diff + the band-consumer de-wire + the targeted-suite gate — not splittable into a third cycle.

## Interface Contracts

T2 modifies an INTERNAL module (`scripts/plan/router.py`) + a serve-layer module (`scripts/serve/capture.py`); it ADDS no new public interface and PRESERVES every protected body verbatim.

- **`summarize(store_read, identity_config=…, genetics_library_root=None)` (UNCHANGED signature + body).** The returned summary's `training-age-band` token now carries the EXACT AGE (an integer-years scalar from the local full `date-of-birth`); the `bodyweight-band` token now carries the de-associated CURRENT WEIGHT + TREND (from the local `bodyweight-kg` series). Both stay CONDITIONALLY set (omitted when their source is absent — AC-6). **PINNED (NFR-1, crown-jewel):** the full DOB string + the raw per-day weight history NEVER appear in the summary; only the de-associated exact age + current weight + trend cross. **DISCRETIONARY (NFR-5):** the exact-age sentinel token for an unparseable DOB, the weight-coarsening, the trend vocabulary.
- **`_age_band(readings)` (REPURPOSED body, same name + signature).** Emits the exact integer age from the full DOB; an unparseable/future DOB → the defined sentinel; never echoes the raw DOB.
- **`_bodyweight_trend(readings)` (NEW internal deriver).** Emits the de-associated current-weight + coarse-trend scalar from the `bodyweight-kg` series; never echoes the per-day history.
- **`capture.persist_capture` (capture region).** A `bodyweight-kg` form field → the named-excluded `bodyweight-kg` local time-series (the new special-case, mirroring `_DOB_FIELD`); the full DOB → the `date-of-birth` local item (the existing `_DOB_FIELD` case). `bodyweight-band` is NO LONGER a wired/bounded capture token.
- **Cross-task seam (T2↔T4, a FORMAT coupling, not a code dep):** T4's `<input type=date name='date-of-birth'>` submits the full DOB as ISO `YYYY-MM-DD` (the format `_age_band` parses); T4's body-weight number (kg-normalized client-side from the lbs/kg unit select) submits under the form field name `bodyweight-kg` (the name capture's `_WEIGHT_FIELD` special-case reads). **For reviewers:** T4 (W1) must already drop `bodyweight-band` from `_DEMOGRAPHIC_SELECTS` (the Entry-State ordering gate) and build the full-date + weight-number inputs to these pinned names; a mismatch is a contract break, not a T2 deriver edit.
- **Cross-task seam (downstream):** T6's completeness predicate reads `summarize(...)["training-age-band"]` + `["bodyweight-band"]` as required tokens; T9's crown-jewel E2E drives a seeded full DOB + raw weight series through `summarize` → `dispatch` and asserts 0 full-DOB / 0 per-day-history crossing. They consume the token's PRESENCE + de-association, not the deriver internals.
- **Frozen surface (PRESERVED).** The `SUMMARY_FIELD_SET` tuple members, `dispatch`, the disjointness/tripwire asserts, `_ALWAYS_SET_DERIVED`, and every other deriver (`_issue_class`, `_trend_token`, `_recent_trend_direction`, `_rx_interaction_classes_token`, `_genetic_trait_classes_token`, the four chat-sourced derivers) are byte-unchanged (the scoped diff + the mechanical guard). The frozen engine + `scripts/store/*` stay numstat=0; `capture.WIRED_TOKENS`'s tripwire stays valid.

## Verification Checklist

- [ ] **Adjudicated scope honored (DE-WIRE-not-delete, 7-file manifest)** — the `BODYWEIGHT_BANDS` constant is RETAINED (bead `rod1`); the changed set is exactly the 7 manifest files (incl. `tests/genetics/test_dna_aware_plan_e2e.py`, re-keyed); `intake.py` / `generate.py` / `tests/generate/test_intake.py` / `tests/serve/test_app_shell.py` are untouched.
- [ ] **E2E `bodyweight-band`-now-derived seed re-key (Factory-to-Component Wiring)** — `tests/genetics/test_dna_aware_plan_e2e.py::_CLEAN_RECORDS` seeds a `bodyweight-kg` reading (not the directly-stored `bodyweight-band` item), so the now-DERIVED `bodyweight-band` token stays present and `dispatch` does NOT raise `partial summary, missing ['bodyweight-band']`; verified by `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/genetics/test_dna_aware_plan_e2e.py -q` green.
- [ ] AC-1 (`SUMMARY_FIELD_SET` BYTE-IDENTICAL + tripwires) — `test_summary_field_set_byte_identical_after_repurpose`: `import router` raises no `AssertionError`; tuple-equality vs the pinned pre-edit membership.
- [ ] AC-2 (NON-TAUTOLOGICAL exact age) — `test_training_age_band_is_exact_age_non_tautological`: DOB A → integer age A (dynamic), DOB B → age B ≠ A.
- [ ] AC-3 (CROWN-JEWEL raw-DOB-absent, SEC-F2 + QA-F3) — `test_training_age_band_carries_no_full_dob_crown_jewel`: the token is the integer age; the seeded full-DOB string absent from the summary (PRIMARY, format-agnostic); `pii_scan.scan_text` over the summary = 0; the deriver-stubbed-to-leak negative control goes RED.
- [ ] AC-4 (NON-TAUTOLOGICAL current weight + trend) — `test_bodyweight_band_is_current_weight_plus_trend_non_tautological`: a down-trending series → current + a distinct trend from a flat series.
- [ ] AC-5 (CROWN-JEWEL weight-series-local, QA-F3) — `test_bodyweight_series_stays_local_crown_jewel`: the non-current history absent from the summary; `dispatch` rejects a `bodyweight-kg` payload field; the deriver-stubbed-to-leak negative control goes RED.
- [ ] AC-6 (ALWAYS-SET/CONDITIONAL coherence) — `test_age_and_weight_conditional_omitted_when_absent`: no DOB/no weight → both tokens omitted; `dispatch` over a complete summary does NOT raise.
- [ ] AC-7 (SCOPED-ADDITIVE diff + mechanical guard) — the SHELL gate: `git diff $(git merge-base HEAD origin/main) -- scripts/plan/router.py` shows ONLY the 5 sanctioned hunks (the repurposed `_age_band`, the new `_bodyweight_trend`, the `_FIELD_DERIVATION` `bodyweight-band` entry, the `_RAW_TO_FIELD` `bodyweight-kg` entry, the `EXCLUDED_RAW_PII` `bodyweight-kg` member); the mechanical guard `… | grep '^-' | grep -E 'def dispatch|SUMMARY_FIELD_SET|EXCLUDED_RAW_PII'` → 0; `dispatch` + the `SUMMARY_FIELD_SET` tuple byte-unchanged.
- [ ] AC-8 (CAPTURE band-consumer DE-WIRE, not delete; spec AMENDED) — `test_bodyweight_band_de_wired_weight_routes_to_kg_series`: `'bodyweight-band'` out of `WIRED_TOKENS`/`_BOUNDED_ENUMS`; the `BODYWEIGHT_BANDS` constant RETAINED (`hasattr(capture, "BODYWEIGHT_BANDS")` True — still imported by the legacy `intake.py`; full deletion is bead `rod1`); a captured weight writes a `bodyweight-kg` reading + 0 `bodyweight-band` write; `import capture` raises no `AssertionError`; AND `rg -l "BODYWEIGHT_BANDS" scripts vault` shows `capture.py` + `intake.py` ONLY (no live serve/plan consumer).
- [ ] AC-9 (targeted suite green) — `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/plan/test_router.py tests/serve/test_capture.py` → passes; PLUS the PORT-PINNED full suite green (0 failed, or the 2 named env tests only).
- [ ] NFR-2 (EXTEND-NOT-REBUILD) — the frozen-engine numstat (`<7 frozen files>` + `scripts/store/*.py`) = 0 rows; router scoped-additive (AC-7), STANDING INVARIANTS run in REGRESSION + at the Wave-2 checkpoint.
- [ ] CROWN-JEWEL boundary intact — the planner receives the de-associated exact age + current weight + trend ONLY; the full DOB + the raw per-day weight history stay local; the deriver fail-safes on an unparseable DOB; `dispatch` rejects `bodyweight-kg`/`date-of-birth`. 0 full-DOB / 0 weight-history crossing.
- [ ] Store-surface (bead `pka`) — the `bodyweight-kg` write goes through the UNCHANGED `store.append`; the store-adversarial battery (`docs/checklists/store-adversarial-tests.md`) covers the new `bodyweight-kg` stream, re-keyed from the de-wired `bodyweight-band` battery in `tests/serve/test_capture_store_adversarial.py` (manifest file 6).
- [ ] AC-9 extended — the full targeted set passes: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/plan/test_router.py tests/serve/test_capture.py tests/serve/test_intake_demographics.py tests/serve/test_capture_store_adversarial.py tests/genetics/test_dna_aware_plan_e2e.py -q` → passes.
- [ ] MOCK/fixture 0-live-spend — every test injects a fixture `store_read` / tmp roots; 0 live calls, 0 key, 0 network, no `key_source.resolve()`-gated call, no `converse`/curation call (NFR-3).
- [ ] Wave-2 → Wave-3 checkpoint gates carried (build plan § Wave 2 Boundary): field-set-byte-identical [AC-1] + exact age [AC-2] + crown-jewel-DOB-absent w/ negative control [AC-3] + weight+trend [AC-4] + crown-jewel-weight-local w/ negative control [AC-5] + conditional [AC-6] + scoped-diff/mechanical-guard [AC-7] + band-consumer de-wire [AC-8] — all green; the full suite green; 0 live spend.
- [ ] No regression in the full test suite (port 8765 free; re-baseline only with a recorded count delta). INV-CORE-CAPABILITY not tripped (the repurposed derivers are not referenced by `core-capability-audit.sh`'s `plan_driver.py`/`plan_orchestrator.py` greps).
- [ ] Scope-discipline check: `git diff --name-only $(git merge-base HEAD origin/main)` lists the 7 adjudicated manifest files ONLY — `scripts/plan/router.py`, `scripts/serve/capture.py`, `tests/plan/test_router.py`, `tests/serve/test_capture.py`, `tests/serve/test_intake_demographics.py`, `tests/serve/test_capture_store_adversarial.py`, `tests/genetics/test_dna_aware_plan_e2e.py`; NOT `intake.py` / `generate.py` / `test_intake.py` (bead `rod1`) / `test_app_shell.py` (T5's W2); any other file needs a written justification.
- [ ] Interface contracts documented (the exact-age + current-weight+trend de-association; the T2↔T4 ISO-DOB + `bodyweight-kg` field-name coupling; the no-engine-edit / `SUMMARY_FIELD_SET`-byte-identical property; the crown-jewel boundary flagged for reviewers).

## Commit

```
feat(plan): T2 actual-age + actual-weight de-association
```
Stage only (the adjudicated 7-file manifest):
- `scripts/plan/router.py`
- `scripts/serve/capture.py`
- `tests/plan/test_router.py`
- `tests/serve/test_capture.py`
- `tests/serve/test_intake_demographics.py`
- `tests/serve/test_capture_store_adversarial.py`
- `tests/genetics/test_dna_aware_plan_e2e.py`

Stage by explicit path (`git add <path>` as its OWN command, then `git commit` separately — the block-pii-commit PreToolUse hook denies single-call stage+commit, `git commit -a/--all`, and pathspec `git commit <path>`). Do NOT `git add vault/` blindly or the daemon frontmatter churn (CLAUDE.md § Vault hygiene). Do NOT stage `vault/design/templates/intake.py` / `scripts/generate/generate.py` / `tests/generate/test_intake.py` / `tests/serve/test_app_shell.py` (untouched — the `BODYWEIGHT_BANDS` constant is RETAINED, the legacy wizard is bead `rod1`, and the app-shell demographic markup is T5's W2 / T4-reconciled), `.beads/issues.jsonl` / `harvest.jsonl` / `memory/process-failures.md` (close-protocol artifacts), or any frozen-engine/`scripts/store/*` file (numstat=0). Commit on `feature/intake-build` in the main checkout (a worktree commit false-trips `block-commit-main`, the S95 residual). `feat`-type, ≤50 chars.

## Rollback

If verification fails after 3 fix cycles:
1. `git stash -m "failed-ADR-0033-0035-T2-attempt-{N}"` — preserve work for diagnosis.
2. `git checkout {commit-before-task}` — revert to the entry-state commit (the branch HEAD this task was built on top of).
3. Update the bead tracker: flag ADR-0033-0035-T2 as BLOCKED with the failure log.
4. Escalate: "Task ADR-0033-0035-T2 failed verification after 3 attempts. Stash ref: {ref}. Failure log: {summary}."
5. After abandonment or re-planning, drop all `failed-ADR-0033-0035-T2` entries from `git stash list`.

Do NOT: silently skip failing acceptance criteria, weaken tests to make them pass (especially the crown-jewel raw-DOB-absent / weight-series-local probes [AC-3/AC-5] and their QA-F3 negative controls — a falsification that cannot go RED on a leaked full DOB / per-day history is worthless — or the non-tautological exact-age / weight-trend probes [AC-2/AC-4]); leak the full DOB or the raw per-day weight history into the summary (the crown-jewel boundary T2 forbids — HALT, restore the de-associated-token deriver); edit `dispatch`, the `SUMMARY_FIELD_SET` tuple, the disjointness asserts, or any other deriver (a non-additive change is the EXTEND-NOT-REBUILD / mechanical-guard violation — HALT, restore the byte-frozen body); edit any frozen-engine / `scripts/store/*` file (a non-zero numstat is the NFR-2 breach — HALT); DELETE the `BODYWEIGHT_BANDS` constant or touch `intake.py` / `generate.py` / `tests/generate/test_intake.py` (the de-wire-not-delete disposition retains the constant for the legacy wizard; full deletion is bead `rod1`, OUT of scope — HALT, restore the constant); or expand beyond the adjudicated 7-file manifest without a written justification (and do NOT add `tests/serve/test_app_shell.py` — a same-wave T5 collision).

## Report

- Recipe: `docs/task-plan/.pipeline/adr-0033-0035/draft-ADR-0033-0035-T2.md` (status: draft; the earlier blast-radius gap is ADJUDICATED — DE-WIRE-not-delete, 7-file manifest, spec AMENDED 2026-06-30 S103; no longer a HALT).
- Every ADR-0033-0035-T2 acceptance criterion (AC-1..AC-9) maps to a Verification-Checklist step + a Wave-2 checkpoint gate; the RED-first feature probes are AC-2/AC-3(integer-age half + negative control)/AC-4/AC-5(weight half + negative control)/AC-6(weight half)/AC-8; AC-1 (tuple-equality) is a standing-green lock; AC-7 (scoped-diff/mechanical-guard) + the frozen-engine numstat are STANDING-INVARIANT SHELL gates.
- GROUNDED against the live tree (`feature/intake-build`, `$(git merge-base HEAD origin/main)` = `12f769f`, 2026-06-30): `SUMMARY_FIELD_SET` [router.py:23-56] (training-age-band :25 + bodyweight-band :27 present, NO bodyweight-kg); `EXCLUDED_RAW_PII` [:61-96] (date-of-birth :64); `_RAW_TO_FIELD` [:116-125] (date-of-birth → training-age-band :117, no bodyweight entry); the disjointness tripwires [:590-591]; `_age_band` [:324-340] (born-decade); `_FIELD_DERIVATION` [:560-568] (training-age-band → _age_band :561); `_ALWAYS_SET_DERIVED` [:578-583] (neither token is always-set); `summarize` [:630-739] (the conditional source_items path :712-719); `dispatch` [:742-802] (whitelist :777); capture `WIRED_TOKENS` [:50-64] (bodyweight-band :62), `BODYWEIGHT_BANDS` [:118], `_BOUNDED_ENUMS` [:120-126] (bodyweight-band :124), `_DOB_FIELD` [:84] + its special-case [:276-281], the tripwire [:193]; the test harnesses (`_store_read_factory`/`_clean_records`/`_reading` in test_router.py; `_bound`/`_summary`/`_ABSENT_IDENTITY` in test_capture.py) present.
- ZERO literal implementation code in the recipe — the change is the additive `_RAW_TO_FIELD`/`EXCLUDED_RAW_PII`/`_FIELD_DERIVATION` entries + the `_age_band` exact-age RULE + the `_bodyweight_trend` current-weight+trend RULE (de-associated; no raw-value echo) + the capture band-consumer DE-WIRE (RETAIN the `BODYWEIGHT_BANDS` constant) + the `bodyweight-kg` special-case (mirror `_DOB_FIELD`), with cited `file:line` seams (the sentinel token / weight-coarsening / trend vocabulary implementer-discretionary per NFR-5); the SE authors it under TDD.
- **ADJUDICATED SCOPE (was a blast-radius flag, now resolved + spec AMENDED 2026-06-30 S103):** DE-WIRE-not-delete — T2 removes `bodyweight-band` from `WIRED_TOKENS`/`_BOUNDED_ENUMS` but RETAINS the `BODYWEIGHT_BANDS` tuple constant (still imported by the superseded non-served `intake.py`; the server renders `generate.run('app')`, never `'intake'`). Full deletion + `intake.py` retirement is tracked bead `a-plus-maxing-rod1`, OUT of this build. The manifest EXPANDED to 7 files (the Factory-to-Component Wiring Rule — every store-seed + summarize-driving call site of the de-wired/repurposed token): the original 4 + `tests/serve/test_intake_demographics.py` (reconcile the WIRED-`bodyweight-band` cases + the born-year→born-decade `training-age-band` cases to the derived `bodyweight-kg`→current-weight+trend + full-DOB→exact-age behavior) + `tests/serve/test_capture_store_adversarial.py` (re-key the `pka` Cat-1 demographic battery from `bodyweight-band` to the `bodyweight-kg` series) + `tests/genetics/test_dna_aware_plan_e2e.py` (the judge-traced 7th site — re-key the `_CLEAN_RECORDS` STORE seed at [:116] from the now-ignored `bodyweight-band` item to a `bodyweight-kg` reading so `summarize` derives the token + `dispatch` stays complete, else the E2E REDs the Wave-2 full-suite gate). `intake.py` / `generate.py` / `test_intake.py` keep working against the retained constant and are NOT touched; `tests/serve/test_app_shell.py` is T5's W2 (same-wave collision) and its demographic round-trip is T4-reconciled to the raw source — no T2 coupling. The full-suite-green Wave-2 checkpoint stays achievable.
- One nuance flagged to the executor + reviewers: keeping the function name `_age_band` (repurposing only its body) keeps the `_FIELD_DERIVATION` `training-age-band` line byte-unchanged (minimal scoped diff); the token NAMES (`training-age-band` / `bodyweight-band`) now carry a more-precise value than a band (an accepted misnomer the ADR sanctioned — a rename is non-additive field-set churn, rejected). The crown-jewel negative controls (QA-F3) are the load-bearing prove-it-can-RED at THIS wave — a probe that cannot RED on a leaked full DOB / per-day history is worthless.
