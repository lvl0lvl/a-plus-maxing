---
task-id: ADR-0043-T2
source-spec: docs/spec/adr-0041-0046-comprehensive-plan-spec.md
source-build-plan: docs/build-plan/build-plan-adr-0041-0046.md
wave: 3
assigned-agent: SE
reviewers: [QA, Security]
created: 2026-07-12
status: approved
---

# Recipe: Collect + Reconcile the `cross_domain_seams` — Generalize the Five Holds, Safety Floors Always-On (FROZEN-SPINE + SAFETY)

> Wave-3 task (build-plan Wave 3 = {ADR-0044-T2, ADR-0046-T1, ADR-0045-T1, ADR-0043-T2}). It generalizes
> `orchestrate.reconcile`'s FIVE hard-coded pair-specific cross-domain holds (`orchestrate.py:337-456`,
> grounded exact) into ONE uniform `cross_domain_seams` pass: each specialist's DOMAIN PROGRAM (ADR-0041)
> declares its cross-domain seams in the `cross_domain_seams` field, and `reconcile` collects + resolves
> them through ONE code path — no per-domain-PAIR branch per seam. The SAFETY-CRITICAL subset — the
> RED-S/LEA cross-domain short-circuit, the supplement↔peptide additive-AE screen, the supplement↔Rx BPMH
> screen — is RE-BASED as ALWAYS-ON FLOORS that fire from their own trigger conditions EVEN WHEN a program
> does NOT declare the seam (re-based, never deleted — the load-bearing patient-safety property). It MODIFIES
> `scripts/plan/orchestrate.py` (the `reconcile` generalization) + `scripts/serve/care_chat.py` (the
> collect+reconcile control flow, placed AFTER + DISJOINT from 0043-T1's decompose region). It CREATES
> `tests/serve/test_orchestrator_reconcile.py`.
>
> TRANSITIONAL ADDITIVE (disposition #2, the 0041-T2 precedent applied to the reconciler — Deviation #1):
> the uniform `cross_domain_seams` pass is LAYERED ON. The existing five behaviors' trigger reads
> (`meta.energy_budget` / `meta.conflicts` / `meta.ae_profile` / the nutrition RED-S/LEA reason / the
> compound-identity overlap) are PRESERVED — the three safety reads become the always-on floors; the two
> softer reconciliations (energy bounce, overlap+conflict) gain the uniform seam path additively while
> their transitional meta-reads remain, so the 1960-line `tests/plan/test_orchestrate.py` behavioral suite
> (the frozen-guard behavioral replacement, ruling §4) stays GREEN with NO manifest expansion, AC-4's
> five-behavior coverage holds, and the uniform pass adds the generalized any-pair seam handling
> (AC-1/AC-2/PF-S130-01). `reconcile`'s public signature (`reconcile(candidates, *, operator_rx_classes)`)
> is UNCHANGED — `generate_plans` (its production caller, 482-635) still calls it, so the whole
> `orchestrate.py` production suite stays green. The care_chat capture path (`extract.persist_extraction`,
> 371-374) + the T1 decompose region (`_derive_goals` 217-230, `decompose` 233-256, the `_care_messages`
> orchestration block 291-297) stay BYTE-UNCHANGED (a grep asserts it).
>
> FROZEN-SPINE (Wave-3): `orchestrate.py` is a NEW Wave-3 SUPERSEDED surface — the reconcile generalization
> is the operator-signed-off supersession (build plan invariant 1 + the frozen-guard ruling §5 "Wave 3's
> `orchestrate.py` reconciliation (0043-T2)"). Its prior-feature byte-guard carve-out is the SHARED Wave-3
> frozen-guard reconciliation step (Option-A, F-011 — owned by neither Wave-3 task, run as the wave's first
> build step; NOT this task's manifest — this task stages NO guard file). The per-ADR-scoped numstat probe
> (AC-5) bounds `orchestrate.py`'s delta to the sanctioned generalization; the frozen four
> (`store.py`/`keying.py`/`pipeline.py`/`adjudicate.py`) + the `<always-frozen>` HARD six stay byte-frozen.
>
> Two load-bearing deliverables: (1) SAFETY — a RED-S/LEA / additive-AE / Rx-BPMH condition is held
> REGARDLESS of declaration: the always-on floors fire whether the seam is UNDECLARED (AC-3) OR DECLARED
> (AC-3b, SEC-W3-02 — per-floor RED tests + floor-removal AND floor-GATING mutation-RED), AND fire through
> the new care_chat collect+reconcile path (SEC-W3-03 — the collect preserves `meta.ae_profile`/`reason` +
> derives `operator_rx_classes`); a floor that fires only when declared, only when undeclared, or is dead on
> the care_chat path is a fail-OPEN regression — BLOCKING; (2) NON-TAUTOLOGICAL SWAP — PF-S130-01: the
> production-path reconcile is driven with a `cross_domain_seams` seam between a pair NOT among the five
> hard-coded pairs and REDs on a revert to the pre-generalization five-holds reconcile (the exact Wave-1 gap
> Tier-3 caught).

## Entry State

**Prerequisites** (ADR-0043-T2 depends on ADR-0041-T1 + ADR-0043-T1 per the spec Dependency Map; BOTH built + merged in Wave 1/Wave 2, on this branch's history at `3e17b1d8`, PR #332):

- [ ] Predecessor **ADR-0041-T1** present (the seven-field `cross_domain_seams` shape it reconciles): `grep -q "CROSS_DOMAIN_SEAMS" scripts/plan/domain_program.py && grep -q 'cross_domain_seams' scripts/plan/domain_program.py && echo OK` (the field-name constant + the conditional-field membership; `domain_program.py` also states "ADR-0043-T2 owns the reconciliation semantics AND the seam-edge validation" — this task DEFINES + reads the seam-edge shape).
- [ ] Predecessor **ADR-0043-T1** present (the decompose region it collects from; both modify `care_chat.py` in DISJOINT regions): `grep -q "def decompose" scripts/serve/care_chat.py && grep -q "def _derive_goals" scripts/serve/care_chat.py && echo OK`.
- [ ] The reconcile supersession anchor is live: `grep -q "def reconcile" scripts/plan/orchestrate.py && grep -q "def generate_plans" scripts/plan/orchestrate.py && echo OK` (the five-holds `reconcile` at 337-456 + its production caller `generate_plans`).
- [ ] Full test suite at the Wave-3 baseline floor: `.venv/bin/python -m pytest -q` reports **`2 failed, 2577 passed, 8 skipped`** (Wave 1 + Wave 2 merged: this floor SUPERSEDES the build plan's pre-build `2 failed, 2489 passed` figure — the +88 are the merged `test_domain_program`/`test_context_assembler`/`test_generate_plan_uniform`/`test_plan_model`/`test_orchestrator_brief` cases). The two failures (`tests/serve/test_bind.py::test_port_collision_exits_nonzero_fail_loud`, `tests/serve/test_server.py::test_main_reads_no_stdin`) are PRE-EXISTING env-sensitive OS-port/stdin cases, NOT this task's to fix; the gate is **no-NEW-failure**, a 3rd distinct failure is a regression → HALT.
- [ ] Working tree "clean" — **NOTE the daemon caveat:** a bare `git status` is NOT empty (the basic-memory daemon perpetually re-churns `vault/**/*.md` frontmatter; parked untracked files exist — `vault/library/genetics/*.md`, `docs/**/.verify-*.md`). "Clean" for THIS task = (a) **no STAGED changes** (`git diff --cached --name-only` prints nothing); (b) the Create target ABSENT (below); (c) `orchestrate.py`+`care_chat.py` byte-clean vs `PRE_TASK_HEAD` (below). Do NOT `git restore vault/` or try to zero `git status` — the daemon re-churns within ~1s (CLAUDE.md vault hygiene).
- [ ] On a feature branch cut off `main`: `feature/comprehensive-plan-wave-3` (the current Wave-3 build branch) or an equivalent `feature/*` — never `main`.
- [ ] Rollback + numstat base recorded: `git rev-parse HEAD` at task entry — the `{commit-before-task}` the Rollback Protocol reverts to AND the **`PRE_TASK_HEAD`** base the AC-5 per-ADR numstat probe diffs against (mirror `tests/plan/test_generate_plan_uniform.py`'s `ln` constant pattern). Record the ACTUAL entry HEAD at execution (a Wave-3 sibling — 0044-T2 / 0046-T1 / 0045-T1 — may have committed first; `PRE_TASK_HEAD` is HEAD when THIS task starts, so the probe sees only 0043-T2's delta).
- [ ] The Create target is ABSENT (Create-only, no stale/duplicate artifact): `test -e tests/serve/test_orchestrator_reconcile.py && { echo "PRESENT (stop)"; exit 1; } || echo "ABSENT (Create OK)"`.
- [ ] `orchestrate.py` + `care_chat.py` byte-clean vs `PRE_TASK_HEAD` at entry: `git diff --numstat $PRE_TASK_HEAD -- scripts/plan/orchestrate.py scripts/serve/care_chat.py` prints nothing (this task is the sole editor of both in Wave 3 — build-plan Coordination note; no sibling Wave-3 task shares either file).
- [ ] The `<always-frozen>` HARD six byte-frozen at entry (numstat==0 vs origin/main): `git diff --numstat origin/main -- scripts/store/store.py scripts/store/keying.py scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/router.py` prints nothing (empty) — invariant 1, every wave.
- [ ] **Wave-3 frozen-guard reconciliation RAN (orchestrate.py already carved) — the SHARED step, NOT this task.** The prior-feature byte-guards that freeze `orchestrate.py` — G1 `tests/serve/test_route.py` (glob), G2 `tests/serve/test_pdf_ingestion_e2e.py` (glob), G3 `tests/serve/test_intake_onboarding_e2e.py` (explicit :137), G4 `tests/genetics/test_dna_aware_plan_e2e.py` (`_FROZEN_SET` :95), G5 `tests/serve/test_plan_loop_hold.py` (`_FROZEN_GLOB` :66), G6 `tests/plan/test_confirm_pointer_skip.py` (`FROZEN_GLOB` :36), G7 `tests/runner/test_cadence_runner.py` (inline `frozen` :293) + any F-011-surfaced single-file byte guard (the assemble.py-analogue `::test_*_byte_unchanged_vs_pretask`) — are carved by the **Wave-3 frozen-guard reconciliation step** (owned by neither Wave-3 task, run BEFORE this task's TDD commit; frozen-guard ruling §5 + §2-addendum "Inheritance to Wave 3": stage the supersession, run the suite, carve exactly the RED guards, remove ONLY `orchestrate.py`, leave the `<always-frozen>` HARD set frozen). Confirm the reconciliation ran: `grep -q 'orchestrate.py' tests/serve/test_intake_onboarding_e2e.py && echo "check the carve annotation present"` — this task's Step-4 regression runs AFTER the reconciliation (guards already carved). This task stages NO guard file.
- [ ] `activation.py` is ABSENT (Wave 3, ADR-0046-T1, not yet built): `test -e scripts/plan/activation.py && echo "PRESENT (unexpected)" || echo "ABSENT (OK — the front-door active-set SOURCE is 0043-T3/0046-T1, not this task)"`.

## Context Load List

**Files to read (this task's file manifest + the surfaces it reuses + the freeze context):**

- `scripts/plan/orchestrate.py` — the file this task's load-bearing change lives in. Read:
  - `reconcile` (337-456, grounded exact) — the SUPERSEDED surface: today's FIVE hard-coded pair-specific holds — (1) RED-S/LEA cross-domain short-circuit (375-378, reads `nutrition.get("reason") == RED_S_LEA_CLINICAL_ROUTING`), (2) energy bounce (382-390, reads `nutrition meta.energy_budget.sustains`), (3) overlap detection (395-401) + author-declared cross-domain conflict (413-418, reads `cand.meta.conflicts`), (4) supplement↔peptide additive-AE (426-435, reads `meta.ae_profile`), (5) supplement↔Rx BPMH (446-453, reads `_rx_bpmh_matched_classes`). Returns `{report, holds, conflict_held, rx_bpmh_held}`. The three SAFETY reads (1/4/5) become the always-on floors; the two SOFTER (2/3) gain the uniform seam path additively.
  - `generate_plans` (482-635) — the PRODUCTION caller: `candidates = {domain: compute_plan(...)}` → `reconcile(candidates, operator_rx_classes=...)` → applies holds/bounce/adjudication → records. This is the PF-S130-01 production path; its signature + the `reconcile` call stay UNCHANGED (the whole `generate_plans` suite stays green).
  - The five `_safety_finding` builders + `_additive_ae_findings` / `_rx_bpmh_matched_classes` / `_compound_identities` (72-334) — the safety-floor machinery to PRESERVE (the always-on floors reuse them).
- `scripts/serve/care_chat.py` — the collect+reconcile control flow is added here, AFTER + DISJOINT from the T1 decompose region. Read:
  - `decompose` (233-256) + `_derive_goals` (217-230) + `_ORCHESTRATION_NOTE` (70-76) + the `_care_messages` orchestration block (291-297) — the T1 decompose region, BYTE-UNCHANGED (do NOT edit).
  - the capture call site `receipt = extract.persist_extraction(result.get("extraction"), turn_text, root=store_root, scaffold_root=scaffold_root, identity_config=identity_config)` (371-374) — BYTE-UNCHANGED (AC — the orchestration is additive to the existing agent).
  - the lazy-import convention `from scripts.serve import plan_loop` inside `respond` (380) — the pattern to MIRROR if the collect+reconcile reaches `orchestrate.reconcile` via a function-level import (sidesteps any module-load cycle; grounded cycle-free either way — see Risk Pre-Check).
- `scripts/plan/domain_program.py` — READ for the seam contract (do NOT edit — change-control frozen, ADR-0041-T1). `CROSS_DOMAIN_SEAMS = "cross_domain_seams"` (74); a CONDITIONAL field (77); the module docstring pins the edge shape as "a paired-domain reference + a seam-nature/conflict token" and states **"ADR-0043-T2 owns the reconciliation semantics AND the seam-edge validation (`validate` does not check it)"** — THIS task defines + reads that edge shape.
- `scripts/plan/generate_plan.py` — READ-ONLY (an AC-5 sibling-superseded surface — 0046-T1's roster manifest — + a numstat-watched surface; do NOT edit). Ground WHERE the DOMAIN PROGRAM (carrying `cross_domain_seams`) rides on a `compute_plan` candidate so `reconcile` reads the UNIFORM field: `_lift_program` (169-210) constructs the program with `domain_program.CROSS_DOMAIN_SEAMS: []` (204) + `PROGRAM_KEY` (`assemble.py:33` = `"domain_program"`); the translators emit it additively onto the recorded plan (the 0041-T2 open-on-extras ride). Confirm the exact per-candidate accessor (`candidate["plan"][PROGRAM_KEY][CROSS_DOMAIN_SEAMS]` vs a top-level attach) at Step 2 by `grep`/`sed`, never from this description.
- `tests/plan/test_orchestrate.py` (1960 lines) — the EXISTING reconcile behavioral suite: the fixture convention to MIRROR (how a `candidate` dict is built — `{domain, plan, meta, specialist, section, reason}`), AND the REGRESSION surface + the frozen-guard behavioral replacement (ruling §4) that MUST stay byte-green post-generalization (AC-4's re-based five-behavior coverage). New fixtures for `test_orchestrator_reconcile.py` mirror its `candidate` shape, now carrying a conformant DOMAIN PROGRAM with populated `cross_domain_seams`.
- `docs/adr/.pipeline/frozen-guard-reconciliation-ruling.md` **§5 + §2-addendum ("Inheritance to Wave 3")** — the BINDING carve-out authority (context for WHY the Wave-3 reconciliation exists; this task does NOT perform the carve): §5 rules the reconciliation is a standing per-wave item; the §2-addendum pins the F-011 method Wave 3 inherits (stage → run → carve exactly the RED guards). Read to understand the freeze, do NOT edit the guard files here.

**Do NOT load:**
- The ADRs (`docs/adr/ADR-0041-*.md` … `ADR-0046-*.md`) — the spec already distilled them; re-reading re-opens resolved trade-offs (TP-02 drift).
- `scripts/serve/plan_loop.py` / `scripts/serve/server.py` — the front-door redefinition + the `active_domains` SOURCE + the synthesize step are ADR-0043-T3 (Wave 4); this task collects + reconciles, it does NOT wire the front door or synthesize the integrated plan.
- `scripts/plan/activation.py` (ABSENT, Wave 3 / ADR-0046-T1) — the active set is not computed here.
- The guard files that freeze `orchestrate.py` (G1–G7 above) — carved by the **Wave-3 frozen-guard reconciliation step**, NOT this task; this task's Step-4 re-runs them GREEN (already carved), it does NOT edit them.
- The frozen four (`store.py`/`keying.py`/`pipeline.py`/`adjudicate.py`) + `adjust.py`/`router.py` + the sibling-superseded surfaces (`generate_plan.py`/`assemble.py`/`plan_schema.py`) — this task edits none of them; they are numstat check surfaces, not reads (except the read-only `generate_plan.py` grounding above).
- The other Wave-3 / Wave-4+ task blocks — this recipe covers ONLY the `### ADR-0043-T2` block.

## Risk Pre-Check

**BLOCKING GATE — a SAFETY-CRITICAL frozen-spine SWAP.** Do NOT proceed to GREEN until a real (non-`assert True`), RED-capable test exists for every risk below. A placeholder test, or a production-path test that passes with OR without the generalization (tautological), FAILS this gate.

- **Risk (SAFETY — the always-on floors must fire REGARDLESS of declaration; the load-bearing patient-safety property; ADR-0043 Consequences-Negative-2/3 fail-OPEN class — BLOCKING):** the reconcile generalization (a) drops a safety floor, (b) re-bases it to fire ONLY when a program DECLARES the seam in `cross_domain_seams` (a RED-S/LEA / additive-AE / Rx-BPMH condition escapes because no specialist declared the seam), OR (c) — the SEC-W3-02 gate-bypass — gates the floor behind `if declared: uniform-pass else: floor`, SKIPPING the floor exactly when a seam IS declared (the transitional-additive design makes the SOFT reconciliations reachable via BOTH the `meta` path AND the uniform seam pass, which invites this gating). All three are fail-OPEN patient-safety regressions (why the build-plan matrix routes 0043-T2 to Security). The floor must fire from its OWN trigger INDEPENDENT of declaration, in BOTH directions.
  - Gate condition: RED must include SIX per-floor tests — the THREE undeclared (`test_red_s_lea_floor_fires_undeclared`, `test_additive_ae_floor_fires_undeclared`, `test_rx_bpmh_floor_fires_undeclared`; empty `cross_domain_seams`) AND the THREE fires-even-when-DECLARED (`test_<red_s_lea|additive_ae|rx_bpmh>_floor_fires_even_when_seam_declared`; the SAME trigger + a well-formed seam on the same pair — SEC-W3-02). Each seeds the floor's trigger condition (a tripped nutrition RED-S/LEA reason with an energy-prescribing workout; a shared additive-AE class across a supplement + peptide; a compound additive-AE class intersecting an operator Rx-interaction class) and asserts the floor FIRES (the workout / supplement is held / escalated) INDEPENDENT of declaration. The Step-2.5 floor-removal mutation MUST turn the undeclared tests RED; the Step-2.5 floor-GATING mutation (gate the floor behind "no declared seam", `elif not declared_seam:`) MUST turn the declared tests RED — and is INVISIBLE to the undeclared tests, which is exactly why floor-removal alone misses the fail-OPEN bypass. A floor that fires only when declared, OR only when UNdeclared, FAILS this gate.
- **Risk (PF-S130-01 — the tautological component-SWAP test, the EXACT Wave-1 gap Tier-3 caught):** the reconcile is a SWAP (five hard-coded holds → one uniform pass). A test that seeds only the OLD five hard-coded pairs (nutrition/workout, supplements/peptides) proves nothing — the old five-holds reconcile already handles those, so a revert stays green.
  - Gate condition (production-path + revert-REDs, MANDATORY): RED must include `test_uniform_seam_pass_catches_non_hardcoded_pair` — drive the PRODUCTION reconcile path (`orchestrate.reconcile` / `generate_plans`, and the care_chat collect+reconcile that routes THROUGH it) with two conformant DOMAIN PROGRAMs declaring a `cross_domain_seams` seam between a pair NOT among the five hard-coded pairs (e.g. `workout`↔`supplements`); assert the seam is detected + held/adjusted/routed (0 un-reconciled). RED-capability is proven by reverting `reconcile` to the pre-generalization five-holds-only body (Step 2.5): the un-generalized reconcile has no branch for that pair → the seam reaches the output un-reconciled → the test REDs. "REDs against the reverted five-holds" == "REDs on a revert of the generalization." A test that stays green under the revert is tautological and FAILS this gate.
- **Risk (PF-S131-01 — non-canonical fixtures, MANDATORY):** a fixture DOMAIN PROGRAM that uses a convenient simplified stand-in (a flat/bare `prescription`, a fabricated seam shape, a monitoring signal with no validity tier) does not exercise the real reconcile against the real field shapes.
  - Gate condition: EVERY fixture carrying a `domain_program` uses the CANONICAL shape — `prescription` PERIODIZED (`{"blocks": [{"date": ..., "load": ...}]}`, per state.md / bead 58z0), `monitoring_signals` each carrying a `VALIDITY_TIERS` tier, a compound-kind program's `required_labs` NON-EMPTY, and `cross_domain_seams` the REAL edge shape this task defines (a paired-domain reference + a seam-nature/conflict token). Every fixture program passes `domain_program.validate` (a helper asserts it). A simplified stand-in FAILS this gate.
- **Risk (SEC-W3-03 — the care_chat collect path can silently drop ALL THREE floors; fail-OPEN — BLOCKING):** the NEW care_chat collect+reconcile is a path THIS task creates; if the collect shapes only the `domain_program` onto the candidates (dropping `meta.ae_profile` + the nutrition RED-S/LEA `reason`) and calls `reconcile` WITHOUT deriving `operator_rx_classes` (the default `frozenset()`, orchestrate.py:337), then on the care_chat path the additive-AE floor is dead (`_ae_profile` → `{}`, orchestrate.py:101), the RED-S/LEA floor is dead (no `reason`, orchestrate.py:375), and the Rx-BPMH floor is dead twice (no `ae_profile`, `operator_rx_classes` unset — orchestrate.py:262,450). ADR-0043-T3 (synthesize) builds ON this collect and inherits the hole.
  - Gate condition: the collect+reconcile contract REQUIRES the shaped candidates preserve every floor trigger the source carries (`meta.ae_profile`, the RED-S/LEA `reason`) AND derive+thread `operator_rx_classes` from the store (`router.rx_interaction_class_set(router.summarize(store_read))`, orchestrate.py:537 — feasible, `store_read` is bound at care_chat.py:344). RED must include THREE tests that each floor FIRES through the care_chat collect+reconcile path (`test_<red_s_lea|additive_ae|rx_bpmh>_floor_fires_through_care_chat_collect`); a collect that shapes only the domain_program / defaults `operator_rx_classes` drives them RED. A care_chat collect path where any floor is dead FAILS this gate.
- **Risk (ADR-0043 Consequences-Negative-2 — breaks the ADR-0032 freeze on the SAFETY-CRITICAL `orchestrate.reconcile`):** an un-bounded edit bleeds into the frozen engine / a sibling ADR's surface.
  - Gate condition (per-ADR-scoped numstat, tension-resolution #27): RED must include **AC-5** — `git diff --numstat $PRE_TASK_HEAD -- scripts/plan/orchestrate.py` is NON-empty (the sanctioned generalization) AND the frozen four + `assemble.py` are EMPTY, AND the `<always-frozen>` HARD six (origin/main base) are EMPTY. RED-capable: a throwaway edit to any forbidden file makes the probe RED (proven Step 1-continued, reverted).
- **Risk (frozen-guard carve-out without a behavioral replacement = gate gap, ruling §4 "HALT"):** the Wave-3 reconciliation carves `orchestrate.py` out of the guards that freeze it (G1–G7), removing the byte-freeze; without a behavioral guarantor the supersession is unguarded.
  - Gate condition (behavioral replacement present + green): the `orchestrate.py` carve-out's replacement — THIS task's deliverable — = `tests/plan/test_orchestrate.py` (the existing 1960-line behavioral suite, PRESERVED byte-green — AC-4's re-based five-behavior coverage) + the new `tests/serve/test_orchestrator_reconcile.py` (the generalization behavioral suite) + the AC-5 per-ADR numstat (bounds `orchestrate.py`'s delta to the sanctioned generalization, replacing the removed "0-line" tripwire). This task's Step-4 regression MUST run the reconciled guards GREEN (carved by the Wave-3 reconciliation, which runs BEFORE this task's commit). A reconciled guard that also removed any HARD-frozen surface, or a still-RED guard, HALTs.

## TDD Steps

Single cycle. All six acceptance criteria (+ the PF-S130-01 production-path test + the three always-on-floor safety tests + the care_chat byte-unchanged grep) are properties of ONE coherent unit — the `reconcile` uniform-`cross_domain_seams` generalization + the always-on floors + the care_chat collect+reconcile control flow — exercised over the SAME seeded synthetic DOMAIN PROGRAM fixtures, so they are not split (task-planning: "all criteria test the same component"). RED writes every AC + the safety-floor tests + the production-path test as a specific failing test BEFORE any GREEN; the mutation-RED FALSIFICATION runs POST-GREEN (Step 2.5), because the generalization + floors being mutated do not exist until GREEN.

### Step 1: RED — Write Failing Tests

Call `/write-tests` for `scripts/plan/orchestrate.py`'s `reconcile` generalization + `scripts/serve/care_chat.py`'s new collect+reconcile control flow, creating `tests/serve/test_orchestrator_reconcile.py`, targeting each spec AC. Mirror `tests/plan/test_orchestrate.py`'s `candidate` fixture convention (`{domain, plan, meta, specialist, section, reason}`), now each carrying a conformant DOMAIN PROGRAM. ALL fixtures are SYNTHETIC (0 real operator PII, 0 live-API). **PF-S131-01 (MANDATORY):** every `domain_program` fixture uses the CANONICAL shape — periodized `prescription` `{"blocks":[{"date","load"}]}`, `monitoring_signals` each with a `VALIDITY_TIERS` tier, a compound-kind `required_labs` non-empty, `cross_domain_seams` the real edge shape (paired-domain ref + seam-nature/conflict token) — and passes `domain_program.validate`.

- **AC-1 — Reconcile through ONE code path (ADR-0041 constraint, load-bearing):** seed two domains whose conformant programs declare a seam in `cross_domain_seams`; assert the reconcile step detects + holds/adjusts/routes the seam reading the UNIFORM field — a SINGLE pass over all candidates' `cross_domain_seams`, **0 new per-domain-pair hard-coded branches** (a helper/grep asserts the generalization adds no new `if domain == "X" and other == "Y"` per-pair branch for a seam). `test_reconcile_reads_cross_domain_seams_through_one_path`.
- **AC-2 — Un-reconciled-conflict falsifier (falsification #1, deterministic, RED-capable):** seed two DOMAIN PROGRAMs declaring a `cross_domain_seams` CONFLICT seam; assert **0 declared-seam conflicts reach the synthesized output un-reconciled** — every declared conflict seam is detected + held/routed; a declared conflict left un-reconciled drives the assertion RED. This is the test the Step-2.5 drop-seam mutation turns RED. `test_declared_seam_conflict_never_unreconciled`.
- **AC-3 — Always-on safety floors, UNDECLARED (disposition #9; the SAFETY BLOCKING gate — three tests):** for EACH floor, seed the trigger condition with `cross_domain_seams` EMPTY (undeclared) and assert the floor FIRES:
  - `test_red_s_lea_floor_fires_undeclared` — a tripped nutrition RED-S/LEA reason + an energy-prescribing workout (empty seams) → the workout is HELD (`RED_S_LEA_CROSS_DOMAIN`).
  - `test_additive_ae_floor_fires_undeclared` — a supplement + a peptide candidate sharing an additive-AE class via `meta.ae_profile` (empty seams) → the supplement is HELD (`ADDITIVE_AE_HELD`).
  - `test_rx_bpmh_floor_fires_undeclared` — a compound whose additive-AE class intersects an operator Rx-interaction class (empty seams) → the compound is in `rx_bpmh_held`.
  Each is RED-capable via the Step-2.5 floor-removal mutation.
- **AC-3b — Always-on safety floors fire even when the seam IS DECLARED (SEC-W3-02, the fail-OPEN gate-bypass closure; SAFETY BLOCKING — three tests):** for EACH floor, seed the SAME trigger condition AND a well-formed `cross_domain_seams` seam between the same pair — the floor MUST fire INDEPENDENT of declaration. (The transitional-additive design makes the SOFT reconciliations reachable via BOTH the `meta` path AND the uniform seam pass, which invites an implementer to gate the floor behind `if declared: uniform-pass else: floor` — a fail-OPEN where the floor is SKIPPED exactly when a seam is declared; the undeclared AC-3 tests + floor-removal mutation do NOT catch this.):
  - `test_red_s_lea_floor_fires_even_when_seam_declared` — the tripped nutrition RED-S/LEA reason + energy-prescribing workout AND a declared `nutrition`↔`workout` seam → the workout is STILL HELD (`RED_S_LEA_CROSS_DOMAIN`).
  - `test_additive_ae_floor_fires_even_when_seam_declared` — the shared additive-AE class across a supplement + peptide AND a declared `supplements`↔`peptides` seam → the supplement is STILL HELD (`ADDITIVE_AE_HELD`).
  - `test_rx_bpmh_floor_fires_even_when_seam_declared` — the compound additive-AE class intersecting an operator Rx-interaction class AND a declared seam naming that compound's pair → the compound is STILL in `rx_bpmh_held`.
  Each is RED-capable via the Step-2.5 floor-GATING mutation (gate the floor behind "no declared seam") — DISTINCT from floor-removal: the undeclared AC-3 tests STAY GREEN under gating, the declared AC-3b tests RED, proving the floor fires in BOTH directions. A floor that fires only when declared, OR only when UNdeclared, FAILS this gate.
- **AC-4 — Safety content re-based, not deleted (coverage):** assert each of the FIVE prior behaviors still reconciles its case post-generalization — RED-S/LEA short-circuit, energy bounce, overlap+conflict, additive-AE, Rx-BPMH — a per-behavior coverage assertion in the new module PLUS the REGRESSION that the existing `tests/plan/test_orchestrate.py` (the five behaviors' authoritative suite) stays byte-GREEN. `test_five_prior_behaviors_still_reconcile`.
- **AC-5 — Per-ADR-scoped freeze-break numstat** — `test_per_adr_scoped_freeze_break_numstat` (subprocess `git diff --numstat`, mirror `test_generate_plan_uniform.py`'s `ln`/`PRE_TASK_HEAD` pattern): (a) `git diff --numstat $PRE_TASK_HEAD -- scripts/plan/orchestrate.py` is NON-empty (the sanctioned generalization); (b) `git diff --numstat $PRE_TASK_HEAD -- scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/store/store.py scripts/store/keying.py scripts/plan/assemble.py` is EMPTY (the frozen four + `assemble.py` — no Wave-3 editor — untouched by THIS task); (c) `git diff --numstat origin/main -- <always-frozen HARD six>` is EMPTY (the whole-build invariant). Per the F-011 working-tree narrowing (Deviation #1), `generate_plan.py` + `plan_schema.py` are DROPPED from (b) — a Wave-3 SIBLING (0046-T1) legitimately edits them, so a working-tree probe vs `$PRE_TASK_HEAD` would false-RED (the exact Wave-2 addendum-(iv)(b) precedent); this task's OWN commit touching only `orchestrate.py`+`care_chat.py`+the new test is asserted by the Step-4 scope grep. RED-capable: a transient edit to any forbidden file makes (b)/(c) RED (proven Step 1-continued, reverted).
- **AC-6 — Module green:** `tests/serve/test_orchestrator_reconcile.py` passes against synthetic fixtures — 0 live-API calls, 0 real operator PII (verified by the RED→GREEN transition + an import grep asserting the module imports only `pytest` + the manifest modules + `scripts.plan`/`scripts.store` fixture seams, 0 live-client import, 0 real operator-identity literal).
- **PF-S130-01 — Production-path integration (MANDATORY, load-bearing, non-tautological):** `test_uniform_seam_pass_catches_non_hardcoded_pair` — drive the REAL production reconcile path (`orchestrate.reconcile` / `generate_plans`, AND the care_chat collect+reconcile that routes collected programs THROUGH the one `orchestrate.reconcile` path) with two conformant DOMAIN PROGRAMs declaring a `cross_domain_seams` seam between a pair NOT among the five hard-coded pairs (e.g. `workout`↔`supplements`); assert the seam is detected + held/adjusted/routed (0 un-reconciled). This exercises the WHOLE collect→reconcile path, not a hand-built internal, and (Step 2.5) goes RED on a revert of the generalization (the un-generalized five-holds reconcile has no branch for that pair).
- **care_chat byte-unchanged + collect control flow:** `test_capture_and_decompose_byte_unchanged` — a source-level assertion that `care_chat.py` still contains the exact capture call block (`extract.persist_extraction(result.get("extraction"), turn_text, root=store_root, scaffold_root=scaffold_root, identity_config=identity_config)`) verbatim AND `decompose` (233-256) + `_derive_goals` (217-230) are byte-unchanged; PLUS `test_collect_routes_through_one_reconcile_path` — the new care_chat collect+reconcile gathers seeded specialist DOMAIN PROGRAMs and routes them through the ONE `orchestrate.reconcile` path (not a second bespoke reconciler), returning the reconciled report/holds.
- **care_chat collect-path safety floors (SEC-W3-03, BLOCKING — three tests):** each safety floor FIRES through the care_chat collect+reconcile path (not only the direct `orchestrate.reconcile` call), proving the collect step preserves the trigger downstream — closing the fail-OPEN where a collect adapter shapes only the `domain_program` and lets all three floors go dead:
  - `test_red_s_lea_floor_fires_through_care_chat_collect` — a collected nutrition program carrying the tripped RED-S/LEA `reason` + an energy-prescribing workout → the collect+reconcile returns the workout HELD (`RED_S_LEA_CROSS_DOMAIN`). RED if the collect drops the source `reason`.
  - `test_additive_ae_floor_fires_through_care_chat_collect` — collected supplement + peptide programs sharing an additive-AE class via `meta.ae_profile` → the supplement HELD (`ADDITIVE_AE_HELD`). RED if the collect drops the source `meta`.
  - `test_rx_bpmh_floor_fires_through_care_chat_collect` — a collected compound whose additive-AE class intersects an operator Rx-interaction class SEEDED ON THE SCRATCH STORE → the compound in `rx_bpmh_held`. RED if the collect does NOT derive+thread `operator_rx_classes` (`router.rx_interaction_class_set(router.summarize(store_read))`) — a defaulted `frozenset()` makes the floor dead.
  Each is RED-capable: a collect adapter that shapes only the `domain_program` (drops `meta`/`reason`, defaults `operator_rx_classes`) drives the corresponding test RED — the SEC-W3-03 fail-OPEN falsifier.
- **Regression pin — the existing orchestrate suite:** a lightweight assertion (or relied on via Step 4) that `tests/plan/test_orchestrate.py`'s five-behavior + `generate_plans` tests stay GREEN — proving the generalization is ADDITIVE (the safety floors + the transitional soft reads are intact, the public `reconcile` signature is unchanged).

Run: `.venv/bin/python -m pytest tests/serve/test_orchestrator_reconcile.py -q`
Expected: FAIL — the uniform `cross_domain_seams` pass / the always-on-floor-under-empty-seams behavior / the care_chat collect+reconcile do not yet exist, so the imports/assertions fail. (If any test passes at this stage — especially the PF-S130-01 non-hardcoded-pair or an always-on-floor test — the test is TAUTOLOGICAL or wrong; investigate before GREEN.)

### Step 1 (continued): RED-capability falsification pre-check (NON-SKIPPABLE)

Before implementing, prove the numstat probe bites: make a throwaway one-line edit to a forbidden file (a comment in `scripts/plan/pipeline.py`), CONFIRM `test_per_adr_scoped_freeze_break_numstat` goes RED, `git checkout` the file. (The safety-floor + revert falsifiers are POST-GREEN — Step 2.5 — because the floors + generalization do not exist yet.)

### Step 2: GREEN — Implement

Changes to make (behavioral descriptions, NOT literal code):

- `scripts/plan/orchestrate.py` (Modify — `reconcile` (337-456) internals only; the five `_safety_finding`/`_findings` helpers + `generate_plans` structure otherwise unchanged; `reconcile`'s public signature UNCHANGED):
  - **Add the ONE uniform `cross_domain_seams` pass:** a single loop over all candidates that reads each candidate's DOMAIN PROGRAM `cross_domain_seams` (ground the exact accessor at Step 2 — the program rides on the candidate per 0041-T2's additive emit; a candidate with no program / empty seams contributes no seam, mirroring the trusted-author-malformed-is-inert `_ae_profile` contract), and for EACH declared seam detects it and holds/adjusts/routes the declaring domain through ONE code path — NO per-domain-PAIR branch. The seam-edge shape THIS task defines: a paired-domain reference (the `with`/`with_domain` the seam names) + a seam-nature/conflict token (the token distinguishing a conflict-hold from a softer adjust/route). A declared CONFLICT seam holds the declaring domain (tracked in the existing `conflict_held` independent set, mirroring the current author-conflict hold); a softer seam routes/adjusts per its nature token. This satisfies AC-1 (one path, 0 new per-pair branches), AC-2 (declared conflicts never un-reconciled), and PF-S130-01 (a non-hardcoded-pair seam is caught).
  - **Re-base the three SAFETY reads as ALWAYS-ON FLOORS:** the RED-S/LEA cross-domain short-circuit (375-378), the additive-AE screen (426-435), and the Rx-BPMH screen (446-453) fire from their OWN trigger conditions (the nutrition RED-S/LEA reason; `meta.ae_profile`; `_rx_bpmh_matched_classes`) INDEPENDENT of whether any program declares the seam in `cross_domain_seams` — a program with EMPTY `cross_domain_seams` does NOT escape the floor. Keep their existing hold-set bookkeeping (`holds`, `rx_bpmh_held`) + the `_safety_finding` routing intact. This is the load-bearing SAFETY property (AC-3).
  - **Preserve the two SOFTER reconciliations' transitional reads (additive, disposition #2 — Deviation #1):** the energy bounce (382-390) + overlap+conflict (395-401, 413-418) KEEP their existing `meta`-based reads so the 1960-line `tests/plan/test_orchestrate.py` suite stays byte-green (the behavioral replacement guard), AND they are ALSO reachable via the uniform seam pass when a specialist DECLARES the seam in `cross_domain_seams` (the generalized path). No `meta`-seeded existing test breaks (empty `cross_domain_seams` → the uniform pass is inert on those fixtures); AC-4's five-behavior content is preserved.
  - **No new import cycle, no edit to `generate_plan.py`/`plan_schema.py`/the frozen four** — `reconcile` stays pure over `candidates`; `generate_plans` calls it unchanged. (Satisfies AC-4/AC-5, keeps the production suite green.)
- `scripts/serve/care_chat.py` (Modify — ADD the collect+reconcile control flow AFTER + DISJOINT from the T1 decompose region; the capture path (371-374) + `decompose`/`_derive_goals` (217-256) BYTE-UNCHANGED):
  - **Add the collect+reconcile control flow** (a new function placed after the decompose region, e.g. `collect(...)` + a reconcile call): given the collected dispatched-specialist DOMAIN PROGRAMs (an INJECTED input — seeded in tests; the real dispatch SOURCE is wired at the front door by ADR-0043-T3, mirroring how T1's `active_domains` is injected), GATHER them into the shape `orchestrate.reconcile` consumes and route them through the ONE `orchestrate.reconcile` path (reach it via a function-level `from scripts.plan import orchestrate` mirroring the existing lazy `from scripts.serve import plan_loop` at 380, OR a grounded-cycle-free top-level import — executor discretion, SE Ask-vs-Proceed rule 3, state the assumption).
    - **SEC-W3-03 — the collect step MUST PRESERVE every safety-floor trigger through to `reconcile` (BLOCKING, fail-OPEN otherwise):** `reconcile` reads the floor triggers OFF the candidates — the additive-AE + Rx-BPMH floors read `candidate.meta.ae_profile` (`_ae_profile`, orchestrate.py:101), the RED-S/LEA floor reads `candidate.reason == RED_S_LEA_CLINICAL_ROUTING` (orchestrate.py:375), and the Rx-BPMH floor reads the `operator_rx_classes` kwarg (orchestrate.py:262,450) which DEFAULTS to `frozenset()` (orchestrate.py:337 — an omitted kwarg makes the Rx-BPMH floor DEAD). So the shaped candidates MUST carry the source `meta.ae_profile` + the nutrition RED-S/LEA `reason` (not only the `domain_program`), AND the collect step MUST derive + thread `operator_rx_classes` into `reconcile(..., operator_rx_classes=…)` — deriving it from the store exactly as `generate_plans` does: `operator_rx_classes = router.rx_interaction_class_set(router.summarize(store_read))` (orchestrate.py:537; `store_read = functools.partial(store.read, root=store_root)` already bound at care_chat.py:344). Shaping ONLY the `domain_program` (dropping `meta`/`reason`, defaulting `operator_rx_classes`) makes all three floors dead on the care_chat path — the exact SEC-W3-03 fail-OPEN.
    The collect step does NOT itself dispatch specialists, does NOT compute the active set, does NOT synthesize the integrated plan (that is T3). (Satisfies AC-1's "through ONE code path" + PF-S130-01's care_chat collect+reconcile leg + the SEC-W3-03 floor-preservation.)

Run: `.venv/bin/python -m pytest tests/serve/test_orchestrator_reconcile.py -q`
Expected: PASS.

### Step 2.5: Falsify the load-bearing tests (POST-GREEN mutation-RED)

The generalization + floors the mutations target now EXIST (GREEN landed), so the falsifiers are meaningful here. For EACH mutation: apply it to the GREEN tree, run the named test, OBSERVE it turns RED, then REVERT (the mutation is NEVER committed). A test that stays green under its mutation is tautological and FAILS its AC.

- **(floor-removal ×3) — the SAFETY BLOCKING falsifier:** delete each always-on floor branch in turn (the RED-S/LEA short-circuit; the additive-AE hold; the Rx-BPMH hold) → `test_red_s_lea_floor_fires_undeclared` / `test_additive_ae_floor_fires_undeclared` / `test_rx_bpmh_floor_fires_undeclared` (AC-3) go RED (the undeclared trigger no longer holds/escalates). Revert each. A floor whose test stays green under removal is a fail-OPEN safety regression — HALT.
- **(floor-GATING ×3) — the SEC-W3-02 fail-OPEN gate-bypass falsifier (DISTINCT from floor-removal):** gate each always-on floor behind "no declared seam" (`elif not declared_seam:` / `if not declared_seam: <floor>`) so it fires ONLY when the seam is UNdeclared → `test_red_s_lea_floor_fires_even_when_seam_declared` / `test_additive_ae_floor_fires_even_when_seam_declared` / `test_rx_bpmh_floor_fires_even_when_seam_declared` (AC-3b) go RED (the floor is skipped when the seam is declared), while the undeclared AC-3 tests STAY GREEN (the mutation is invisible to them — which is exactly why floor-removal alone does not catch this bypass). Revert each. A floor whose fires-even-when-declared test stays green under gating is the fail-OPEN gate bypass — HALT.
- **(care_chat-collect-strip ×3) — the SEC-W3-03 fail-OPEN falsifier:** in the care_chat collect step, strip the floor triggers off the shaped candidates (drop `meta.ae_profile` / the RED-S/LEA `reason`) and/or call `reconcile` without `operator_rx_classes` (the default `frozenset()`) → `test_red_s_lea_floor_fires_through_care_chat_collect` / `test_additive_ae_floor_fires_through_care_chat_collect` / `test_rx_bpmh_floor_fires_through_care_chat_collect` go RED (the floor is dead on the care_chat path). Revert. A care_chat collect that lets a floor go dead is a fail-OPEN patient-safety regression — HALT.
- **(revert-to-five-holds) — PF-S130-01:** revert `reconcile` to the pre-generalization five-holds-only body (remove the uniform `cross_domain_seams` pass) → `test_uniform_seam_pass_catches_non_hardcoded_pair` goes RED (the `workout`↔`supplements` seam is in no hard-coded branch → un-reconciled). This proves the production-path test is NOT tautological. Revert.
- **(drop-declared-seam) — AC-2:** in the uniform pass, skip one declared `cross_domain_seams` conflict → `test_declared_seam_conflict_never_unreconciled` (AC-2) goes RED (a declared conflict reaches the output un-reconciled). Revert.
- **(add-per-pair-branch) — AC-1 falsifier:** confirm AC-1's "0 new per-pair branches" assertion would RED if a per-domain-pair `if` branch were added for a seam (a grep/structural check the test performs). Revert any probe edit.

Run after each revert: `.venv/bin/python -m pytest tests/serve/test_orchestrator_reconcile.py -q`
Expected: PASS (every mutation reverted; the suite is green again). Confirm NO mutation edit remains in the working tree before Step 3.

### Step 3: REFACTOR

Review for:
- The uniform `cross_domain_seams` pass is ONE loop / ONE resolution helper — NOT a per-domain-pair copy; the seam-edge read (paired-domain ref + nature token) is a single named accessor, not inlined per behavior.
- The three always-on floors reuse the existing `_additive_ae_findings` / `_rx_bpmh_matched_classes` / RED-S/LEA machinery (72-334) — no duplicated safety logic; their hold-set bookkeeping (`holds`/`conflict_held`/`rx_bpmh_held` independence) is unchanged.
- `reconcile` stays PURE over `candidates` (no store read, no model call, no active-set computation); its public signature is unchanged; `generate_plans` calls it unchanged.
- The care_chat collect+reconcile is disjoint from + after the decompose region; it reaches `orchestrate.reconcile` through ONE path (no second bespoke reconciler); the lazy/top-level import choice is consistent and cycle-free.
- Internal dead code introduced during GREEN — remove any. Confirm `decompose`/`_derive_goals` (217-256) + the capture call site (371-374) are byte-unchanged.

Run: `.venv/bin/python -m pytest tests/serve/test_orchestrator_reconcile.py tests/plan/test_orchestrate.py -q`
Expected: PASS (no behavior change; the new module + the existing reconcile behavioral suite green).

### Step 4: REGRESSION

Run: `.venv/bin/python -m pytest -q`
Expected: PASS at the baseline floor — `2 failed, 2577 passed, 8 skipped` with NO new failure beyond the two known env-sensitive `test_bind`/`test_server` reds (plus the new `test_orchestrator_reconcile.py` tests passing; the passed count RISES by the new module's count). Specifically confirm:
- The frozen-guard behavioral replacement: `tests/plan/test_orchestrate.py` (the five behaviors + `generate_plans`) stays byte-GREEN — AC-4's re-based coverage.
- The Wave-3-reconciled guards (G1–G7, carved by the Wave-3 frozen-guard reconciliation step BEFORE this commit) are GREEN — grep each guard's frozen set to confirm ONLY `orchestrate.py` was removed and the `<always-frozen>` HARD set remains a member (a reconciled guard that also removed a HARD-frozen surface, or a still-RED guard, HALTs).
- The care-chat conversational tests (`tests/serve/test_care_chat.py`) + the orchestrator-brief tests (`tests/serve/test_orchestrator_brief.py`, T1) stay GREEN (the collect+reconcile is additive to the existing agent + the decompose region).

Then re-check the frozen spine + scope (invariant 1 + AC-5):
- `git diff --numstat $PRE_TASK_HEAD -- scripts/plan/orchestrate.py` → NON-empty (the sanctioned generalization).
- `git diff --numstat $PRE_TASK_HEAD -- scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/store/store.py scripts/store/keying.py scripts/plan/assemble.py` → EMPTY.
- `git diff --numstat origin/main -- scripts/store/store.py scripts/store/keying.py scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/router.py` → EMPTY (the `<always-frozen>` HARD six).
- `git diff --name-only $PRE_TASK_HEAD -- scripts/ tests/` → exactly `scripts/plan/orchestrate.py`, `scripts/serve/care_chat.py`, `tests/serve/test_orchestrator_reconcile.py` (no scope creep — `care_chat.py`'s capture path + decompose region byte-unchanged; `generate_plan.py`/`plan_schema.py`/`plan_loop.py`/`server.py` untouched; NO guard file staged — those are the Wave-3 reconciliation commit's).

## Interface Contracts

This task DEFINES the `cross_domain_seams` seam-edge shape + the uniform reconcile semantics — the seam every downstream ADR-0043/0046 task emits + reads.

- **The `cross_domain_seams` seam-edge shape (T2-owned, per `domain_program.py`'s change-control note):** each entry in a program's `cross_domain_seams` list = a paired-domain reference (the domain the seam is with) + a seam-nature/conflict token (distinguishing a conflict-hold from a softer adjust/route). `domain_program.validate` does NOT check it (T1 pinned only the field name); `orchestrate.reconcile` reads + resolves it. **SEC-W3-04 (LOW, informational):** a conflict declared ONLY via a MALFORMED seam is inert (no hold) — consistent with the established, operator-accepted trusted-author-malformed-is-inert posture (`_ae_profile` orchestrate.py:101; the author-conflict path reads `meta.conflicts`, not the seam). NOT a new regression here; but once specialists emit `cross_domain_seams` as the PRIMARY conflict channel and the transitional `meta.conflicts` read is retired (the ADR-0046-T1 window), malformed-inert would become a silent-drop of a real conflict — revisit then whether the conflict axis should fail-LOUD on a malformed seam.
- `orchestrate.reconcile(candidates, *, operator_rx_classes=frozenset()) -> {report, holds, conflict_held, rx_bpmh_held}` — SIGNATURE UNCHANGED. Generalized internals: (1) ONE uniform pass reads each candidate's DOMAIN PROGRAM `cross_domain_seams` and holds/adjusts/routes every declared seam through ONE code path (0 new per-domain-pair branches); (2) the three SAFETY floors (RED-S/LEA, additive-AE, Rx-BPMH) fire ALWAYS-ON from their own trigger conditions, independent of any declaration; (3) the two softer reconciliations (energy bounce, overlap+conflict) keep their transitional `meta` reads AND gain the uniform seam path. `generate_plans` (the production caller) is unchanged.
- `care_chat.<collect+reconcile>` — the care-agent Orchestrator's collect step: gathers the collected dispatched-specialist DOMAIN PROGRAMs (INJECTED — the front-door dispatch SOURCE is ADR-0043-T3), **preserves every safety-floor trigger the source carries (`meta.ae_profile`, the nutrition RED-S/LEA `reason`) onto the shaped candidates AND derives+threads `operator_rx_classes` from the store (`router.rx_interaction_class_set(router.summarize(store_read))`) into `reconcile` (SEC-W3-03 — else the always-on floors are DEAD on the care_chat path)**, and routes them through the ONE `orchestrate.reconcile` path; returns the reconciled report/holds. Disjoint from + after the T1 decompose region.
- **Behavioral invariants:** (1) a seam declared between ANY pair is reconciled through ONE path (no per-pair branch); (2) a RED-S/LEA / additive-AE / Rx-BPMH condition is held REGARDLESS of declaration — the always-on floors fire whether the seam is UNDECLARED **or DECLARED** (SEC-W3-02); (3) 0 declared-seam conflicts reach the output un-reconciled; (4) the five prior behaviors' safety content is preserved (re-based, not deleted); (5) `reconcile`'s signature + `generate_plans` + the frozen four are byte-unchanged in this commit; (6) the care_chat collect step PRESERVES every floor trigger (`meta.ae_profile`/`reason`) + derives+threads `operator_rx_classes`, so the always-on floors fire through the care_chat path too (SEC-W3-03).
- **Dependent tasks:** ADR-0043-T3 (synthesizes ONE integrated plan from these reconciled programs; both modify `care_chat.py` in DISJOINT regions, T3's synthesize region separate from T2's collect+reconcile region) and ADR-0046-T1 (its dispatched specialists emit populated `cross_domain_seams` this reconcile reads). Dependent tasks MUST NOT change the seam-edge shape, weaken an always-on floor, or add a per-domain-pair branch without review. **Reviewer note:** per the build-plan Agent Matrix (line 178), 0043-T2 generalizes the reconciler IN PLACE (no new published Architect-reviewed contract) → reviewers are Security (the always-on safety-floor generalization is a fail-OPEN surface) + QA (the AC suite + the per-ADR numstat); the seam-edge shape is documented here as the T2-owned reconciliation detail, not routed to Architect.

## Verification Checklist

- [ ] AC-1: `reconcile` reads `cross_domain_seams` through ONE code path, 0 new per-domain-pair branches — `test_reconcile_reads_cross_domain_seams_through_one_path`.
- [ ] AC-2: 0 declared-seam conflicts reach the output un-reconciled — `test_declared_seam_conflict_never_unreconciled`; Step-2.5 drop-seam mutation turns it RED, reverted.
- [ ] AC-3 (SAFETY, BLOCKING): the three always-on floors (RED-S/LEA, additive-AE, Rx-BPMH) fire when the seam is UNDECLARED (empty `cross_domain_seams`) — `test_red_s_lea_floor_fires_undeclared` + `test_additive_ae_floor_fires_undeclared` + `test_rx_bpmh_floor_fires_undeclared`; Step-2.5 floor-removal mutation turns each RED, reverted.
- [ ] AC-3b (SAFETY, BLOCKING — SEC-W3-02 fail-OPEN gate-bypass closure): the three floors ALSO fire when the seam IS DECLARED (trigger + a well-formed seam on the same pair) — `test_red_s_lea_floor_fires_even_when_seam_declared` + `test_additive_ae_floor_fires_even_when_seam_declared` + `test_rx_bpmh_floor_fires_even_when_seam_declared`; the Step-2.5 floor-GATING mutation (`elif not declared_seam:`) turns each RED (invisible to AC-3's undeclared tests), reverted.
- [ ] AC-4: the five prior behaviors' safety content is re-based, not deleted — `test_five_prior_behaviors_still_reconcile` + `tests/plan/test_orchestrate.py` stays byte-GREEN (the frozen-guard behavioral replacement, ruling §4).
- [ ] AC-5: `git diff --numstat $PRE_TASK_HEAD -- scripts/plan/orchestrate.py` NON-empty; `... -- pipeline.py adjudicate.py store.py keying.py assemble.py` EMPTY; `git diff --numstat origin/main -- <always-frozen HARD six>` EMPTY — `test_per_adr_scoped_freeze_break_numstat` (RED under a throwaway forbidden-file edit, reverted). `generate_plan.py`+`plan_schema.py` dropped from the working-tree forbidden list per the F-011 narrowing (Deviation #1).
- [ ] AC-6: `.venv/bin/python -m pytest tests/serve/test_orchestrator_reconcile.py` passes (0 live-API, 0 real PII — an import grep confirms 0 live-client import + no real operator-identity literal).
- [ ] PF-S130-01 (MANDATORY): the production-path reconcile is driven with a non-hardcoded-pair `cross_domain_seams` seam through the care_chat collect+reconcile → `orchestrate.reconcile` path — `test_uniform_seam_pass_catches_non_hardcoded_pair`; Step-2.5 revert-to-five-holds mutation turns it RED (proving non-tautological), reverted.
- [ ] PF-S131-01: every `domain_program` fixture uses the canonical periodized `prescription` `{"blocks":[{date,load}]}` + tiered `monitoring_signals` + compound non-empty `required_labs` + the real `cross_domain_seams` edge shape, and passes `domain_program.validate` (not a simplified stand-in).
- [ ] care_chat additive: the capture call site (371-374) + `decompose`/`_derive_goals` (217-256) byte-unchanged — `test_capture_and_decompose_byte_unchanged`; the collect routes through the ONE `orchestrate.reconcile` path — `test_collect_routes_through_one_reconcile_path`; `tests/serve/test_care_chat.py` + `tests/serve/test_orchestrator_brief.py` stay GREEN (REGRESSION).
- [ ] care_chat collect-path safety floors (SEC-W3-03, BLOCKING): each floor fires THROUGH the care_chat collect+reconcile — `test_red_s_lea_floor_fires_through_care_chat_collect` + `test_additive_ae_floor_fires_through_care_chat_collect` + `test_rx_bpmh_floor_fires_through_care_chat_collect`; the collect PRESERVES `meta.ae_profile`/`reason` + derives+threads `operator_rx_classes` (RED if it shapes only the domain_program / defaults `operator_rx_classes`); Step-2.5 care_chat-collect-strip mutation turns each RED, reverted.
- [ ] Risk (SAFETY always-on floors) covered by AC-3 + the floor-removal mutations; Risk (PF-S130-01 tautological SWAP) by the production-path revert-REDs test; Risk (Neg-2 freeze break) by AC-5; Risk (frozen-guard carve-out gate gap) by the behavioral replacement (`test_orchestrate.py` green + the new module + AC-5) with the Wave-3 reconciliation carving `orchestrate.py` (NOT this task).
- [ ] Frozen spine: `git diff --numstat origin/main -- <always-frozen HARD six>` EMPTY — invariant 1. The `orchestrate.py` guard carve-out is the Wave-3 frozen-guard reconciliation step's (NOT this task's manifest — this task stages NO guard file).
- [ ] No regression: `.venv/bin/python -m pytest -q` holds the `2 failed, 2577 passed, 8 skipped` floor (no 3rd unexpected failure) with the new tests passing.
- [ ] Files modified match the manifest (`scripts/plan/orchestrate.py`, `scripts/serve/care_chat.py`, `tests/serve/test_orchestrator_reconcile.py`) — no scope creep (`generate_plan.py`/`plan_schema.py`/`plan_loop.py`/`server.py`/the guard files untouched); any Step-2.5 mutation edit is REVERTED, never committed.
- [ ] Interface contract documented (the `cross_domain_seams` seam-edge shape + the generalized `reconcile` semantics + the three always-on-floor invariants + the care_chat collect + the ADR-0043-T3/0046-T1 consumers).

## Deviations

| # | Spec Says | Recipe Says | Reason | Impact |
|---|----------|------------|--------|--------|
| 1 | AC-5: `git diff --numstat <base> -- scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/store/store.py scripts/store/keying.py scripts/plan/generate_plan.py scripts/plan/assemble.py scripts/store/plan_schema.py` returns EMPTY in THIS commit's delta. | The working-tree `$PRE_TASK_HEAD`-scoped probe DROPS `scripts/plan/generate_plan.py` + `scripts/store/plan_schema.py` from the forbidden-EMPTY list (keeps `assemble.py` + the frozen four). | The spec list is the COMMIT-scoped intent (0043-T2's OWN commit touches only `orchestrate.py`+`care_chat.py`+the test). But the persisted test runs at future checkpoints against the working tree/HEAD, where a Wave-3 SIBLING (ADR-0046-T1) LEGITIMATELY edits `generate_plan.py` (`_PLAN_TRANSLATORS` roster) + `plan_schema.py` (dispatch registry) — freezing them on the working-tree probe false-REDs. This is the EXACT precedent the Wave-2 frozen-guard-reconciliation addendum (iv)(b) set when it narrowed 0041-T2's probe to drop `plan_schema.py` (0044-T1's sibling record-spine edit). `assemble.py` STAYS frozen (no Wave-3 editor). The COMMIT-scoped intent is preserved by the Step-4 scope grep (`git diff --name-only $PRE_TASK_HEAD -- scripts/ tests/` == the three-file manifest). | Faithful to the spec's supersession-bounding intent; avoids a false-RED at later Wave-3 checkpoints. The `<always-frozen>` HARD six + `orchestrate.py`-NON-empty + `assemble.py`/frozen-four-EMPTY assertions — the load-bearing ones — are unchanged. |
| 2 | Decision §2/§3: "generalizing `orchestrate.reconcile`'s five hard-coded pair-specific holds … into one uniform-field pass; the softer reconciliations (energy bounce, overlap+conflict) become specialist-authored seams." | The generalization is TRANSITIONAL ADDITIVE: the uniform `cross_domain_seams` pass is LAYERED ON; the two softer reconciliations KEEP their existing `meta`-based reads (energy bounce via `meta.energy_budget`, conflict via `meta.conflicts`) AND become reachable via the uniform seam path when declared. | A big-bang MOVE of the soft reconciliations to `cross_domain_seams`-only would break the `meta`-seeded existing `tests/plan/test_orchestrate.py` fixtures — the 1960-line suite that IS the `orchestrate.py` frozen-guard behavioral replacement (ruling §4) — forcing a `test_orchestrate.py` edit NOT in this task's file manifest (scope creep). The transitional-additive reading mirrors disposition #2 + the 0041-T2 adapter precedent exactly: both shapes valid during migration; the full retirement of the `meta` reads follows once specialists emit populated `cross_domain_seams` (ADR-0046-T1). SE Ask-vs-Proceed rule 3: same public interface, behavior-preserving implementation, assumption stated. | Strictly SAFER (preserves the five behaviors' content + the existing suite green + the always-on floors). AC-1 (0 new per-pair branches) + AC-2 + PF-S130-01 hold via the layered uniform pass; AC-4 holds via the preserved reads. No manifest expansion (`test_orchestrate.py` stays byte-green, unedited). |

## Commit

```
feat(plan): reconcile via cross_domain_seams
```
Stage only: `scripts/plan/orchestrate.py`, `scripts/serve/care_chat.py`, `tests/serve/test_orchestrator_reconcile.py`.

(Subject is 44 chars — within the ≤50 convention; keeps the two load-bearing tokens `reconcile` + `cross_domain_seams` (the longer "generalize reconcile to cross_domain_seams" phrasing is 54 chars, over budget). Stage with `git add <the three paths>` as its OWN command, then `git commit` separately — the PII commit hook denies single-call stage+commit, `git commit -a`, and pathspec `git commit <path>`. Confirm NO Step-2.5 mutation edit remains in the working tree, `care_chat.py`'s capture path + decompose region are byte-unchanged, the `<always-frozen>` six are byte-unchanged, and NO guard file / `vault/` daemon-churn file is staged, before committing. The `orchestrate.py` guard carve-out is the Wave-3 frozen-guard reconciliation commit — owned by neither Wave-3 task — this task stages NONE of the guard files.)

## Rollback

If verification fails after 3 fix cycles:
1. `git stash -m "failed-ADR-0043-T2-attempt-{N}"` — preserve the work for diagnosis (also stashes any un-reverted Step-2.5 mutation edit).
2. `git checkout {commit-before-task}` — revert to the entry-state HEAD (`PRE_TASK_HEAD`) recorded in Entry State (restores `orchestrate.py`/`care_chat.py`, discards this task's edits + any residual Step-2.5 mutation; the guard carve-out belongs to the separate Wave-3 reconciliation commit and is reverted with it, not here).
3. Update the issue tracker: flag ADR-0043-T2 as BLOCKED with the failure log.
4. Escalate: "Task ADR-0043-T2 failed verification after 3 attempts. Stash ref: {ref}. Failure log: {summary}." ADR-0043-T2 is the SAFETY-CRITICAL reconcile generalization ADR-0043-T3 (synthesize) + ADR-0045-T2 (Tier-3 calls the `cross_domain_seams` reconciler) depend on — a BLOCK halts the Wave 3→4 boundary; surface it immediately, do not start any downstream task.
5. Stash cleanup: after abandonment or re-planning, drop all `failed-ADR-0043-T2` entries from `git stash list`.

Do NOT: silently skip a failing acceptance criterion; weaken a test to make it pass (ESPECIALLY the AC-3 always-on-floor safety tests or the PF-S130-01 production-path revert-REDs test — these ARE the fail-OPEN patient-safety guard + the anti-tautology guard); edit any frozen guard here (the `orchestrate.py` carve-out is the Wave-3 reconciliation commit's, not this task's — this task stages NO guard file); or proceed to any downstream task with the reconciler in a broken state.
