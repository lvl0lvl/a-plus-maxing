---
task: ADR-0048-T2
title: App-mediated OAuth authorization-code flow + localhost PKCE callback + D7 wire-scan
status: approved
wave: 4
adr: ADR-0048
depends_on: [ADR-0048-T1, ADR-0048-T3]
branch: feature/adr-0048-t2-oauth-callback
base: main
files:
  - path: scripts/serve/oauth_callback.py
    action: create
  - path: scripts/serve/server.py
    action: modify
  - path: tests/serve/test_oauth_callback.py
    action: create
frozen_six_forkpoint: 3ab1c3abb6c995fbaaadcb179735759e4a61d73d
---

# ADR-0048-T2 — App-mediated OAuth authorization-code flow + localhost PKCE callback + the D7 wire-scan

## Goal

Build `scripts/serve/oauth_callback.py` — the one-shot `127.0.0.1` OAuth-2.0 authorization-code
callback (PKCE code-challenge/verifier + `state`/CSRF + a code→token exchange behind a fail-closed
no-follow-redirect opener) — and a `server.py` connect-start route that spins up the listener and
opens the vendor authorize URL. On a matching callback the exchanged **refresh_token** (in the
per-vendor shape the reader consumes — see the consumer contract) is written through the ADR-0048-T3
per-source write path into the SAME `a-plus-maxing-<source>-oauth` item `oauth_pull`'s reader reads.
The load-bearing deliverable is the **D7 crown-jewel wire-scan** — the OAuth sibling of the ADR-0049-T1
D2 scan, captured at the TRUE HTTP wire (`opener.open(request)`): driving auth→callback→exchange
proves 0 store bytes in any outbound request, 0 no-train-model-lane calls, and the vendor's own
authorize/token endpoint as the ONLY outbound host contacted — each family per-family RED-gated by a
self-invalidating mutation on the REAL opener (the D2 gold standard).

## Consumer-contract grounding (PF-S141-02 — enumerate the FULL contract of the seam this task WIRES ONTO, incl. the DOWNSTREAM READER's value-format)

The ADR-0049-T1 REJECT taught: a wired-onto seam's FULL contract must be enumerated at grounding —
its write-site gates AND its downstream reader's value-format — and the E2E must drive the PRODUCTION
consumer, never a fixture. This task writes an item with exactly ONE downstream consumer; both halves:

**(A) The write-site (`_save_tracker_token`, `server.py:1017`).** Write core:
`secret_store.set_secret(oauth_pull._oauth_service_name(source), token)` (`:1087`), reached ONLY after
exact-membership `source in oauth_pull._MANIFESTS` (`:1074`, fail-closed 400 — closes service-name
injection: an unvalidated source could write an arbitrary keyring item) + `token.strip()` non-empty
(`:1077`). `set_secret` raises `secret_store.KeyStoreError` (`secret_store/__init__.py:87`) on a failed
write INCLUDING the clear-fail case where the write SUCCEEDED but stale fallback residue could not be
cleared — so a failure path must NOT assert "unstored" as certainty. **MANDATE (resolves QA SHOULD-FIX
4 / the extract-vs-duplicate ambiguity): EXTRACT the shared write path** (source-membership-validate →
strip-non-empty → `set_secret(_oauth_service_name(source), token)` → KeyStoreError-fail-closed) into
ONE helper (e.g. `oauth_callback.store_oauth_credential(source, value)` or a `server.py` shared method)
that BOTH `_save_tracker_token` AND the callback call — one validated writer, no second unvalidated
path. Refactoring `_save_tracker_token` to call the helper is IN SCOPE (its existing
`test_credential_writes.py` gate/injection/500 tests then cover the shared helper unchanged — verify
they stay green).

**(B) The downstream READER's value-format (`oauth_pull.access_token`, `scripts/ingest/oauth_pull.py:362`
— the ONLY consumer of the item; the LOAD-BEARING half F1 caught).** `access_token(source)` →
`_parse_credentials(payload)` (`:368`→`:304-316`) → `body = {"grant_type": "refresh_token",
"refresh_token": creds["refresh_token"], ...}` (`:370`), forwarding `client_id`/`client_secret` into
the grant IFF the parsed blob carries them (`:372-375`). So the stored value MUST be:
- the OAuth **`refresh_token`** (NOT the access token — the reader's sole use is as the `refresh_token`
  in a refresh grant; storing the access token yields a credential dead on the first live tick →
  vendor 400 → `TrackerPullError`), and
- for a **confidential-client** vendor (ADR-0048-T1 per-vendor client-type; Whoop/Google), the JSON
  blob `{"refresh_token": ..., "client_id": ..., "client_secret": ...}` `_parse_credentials` consumes
  (the refresh grant requires them, `oauth_pull.py:141-142`); for a **PKCE-public** vendor, the bare
  refresh token (no secret). The per-vendor shape is governed by the ADR-0048-T1 client-type record.

**The `source` used in the write is the in-memory source validated at connect-start** (held with the
verifier+state for the one-shot match), NEVER re-parsed from the attacker-influenceable callback
request (Sec clarity add — the callback request carries only `code`+`state`; `source` is server-side).

## Entry State (verify GREEN before writing any code)

1. On the branch `feature/adr-0048-t2-oauth-callback` (off main after the S141 close #361). NEVER `main`.
2. `scripts/serve/server.py:_save_tracker_token` (`:1017`) — the ADR-0048-T3 write class (contract §A).
   `do_POST` dispatch (`:199-224`) is where the connect-start route is added. SEC-001
   `application/json`→415 idiom at `:1036-1038`. Verify: `grep -n "def _save_tracker_token\|_oauth_service_name\|_MANIFESTS" scripts/serve/server.py`.
3. `scripts/ingest/oauth_pull.py`: `_NoFollowRedirect` (`:255`, `redirect_request`→`None` — declines
   every 3xx; docstring "the redirect target is never contacted" `:262`) + `_OPENER` (`:270`) — the
   fail-closed opener AC-4 MIRRORS. `access_token(source, *, http=None)` (`:330`/`:362`) +
   `_parse_credentials` (`:304`) — the READER (contract §B). `_oauth_service_name` (`:184`) →
   `a-plus-maxing-<source>-oauth`; `_MANIFESTS` = `{whoop, oura, garmin, google-health}` (`:165-170`;
   **garmin is OAuth-1.0a — `"auth": "oauth1a"` `:125`, NO `token_url`/authorize endpoint**; the
   authcode-servable subset is `{whoop, oura, google-health}` — those with a `token_url`). Verify:
   `grep -n "class _NoFollowRedirect\|_OPENER\|def access_token\|def _parse_credentials\|_MANIFESTS =\|oauth1a\|token_url" scripts/ingest/oauth_pull.py`.
4. `scripts/secret_store/__init__.py`: `set_secret(...)` (`:230`) raises `KeyStoreError` (`:87`).
5. `scripts/model/client.py:_ClaudeNoTrainBackend` (`:678`; `_client()` `:689`) — the no-train MODEL
   lane the D7 scan asserts 0 calls to (a monkeypatched call-counting fake). Verify present.
6. `scripts/store/store.py` read surface `read`/`read_all`/`items` (`:214`/`:263`/`:241`) — the D7
   store family's module-level spy + AST no-import targets (mirror the D2 pattern in
   `tests/runner/test_metered_dispatch.py`).
7. `secrets` stdlib available for the CSPRNG mandate (verifier/state); no `secrets`/`token_urlsafe`
   precedent exists in `scripts/serve/` (greenfield — mandate it explicitly, Sec MEDIUM-2).
8. CREATE targets ABSENT: `ls scripts/serve/oauth_callback.py tests/serve/test_oauth_callback.py`.
9. Frozen-six clean:
   `git diff --numstat 3ab1c3abb6c995fbaaadcb179735759e4a61d73d -- scripts/store/store.py scripts/store/keying.py scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/router.py`
   prints nothing.
10. Baseline: `.venv/bin/python -m pytest -q` — no NEW failure vs the current baseline (2 env-floor
    reds: `test_port_collision_exits_nonzero_fail_loud` + `test_main_reads_no_stdin`, port 8765 held).
11. Route-count guard (bead `2zz8`): `tests/serve/test_server.py:2349` pins `count('self.path == "/') == 9`;
    the connect-start route bumps 9→10 in the SAME PR (the `/confirm-plan-change` precedent). ADJACENT
    guard `test_server.py:2338` pins `count("ModelClient(") == 2` — the connect-start route must add
    NO `ModelClient(` construction to `server.py` (the callback is a separate module). Verify both:
    `grep -c 'self.path == "/'` and `grep -c 'ModelClient(' scripts/serve/server.py`.

## Context Load List (read exactly these — no others)

- `scripts/serve/server.py` (`_save_tracker_token` `:1017-1094`; `do_POST` `:199-224`; the SEC-001 415
  + body-ceiling + JSON-shape gates `:1032-1065`; `build_server` `:1130`).
- `scripts/ingest/oauth_pull.py` (`_NoFollowRedirect`+`_OPENER`+`_http` `:255-300`; `access_token`+
  `_parse_credentials` `:304-386` — the reader; `_oauth_service_name`; `_MANIFESTS`; the vendor manifests
  incl. garmin `oauth1a`) — the fail-closed opener + the reader value-format + the source-servability set.
- `scripts/secret_store/__init__.py` (`set_secret`+`KeyStoreError` `:87`/`:230`).
- `scripts/model/client.py` (`_ClaudeNoTrainBackend` `:678-694`).
- `scripts/store/store.py` (`read`/`read_all`/`items` `:214-263`).
- `tests/serve/test_credential_writes.py` — the write-class test patterns (`test_text_plain_post_is_415_before_any_write`,
  `test_no_content_type_header_is_415_before_any_write`, `test_unwired_source_is_400`, the 500 case) to
  mirror for the connect-start route + confirm the extracted-helper refactor keeps them green.
- `tests/runner/test_metered_dispatch.py` — the D2 gold standard (`_RecordingSDK`/`_RecordingMessages`
  full-kwargs capture `:77-113`; `_assert_clean_metered_egress` full-serialization `:141-157`; the
  module-level store spy `:110-121`; the AST no-store-import `:325-388`; the THREE distinct mutants —
  content-append `:464`, store-**read** `:473`, lane-swap) — the D7 scan MIRRORS this shape.
- `tests/serve/test_server.py` (the route-count guard `:2349` + the `ModelClient(` guard `:2338`).

## Design (what the SE builds — no literal code here; this is the contract)

### `scripts/serve/oauth_callback.py` (CREATE)

- **PKCE + state generation (Sec MEDIUM-2 — CSPRNG mandated):** mint the `state` (CSRF nonce) + the
  PKCE `code_verifier` from `secrets.token_urlsafe(≥32)` (verifier length ≥ 43 chars, RFC 7636); the
  `code_challenge` is `base64url(SHA256(code_verifier))` (S256). The verifier+state+source are held
  in memory for the one-shot callback match (never persisted, never logged).
- **The authorize-URL builder:** given a `source` restricted to the OAuth-2.0-authcode-servable set
  (has an authorize + `token_url` endpoint per its ADR-0048-T1 client type — i.e. NOT garmin/OAuth-1.0a,
  NOT a PAT vendor), build the authorize URL carrying the challenge + state + the `127.0.0.1:<ephemeral>`
  redirect_uri, using the vendor `client_id` from `alpha_config`. Consume the T1 per-vendor client-type
  record (confidential / PKCE-public), which governs whether a `client_secret` participates in the
  exchange + must be persisted in the stored blob (contract §B).
- **The one-shot `127.0.0.1` listener:** binds a LOOPBACK address on an ephemeral port; after
  accepting EXACTLY one request (ANY request — a state-mismatched/malformed first callback ALSO
  consumes the one-shot, Sec LOW-1: fail-closed, smaller window) it tears down deterministically (the
  accept-loop terminates + the listening socket closes + the server thread joins).
- **The callback handler:** REJECT a mismatched `state` (0 token writes — AC-3); on a match run the
  exchange. The browser response + any stderr on EVERY path (success + error) echoes NONE of the
  auth `code`, `code_verifier`, or `client_secret` (Sec MEDIUM-3; extend `oauth_pull`'s NFR-3
  constant-message discipline to all three secret classes).
- **The code→token EXCHANGE (fail-closed, AC-1/AC-4 — an INJECTABLE opener seam, QA MUST-FIX-1 / Arch
  F4):** POST the `code` + `code_verifier` (+ `client_id`/`client_secret` for a confidential client)
  to the vendor token endpoint through an **injectable opener** (an `opener=`/`http=` param or a
  monkeypatchable module attr, mirroring `oauth_pull.access_token(..., http=None)` resolved at call
  time) whose DEFAULT is a build_opener over a `redirect_request`→`None` handler that MIRRORS
  `oauth_pull._NoFollowRedirect` + host-locks the token endpoint — so the exchange **never contacts a
  redirect host** (a 3xx raises/fails-closed exactly like `oauth_pull._http`), and a token endpoint
  returning a 3xx-to-another-host aborts with **0 connections to the redirect host** + 0 token writes
  (AC-4). PKCE protects the EXCHANGE: the token request carries the `code_verifier` for the issued
  challenge; a fixture endpoint validating `S256(code_verifier) == code_challenge` rejects a
  missing/mismatched verifier → 0 token writes (AC-1).
- **The write (contract §A/§B):** on a successful exchange, PARSE the response into the per-vendor
  stored shape (the `refresh_token`, or the `{refresh_token, client_id, client_secret}` blob for a
  confidential client) and write it via the EXTRACTED shared write helper (source-membership-validate
  → strip → `set_secret(_oauth_service_name(source), value)`) for the in-memory validated `source`.
  Never log/echo the value. `KeyStoreError` → fail-closed, not asserting "unstored" as certainty.
- The module reads NO store (structural — imports no `scripts.store` symbol; the D2 guarantee) + opens
  NO no-train model lane; the ONLY outbound hosts are the vendor authorize/token endpoints.

### `scripts/serve/server.py` (MODIFY — the connect-start route, a DISTINCT region)

Add a `self.path == "/settings/connect"` POST branch in `do_POST` (`:209`-adjacent) + a new handler
method that BYTE-MIRRORS `_save_tracker_token`'s gate order (`:1032-1065`): SEC-001
`application/json`→415 CSRF gate FIRST (BEFORE any listener bind or browser-open — a cross-site
CORS-simple `text/plain`/no-Content-Type POST refused 415), then the 16-KiB `_SETTINGS_MAX_BYTES`
ceiling + JSON-parse/shape gates, then validate the JSON `{source}` is in the OAuth-2.0-authcode-servable
set (has `token_url` per its T1 client type — reject garmin/PAT/unknown with a clean 400, Arch F2),
then spin up the one-shot listener + open the vendor authorize URL. Adds NO `ModelClient(`
construction. Bump the `test_server.py` route-count guard 9→10 in the same PR. Does NOT touch the
ADR-0048-T3 `_save_tracker_token` region (except the sanctioned extract-the-write-helper refactor) or
the ADR-0049 `main()`/loop region — a distinct `server.py` region.

## Acceptance Criteria (executable RED/GREEN checks)

Each AC either REDs on feature-absence, or — for a negative/no-leak assertion — is validated by an
in-suite `pytest.raises` meta-assertion (the D2/AR-005 discipline). All env via `monkeypatch`; hermetic,
$0. **The exchange + authorize legs run through the REAL injectable opener against REAL `127.0.0.1`
loopback fixture servers** (the authorize endpoint, the token endpoint, and a redirect/attacker host),
each fixture recording the FULL received request (URL + headers + body); NO substituted exchange seam
that bypasses the real no-follow opener (Sec MEDIUM-1 / QA MUST-FIX-1).

1. **PKCE exchange + write the refresh_token via the ADR-0048-T3 write path (AC-1).** A matching
   callback drives the exchange carrying the `code_verifier` for the issued challenge → a fixture token
   endpoint that VALIDATES `S256(verifier)==challenge` (QA SHOULD-FIX 6) accepts + returns a token
   response → the **refresh_token** (in the per-vendor shape) is written via the shared helper
   (`set_secret(_oauth_service_name(source), <value>)`; assert the exact item + value). **Reader
   round-trip (Arch F1 — drive the PRODUCTION consumer):** the written value, fed to
   `oauth_pull._parse_credentials` (and/or `oauth_pull.access_token(source, http=<fixture>)`), yields a
   usable refresh grant (a `refresh_token` grant body; for a confidential vendor the `client_id`/
   `client_secret` are forwarded) — proving the stored value satisfies the reader, not an author-chosen
   scalar. **Falsifier:** a fixture endpoint receiving a MISSING/MISMATCHED `code_verifier` rejects →
   **0 token writes**; and storing the ACCESS token instead of the refresh token → the reader
   round-trip REDs (the value the reader needs is absent).
2. **Loopback-only bind (AC-2).** Assert the LISTENER'S REAL bound address (`server_address[0]` /
   `getsockname()[0]`, not a config value — QA SHOULD-FIX 7) is loopback (`127.0.0.1`/`::1`), never
   `0.0.0.0`/`""`/a routable address (Sec LOW-2). Falsifier: a mutant binding `0.0.0.0` REDs.
3. **`state`/CSRF mismatch (AC-3).** A callback whose `state` ≠ the issued value → **0 token writes**
   (the exchange never runs). Falsifier: the matching-state case DOES write (the path is live).
4. **The redirect host is NEVER CONTACTED on the exchange (AC-4 — Sec HIGH-1: assert the real control,
   not "0 bytes").** A fixture token endpoint returning a 3xx-to-another-host → the exchange
   ABORTS/fails-closed AND the redirect/attacker-host fixture records **0 connections** (connection-count
   == 0 at its socket — the `_NoFollowRedirect` guarantee "the redirect target is never contacted") AND
   **0 token writes**. Falsifier (in-suite meta-assertion): a mutant opener that swaps ONLY
   `redirect_request` (None→returns-a-Request) on the REAL exchange path → urllib `.open()`s the
   attacker host → the attacker fixture's connection-count > 0 → `pytest.raises(AssertionError)` the
   scan REDs. (A byte-content assertion is INSUFFICIENT: a 302/303 drops the POST body so 0 bytes leak
   even when following — the connection-count is the RED-capable check across 302/303/307/308 + both
   header-auth and body-auth vendors.)
5. **One-shot listener (AC-5).** After accepting exactly one request (any request, incl. a rejected
   one) the listener tears down; assert the DETERMINISTIC post-condition (the accept-loop terminated /
   the server thread joined / the listening socket is closed — QA SHOULD-FIX 5), with a
   second-request-refused check as a SECONDARY (not the load-bearing racy `ConnectionRefusedError`).
   Falsifier: a mutant that re-accepts → the socket stays open → the one-shot assertion REDs.
6. **D7 crown-jewel wire-scan (AC-6, load-bearing — the D2 gold-standard shape, captured at
   `opener.open(request)`).** Drive auth→callback→exchange through the REAL injectable opener against
   loopback fixtures, recording each FULL outbound request (`request.full_url` + `request.headers` +
   `request.data` — serialize ALL of it, `json.dumps(recorded, default=str)`, NOT an enumerated
   subset), covering BOTH the authorize-URL leg AND the exchange POST. A store-content sentinel seeded
   into the store + a model-lane call-counting fake. Assert the CLEAN direction FIRST:
   (a) **0 bytes of store content** in any recorded outbound request (the sentinel appears 0 times) AND
   the module performs **0 store reads** (a module-level `store.read/read_all/items` spy == 0) AND
   imports no `scripts.store` symbol (AST structural check);
   (b) **0 calls to the no-train model lane** (`_ClaudeNoTrainBackend` fake call-count 0);
   (c) the **ONLY outbound host** contacted is the vendor's own authorize/token endpoint.
   RED-gate EACH sub-guarantee with a self-invalidating mutation, each wrapped in
   `pytest.raises(AssertionError)` so a scan that CANNOT go RED fails the meta-assertion:
   - a stub adding a store-content byte to an outbound request MUST RED (a)'s content assertion;
   - **a stub opening a `store.read(...)` on the callback path MUST RED (a)'s store-read spy** (the
     DISTINCT store-READ mutant — Arch F3 / QA MUST-FIX 2 — the store-content-append mutant does NOT
     exercise the spy, mirror `test_metered_dispatch.py:473`);
   - a fixture token endpoint returning a 302-to-attacker-host MUST RED (c)'s vendor-only-host
     assertion (the attacker fixture's connection-count > 0 — shared with AC-4);
   - a stub opening a no-train model-lane call on the auth/callback path MUST RED (b)'s model-lane
     assertion.
   **The test is invalid, and itself fails, if any mutation does not RED its sub-guarantee.**
7. **Connect-start route coverage (AC-7 — QA MUST-FIX 3: a new attacker-facing state-changing POST).**
   In a live-socket `build_server(0)` test (the `test_credential_writes.py` pattern): (a) a `text/plain`
   AND a no-Content-Type POST to `/settings/connect` → **415 BEFORE any listener bind or browser-open**
   (a negative assertion: no ephemeral port bound + the browser-open shim uncalled + no listener
   thread spawned — the `_save_tracker_token:1032` before-any-side-effect parity); (b) an
   out-of-authcode-servable `source` (garmin / a non-`_MANIFESTS` source) → clean 400, no
   listener/browser side effect; (c) the happy path — a valid authcode-servable source → the loopback
   listener binds + the authorize URL opens (a browser-open shim records the vendor-host URL). Falsifier
   for (a): a mutant placing the 415 gate AFTER the listener spin-up → the negative assertion REDs.
8. **No secret echo on any path (AC-8 — Sec MEDIUM-3).** Seed synthetic `code`/`code_verifier`/
   `client_secret` sentinels; drive the success path AND every error path (non-2xx token response, the
   3xx abort, a network error, `KeyStoreError`); assert the captured stderr AND the callback's HTTP
   browser response contain each sentinel **0 times**. Falsifier (in-suite meta-assertion): a stub that
   echoes the exchange error into the browser response → the scan REDs.
9. **Frozen-six untouched (AC-9).** Re-run the Entry State 9 frozen-six `git diff --numstat` on the
   FINAL tree; it prints nothing (the callback lands in `scripts/serve/`, the route in the connect-start
   `server.py` region — both outside the six).
10. **Suite green (AC-10).** `.venv/bin/python -m pytest tests/serve/test_oauth_callback.py tests/serve/test_credential_writes.py -q`
    passes (incl. the extracted-helper refactor keeping the T3 write-class tests green) + the
    `test_server.py` route-count guard bumped 9→10; the full baseline shows no NEW failure vs entry.

## TDD Cycle (1 cycle, 10 ACs)

- **RED:** write `tests/serve/test_oauth_callback.py` (AC-1..AC-8) + the `test_server.py` route-count
  bump targeting each AC. Every test RED-capable per its falsifier — ESPECIALLY the AC-4 connection-count
  redirect mutant + the AC-6 FOUR mutants (content-append, store-READ, vendor-host, model-lane) + the
  AC-1 mismatched-verifier-0-write + wrong-value reader-round-trip + the AC-7 gate-after-side-effect
  mutant. Run → confirm RED.
- **GREEN:** create `scripts/serve/oauth_callback.py` (PKCE-CSPRNG + one-shot listener + injectable
  no-follow opener + the per-vendor refresh-token parse + the extracted shared write helper) + the
  `server.py` connect-start route + refactor `_save_tracker_token` onto the shared helper + the
  count-guard bump. Run → confirm GREEN + the T3 write-class tests stay green.
- **FALSIFIER VALIDATION (post-GREEN):** EXECUTE every falsifier + confirm RED (the AC-4/AC-6/AC-8
  mutations + AC-7(a) are in-suite `pytest.raises`; the AC-1 wrong-value, AC-2/AC-5 mutant-bind/re-accept
  run once, confirm RED, revert). A falsifier that cannot go RED = a vacuous assertion → HALT.
- **REFACTOR:** de-duplicate the loopback-fixture / recording-opener harness; match house Google-style
  docstrings. No behavior change; tests stay GREEN.
- **REGRESSION:** `.venv/bin/python -m pytest -q` — no NEW failure vs the entry baseline.

## Verification Checklist (Tier-1 SE self-check — every AC → a passing test/command)

- [ ] AC-1: the REFRESH_TOKEN (per-vendor shape) written via the shared helper; the reader round-trip
      (`oauth_pull._parse_credentials`/`access_token`) yields a usable grant; mismatched-verifier → 0
      writes; wrong-value (access token) → reader round-trip RED (executed).
- [ ] AC-2: real bound socket asserted loopback (never `0.0.0.0`); mutant-bind RED.
- [ ] AC-3: mismatched `state` → 0 writes; matching-state falsifier proves the path live.
- [ ] AC-4: the redirect host records 0 connections + the exchange aborts + 0 writes; the
      swap-only-`redirect_request`-on-the-real-opener mutant REDs the connection-count (NOT a byte check).
- [ ] AC-5: deterministic one-shot teardown (socket closed / thread joined); re-accept mutant REDs.
- [ ] AC-6 (D7): captured at `opener.open(request)` over the FULL request (both authorize + exchange
      legs); 0 store bytes + 0 store reads (spy) + 0 model-lane calls + vendor-only-host; FOUR mutants
      (content-append, store-READ, vendor-host, model-lane) each RED under `pytest.raises` (executed).
- [ ] AC-7: `/settings/connect` 415-before-side-effect (mutant-gate-after REDs) + out-of-servable-source
      400 + happy-path binds listener & opens authorize URL.
- [ ] AC-8: no `code`/`verifier`/`client_secret` echo in stderr or the browser response on any path;
      echo-mutant REDs.
- [ ] AC-9: frozen-six numstat empty (executed on the final tree).
- [ ] AC-10: `test_oauth_callback.py` + `test_credential_writes.py` pass; route-count guard 9→10;
      `ModelClient(` count unchanged at 2; full baseline no NEW failure.
- [ ] PF-S141-02 consumer contract honored (both halves): the write goes through the ONE extracted
      validated helper (no second writer); the stored value satisfies the PRODUCTION reader
      (`oauth_pull.access_token`) proven by the round-trip AC; the connect-start route rejects a
      source it cannot service (garmin/OAuth-1.0a/PAT).

## Commit Protocol

Stage ONLY the three manifest files, `git add` then `git commit` as SEPARATE commands
(block-pii-commit denies single-call stage+commit). Message:
`feat(serve): OAuth authorization-code callback + D7 wire-scan (ADR-0048-T2)`.
No operator PII / no real secret / no real vendor `client_secret` in any staged file (synthetic
fixtures only; the vendor + attacker endpoints are `127.0.0.1` loopback test servers). One atomic commit.

## Review Remediation Log (Phase-4 3-lens — Security / Architect / QA; all findings legitimate, all fixed)

- **Arch F1 (REJECT — the PF-S141-02 value-format miss):** the stored value is now pinned as the OAuth
  REFRESH_TOKEN in the per-vendor shape `oauth_pull._parse_credentials` consumes (not "the exchanged
  token"); AC-1 adds a reader round-trip driving the PRODUCTION consumer.
- **Sec HIGH-1 (redirect leak not RED-capable):** AC-4 now asserts the redirect host is NEVER
  CONTACTED (connection-count 0), not "0 bytes" (a 302/303 drops the body → 0-bytes false-greens a
  following opener); the mutant swaps only `redirect_request` on the real opener.
- **Sec MEDIUM-1 + Arch F4 + QA MUST-FIX-1 (capture at the true boundary through the real opener):** the
  exchange/authorize legs run through the REAL injectable opener against loopback fixtures; AC-6
  captures at `opener.open(request)` over the FULL request (URL+headers+body).
- **Arch F3 + QA MUST-FIX-2 (missing store-READ mutant):** AC-6 adds the DISTINCT store-read mutant
  (the content-append mutant does not exercise the spy — mirror the D2 two-mutant pattern).
- **QA MUST-FIX-3 (connect-start route untested):** new AC-7 covers 415-before-side-effect,
  source-validation, and the happy path (the route spins up a listener + opens a browser — heavier
  than a keychain write).
- **Sec MEDIUM-2 (PKCE CSPRNG):** `secrets.token_urlsafe(≥32)` mandated for verifier+state; AC-1's
  fixture validates `S256(verifier)==challenge` (QA SHOULD-FIX 6).
- **Sec MEDIUM-3 (secret echo):** new AC-8 scans stderr + the browser response for the code/verifier/
  client_secret across every error path.
- **Arch F2 (source-servability):** the connect-start gate restricts to OAuth-2.0-authcode-servable
  vendors (rejects garmin/OAuth-1.0a/PAT), consuming the T1 client-type record.
- **QA SHOULD-FIX-4 (extract-vs-duplicate):** MANDATE extracting the shared write helper (one validated
  writer; the existing T3 tests cover it).
- **QA SHOULD-FIX-5/7 + Sec LOW-1/LOW-2:** deterministic one-shot teardown; real-socket loopback
  assertion (`::1` accepted); the first-request-consumes-the-one-shot posture.
- **Sec clarity + Arch F5 + the ModelClient-count awareness:** the write `source` is the server-side
  validated value (never the callback request); the connect-start route carries the body-ceiling +
  JSON-shape gates + adds no `ModelClient(` construction.
