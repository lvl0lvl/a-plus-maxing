---
task-id: ADR-0044-T4
source-spec: docs/spec/adr-0041-0046-comprehensive-plan-spec.md
source-build-plan: docs/build-plan/build-plan-adr-0041-0046.md
wave: 4
assigned-agent: SE
reviewers: [QA]
created: 2026-07-12
status: approved
---

# Recipe: Re-base the ADR-0040 Confirmation-Pointer Hold onto the Comprehensive Resolver (STORE SURFACE + FROZEN-SPINE)

> Wave-4 Tier-2 task (Dependency: ADR-0044-T2, MERGED to `origin/main` via #334 — the mixed-history
> reader `read_standing_plan` + the `resolve_comprehensive`/`read_plan_version` resolver this task
> re-bases the confirm-hold onto). It MODIFIES `scripts/store/plan_model.py` so the COMPREHENSIVE
> standing resolution honors the ADR-0040 confirmation-pointer skip: an unconfirmed large-change
> comprehensive plan version resolves **NOT-standing**, a confirmed one stands, and a version with no
> pointer stands (default). The re-base lives in the RESOLVER (`read_plan_version`, disjoint from
> 0044-T2's `read_standing_plan` region); the ADR-0040 pointer MECHANISM (`scripts/store/plan_confirm.py`)
> is byte-UNCHANGED (`numstat=0`) — the resolver CONSUMES `plan_confirm.decision_for` unchanged.
>
> WHY the re-base is needed. 0044-T2 re-pointed the production standing-plan path off the thin
> `plan_schema.read_plan` (which applies `plan_confirm.filter_confirmed`) onto `read_standing_plan`,
> whose COMPREHENSIVE-WINS branch (`read_plan_version` → `resolve_comprehensive`) currently applies NO
> confirm filter. So today a HELD (unconfirmed) large-change COMPREHENSIVE re-gen would STAND — the
> comprehensive branch bypasses the thin hold. This task closes that hole (spec falsification #3, ADR-0044
> Consequences-Negative-2, tension-resolution #29). The Tier-4 human-gate of ADR-0045-T2 REUSES this
> re-based hold (Wave 5) — it is the zero-slack critical-path task on the model-resolver line.
>
> MANIFEST = 2 files: `scripts/store/plan_model.py` (Modify — the confirm-hold-skip region) +
> `tests/store/test_plan_model_confirm_hold.py` (Create). NO `track.py` re-point (0044-T2 already did it;
> this task inherits the hold TRANSITIVELY through `read_standing_plan` → `read_plan_version`). NO
> frozen-guard reconciliation: `plan_model.py` is ALREADY unfrozen (the `test_cadence_runner.py` recursive
> `scripts/store/**` glob excludes `plan_model.py`, `:309`; `test_confirm_pointer_skip.py::FROZEN_GLOB`
> omits it) — this task supersedes no NEW file, so it trips no byte-guard.
>
> STORE SURFACE — the four-part store-adversarial battery (bead `pka`, `docs/checklists/store-adversarial-tests.md`)
> is a BLOCKING Tier-1 gate over the confirm-hold-skip surface; category (d) runs RED-then-revert under the
> NAMED confirm-hold-removal mutation. A placeholder / `assert True` test fails the gate. The S37–S41
> five-wave store-keying escape pattern (cross-stream, same-timepoint dedupe) is the exact failure class
> this battery moves upstream of Tier-3.
>
> PF-S130-01 (the Wave-1 tautological-swap catch). The confirm re-base restores, on the comprehensive
> branch, the hold that 0044-T2's comprehensive-wins re-point BYPASSED — so AC-1 carries a PRODUCTION-PATH
> integration test on the REAL `track.resolve_plan_progress` over a HELD comprehensive version, whose input
> DISTINGUISHES the new (held → not standing) behavior from the old (held → stands), and which REDs on a
> revert of the filter re-point (the reverted resolver stands the held comprehensive version). A test that
> passes whether or not the filter was added is tautological and does NOT satisfy the AC.
>
> PF-S131-01 (fixture shape). Every fixture carrying a `domain_program` prescription uses the CANONICAL
> PERIODIZED shape (`{"blocks":[{date, ...}]}`, `domain_program.validate`-conformant), NOT a flat payload;
> the held/confirmed/no-pointer comprehensive-version fixtures are the REAL `plan_model` composite (reuse
> the `_comprehensive_version` / `_training_program` builders proven in `tests/store/test_plan_model.py` +
> `tests/store/test_plan_model_reader.py`), recorded via `plan_model.record_plan_version`.
>
> PF-S132-02 (worktree isolation for mutation testing). Every mutation/revert step below (the AC-4(d)
> confirm-hold-removal, the PF-S130-01 filter-revert, the AC-FS forbidden-file edit) MUTATES source. Any
> agent performing them MUST run them in an ISOLATED git worktree (skill `using-git-worktrees`), NEVER
> mutate-and-restore the shared main checkout — the basic-memory daemon watches the main checkout's `vault/`
> and a mutate-and-restore races it / risks residue (S90; the `git-worktree-commit-blocked` note applies —
> use the worktree for READ-ONLY mutation observation, commit the real work in the main checkout on the
> feature branch).

## Entry State

**Prerequisites** (ADR-0044-T4 depends ONLY on ADR-0044-T2 — the mixed-history reader + the comprehensive
resolver it re-bases the hold onto; per the Dependency Map `ADR-0044-T2 --> ADR-0044-T4`, "the mixed-history
reader lands first, the confirm-hold skip rebases on it"):

- [ ] Task ADR-0044-T2 complete and on `origin/main` (merged #334; `origin/main` = the parent of this
  branch's single spec-reconcile commit — Wave 3 is on the trunk): `grep -q "^def read_standing_plan" scripts/store/plan_model.py && grep -q "^def read_plan_version" scripts/store/plan_model.py && grep -q "^def resolve_comprehensive" scripts/store/plan_model.py && grep -q '_PREFIX_MODEL = "plan-model::"' scripts/store/plan_model.py && echo OK` (the mixed-history reader + the comprehensive resolver + its public entry the confirm-hold re-bases onto).
- [ ] The production consumer is already re-pointed onto the reader (0044-T2 — so the confirm-hold is inherited TRANSITIVELY, this task edits NO consumer): `grep -q "plan = plan_model.read_standing_plan(domain, on_date, root)" scripts/plan/track.py && echo OK` (`resolve_plan_progress:113`).
- [ ] The ADR-0040 pointer MECHANISM is present in its pre-existing shape (this task CONSUMES it unchanged): `grep -q "^def mark_pending" scripts/store/plan_confirm.py && grep -q "^def decision_for" scripts/store/plan_confirm.py && grep -q "^def filter_confirmed" scripts/store/plan_confirm.py && grep -q '_PREFIX_CONFIRM = "plan-confirm::"' scripts/store/plan_confirm.py && echo OK`.
- [ ] The comprehensive resolver does NOT yet honor the confirm skip (this task ADDS it — a clean-add precondition): `grep -q "plan_confirm" scripts/store/plan_model.py && { echo "PRESENT (stop — resolver already consults plan_confirm)"; exit 1; } || echo "ABSENT (add OK)"` (grounded: `plan_model.py`'s imports are `from scripts.store import plan_schema, store` + `from scripts.plan import domain_program` — 0 `plan_confirm` references at entry).
- [ ] Full test suite at the entry baseline floor: `.venv/bin/python -m pytest -q` shows ONLY the two PRE-EXISTING env-sensitive failures — `tests/serve/test_bind.py::test_port_collision_exits_nonzero_fail_loud` and `tests/serve/test_server.py::test_main_reads_no_stdin` (OS-port/stdin cases, NOT this task's to fix). Baseline at authoring (`c1e1f00e`): **2 failed, 2632 passed, 8 skipped**. The gate is **no-NEW-failure**; a 3rd distinct failure is a regression → HALT. Record the exact passed/skipped counts at entry — they are env- and wave-sensitive; do NOT hardcode a number.
- [ ] Working tree clean for the task scope: `git status --short` shows no STAGED changes and no uncommitted changes to `scripts/store/` / `tests/store/`. ("Clean" = no STAGED path + the Create target absent + the frozen numstat=0 below. The basic-memory daemon perpetually re-churns `vault/**/*.md` frontmatter — S90; that churn + any parked `??` files are EXPECTED noise, NOT this task's changes. Do NOT stage them.)
- [ ] On a feature branch cut off `main`: `feature/comprehensive-plan-wave-4` (or equivalent `feature/*`) — never `main`.
- [ ] Rollback target + per-ADR numstat base recorded: `PRE_TASK_HEAD = $(git merge-base origin/main HEAD)` (the DURABLE base == `origin/main` at task entry) — BOTH the `{commit-before-task}` the Rollback Protocol reverts to AND the base the per-ADR numstat probe diffs against. Do NOT hardcode an intermediate branch SHA (the wdhc/SF-1 latent-SHA flaw: a per-task SHA orphans on squash-merge, erroring the numstat test on a fresh clone; do NOT mirror `tests/store/test_plan_model.py:27`, which exhibits exactly that flaw and is tracked for repair by bead `a-plus-maxing-wdhc`). Diffing against `origin/main` is PRE-COMMIT-runnable + durable: the frozen files are EMPTY vs `origin/main` (the wave does not touch them) AND `plan_model.py` is NON-EMPTY vs `origin/main` (the re-base surface changed) — the non-tautological pairing holds against the durable base, and at 0044-T4's build point (before 0043-T3 per RULING A) the `plan_model.py` delta is 0044-T4's own. Because ADR-0044-T2 is IN `origin/main` (this entry HEAD), the working-tree-vs-`PRE_TASK_HEAD` diff isolates ONLY 0044-T4's own delta.
- [ ] The Create target is ABSENT (Create-only, no stale/duplicate artifact): `test -e tests/store/test_plan_model_confirm_hold.py && { echo "PRESENT (stop)"; exit 1; } || echo "ABSENT (Create OK)"`.
- [ ] The FROZEN surfaces byte-frozen at entry (numstat==0 vs `origin/main`): `git diff --numstat origin/main -- scripts/store/store.py scripts/store/keying.py scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/router.py scripts/store/plan_confirm.py` prints nothing (the `<always-frozen>` six + the ADR-0040 pointer mechanism `plan_confirm.py` — AC-4). The resolver rides `store.read` + `plan_confirm.decision_for` unchanged (it writes nothing, edits no mechanism).

## Context Load List

**Files to read (this task's 2-file manifest + the frozen mechanism it consumes + the reader it inherits + the store-adversarial precedent + the ADR-0040 thin-path precedent):**

- `scripts/store/plan_model.py` — the module this task MODIFIES. `read_plan_version(on_date, root)` (`:235`, `resolve_comprehensive(store.read(_PREFIX_MODEL), on_date)` — the comprehensive resolver's public standing entry the confirm-hold re-bases onto; its SOLE caller is `read_standing_plan:275`); `resolve_comprehensive(readings, on_date)` (`:207`, the PURE reversed-scan latest-wins-per-date resolver — do NOT edit, it has no `root` and cannot consult the pointer stream); `read_standing_plan(domain, on_date, root)` (`:248`, 0044-T2's mixed-history reader — comprehensive-wins; **do NOT edit its body — this is 0044-T2's disjoint region; it inherits the hold transitively through `read_plan_version`**); `_PREFIX_MODEL = "plan-model::"` (`:58`); `DOMAIN_PROGRAMS = "domain_programs"` (`:68`, the version's covered-domain map the hold is scoped over) + `VERSION_DATE`/`NARRATIVE`/`MILESTONES`/`MONITORING_CONFIG` (`:67-71`); `NO_PLAN`/`NO_PLAN_TODAY` (`:61-62`). Imports at entry: `from scripts.store import plan_schema, store` (`:53`) — this task ADDS `plan_confirm`.
- `scripts/store/plan_confirm.py` — the ADR-0040 pointer MECHANISM this task CONSUMES UNCHANGED (READ-only; **do NOT edit — AC-4 asserts it byte-frozen**): `decision_for(domain, plan_date, root)` (`:85`, returns the current decision token or `None` — the pure lookup the hold reads per version-domain); `filter_confirmed(readings, domain, root)` (`:102`, the thin-path per-domain pre-filter — the SHAPE to mirror on the comprehensive side); `mark_pending`/`set_decision` (`:44,:61`, the WRITE sinks the TEST seeds through — NOT the resolver); `DECISION_PENDING`/`DECISION_CONFIRMED`/`DECISION_DECLINED` (`:27-29`); `_PREFIX_CONFIRM = "plan-confirm::"` (`:33`) + `_SOURCE = "plan-confirm"` (`:37`). The value is bounded to `{"decision": <token>}` — no plan content in this stream.
- `tests/store/test_plan_model_confirm_hold.py` — the Create target (absent at entry).
- `tests/store/test_plan_model_reader.py` — 0044-T2's reader test, the CLOSEST shape to MIRROR (comprehensive + PRODUCTION-PATH): `_seed_thin(...)` (`:49`); the `record_plan_version` seed + `read_standing_plan`/`read_plan_version` reads + `store.read(plan_model._PREFIX_MODEL)` append-only check (`:81-99`); the `RENDER`-date convention; the two-comprehensive-version same-timepoint case (`:229-233`). Reuse its `_comprehensive_version` import.
- `tests/store/test_plan_model.py` — the fixture builders to REUSE (PF-S131-01): `_training_program(**over)` (`:36`), `_compound_program(**over)` (`:62`), `_comprehensive_version(...)` (`:88`, the real `plan_model` composite spanning ≥2 domains, each a periodized `{"blocks":[{date, phase, detail}]}` seven-field program); the `tmp_path`-root scratch-store pattern; the `PRE_TASK_HEAD` constant + `_numstat`/`_func_src` helpers (`:27,345-359`); `test_no_operator_identity_literal_in_test_tree` (`:481`, the 0-real-PII grep to mirror). Reuse these — do NOT re-invent the composite shape.
- `tests/plan/test_confirm_pointer_skip.py` — the ADR-0040 THIN-path confirm-hold PRECEDENT (READ-ONLY, NOT this task's file): `_seed_held(root, domain, plan, plan_date, specialist)` (`:60`, `record_plan` + `mark_pending` — the exact held-state seeding shape to mirror on the COMPREHENSIVE side, `record_plan_version` + `mark_pending` per domain); `test_read_plan_skips_held_not_walkback` (`:89`, the held→`NO_PLAN_TODAY` assertion pattern); the held/confirmed/no-pointer paired-control discipline (its AC-1/AC-5). READ for the seeding + assertion pattern; do NOT edit it (it guards the THIN path; this task adds the COMPREHENSIVE-path analog in a NEW test module).
- `scripts/plan/track.py` — the PRODUCTION consumer the PF-S130-01 test drives (READ-ONLY — **do NOT edit; 0044-T2 already re-pointed it**): `resolve_plan_progress(domain, on_date, root)` (`:81`) reads `plan = plan_model.read_standing_plan(domain, on_date, root)` (`:113`) → `has_plan = plan["state"] is None` (`:115`) → the return dict `{domain, plan, specialist, plan_date, tracking, has_plan, has_tracking}` (`:116-126`). The real production standing-plan path the held comprehensive version must NOT stand through.
- `scripts/plan/domain_program.py` — the seven-field contract each comprehensive per-domain program conforms to: `validate(program)` (`:134`), `PRESCRIPTION` (`:68`, the periodized `{"blocks":[...]}` field), `REQUIRED_FIELDS` (`:76`). READ-only reuse for the fixture builders (the frozen schema seam — do NOT edit).
- `docs/checklists/store-adversarial-tests.md` — the four required categories AC-4-adversarial embeds (cross-stream namespace collision, same-timepoint dedupe, dedupe-key boundary, mutation-RED), the enforcement points, and the S37–S41 escape pattern this battery moves upstream.
- `docs/build-plan/build-plan-adr-0041-0046.md` — the Wave-4 schedule (`:117-130`) + the Wave 4 → Wave 5 checkpoint (`:235-244`, the confirm-hold-re-base go/no-go that gates THIS task) + the Agent Matrix row (`:182`, `QA` reviewer — "reuses the existing confirm contract → QA only").

**Do NOT load:**
- The ADRs (`docs/adr/ADR-0040-*.md`, `ADR-0044-*.md` …) — the spec already distilled them; re-reading re-opens resolved trade-offs (TP-02 drift).
- The other Wave-4/Wave-5 task blocks + their manifests (the `horizons.py` re-base = 0044-T3; the front-door redefinition + roster growth = 0043-T3; the four-tier executor that CONSUMES this hold at Tier-4 = 0045-T2). This recipe covers ONLY the `### ADR-0044-T4` block (the confirm-hold skip on the comprehensive resolver).
- `scripts/serve/plan_loop.py`'s large-change DETECTION + the `mark_pending` WRITE side (`:203-209`) and `scripts/serve/confirm.py`'s confirm-UX WRITE (`set_decision`) — this task re-bases ONLY the READ-side resolver skip; the write side (who marks pending on a large change, who flips to confirmed) is UNCHANGED and out of scope. The test seeds the held/confirmed states DIRECTLY via `plan_confirm.mark_pending`/`set_decision` (the `_seed_held` pattern), so the loop's detection logic is not needed.
- The frozen primitives beyond the entry-state numstat check: `scripts/store/store.py` / `scripts/store/keying.py` are the read/dedupe CONTRACT the resolver rides (already understood via `plan_model.py`); `pipeline.py` / `adjudicate.py` / `adjust.py` / `router.py` are byte-frozen non-surfaces, not read.

## Risk Pre-Check

**BLOCKING GATE — STORE SURFACE (bead `pka`) + the FROZEN-SPINE ADR-0040-mechanism freeze + the PF-S130-01 tautological-re-base failure class.** Do NOT proceed to GREEN until a real (non-`assert True`), RED-capable test exists for every risk below. The task is NOT complete until the AC-4(d) confirm-hold-removal mutation + the PF-S130-01 filter-revert falsifier are proven RED against the GREEN implementation (Step 2.5 — the confirm-hold logic being mutated does not exist until GREEN). A placeholder test fails this gate. **PF-S132-02:** every mutation/revert below runs in an ISOLATED git worktree (skill `using-git-worktrees`), never a mutate-and-restore of the shared main checkout.

- **Risk (S37–S41 store-keying escape pattern, bead `pka`):** the builder + Tier-2 systematically under-test store cross-stream / same-timepoint-dedupe / dedupe-key cases; five consecutive waves shipped a store-keying escape caught only at Tier-3. The confirm-hold reads TWO streams (`plan-model::` versions + `plan-confirm::<domain>` pointers) and cross-references them per version-domain — a cross-stream mis-read (reading a `plan::` value as a decision, or holding on a domain-pointer OUTSIDE the version's `domain_programs`) is the exact S41 fabricated-value class.
  - Gate condition: RED must include all four adversarial categories as real tests over the confirm-hold-skip surface — (a) cross-stream namespace collision (a `pending` pointer for a domain NOT in the version's `domain_programs` does NOT hold it; a `pending` pointer for a domain THAT IS in the version holds it; the filter reads ONLY `plan-confirm::` decisions via `decision_for`, never a `plan::<domain>` / `plan-model::` value as a decision, AND never an item-`""` SEC-01 decoy), (b) same-timepoint per-pointer resolution (TWO distinct comprehensive versions at the SAME date D — distinct content-tag sources, BOTH persisting — with a pending `plan-confirm::<domain>` pointer keyed at (domain, D): the (domain, D) pointer holds the SHARED timepoint for EVERY same-date version covering that domain, so the reversed-scan latest-wins does not stand the later-appended version while held; confirming releases the shared-timepoint hold and the reversed-scan latest-wins then stands. This exercises the per-`(domain, date)` pointer keying against the reversed-scan over ONE SHARED timepoint — NOT two DIFFERENT dates, which `resolve_comprehensive`'s strict date-equality [`plan_model.py`: `if reading["timepoint"] == on_date`] resolves independently, a near-tautology), (c) dedupe-key boundary + no-new-keying + no-write-primitive (the confirm-hold ADDITION defines 0 new dedupe key AND calls 0 write primitive — it is READ-only over both streams), (d) **mutation — TWO named mutations drive the battery RED, then revert** (Step 2.5): (d-1) the confirm-hold REMOVAL (treat every version as unheld) drives the held-not-standing test (AC-1) RED; (d-2) the confirm-hold OVER-HOLD (treat a `confirmed`-or-no-pointer version AS held) drives the confirmed-stands (AC-2) + no-pointer-stands (AC-3) "0 false holds" tests RED. A battery green under EITHER mutation is tautological and FAILS this gate — (d-1) proves the hold FIRES on a pending pointer, (d-2) proves it does NOT over-fire on a confirmed/default plan.
- **Risk (PF-S130-01 — tautological confirm re-base; the Wave-1 gap Tier-3 caught):** the re-base restores, on the comprehensive branch, the hold that 0044-T2's comprehensive-wins re-point bypassed; a test that passes whether or not the filter was added (e.g. one that seeds ONLY a thin held plan, which the thin `filter_confirmed` already holds pre-T4) encodes broken behavior and tests nothing.
  - Gate condition: AC-1 MUST run the REAL `track.resolve_plan_progress(domain, on_date, root)` over a scratch store carrying a HELD COMPREHENSIVE version (a large-change unit — `record_plan_version` of a ≥3-domain periodized composite dated the render date + `mark_pending` each of its domains at that date), with NO confirmed plan for the domain, so the thing that would stand WITHOUT the re-base is the COMPREHENSIVE version (the comprehensive-wins branch, not the thin filter, is what the test isolates). It resolves NOT-standing: `has_plan is False` (the held comprehensive plan does NOT stand). The **PF-S130-01 revert falsifier** (Step 2.5): remove the confirm-hold filter from the resolver → over the SAME held history the comprehensive-wins branch STANDS the held version → `test_held_comprehensive_not_standing_production_path` goes RED (`has_plan is True`, `plan` carries the comprehensive prescription). A test green under this revert FAILS the AC.
- **Risk (PF-S131-01 — flat-prescription / non-composite fixtures):** a fixture carrying a flat `domain_program` prescription or a hand-built (non-`plan_model`) composite would not be a real large-change comprehensive version, re-opening the PF-S130-01 tautology on the fixture side.
  - Gate condition: every fixture carrying a `domain_program` prescription uses the CANONICAL PERIODIZED shape (`{"blocks":[{date, ...}]}`, `domain_program.validate`-conformant); the held/confirmed/no-pointer comprehensive-version fixtures are the REAL `plan_model` composite (reuse `tests/store/test_plan_model.py`'s `_comprehensive_version` / `_training_program` builders, recorded via `plan_model.record_plan_version`). A flat-payload prescription or a hand-built composite dict FAILS this gate.
- **Risk (ADR-0044-T4 AC-4 — the ADR-0040 pointer MECHANISM must stay byte-unchanged; the re-base is in the RESOLVER):** an edit that reached into `plan_confirm.py` (or the frozen engine) would fork the pointer contract that ADR-0040 / ADR-0045-T2 depend on — the whole point of the re-base is that the mechanism is untouched.
  - Gate condition: RED must include the **per-ADR freeze-break numstat probe** (the 0044-T4 AC — asserts the pointer primitive EMPTY while the re-base surface is NON-empty; maps to the Wave-4 checkpoint "pointer mechanism byte-unchanged", build-plan `:237`): `git diff --numstat PRE_TASK_HEAD -- scripts/store/plan_confirm.py scripts/store/store.py scripts/store/keying.py scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/router.py` == EMPTY (the ADR-0040 mechanism + the `<always-frozen>` six) AND `git diff --numstat PRE_TASK_HEAD -- scripts/store/plan_model.py` == NON-EMPTY (the re-base surface DID change — the non-tautological pairing) AND a grep asserts `plan_confirm.py`'s `mark_pending`/`set_decision`/`decision_for`/`filter_confirmed` + `plan-confirm::` + `_SOURCE` tokens are present. RED-capable: a throwaway edit to `plan_confirm.py` / any frozen file makes the EMPTY-probe RED; an empty `plan_model.py` delta makes the NON-EMPTY-probe RED.
- **Risk (append-only preserved — the store contract; ADR-0044 Consequences-Negative-3):** the resolver must respect append-only immutable history — it reads and resolves, it NEVER rewrites a `plan-model::` version NOR a `plan-confirm::` pointer (a hold that re-recorded state on read would corrupt the audit trail).
  - Gate condition: AC-1 asserts the `plan-model::` stream is BYTE-UNCHANGED after the held read (`store.read(plan_model._PREFIX_MODEL)` line count + values identical pre/post — 0 versions rewritten on read) AND AC-4(c) greps the confirm-hold addition for 0 write-primitive calls (`store.append`/`store.correct`/`mark_pending`/`set_decision`/`record_plan_version`). RED-capable: a resolver that re-recorded on read would change the line count / carry a write call → RED.

## TDD Steps

Single cycle. All acceptance criteria are properties of ONE coherent unit — the comprehensive resolver's confirm-hold skip — exercised over the same seeded scratch-store fixtures (a held / confirmed / no-pointer comprehensive version), so they are not split (task-planning: "all criteria test the same component"). RED writes every AC as a specific failing test BEFORE any GREEN; the category-(d) mutations — (d-1) confirm-hold-removal (AC-1's falsifier) and (d-2) over-hold (AC-2/AC-3's falsifier) — plus the PF-S130-01 filter-revert falsifier run POST-GREEN (Step 2.5), because the confirm-hold logic being mutated does not exist until GREEN.

### Step 1: RED — Write Failing Tests

Call `/write-tests` for `scripts/store/plan_model.py` (the `read_plan_version` confirm-hold re-base), creating `tests/store/test_plan_model_confirm_hold.py`, targeting each spec AC. Mirror `tests/store/test_plan_model.py` (reuse its `_training_program` / `_compound_program` / `_comprehensive_version` builders + `PRE_TASK_HEAD`/`_numstat` helpers — note THIS test computes `PRE_TASK_HEAD` from the DURABLE base (`git merge-base origin/main HEAD`), NOT a hardcoded SHA — do NOT reproduce `test_plan_model.py:27`'s hardcoded-SHA constant (the `wdhc`-tracked latent-SHA flaw that breaks on a fresh clone post-squash)), `tests/store/test_plan_model_reader.py` (the comprehensive + production-path shape), and `tests/plan/test_confirm_pointer_skip.py` (the `_seed_held` held-state seeding pattern → the comprehensive analog `_seed_held_comprehensive`: `record_plan_version(version)` + `mark_pending(domain, plan_date)` for EACH of the version's `domain_programs`). Seed via the `plan_model` / `plan_confirm` APIs under a `tmp_path` root; run each assertion over the REAL read path. ALL fixtures are SYNTHETIC (0 real operator PII, 0 live-API), on scratch stores.

**Shared fixtures:** a large-change held comprehensive version = `_comprehensive_version(date=RENDER, ...)` covering ≥3 of `PLAN_DOMAINS` (`workout`/`nutrition`/`supplements`/`peptides` — ≥3 clears `LARGE_CHANGE_THRESHOLD_DOMAINS=3`), each domain a periodized-prescription seven-field program (PF-S131-01), recorded via `plan_model.record_plan_version`; the held state via `mark_pending(domain, RENDER, root)` for each covered domain; the confirmed state by additionally `plan_confirm.set_decision(domain, RENDER, DECISION_CONFIRMED, root)` for EACH covered domain; the no-pointer state by recording the version and marking NOTHING.

- **AC-1 — Unconfirmed large-change comprehensive plan → NOT standing (falsification #3, resolver-level):** `test_held_comprehensive_does_not_stand` — record a large-change comprehensive version dated `RENDER` covering ≥3 domains; `mark_pending` each covered domain at `RENDER`; the REAL comprehensive resolver `read_plan_version(RENDER, root)` resolves NOT-standing (`state` is NOT `None` — a `NO_PLAN`/`NO_PLAN_TODAY` absence, never the held version); AND `read_standing_plan(domain, RENDER, root)` (for a covered domain) does NOT return the held comprehensive prescription as standing (`state` is not `None` on that comprehensive branch — it falls through to the thin path). AND — append-only preserved — the `plan-model::` stream is BYTE-UNCHANGED after the read (`store.read(_PREFIX_MODEL)` line count identical pre/post, 0 versions rewritten). RED-capable: the (d) confirm-hold-removal mutation (Step 2.5) turns it RED (the held version stands); a resolver that re-recorded on read would change the line count → RED. If it STANDS pre-confirm → RED (this IS the falsifier).
- **AC-1-prod — PRODUCTION-PATH, held comprehensive not standing (PF-S130-01 load-bearing):** `test_held_comprehensive_not_standing_production_path` — seed a HELD comprehensive version dated `RENDER` covering `workout` (+ ≥2 more), `mark_pending` each covered domain, with NO thin `plan::workout` reading (comprehensive-only, so the comprehensive-wins branch is what would stand WITHOUT the re-base); call the REAL `track.resolve_plan_progress("workout", RENDER, root)`; assert `result["has_plan"] is False` (the held comprehensive plan does NOT stand) AND the return dict still carries every KEY (`domain`, `plan`, `specialist`, `plan_date`, `tracking`, `has_plan`, `has_tracking`). Step-2.5 filter-revert turns it RED (the reverted resolver stands the held version → `has_plan True`, `plan` = the comprehensive prescription).
- **AC-2 — Confirmed comprehensive plan STANDS (0 false holds):** `test_confirmed_comprehensive_stands` — record the same large-change comprehensive version dated `RENDER`; `mark_pending` then `set_decision(…, DECISION_CONFIRMED, …)` for EACH covered domain (the whole swap confirmed as a unit); `read_plan_version(RENDER, root)` resolves STANDING (`state is None`, `version` carrying the composite); `read_standing_plan(domain, RENDER, root)` returns the comprehensive prescription (`domain_program.PRESCRIPTION`, the `{"blocks":[...]}`); the hold RELEASES on confirm — 0 false holds on a confirmed plan. RED-capable: the Step-2.5 (d-2) OVER-HOLD mutation (invert the filter to treat a `confirmed`-or-no-pointer version AS held) makes this confirmed version resolve `NO_PLAN_TODAY` (a false hold) → RED. This is AC-2's dedicated falsifier — the confirm-hold-removal / filter-revert mutations do NOT red it (a confirmed version stands with or without the hold), so without the over-hold mutation AC-2 has no RED-capability.
- **AC-3 — No-pointer default STANDS (no default hold):** `test_no_pointer_comprehensive_stands` — record a comprehensive version dated `RENDER` (a non-large-change re-gen) with NO `mark_pending` on any domain; `read_plan_version(RENDER, root)` resolves STANDING (`state is None`, `version` present); `read_standing_plan(domain, RENDER, root)` returns the comprehensive prescription. The hold fires ONLY on a pending-pointer domain, never a default hold. RED-capable: the Step-2.5 (d-2) OVER-HOLD mutation (treat a `confirmed`-or-no-pointer version AS held) makes this no-pointer version resolve `NO_PLAN_TODAY` (a false default hold) → RED. This is AC-3's dedicated falsifier — like AC-2, the confirm-hold-removal / filter-revert mutations do NOT red it (a no-pointer version stands with or without the hold), so the over-hold mutation is what proves AC-3 non-vacuous.
- **AC-4 — Pointer MECHANISM byte-unchanged (grep + numstat, spec AC-4):** `test_confirm_mechanism_byte_unchanged` — (i) subprocess `git diff --numstat PRE_TASK_HEAD -- scripts/store/plan_confirm.py` is EMPTY (the ADR-0040 mechanism untouched) AND `git diff --numstat PRE_TASK_HEAD -- scripts/store/plan_model.py` is NON-EMPTY (the re-base surface DID change — the non-tautological pairing the 0044-T4 AC names); (ii) a grep over `plan_confirm.py` asserts `mark_pending`/`set_decision`/`decision_for`/`filter_confirmed` + `_PREFIX_CONFIRM = "plan-confirm::"` + `_SOURCE = "plan-confirm"` are present (the pointer stream + sinks intact). Mirror `test_plan_model.py:345`'s `_numstat` helper + the `PRE_TASK_HEAD` constant. RED-capable via the AC-FS forbidden-file mutation (Step 1-continued).
- **AC-4-FS — Frozen-spine numstat (the `<always-frozen>` six, added gate; maps to build-plan Wave-4 `<always-frozen> numstat=0`, `:130`):** `test_frozen_spine_numstat` — `git diff --numstat origin/main -- scripts/store/store.py scripts/store/keying.py scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/router.py scripts/store/plan_confirm.py` is EMPTY (the six + the ADR-0040 mechanism vs the pre-build trunk). RED-capable: a throwaway edit to any of these makes the probe RED.
- **AC-5 — Suite-local green + 0 real PII (spec AC-5):** `.venv/bin/python -m pytest tests/store/test_plan_model_confirm_hold.py` passes against scratch stores — 0 live-API calls, 0 real operator PII (verified by the Step-1→Step-2 RED→GREEN transition + a `grep` that the test tree carries no real operator-identity literal, mirror `test_plan_model.py::test_no_operator_identity_literal_in_test_tree`).
- **AC-4-adversarial — Store-adversarial battery (all four, per `docs/checklists/store-adversarial-tests.md`, over the confirm-hold-skip surface):**
  - (a) **cross-stream namespace collision:** `test_confirm_hold_cross_stream_disjoint` — a `pending` pointer for a domain NOT in the version's `domain_programs` (record a version covering `workout`+`nutrition`+`supplements`; `mark_pending("peptides", RENDER, root)` only) does NOT hold the version (it STANDS — `peptides` is not its domain); a `pending` pointer for a domain THAT IS in the version (`mark_pending("workout", …)`) holds it (does not stand); AND the confirm-hold reads ONLY `plan-confirm::` decisions via `decision_for`, never a `plan::<domain>` value (seed a distinct `plan::workout` thin reading dated `RENDER` — the hold decision is unaffected by its VALUE) NOR an item-`""` SEC-01 decoy (mirror `test_plan_model.py::test_cross_stream_disjoint`'s item-`""` decoy seeded dated `RENDER` — the version resolves on its real pointers only).
  - (b) **same-timepoint per-pointer resolution:** `test_confirm_hold_same_timepoint` — seed TWO DISTINCT comprehensive versions at the SAME date D (distinct content-tag sources, e.g. via distinct narratives — mirror `test_plan_model.py::test_same_timepoint_two_versions_persist`), appended in order [A, B] (B last), BOTH covering ≥3 domains incl. `workout`; assert BOTH persist (`store.read(_PREFIX_MODEL)` returns 2, distinct sources). `mark_pending` each covered domain at D (the (workout, D)/(nutrition, D)/(supplements, D) pointers). Assert `read_plan_version(D, root)` resolves NOT-standing (`state` is not `None`): the (domain, D) pending pointers hold the SHARED timepoint, so BOTH same-date versions are dropped — the reversed-scan latest-wins does NOT stand the later-appended B just because it is newest; a per-`(domain, date)` pointer applies to EVERY version at D covering that domain. Then `set_decision(…, DECISION_CONFIRMED, …)` each covered domain at D → `read_plan_version(D, root)` resolves STANDING with `version == B`'s composite (the shared-timepoint hold RELEASES and the reversed-scan latest-wins picks the last-appended same-date version). This is the SAME-timepoint keying interplay (one shared date D, two versions, one (domain, D) pointer-set) — NOT the trivially-independent two-DIFFERENT-dates case `resolve_comprehensive`'s strict date-equality resolves per date (the near-tautology this replaces). RED-capable: the (d-1) confirm-hold-removal mutation stands B while the pointers are pending → the NOT-standing assertion REDs; the (d-2) over-hold mutation drops B even after confirm → the STANDING/`version == B` assertion REDs.
  - (c) **dedupe-key boundary + no-new-keying + no-write-primitive:** `test_confirm_hold_no_new_keying` — a scoped read over `plan_model.py`'s confirm-hold ADDITION (the new filter helper + the `read_plan_version` edit — NOT the whole module, which legitimately contains `record_plan_version`/`store.append`) asserts (i) 0 new dedupe-key / keying logic (no `DEDUPE_FIELDS`/`dedupe_key`/`LINE_FIELDS`/`_DEDUPE_EXCLUDED` redefinition; identity stays `keying`'s frozen `(item, timepoint, source)`, reached only through `store.read`), AND (ii) 0 write-primitive CALLS — the confirm-hold body invokes NO `store.append(`/`store.correct(`/`mark_pending(`/`set_decision(`/`record_plan_version(` (it is READ-only: `store.read` + `plan_confirm.decision_for`). Both counts == 0.
  - (d) **mutation-style (run RED, observe, revert — Step 2.5, in an isolated worktree):** TWO named mutations, each with the battery test it turns RED. (d-1) confirm-hold REMOVAL — always treat every version as unheld → `test_held_comprehensive_does_not_stand` (AC-1) goes RED (the held version stands). (d-2) confirm-hold OVER-HOLD — invert the filter to treat a `confirmed`-or-no-pointer version AS held → `test_confirmed_comprehensive_stands` (AC-2) + `test_no_pointer_comprehensive_stands` (AC-3) go RED (the "0 false holds" controls red on an over-firing hold). Both directions are required: (d-1) proves the hold FIRES (AC-1's falsifier), (d-2) proves it does NOT over-fire (AC-2/AC-3's falsifier — without it those two controls have NO RED-capability, since a confirmed/no-pointer version stands with or without the filter). Re-run each, CONFIRM RED, revert. A battery green under EITHER mutation is tautological and FAILS AC-4.

Run: `.venv/bin/python -m pytest tests/store/test_plan_model_confirm_hold.py -q`
Expected: FAIL — the confirm-hold does not exist yet, so a held comprehensive version STANDS (`read_plan_version`/`read_standing_plan` resolve it standing; `resolve_plan_progress` returns `has_plan True`), so AC-1/AC-1-prod fail. (AC-2/AC-3 may PASS at this stage — a no-pointer/confirmed version already stands under the un-re-based resolver; that is expected and NOT a wrong test. Their RED-capability is proven SPECIFICALLY by the Step-2.5 (d-2) OVER-HOLD mutation — invert the filter to treat a `confirmed`-or-no-pointer version AS held, and AC-2/AC-3 go RED (a false hold on a plan that should stand). The (d-1) confirm-hold-removal + the PF-S130-01 filter-revert mutations red ONLY AC-1/AC-1-prod (an unheld version stands under both), so they do NOT establish AC-2/AC-3's non-vacuity — the over-hold mutation is what does, and it is a REQUIRED Step-2.5 falsifier, not optional. The AC-4-FS/AC-4 numstat probes are `PRE_TASK_HEAD`-based: the `plan_confirm.py`-EMPTY leg is GREEN at entry, the `plan_model.py`-NON-EMPTY leg is RED until GREEN lands the edit — its RED-capability is the edit itself; re-confirmed at Step 4.)

### Step 1 (continued): RED-capability falsification (NON-SKIPPABLE — in an isolated worktree, PF-S132-02)

Before AND after implementing, prove per-assertion RED-capability (a coincidence can make a test pass for the wrong reason). Perform each mutation in an ISOLATED git worktree (skill `using-git-worktrees`), observe, discard the worktree — NEVER mutate-and-restore the shared main checkout:

1. **AC-1-prod filter-revert REDs (PF-S130-01, the load-bearing proof):** after GREEN, remove the confirm-hold filter from `read_plan_version`; confirm `test_held_comprehensive_not_standing_production_path` goes RED (the comprehensive-wins branch stands the held version → `has_plan True`); discard.
2. **AC-4(d-1) confirm-hold-removal mutation (the hold FIRES):** after GREEN, invert/remove the confirm-hold filter; confirm `test_held_comprehensive_does_not_stand` goes RED; discard.
3. **AC-4(d-2) OVER-HOLD mutation (the hold does NOT over-fire — AC-2/AC-3's dedicated falsifier):** after GREEN, invert the filter to treat a `confirmed`-or-no-pointer version AS held (a false hold); confirm BOTH `test_confirmed_comprehensive_stands` (AC-2) AND `test_no_pointer_comprehensive_stands` (AC-3) go RED (each resolves `NO_PLAN_TODAY` when it should stand); discard. This is REQUIRED, not optional: the (d-1)/PF-S130-01 mutations red only AC-1 (an unheld version stands under both directions), so AC-2/AC-3's "0 false holds" controls have NO RED-capability without this over-hold falsifier.
4. **AC-4/AC-4-FS forbidden-file mutation:** make a throwaway one-line edit to `scripts/store/plan_confirm.py` (a comment) → confirm `test_confirm_mechanism_byte_unchanged` (the `plan_confirm.py`-EMPTY leg) AND `test_frozen_spine_numstat` go RED; discard. Proves the mechanism-freeze probe bites.

This is a mandatory gate of THIS recipe — do not silently delegate RED-capability to `/write-tests`.

### Step 2: GREEN — Implement

Changes to make (behavioral descriptions, NOT literal code):

- `scripts/store/plan_model.py` (Modify — the confirm-hold-skip region, DISJOINT from 0044-T2's `read_standing_plan` body; do NOT edit `resolve_comprehensive` or `read_standing_plan`):
  - **Re-base the confirm-hold onto the comprehensive resolver (satisfies AC-1/AC-2/AC-3):** add a private confirm-hold filter (e.g. `_standing_versions(readings, root)` / `_drop_held(...)`) and apply it inside `read_plan_version(on_date, root)` (`:235`) BEFORE `resolve_comprehensive` — i.e. `resolve_comprehensive(<filter>(store.read(_PREFIX_MODEL, root=root), root), on_date)`. The filter drops each `plan-model::` reading whose `timepoint` carries a NON-confirmed (`pending`/`declined`) `plan-confirm::<domain>` pointer for ANY domain in that reading's `value[DOMAIN_PROGRAMS]` — consulting `plan_confirm.decision_for(domain, reading["timepoint"], root)` (the UNCHANGED mechanism): a reading stands only when EVERY covered domain reads `None` (no pointer) or `DECISION_CONFIRMED`; a version with any pending/declined covered-domain pointer is HELD (dropped, so it does not stand — the whole large-change swap is held as a unit, OQ-3). This MIRRORS the thin path (`plan_schema.read_plan` applies `plan_confirm.filter_confirmed` before `resolve_plan`) generalized from one domain to a composite version's domain set. `resolve_comprehensive` stays PURE/UNCHANGED (it never sees the held readings); `read_standing_plan` is UNEDITED — it calls `read_plan_version`, so it inherits the hold TRANSITIVELY (a held comprehensive version resolves `NO_PLAN_TODAY`/`NO_PLAN`, so its comprehensive-wins branch short-circuits and it falls through to the thin path). Add `from scripts.store import plan_confirm` to the import block (`:53`) — ACYCLIC (`plan_confirm` imports only `store`, never `plan_model`). The filter WRITES nothing, REWRITES nothing (append-only preserved), and defines NO store key (identity reached only through `store.read`); `plan_confirm.py` is NOT edited (AC-4).

Run: `.venv/bin/python -m pytest tests/store/test_plan_model_confirm_hold.py -q`
Expected: PASS (the `plan_model.py`-NON-EMPTY numstat leg is now GREEN — the edit landed; the `plan_confirm.py`-EMPTY leg stays GREEN — the mechanism is untouched).

### Step 2.5: Falsify the store-adversarial battery + PF-S130-01 (POST-GREEN mutation-RED / revert — isolated worktree, PF-S132-02)

The confirm-hold logic the falsifiers target now EXISTS (GREEN landed), so they are meaningful here (at Step 1 there is no filter to mutate). For EACH: apply IN AN ISOLATED WORKTREE, run the named test, OBSERVE RED, then DISCARD the worktree (never committed to the feature branch). A test that stays green under its falsifier is tautological and FAILS its AC.

- **(d-1) — remove the confirm-hold rule (the hold FIRES):** in `read_plan_version`, drop/invert the confirm-hold filter (always resolve over the unfiltered readings) → `test_held_comprehensive_does_not_stand` (AC-1) goes RED (the held version stands). Re-run, CONFIRM RED, discard.
- **(d-2) — OVER-HOLD (the hold does NOT over-fire — AC-2/AC-3's falsifier):** in `read_plan_version`, invert the filter to treat a `confirmed`-or-no-pointer version AS held (drop a version whose covered domains are all `confirmed`/no-pointer) → `test_confirmed_comprehensive_stands` (AC-2) AND `test_no_pointer_comprehensive_stands` (AC-3) both go RED (each resolves `NO_PLAN_TODAY` when it should stand — a false hold). Re-run, CONFIRM RED, discard. Without this mutation AC-2/AC-3 are unfalsified: the (d-1)/(pf-s130) reverts leave them green (a confirmed/no-pointer version stands with or without the filter), so this is the "0 false holds" controls' REQUIRED RED-capability proof.
- **(pf-s130) — revert the filter re-point:** in `read_plan_version`, restore the pre-re-base `resolve_comprehensive(store.read(_PREFIX_MODEL, root=root), on_date)` (no filter) → `test_held_comprehensive_not_standing_production_path` (AC-1-prod: the comprehensive-wins branch stands the held version → `has_plan True`) goes RED — proving the re-base is non-tautological (it distinguishes the held-comprehensive-not-standing behavior from the pre-T4 stands). Discard.

Run in the main checkout after confirming the worktrees are discarded: `.venv/bin/python -m pytest tests/store/test_plan_model_confirm_hold.py -q`
Expected: PASS (no falsifier edit in the main checkout's working tree). Confirm NO mutation residue before Step 3.

### Step 3: REFACTOR

Review for:
- Duplication between the new confirm-hold filter and `plan_confirm.filter_confirmed`/`resolve_comprehensive` — the filter COMPOSES `plan_confirm.decision_for` (the per-domain lookup) generalized over the version's domain set; it does NOT re-implement the pointer resolution NOR the reversed-scan (`resolve_comprehensive` unchanged, `plan_confirm` unchanged). No second copy of the decision logic.
- `read_standing_plan` (0044-T2's region) is BYTE-UNEDITED — confirm no incidental change leaked into it (it inherits the hold via `read_plan_version` only); its signature + the comprehensive-wins precedence are untouched (Interface Contract).
- No local `keying` redefinition; the filter appends nothing and defines no key (the store-keying escape vector).
- The import direction stays acyclic (`plan_model` imports `plan_confirm`; `plan_confirm` imports only `store`).
- Remove any Step-1/Step-2.5 temporary stub/mutation residue.

Run: `.venv/bin/python -m pytest tests/store/test_plan_model_confirm_hold.py tests/store/test_plan_model_reader.py tests/store/test_plan_model.py tests/plan/test_confirm_pointer_skip.py -q`
Expected: PASS (the new confirm-hold module + 0044-T2's reader + the T1 model suite + the ADR-0040 THIN-path guard all green; no behavior change — the thin-path confirm-hold is untouched, the reader's no-pointer versions still stand).

### Step 4: REGRESSION

Run: `.venv/bin/python -m pytest -q`
Expected: PASS at the entry baseline floor — the SAME two pre-existing env-sensitive failures (`test_bind`/`test_server`) + the entry skipped count, PLUS the new `test_plan_model_confirm_hold.py` tests passing, with NO new failure. Specifically confirm the ADR-0040 THIN-path guard + 0044-T2's reader stay GREEN (the re-base did not loosen the thin hold nor the no-pointer standing): `.venv/bin/python -m pytest tests/plan/test_confirm_pointer_skip.py tests/store/test_plan_model_reader.py tests/runner/test_cadence_runner.py::test_frozen_glob_numstat_empty -q` passes (the cadence recursive `scripts/store/**` glob still freezes `plan_confirm.py`; `plan_model.py` stays excluded, so the resolver edit is unfrozen there). A 3rd unexpected failure means the filter reached outside the manifest or a shared-fixture touch had a side effect — do NOT fix unrelated tests; investigate whether the change reached outside the manifest and flag per the feedback protocol.

Then re-check the frozen spine + the per-ADR numstat (based on `PRE_TASK_HEAD`, the fixed entry HEAD — pre-commit-runnable, re-confirmed here against the final working tree/commit):
- `git diff --numstat PRE_TASK_HEAD -- scripts/store/plan_confirm.py scripts/store/store.py scripts/store/keying.py scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/router.py` → empty (the ADR-0040 mechanism + the `<always-frozen>` six untouched; AC-4 + AC-4-FS).
- `git diff --numstat PRE_TASK_HEAD -- scripts/store/plan_model.py` → NON-empty (the re-base surface changed — the non-tautological pairing).
- `git diff --numstat origin/main -- scripts/store/store.py scripts/store/keying.py scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/router.py scripts/store/plan_confirm.py` → empty (the six + the mechanism vs the pre-build trunk).
- `git diff --name-only PRE_TASK_HEAD -- scripts/ tests/` → exactly `scripts/store/plan_model.py`, `tests/store/test_plan_model_confirm_hold.py` (no scope creep — this task edits no consumer, no guard, no mechanism).

## Interface Contracts

This task RE-BASES the standing behavior of one existing seam and CONSUMES one frozen mechanism; it does NOT change any signature.

- `plan_model.read_plan_version(on_date, root) -> dict` — SIGNATURE UNCHANGED. BEHAVIOR re-based: it now honors the ADR-0040 confirm skip — a `plan-model::` version whose date carries a non-`confirmed` (`pending`/`declined`) pointer for ANY of its covered domains resolves NOT-standing (`NO_PLAN_TODAY`/`NO_PLAN`); a version with all-`confirmed`-or-no-pointer domains stands. Its SOLE caller is `read_standing_plan`; no consumer needs the raw (unfiltered) held version (grounded: `read_plan_version`'s only production caller is `read_standing_plan:275`; `confirm.py`/`plan_loop.py` do not read the comprehensive version). Consumed (transitively) by every `read_standing_plan` / `track.resolve_plan_progress` consumer.
- `plan_model.read_standing_plan(domain, on_date, root) -> dict` — SIGNATURE + comprehensive-wins PRECEDENCE UNCHANGED (0044-T2's region — this task does NOT edit its body). It inherits the hold TRANSITIVELY: a held comprehensive version resolves absent through `read_plan_version`, so the comprehensive-wins branch short-circuits and it falls through to the thin path (`plan_schema.read_plan`, which applies its own `filter_confirmed`).
- `plan_model.resolve_comprehensive(readings, on_date)` — UNCHANGED (PURE; the filter runs UPSTREAM of it in `read_plan_version`, mirroring how the thin `resolve_plan` body stays byte-frozen while `read_plan` applies `filter_confirmed`).
- `plan_confirm.{mark_pending,set_decision,decision_for,filter_confirmed}` + the `plan-confirm::` pointer stream — CONSUMED UNCHANGED (byte-frozen, AC-4). The re-base reads `decision_for`; it writes to neither the pointer stream nor the version stream.
- **Consumed by ADR-0045-T2** (Wave 5) — the four-tier executor's **Tier-4 human-gate REUSES this re-based ADR-0040 confirm hold** (`ADR-0044-T4 --> ADR-0045-T2`, spec `:426`; build-plan `:143`). The confirm-hold is the standing-resolution semantics the Tier-4 gate depends on; the plan-loop/dashboard read it transitively via `track.resolve_plan_progress`.
- **Change control:** ADR-0045-T2 MUST NOT change the confirm-hold semantics (held → not standing; confirmed/no-pointer → standing), the `read_plan_version`/`read_standing_plan` signatures, or the `plan_confirm` mechanism without QA review (this task carries QA — the store read surface + the append-only-preserving read contract + the byte-frozen ADR-0040 mechanism).

## Verification Checklist

- [ ] AC-1: an unconfirmed large-change comprehensive plan (pending `plan-confirm::` pointer on ≥3 covered domains) does NOT stand — `read_plan_version`/`read_standing_plan` resolve it absent, `plan-model::` stream byte-unchanged (0 versions rewritten) — `test_held_comprehensive_does_not_stand`; Step-2.5 (d) confirm-hold-removal turns it RED, discarded.
- [ ] AC-1-prod (PF-S130-01): the REAL `track.resolve_plan_progress` over a HELD comprehensive-only version returns `has_plan False` (not the held prescription), all return-dict keys intact, via the production path — `test_held_comprehensive_not_standing_production_path`; Step-2.5 filter-revert turns it RED (non-tautology), discarded.
- [ ] AC-2: a confirmed large-change comprehensive plan (all covered domains `DECISION_CONFIRMED`) STANDS — `read_plan_version` `state is None` + `read_standing_plan` returns the comprehensive prescription; 0 false holds on a confirmed plan — `test_confirmed_comprehensive_stands`; Step-2.5 (d-2) OVER-HOLD mutation turns it RED (its dedicated falsifier — the removal/revert mutations do not), discarded.
- [ ] AC-3: a comprehensive plan with NO confirm pointer STANDS (no default hold — the hold fires only on a pending-pointer domain) — `test_no_pointer_comprehensive_stands`; Step-2.5 (d-2) OVER-HOLD mutation turns it RED (its dedicated falsifier), discarded.
- [ ] AC-4: the ADR-0040 pointer MECHANISM `plan_confirm.py` is byte-unchanged (`numstat PRE_TASK_HEAD == EMPTY`) WHILE the re-base surface `plan_model.py` is NON-empty (the non-tautological pairing) + a grep confirms `mark_pending`/`set_decision`/`decision_for`/`filter_confirmed` + `plan-confirm::` + `_SOURCE` present — `test_confirm_mechanism_byte_unchanged`; RED under the AC-4 forbidden-file mutation, discarded.
- [ ] AC-4-FS (added FROZEN-SPINE gate): the `<always-frozen>` six + `plan_confirm.py` == 0 vs `origin/main` — `test_frozen_spine_numstat`. Maps to the build-plan Wave-4 `<always-frozen> numstat=0` checkpoint (`:130`).
- [ ] AC-5 (spec AC-5): `.venv/bin/python -m pytest tests/store/test_plan_model_confirm_hold.py` passes (0 live-API, 0 real PII — a `grep` confirms no real operator-identity literal in the test tree).
- [ ] AC-4-adversarial: four-part store-adversarial battery over the confirm-hold surface — (a) cross-stream disjoint (a pointer for a domain OUTSIDE the version does not hold it; a covered-domain pointer does; reads only `plan-confirm::` decisions, never a `plan::`/`plan-model::` value nor the item-`""` decoy), (b) same-timepoint per-pointer resolution — TWO distinct versions at ONE shared date D both persist; the (domain, D) pointer holds BOTH while pending, and on confirm the reversed-scan latest-wins stands the last-appended (NOT the D/D′ two-different-dates near-tautology), (c) 0 new keying + 0 write-primitive call in the confirm-hold addition (grep), (d) run RED under the TWO NAMED mutations — (d-1) confirm-hold removal reds AC-1, (d-2) over-hold reds AC-2/AC-3 — then discarded — `test_confirm_hold_cross_stream_disjoint` + `test_confirm_hold_same_timepoint` + `test_confirm_hold_no_new_keying` + the documented (d-1)/(d-2) procedure.
- [ ] Risk (S37–S41 store-keying escape, bead `pka`) covered by AC-4-adversarial (all four, (d) run RED); Risk (PF-S130-01 tautological re-base) covered by AC-1-prod production-path + the filter-revert falsifier; Risk (PF-S131-01 fixture shape) covered by the reused `_comprehensive_version` periodized builders; Risk (AC-4 mechanism-freeze) covered by AC-4 + AC-4-FS; Risk (append-only preserved) covered by AC-1's 0-versions-rewritten assertion.
- [ ] PF-S132-02: every mutation/revert step (Step 1-continued + Step 2.5) ran in an ISOLATED git worktree, discarded — the shared main checkout was never mutate-and-restored; the final main-checkout working tree carries NO falsifier residue.
- [ ] No regression: `.venv/bin/python -m pytest -q` holds the entry floor (the two known env-reds, no 3rd) with the new tests passing; `test_confirm_pointer_skip.py` (thin path) + `test_plan_model_reader.py` (0044-T2) + `test_cadence_runner.py::test_frozen_glob_numstat_empty` GREEN.
- [ ] Files modified match the 2-file manifest (`scripts/store/plan_model.py`, `tests/store/test_plan_model_confirm_hold.py`) — NO consumer edit (`track.py` untouched — 0044-T2 already re-pointed it), NO guard edit (no carve-out — `plan_model.py` is unfrozen), NO mechanism edit (`plan_confirm.py` byte-frozen); any Step-2.5 falsifier edit was in a discarded worktree, never committed.
- [ ] Interface contract documented (the `read_plan_version` behavior re-base + the transitive inheritance to `read_standing_plan`/`track` + `resolve_comprehensive`/`plan_confirm` unchanged + the ADR-0045-T2 Tier-4 consumer).

## Deviations

| # | Spec Says | Recipe Says | Reason | Impact |
|---|----------|------------|--------|--------|
| 1 | AC-1: "the comprehensive resolver does NOT stand" a held large-change plan; the HELD-predicate for a multi-domain composite version (the pointer stream is per-`(domain, plan_date)`) is unspecified. | A comprehensive version STANDS only when NONE of its covered domains carries a non-`confirmed` (`pending`/`declined`) `plan-confirm::` pointer at the version's date; a version with ANY pending/declined covered-domain pointer is HELD (does not stand). | SE Ask-vs-Proceed rule 3 (same public interface, pick the simpler internal behavior, state the assumption): grounded in the WRITE side — `plan_loop.regenerate` marks EVERY promoted domain `pending` as a unit ("the whole materially-large swap is held as a unit", `plan_loop.py:204-209`), and the confirm UX confirms the swap as a unit (`confirm.py` flips per-domain but over the whole advisory). "Any covered-domain pending → held" is the faithful generalization of the thin `filter_confirmed` (one domain) to the composite. No AC tests a competing predicate. | AC-2 seeds ALL covered domains `confirmed`; AC-1 fires on any pending. Consistent with the thin-path unit-hold; ADR-0045-T2's Tier-4 reuses this exact semantics. |
| 2 | ADR-0044-T4 manifest: "`scripts/store/plan_model.py` (Modify) … Disjoint from ADR-0044-T2's reader region." The exact FUNCTION the filter attaches to is unspecified. | The confirm-hold filter attaches at `read_plan_version` (`:235`) + a new private helper, NOT `read_standing_plan` (0044-T2's region). `read_standing_plan` inherits the hold transitively (it calls `read_plan_version`). | `read_plan_version`'s SOLE caller is `read_standing_plan` (grounded grep); no consumer needs the raw held version; attaching here (a) keeps the edit DISJOINT from 0044-T2's `read_standing_plan` body (the spec's disjointness constraint), (b) mirrors the thin path exactly (`read_plan` filters via `filter_confirmed` before `resolve_plan`), (c) keeps `resolve_comprehensive` PURE. An executor may instead add a standing-only wrapper, but MUST keep `resolve_comprehensive` + `plan_confirm` byte-unchanged and stay disjoint from `read_standing_plan`. | No downstream contract change; `read_standing_plan`/`track` inherit the hold with 0 edits. |
| 3 | Brief-suggested commit subject: `feat(store): re-base confirm-pointer on comprehensive resolver` (62 chars). | `feat(store): re-base confirm-hold on comprehensive` (50 chars). | The project convention (CLAUDE.md + template Commit Protocol) caps the subject at ≤50 chars; the suggested subject is 62. The trim drops the trailing "resolver" (the scope `store` + "comprehensive [resolver]" is unambiguous) and "pointer"→"hold" (the hold IS the re-based thing; the pointer MECHANISM is explicitly UNCHANGED). | Cosmetic; the body carries the full description. |

**UPSTREAM FLAG: SPEC — imprecise citation (NON-BLOCKING)**
- Source: `docs/spec/adr-0041-0046-comprehensive-plan-spec.md` `:278` (the ADR-0044-T4 `plan_model.py` manifest note) + the file-manifest row `:50`.
- Issue: the note describes the re-base as "re-basing the current thin-reader skip that reads `plan_confirm`'s `plan-confirm::<domain>` pending pointers, `plan_loop.py:198-208`", but `plan_loop.py:198-208` is the WRITE side (the `mark_pending` hold-trigger on a large change), NOT a "thin-reader skip" — the actual read-side skip is `plan_schema.read_plan`'s `filter_confirmed` application (`plan_schema.py:530`). The comprehensive resolver has NO read-side skip today (the gap this task closes).
- Impact on recipe: none for correctness — the recipe grounds the re-base against the LIVE tree (the read-side skip = `filter_confirmed` in `read_plan`; the re-base target = `read_plan_version`), not the note's line pointer. But the citation could mislead an executor into editing `plan_loop.py:198-208` (OUTSIDE the 2-file manifest) — the recipe pre-empts this by excluding `plan_loop.py`'s write side in "Do NOT load" and pinning the manifest to `plan_model.py` + its test.
- Recommended action: reword the note to cite `plan_schema.read_plan`'s `filter_confirmed` (`:530`) as the read-side skip precedent and keep `plan_loop.py:198-208` labelled as the WRITE-side hold-trigger the resolver reads the pointers OF.
- Blocking: No.

## Commit

```
feat(store): re-base confirm-hold on comprehensive
```
Stage only: `scripts/store/plan_model.py`, `tests/store/test_plan_model_confirm_hold.py` (this task edits NO consumer — `track.py` was re-pointed by 0044-T2 — NO guard — `plan_model.py` is unfrozen — and NO mechanism — `plan_confirm.py` is byte-frozen, AC-4).

(Stage with `git add <the two paths>` as its OWN command, then `git commit` separately — the PII commit hook denies single-call stage+commit, `git commit -a`, and pathspec `git commit <path>`. Confirm NO Step-2.5 falsifier edit remains (all were in discarded worktrees — PF-S132-02), NO daemon vault-churn / parked `??` file is staged, and `plan_confirm.py`/`store.py`/`keying.py`/`pipeline.py`/`adjudicate.py`/`adjust.py`/`router.py` are byte-unchanged, before committing. The per-ADR numstat probes (`git diff --numstat PRE_TASK_HEAD …`) are pre-commit-runnable and re-confirmed against the final commit — `plan_confirm.py` EMPTY, `plan_model.py` NON-empty.)

## Rollback

If verification fails after 3 fix cycles:
1. `git stash -m "failed-ADR-0044-T4-attempt-{N}"` — preserve the work for diagnosis.
2. `git checkout "$(git merge-base origin/main HEAD)"` (== the durable `PRE_TASK_HEAD` recorded in Entry State — NOT a hardcoded per-task SHA, which would drop later docs amendments on a stray checkout) — revert to the entry-state base. (Any mutation/revert falsifier lived in a DISCARDED worktree, not the main checkout — nothing to un-revert here, PF-S132-02.)
3. Update the issue tracker: flag ADR-0044-T4 as BLOCKED with the failure log.
4. Escalate: "Task ADR-0044-T4 failed verification after 3 attempts. Stash ref: {ref}. Failure log: {summary}." ADR-0044-T4 is a zero-slack critical-path task on the model-resolver line (`0044-T1 → 0044-T2 → 0044-T4 → 0045-T2`) — the Tier-4 human-gate of ADR-0045-T2 (Wave 5) reuses this re-based confirm hold; a BLOCK halts the Wave 4 → Wave 5 boundary. Surface it immediately; do not start any downstream task.
5. Stash cleanup: after abandonment or re-planning, drop all `failed-ADR-0044-T4` entries from `git stash list`.

Do NOT: silently skip a failing acceptance criterion; weaken a test to make it pass (especially the AC-4-adversarial store-adversarial mutation-RED battery, the AC-1-prod production-path filter-revert falsifier, or the AC-4 mechanism-freeze numstat — these ARE the store-surface + frozen-spine deliverable); edit `plan_confirm.py` / the frozen six to make a numstat pass (the mechanism byte-freeze IS AC-4); or proceed to any downstream task (0045-T2) with the confirm-hold in a broken state.
