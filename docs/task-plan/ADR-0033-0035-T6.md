---
task-id: ADR-0033-0035-T6
source-spec: docs/spec/adr-0033-0035-intake-onboarding-spec.md
source-build-plan: docs/build-plan/build-plan-adr-0033-0035-intake.md
wave: 4
assigned-agent: SE
reviewers: [QA, Architect, plan-integrity]
created: 2026-06-30
status: approved
depends-on: [ADR-0033-0035-T1, ADR-0033-0035-T2, ADR-0033-0035-T3, ADR-0033-0035-T4]
---

# Recipe: The First-Run Completeness Gate + Platform-Unlock Render Branch — Derived, Store-Grounded, No New Route; Excludes Post-Unlock Tokens (ST-04 Deadlock-Avoidance); Unlock-Target by Stable `data-tab` SLOT (QA-F1), Not the "My Info" LABEL; Mock/Fixture-Tested, 0 Live Spend

A Wave-4 single-task wave (the spec's L3 Kahn level; depends on T1's rich-domain tokens, T2's
`training-age-band`/`bodyweight-band` derived tokens, T3's safety-screen-answered markers, and T4's
Create-Profile body markup the gate serves). T6 lands the ONE behavior ADR-0033 names: a DERIVED,
store-grounded completeness predicate + a platform-unlock render BRANCH inside the EXISTING
`app_shell.render` ([app_shell.py:258-288](../../../vault/design/templates/app_shell.py)) / `generate.run('app')`
seam — NOT a new route. Today `render` is UNCONDITIONAL: every surface is reachable on first run.
T6 makes the served body conditional on a pure function of store-derived state: an INCOMPLETE profile
hard-gates GET `/` to the Create-Profile-only surface (0 platform surfaces in the served body); a
COMPLETE profile serves the full shell opening on Chat-with-Team (`screen-team`) with the right-panel
default-active on the My-Info SLOT addressed by its stable `data-tab='build'` id.

The predicate is the load-bearing decision (ADR-0033 Decision; OQ-2 / ST-04 / ST-05). It is True iff
the SEVEN directly-captured required tokens (`training-age-band`, `sex-for-dosing`, `bodyweight-band`,
`equipment-access-class`, `goal-domains`, `goal-targets`, `goal-priority-order`) are PRESENT in
`router.summarize(store_read)` AND the THREE `safety-screen::exercise-safety` / `::phq2` / `::apnea`
answered markers are present in the store. It DELIBERATELY EXCLUDES the post-unlock-filled
`rx-interaction-classes` (the ADR-0035 review fills it AFTER unlock — requiring it would DEADLOCK the
gate, ST-04) and the always-set tokens (`recent-trend-direction`, `genetic-trait-classes`, the four chat
bands). This exclusion is natural-by-construction: the seven required tokens are all CONDITIONALLY SET
(omitted from the summary dict when their source readings are absent — [router.py:712-738](../../../scripts/plan/router.py)),
while `rx-interaction-classes` + the always-set tokens are ALWAYS in the dict (their key is present even
empty — [router.py:676-711](../../../scripts/plan/router.py)), so they cannot be members of a
"required key present" set keyed on conditional presence.

The verdict is STORE-GROUNDED (AC4): re-read every render as a pure function of the store-derived
`summarize` tokens + the safety markers — NOT a stored boolean, NOT a turn-count, NOT a model-supplied
flag, and explicitly NOT `chat.plan_next_turn(...).intake_complete` (which is vacuously True at zero
domain coverage — [chat.py:207](../../../scripts/serve/chat.py): `intake_complete = not open_domains`,
True when `covered` is empty). A 0-coverage operator whose `intake_complete` is vacuously True MUST read
INCOMPLETE through this predicate (the unlock-without-data falsification).

The change is an EXTEND-NOT-REBUILD branch (NFR-2): the inner plan ENGINE
(`scripts/plan/{orchestrate,pipeline,assemble,generate_plan,adjudicate,adjust,track}.py`) + the store
(`scripts/store/*.py`) stay numstat=0; `scripts/plan/router.py` stays at its W2 scoped-additive shape
(T6 does NOT touch it); `scripts/serve/server.py` stays numstat=0 (NO new route — the route table stays
its seven entries [server.py:142-166](../../../scripts/serve/server.py)). The ONLY production edit is the
gate region in `app_shell.render` (a DISTINCT region from T4's W1 `_prefill_form` rework). The build is
mock/fixture-tested at 0 live spend (NFR-3): every test seeds a fixture `store_read` over a tmp store
root + asserts on the rendered HTML string / `generate.run('app')` — no live API, no key, no network, no
`key_source.resolve()`-gated call, no `converse`/curation call. T6 makes NO model request (the gate is a
render branch; the crown-jewel re-asserts as a cross-cutting invariant — the served Create-Profile body
carries no raw-PII token, the predicate reads only store-derived state).

## Entry State

**Prerequisites:**
- [ ] Working tree clean: `git status` shows no changes (the daemon vault-frontmatter churn under
      `vault/**` is daemon noise — leave it uncommitted, do not stage it; see CLAUDE.md § Vault hygiene).
- [ ] Full test suite passes at the behavior-preservation baseline: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES
      .venv/bin/python -m pytest -q` green with port 8765 FREE (0 failed). With the operator's intake server
      holding 8765, EXACTLY the two named env tests fail (`tests/serve/test_bind.py::test_port_collision_exits_nonzero_fail_loud`
      + `tests/serve/test_server.py::test_main_reads_no_stdin`) — the documented env delta, nothing else.
      Re-baseline ONLY with a recorded count delta — the gate is the suite staying green across the additive
      render branch, not the absolute integer.
- [ ] Loopback default port 8765 FREE: `! lsof -nP -iTCP:8765 -sTCP:LISTEN >/dev/null 2>&1 && echo "8765 FREE"`.
- [ ] Infrastructure prerequisite verified — the EXACT `render` seam T6 branches is present and carries NO
      completeness-gate text yet (so the AC-1/AC-2/AC-4/AC-5 feature probes are genuinely RED-first), and the
      summarize import T6 adds is absent:
      `grep -q "def render(store_read=None" vault/design/templates/app_shell.py && grep -q "def _prefill_form" vault/design/templates/app_shell.py && ! grep -q "from scripts.plan.router import summarize" vault/design/templates/app_shell.py && ! grep -qE "def _(intake|profile)_complete|_required_tokens|unlock" vault/design/templates/app_shell.py && echo OK`.
- [ ] Infrastructure prerequisite verified — the unlock-target markers PRE-EXIST live (so AC-5's slot/screen
      assertion targets real markup, NOT the "My Info" label T7 builds in W5): the `screen-team` Chat-with-Team
      section + the middle workspace-tab slot `data-tab='build'` (currently `class="ws-tab active"`, labeled
      "Build Plan" — the LABEL is T7's, the SLOT id is what T6 asserts):
      `grep -q 'id="screen-team"' vault/design/templates/app_view.html && grep -q 'data-tab="build"' vault/design/templates/app_view.html && grep -q 'data-screen="team"' vault/design/templates/app_view.html && echo OK`.
- [ ] Infrastructure prerequisite verified — the REQUIRED-token derivation seam T6 reads through is present
      (the predicate keys on `summarize` token presence): the seven required tokens are `SUMMARY_FIELD_SET`
      members, `summarize` omits a conditional token when its source is absent, and `chat.plan_next_turn` exposes
      the vacuously-True `intake_complete` AC-4 must NOT be:
      `grep -q "def summarize(store_read" scripts/plan/router.py && grep -q "intake_complete = not open_domains" scripts/serve/chat.py && grep -qE "training-age-band|goal-priority-order" scripts/plan/router.py && echo OK`.
- [ ] Infrastructure prerequisite verified — **the T1/T2/T3/T4 dependencies are built** (the W1→W2→W3
      checkpoints RAN green): T2's `bodyweight-kg → bodyweight-band` deriver edit landed in `router.py`; T3's
      safety-screen markers + the read-only `referral.py` landed; T4's Create-Profile wizard body + the reworked
      `_prefill_form` (the old `placeholder="e.g. 1986"` born-year/band-`<option>` replace targets retired) landed
      in `app_view.html`/`app_shell.py`:
      `grep -q "bodyweight-kg" scripts/plan/router.py && test -f scripts/serve/referral.py && grep -q "safety-screen" scripts/serve/capture.py && echo OK`.
      (T6's predicate READS T2's derived tokens + T3's markers + serves T4's Create-Profile body; their presence
      + the prior-wave checkpoint-green attestation is an ENTRY-STATE precondition for this Wave-4 task.)
- [ ] Infrastructure prerequisite verified — `server.py`'s seven-entry route table is intact (so AC-6's
      no-new-route numstat is a true 0-row gate): `grep -q "/confirm-extraction" scripts/serve/server.py && grep -q "/generate-plan" scripts/serve/server.py && echo OK`.
- [ ] Infrastructure prerequisite verified — the test harness the T6 cases reuse is present in the append
      target: `grep -q "def _seed_demographics" tests/serve/test_app_shell.py && grep -q "from scripts.store import store" tests/serve/test_app_shell.py && grep -q "from scripts.plan.router import summarize" tests/serve/test_app_shell.py && grep -q "app_shell.render(" tests/serve/test_app_shell.py && echo OK`.
- [ ] On feature branch: `feature/intake-build` (the ADR-0033/0034/0035 build branch, cut off `main`; the
      fork-point `<wave-base>` = `git merge-base HEAD origin/main` = `12f769f` as of 2026-06-30 — the frozen
      7-file engine set, `scripts/store/*`, and `scripts/serve/server.py` are numstat=0 from this base before
      T6 lands; `router.py` is at its W2 scoped-additive shape).
- [ ] `plan-integrity` has grounded the plan AND attested the **Wave-3 checkpoint Go/No-Go RAN green**
      (executed, not reasoned): T3's safety-marker routing + the `referral.py` 0-egress surface green, the frozen
      set numstat=0 + `router.py` scoped-additive at the Wave-3 boundary. T6 is a Wave-4 task — it does NOT
      proceed until the prior wave's checkpoint passed (V1 Build Execution: the wave is the unit).

This entry state doubles as the rollback target — the entry-state commit is the branch HEAD this task is
built on top of.

## Context Load List

**Files to read:**
- `vault/design/templates/app_shell.py` — the EXACT file to extend. Read the seams: `render(store_read=None,
  *, status=None, _today=None)` ([:258-288](../../../vault/design/templates/app_shell.py) — the
  UNCONDITIONAL GET `/` body; T6 adds the gate BRANCH here, keying on the predicate, in a region DISTINCT from
  `_prefill_form`); `_prefill_form` ([:212-255](../../../vault/design/templates/app_shell.py) — T4's W1 rework;
  T6 does NOT touch it — the gate branch is its own region); `_latest_values` ([:185-209](../../../vault/design/templates/app_shell.py)
  — **the flat-list-read PATTERN** the predicate's safety-marker scan mirrors: `render`'s `store_read` is the
  FLAT reading list `store.read_all` returns, scanned by `r.get("item")`); the `from scripts.ingest import
  status as ingest_status` import ([:18](../../../vault/design/templates/app_shell.py) — the additive
  `from scripts.plan.router import summarize` import follows this shape). READ for the seam + the flat-list
  read pattern; modify ONLY the additive gate region.
- `scripts/plan/router.py` — **the predicate's upstream** (DO NOT modify — at its W2 scoped-additive shape).
  Read `SUMMARY_FIELD_SET` ([:23-56](../../../scripts/plan/router.py) — the seven required tokens are members:
  `training-age-band` [:25], `sex-for-dosing` [:26], `bodyweight-band` [:27], `equipment-access-class` [:28],
  `goal-domains` [:30], `goal-targets` [:31], `goal-priority-order` [:32]); `summarize(store_read,
  identity_config=…, genetics_library_root=None)` ([:630-739](../../../scripts/plan/router.py)) — **the
  load-bearing seam**: `store_read` is a per-item CALLABLE (`store.read`-shaped, called as `store_read(field)`
  per [:709,:717,:722](../../../scripts/plan/router.py)), NOT the flat list `render` holds — the predicate must
  ADAPT (see Risk Pre-Check); a CONDITIONAL token is set only `if readings` ([:718-719,:723-738]) so it is
  OMITTED from the dict when its source is absent (the required-token "present" key-in-dict semantics), whereas
  the always-set tokens (`recent-trend-direction` [:681], `rx-interaction-classes` [:688], `genetic-trait-classes`
  [:698], `_ALWAYS_SET_DERIVED` [:702]) are ALWAYS in the dict; the 8j6 fail-closed `ValueError` ([:732-737] —
  the predicate does NOT swallow it, see Risk Pre-Check). Read for the contract the predicate consumes; never
  modify.
- `scripts/serve/chat.py` — `plan_next_turn(summary, covered_domains, declined_domains)`
  ([:126-216](../../../scripts/serve/chat.py)) — read `intake_complete = not open_domains` ([:207]), VACUOUSLY
  True at zero coverage. This is what AC-4 proves the predicate is NOT. Read for the contract; do NOT modify
  (frozen serve surface, not in T6's manifest).
- `vault/design/templates/app_view.html` — read the platform-surface + unlock-target markup the branch keys on
  (DO NOT modify — T6 edits app_shell.py only; T4 built the Create-Profile body in W1): the four platform
  `.screen` sections `id="screen-dashboard"`/`screen-plan"`/`screen-team"`/`screen-profile"` + the nav
  `data-screen="dashboard|plan|team|profile"` buttons ([:350-353,:359,:434,:444,:501]); the right-panel
  workspace tabs `data-tab="myplan"`/`data-tab="build"`/`data-tab="generate"` ([:456-458] — `data-tab='build'`
  carries `ws-tab active` statically, the My-Info SLOT, "Build Plan" label until T7); the `<!--DOC_CARDS-->`/
  `<!--PLAN_ZONE-->` substitution markers `render` injects. Read for the markers AC-1/AC-5 assert; never modify.
- `tests/serve/test_app_shell.py` — the harness the T6 cases reuse: `_seed_demographics(root, values, *, …)`
  ([:857-862](../../../tests/serve/test_app_shell.py) — appends one `source:"intake"` reading per (item, value)
  pair; the T6 fixtures extend it to seed the post-T2 sources `date-of-birth`/`bodyweight-kg` + the pass-through
  `sex-for-dosing`/`equipment-access-class`/`goal-domains`/`goal-targets`/`goal-priority-order` + the three
  `safety-screen::*` markers); `app_shell.render(store.read_all(root))` ([:883] — the FLAT-list render call the
  predicate must handle); `_spa_html()` = `app_shell.render([])` ([:73-75] — the empty-store render); the
  `summarize`/`store`/`functools` imports ([:208-213]); `generate.run('app')` driven into tmp roots via
  `_emit_app(tmp_path)` ([:62-71]). The new T6 cases APPEND to this module.

**Do NOT load:** the ADR (`docs/adr/ADR-0033*.md`/`ADR-0034*.md`/`ADR-0035*.md`) + the dispositions ledger —
the spec distilled them; the OTHER tasks' surfaces (`scripts/serve/capture.py` [T1/T2/T3], `scripts/serve/referral.py`
+ `scripts/serve/care_review.py` [T3/T8], `tests/serve/{test_capture,test_referral,test_care_review,test_intake_onboarding_e2e}.py`)
— untouched here, read only the seams above to CONFIRM the deps landed; the frozen engine
(`scripts/plan/{orchestrate,pipeline,assemble,generate_plan,adjudicate,adjust,track}.py` + `scripts/store/*`) —
byte-frozen, never modify (NFR-2); `scripts/serve/server.py` — read-only to CONFIRM the 7-route table (T6 adds
no route — AC-6 is a numstat=0 over it), NEVER edit; any `vault/**` content file — daemon-churned.

## Risk Pre-Check

This task carries the spec's ADR-0033-0035-T6 Risk Mitigations + the build plan's Wave-4 cross-cutting
invariants + the QA-F1 slot-not-label unlock-target framing. Before proceeding to RED, verify a test exists
(or the RED phase adds one) for EACH; a placeholder test (`assert True`) fails this gate. The gate conditions
reference live tests/commands, not subjective assessments.

- **FLAT-LIST → CALLABLE ADAPTER (the load-bearing integration seam, NFR-1/Integration-Verification mandate).**
  `render`'s `store_read` is the FLAT reading list (`store.read_all` — scanned by `r.get("item")`,
  [app_shell.py:201-208]); `summarize`'s `store_read` is a per-item CALLABLE (`store.read`-shaped, called
  `store_read(field)` — [router.py:709,:717,:722]). The predicate MUST adapt `render`'s flat list into the
  per-item callable `summarize` consumes (a closure filtering the flat list by `item` name), or `summarize`
  raises `TypeError` (list is not callable). Gate: AC-2/AC-4 call the predicate with the SAME flat-list shape
  `render` passes in production (`store.read_all(root)` / `[]`), NOT a hand-built callable — so the test
  exercises the adapter on the production shape (a naive `store_read(item)` on a list reds with `TypeError`;
  this is the factory/call-site wiring the predicate must get right, CLAUDE.md § Integration Verification).
- **STORE-GROUNDED, not a flag / not the chat terminator (AC-4 — the unlock-without-data falsification).** The
  verdict is a pure function of the store-derived `summarize` tokens + the safety markers, re-read each render —
  NOT a stored boolean, NOT a turn-count, NOT a model flag, and NOT `chat.plan_next_turn(...).intake_complete`
  (vacuously True at 0 coverage). Gate: AC-4 — a 0-coverage `store_read` (no required tokens) over which
  `chat.plan_next_turn(summarize(<adapter>), covered_domains=set(), declined_domains=set()).intake_complete`
  IS True (the test asserts the vacuous-True hazard is real) reads INCOMPLETE through the predicate (locked
  render). FAILING-CAPABLE: a predicate wired to
  `intake_complete` flips that fixture to UNLOCKED and reds the lock assertion.
- **EXCLUDES post-unlock tokens (AC-3 — ST-04 deadlock-avoidance).** The predicate does NOT require
  `rx-interaction-classes` (filled only post-unlock by the ADR-0035 review) nor the always-set tokens. Gate:
  AC-3 — a `store_read` complete on the seven required tokens + three markers but with `rx-interaction-classes`
  EMPTY (`""`, its always-set no-medication state — [router.py:688]) UNLOCKS. FAILING-CAPABLE: a predicate
  that required `rx-interaction-classes` would keep this fixture LOCKED (the deadlock) and red the unlock
  assertion.
- **HIDDEN-UNTIL-COMPLETE, markup-level (AC-1 — the gate-bypass falsification).** An incomplete profile serves
  ONLY the Create-Profile surface; the platform surfaces are NOT in the served body (markup-level absence, NOT
  a CSS hide — "0 of the platform surfaces are reachable" requires the markup itself be absent). Gate: AC-1 —
  an empty/partial `store_read` → the rendered body carries 0 of the platform-surface markers
  (`id="screen-dashboard"`/`screen-plan"`/`screen-team"` + the `data-tab="generate"` Generate-Plan tab) AND
  carries the Create-Profile/intake surface. FAILING-CAPABLE: the current unconditional render serves all
  platform surfaces on an empty store → AC-1 reds today (RED-first).
- **UNLOCK TARGET by stable SLOT id, NOT the LABEL (AC-5 — QA-F1).** With the required set present, `render`
  serves the full shell opening on Chat-with-Team — exactly 1 active `.screen` = `screen-team` AND the
  default-active right-panel tab is the My-Info SLOT addressed by its stable `data-tab='build'` id (the live
  middle workspace-tab slot, "Build Plan" label until T7 renames it in W5). Gate: AC-5 asserts the active
  SCREEN is `team` + the active `ws-tab` is `data-tab="build"` — NEVER the "My Info" text LABEL (which does not
  exist until T7; asserting it here would false-RED against the still-"Build Plan" slot, or pass vacuously).
  The `= "My Info"` LABEL assertion is T7's at the W5 checkpoint.
- **NO NEW ROUTE + EXTEND-NOT-REBUILD (AC-6 / NFR-2).** The gate is a branch in `render`/`generate.run('app')`,
  not a route. Gate (SHELL gates run in REGRESSION + at the Wave-4 checkpoint, NOT RED-first pytest tests —
  `12f769f` is a SHA / `git merge-base` unresolvable inside pytest): (a) `git diff --numstat $(git merge-base
  HEAD origin/main) -- scripts/serve/server.py` = 0 rows (AC-6 — the route table byte-unchanged); (b) the frozen
  7-file engine + `scripts/store/*` numstat=0; (c) `scripts/plan/router.py` is at its W2 scoped-additive shape
  (T6 adds 0 router hunks — `git diff $(git merge-base HEAD origin/main) -- scripts/plan/router.py` is unchanged
  from the W2 checkpoint diff); (d) the only changed production file is `app_shell.py` (the gate region) — the
  scoped `git diff --name-only` lists exactly `app_shell.py` + `test_app_shell.py`.
- **CROWN-JEWEL re-assert (NFR-1, cross-cutting).** The gate is a render branch — NO model request. The served
  Create-Profile body carries no raw-PII token; the predicate reads only store-derived state. The 8j6
  fail-closed `summarize` `ValueError` ([router.py:732-737]) is NOT swallowed by the predicate (fail-loud at the
  boundary — a pass-through field carrying raw PII fails the render, it is not masked; do NOT add a defensive
  try/except, SE Core Rule 4/7). Gate: re-asserted as the cross-cutting invariant (no new egress probe — the
  gate makes no request).
- **0 LIVE SPEND — mock/fixture-tested (NFR-3).** Gate: every T6 test seeds a fixture `store_read` over a tmp
  store root + asserts on the rendered HTML / `generate.run('app')` — 0 live calls, 0 key, 0 network, no
  `key_source.resolve()`-gated call, no `converse`/curation call.

## TDD Steps

ONE TDD cycle (RED → GREEN → REFACTOR → REGRESSION). T6 is a single logical change — one completeness
predicate helper + one gate branch in `render` + one additive `summarize` import — so a single cycle is
correct (it does not warrant a multi-cycle decomposition; the seven ACs are facets of the one lock/unlock
seam, none splittable into a second production body). All cases are direct-call (`app_shell.render` over an
injected fixture `store_read`) + one real `generate.run('app')` emit, 0 live spend. Run every pytest
invocation with `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES` (macOS fork-safety on the `.venv` runtime).

**RED-first integrity note.** The genuinely RED-on-T6 probes are AC-1 (the unconditional render serves all
platform surfaces on an empty store → the 0-platform-surface assertion fails), AC-2 (no predicate yet → no
lock↔unlock flip on toggling a required element), AC-3 (no predicate yet → no unlock with `rx-interaction-classes`
empty), AC-4 (no predicate yet → no store-grounded INCOMPLETE verdict for a 0-coverage operator), and AC-5's
screen half (the current render marks NO `.screen` active server-side → asserting "active screen = team"
fails) — each FAILS until Step 2 adds the predicate + the gate branch + the import. AC-5's tab half
(`data-tab="build"` carries `ws-tab active` statically) is a STANDING-GREEN lock that applies to the UNLOCKED
body only. AC-6 (server.py numstat=0) + the EXTEND-NOT-REBUILD SHELL gates (frozen-engine numstat, router
W2-scoped-additive-unchanged) are STANDING INVARIANTS — the tree is byte-clean from `12f769f` over server.py
+ the frozen set before any code lands, and `12f769f` is a SHA / `git merge-base` unresolvable inside pytest —
run in REGRESSION + at the Wave-4 checkpoint, not as RED-first pytest tests. This mirrors the ADR-0032-T3 /
ADR-0033-0035-T2 precedent (the scoped-change / frozen-set SHELL-gate framing).

### Step 1: RED — Write Failing Tests

Call `/write-tests` for `vault/design/templates/app_shell.py` (`tests/serve/test_app_shell.py`, appended).
First add the T6 fixtures — a `_seed_complete_profile(root)` helper that seeds (via `_seed_demographics` /
`store.append`) the post-T2 + pass-through sources so `summarize(<flat-list adapter>)` yields all SEVEN
required tokens AND the store carries the THREE safety markers: a `date-of-birth` full-date reading
(→ `training-age-band`), a `bodyweight-kg` reading (→ `bodyweight-band`, post-T2), `sex-for-dosing="male"`,
`equipment-access-class="full-home-gym"`, `goal-domains`/`goal-targets`/`goal-priority-order` pass-through
PII-free values, and `safety-screen::exercise-safety`/`::phq2`/`::apnea` markers (any answer value). The
fixture GROUNDS itself against the real deriver: assert `summarize(<flat-list adapter over the seeded read>)`
contains all seven required keys (so the fixture is non-tautological — it drives the actual `summarize`, not a
hand-asserted token set). Then target:

- **AC-1 — `test_incomplete_store_serves_create_profile_only_no_platform_surfaces`** (RED-first; the
  gate-bypass falsification): with an empty `store_read` (`app_shell.render([])`) AND a partial one (some but
  not all required elements seeded), the rendered body carries 0 of the platform-surface markers
  (`id="screen-dashboard"`, `id="screen-plan"`, `id="screen-team"`, the `data-tab="generate"` Generate-Plan
  tab — count == 0 for each) AND carries the Create-Profile/intake surface (T4's wizard markers present). RED
  today: the unconditional render serves every platform surface on an empty store.
- **AC-2 — `test_required_predicate_is_the_directly_captured_set_toggle_flips_lock_unlock`** (RED-first;
  table-driven, the predicate definition): with the COMPLETE fixture, `render` serves the UNLOCKED full shell
  (the platform surfaces present); then a table over each of the TEN required elements (the seven required
  tokens' sources + the three safety markers) — for each, seed the complete fixture MINUS that one element and
  assert `render` serves the LOCKED Create-Profile-only body (0 platform surfaces). Proves the predicate is
  True iff ALL ten are present (removing ANY re-locks), keyed on `summarize` token presence + the markers, not
  a constant. RED today: no predicate → no flip.
- **AC-3 — `test_predicate_excludes_post_unlock_rx_interaction_classes_no_deadlock`** (RED-first; ST-04
  deadlock-avoidance): the COMPLETE fixture (seven required + three markers) with `rx-interaction-classes`
  EMPTY (no medication store state — its always-set `""`) UNLOCKS (`render` serves the full shell); the
  predicate does NOT require `rx-interaction-classes` nor the always-set tokens (`recent-trend-direction`,
  `genetic-trait-classes`, the four chat bands). FAILING-CAPABLE: a predicate requiring `rx-interaction-classes`
  keeps this fixture LOCKED and reds the unlock assertion (the deadlock).
- **AC-4 — `test_predicate_is_store_grounded_not_intake_complete`** (RED-first; the unlock-without-data
  falsification): a 0-coverage `store_read` (no required tokens) over which `chat.plan_next_turn(summarize(<flat
  adapter>), covered_domains=set(), declined_domains=set()).intake_complete` IS True (assert this vacuous-True
  hazard is real — import `plan_next_turn`) reads INCOMPLETE through the predicate (`render` serves the LOCKED
  Create-Profile-only body). Proves the predicate
  is the store-grounded token/marker function, NOT `intake_complete`. RED today: no predicate.
- **AC-5 — `test_unlock_target_opens_chat_with_team_my_info_slot`** (RED-first screen half + standing-green tab
  half; QA-F1 slot-not-label): with the COMPLETE fixture, the UNLOCKED render carries exactly 1 active
  `.screen` and it is `screen-team` (the Chat-with-Team surface) AND the default-active right-panel `ws-tab` is
  the SLOT `data-tab="build"` (the My-Info slot by its stable id) — assert the SLOT id, NEVER the "My Info"
  text LABEL (T7 builds the rename in W5; the live slot label is still "Build Plan"). RED today: the render
  marks NO `.screen` active server-side, so "active screen = team" fails; the tab-slot half is the
  standing-green lock.
- **AC-7 — `test_app_shell.py` GREEN** is the targeted-suite gate (the full `tests/serve/test_app_shell.py`
  run below), not a separate case.

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/serve/test_app_shell.py -q -k "incomplete or required_predicate or excludes_post_unlock or store_grounded or unlock_target"`
Expected: FAIL — AC-1 (platform surfaces served on empty store), AC-2/AC-3/AC-4 (no predicate), AC-5's screen
half (no server-active screen) all red; AC-5's tab-slot half passes against the standing `ws-tab active`
markup. Step 2 turns them GREEN.

### Step 2: GREEN — Implement

Changes to make (ALL in `vault/design/templates/app_shell.py`, ADDITIVE only — no literal code reproduced
here; the SE authors it under TDD within the pinned contracts, NFR-5):
- **Import (module level):** add `from scripts.plan.router import summarize` (the predicate keys on
  `summarize` token presence), mirroring the existing `from scripts.ingest import status as ingest_status`
  import shape.
- **Predicate helper `_intake_complete(store_read)` (NEW — internal):** a pure function over `render`'s
  FLAT-LIST `store_read`. (a) ADAPT the flat list into the per-item callable `summarize` consumes (a closure
  filtering the flat list by `item` name — `summarize` calls `store_read(field)`); (b) compute
  `summary = summarize(<adapter>)` and require ALL SEVEN required tokens are PRESENT keys in the summary dict
  (`training-age-band`, `sex-for-dosing`, `bodyweight-band`, `equipment-access-class`, `goal-domains`,
  `goal-targets`, `goal-priority-order` — pin them as a module-level `_REQUIRED_TOKENS` tuple); (c) scan the
  flat list for the THREE `safety-screen::exercise-safety`/`::phq2`/`::apnea` answered markers present
  (mirroring `_latest_values`' `r.get("item")` scan — pin a module-level `_REQUIRED_SAFETY_MARKERS` tuple);
  return True iff all ten present. Do NOT require `rx-interaction-classes` or any always-set token (excluded by
  construction — they are not in `_REQUIRED_TOKENS`). Do NOT swallow `summarize`'s 8j6 fail-closed `ValueError`
  (fail-loud at the boundary).
- **Gate branch in `render`:** key the served body on `_intake_complete(store_read)`. When INCOMPLETE, serve
  ONLY the Create-Profile/intake surface (the platform-surface markup ABSENT from the served body — a
  markup-level branch over T4's Create-Profile body, NOT a CSS hide). When COMPLETE, serve the full shell with
  the `team` screen marked active server-side (exactly 1 active `.screen` = `screen-team` + its nav button) and
  the pre-existing `data-tab="build"` workspace-tab default-active preserved. The branch is a DISTINCT region
  from `_prefill_form` (T4's W1 rework) — it wraps/selects the body BEFORE or AROUND the existing
  `<!--DOC_CARDS-->`/`<!--PLAN_ZONE-->`/`_prefill_form` substitutions. The EXACT markup-manipulation mechanism
  (strip the platform `.screen` sections + the Generate tab for the lock body / inject the `screen active`
  marker for the unlock body) is implementer-discretionary within the RED-capable AC contract; the SE confirms
  the precise markers against T4's landed Create-Profile markup at execution.
- Make NO change to `_prefill_form`, `_doc_cards`, `_plan_zone`, `_latest_values`, or any other existing helper;
  make NO change to `router.py`/`server.py`/the frozen engine.

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/serve/test_app_shell.py -q -k "incomplete or required_predicate or excludes_post_unlock or store_grounded or unlock_target"`
Expected: PASS — AC-1/2/3/4/5 all green.

### Step 3: REFACTOR

Review for:
- `_intake_complete` is a PURE function of the store-derived state (the adapted `summarize` tokens + the
  flat-list safety-marker scan), re-read each render — confirm it reads NO stored boolean, NO turn-count, NO
  model flag, and is NOT `chat.plan_next_turn.intake_complete` (AC-4 the proof). Confirm the flat-list→callable
  adapter is a thin closure (no second store read model, no duplicate `summarize`).
- The required set is EXACTLY the seven conditional tokens + three markers — confirm `rx-interaction-classes`
  and the always-set tokens are NOT in `_REQUIRED_TOKENS` (AC-3 excluded by construction), and the `_REQUIRED_TOKENS`/
  `_REQUIRED_SAFETY_MARKERS` tuples match the spec's pinned required-unlock set ([spec line 41]).
- The gate branch is the ONLY render change — confirm `_prefill_form` + the other helpers are byte-unchanged
  (the gate region is distinct from T4's `_prefill_form` rework), and the unlock body opens on `screen-team`
  with `data-tab="build"` active (AC-5 by stable slot id, not the label).
- The lock body's platform-surface ABSENCE is markup-level (AC-1 — not a CSS hide); confirm 0 platform-surface
  markers in the served lock body.

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/serve/test_app_shell.py -q -k "incomplete or required_predicate or excludes_post_unlock or store_grounded or unlock_target"`
Expected: PASS (no behavior change).

### Step 4: REGRESSION

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/serve/test_app_shell.py -q`
(the full app-shell suite — AC-7) then `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest -q`
(the full suite, port 8765 free → 0 failed, or the 2 named env failures with the port held).
Expected: PASS — the full suite green plus the new T6 cases; the existing app-shell cases (the prefill /
doc-card / plan-zone / inline-asset tests) unaffected by the gate branch (they seed COMPLETE or use
`render([])` against the new lock/unlock body — confirm the existing `render([])`-based tests are reconciled:
any existing test asserting a platform surface renders on an EMPTY store now expects the LOCKED body, an
ENTRY-STATE reconcile the SE makes in the same task per the Factory-to-Component Wiring Rule). Then run the
SHELL gates (STANDING INVARIANTS; `<base> = $(git merge-base HEAD origin/main)` = `12f769f` as of 2026-06-30,
unresolvable inside pytest):
```
base=$(git merge-base HEAD origin/main)
git diff --numstat "$base" -- scripts/serve/server.py                                  # → 0 rows (AC-6: no new route)
git diff --numstat "$base" -- scripts/plan/orchestrate.py scripts/plan/pipeline.py \
  scripts/plan/assemble.py scripts/plan/generate_plan.py scripts/plan/adjudicate.py \
  scripts/plan/adjust.py scripts/plan/track.py                                         # → 0 rows (frozen engine)
git diff --numstat "$base" -- 'scripts/store/*.py'                                      # → 0 rows (frozen store glob)
git diff "$base" -- scripts/plan/router.py | grep '^-' | grep -E 'def dispatch|SUMMARY_FIELD_SET|EXCLUDED_RAW_PII'
                                                                                       # → 0 lines (router unchanged from W2 scoped-additive)
git diff --name-only "$base" -- vault/design/templates/ scripts/ tests/ | sort        # → exactly app_shell.py + test_app_shell.py changed by T6
```
→ server.py + the frozen 7-file engine + the store glob all emit 0 numstat rows; the router shows no NEW
deleted line touching `dispatch`/`SUMMARY_FIELD_SET`/`EXCLUDED_RAW_PII` (it is unchanged from its W2 shape);
the only T6-changed files are `app_shell.py` + `test_app_shell.py`. Confirm the gate-bypass + unlock-target
probes stay green: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/serve/test_app_shell.py -q -k "incomplete or unlock_target"`.

**TDD step count: 4 (1 cycle).** Well within the ≤10-step guidance — T6 is a single behavior (one predicate
+ one render branch + one import, one test module). The 7 ACs span hidden-until-complete + the required
predicate + the post-unlock exclusion + the store-grounded verdict + the slot-id unlock target + the
no-new-route numstat + the targeted-suite gate, none splittable into a second cycle.

## Interface Contracts

T6 modifies an INTERNAL render module (`vault/design/templates/app_shell.py`); it ADDS one internal predicate
helper + a gate branch in `render` + one additive import, and CREATES no new public interface, no new route.
It PRESERVES every other body verbatim.

- **`render(store_read=None, *, status=None, _today=None)` (MODIFIED — additive gate branch).**
  - The signature is UNCHANGED. The returned body is now CONDITIONAL on `_intake_complete(store_read)`:
    INCOMPLETE → the Create-Profile-only surface (0 platform surfaces in the served body); COMPLETE → the full
    shell opening on `screen-team` with the `data-tab="build"` workspace-tab default-active. **PINNED (NFR-1 /
    ST-04):** the predicate is store-grounded (the adapted `summarize` tokens + the safety markers, re-read each
    render), excludes `rx-interaction-classes` + the always-set tokens, is NOT `intake_complete`; the lock body
    is markup-level platform-surface absence (not CSS hide); the unlock target is the stable `data-tab='build'`
    SLOT (QA-F1), NOT the "My Info" label. **DISCRETIONARY (NFR-5):** the exact markup-manipulation mechanism
    (strip vs select the Create-Profile body / inject the active marker), the predicate's internal closure form.
  - **Cross-task seam (upstream):** the predicate consumes T2's `summarize` derived tokens (`training-age-band`/
    `bodyweight-band`), T1's pass-through goal/demographic tokens, and T3's `safety-screen::*` markers; it serves
    T4's Create-Profile body markup. **Cross-task seam (downstream):** T7 (W5) reshapes the unlocked shell behind
    this branch — the tab RENAME ("Build Plan" `data-tab='build'` slot → the "My Info" LABEL), the editable
    My-Info, the Generate Plan roster — and the W5/T7 checkpoint moves the `= "My Info"` LABEL assertion here
    (it lives at W4 only as the stable-slot id). T8 (W6) fires the care-agent review at the final-save UNLOCK
    transition this gate opens.
  - **No new route / no model request (load-bearing):** the gate is a render branch in `render`/`generate.run('app')`;
    `server.py`'s seven-entry route table + the `_LOOPBACK` bind are byte-unchanged (AC-6 numstat=0). **For
    reviewers:** if a downstream task ever needs a stored finalized-flag layer or a turn-count gate, that is an
    ADR amendment + Architect review — NOT a T6 predicate edit; the store-grounded, derived-not-stored verdict
    (ADR-0033 Decision / Negative#2 re-lock-on-required-set-growth) is the accepted V1 contract.
- **`_intake_complete(store_read)` (NEW — internal predicate).** A pure function over `render`'s flat-list
  `store_read`: adapts it into the per-item callable `summarize` consumes, requires all seven `_REQUIRED_TOKENS`
  present in the summary dict AND all three `_REQUIRED_SAFETY_MARKERS` present in the flat list, returns the
  boolean lock/unlock verdict. Does NOT require `rx-interaction-classes` / the always-set tokens; does NOT
  swallow `summarize`'s 8j6 fail-closed `ValueError`.
- **Frozen surface (PRESERVED).** `_prefill_form`, `_doc_cards`, `_doc_card`, `_default_status`, `_plan_zone`,
  `_plan_item_lines`, `_latest_values`, `_esc`, the `_DEMOGRAPHIC_SELECTS`/`_DEMOGRAPHIC_DOB`/`_DOC_ICONS`
  module constants are byte-unchanged (the gate region is distinct from `_prefill_form`). `scripts/plan/router.py`
  stays at its W2 scoped-additive shape; `scripts/serve/server.py` + the frozen engine + `scripts/store/*` stay
  numstat=0.

## Verification Checklist

- [ ] AC-1 (HIDDEN-UNTIL-COMPLETE, gate-bypass falsification) — verified by
      `test_incomplete_store_serves_create_profile_only_no_platform_surfaces`: an empty AND a partial
      `store_read` → the rendered body carries 0 of the platform-surface markers (`id="screen-dashboard"`/
      `screen-plan"`/`screen-team"` + the `data-tab="generate"` tab) AND carries the Create-Profile surface.
      RED-first (the unconditional render serves all platform surfaces today).
- [ ] AC-2 (the REQUIRED predicate = the directly-captured set) — verified by
      `test_required_predicate_is_the_directly_captured_set_toggle_flips_lock_unlock`: the COMPLETE fixture
      UNLOCKS; a table over each of the ten required elements (seven tokens + three markers) — removing ANY one
      re-LOCKS. Keyed on `summarize` token presence + the markers, 0 live spend.
- [ ] AC-3 (EXCLUDES post-unlock tokens, ST-04 deadlock-avoidance) — verified by
      `test_predicate_excludes_post_unlock_rx_interaction_classes_no_deadlock`: the complete fixture with
      `rx-interaction-classes` EMPTY UNLOCKS (requiring it would deadlock); the always-set tokens are not
      required either.
- [ ] AC-4 (STORE-GROUNDED, unlock-without-data falsification) — verified by
      `test_predicate_is_store_grounded_not_intake_complete`: a 0-coverage `store_read` over which
      `chat.plan_next_turn(summarize(<adapter>), covered_domains=set(), declined_domains=set()).intake_complete`
      IS True reads INCOMPLETE (locked) through the predicate. Proves the verdict is the store-grounded
      token/marker function, not `intake_complete`.
- [ ] AC-5 (UNLOCK TARGET by stable slot id, QA-F1) — verified by
      `test_unlock_target_opens_chat_with_team_my_info_slot`: the unlocked render carries exactly 1 active
      `.screen` = `screen-team` AND the default-active `ws-tab` is the SLOT `data-tab="build"` — NOT the
      "My Info" LABEL (T7's at W5). RED-first screen half; standing-green tab-slot half.
- [ ] AC-6 (NO NEW ROUTE) — verified by the SHELL gate: `git diff --numstat $(git merge-base HEAD origin/main)
      -- scripts/serve/server.py` → 0 rows (the route table byte-unchanged at its seven entries; the gate logic
      lives in `app_shell.py`).
- [ ] AC-7 (targeted suite green) — verified by `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m
      pytest tests/serve/test_app_shell.py` → passes.
- [ ] NFR-2 (EXTEND-NOT-REBUILD) — the SHELL gate: `git diff --numstat $(git merge-base HEAD origin/main) --
      <7 frozen files> 'scripts/store/*.py'` → 0 rows; `router.py` unchanged from its W2 scoped-additive shape
      (no NEW deleted line touching `dispatch`/`SUMMARY_FIELD_SET`/`EXCLUDED_RAW_PII`); `server.py` numstat=0;
      the only T6-changed files are `app_shell.py` + `test_app_shell.py`. STANDING INVARIANTS (`12f769f` a
      `git merge-base` unresolvable inside pytest), run in REGRESSION + at the Wave-4 checkpoint.
- [ ] FLAT-LIST → CALLABLE adapter intact — the predicate is called with the production flat-list shape
      (`store.read_all(root)` / `[]`) `render` passes, NOT a hand-built callable; it adapts the flat list into
      the per-item callable `summarize` consumes (AC-2/AC-4 exercise it; a naive `store_read(item)` on a list
      reds with `TypeError`).
- [ ] CROWN-JEWEL re-assert (NFR-1, cross-cutting) — the gate is a render branch (no model request); the served
      Create-Profile lock body carries no raw-PII token; the predicate reads only store-derived state; the
      `summarize` 8j6 fail-closed `ValueError` is not swallowed (fail-loud at the boundary).
- [ ] MOCK/fixture 0-live-spend — every test seeds a fixture `store_read` over a tmp store root + asserts on
      the rendered HTML / `generate.run('app')`; 0 live calls, 0 key, 0 network, no `key_source.resolve()`-gated
      call, no `converse`/curation call (NFR-3).
- [ ] Wave-4 → Wave-5 checkpoint gates carried (build plan § Wave 4 → Wave 5 Boundary): hidden-until-complete
      [AC-1] + required-predicate-toggle [AC-2] + excludes-post-unlock [AC-3] + store-grounded [AC-4] +
      unlock-target-slot-id [AC-5] + no-new-route [AC-6], all green; the frozen-engine + no-new-route numstat;
      the full suite green; 0 live spend.
- [ ] No regression in full test suite (`OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest -q`
      green with port 8765 free, or the 2 named env failures with the port held; re-baseline only with a
      recorded count delta). INV-CORE-CAPABILITY not tripped (the render gate is not referenced by
      `core-capability-audit.sh`, which greps `plan_driver.py`/`plan_orchestrator.py` only).
- [ ] Files modified match the spec File Manifest for T6 (`vault/design/templates/app_shell.py` +
      `tests/serve/test_app_shell.py`) — no scope creep (no router/server/engine/store edit, no `app_view.html`
      edit — T6 reads T4's Create-Profile body, never re-authors it; the existing `render([])`-based app-shell
      tests are reconciled to the new lock/unlock body in the same task per the Factory-to-Component Wiring Rule).
- [ ] Interface contracts documented (the conditional `render` body + the `_intake_complete` predicate's
      store-grounded/excludes-post-unlock/not-intake_complete semantics + the flat-list→callable adapter; the
      T7/T8 downstream seams + the no-new-route/no-model-request property flagged for reviewers).
- [ ] Scope-discipline check: `git diff --name-only` lists only the 2 manifest files; any other file needs a
      written justification.

## Commit

```
feat(serve): T6 first-run completeness gate + unlock branch
```
Stage only:
- `vault/design/templates/app_shell.py`
- `tests/serve/test_app_shell.py`

Stage by explicit path (`git add <path>` as its own command, then `git commit` separately — the
block-pii-commit PreToolUse hook denies single-call stage+commit, `git commit -a/--all`, and pathspec
`git commit <path>`). Do NOT `git add vault/` blindly or the daemon frontmatter churn (CLAUDE.md § Vault
hygiene) — stage the ONE `app_shell.py` template by explicit path. Do NOT stage `.beads/issues.jsonl` /
`harvest.jsonl` / `memory/process-failures.md` — those are close-protocol artifacts. Do NOT stage T1/T2/T3/T4's
files (read, not modified). Commit on `feature/intake-build`.

## Rollback

If verification fails after 3 fix cycles:
1. `git stash -m "failed-ADR-0033-0035-T6-attempt-{N}"` — preserve work for diagnosis.
2. `git checkout {commit-before-task}` — revert to the entry-state commit (the branch HEAD this task was built
   on top of).
3. Update the bead tracker: flag ADR-0033-0035-T6 as BLOCKED with the failure log.
4. Escalate: "Task ADR-0033-0035-T6 failed verification after 3 attempts. Stash ref: {ref}. Failure log:
   {summary}."
5. After abandonment or re-planning, drop all `failed-ADR-0033-0035-T6` entries from `git stash list`.

Do NOT: silently skip failing acceptance criteria, weaken tests to make them pass (especially the gate-bypass
[AC-1] — a falsification that cannot go RED on a platform surface leaking into the locked body is worthless —
the store-grounded probe [AC-4] whose vacuous-True `intake_complete` control distinguishes store-grounded from
the chat terminator, or the deadlock-avoidance [AC-3]); wire the predicate to a stored flag / a turn-count /
`chat.plan_next_turn.intake_complete` (that is the AC-4 breach — the gate would unlock a 0-coverage operator;
HALT, key the predicate on the store-derived `summarize` tokens + the safety markers); require
`rx-interaction-classes` or an always-set token in the required set (that is the ST-04 deadlock — HALT, the
required set is the seven conditional tokens + the three markers only); CSS-hide the platform surfaces instead
of omitting their markup from the locked body (AC-1 demands markup-level absence — HALT); assert the "My Info"
text LABEL at this wave (the slot is still "Build Plan" until T7 — HALT, assert the stable `data-tab='build'`
SLOT id per QA-F1); add a new route / edit `server.py` / the frozen engine / `router.py` (a non-zero numstat
is the NFR-2 / no-new-route breach — HALT, the new code lives only in the additive `app_shell.py` gate region).

## Report

- Recipe: `docs/task-plan/.pipeline/adr-0033-0035/draft-ADR-0033-0035-T6.md` (status: draft).
- Every ADR-0033-0035-T6 acceptance criterion (AC-1..AC-7) maps to a Verification-Checklist step + a Wave-4
  checkpoint gate; the RED-first feature probes are AC-1/2/3/4/5(screen half) (the predicate + gate branch are
  absent today — the unconditional render serves every surface, marks no `.screen` active server-side); AC-6
  (server.py numstat=0) + the EXTEND-NOT-REBUILD gates (frozen-engine numstat, router W2-unchanged) are
  STANDING-INVARIANT SHELL gates; AC-5's `data-tab="build"` tab-slot half is a standing-green lock.
- GROUNDED against the live tree (`feature/intake-build`, `git merge-base HEAD origin/main` = `12f769f`,
  2026-06-30): `render(store_read=None, *, status=None, _today=None)` [app_shell.py:258-288] UNCONDITIONAL
  today + carries NO completeness-gate text (the AC-1/2/4/5 RED-first precondition); `_prefill_form` [:212-255]
  (T4's W1 rework — T6's distinct region); `_latest_values` [:185-209] (the flat-list `r.get("item")` scan the
  marker check mirrors); the additive import follows `from scripts.ingest import status` [:18]. `summarize(store_read,
  identity_config=…, genetics_library_root=None)` [router.py:630-739] — `store_read` is a per-item CALLABLE
  [:709,:717,:722] (the adapter seam vs `render`'s flat list); the seven required tokens are `SUMMARY_FIELD_SET`
  members [:23-32] + CONDITIONAL (omitted when source absent, [:712-738]); `rx-interaction-classes` [:688] + the
  `_ALWAYS_SET_DERIVED` bands [:702] are always-set (excluded by construction). `chat.plan_next_turn` —
  `intake_complete = not open_domains` [chat.py:207] vacuously True at 0 coverage (the AC-4 control). The
  platform surfaces `id="screen-dashboard"`/`screen-plan"`/`screen-team"`/`screen-profile"` + the nav
  `data-screen` buttons [app_view.html:350-353,:359-501] + the workspace tabs `data-tab="myplan"`/`"build"`/
  `"generate"` [:456-458] (`data-tab='build'` carries `ws-tab active` statically — the My-Info slot, "Build
  Plan" label until T7). `server.py`'s seven-route table [server.py:142-166] (AC-6 numstat=0). The
  `_seed_demographics`/`store`/`summarize`/`app_shell.render` harness present in `tests/serve/test_app_shell.py`
  (938 lines). T1/T2/T3/T4 (the deps) are built by W1→W3 — their presence + the Wave-3 checkpoint-green
  attestation is an ENTRY-STATE precondition for this Wave-4 task.
- ZERO literal implementation code in the recipe — the change is described as the additive `summarize` import +
  the `_intake_complete` predicate RULE (adapt the flat list → the per-item callable; require the seven
  conditional required tokens present + the three safety markers; exclude `rx-interaction-classes`/the always-set
  tokens; do not swallow the 8j6 raise) + the `render` gate branch (lock → Create-Profile-only / 0 platform
  surfaces; unlock → full shell on `screen-team` + `data-tab="build"`), with cited `file:line` seams (the
  markup-manipulation mechanism + the closure form implementer-discretionary per NFR-5); the SE authors it
  under TDD.
- Two nuances flagged to the executor + reviewers: (1) the FLAT-LIST → CALLABLE adapter is the load-bearing
  integration seam — `render` holds the flat reading list, `summarize` contracts a per-item callable, so the
  predicate MUST adapt or `summarize` raises `TypeError`; the AC-2/AC-4 tests call the predicate with the
  production flat-list shape (not a hand-built callable) so the adapter is exercised on the real call path
  (CLAUDE.md § Integration Verification). (2) AC-5 is the QA-F1 slot-not-label discipline — assert the active
  screen = `team` + the active workspace-tab SLOT = `data-tab="build"`, NEVER the "My Info" text LABEL (T7
  builds the rename in W5; asserting the label here would false-RED against the still-"Build Plan" slot); the
  `= "My Info"` LABEL assertion is deferred to the W5/T7 checkpoint.
</content>
</invoke>
