---
task-id: ADR-0047-T2
source-spec: docs/spec/adr-0047-0049-credential-onboarding-spec.md
source-build-plan: docs/build-plan/build-plan-adr-0047-0049.md
wave: 2
assigned-agent: SE
reviewers: [Architect, Security, QA]
created: 2026-07-16
status: approved
---

# Recipe: Refactor key_source + auth_isolation onto the Secret-Store Abstraction (WAVE-2 — SEAM-PRESERVING, ENV-FIRST PRESERVED, ERROR-CLASS PRESERVED, FROZEN-SIX BYTE-FROZEN)

> **Reviewer note (build-plan Agent Assignment Matrix):** SE implements; **Security\*** reviews the credential-I/O refactor (0 direct `security` shell-out must survive — verified in CODE-FORM not prose; env-first precedence must not regress into a keyring-primary order; the `store` write-failure must stay fail-loud as `key_source.KeyStoreError` — the class production `server.py:976` catches); **Architect** reviews the seam contract (`keychain_runner`/`keychain_writer`/`keychain_reader` param names + the None-on-absent contract + the `key_source.KeyStoreError` error-class identity every injected test + the production `/settings/key` catch depend on); **QA** runs the abstraction round-trip + 0-shell-out + env-first + error-class-preservation suites.
>
> **Wave-2 REFACTOR (Dependencies: ADR-0047-T1 only).** MODIFIES the deployed consumers — routes their DEFAULT credential I/O through the `scripts.secret_store` abstraction (#352), replacing the three direct macOS `security` shell-outs with `secret_store.get_secret`/`set_secret` calls, KEEPING the seam parameter names, the env-first order, the None-on-absent contract, and — critically — the `store` **write-error class identity** (`key_source.KeyStoreError`, which `server.py:976` catches). `key_source.py`/`auth_isolation.py` are OUTSIDE the ADR-0032 `<frozen-six>`. **Mock/fixture at $0.**
>
> **File manifest = EXACTLY 5 (Modify):** `scripts/model/key_source.py`, `scripts/runner/auth_isolation.py`, `tests/model/test_key_source.py`, `tests/model/test_key_store.py` (the writer's ARGV+no-leak tests — hard-coupled to the seam being refactored; Architect-F1/SEC-2), `tests/runner/test_auth_isolation.py`. **[AMENDED 2026-07-16 per Phase-4]:** `test_key_store.py` added (the spec's file list + the original 4-file draft OMITTED it — a spec/recipe manifest gap that would DEADLOCK AC-7 vs the scope gate).
>
> **Grounded live against `feature/adr-0047-t2-consumer-refactor` off main `30b3e554` (2026-07-16):** `scripts/secret_store/__init__.py` present + green (33 tests, `get_secret(service)->str|None` None-on-ABSENT [fallback engages on keyring-ERROR, not on an available-miss] / `set_secret` [raises `secret_store.KeyStoreError` on total failure] / `delete_secret`); frozen-six numstat vs `3ab1c3abb6c995fbaaadcb179735759e4a61d73d` EMPTY. `key_source.py`: `resolve(keychain_runner=_keychain_runner)`@65, env-first `os.environ.get(ENV_VAR)`@80 THEN `keychain_runner()` (gated `if keychain_key:`@85) THEN `KeyUnavailableError`@88 (`ENV_VAR="ANTHROPIC_API_KEY"`@24); `store(key,*,keychain_writer=_keychain_writer)`@135 (docstring `Raises: KeyStoreError`@152); `_keychain_runner`@44 (list `["security","find-generic-password",…]`@53, None-on-absent); `_keychain_writer`@103 (`["security","add-generic-password","-U","-A",…]`@121, raises `key_source.KeyStoreError`@94/114/131); `_KEYCHAIN_SERVICE="a-plus-maxing-api-key"`@30; `store` empty-reject@154-156. `auth_isolation.py`: `build_subscription_env(base_env,*,keychain_reader=_oauth_keychain_reader)`@87 (token-absent branch `if not token:`@113); `_oauth_keychain_reader`@65 (`["security","find-generic-password",…]`@75); `_KEYCHAIN_SERVICE="a-plus-maxing-oauth-token"`@50. **Two `KeyStoreError` classes exist** (`key_source.py:94` + `secret_store/__init__.py:87`) — SIBLING `RuntimeError` subclasses, `issubclass(secret_store.KeyStoreError, key_source.KeyStoreError)==False` (executed). Production catch: `server.py:973-979` `except (ValueError, key_source.KeyStoreError):` → degrade to 500. Only CODE caller of the private seams outside their files: `tests/model/test_key_store.py` (Architect-grounded); `oauth_pull.py` references them in prose only + has its own `credential_*` seams (T3); `subscription_dispatch` unaffected.

## Entry State

**Prerequisites** (ADR-0047-T2 depends on ADR-0047-T1; Wave-1 checkpoint GREEN):

- [ ] On a feature branch cut off `main` — `feature/adr-0047-t2-consumer-refactor`; never `main`: `git rev-parse --abbrev-ref HEAD` prints a `feature/*` name.
- [ ] **Wave-1 abstraction present + green**: `.venv/bin/python -c "from scripts.secret_store import get_secret, set_secret, delete_secret; print('OK')"` == OK; `.venv/bin/python -m pytest tests/secret_store/test_secret_store.py -q` == `33 passed`.
- [ ] **Durable numstat base** — `FORK_POINT = 3ab1c3abb6c995fbaaadcb179735759e4a61d73d`: `git cat-file -t 3ab1c3abb6c995fbaaadcb179735759e4a61d73d` == `commit`. Use this FIXED SHA (PF-S133-03).
- [ ] The `<frozen-six>` byte-frozen: `git diff --numstat 3ab1c3abb6c995fbaaadcb179735759e4a61d73d -- scripts/store/store.py scripts/store/keying.py scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/router.py` prints **nothing**.
- [ ] All 5 Modify targets PRESENT: `for f in scripts/model/key_source.py scripts/runner/auth_isolation.py tests/model/test_key_source.py tests/model/test_key_store.py tests/runner/test_auth_isolation.py; do test -e "$f" || { echo "MISSING $f (stop)"; exit 1; }; done && echo "present (Modify OK)"`.
- [ ] Full suite baseline — capture the exact line (gate = NO-NEW-FAILURE): `.venv/bin/python -m pytest -q 2>&1 | tail -1` == `2 failed, 2883 passed, 8 skipped …` (the two env-floor reds `test_port_collision`+`test_main_reads_no_stdin`).
- [ ] No STAGED changes (`git diff --cached --name-only` prints nothing). Do NOT `git add vault/` (daemon churn).

## Context Load List

**Files to read (the 5-file manifest + the ONE abstraction reference + the ONE production-catch reference):**

- `scripts/model/key_source.py` — Modify target. Read in FULL.
- `scripts/runner/auth_isolation.py` — Modify target. Read in FULL.
- `tests/model/test_key_source.py` — Modify target. The existing env-precedence + seam-injection tests (must NOT be weakened).
- `tests/model/test_key_store.py` — Modify target. The writer's ARGV test (`test_keychain_writer_invokes_security_add_and_fails_loud_on_nonzero`@45-68) + the no-leak assertions (`test_store_error_message_never_carries_the_key`) — these MIGRATE to the abstraction-backed default (do NOT delete the no-leak coverage).
- `tests/runner/test_auth_isolation.py` — Modify target. The existing seam-injection tests.
- **Cross-manifest read (reason documented, TP-02):** `scripts/secret_store/__init__.py` (read-only, ADR-0047-T1's) — the public-trio contract the new defaults call: `get_secret(service)->str|None` (None-on-ABSENT — matches the old runner's absent case); `set_secret(service,value)` (raises `secret_store.KeyStoreError` on total failure — the class the new writer must TRANSLATE); the `KeyStoreError` class.
- **Cross-manifest read (reason documented — the F2 production-catch site):** `scripts/serve/server.py:973-979` (read-only, ADR-0048/0049's) — the `except (ValueError, key_source.KeyStoreError):` that degrades a `store` failure to a 500. This is WHY the new writer must re-raise `key_source.KeyStoreError`, not let `secret_store.KeyStoreError` escape. Read ONLY that handler; do NOT edit `server.py`.

**Do NOT load:** the ADRs / `.pipeline/` docs; the `<frozen-six>`; `oauth_pull.py` (ADR-0047-T3), `alpha_config.py` (ADR-0048).

## Risk Pre-Check

**BLOCKING GATE — a REFACTOR of DEPLOYED credential I/O.** Do NOT proceed to GREEN until a real, RED-capable test exists for every risk. Risk Mitigations from the spec + the Phase-4 review.

- **Risk (env-first regresses into keyring-primary — AC-5, crown jewel):** must NOT read the abstraction before the env var (breaks the BYO `export ANTHROPIC_API_KEY` flow + the ADR-0048-T1 bridge).
  - Gate (AC-5): `test_resolve_env_first_no_abstraction_read` — `monkeypatch.setenv("ANTHROPIC_API_KEY", …)`; **spy on `scripts.secret_store.get_secret` (the MODULE attribute — NOT an injected `keychain_runner`, which would only re-test the pre-existing env-vs-seam order)**; assert `resolve()` returns env AND `secret_store.get_secret` was called 0 times. GREEN calls the abstraction MODULE-QUALIFIED (`secret_store.get_secret(...)`) so the spy binds (a `from … import get_secret` would defeat it — QA-F4). RED-capable: reorder `resolve` to read the abstraction before the env check → spy ≥1 → RED. **Existing env-precedence tests NOT weakened** — verify `test_resolve_reads_set_env_var`@40, `test_resolve_falls_back_to_keychain_command`@58, `test_resolve_raises_fail_loud_when_key_absent`@74 still ASSERT env-wins (note: these carry a pre-existing ADR-0015 "AC-5" label — do NOT conflate with this spec's AC-5).
- **Risk (a direct `security` shell-out survives — AC-3):** the whole point is to remove the direct keychain shell-out from the consumers.
  - Gate (AC-3, CODE-FORM not prose — SEC-1/QA-F1): `test_no_security_shellout` — the shell-outs are LIST literals `["security","find-generic-password",…]`, so a `security …-generic-password` (space) pattern matches only DOCSTRING prose and is a false-negative gate. Use `grep -rnE '\[\s*"security"\s*,\s*"(find|add)-generic-password"' scripts/model/key_source.py scripts/runner/auth_isolation.py` == 0 hits **AND** `grep -c "import subprocess" scripts/model/key_source.py scripts/runner/auth_isolation.py` == 0 post-refactor (subprocess is used ONLY for the shell-outs in these two files — verified; an import can't be faked by docstrings). RED-capable: the CURRENT (pre-refactor) code has the list-form calls → the code-form grep REDs before GREEN (the Step-1 RED tracks the CODE shell-out, not docstrings). A surviving `subprocess.run(["security",…])` after GREEN → the pattern finds it → RED.
- **Risk (`store` write-error class identity changes — F2/SEC-3/QA-F3, a PRODUCTION regression):** `store`'s contract is `key_source.KeyStoreError` (which `server.py:976` catches); `secret_store.set_secret` raises the SIBLING `secret_store.KeyStoreError` — a naive delegate leaves a write failure UNCAUGHT at `/settings/key`.
  - Gate (AC-8, NEW): `test_store_default_writer_translates_error_class` — monkeypatch `scripts.secret_store.set_secret` to raise `secret_store.KeyStoreError`; assert `key_source.store(key)` (the DEFAULT writer, NOT an injected fake) raises `key_source.KeyStoreError` (constant message, no key value). RED-capable: a default writer that lets `secret_store.KeyStoreError` propagate → the `pytest.raises(key_source.KeyStoreError)` REDs. (Drive the DEFAULT — the existing injected-fake catcher tests bypass it and stay green regardless, the factory-bypass CLAUDE.md mandate.)
- **Risk (None-on-absent parity breaks — AC-4):** the new default must return None-on-absent so `resolve`'s `if keychain_key:` fall-through to `KeyUnavailableError` (and the token-absent branch) is byte-preserved.
  - Gate (AC-4): `test_abstraction_roundtrip` — `store`→`resolve` through the mocked abstraction recovers the key byte-for-byte; `build_subscription_env` returns the mocked token; absent → `get_secret` None → `resolve` raises `KeyUnavailableError` unchanged. RED-capability mutation (QA-F2 — None→"" does NOT bite, both are falsy → same fail-loud branch): stub the default to return a TRUTHY sentinel (`"LEAKED"`) on absent → `resolve` returns it instead of raising → the absent→`KeyUnavailableError` assertion REDs.
- **Risk (frozen-six touched — AC-6):** `test_frozen_six_numstat_empty` — `git diff --numstat 3ab1c3abb6c995fbaaadcb179735759e4a61d73d -- <the six>` prints nothing (FIXED fork-point).

## TDD Steps

One cycle (a contained REFACTOR of 2 consumers + 3 test files). RED extends the 3 test modules; GREEN swaps the three default seam impls + adds the error-class translation; REGRESSION runs the WHOLE suite. Fixtures SYNTHETIC (mocked abstraction, `monkeypatch`-scoped env).

### Step 1: RED — Write Failing Tests
Call `/write-tests` extending `tests/model/test_key_source.py`, `tests/model/test_key_store.py`, `tests/runner/test_auth_isolation.py`, targeting:
- **AC-5** (env-first, module-attr spy): `test_resolve_env_first_no_abstraction_read` — env set → `resolve()` returns env + 0 `secret_store.get_secret` calls (module-attr spy, no `keychain_runner` injection).
- **AC-1/AC-2** (seam params unchanged): `test_seam_params_unchanged` — `inspect.signature` confirms `resolve` keeps `keychain_runner`, `store` keeps `keychain_writer`, `build_subscription_env` keeps `keychain_reader`; an injected fake through each still overrides the new default. RED-capable: a renamed param → the signature assertion / injection test REDs.
- **AC-3** (0 shell-out, CODE-FORM): `test_no_security_shellout` — the `\[\s*"security"\s*,\s*"(find|add)-generic-password"` grep + the `import subprocess`-count grep both 0 over the two consumer files.
- **AC-8** (error-class preserved): `test_store_default_writer_translates_error_class` — `secret_store.set_secret` raising `secret_store.KeyStoreError` → `key_source.store(key)` raises `key_source.KeyStoreError`.
- **AC-4** (round-trip + None-on-absent): `test_abstraction_roundtrip` (+ the migrated writer test in `test_key_store.py` now asserting delegation to `secret_store.set_secret` via a spy, + the MIGRATED no-leak assertions).
- **AC-6** (frozen-six): `test_frozen_six_numstat_empty`.

Run: `.venv/bin/python -m pytest tests/model/test_key_source.py tests/model/test_key_store.py tests/runner/test_auth_isolation.py -q`
Expected: FAIL — the code-form shell-out grep + the env-first module-attr spy + the error-class-translation assertions RED against the current shell-out defaults; the migrated `test_key_store.py` writer test REDs against the current ARGV-asserting form.

#### Step 1 (continued): RED-capability falsification (NON-SKIPPABLE)
Prove each new assertion bites: (a) AC-5 spy REDs if `resolve` reads the abstraction before the env check; (b) AC-3 code-form grep REDs against the CURRENT code (it has the list-form calls) — the Step-1 RED tracks the CODE shell-out; (c) AC-4 None-on-absent — mutate the default to return a TRUTHY sentinel on absent (NOT ""; both None and "" are falsy → same fail-loud branch, so ""→wrong-branch cannot occur) → the absent→`KeyUnavailableError` assertion REDs; (d) AC-8 error-class — a default writer that propagates `secret_store.KeyStoreError` → the `pytest.raises(key_source.KeyStoreError)` REDs; (e) the seam-name assertion REDs on a renamed param.

### Step 2: GREEN — Implement
Changes to make (behavioral descriptions — NO literal code):
- `scripts/model/key_source.py` (Modify — AC-1/AC-3/AC-4/AC-5/AC-8): the DEFAULT `keychain_runner` reads `scripts.secret_store.get_secret("a-plus-maxing-api-key")` MODULE-QUALIFIED (None-on-absent); the DEFAULT `keychain_writer` calls `scripts.secret_store.set_secret("a-plus-maxing-api-key", key)` AND **TRANSLATES the error class** — catch `secret_store.KeyStoreError`, re-raise `key_source.KeyStoreError` with the existing constant message, `from None` (so `server.py:976`'s catch still fires + no secret-bearing cause chains — F2). Keep the `keychain_runner`/`keychain_writer` PARAM names + env-first (`os.environ.get(ENV_VAR)` stays FIRST in `resolve`) + the `store` empty-reject + the constant-message errors. REMOVE the direct `security` shell-out from the two defaults + the now-dead `import subprocess` (verify nothing else in the file uses it). Keep the defaults as NAMED module-level callables (not lambdas) so the signature defaults stay stable.
- `scripts/runner/auth_isolation.py` (Modify — AC-2/AC-3/AC-4): the DEFAULT `keychain_reader` reads `scripts.secret_store.get_secret("a-plus-maxing-oauth-token")` MODULE-QUALIFIED (None-on-absent). Keep the `keychain_reader` PARAM name + the token-absent branch. REMOVE the `security` shell-out from `_oauth_keychain_reader` + the now-dead `import subprocess`.

Run: `.venv/bin/python -m pytest tests/model/test_key_source.py tests/model/test_key_store.py tests/runner/test_auth_isolation.py -q`
Expected: PASS.

### Step 3: REFACTOR
Review for: the default wrappers read as house style (thin, named, `secret_store`-calling, module-qualified); the env-first order visually FIRST in `resolve`; the error-class translation present in the writer; the `import subprocess` removed from both files (grep-confirm); no behavior change beyond the seam-source swap + the error-class translation.
Run: the 3-module suite → PASS.

### Step 4: REGRESSION
Run: `.venv/bin/python -m pytest -q`
Expected: PASS at the entry baseline — the SAME two env-floor failures + 8 skipped, PLUS the refactored suites green, NO NEW failure vs `2883 passed` (**AC-7**). CRITICAL: the WHOLE suite — `resolve`/`store`/`build_subscription_env` have 61 call-sites across 19 files (incl. `client.py`, `server.py`, `subscription_dispatch.py`), so a regression could surface anywhere; AC-8's targeted test covers the factory-bypass case the whole-suite gate alone misses.
Then re-confirm: `git diff --numstat 3ab1c3abb6c995fbaaadcb179735759e4a61d73d -- <the six>` → empty (**AC-6**), and `git diff --name-only 30b3e554 -- scripts/ tests/` → exactly the **5** manifest files (branch-base anchor — PF-S139 recipe-scope-anchor lesson — NOT the fork-point).

## Interface Contracts

This task PRESERVES the existing consumer interfaces (a default-implementation swap + an error-class translation, not a signature change).

- **`key_source.resolve(keychain_runner=<secret_store-backed default>) -> str`** — UNCHANGED: env-first (`ANTHROPIC_API_KEY`) THEN the injected `keychain_runner` (DEFAULT now `secret_store.get_secret("a-plus-maxing-api-key")`, None-on-absent) THEN `KeyUnavailableError`. Env-first PRESERVED (AC-5) — the ADR-0048-T1 bridge still resolves BEFORE the abstraction.
- **`key_source.store(key, *, keychain_writer=<secret_store-backed default>) -> None`** — UNCHANGED signature; empty-reject THEN the injected `keychain_writer` (DEFAULT now `secret_store.set_secret("a-plus-maxing-api-key", key)`). **Error contract PINNED: `Raises: key_source.KeyStoreError`** — the default writer TRANSLATES `secret_store.KeyStoreError` → `key_source.KeyStoreError` (F2), so `server.py:976`'s `except (ValueError, key_source.KeyStoreError)` still degrades a write failure to a 500 (no uncaught escape, no secret in the traceback).
- **`auth_isolation.build_subscription_env(base_env, *, keychain_reader=<secret_store-backed default>)`** — UNCHANGED: DEFAULT `keychain_reader` now `secret_store.get_secret("a-plus-maxing-oauth-token")` (None-on-absent); token-absent branch unchanged.
- **Seam names preserved:** `keychain_runner`/`keychain_writer`/`keychain_reader` — the PARAMETER names are the contract; only the DEFAULT bodies change. None-on-absent parity holds on the absent case (`secret_store.get_secret` is None-on-absent; the fallback-file engages on a keyring-ERROR, not an available-miss — the old shell-out returned None on both, but the fall-through logic only cares about the absent→None case, which is byte-preserved).

## Verification Checklist

- [ ] **AC-1** (`key_source` defaults route through the abstraction, seams kept): `resolve`/`store` DEFAULT I/O → `secret_store` for `a-plus-maxing-api-key`; params unchanged — `test_abstraction_roundtrip` + `test_seam_params_unchanged`.
- [ ] **AC-2** (`auth_isolation` default routes through the abstraction, seam kept): `build_subscription_env` DEFAULT read → `secret_store` for `a-plus-maxing-oauth-token`; param unchanged — `test_abstraction_roundtrip` + `test_seam_params_unchanged`.
- [ ] **AC-3** (0 direct shell-out, CODE-FORM): `grep -rnE '\[\s*"security"\s*,\s*"(find|add)-generic-password"' scripts/model/key_source.py scripts/runner/auth_isolation.py` == 0 AND `grep -c "import subprocess"` == 0 — `test_no_security_shellout` (NOT the prose-matching `security …-generic-password` pattern — SEC-1/QA-F1).
- [ ] **AC-4** (round-trip + None-on-absent): `store`→`resolve` recovers the key; `build_subscription_env` returns the token; absent → `KeyUnavailableError` unchanged — `test_abstraction_roundtrip` (RED-mutation = truthy sentinel on absent, NOT None→"").
- [ ] **AC-5** (env-first PRESERVED, module-attr spy): `ANTHROPIC_API_KEY` set → `resolve` returns env with 0 `secret_store.get_secret` calls; the 3 named env-precedence tests NOT weakened — `test_resolve_env_first_no_abstraction_read`.
- [ ] **AC-6** (frozen-six): `git diff --numstat 3ab1c3abb6c995fbaaadcb179735759e4a61d73d -- <the six>` prints nothing — `test_frozen_six_numstat_empty`.
- [ ] **AC-7** (suite green + 0 regressions): the 3-module suite passes AND `pytest -q` stays at `2883 passed / 2 env-floor / 8 skipped`, NO NEW failure.
- [ ] **AC-8** (`store` error-class PRESERVED — F2): the default writer with a `secret_store.set_secret` raising `secret_store.KeyStoreError` surfaces `key_source.KeyStoreError` (constant message) — `test_store_default_writer_translates_error_class`.
- [ ] **Scope discipline**: `git diff --name-only 30b3e554 -- scripts/ tests/` == the **5** manifest files (branch-base anchor) and nothing else.
- [ ] **No-secret-in-tree**: `git ls-files | xargs grep -lE 'sk-ant-[A-Za-z0-9_-]{20,}'` == 0; the migrated `test_key_store.py` no-leak assertions preserved (synthetic key never in an error message).

## Commit

```
refactor(cred): route key_source + auth_isolation via secret-store
```
(≤50-char; why body: "The consumers route their default credential I/O through the ADR-0047-T1 secret-store abstraction — 0 direct `security` shell-out survives (code-form-verified), the `keychain_*` seam names + env-first precedence + None-on-absent contract + the `key_source.KeyStoreError` write-error class (which `server.py` catches) are all preserved.")

Stage ONLY the 5 manifest files, by explicit path (`git add` as its OWN command, THEN `git commit` separately):
- `scripts/model/key_source.py`, `scripts/runner/auth_isolation.py`, `tests/model/test_key_source.py`, `tests/model/test_key_store.py`, `tests/runner/test_auth_isolation.py`

## Rollback

If verification fails after 3 fix cycles: 1. `git stash -m "failed-ADR-0047-T2-attempt-{N}"`. 2. `git restore --source=30b3e554 -- <the 5 files>`; re-confirm frozen-six numstat vs `3ab1c3ab` EMPTY. 3. Flag BLOCKED. 4. Escalate. 5. Stash cleanup.

Do NOT: reorder `resolve` keyring-primary (AC-5); rename a seam param; let `secret_store.KeyStoreError` escape `store` (translate it — AC-8); use the prose-matching shell-out grep (code-form only — AC-3); DELETE the `test_key_store.py` no-leak assertions (migrate them); leave a `security` shell-out or a dead `import subprocess`; touch the frozen-six; weaken the env-precedence tests; proceed with a broken state.

## Notes — No blocking upstream defect (recipe strengthened by the Phase-4 review)

The recipe covers all ACs; the Phase-4 review (Architect REJECT + Security BLOCK + QA NOT-READY, all executed) hardened it: the manifest gained `tests/model/test_key_store.py` (5 files — the spec's list + the draft omitted it, an AC-7-vs-scope DEADLOCK); the AC-3 grep is CODE-FORM (the prose pattern was a false-negative gate certifying a surviving shell-out as clean AND a false-RED on a correct refactor); a NEW **AC-8** pins the `store` write-error class (`key_source.KeyStoreError` — the naive delegate raised the sibling `secret_store.KeyStoreError` that `server.py:976` does not catch, a real production regression); the AC-4 RED-mutation is a TRUTHY sentinel (None→"" can't bite — both falsy); the AC-5 spy targets `secret_store.get_secret` module-attr with a module-qualified GREEN call. Every AC maps to a named test/command; the error-class + shell-out gates are code-form + drive the real default (not injected-fake tautologies). No untestable AC, no manifest gap (fixed), no undeclared dependency — the recipe proceeds.

**Tracked follow-ups (bead, not this recipe):** the get-side account-binding change (ADR-0047-T1 F5 — `secret_store` binds reads to `getpass.getuser()`; the runbook's api-key write uses `-a "$USER"` == getuser so the documented path round-trips; the SAME agnostic→getuser change applies to `auth_isolation`'s `a-plus-maxing-oauth-token` read [named here per SEC-4]; out-of-band credentials under a different/absent account are the LIVE-run scope, bead `a-plus-maxing-m8ia`); SEC-09 (keyring dep hash-pin, project-wide); the no-secret-in-tree scan is `sk-ant-`-shaped only — the OAuth-token shape relies on the PII hooks + `secret_store`'s own tests (SEC-5, note).
