---
task-id: ADR-0039-T2
source-spec: docs/spec/adr-0039-scheduled-agent-runner-spec.md
source-build-plan: docs/build-plan/build-plan-adr-0039.md
wave: 2
assigned-agent: SE
reviewers: [Security]
created: 2026-07-06
status: approved
---

# Recipe: Auth-Isolation — Env-Scrubbed Subscription Session on `CLAUDE_CODE_OAUTH_TOKEN`; De-ID Client Still Resolves the Keychain (ADR-0039-T2)

Build-plan wave: **Wave 2** (one of three independent leaves off the T1 driver — auth-isolation ∥ scheduler+activation (T3) ∥ store-concurrency (T4); Kahn L2 over `T1→{T2,T3,T4}`). Prior-wave attestation: the **Wave 1 → Wave 2** checkpoint Go/No-Go MUST have RAN green (executed, not reasoned) before this task builds — T1's single `signal(CADENCE_TRIGGER)` binding (AC-1/AC-2), the 0-re-hosting grep + 0 `scripts/serve/` edits (AC-3, SEC-03), the frozen-glob numstat=0 incl. `scripts/serve/plan_loop.py` (AC-4, SEC-03), the crown-jewel non-egress proven-non-empty + de-id-bypass mutation-RED (AC-5, QA-F1), local-store-only de-id-IN-invoked (AC-6, QA-F1), import-inert (AC-7), and the **default subscription-session factory REFUSES live construction pre-scrub (AC-9, SEC-04)**. This task is the sole writer of `scripts/runner/auth_isolation.py` and the sole T2 Modify of `scripts/runner/subscription_dispatch.py` (created by T1); it wires the env-scrub into the session factory and **LIFTS T1 AC-9's pre-scrub refusal** (AC-8). It touches NONE of T3's `schedule/*`/`.gitignore` nor T4's `store_lock.py`/`cadence_runner.py` (disjoint file sets — BP-07 clear).

## Entry State

**Prerequisites:**
- [ ] Task ADR-0039-T1 complete (committed at `{commit-after-T1}` — the recipe `docs/task-plan/adr-0039-t1.md`; the runner-build tracked under the ADR/design bead `a-plus-maxing-31fp` (closed) — the per-task build bead, if opened at execute time, is the T1 dependency reference). Specifically: `scripts/runner/subscription_dispatch.py` exists with `build_dispatch(session)` returning a `dispatch(domain, prompt, summary) -> author envelope` seam, the session obtained through an **injectable session factory**, and the **DEFAULT (non-injected, non-fixture) factory RAISES a typed error** (T1 AC-9, SEC-04) citing the missing env-scrub — no live subscription session is constructible pre-scrub. `scripts/runner/{__init__.py,cadence_runner.py}` + `tests/runner/test_cadence_runner.py` exist and pass.
- [ ] Full test suite passes at the T2-entry floor: `.venv/bin/python -m pytest -q` — the captured baseline is `3 failed, 2312 passed, 7 skipped` (`passed` grown by T1's landed tests). The 3 known-red are `tests/serve/test_bind.py::test_port_collision_exits_nonzero_fail_loud` + `tests/serve/test_server.py::test_main_reads_no_stdin` (env-sensitive serve) + `tests/serve/test_plan_loop_hold.py::test_frozen_spine_and_only_regenerate_changed` (the ADR-0040 post-merge-vacuous merge-artifact). A **4th distinct failure is a regression — HALT** and fix before starting. ADR-0039 edits none of `scripts/serve/plan_loop.py`, so the 3rd stays invariant.
- [ ] Working tree clean: `git status` shows no changes.
- [ ] On feature branch: `feature/adr-0039-runner-build` (the wave-2 continuation branch off `main`; the wave-1 T1 work is already on it).
- [ ] Frozen glob byte-frozen at entry: `git diff --numstat origin/main -- scripts/plan/orchestrate.py scripts/plan/pipeline.py scripts/plan/assemble.py scripts/plan/generate_plan.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/track.py scripts/plan/plan_driver.py scripts/plan/plan_orchestrator.py scripts/store/ scripts/serve/plan_loop.py` prints EMPTY (verified EMPTY 2026-07-06; T2 must keep it empty — it touches no frozen file).
- [ ] Create targets ABSENT: `scripts/runner/auth_isolation.py` and `tests/runner/test_auth_isolation.py` (`test -e` returns non-zero — the two Create paths do not yet exist).
- [ ] `sk-ant-oat[A-Za-z0-9_-]+` token-VALUE scan over the tracked tree is 0 hits at entry: `git ls-files -z | xargs -0 grep -lE "sk-ant-oat[A-Za-z0-9_-]+"` prints nothing (verified 0 hits 2026-07-06 — T2 must keep it 0).

**Rollback target:** `{commit-after-T1}` (the T1-completion commit — the state to `git checkout` on abandonment).

## Context Load List

**Files to read:**
- `scripts/runner/subscription_dispatch.py` — the module this task MODIFIES (created by T1). Read `build_dispatch(session)` + the injectable session factory + the **DEFAULT-factory typed-raise** (T1 AC-9) this task lifts. T2 wires `build_subscription_env(...)` into the default factory's session construction so the process env carries the OAuth token and no `ANTHROPIC_API_KEY`; the raise is replaced by a successful construct (AC-8). Read the factory's keychain/session-spawn seam shape so the wire is a targeted change, not a re-author.
- `scripts/model/key_source.py` — the pattern to MIRROR (do NOT edit): `_keychain_runner()` (`key_source.py:44-62`) runs `security find-generic-password -w -s <service>` and returns the stripped value or `None` — the new `build_subscription_env` OAuth read mirrors this against a NEW `a-plus-maxing-oauth-token` keychain item (a distinct service label from `a-plus-maxing-api-key`). Read `resolve(keychain_runner=_keychain_runner)` (`key_source.py:65-91`): env `ANTHROPIC_API_KEY` FIRST (`L80-82`) then the keychain fall-through (`L84-86`) — this fall-through is exactly AC-3's de-id-still-resolves path. Read `KeyUnavailableError` (`L35-41`) as the fail-loud precedent to mirror for the absent-token raise (AC-4). `ENV_VAR = "ANTHROPIC_API_KEY"` (`L24`), `_KEYCHAIN_SERVICE = "a-plus-maxing-api-key"` (`L30`).
- `scripts/model/client.py` — READ-ONLY cited input for AC-3's framing only: `_ClaudeNoTrainBackend._client()` (`client.py:675-680`) LAZILY imports `resolve` and calls `Anthropic(api_key=resolve())` **at call time**. The de-id `ModelClient` therefore reads `os.environ` (via `resolve`) in the DRIVER's own process — `build_subscription_env` returns a COPY of `base_env` for the SESSION subprocess and never mutates `os.environ`, so the de-id client's env-first-then-keychain resolution is unaffected by the session scrub. Not modified by T2.
- `tests/runner/test_cadence_runner.py` — the SIBLING test file (created by T1) whose fixtures/conventions this task's new test mirrors: the injectable session-factory / fixture-session substitution pattern and the T1 AC-9 **default-factory-raises** test (the assertion T2 AC-8 lifts — the post-scrub construct is its inverse). Match its fixture-seam style so no real keychain/env/session is touched.

**Do NOT load:** the source ADRs (`docs/adr/ADR-0039/0026/0027/0005/0001-*.md`) — the spec already distilled them; re-loading risks drift. `scripts/serve/plan_loop.py`, `scripts/plan/{deid_in,plan_orchestrator}.py`, `scripts/store/*` — the raw-read → `deid_in` in-Python containment is T1's concern (already asserted at the Wave-1 checkpoint); T2 wires only the SESSION env, not the de-id path. `scripts/runner/schedule/*` + `scripts/runner/store_lock.py` + `scripts/runner/cadence_runner.py` — the T3 (scheduler/activation) and T4 (store-lock/critical-section) leaves; T2 must not read or edit them (disjoint files). `scripts/guard/pii_scan.py` — cited by reference only (it is an operator-PII scanner with NO secret pattern, which is WHY AC-5 needs the token-VALUE scan); do not edit. The rest of the spec beyond the ADR-0039-T2 task block.

## Risk Pre-Check

This task HAS risk mitigations in the spec — Risk Pre-Check is REQUIRED and is a **BLOCKING GATE**: T2 is the auth-isolation surface closing the VERIFIED auth-precedence footgun AND the at-rest OAuth-secret surface. Each gate below MUST be satisfied by a concrete, failing-capable test in RED before any GREEN code (an `assert True` placeholder fails this gate):

- **Risk — ADR-0039 Consequences-Negative-3 (auth-precedence footgun, VERIFIED):** Claude Code's auth precedence puts `ANTHROPIC_API_KEY` ABOVE subscription OAuth, so a stray env key silently BILLS the metered API instead of the subscription. Gate: a test asserts `build_subscription_env(base_env)` given `ANTHROPIC_API_KEY=sk-stray` returns a mapping with NO `ANTHROPIC_API_KEY` key AND a set `CLAUDE_CODE_OAUTH_TOKEN`; AND (AC-6) the session `subscription_dispatch` constructs is handed exactly that scrubbed env. FAILING-CAPABLE: a no-op pass-through (or a `dict(base_env)` that forgets the `del`) leaves `sk-stray` in the result, reddening AC-1/AC-6. Non-tautology guard: the same test asserts an UNRELATED `base_env` key survives, so a trivial `return {"CLAUDE_CODE_OAUTH_TOKEN": ...}` (drop everything) also reddens. (Spec T2 AC-1/AC-2/AC-6.)
- **Risk — ADR-0039 Consequences-Negative-5 / SEC-02 (at-rest OAuth secret; the PII hooks are NOT a secret backstop):** `scripts/guard/pii_scan.py` is an OPERATOR-PII scanner with NO secret/credential pattern, so a leaked `CLAUDE_CODE_OAUTH_TOKEN` (`sk-ant-oat…`) in a tracked file passes `block-pii-commit.sh` + `pre-push-pii-scan.sh` CLEAN. Gate: a test runs the token-VALUE pattern `sk-ant-oat[A-Za-z0-9_-]+` over the WHOLE tracked tree (`git ls-files`) and asserts 0 hits, PLUS a name-literal grep over `scripts/runner/` for hardcoded token-value assignments == 0 — the token is read from the keychain at call time, never written to a tracked file. FAILING-CAPABLE / non-vacuous: the same test runs the pattern over a SYNTHETIC positive-control string ASSEMBLED AT RUNTIME FROM FRAGMENTS (e.g. `"sk-ant-" + "oat-" + "SYNTHETIC0000"`) and asserts it MATCHES (proving the regex catches the token shape, so the 0-hits-on-tree assertion is a real clean signal, not a broken pattern that can never match). **The positive-control MUST be fragment-concatenated so no verbatim `sk-ant-oat…` value substring is ever written to a tracked file — else the AC-5(b) whole-tree scan self-matches the moment this recipe/test is committed and the 0-hits assertion goes permanently RED (SEC-02 time-bomb).** (Spec T2 AC-5, SEC-02.)
- **Risk — ADR-0039 OQ-2 (a) fail-loud on absent token (never a silent unauthenticated run):** an absent OAuth token must fail loud, not return an env that runs the session unauthenticated (a silent unauth run is a worse failure than a loud stop). Gate: a test asserts that when the injected keychain seam returns `None`/empty, `build_subscription_env` RAISES a typed error naming `CLAUDE_CODE_OAUTH_TOKEN` (mirroring `KeyUnavailableError`), and does NOT return a mapping. FAILING-CAPABLE: a silent fall-through that returns an env WITHOUT `CLAUDE_CODE_OAUTH_TOKEN` reddens the `pytest.raises`. (Spec T2 AC-4.)
- **Risk — SEC-04 (un-scrubbed default factory before hardening; the T1 AC-9 refusal is lifted ONLY once the env-scrub is wired):** T1 leaves the default session factory refusing live construction pre-scrub; T2 must LIFT that refusal by wiring `build_subscription_env`, and the lifted factory must STILL drop a stray key (the refusal is replaced by a SAFE construct, not an unsafe one). Gate: a test asserts that after wiring, constructing the default (non-fixture) factory under an INJECTED fake keychain seam + an injected session-spawn seam SUCCEEDS (returns a session; no raise — the T1 AC-9 raise is gone) AND the env handed to the spawner has no `ANTHROPIC_API_KEY` and a set `CLAUDE_CODE_OAUTH_TOKEN`. FAILING-CAPABLE: if the wire is absent the factory still raises (reddening the no-raise assertion); if the wire is present but forgets the scrub, `sk-stray` survives into the session env (reddening the drop assertion). 0-spend: construction ≠ dispatch — the injected session-spawn seam returns a stub, no live Agent-SDK call fires. (Spec T2 AC-8, SEC-04.)

## TDD Steps

Eight testable ACs (1–6, 8; AC-7 is the whole-file pytest run) across two files → split into two TDD cycles (8 steps total, under the 10-step ceiling). Cycle 1 = the pure `build_subscription_env` env-scrub + keychain read + de-id-still-resolves + fail-loud + no-token-on-tree (`scripts/runner/auth_isolation.py`, driven directly with fake keychain/env seams) — AC-1, AC-2, AC-3, AC-4, AC-5. Cycle 2 = wiring the scrub into the session factory (`scripts/runner/subscription_dispatch.py` Modify) — AC-6 (session env carries the scrub) + AC-8 (scrubbed factory constructs post-scrub, lifting T1 AC-9, + still drops a stray key). AC-7 is the whole-file pytest run, verified at each REGRESSION. All fixtures are synthetic; **0 live-API spend, 0 real secret, 0 real keychain touched** (every keychain/env/session seam is injected).

### Cycle 1: `build_subscription_env` — the Env-Scrub + Keychain OAuth Read (fail-loud, de-id-still-resolves, no-token-on-tree)

#### Step 1: RED — Write Failing Tests
Call `/write-tests` for `scripts/runner/auth_isolation.py`, creating `tests/runner/test_auth_isolation.py`, targeting (use a fake `keychain_reader` seam returning a synthetic OAuth token ASSEMBLED AT RUNTIME FROM FRAGMENTS — e.g. `"sk-ant-" + "oat-" + "FIXTURE0000"`, so no verbatim `sk-ant-oat…` substring lands in the tracked test file (SEC-02); `monkeypatch` for env; and a synthetic `base_env` dict — 0 live spend, 0 real secret, 0 real keychain):
- **AC-1 (stray API key dropped):** given `base_env = {"ANTHROPIC_API_KEY": "sk-stray", "PATH": "/usr/bin", "HOME": "/tmp/x"}`, `build_subscription_env(base_env, keychain_reader=fake)` returns a mapping with `"ANTHROPIC_API_KEY" not in result` (True) — the stray metered-API key is scrubbed from the subscription session's env. Non-tautology guard: `result["PATH"]` and `result["HOME"]` are PRESERVED unchanged (so a trivial "return an empty/OAuth-only dict" also reddens); `base_env` itself is UNMUTATED (`build_subscription_env` returns a COPY — `"ANTHROPIC_API_KEY" in base_env` stays True, the caller's mapping and `os.environ` are never edited in place).
- **AC-2 (OAuth token set):** `build_subscription_env(base_env, keychain_reader=fake)` sets `result["CLAUDE_CODE_OAUTH_TOKEN"]` equal to the value the injected `keychain_reader` returned (the fragment-assembled fixture OAuth token `"sk-ant-" + "oat-" + "FIXTURE0000"`) — the subscription session authenticates via the OAuth token read from the keychain at call time.
- **AC-3 (de-id ModelClient still resolves the keychain under the scrubbed env):** with `ANTHROPIC_API_KEY` absent from `os.environ` (`monkeypatch.delenv("ANTHROPIC_API_KEY", raising=False)`) and a fake `keychain_runner` returning `"kc-api-key"`, `key_source.resolve(keychain_runner=fake)` returns `"kc-api-key"` (the env-first-then-keychain fall-through, `key_source.py:84-86`) — the de-id `ModelClient` is UNAFFECTED by the session scrub. A supporting assertion pins the mechanism: `build_subscription_env` scrubs a COPY handed to the SESSION subprocess, NOT `os.environ`, so the driver-process de-id client (which reads `os.environ` via `resolve` at call time, `client.py:678-680`) still resolves the `a-plus-maxing-api-key` keychain item. (Both the subscription session's OAuth auth AND the de-id client's keychain auth hold simultaneously — the whole point of the scrub design.)
- **AC-4 (fail-loud on absent token):** when the injected `keychain_reader` returns `None` (and separately, empty `""`), `build_subscription_env(base_env, keychain_reader=fake_none)` raises a TYPED error (a named runner error, e.g. `OAuthTokenUnavailableError`, mirroring `key_source.KeyUnavailableError`) whose message names the missing `CLAUDE_CODE_OAUTH_TOKEN` — asserted via `pytest.raises`. It does NOT return a mapping (never an env that would run the session unauthenticated). Failing-capable: a silent fall-through returning an env without the token reddens the `pytest.raises`.
- **AC-5 (no token on the tracked tree — name-literal grep + `sk-ant-oat…` token-VALUE secret-scan, SEC-02):** (a, name-literal) a grep over `scripts/runner/` finds 0 hardcoded token-value assignments to `CLAUDE_CODE_OAUTH_TOKEN` (count == 0) — the token is read from the keychain at call time, never a tracked literal; (b, token-VALUE scan over the whole tree) the pattern `sk-ant-oat[A-Za-z0-9_-]+` run over `git ls-files` returns 0 hits (count == 0), because `scripts/guard/pii_scan.py` has NO secret pattern so a leaked `sk-ant-oat…` value is NOT caught by `block-pii-commit.sh`/`pre-push-pii-scan.sh` — this scan is the actual control. Non-vacuity / failing-capable: the SAME test asserts the pattern MATCHES a synthetic positive-control string ASSEMBLED AT RUNTIME FROM FRAGMENTS (`"sk-ant-" + "oat-" + "SYNTHETIC0000"`) (≥1 match), proving the regex catches the token shape, so 0-hits-on-`git ls-files` is a genuine clean signal not a dead pattern. **SEC-02 TIME-BOMB — the positive-control AND the AC-1/AC-2 fixture OAuth token MUST be fragment-concatenated at runtime so no verbatim `sk-ant-oat…` value substring is ever written to any tracked file (recipe or test); else the AC-5(b) `git ls-files` scan SELF-MATCHES this test (or this recipe) once committed → the 0-hits assertion goes RED → T3 entry / Wave-2 checkpoint / CI HALT, permanently. The three "obvious" fixes are FORBIDDEN as SEC-02-weakening: (i) excluding test files from the scan = a detection blind spot; (ii) narrowing the regex so it misses the SYNTHETIC control = weakened detection; (iii) deleting the positive-control = re-opens the broken-regex vacuity. The ONLY correct fix is runtime fragment-assembly of the token literals.**

Run: `.venv/bin/python -m pytest tests/runner/test_auth_isolation.py -q`
Expected: FAIL (`scripts.runner.auth_isolation.build_subscription_env` does not yet exist — ImportError).

#### Step 2: GREEN — Implement
Changes to make (NO literal code here — WHAT the module must satisfy):
- `scripts/runner/auth_isolation.py` (Create) — `build_subscription_env(base_env, *, keychain_reader=<default OAuth keychain read>)`:
  - Returns a COPY of `base_env` (never mutates the caller's mapping nor `os.environ`) with the `ANTHROPIC_API_KEY` key REMOVED (satisfies AC-1) and `CLAUDE_CODE_OAUTH_TOKEN` SET to the value from `keychain_reader()` (satisfies AC-2). All other `base_env` keys pass through unchanged (satisfies AC-1's non-tautology guard).
  - The default `keychain_reader` MIRRORS `key_source._keychain_runner` — runs `security find-generic-password -w -s <service>` against a NEW `a-plus-maxing-oauth-token` keychain item (a service label DISTINCT from `a-plus-maxing-api-key`; the service NAME is a project label, the token VALUE lives only in the keychain), returning the stripped value or `None` when absent. The seam is injectable so tests touch no real keychain.
  - When `keychain_reader()` returns `None`/empty, RAISE a typed error (a named runner error mirroring `key_source.KeyUnavailableError`) naming the missing `CLAUDE_CODE_OAUTH_TOKEN` (satisfies AC-4) — never return an unauthenticated env.
  - Hold NO token literal anywhere in the module (satisfies AC-5a); the token is resolved at call time from the keychain (mirrors `key_source`'s runtime-resolution discipline — no secret literal lands in the PUBLIC repo).
- The AC-3 de-id-still-resolves and AC-5b token-VALUE-scan tests need NO production code beyond the above — AC-3 is a property of `key_source.resolve` (unchanged) that the scrub design preserves (COPY-not-mutate), and AC-5b is a scan over the tracked tree that stays clean because AC-5a's discipline holds.

Run: `.venv/bin/python -m pytest tests/runner/test_auth_isolation.py -q`
Expected: PASS.

#### Step 3: REFACTOR
Review implementation for:
- Duplication with `scripts/model/key_source.py`'s `_keychain_runner` — the OAuth read mirrors the SHAPE (subprocess `security find-generic-password -w -s <service>`, strip, `None` on absent/unavailable) but against a DISTINCT service (`a-plus-maxing-oauth-token`); do not import/edit `key_source` (it owns the API key, not the OAuth token) and do not fork a divergent subprocess idiom.
- The typed absent-token error mirrors `KeyUnavailableError`'s fail-loud CONSTANT-message posture — the message names the env var + a runbook, never the token value (NFR-3: the repo is PUBLIC).
- No `ANTHROPIC_API_KEY` / `CLAUDE_CODE_OAUTH_TOKEN` literal VALUES in the module — grep the diff for a hardcoded `sk-` string.

Run: `.venv/bin/python -m pytest tests/runner/test_auth_isolation.py -q`
Expected: PASS (no behavior change).

#### Step 4: REGRESSION
Run: `.venv/bin/python -m pytest -q`
Expected: PASS with no NEW failure vs the T2-entry floor (the 3 known-red stay; a 4th distinct failure is a regression, HALT).

### Cycle 2: Wire the Scrub into the Session Factory — Scrubbed Factory Constructs Post-Scrub (lifts T1 AC-9), Still Drops a Stray Key

#### Step 5: RED — Write Failing Tests
Call `/write-tests` for `scripts/runner/subscription_dispatch.py`, EXTENDING `tests/runner/test_auth_isolation.py`, mirroring T1's `test_cadence_runner.py` fixture-session/factory-injection conventions. Targeting:
- **AC-6 (session env carries the scrub):** `subscription_dispatch` constructs the subscription session with an env produced by `build_subscription_env` — a spy over the session-spawn seam (the Agent-SDK session constructor) asserts the env it is handed has `"ANTHROPIC_API_KEY" not in env` (True) AND a set `CLAUDE_CODE_OAUTH_TOKEN` (the fixture token). Driven with a fake keychain seam + a spy session-spawn seam — 0 live spend.
- **AC-8 (scrubbed factory constructs post-T2 + still drops a stray key — SEC-04, lifts T1 AC-9):** after the wire, constructing the DEFAULT (non-fixture) session factory under an INJECTED fake keychain seam (returns the fixture OAuth token) + an injected session-spawn seam (returns a stub session) SUCCEEDS — it returns a session and does NOT raise (the T1 AC-9 pre-scrub refusal is LIFTED). AND with a stray `ANTHROPIC_API_KEY=sk-stray` in the base env, the env handed to the spawner has `"ANTHROPIC_API_KEY" not in env` and a set `CLAUDE_CODE_OAUTH_TOKEN`. Failing-capable: if the wire is absent the default factory still raises (reddens the no-raise assertion); if the wire forgets the scrub, `sk-stray` survives (reddens the drop assertion). 0-spend: the injected session-spawn seam returns a stub — construction ≠ dispatch, no live Agent-SDK call fires. (This test is the INVERSE of T1's AC-9 default-factory-raises test — reference it so the lift is explicit.)

Run: `.venv/bin/python -m pytest tests/runner/test_auth_isolation.py -q`
Expected: FAIL (the default factory still RAISES per T1 AC-9 — the env-scrub is not yet wired; the AC-6/AC-8 assertions cannot pass until the wire lands).

#### Step 6: GREEN — Implement
Changes to make:
- `scripts/runner/subscription_dispatch.py` (Modify) — in the DEFAULT session factory (the one T1 made raise a typed error pre-scrub, AC-9), REPLACE the pre-scrub refusal with: read the OAuth token via the keychain seam, build the scrubbed env via `auth_isolation.build_subscription_env(base_env, keychain_reader=...)`, and construct the Agent-SDK subscription session with that scrubbed env handed to the session-spawn seam (so the session PROCESS env carries the OAuth token and no `ANTHROPIC_API_KEY`). Satisfies AC-6 (the session's env is scrubbed) + AC-8 (the factory now constructs rather than raises, and still drops the stray key).
  - Keep the keychain read seam AND the session-spawn seam INJECTABLE (the injectable-factory pattern T1 established) so tests substitute fakes — no real keychain, no real session, 0 live spend. Construction is INERT (no live dispatch fires at construct time — mirroring `_ClaudeNoTrainBackend._client()`'s lazy-at-call-time posture; the live call is a dispatch against the session, not the construct).
  - Do NOT touch `build_dispatch(session)` or the `dispatch(domain, prompt, summary)` seam shape (T1's public contract every leaf consumes) — T2 changes only the SESSION CONSTRUCTION path, not the dispatch seam. If the T1 default-factory signature does not expose an injectable keychain/session-spawn seam, FLAG to Architect (a T1 interface gap) rather than re-authoring the factory.

Run: `.venv/bin/python -m pytest tests/runner/test_auth_isolation.py -q`
Expected: PASS.

#### Step 7: REFACTOR
Review implementation for:
- The scrubbed env built via `auth_isolation.build_subscription_env` (one home) — no second env-scrub idiom inside `subscription_dispatch.py`.
- The default factory's construction path stays INERT at construct time (no eager live Agent-SDK dispatch) — the 0-spend property rests on construction ≠ dispatch; confirm no live call was introduced on the construct path.
- The `build_dispatch(session)` / `dispatch(domain, prompt, summary)` seam shape is byte-unchanged (T1's contract) — grep the diff to confirm only the session-CONSTRUCTION path changed.

Run: `.venv/bin/python -m pytest tests/runner/test_auth_isolation.py -q`
Expected: PASS (no behavior change).

#### Step 8: REGRESSION
Run: `.venv/bin/python -m pytest -q`
Expected: PASS with no NEW failure vs the T2-entry floor (AC-7). T1 scoped its AC-9 default-factory-refusal test to a pre-scrub-DETECTABLE sentinel (a pre-scrub condition — per T1's recipe), so wiring the env-scrub does NOT flip that test: it stays scoped to the pre-scrub state and keeps passing. Confirm this at execute time by reading `test_cadence_runner.py`'s AC-9 case — it is pre-scrub-scoped → **NO edit to `tests/runner/test_cadence_runner.py` is needed**. T2 only ADDS the AC-8 post-scrub-constructs assertion to `tests/runner/test_auth_isolation.py`.

## Interface Contracts

This task exposes the auth-isolation seam consumed INSIDE the runner package (no in-DAG SIBLING task consumes it directly — T3 enables the runner, T4 wraps `cadence_runner.py`; neither reads `auth_isolation`). The one hidden coupling — T4 modifies `cadence_runner.py`, which imports `subscription_dispatch` (T2-modified) — is safe because T1 established the stable `build_dispatch(session)` / session-factory interface between them; T2 changes only the session-CONSTRUCTION internals, not the public shape (per the build plan's Coordination note).

- **`build_subscription_env(base_env, *, keychain_reader=<default OAuth keychain read>)`** (in `scripts/runner/auth_isolation.py`):
  - **Signature:** `base_env` (a mapping — typically a copy of the parent env); keyword-only `keychain_reader` (the OAuth-token keychain read seam, returns the token str or `None`; defaults to the `security find-generic-password -w -s a-plus-maxing-oauth-token` runner; injected in tests).
  - **Behavior:** returns a COPY of `base_env` with `ANTHROPIC_API_KEY` removed and `CLAUDE_CODE_OAUTH_TOKEN` set from `keychain_reader()`, all other keys preserved; raises a typed error naming `CLAUDE_CODE_OAUTH_TOKEN` when the token is absent/empty (never an unauthenticated env). Never mutates the caller's mapping or `os.environ` — the de-id `ModelClient` (driver-process, `os.environ` via `resolve`) is unaffected. Holds no token literal.
- **`subscription_dispatch` default session factory** (in `scripts/runner/subscription_dispatch.py`):
  - **Change:** the T1 pre-scrub typed-raise (AC-9) is LIFTED — the default factory now constructs the subscription session under `build_subscription_env(...)` (OAuth-authed, `ANTHROPIC_API_KEY`-free process env). The `build_dispatch(session)` / `dispatch(domain, prompt, summary)` public seam is unchanged.
  - **Change control:** a later change to `build_subscription_env`'s signature or the session-construction contract triggers an Architect contract-update notice (the T1 seam every leaf consumes).

## Verification Checklist

- [ ] AC-1: stray API key dropped — `build_subscription_env({"ANTHROPIC_API_KEY":"sk-stray",...})` → `"ANTHROPIC_API_KEY" not in result`, unrelated keys preserved, `base_env`/`os.environ` unmutated — verified by `test_auth_isolation.py` (stray-key-dropped case).
- [ ] AC-2: OAuth token set — `result["CLAUDE_CODE_OAUTH_TOKEN"]` equals the injected keychain fixture token — verified by `test_auth_isolation.py` (oauth-set case).
- [ ] AC-3: de-id ModelClient still resolves under the scrub — env-absent + fake `keychain_runner` → `key_source.resolve(keychain_runner=fake) == "kc-api-key"` (fall-through); the scrub is a COPY not an `os.environ` mutation — verified by `test_auth_isolation.py` (de-id-still-resolves case).
- [ ] AC-4: fail-loud on absent token — `keychain_reader` → `None`/empty makes `build_subscription_env` raise a typed error naming `CLAUDE_CODE_OAUTH_TOKEN` (`pytest.raises`), no mapping returned — verified by `test_auth_isolation.py` (fail-loud case).
- [ ] AC-5 (SEC-02): no token on the tracked tree — grep over `scripts/runner/` for hardcoded `CLAUDE_CODE_OAUTH_TOKEN` values == 0, AND `sk-ant-oat[A-Za-z0-9_-]+` over `git ls-files` == 0 hits, AND the pattern matches the synthetic positive-control (non-vacuous) — verified by `test_auth_isolation.py` (token-value-scan case). [`git ls-files -z | xargs -0 grep -lE "sk-ant-oat[A-Za-z0-9_-]+"` prints nothing.]
- [ ] AC-6: session env carries the scrub — a spy over the session-spawn seam asserts the handed env has no `ANTHROPIC_API_KEY` + a set `CLAUDE_CODE_OAUTH_TOKEN` — verified by `test_auth_isolation.py` (session-env-scrub case).
- [ ] AC-7: `.venv/bin/python -m pytest tests/runner/test_auth_isolation.py -q` passes against fake keychain/env/session seams (0 live-API calls, 0 real secret, 0 real keychain).
- [ ] AC-8 (SEC-04): scrubbed factory constructs post-T2 + still drops a stray key — the default factory under injected fake keychain + spawn seams SUCCEEDS (no raise — T1 AC-9 lifted) AND drops a stray `ANTHROPIC_API_KEY`; 0-spend (stub spawner) — verified by `test_auth_isolation.py` (factory-lift case).
- [ ] Risk Neg-3 (auth-precedence footgun) covered — AC-1 + AC-2 + AC-6.
- [ ] Risk Neg-5 / SEC-02 (at-rest OAuth secret; PII hooks not a secret backstop) covered — AC-5 (`sk-ant-oat…` token-VALUE scan over `git ls-files`).
- [ ] Risk OQ-2(a) fail-loud covered — AC-4.
- [ ] Risk SEC-04 (default factory refusal lifted only once scrubbed) covered — AC-8 (lifts T1 AC-9).
- [ ] Frozen glob byte-frozen: `git diff --numstat origin/main -- scripts/plan/orchestrate.py scripts/plan/pipeline.py scripts/plan/assemble.py scripts/plan/generate_plan.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/track.py scripts/plan/plan_driver.py scripts/plan/plan_orchestrator.py scripts/store/ scripts/serve/plan_loop.py` prints EMPTY (T2 touches no frozen file, incl. `scripts/serve/plan_loop.py`, SEC-03).
- [ ] No regression in full suite (no NEW 4th failure vs the T2-entry floor `3 failed, …, 7 skipped`).
- [ ] Scope discipline: `git diff --name-only` shows ONLY `scripts/runner/auth_isolation.py`, `scripts/runner/subscription_dispatch.py`, `tests/runner/test_auth_isolation.py` — no touch to `tests/runner/test_cadence_runner.py` (T1's AC-9 test is pre-scrub-scoped, needs no edit), T3's `schedule/*`/`.gitignore`, T4's `store_lock.py`/`cadence_runner.py`, the frozen glob, or `scripts/model/*`.
- [ ] Interface contract (`build_subscription_env` + the lifted default factory) documented for the runner package.

## Commit

```
feat(runner): auth-isolation env-scrub for subscription session
```
Body explains WHY: drop a stray `ANTHROPIC_API_KEY` so the scheduled subscription session authenticates via `CLAUDE_CODE_OAUTH_TOKEN` (the VERIFIED auth-precedence footgun), while the de-id `ModelClient` still resolves the `a-plus-maxing-api-key` keychain item under the scrubbed session env; the OAuth token is keychain-held off the tracked tree (SEC-02). Note the T1 AC-9 default-factory refusal is lifted post-scrub (SEC-04); T1's AC-9 test is pre-scrub-scoped, so no T1-test edit is needed.

Stage only:
- `scripts/runner/auth_isolation.py`
- `scripts/runner/subscription_dispatch.py`
- `tests/runner/test_auth_isolation.py`

Stage each path explicitly (`git add <path>` as its own command, then `git commit` separately — the commit-sequencing rule; no `git commit -a`, no pathspec commit).

## Rollback

If verification fails after 3 fix cycles:
1. `git stash -m "failed-ADR-0039-T2-attempt-{N}"` — preserve work for diagnosis.
2. `git checkout {commit-after-T1}` — revert to entry state (the T1-completion commit from the Entry State prerequisites).
3. Update the issue tracker: flag ADR-0039-T2 as BLOCKED with the failure log.
4. Escalate: "Task ADR-0039-T2 failed verification after 3 attempts. Stash ref: {ref}. Failure log: {summary}."
5. After abandonment or re-planning, drop all `failed-ADR-0039-T2` entries from `git stash list`.

Do NOT: silently skip failing acceptance criteria; weaken the `sk-ant-oat…` token-VALUE secret-scan or the fail-loud-on-absent-token probe (a weakened scan re-opens the at-rest-OAuth-secret hole the PII hooks do NOT backstop; a weakened fail-loud lets the session run silently unauthenticated); leave the default factory constructing a LIVE session under a real key (the construct path must stay inert/fixture-driven — 0 live spend); edit any frozen-glob file or `scripts/model/key_source.py`; or proceed with a broken state. This is a Wave-2 leaf; the whole-spec falsification battery runs at the Wave-2 → Done terminal checkpoint after all three leaves (T2/T3/T4) land.
