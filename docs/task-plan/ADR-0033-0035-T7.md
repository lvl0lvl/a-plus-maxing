---
task-id: ADR-0033-0035-T7
source-spec: docs/spec/adr-0033-0035-intake-onboarding-spec.md
source-build-plan: docs/build-plan/build-plan-adr-0033-0035-intake.md
wave: 5
assigned-agent: SE
reviewers: [QA, Architect, plan-integrity]
created: 2026-06-30
status: approved
depends-on: [ADR-0033-0035-T4, ADR-0033-0035-T6]
---

# Recipe: The Unlocked Shell — Tab Rename (`My Plan | My Info | Generate Plan`, 0 old `Build Plan`/bare `Generate` labels), the Editable My-Info Panel Re-De-Identifying Through the EXISTING `/upload` Seam (No Second Write Path), "+ Add documents" on the Same Seam, the Generate-Plan Tab (Two Buttons + the Full Specialist Roster Each with a Honest-Empty `.bar`), Opens-On-My-Info; Inline-Asset-Gate-Clean, Frozen-Engine Untouched, 0 Live Spend

A Wave-5 single-task wave (the spec's L4 Kahn level). T7 reshapes the UNLOCKED shell that T6's gate serves once the profile is complete: the right-panel workspace tabs in the live `#screen-team` section ([app_view.html:454-498](../../../vault/design/templates/app_view.html)) are RELABELED `My Plan | My Info | Generate Plan` (the live `My Plan | Build Plan | Generate`, [app_view.html:456-458](../../../vault/design/templates/app_view.html) [VERIFIED]), the middle "My Info" panel becomes the operator's full intake — every section editable, an edit re-submitting through the EXISTING `<form action='/upload' method='post'>` capture seam ([app_view.html:465](../../../vault/design/templates/app_view.html) [VERIFIED]) so the SAME `persist_capture` → `summarize` de-identification path runs (no second write path that skips de-id), a "+ Add documents" affordance posting to the same `/upload` flow, and the third panel becomes the Generate-Plan surface (two static buttons "Generate plan"/"Update plan" + the full specialist roster, each specialist carrying a `.bar` status element rendered honest-empty). All of this matches the operator-signed-off `prototype/intake-onboarding-mockup.html` unlocked shell ([:547-550, :579-582, :674](../../../prototype/intake-onboarding-mockup.html)).

T7 is a markup + inline-JS + render-helper task, distinct regions from T4/T5/T6:

1. **`vault/design/templates/app_view.html` (Modify)** — three distinct regions:
   - **The workspace tabs (AC1/AC5):** relabel the middle tab text `Build Plan` → `My Info` and the third tab text `Generate` → `Generate Plan` ([:457-458](../../../vault/design/templates/app_view.html)), KEEPING the stable `data-tab="build"` / `data-tab="generate"` slot ids and the `#panel-build` / `#panel-generate` panel ids byte-unchanged (T6 set the `data-tab='build'` slot default-active in W4 keyed on that stable id — relabeling the slot id would orphan T6's wiring; see Risk Pre-Check + Interface Contracts). Fix the now-stale `#panel-myplan` empty-state copy that references the old `Build Plan` / `Generate` tab labels ([:461](../../../vault/design/templates/app_view.html)) so the rendered body carries 0 old-label occurrences.
   - **The editable My-Info panel (AC2/AC3):** reshape the `#panel-build` content ([:463-494](../../../vault/design/templates/app_view.html)) from the first-run 4-field demographic capture surface to the unlocked My-Info surface — the full intake set (Demographics / Goals / Training & activity / Diet / Supplements & peptides / Medications / Health & lifestyle / Equipment / Documents / API key) rendered editable per the mockup, WITHIN (or submitting to) the EXISTING `<form action='/upload' method='post'>` so the edit re-runs the unchanged capture seam, plus the "+ Add documents" affordance + its `adddocs-panel` posting to the same `/upload` flow.
   - **The Generate-Plan panel (AC4):** populate `#panel-generate` ([:495](../../../vault/design/templates/app_view.html)) with the mockup's two static buttons ("Generate plan"/"Update plan") + the explanatory note + the "Care-team build status" label + the `.gen-roster` container; PORT the mockup's `renderGenerate()` ([mockup:674](../../../prototype/intake-onboarding-mockup.html)) over the live `renderGenerate`/`agentRow` ([app_view.html:723-734](../../../vault/design/templates/app_view.html)) so each specialist row carries a `.bar` (`[app_view.html:131-132]` CSS already exists [VERIFIED]) — rendered HONEST-EMPTY (`width:0%`, no fabricated demo percentages — the ADR-0029 honest-data discipline), and port the mockup's `.gen-roster`/`.genrow`/`.gstat`/`.gm`/`.gtop`/`.gn` layout CSS (the live `<style>` has `.bar`/`.spin` but not those, [app_view.html:127,131-132](../../../vault/design/templates/app_view.html) [VERIFIED]).
2. **`vault/design/templates/app_shell.py` (Modify)** — extend T4's reworked `_prefill_form` ([app_shell.py:212-255](../../../vault/design/templates/app_shell.py)) so the editable My-Info panel renders the operator's saved answers across the FULL intake set (reusing `_latest_values` [:185-209] + the established string-replace prefill discipline), reading the latest saved value per My-Info field and mapping each rich-section field to its saved store item per the pinned capture↔form contract; a field with no saved reading stays blank (no fabricated value). This is a distinct region from T6's gate branch in `render`.
3. **`tests/serve/test_app_shell.py` (Modify)** — the tab-rename + editable-My-Info-`/upload`-form (structural no-bypass) + add-documents + Generate-roster-`.bar` + opens-on-My-Info-LABEL + inline-asset-gate probes, all rendering the UNLOCKED shell (a complete-store fixture flips T6's gate to the full body — else the workspace tabs are not in the served body).

T7 touches NO python production file beyond `app_shell.py`'s `_prefill_form` region: `scripts/serve/capture.py` (T1/T2/T3's), `scripts/plan/router.py` (T2's scoped-additive edit), every `scripts/plan/*.py` frozen engine, `scripts/store/*`, and `scripts/serve/server.py` (T7 adds NO route — the edit + add-docs reuse the EXISTING `/upload` route) are byte-unchanged (EXTEND-NOT-REBUILD, NFR-2). The build is rendered-HTML / fixture tested at 0 live spend, 0 network (no `key_source.resolve()`-gated call, no `converse`/curation call).

## Entry State

**Prerequisites:**
- [ ] On feature branch `feature/intake-build` (the ADR-0033/0034/0035 build branch; [VERIFIED `git branch --show-current` = `feature/intake-build`]). The fork-point `<wave-base>` is `git merge-base HEAD origin/main` = `12f769f` ([VERIFIED 2026-06-30]; computed DYNAMICALLY as `$(git merge-base HEAD origin/main)` in every executable checkpoint command so the gate does not go stale if `main` is rebased — build-plan QA-F6).
- [ ] **Wave 4 checkpoint passed (T6 RAN green) AND T4 (W1) is complete** — T7's dependencies. Attest, per the build-plan wave-gating (a wave advances only when the prior checkpoint Go/No-Go RAN green, executed not reasoned): T6's derived completeness gate + unlock render branch landed in `app_shell.render` (the `screen-team` Chat-with-Team surface + the `data-tab='build'` slot are served default-active on a complete profile); T4's 9-step Create-Profile wizard markup + the reworked `_prefill_form` (amended full-date birthdate + weight-number fields) landed. T7 BUILDS on the post-T4/T6 state — ground every premise below against the LIVE post-T4/T6 tree (`grep`/`stat`), not this recipe's prose.
- [ ] Working tree clean of code changes: `git status` shows only the basic-memory daemon's `vault/**` frontmatter churn (cosmetic daemon noise — leave it uncommitted, never `git add vault/` blindly; CLAUDE.md § Vault hygiene). NB the manifest targets `vault/design/templates/app_view.html` + `vault/design/templates/app_shell.py` live UNDER `vault/` but are NOT daemon-churned entity pages (they are `.html`/`.py`, not the gated `vault/{compounds,biomarkers,library}/*.md`) — stage them by explicit path.
- [ ] Full suite green at the behavior-preservation baseline: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest -q` with port 8765 FREE (0 failed). With the operator's intake server holding 8765, the gate accepts EXACTLY the two pinned env failures `tests/serve/test_bind.py::test_port_collision_exits_nonzero_fail_loud` + `tests/serve/test_server.py::test_main_reads_no_stdin` AND NOTHING ELSE (the documented env delta — a held-port run never launders a regression). Verify the port: `! lsof -nP -iTCP:8765 -sTCP:LISTEN >/dev/null 2>&1 && echo "8765 FREE"`.
- [ ] Live unlocked-shell anchors present (T7 edits real markup): `grep -q 'data-tab="build"' vault/design/templates/app_view.html && grep -q 'data-tab="generate"' vault/design/templates/app_view.html && grep -q 'id="panel-build"' vault/design/templates/app_view.html && grep -q 'id="panel-generate"' vault/design/templates/app_view.html && grep -q "id=\"screen-team\"" vault/design/templates/app_view.html && grep -q "action='/upload'" vault/design/templates/app_view.html && grep -q "function renderGenerate" vault/design/templates/app_view.html && echo OK` ([VERIFIED on the pre-T4/T6 tree: tabs :456-458, `#panel-build` :463, `#panel-generate` :495, `#screen-team` :444, the `/upload` form :465, `renderGenerate` :726]).
- [ ] `_prefill_form` present + reworked by T4 (T7 EXTENDS it for the full intake set): `grep -q "def _prefill_form" vault/design/templates/app_shell.py && grep -q "def _latest_values" vault/design/templates/app_shell.py && grep -q "def render(store_read" vault/design/templates/app_shell.py && echo OK` ([VERIFIED `_prefill_form` :212, `_latest_values` :185, `render` :258 on the pre-T4 tree; CONFIRM T4's amended-field rework is present at Entry State]).
- [ ] The test harness present: `grep -q "def _seed_demographics" tests/serve/test_app_shell.py && grep -q "from scripts.store import store" tests/serve/test_app_shell.py && grep -q "from scripts.plan.router import summarize" tests/serve/test_app_shell.py && grep -q "app_shell.render(store.read_all(" tests/serve/test_app_shell.py && echo OK` ([VERIFIED `_seed_demographics` :857, `app_shell.render(store.read_all(root))` :883]). T7's unlocked-render fixture extends `_seed_demographics` to seed T6's complete required-token set; reuse T6's W4 unlock fixture if T6 exposed one as a test helper.
- [ ] `screen-team` + the `data-tab='build'` slot are served default-active on a COMPLETE profile (T6's W4 unlock target) — confirm against the post-T6 `app_shell.render(<complete store>)` output (the slot, not yet the "My Info" label, which T7 builds): the served unlocked body carries `<section class="screen ... active" id="screen-team">` (or T6's equivalent active marker) and the `data-tab="build"` workspace tab carries the active class. T7's AC5 completes the deferred QA-F1 LABEL assertion on this same slot.
- [ ] EXTEND-NOT-REBUILD baseline: the frozen seven + `scripts/store/*` are numstat=0 vs `<wave-base>` and `scripts/plan/router.py` is SCOPED-ADDITIVE (unchanged from W2) at the Wave-4 boundary; `scripts/serve/server.py` numstat=0; `scripts/serve/capture.py` carries only T1/T2/T3's regions (unchanged from W3). T7 must not move any of these.
- [ ] `plan-integrity` has gated the Wave-4→Wave-5 transition: T7's inputs confirmed against the LIVE post-T4/T6 tree (`stat`/`grep`, not plan prose), the dependency map confirmed an acyclic DAG with artifact-named edges (T4→T7, T6→T7), the Wave-5 checkpoint confirmed runnable, the Wave-4 checkpoint attested green.

This entry state doubles as the rollback target — the entry-state commit is the branch HEAD this task is built on top of.

## Context Load List

**Files to read:**
- `vault/design/templates/app_view.html` — the EXACT file to edit. Read: the `#screen-team` workspace `<aside class="ws-side">` [:454-497] — the `ws-tabs` [:455-459] (the three `.ws-tab` buttons `data-tab="myplan"`/`"build"`/`"generate"`, the middle `data-tab="build"` carrying `class="ws-tab active"`), `#panel-myplan` [:460-462] (its empty-state copy references the old `Build Plan`/`Generate` tab labels — relabel), `#panel-build` [:463-494] (the live `<form action='/upload' method='post'>` [:465] + the demographic fields + "Link your documents" + the confirm-review panel — reshape to the editable My-Info full intake set), `#panel-generate` [:495] (empty, JS-filled). The tab-switch handler [:708-714] (`p.id==='panel-'+t.dataset.tab` keys panel id off `data-tab`; `if(t.dataset.tab==='generate') renderGenerate()`), `renderGenerate` [:726-734] + `agentRow` [:723-724] (the per-agent `.agent-prog` row to PORT to the mockup's `.bar` row) + `REVIEW_AGENTS` [:718-722] + `SPECIALISTS` [:528-546], the `tile`/`dotFor` helpers [:548,:562], the `.bar`/`.spin` CSS [:131-132,:127] (already present), the `.agent-row`/`.agent-prog`/`.ws-genplan` CSS [:174-181,:172] (the live progress styles). MATCH the file's verbatim-port discipline (inline lucide SVG, no off-file asset, honest empty-data).
- `prototype/intake-onboarding-mockup.html` — the operator-signed-off design source (gitignored). Read the unlocked shell: the `ws-tabs` `My Plan | My Info | Generate Plan` [:547,:579], the editable My-Info `ws-docs` panel (the `.mi-note` + the per-section `.mi-head`/`.seclab` + `.mi-edit` `toggleEdit` + the "+ Add documents" `toggleAddDocs` + the `adddocs-panel`) [:549,:581], the Generate-Plan panel (the two `Generate plan`/`Update plan` buttons + the note + "Care-team build status" + `.gen-roster`) [:550,:582], `toggleAddDocs` [:643], `toggleEdit`, the workspace-tab switch [:662-664], and `renderGenerate` [:674] (the `.genrow` + `.bar` per-specialist row). PORT the structure VERBATIM (the visual-verification mandate — "port" = reproduce, not re-author) but render the roster HONEST-EMPTY (drop the mockup's fabricated `ST` demo percentages — `width:0%`), and wire the My-Info edit to the REAL `/upload` form (the mockup's `disabled` inputs + `toggleEdit` are a static prototype; the function the AC demands is a live `/upload` re-submit).
- `vault/design/templates/app_shell.py` — `_prefill_form(html, store_read)` [:212-255] (T4's reworked prefill — the region T7 EXTENDS to the full My-Info intake set; the string-replace prefill of `<select>` options + the `value=` injection + the `✓ Saved` banner) + `_latest_values(store_read, names)` [:185-209] (the latest-by-timepoint resolver to reuse) + `render(store_read=None, *, status=None, _today=None)` [:258-288] (the seam `_prefill_form` runs inside; T7 adds NO new branch here — that is T6's region). READ to extend the prefill field set; do NOT touch `render`'s gate branch.
- `tests/serve/test_app_shell.py` — the harness: `_spa_html()` = `app_shell.render([])` [:73-75], `_emit_app(tmp_path)` (the REAL `generate.run('app')` emit over tmp roots) [:62-70], `_seed_demographics(root, values, *, timepoint, source)` (`store.append` per item) [:857-862], `app_shell.render(store.read_all(root))` [:883] (the flat-list unlocked render call), `_upload_form_html`/`_form_field_names`/`_select_block` [:243-263], `_panel_build_html` [:443-451] (extracts `#panel-build` content), the `summarize(functools.partial(store.read, root=...), identity_config=_ABSENT_IDENTITY)` round-trip [:321], `_FABRICATED`/the honest-awaiting guard [:49-59,:149]. The new T7 cases MATCH this fixture-driven, rendered-HTML-string shape; the unlocked-render fixture seeds T6's complete required-token set.
- `prototype/intake-onboarding-mockup.html` (re-read for the Generate panel CSS) — the `.gen-roster`/`.genrow`/`.gstat`/`.gm`/`.gtop`/`.gn`/`.spin` declarations to port into the app_view `<style>` (the live `<style>` lacks `.gen-roster`/`.genrow`/`.gstat`/`.gm`/`.gtop`/`.gn`; `.bar`/`.spin` already present).

**Do NOT load:** the ADR set (`docs/adr/ADR-003{3,4,5}*.md`) + the dispositions (`docs/spec/.pipeline/adr-0033-0035/`) — the spec/build-plan distilled them; `scripts/serve/capture.py` (T1/T2/T3's surface — T7 makes 0 capture edits; the edit reuses the EXISTING `/upload` route, which calls capture), `scripts/plan/router.py` (T2's scoped-additive edit — T7 reads no router internals), every `scripts/plan/*.py` frozen engine + `scripts/store/*` (byte-frozen, NFR-2), `scripts/serve/server.py` (T7 adds NO route — read only to RE-ASSERT numstat=0, not to edit), `scripts/serve/care_review.py`/`referral.py` (T8/T3's), `scripts/model/client.py` (T8's care-agent seam, not T7's). T7 makes 0 model call.

## Risk Pre-Check

This task carries the spec's ADR-0033-0035-T7 Risk Mitigations + the build plan's Wave-5 → Wave-6 checkpoint gates (incl. the QA-F1 deferred "My Info" LABEL assertion + the SEC-F1 structural/behavioral split). Before proceeding to RED, verify a test exists (or the RED phase adds one) for EACH; a placeholder test (`assert True`) fails this gate. Each gate condition references a live test/command, not a subjective assessment.

- **TAB RENAME — 0 old labels in the rendered body (AC1, RED-capable).** The unlocked right-panel tabs must read `My Plan | My Info | Generate Plan` with 0 occurrences of the old `Build Plan` / bare `Generate` tab labels in the served body. Gate: over the UNLOCKED render (a complete-store fixture), assert the new tab markup is present (`data-tab="build">My Info<` and `data-tab="generate">Generate Plan<`) AND a blanket `'>Build Plan<' not in html` and `'>Generate<' not in html` (the old tab text-nodes — note `>Generate<` is the bare tab label; `Generate plan →` / `Generate plan` button copy is NOT `>Generate<`, so the blanket check is precise). FAILING-CAPABLE: before the relabel, `data-tab="build">Build Plan<` is present → `'>Build Plan<' not in html` reds; the `#panel-myplan` stale copy [:461] also carries `<b>Build Plan</b>`/`<b>Generate</b>` (text-node `>Build Plan<`/`>Generate<`), so the blanket check forces that copy fixed too. Revert either the tab relabel or the copy fix → the case reds.
- **EDITABLE MY-INFO — re-de-identify through `/upload`, NO second write path (AC2, crown-jewel STRUCTURAL; SEC-F1).** The My-Info tab renders all intake sections editable AND the edit re-submits through the EXISTING `<form action='/upload' method='post'>` — the SAME `persist_capture` → `summarize` boundary, no second write path that skips de-identification. Per SEC-F1 the W5 check is STRUCTURAL: grep the My-Info edit-form action targets `/upload` and assert there is no second POST target inside the My-Info panel (no `action='/...'` other than `/upload`). Gate: over the UNLOCKED render, the `#panel-build` (My-Info) content contains `<form action='/upload' method='post'>` AND every `action='...'` within the panel equals `/upload` (0 non-`/upload` write targets). FAILING-CAPABLE: inject a second `action='/save-profile'` form into the My-Info panel → the "0 non-`/upload` targets" assertion reds; remove the `/upload` form → the "edit form targets `/upload`" assertion reds. The BEHAVIORAL no-bypass proof (drive a raw value through the edit `/upload` re-submit + assert 0 raw crossing in the `dispatch` payload + every model request) is the Security-reviewed T9 E2E probe (W7, SEC-F1) — REFERENCED in Interface Contracts so the coupling is explicit; this lean QA+Architect wave grounds it structurally, T9 proves it end-to-end.
- **OPENS-ON-MY-INFO — the unlock-target LABEL (AC5, completes the deferred QA-F1 assertion).** The unlocked default-active right-panel tab is the renamed **My Info** LABEL — the `data-tab="build"` slot (the same slot T6 set default-active by stable id in W4) carries the active class AND its text is now `My Info`, AND its `#panel-build` panel is the active right-panel. Gate: over the UNLOCKED render, the active `.ws-tab` (the one with `class` containing `active`) has the text `My Info` AND `#panel-build` carries the active panel class. This is the `= "My Info"` LABEL assertion DEFERRED from the W4/T6 checkpoint (QA-F1: T6 asserted the SLOT by stable `data-tab='build'` id; T7 BUILDS the label, so the label assertion lands here). FAILING-CAPABLE: before the relabel the active slot reads `Build Plan` → the `= "My Info"` assertion reds; if T7 wrongly relabels a non-active slot, the active-slot-text assertion reds.
- **ADD DOCUMENTS — posts to the existing `/upload` route (AC3).** The My-Info "+ Add documents" affordance routes through the EXISTING multi-source `/upload` flow (no new route). Gate: over the UNLOCKED render, the add-docs affordance + its panel land in the My-Info panel and the upload controls inside it post to `/upload` (the file-picker → `POST /upload` JS path the live `#screen-team` already wires, [app_view.html:736-746,:822](../../../vault/design/templates/app_view.html)); assert the add-docs control is present (`+ Add documents`) and no new route literal (`/add-docs`/`/documents`) appears. FAILING-CAPABLE: a hardcoded `fetch('/add-docs'...)` → the "routes through `/upload`, no new route literal" assertion reds.
- **GENERATE-PLAN TAB — two buttons + per-specialist `.bar`, honest-empty (AC4).** The Generate-Plan panel carries two buttons ("Generate plan"/"Update plan") + the full specialist roster, each specialist with a `.bar` status element, rendered HONEST-EMPTY (no fabricated done/working percentages — ADR-0029 honest-data). Gate: over the UNLOCKED render, `#panel-generate` static markup carries both button labels (`>Generate plan<` AND `>Update plan<`) + a `.gen-roster` container; the `renderGenerate` JS source emits a per-specialist row carrying `class="bar"` (the roster template) over `SPECIALISTS`; and NONE of the `_FABRICATED` demo strings appear (the honest-empty guard) and the roster `.bar` widths are `0%` (no fabricated progress). FAILING-CAPABLE: keep the live single button / the `.agent-prog` (not `.bar`) → the "two buttons" + "per-specialist `.bar`" assertions red; port the mockup's fabricated `ST` percentages → the honest-empty / `_FABRICATED` guard reds.
- **RENDER-THE-UNLOCKED-SHELL test construction (cross-cutting, non-tautological).** Every T7 assertion targets the UNLOCKED body — the workspace tabs live in `#screen-team`, which T6's gate serves ONLY on a COMPLETE profile (an incomplete/empty store renders the Create-Profile-only body with NO `#screen-team`). The T7 fixture seeds T6's complete required-token set (the seven directly-captured tokens + the three `safety-screen::*` markers) so `app_shell.render(store.read_all(root))` returns the unlocked shell. Gate: a NEGATIVE control — the SAME assertions over `_spa_html()` (`app_shell.render([])`, the LOCKED body) find NO workspace tabs (the unlocked markup is absent when locked), proving the T7 cases test the unlocked render, not a constant. FAILING-CAPABLE: if a T7 case passed against the LOCKED body it would be testing nothing — the negative control reds that.
- **ADR-0029 inline-asset gate over the UNLOCKED body (AC6).** The served SPA — INCLUDING T7's unlocked-shell additions — passes the real `render.emit` off-file-asset gate. Gate: `generate.run("app")` over a COMPLETE (unlocked) store returns a written `Path` that EXISTS (does not raise the off-file `ValueError`); T7's new markup carries only inline `<svg>` (the ported lucide icons), no off-file asset reference. FAILING-CAPABLE: the existing negative probe `test_offfile_ref_makes_emit_raise` [:98-118] proves the gate is the real emit, not a substring grep; inject an off-file `<img src=...>` into the My-Info panel → `generate.run('app')` raises.
- **EXTEND-NOT-REBUILD — markup + the `_prefill_form` region only, NO new route, frozen engine untouched (NFR-2).** T7 edits ONLY `vault/design/templates/app_view.html`, `vault/design/templates/app_shell.py` (the `_prefill_form` region), and `tests/serve/test_app_shell.py`. Gate (a standing INVARIANT run in REGRESSION; `<wave-base>` = `$(git merge-base HEAD origin/main)`): `git diff --numstat "$WAVE_BASE"..HEAD -- scripts/plan/orchestrate.py scripts/plan/pipeline.py scripts/plan/assemble.py scripts/plan/generate_plan.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/track.py 'scripts/store/*.py'` → 0 rows; `scripts/serve/server.py` numstat=0 (T7 adds NO route — the edit + add-docs reuse `/upload`); `scripts/serve/capture.py` carries only T1/T2/T3's regions (no T7 hunk); `scripts/plan/router.py` SCOPED-ADDITIVE (unchanged from W2 — T7 adds no hunk). `git diff --name-only "$WAVE_BASE"..HEAD` for T7's commit lists ONLY the three manifest files (plus any prior-wave files if W1-W4 are unmerged on the branch — T7's OWN diff adds only the three).
- **0 LIVE SPEND (NFR-3).** Every T7 test runs against the rendered-HTML string / a fixture `store_read` over tmp roots; the inline-asset gate emits via `generate.run('app')` (a local render+write, 0 network). No `key_source.resolve()`-gated call, no `converse`/curation call, no `aplus-research` dispatch appears anywhere in T7. The operator-present LIVE care-agent run is ADR-0035 OQ-1 (out of every wave).

## TDD Steps

Three TDD cycles over `tests/serve/test_app_shell.py`. Cycle 1 lands the tab rename + opens-on-My-Info LABEL (AC1, AC5). Cycle 2 lands the editable My-Info `/upload` panel + "+ Add documents" + the `_prefill_form` full-set extension (AC2, AC3). Cycle 3 lands the Generate-Plan tab (AC4) + the full regression (AC6, AC7, EXTEND-NOT-REBUILD). All cases render the UNLOCKED shell over a complete-store fixture; 0 live spend, 0 network.

**Every Python command in this recipe is prefixed `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES`** (the macOS fork-safety setting the suite runs under), run with `.venv/bin/python -m pytest`, port 8765 FREE.

**Shared fixture (added in Cycle 1, reused by all):** an `_unlocked_html(tmp_path)` helper that seeds T6's complete required-token set into a tmp store (extend `_seed_demographics` to seed `date-of-birth` + `bodyweight-kg` [→ the T2-derived `training-age-band`/`bodyweight-band`] + `sex-for-dosing` + `equipment-access-class` + `goal-domains` + `goal-targets` + `goal-priority-order` + the three `safety-screen::exercise-safety`/`::phq2`/`::apnea` markers, mirroring T6's W4 unlock fixture — reuse T6's helper if it exposed one) and returns `app_shell.render(store.read_all(root))` (the UNLOCKED body). The negative control renders `_spa_html()` (the LOCKED body) and asserts the workspace tabs are ABSENT.

### Cycle 1: Steps 1-3 — the tab rename + opens-on-My-Info LABEL (AC1, AC5)

#### Step 1: RED — Write Failing Tests
Edit `tests/serve/test_app_shell.py`: add the `_unlocked_html(tmp_path)` fixture (above) + the negative control + the tab cases.

- **Case T7-1 — tab rename (AC1).** `html = _unlocked_html(tmp_path)`; assert `'data-tab="build">My Info<' in html` AND `'data-tab="generate">Generate Plan<' in html` AND `'>Build Plan<' not in html` AND `'>Generate<' not in html`. RED: before the relabel the body carries `data-tab="build">Build Plan<` (+ the `#panel-myplan` stale copy `<b>Build Plan</b>`/`<b>Generate</b>`) → the blanket `not in` assertions red.
- **Case T7-2 — opens-on-My-Info LABEL (AC5).** `html = _unlocked_html(tmp_path)`; extract the active workspace tab (the `.ws-tab` whose class contains `active`) and assert its text == `My Info`; assert `#panel-build` carries the active panel class (`class="ws-panel active ...` on `id="panel-build"`). RED: before the relabel the active slot reads `Build Plan` → the `== "My Info"` assertion reds. This completes the deferred QA-F1 LABEL assertion on T6's stable `data-tab='build'` slot.
- **Case T7-3 — negative control (non-tautological).** `html = _spa_html()` (the LOCKED empty-store body); assert `'class="ws-tabs"' not in html` (or the chosen workspace-tab marker is absent) — the workspace tabs appear ONLY in the unlocked body. RED-capable by construction: if T6's gate did not hide `#screen-team` when locked, this reds (and the T7 cases would be tautological); it stays green once T6's gate is correct, proving T7-1/T7-2 test the unlocked render.

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/serve/test_app_shell.py -q`
Expected: FAIL — T7-1 (old labels present) + T7-2 (active slot reads `Build Plan`) red; T7-3 green (the locked body has no workspace tabs).

#### Step 2: GREEN — Implement
Changes to make (describe; the SE writes the markup):
- `vault/design/templates/app_view.html` [:457] — change the middle tab text `Build Plan` → `My Info`, KEEPING `class="ws-tab active" data-tab="build"` byte-identical (only the text node changes).
- `vault/design/templates/app_view.html` [:458] — change the third tab text `Generate` → `Generate Plan`, KEEPING `class="ws-tab" data-tab="generate"` byte-identical.
- `vault/design/templates/app_view.html` [:461] — rewrite the `#panel-myplan` empty-state copy so it no longer references the old `Build Plan`/`Generate` tab labels (match the mockup's copy: "Your finalized plan appears here once it's generated … Run it from <b>Generate Plan</b>." [mockup:548]) — so the rendered body carries 0 `>Build Plan<` / `>Generate<` text-nodes.

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/serve/test_app_shell.py -q`
Expected: PASS — T7-1 + T7-2 green; T7-3 stays green.

#### Step 3: REFACTOR
Review for:
- The `data-tab="build"` / `data-tab="generate"` slot ids + the `#panel-build` / `#panel-generate` panel ids are byte-UNCHANGED (only the visible label text changed) — confirm the tab-switch handler [:711] still maps `data-tab` → `panel-<data-tab>` and T6's default-active wiring keyed on `data-tab='build'` is intact (the Factory-to-Component Wiring Rule: relabeling the slot id would orphan T6's reference). The mockup's `data-tab="myinfo"` is a cosmetic-id divergence NOT adopted (see Interface Contracts).
- No other rendered-body occurrence of `>Build Plan<` / `>Generate<` survives (grep the whole rendered body, not just the tab region).

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/serve/test_app_shell.py -q`
Expected: PASS (no behavior change).

### Cycle 2: Steps 4-6 — the editable My-Info `/upload` panel + "+ Add documents" + the `_prefill_form` full-set extension (AC2, AC3)

#### Step 4: RED — Write Failing Tests
Edit `tests/serve/test_app_shell.py`: add the My-Info panel cases (reusing `_unlocked_html` + `_panel_build_html`).

- **Case T7-4 — editable My-Info re-de-identifies through `/upload`, no second write path (AC2, crown-jewel STRUCTURAL).** `html = _unlocked_html(tmp_path)`; `panel = _panel_build_html(html)`; assert the My-Info panel renders the full intake sections (assert each section heading present: Demographics / Goals / Training / Diet / Supplements / Medications / Health / Equipment / Documents / API key — match the mockup's `.seclab`s) AND `"<form action='/upload' method='post'>" in panel` AND every `action='...'` in the panel equals `/upload` (extract all `action='([^']*)'` and assert the set == `{"/upload"}` — 0 non-`/upload` write targets). The `action='…'`-set == `{"/upload"}` assertion holds ONLY because the API-key section saves via the EXISTING `fetch('/settings/key')` inline JS ([app_view.html:683](../../../vault/design/templates/app_view.html)), NOT a `<form action='/settings/key'>` — the live panel carries exactly the `/upload` form `action`(s) and the key save is a `fetch`, so the set stays `{"/upload"}` (an SE who ports the API-key step as a `<form action='/settings/key'>` would FALSE-RED this case; the key save MUST stay the `fetch` JS path — see GREEN Step 5). RED: before the reshape, `#panel-build` is the 4-field first-run demographic form (no full intake sections) → the section-heading assertions red.
- **Case T7-5 — "+ Add documents" posts to the existing `/upload` route (AC3).** Over the same panel, assert the `+ Add documents` affordance is present AND no new-route literal (`/add-docs`/`/documents`/`/save-profile`) appears in the panel (the add-docs upload controls reuse the `#screen-team` file-picker → `POST /upload` path). RED: absent before the reshape.
- **Case T7-6 — the My-Info prefill renders saved answers across the full intake set (AC2 prefill leg).** Seed a tmp store with the complete required set PLUS saved rich-section values (e.g. a saved `sex-for-dosing`, a saved `goal-domains`, a saved rich-section raw-source value per the pinned contract); `html = app_shell.render(store.read_all(root))`; assert the My-Info panel renders each saved value back (the `<select>` option pre-SELECTED / the `value=` injected), AND a field with NO saved reading stays blank (no fabricated value). RED: before the `_prefill_form` extension, only the 4 demographic fields prefill → the rich-section saved values are not rendered back.

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/serve/test_app_shell.py -q`
Expected: FAIL — T7-4/T7-5/T7-6 red (the My-Info reshape + the prefill extension do not exist yet); Cycle-1 cases stay green.

#### Step 5: GREEN — Implement
Changes to make:
- `vault/design/templates/app_view.html` `#panel-build` [:463-494] — reshape to the mockup's editable My-Info `ws-docs` panel [mockup:549/581]: the `.mi-note` ("Everything you told us at intake — editable. An edit re-runs the same on-device de-identification path …"), each intake section (Demographics / Goals / Training & activity / Diet / Supplements & peptides / Medications / Health & lifestyle / Equipment / Documents / API key) with its `.seclab` + `.mi-head`/`.mi-edit` Edit toggle, the "+ Add documents" affordance + its `adddocs-panel`, all WITHIN the EXISTING `<form action='/upload' method='post'>` (the field `name=`s per the pinned capture↔form contract — the demographic names + the rich-section names `nutrition-detail`/`supplement-stack`/`peptide-stack`/`training-detail` + the safety/equipment fields). Wire the Edit toggle (`toggleEdit`) to enable the section's fields and the save to POST the `/upload` form (NOT the mockup's static `disabled`-only prototype) — the SAME `persist_capture` → `summarize` boundary, no second write path. PRESERVE the live `#screen-team` upload JS path (the file-picker → `POST /upload`, [:736-746,:822]) for the add-docs controls. Keep all assets inline (`<svg>`, no off-file ref).
- `vault/design/templates/app_view.html` API-key section — the My-Info API-key affordance REUSES the EXISTING `fetch('/settings/key')` inline JS save path ([app_view.html:683,:696](../../../vault/design/templates/app_view.html), the `_pfKeySave` → `pf-key-save` handler), NOT a `<form action='/settings/key'>`; so the My-Info panel's `action='…'` set stays `{"/upload"}` (the AC2/T7-4 crown-jewel assertion). Do NOT introduce a `<form action='/settings/key'>` (it would add a second non-`/upload` write target and FALSE-RED T7-4).
- `vault/design/templates/app_view.html` `<script>` — port the mockup's `toggleAddDocs` [mockup:643] (and `toggleEdit`) inline handlers; port the `.mi-note`/`.mi-head`/`.mi-edit`/`.adddocs-panel` CSS into the `<style>`.
- `vault/design/templates/app_shell.py` `_prefill_form` [:212-255] — EXTEND the prefilled field set from `(_DEMOGRAPHIC_DOB, *_DEMOGRAPHIC_SELECTS)` to the full My-Info intake set: read the latest saved value per My-Info field via `_latest_values` and re-render the field with that value (the same `<select>`-option-select + `value=`-inject discipline), mapping each rich-section field to its saved store item per the pinned capture↔form contract (the default-fed tokens prefill from their saved raw-source item). A field with no saved reading stays blank (no fabricated value — the honest-data discipline). Distinct region from T6's gate branch in `render`; do NOT touch `render`'s gate.

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/serve/test_app_shell.py -q`
Expected: PASS — T7-4 (full sections + only-`/upload` action), T7-5 (add-docs reuses `/upload`), T7-6 (full-set prefill) green; Cycle-1 stays green. RECONCILE any existing `test_app_shell.py` case whose premise T7's `#panel-build` reshape invalidates (e.g. a case asserting `#panel-build` is the objective-only 4-field form, IF T4 did not already reconcile it) — the same-PR reconciliation rule; do NOT weaken a crown-jewel assertion to make it pass.

#### Step 6: REFACTOR
Review for:
- The My-Info edit re-uses the EXISTING `/upload` seam (one `<form action='/upload'>`); there is exactly ONE write target in the panel (`/upload`) — no second POST path, no new route. The Edit toggle is a display-state affordance only (enable/disable), not a separate write surface.
- `_prefill_form` composes the EXISTING `_latest_values` + the established string-replace discipline — no duplicated parser, no bespoke per-field reader; the field→item map is the pinned contract (grounded against capture's `_CHAT_RAW_SOURCE_FIELDS`, not invented). The banner-injection + the blank-when-unsaved (honest-data) behavior is preserved.
- The ported markup keeps every asset inline (the lucide `<svg>` ports, no off-file `<img src>`), so the inline-asset gate (Cycle 3) stays clean.

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/serve/test_app_shell.py -q`
Expected: PASS (no behavior change).

### Cycle 3: Steps 7-9 — the Generate-Plan tab (AC4) + the inline-asset gate + the full regression (AC6, AC7, EXTEND-NOT-REBUILD)

#### Step 7: RED — Write Failing Tests
Edit `tests/serve/test_app_shell.py`: add the Generate-Plan + inline-asset cases.

- **Case T7-7 — Generate-Plan two buttons + per-specialist `.bar`, honest-empty (AC4).** `html = _unlocked_html(tmp_path)`; assert the `#panel-generate` static markup carries BOTH `>Generate plan<` AND `>Update plan<` button labels + a `.gen-roster` container; assert the `renderGenerate` JS source emits a per-specialist row carrying `class="bar"` over `SPECIALISTS` (the roster template string is present in the rendered body's `<script>`); assert NONE of the `_FABRICATED` demo strings appear AND the roster `.bar` widths are honest-empty (`width:0%`, no fabricated progress). RED: the live `#panel-generate` is empty + `renderGenerate` emits ONE button + `.agent-prog` (not `.bar`) → the "two buttons" + "per-specialist `.bar`" assertions red.
- **Case T7-8 — inline-asset render gate over the UNLOCKED body (AC6).** Seed the complete store at `_root`, then `path = _emit_app(tmp_path)` (the REAL `generate.run('app')` emitting the UNLOCKED body); assert `isinstance(path, Path)` AND `path.exists()` (T7's unlocked-shell additions carry no off-file asset → `render.emit` does not raise). RED-capable: the existing negative probe `test_offfile_ref_makes_emit_raise` proves the gate is real; an off-file ref in T7's markup would raise here.

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/serve/test_app_shell.py -q`
Expected: FAIL — T7-7 red (one button / `.agent-prog`); T7-8 green-from-start IF T7's markup is inline-clean (it reds if any ported asset is off-file — a real check during GREEN).

#### Step 8: GREEN — Implement
Changes to make:
- `vault/design/templates/app_view.html` `#panel-generate` [:495] — populate with the mockup's static surface [mockup:550/582]: the two buttons (`<button class="btn primary">Generate plan</button>` + `<button class="btn ghost">Update plan</button>`), the explanatory note, the "Care-team build status" `.seclab`, and the `.gen-roster` container.
- `vault/design/templates/app_view.html` `renderGenerate`/`agentRow` [:723-734] — PORT the mockup's `renderGenerate` [mockup:674]: build the `.gen-roster` from `SPECIALISTS` with a per-specialist `.genrow` row EACH carrying a `<div class="bar"><i style="width:0%"></i></div>` (HONEST-EMPTY — drop the mockup's fabricated `ST` percentages; every `.bar` is `width:0%` / queued). Keep the tab-switch handler's `if(t.dataset.tab==='generate') renderGenerate()` wiring [:712] intact. Port the `.gen-roster`/`.genrow`/`.gstat`/`.gm`/`.gtop`/`.gn` CSS into the `<style>` (the `.bar`/`.spin` CSS already exists). Remove the now-dead single-button `gen-run` path + the `.agent-prog`/`agentRow` if no longer referenced (or repurpose `agentRow` to the `.bar` row).

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/serve/test_app_shell.py -q`
Expected: PASS — T7-7 (two buttons + per-specialist `.bar` + honest-empty) green; T7-8 (inline-asset gate) green; all Cycle-1/2 cases green.

#### Step 9: REFACTOR + REGRESSION
Review for:
- `renderGenerate` renders HONEST-EMPTY (no `_FABRICATED` demo data, every `.bar` `width:0%`) — the ADR-0029 honest-data discipline; the ported CSS is the mockup's verbatim layout (the visual-verification mandate), not re-authored.
- The Generate panel is the ONLY new client-side render; `REVIEW_AGENTS`/`SPECIALISTS`/`tile`/`dotFor` are reused, not duplicated.

Run the full `tests/serve/test_app_shell.py` + the full pytest floor + the EXTEND-NOT-REBUILD gate:
```
OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/serve/test_app_shell.py -q
OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest -q
WAVE_BASE=$(git merge-base HEAD origin/main)
git diff --numstat "$WAVE_BASE"..HEAD -- scripts/plan/orchestrate.py scripts/plan/pipeline.py scripts/plan/assemble.py scripts/plan/generate_plan.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/track.py 'scripts/store/*.py'
git diff --numstat "$WAVE_BASE"..HEAD -- scripts/serve/server.py scripts/serve/capture.py
git diff --name-only "$WAVE_BASE"..HEAD
```
Expected: `test_app_shell.py` all green; full suite green (port 8765 free; or EXACTLY the 2 named env failures with the port held); the frozen-seven + `scripts/store/*` → 0 changed rows; `server.py` + `capture.py` → 0 T7 rows (the edit + add-docs reuse `/upload`; capture is T1/T2/T3's); `git diff --name-only` for T7's commit lists ONLY `vault/design/templates/app_view.html`, `vault/design/templates/app_shell.py`, `tests/serve/test_app_shell.py` (no python production file beyond `app_shell.py`'s `_prefill_form` region; `scripts/plan/router.py` unchanged from W2).

**TDD step count: 9 (3 cycles).** Within the ≤10-step guidance. T7's 7 ACs (1, 2, 3, 4, 5, 6, 7) span the tab rename + the opens-on-My-Info LABEL + the editable My-Info `/upload` panel + add-documents + the Generate-Plan roster + the inline-asset gate + the full-suite pass, none splittable across waves (T7 is the single Wave-5 unlocked-shell task). Two Modify files (app_view.html markup/JS + app_shell.py's `_prefill_form` region) + one Modify test file; 0 new python production code, 0 new route.

## Interface Contracts

T7 CONSUMES T4's wizard structure + reworked `_prefill_form` and renders behind T6's gate/unlock branch; it DEFINES the unlocked-shell surface contract the served body promises (the Architect-reviewed seam) + the rendered markup T9's E2E composes. It edits NO python production surface beyond `app_shell.py`'s `_prefill_form` region; it adds NO server route (EXTEND-NOT-REBUILD).

- **The unlocked-shell surface (DEFINED — the Architect-reviewed contract).**
  - **Workspace tab labels (PINNED):** the right-panel tabs are `My Plan | My Info | Generate Plan`; 0 occurrences of the old `Build Plan` / bare `Generate` tab labels in the rendered body (AC1). The slot ids (`data-tab="myplan"`/`"build"`/`"generate"`) + panel ids (`#panel-myplan`/`#panel-build`/`#panel-generate`) are STABLE — only the visible label text changes.
  - **`data-tab='build'` slot stability (load-bearing — the Factory-to-Component Wiring Rule).** The My-Info slot KEEPS the stable `data-tab="build"` id; T6 (W4) set that slot default-active keyed on the stable id (build-plan T6 crit 5 / QA-F1). The mockup's literal `data-tab="myinfo"` is a cosmetic-id divergence DELIBERATELY NOT adopted: the `data-tab` value is an internal wiring id (not a user-visible element of the operator-signed-off VISUAL — the operator signed off on the tab LABEL "My Info", not the attribute value), and relabeling the slot id would orphan T6's default-active reference + the tab-switch handler's `panel-<data-tab>` mapping ([app_view.html:711](../../../vault/design/templates/app_view.html)). FLAGGED for the Architect: T7 renders the signed-off VISUAL with `data-tab="build"` preserved; AC5 asserts the active `data-tab='build'` slot's text is now `My Info`.
  - **The editable My-Info de-id boundary (PINNED — crown-jewel).** The My-Info edit re-submits through the EXISTING `<form action='/upload' method='post'>` — the SAME `persist_capture` → `summarize` path; there is NO second write target in the panel (every `action='...'` == `/upload`), so no edit bypasses de-identification (AC2). The "+ Add documents" affordance reuses the same `/upload` flow (AC3) — no new route.
  - **The Generate-Plan roster (PINNED):** two buttons ("Generate plan"/"Update plan") + the full `SPECIALISTS` roster, each specialist row carrying a `.bar` status element rendered HONEST-EMPTY (`width:0%`, no fabricated progress — ADR-0029 honest-data) (AC4).
- **SEC-F1 coupling — the BEHAVIORAL no-bypass proof is T9's (flag for reviewers).** This W5 task grounds the editable-My-Info no-bypass STRUCTURALLY (grep the edit-form action == `/upload`, 0 non-`/upload` write targets). The BEHAVIORAL proof — drive a raw value (a new raw drug name / a changed full DOB) THROUGH the My-Info edit `/upload` re-submit → `persist_capture` → `summarize` → `dispatch` and assert 0 raw crossing in the `dispatch` payload + every model request — is the Security-reviewed T9 E2E probe (W7, build-plan T9 crit 3 / SEC-F1). The crown-jewel edit-path de-id is thus PROVEN end-to-end at T9, not only structurally grepped at this lean QA+Architect wave; T7's structural gate is the W5 half of that paired contract.
- **CONSUMES T4 + T6 (not a new contract).** T7 reuses T4's reworked `_prefill_form` (extending its field set, not redefining its interface) + T4's pinned capture↔form field-name contract (the My-Info field `name=`s), and renders inside T6's unlock branch (the unlocked body T6 serves on a complete profile). A change to T6's unlock branch or T4's `_prefill_form` signature is a contract change requiring Architect review at the seam.
- **Frozen surface (PRESERVED).** Every `scripts/plan/*.py` frozen engine, `scripts/store/*`, `scripts/serve/server.py` (no new route), `scripts/serve/capture.py` (T1/T2/T3's), and `scripts/plan/router.py` (T2's, unchanged from W2) are byte-unchanged by T7 (NFR-2) — T7 is the markup + `_prefill_form`-region task, not a route or engine edit.

## Verification Checklist

- [ ] AC1 (tab rename, 0 old labels) — verified by Case T7-1: over the UNLOCKED render, `data-tab="build">My Info<` + `data-tab="generate">Generate Plan<` present AND `'>Build Plan<' not in html` + `'>Generate<' not in html` (the tab text-nodes + the `#panel-myplan` stale copy both fixed; failing-capable: revert either → reds).
- [ ] AC2 (editable My-Info re-de-identifies through `/upload`, no second write path) — verified by Case T7-4 (the full intake sections editable + `<form action='/upload' method='post'>` + every panel `action` == `/upload`, 0 non-`/upload` targets) + Case T7-6 (the full-set prefill renders saved answers, blank-when-unsaved). The BEHAVIORAL no-bypass proof is T9's SEC-F1 E2E probe (referenced in Interface Contracts).
- [ ] AC3 (add documents posts to `/upload`) — verified by Case T7-5: the `+ Add documents` affordance present + 0 new-route literal in the panel (reuses the existing `/upload` flow).
- [ ] AC4 (Generate-Plan tab — two buttons + per-specialist `.bar`, honest-empty) — verified by Case T7-7: `#panel-generate` carries `>Generate plan<` + `>Update plan<` + `.gen-roster`; `renderGenerate` emits a per-specialist `class="bar"` row over `SPECIALISTS`; 0 `_FABRICATED` strings + `width:0%` (honest-empty).
- [ ] AC5 (opens-on-My-Info LABEL — the deferred QA-F1 assertion) — verified by Case T7-2: the active `.ws-tab` text == `My Info` AND `#panel-build` is the active panel (the same stable `data-tab='build'` slot T6 set default-active in W4; failing-capable: pre-relabel the active slot reads `Build Plan` → reds).
- [ ] AC6 (inline-asset render gate over the UNLOCKED body) — verified by Case T7-8: `generate.run("app")` over a complete (unlocked) store returns a written `Path` that EXISTS (no off-file `ValueError`); the existing negative probe `test_offfile_ref_makes_emit_raise` proves the gate is the real emit, not a substring grep.
- [ ] AC7 (the test file + full suite pass) — verified by `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/serve/test_app_shell.py -q` (all cases incl. T7's) AND `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest -q` green (port 8765 free, or EXACTLY the 2 named env failures with the port held).
- [ ] Non-tautological / render-the-unlocked-shell — verified by Case T7-3 (the negative control): the SAME assertions over the LOCKED `_spa_html()` body find NO workspace tabs (T7's cases test the unlocked render T6 serves on a complete profile, not a constant).
- [ ] NFR-2 (EXTEND-NOT-REBUILD — markup + `_prefill_form` region only, NO new route, frozen engine untouched) — verified by the SHELL gate (a standing INVARIANT run in REGRESSION; `<wave-base>` = `$(git merge-base HEAD origin/main)`): the frozen-seven + `scripts/store/*` → 0 rows; `scripts/serve/server.py` + `scripts/serve/capture.py` → 0 T7 rows; `scripts/plan/router.py` unchanged from W2; `git diff --name-only` for T7's commit lists ONLY the 3 manifest files.
- [ ] NFR-3 (0 live spend) — every T7 case runs against the rendered-HTML string / a fixture `store_read` over tmp roots; `generate.run('app')` is a local render+write (0 network); no `key_source.resolve()`-gated or `converse`/curation call, no `aplus-research` dispatch.
- [ ] Wave-5 → Wave-6 checkpoint Go/No-Go gates carried (build plan § Wave 5 → Wave 6 Boundary): tab-rename [AC1] + editable-My-Info-no-bypass-structural [AC2] + add-documents [AC3] + Generate-roster-`.bar` [AC4] + opens-on-My-Info-LABEL [AC5, the deferred QA-F1] + inline-asset-gate [AC6], all green; `test_app_shell.py` + full suite green; frozen-engine numstat 0 + `server.py` numstat 0 (no new route); 0 live spend.
- [ ] Scope-discipline check: `git diff --name-only "$WAVE_BASE"..HEAD` for T7's commit lists ONLY the 3 manifest files; any other file needs a written justification (the daemon `vault/**` churn is NOT staged; the `.html`/`.py` manifest targets under `vault/design/templates/` are staged by explicit path).

## Commit

```
feat(serve): T7 unlocked shell + My Info + roster
```
Stage only:
- `vault/design/templates/app_view.html`
- `vault/design/templates/app_shell.py`
- `tests/serve/test_app_shell.py`

Stage by explicit path: run `git add <path>` as its OWN command, then `git commit` SEPARATELY — the `block-pii-commit` PreToolUse hook denies single-call stage+commit, `git commit -a/--all`, and pathspec `git commit <path>` (their content is invisible to the pre-command staged-set scan). The manifest targets under `vault/design/templates/` are `.html`/`.py` (NOT the gated `vault/{compounds,biomarkers,library}/*.md` entity pages — `block-ungated-vault-write.sh` does not apply), but stage them by EXPLICIT PATH regardless; never `git add vault/` blindly (the daemon frontmatter churn re-appears every sync). Do NOT stage `.beads/issues.jsonl` / `harvest.jsonl` / `memory/process-failures.md` — those are close-protocol artifacts. Commit on `feature/intake-build` (never `main`).

## Rollback

If verification fails after 3 fix cycles:
1. `git stash -m "failed-ADR-0033-0035-T7-attempt-{N}"` — preserve work for diagnosis.
2. `git checkout {commit-before-task}` — revert to the entry-state commit (the branch HEAD this task was built on top of).
3. Update the bead tracker: flag ADR-0033-0035-T7 as BLOCKED with the failure log.
4. Escalate: "Task ADR-0033-0035-T7 failed verification after 3 attempts. Stash ref: {ref}. Failure log: {summary}."
5. After abandonment or re-planning, drop all `failed-ADR-0033-0035-T7` entries from `git stash list`.

Do NOT: silently skip a failing acceptance criterion; weaken a test to make it pass (especially the editable-My-Info-no-bypass STRUCTURAL gate [AC2] — a second non-`/upload` write path that skips de-identification is a crown-jewel breach; the SEC-F1 BEHAVIORAL companion is T9's, not a license to relax the W5 structural floor — HALT); relabel the `data-tab="build"` / `data-tab="generate"` SLOT ids or rename the `#panel-build` / `#panel-generate` panel ids (it orphans T6's default-active wiring + the tab-switch handler — HALT, relabel only the visible text); render the Generate roster with the mockup's fabricated demo percentages (the ADR-0029 honest-data discipline rejects it — HALT, render `width:0%`); add a server route or a second capture/write path for the edit or add-docs (they reuse the EXISTING `/upload` — the no-new-route guarantee rejects it, EXTEND-NOT-REBUILD — HALT); or edit any `scripts/plan/*.py` / `scripts/store/*` / `scripts/serve/server.py` / `scripts/serve/capture.py` / `scripts/plan/router.py` (the EXTEND-NOT-REBUILD breach NFR-2 rejects — HALT, confine to the markup + `app_shell.py`'s `_prefill_form` region + the test file).

## Report

- Recipe: `docs/task-plan/.pipeline/adr-0033-0035/draft-ADR-0033-0035-T7.md` (status: draft).
- Every ADR-0033-0035-T7 acceptance criterion (AC1..AC7 — the spec's crit 1-7) maps to a named Verification-Checklist test/command + the Wave-5 → Wave-6 checkpoint gate; the crown-jewel + non-tautological RED tests are failing-capable by construction — tab-rename [AC1/T7-1] (old labels present pre-relabel → reds), editable-My-Info-no-bypass-structural [AC2/T7-4] (a second non-`/upload` write target reds), opens-on-My-Info-LABEL [AC5/T7-2] (active slot reads `Build Plan` pre-relabel → reds), Generate-roster `.bar` [AC4/T7-7] (live `.agent-prog`/single-button reds), and the render-the-unlocked-shell negative control [T7-3] (the locked body has no workspace tabs) — never tautological.
- GROUNDED against the live tree (`feature/intake-build`, fork-point `$(git merge-base HEAD origin/main)` = `12f769f`, 2026-06-30): the workspace tabs `My Plan | Build Plan | Generate` [app_view.html:456-458]; `#panel-build` `<form action='/upload'>` [:463-494,:465]; `#panel-generate` empty [:495]; `renderGenerate`/`agentRow`/`REVIEW_AGENTS`/`SPECIALISTS` [:723-734,:718-722,:528-546]; the tab-switch handler [:708-714]; the `.bar`/`.spin` CSS present, `.gen-roster`/`.genrow` ABSENT (mockup-only) [:131-132,:127]; `_prefill_form`/`_latest_values`/`render` [app_shell.py:212-255,:185-209,:258-288]; the test harness `_seed_demographics`/`_panel_build_html`/`app_shell.render(store.read_all(...))` [test_app_shell.py:857,:443,:883]; the mockup's unlocked shell [mockup:547-550,:579-582,:643,:674] — all VERIFIED. (NB the live anchors are the PRE-T4/T6 tree; T7 builds on the post-T4/T6 state, which the Entry State re-confirms live.)
- ZERO literal implementation code in the recipe — every change is described as behavior + contract + the convention/mockup region to match, with cited `file:line` seams; the SE authors the markup/JS/markdown under TDD.
- **DESIGN DECISION flagged (non-blocking, for the wave reviewers): the mockup's `data-tab="myinfo"` slot id is DELIBERATELY NOT adopted.** The build plan (T6 crit 5 / QA-F1) pins the My-Info slot to the STABLE `data-tab="build"` id that T6 (W4) set default-active; the `data-tab` value is an internal wiring id, not a user-visible element of the operator-signed-off VISUAL (the operator signed off on the tab LABEL "My Info"). T7 renders the signed-off visual with `data-tab="build"` preserved (relabeling the slot id would orphan T6's default-active reference + the tab-switch handler's `panel-<data-tab>` mapping — the Factory-to-Component Wiring Rule). AC5 asserts the active `data-tab='build'` slot's text is `My Info`. Recorded in Interface Contracts.
- **FIDELITY-vs-FUNCTION reconciliation flagged (non-blocking):** the mockup's editable My-Info is a STATIC prototype (`disabled` inputs + a `toggleEdit` enable toggle) and its `renderGenerate` carries fabricated `ST` demo percentages. T7 ports the mockup's STRUCTURE verbatim (the visual-verification mandate) but (a) wires the My-Info edit to the REAL `<form action='/upload'>` re-submit (the function AC2 demands — the same `persist_capture` → `summarize` boundary, no second write path) and (b) renders the Generate roster HONEST-EMPTY (`width:0%`, 0 `_FABRICATED` strings — the ADR-0029 honest-data discipline). Recorded in the TDD steps + Rollback.
- **CROSS-TASK reconciliation noted (non-blocking):** T7's `#panel-build` reshape (4-field first-run form → editable My-Info full intake set) may invalidate the premise of existing `test_app_shell.py` cases that assert `#panel-build` is the objective-only demographic form (e.g. the form-name-set / objective-only cases) IF T4 did not already reconcile them when it built the comprehensive wizard. T7 reconciles any such case in `test_app_shell.py` (its owned file) under the same-PR reconciliation rule and the REGRESSION step proves the full suite green; the exact reconciliation set is grounded at Entry State against the post-T4/T6 tree, never weakening a crown-jewel assertion.
- No blocking spec defect found — the spec T7 block, the build-plan Wave-5 → Wave-6 checkpoint, and the signed-off mockup are mutually consistent (the only tension, the mockup `data-tab` id, is resolved by the build plan's explicit stable-slot mandate, handled above). T7 proceeds.
