---
task-id: ADR-0044-T3
source-spec: docs/spec/adr-0041-0046-comprehensive-plan-spec.md
source-build-plan: docs/build-plan/build-plan-adr-0041-0046.md
wave: 4
assigned-agent: SE
reviewers: [QA]
created: 2026-07-12
status: approved
---

# Recipe: Re-base the ADR-0038 Horizon Composition onto the Comprehensive Periodization

> Wave-4 Tier-2 task (Dependencies: ADR-0044-T1 — the comprehensive model's first-class periodization
> `read_plan_version`/`resolve_comprehensive`, merged to `main` Wave 2; ADR-0044-T2 — the mixed-history
> reader `read_standing_plan`, landed on the wave-4 branch Wave 3). It RE-BASES the ADR-0038 horizon
> date-range composition in `scripts/plan/horizons.py`: today `window_block` (`:132`) does a RANGE query over
> the FLAT `plan::<domain>` dated history (`store.read("plan::<domain>")` + `plan_confirm.filter_confirmed`,
> latest-in-window). After the re-base, when a COMPREHENSIVE plan stands for the render date and covers the
> domain, `window_block` reads that plan's FIRST-CLASS periodization (`prescription["blocks"]`, dated blocks)
> and applies window membership to the BLOCKS' dates — the flat dated-history range is RETIRED for the
> comprehensive case. A PRE-MIGRATION thin-only store (no comprehensive version) still resolves through the
> mixed-history reader's thin fallback (the retained flat range-query) — 0 crashes, existing behavior preserved.
> `read_horizons`'s `today` slot re-sources to the same comprehensive-wins reader; `assess_pace` re-sources
> TRANSITIVELY through `window_block` (no direct edit).
>
> READ-ONLY COMPOSITION — NOT a store surface, NOT a frozen-spine supersession. `horizons.py` is a pure
> read/query layer: it reads through `store.read` / the existing resolvers, WRITES no store stream, and defines
> no `::`-prefixed item id (the module's own invariant, `:5-12`). It is NOT one of the `<always-frozen>` six
> (`store.py`/`keying.py`/`pipeline.py`/`adjudicate.py`/`adjust.py`/`router.py`) and imports NONE of them except
> the `store` read-primitive (used read-only). Numstat expectation: `scripts/plan/horizons.py` NON-empty (the
> re-base); the `<always-frozen>` six numstat=0 vs `origin/main`. No store-adversarial battery (bead `pka`) and
> no per-ADR freeze-break numstat AC apply here (this task supersedes no frozen surface). Agent matrix (build
> plan `:181`): SE / QA only — "no new contract, no store/de-id/safety surface."
>
> PF-S130-01 (the tautological-swap catch — MANDATORY). The horizon composition SWAP (flat-history →
> comprehensive periodization) carries a PRODUCTION-PATH integration test on the REAL `window_block` /
> `read_horizons` over a seeded store, with an INPUT that distinguishes the new (comprehensive-periodization)
> read from the old (flat-history) read — a comprehensive plan whose in-window block gives a DIFFERENT answer
> than a coexisting flat `plan::<domain>` DECOY reading, and a comprehensive-only store with NO flat reading at
> all. The test REDs on a revert of the re-base (drop the comprehensive-first branch → `window_block` returns
> the flat decoy / None). A test that passes whether or not the re-base happened is tautological and does NOT
> satisfy AC-1. The "0 reads of the retired flat history" clause is falsifiable: on a comprehensive-standing
> store the answer is UNCHANGED when the flat `plan::<domain>` stream is absent.
>
> PF-S131-01 (fixture shape — MANDATORY). Every `domain_program` prescription fixture uses the CANONICAL
> PERIODIZED shape (`{"prescription": {"blocks": [{"date", "phase", "detail"}]}}`), NEVER a flat payload — the
> comprehensive periodization IS the dated-blocks shape, so the horizon window reads the blocks' `date`s. Reuse
> the `_training_program` / `_compound_program` / `_comprehensive_version` builders proven in
> `tests/store/test_plan_model.py` (`:36-115`), recorded via `plan_model.record_plan_version`. The thin decoy
> uses the flat `{"exercises":[...]}` shape (`tests/plan/test_horizons.py::_workout_plan`) — structurally
> distinguishable from the comprehensive `{"blocks":[...]}`, the input that makes AC-1/AC-2 non-tautological.
>
> PF-S132-02 (worktree hygiene — verification guidance). The Step-2.5 revert-RED falsifiers MUTATE
> `horizons.py`; apply → observe RED → REVERT on the feature branch working tree, confirming NO falsifier edit
> remains before commit. Any REVIEW/verify agent that re-runs a falsifier by mutating source MUST use an
> ISOLATED git worktree (`git worktree add`), never mutate-and-restore the main checkout — the basic-memory
> daemon watches the main checkout and a mutate-and-restore there risks a daemon-churn commit (PF-S132-02).

## Entry State

**Prerequisites** (ADR-0044-T3 depends on ADR-0044-T1 + ADR-0044-T2 — Dependency Map `ADR-0044-T1 --> ADR-0044-T3`
[the first-class periodization it reads] + `ADR-0044-T2 --> ADR-0044-T3` [AC-3 thin-only back-compat resolves
via T2's mixed-history reader]):

- [ ] Task ADR-0044-T1 present (comprehensive model + resolver, merged to `main` Wave 2): `grep -qE "^def read_plan_version" scripts/store/plan_model.py && grep -qE "^def resolve_comprehensive" scripts/store/plan_model.py && grep -q '_PREFIX_MODEL = "plan-model::"' scripts/store/plan_model.py && echo OK` (the comprehensive resolver + `DOMAIN_PROGRAMS` composite constant + `plan-model::` stream the re-base reads).
- [ ] Task ADR-0044-T2 present (the mixed-history reader, landed on the wave-4 branch Wave 3): `grep -qE "^def read_standing_plan" scripts/store/plan_model.py && echo OK`. **Grounding note (not a defect):** 0044-T1 is on `main`; 0044-T2's `read_standing_plan` is the +39-line delta on `feature/comprehensive-plan-wave-4` vs `main` (`git diff --stat main -- scripts/store/plan_model.py` shows `plan_model.py` +39). Both are present in the wave-4 checkout — the normal wave-stacking (Wave 4 builds atop Wave 3 on the branch, before Wave 3's own merge to `main`). Ground the prerequisite by SYMBOL presence, not a hash.
- [ ] `read_standing_plan` returns the comprehensive prescription on the comprehensive-wins branch: `grep -q "domain_program.PRESCRIPTION" scripts/store/plan_model.py && echo OK` (the `version["domain_programs"][domain][PRESCRIPTION]` the block-window reads).
- [ ] The thin resolver the fallback reuses is retained: `grep -qE "^def read_plan" scripts/store/plan_schema.py && grep -q "PLAN_DOMAINS =" scripts/store/plan_schema.py && grep -q "TRACKED_DOMAINS =" scripts/store/plan_schema.py && echo OK` (`plan_schema.read_plan` date-equality + `PLAN_DOMAINS`/`TRACKED_DOMAINS` the composition iterates).
- [ ] Full test suite at the entry baseline floor: `.venv/bin/python -m pytest -q` shows ONLY the two PRE-EXISTING env-sensitive failures — `tests/serve/test_bind.py::test_port_collision_exits_nonzero_fail_loud` and `tests/serve/test_server.py::test_main_reads_no_stdin` (OS-port/stdin cases, NOT this task's to fix). The gate is **no-NEW-failure**; a 3rd distinct failure is a regression → HALT. Record the exact passed/skipped counts at entry (~263 passed observed on the branch — env- and wave-sensitive; do NOT hardcode a number).
- [ ] Working tree clean for the task scope: `git status --short` shows no STAGED changes and no uncommitted changes to `scripts/plan/horizons.py` / `tests/plan/`. ("Clean" = no STAGED path + the Create target absent. The basic-memory daemon perpetually re-churns `vault/**/*.md` frontmatter — S90; that churn + any parked `??` files are EXPECTED noise, NOT this task's changes. Do NOT stage them.)
- [ ] On a feature branch cut off `main`: `feature/comprehensive-plan-wave-4` (or equivalent `feature/*`) — never `main`.
- [ ] Rollback target recorded: `PRE_TASK_HEAD = git rev-parse HEAD` at task entry — the `{commit-before-task}` the Rollback Protocol reverts to.
- [ ] The Create target is ABSENT (Create-only, no stale/duplicate artifact): `test -e tests/plan/test_horizons_rebase.py && { echo "PRESENT (stop)"; exit 1; } || echo "ABSENT (Create OK)"`.
- [ ] `horizons.py` does NOT yet read the comprehensive model (this task adds it): `grep -q "plan_model" scripts/plan/horizons.py && { echo "PRESENT (stop — already re-based?)"; exit 1; } || echo "ABSENT (re-base OK)"`.
- [ ] The `<always-frozen>` six byte-frozen at entry (numstat==0 vs `origin/main`): `git diff --numstat origin/main -- scripts/store/store.py scripts/store/keying.py scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/router.py` prints nothing. `horizons.py` reads `store.read` unchanged and edits none of the six.

**Intra-wave coordination note — the co-wave `PLAN_DOMAINS` growth (Architect RULING D1).** ADR-0043-T3 (the SAME Wave 4, file-disjoint — `plan_schema.py`/`plan_driver.py`/`generate_plan.py`/… vs this task's `horizons.py`; build-plan `:128`) grows `plan_schema.PLAN_DOMAINS` 4→§1-§13 (≥13; 0043-T3 AC-8), and `TRACKED_DOMAINS` STAYS the three (grounded: 0043-T3's manifest grows the dispatch registries only, never `TRACKED_DOMAINS`). `PLAN_DOMAINS` is the constant `horizons.py:318` (`domain_horizons`, LEFT UNCHANGED by this task — Deviation #2) iterates.

- **Build order — which case applies.** Architect RULING A sequences `0044-T4 → 0043-T3` within Wave 4, and 0043-T3 is the atomic roster-growth + front-door swap that co-lands the SHARED frozen-guard reconciliation, so it builds LAST in the wave. This task (0044-T3) is a Wave-4 TERMINAL LEAF — 2 waves slack, no downstream dependency, no dependency edge to 0043-T3 (build-plan `:280`) — so under the RULING-A ordering it builds BEFORE 0043-T3's growth lands. So **during THIS task's own RED/GREEN, `PLAN_DOMAINS` is EXPECTED to be the closed four** (`grep '^PLAN_DOMAINS = ("workout", "nutrition", "supplements", "peptides")' scripts/store/plan_schema.py` — informational, NOT a hard gate) — its fixtures use closed-four domains and NO grown-roster fixture is required for its own tests to pass (Option 2 for the build order). The build is nonetheless order-ROBUST: because there is no hard edge pinning 0044-T3 before 0043-T3, IF the grown roster were already present (0043-T3 built first), this task's closed-four fixtures still pass as a SUBSET of the grown roster and AC-D1 proves the tolerance — so neither order breaks the build.
- **BUT the Wave-4→Wave-5 checkpoint co-locates both** (build-plan `:130` — every Wave-4 AC asserted together, both tasks landed), so at wave close `PLAN_DOMAINS` IS grown and this task's committed `horizons.py` must remain correct over the grown roster. It IS growth-tolerant BY CONSTRUCTION, and AC-D1 pins it so the co-wave growth cannot silently regress the file at the checkpoint: (a) `domain_horizons` (`:318`, unchanged) skips any domain absent from `TRACKED_DOMAINS` (unchanged at three) and any absent-plan resolve, so a grown-roster domain with no plan contributes nothing; (b) the re-based per-domain `window_block`/`read_horizons`/`assess_pace` forward `domain` to `plan_model.read_standing_plan` → `plan_schema.read_plan`, which ACCEPTS any grown-`PLAN_DOMAINS` member and returns `NO_PLAN` for a planless one (`read_plan` raises `ValueError` ONLY for a domain OUTSIDE `PLAN_DOMAINS`; a grown member is inside → no raise). So this task's re-base is absence-tolerant over the grown roster — a domain with no comprehensive periodization yet resolves None/absent, never a crash. Both cases therefore apply at different points: closed-four at build time, growth-tolerant at the shared checkpoint.

## Context Load List

**Files to read (this task's file manifest + the reader interface it re-bases onto + the existing tests it must keep green):**

- `scripts/plan/horizons.py` — the module this task RE-BASES. `window_block(domain, root, on_date, span_days)` (`:132`, the flat `plan_confirm.filter_confirmed(store.read("plan::<domain>"))` + `_in_window` + latest-in-window range query — the surface the re-base replaces with the comprehensive block-window); `_in_window(timepoint, on_date, span_days)` (`:111`, the boundary-inclusive membership predicate REUSED unchanged over block dates); `read_horizons(domain, slug, root, on_date)` (`:167`, composes `today`/`week`/`month`/`milestone` — the `today` slot [`:190`, currently `plan_schema.read_plan`] re-sources to the comprehensive-wins reader; `week`/`month` re-source transitively via `window_block`); `assess_pace` (`:263`, reads `window_block(...)`'s `week` block extras — re-sources TRANSITIVELY, no direct edit); `plan_extras`/`HORIZON_EXTRAS` (`:36-56`, the week-framing extras + its empty-dict graceful floor); `WEEK_SPAN_DAYS`/`MONTH_SPAN_DAYS` (`:23-24`); `domain_horizons` (`:297`, the render-date-plan tracking gate — OUT of the re-base scope, see Deviation #2).
- `scripts/store/plan_model.py` — the reader interface the re-base READS THROUGH (READ-only, do NOT edit). `read_standing_plan(domain, on_date, root)` (`:248`, the ADR-0044-T2 mixed-history reader: comprehensive-wins [state None, `plan` = the domain's PRESCRIPTION `{"blocks":[...]}`] ELSE the thin `plan_schema.read_plan` verbatim; returns the `read_plan` shape `{state, plan, specialist, plan_date}`); `read_plan_version(on_date, root)` (`:235`) + `resolve_comprehensive` (`:207`); `DOMAIN_PROGRAMS`/`NO_PLAN`/`NO_PLAN_TODAY` (`:61-68`). The re-base COMPOSES this reader — it does not re-implement the comprehensive-vs-thin precedence or the store I/O.
- `scripts/store/plan_schema.py` — the thin-side surfaces the fallback preserves (READ-only reuse, do NOT edit): `read_plan(domain, on_date, root)` (`:514`, date-equality thin resolve) + `resolve_plan` (`:469`); `PLAN_DOMAINS`/`TRACKED_DOMAINS` (`:49-50`); `NO_PLAN`/`NO_PLAN_TODAY` (`:45-46`). `window_block`'s thin fallback keeps the existing `plan_confirm.filter_confirmed(store.read("plan::<domain>"))` range query (retained, not this reader) — see Deviation #1.
- `scripts/plan/domain_program.py` — the DOMAIN PROGRAM contract: `PRESCRIPTION = "prescription"` (`:68`, the periodized `{"blocks":[...]}` field the comprehensive branch reads); `validate` checks PRESCRIPTION PRESENCE only — the `blocks` shape is a downstream-consumer contract (`:14`, `{"blocks":[{date, ...}]}`), so the block-extraction reads `blocks` with the graceful empty floor (consistent with `plan_extras`). READ-only.
- `scripts/store/plan_confirm.py` — `filter_confirmed(readings, domain, root)` (`:102`, the confirmed-plan filter the RETAINED thin fallback keeps). READ-only.
- `tests/plan/test_horizons.py` — the EXISTING ADR-0038 horizon tests that MUST stay GREEN (the regression proof the re-base preserved thin-store back-compat): the flat-range cases `test_latest_in_window_selection` (`:348`, two thin plans 06-05/06-08 read 06-10 → `week.plan_date == "2026-06-08"` latest-in-window), `test_window_boundary_inclusive` (`:368`, inclusive far-edge + out-of-window → `week is None`), `test_four_horizon_composition` (`:320`, incl. `items == {"goal::bench", "plan::workout"}` — NO new store stream), `test_today_by_date_equality` (`:387`, `today` date-equality); the `_workout_plan` `{"exercises":[...]}` / `_enriched_workout_plan` fixture convention to MIRROR for the thin decoy; `_in_window` boundary case (`:539`). These seed THIN `plan::<domain>` via `plan_schema.record_plan` — the thin fallback preserves their behavior.
- `tests/store/test_plan_model.py` — the comprehensive-fixture builders to REUSE (PF-S131-01): `_training_program()` / `_compound_program()` (`:36-75`, the canonical periodized `{"prescription": {"blocks": [{"date","phase","detail"}]}}`) + `_comprehensive_version(...)` composite + the `record_plan_version` seed pattern + `test_no_operator_identity_literal_in_test_tree` (the 0-real-PII grep to mirror). Do NOT re-invent the composite shape.

**Do NOT load:**
- The ADRs (`docs/adr/ADR-0038-*.md`, `ADR-0044-*.md`) — the spec already distilled them; re-reading re-opens resolved trade-offs (TP-02 drift). ADR-0038 is the composition being re-based; its behavior is captured by `test_horizons.py` + the spec AC block.
- The other ADR-0044/0043/0046 task blocks + their manifests (the confirm-hold skip on the resolver = 0044-T4; the synthesize + front-door = 0043-T3) — out of THIS task's scope. This recipe covers ONLY the `### ADR-0044-T3` block (`horizons.py` re-base + `test_horizons_rebase.py`).
- The frozen primitives beyond the entry-state numstat check: `store.py`/`keying.py`/`pipeline.py`/`adjudicate.py`/`adjust.py`/`router.py` are not read/edited (byte-frozen, not this task's surface — `horizons.py` imports only the `store` read-primitive, used read-only).
- `scripts/plan/track.py` — the 0044-T2 consumer re-point; already landed, not this task's surface.

## Risk Pre-Check

**BLOCKING GATE — the PF-S130-01 tautological-swap failure class + PF-S131-01 fixture shape + the thin-only back-compat.** Do NOT proceed to GREEN until a real (non-`assert True`), RED-capable test exists for every risk below. The task is NOT complete until the PF-S130-01 revert falsifier is proven RED against the GREEN implementation (Step 2.5 — the comprehensive-first branch being reverted does not exist until GREEN). A placeholder test fails this gate.

- **Risk (PF-S130-01 — tautological composition swap; the Wave-1 gap Tier-3 caught):** the re-base swaps `window_block`'s source (flat `plan::<domain>` history → the comprehensive periodization); a test that passes whether or not the swap happened encodes the OLD behavior and lets the re-base regress silently. ADR-0044 Consequences-Negative-2 (forces re-basing ADR-0038 onto the comprehensive resolver) is the risk this AC-1/AC-2 gate covers.
  - Gate condition: AC-1 MUST run the REAL `window_block("workout", root, on_date, WEEK_SPAN_DAYS)` (and `read_horizons`) over a scratch store seeded with a COMPREHENSIVE plan (via `plan_model.record_plan_version`) whose `domain_programs["workout"].prescription.blocks` are dated in-window, in TWO non-tautological arrangements: (i) a coexisting flat `plan::workout` DECOY reading dated in-window whose latest-in-window date DIFFERS from the comprehensive block's date → `window_block` returns the COMPREHENSIVE block's date, NOT the flat decoy; (ii) a comprehensive-only store with NO flat `plan::workout` reading at all → `window_block` returns the comprehensive block (the "0 reads of the retired flat history" falsifier — the answer is unchanged when the flat stream is absent). The **PF-S130-01 revert falsifier** (Step 2.5): revert the re-base (drop the comprehensive-first branch → `window_block` falls to the flat range-query) → over arrangement (i) it returns the flat decoy date, over (ii) it returns None → `test_window_block_reads_comprehensive_periodization` + `test_window_block_comprehensive_no_flat_history_needed` go RED. A test green under this revert FAILS AC-1.
- **Risk (PF-S131-01 — flat-prescription / non-composite fixtures):** a fixture carrying a flat `domain_program` prescription (or a hand-built non-`plan_model` composite) would not distinguish the comprehensive periodization path from the thin path, re-opening the PF-S130-01 tautology on the fixture side.
  - Gate condition: every fixture carrying a `domain_program` prescription uses the CANONICAL PERIODIZED shape (`{"prescription": {"blocks": [{"date","phase","detail"}]}}`, `domain_program.validate`-conformant); the comprehensive-version fixtures are the REAL `plan_model` composite (reuse `tests/store/test_plan_model.py`'s `_comprehensive_version` / `_training_program` builders, recorded via `plan_model.record_plan_version`). The thin decoy uses the flat `{"exercises":[...]}` shape. A flat-payload prescription or a hand-built composite dict fed to the seed FAILS this gate.
- **Risk (thin-only back-compat — ADR-0044 Consequences-Negative-3, pre-migration store):** a pre-migration store carrying ONLY thin `plan::<domain>` readings must still compose a horizon (0 crashes) — the re-base must not force a comprehensive version to exist.
  - Gate condition: AC-3 asserts the REAL `window_block` AND `read_horizons` over a THIN-ONLY store (no `plan-model::` version) resolve WITHOUT error via the mixed-history reader's thin fallback — the retained flat range-query returns the latest-in-window thin block, and a thin store with no in-window plan returns None; `read_horizons` returns all four slots without raising. RED-capable: a re-base that unconditionally read the comprehensive model (assuming a version exists) would `KeyError`/`TypeError` on a thin-only store → RED.
- **Risk (no new store stream — the `horizons.py` read-only invariant):** the composition writes no store stream and defines no `::`-prefixed id (`horizons.py:5-12`); the re-base reading `plan-model::` must not create it on a thin-only store.
  - Gate condition: the existing `test_four_horizon_composition::items == {"goal::bench", "plan::workout"}` (no `plan-arc::`/`horizon::`/`periodization::`/`plan-model::` stream) stays GREEN post-re-base — `store.read("plan-model::")` on a missing item is a pure read (returns `[]`, creates no file). Confirm in Step-4 regression.

## TDD Steps

Single cycle. Every acceptance criterion is a property of ONE coherent unit — the re-based `window_block` (+ the `read_horizons` `today` re-source, and the transitive `assess_pace`/`domain_horizons` reads over it) — exercised over the same seeded scratch-store fixtures, so they are not split (task-planning: "all criteria test the same component"); AC-3c (`assess_pace`) and AC-D1 (growth-tolerance) are transitive properties of the same re-based composition, not new components. RED writes every AC as a specific failing test BEFORE any GREEN; the PF-S130-01 revert falsifier runs POST-GREEN (Step 2.5), because the comprehensive-first branch being reverted does not exist until GREEN.

### Step 1: RED — Write Failing Tests

Call `/write-tests` for `scripts/plan/horizons.py` (the re-based `window_block` + `read_horizons`), creating `tests/plan/test_horizons_rebase.py`, targeting each AC below (the spec ACs 1/2/3/4 + the recipe-added AC-3b `today` re-source, AC-3c `assess_pace` re-source [spec `:260`], and AC-D1 growth-tolerance [Architect RULING D1]). Mirror `tests/store/test_plan_model.py` (reuse its `_training_program` / `_compound_program` / `_comprehensive_version` builders + `record_plan_version` seed) and `tests/plan/test_horizons.py` (the thin `_workout_plan` / `_enriched_workout_plan` / `goal_schema.record_goal` conventions): seed via the `plan_model` / `plan_schema` / `goal_schema` APIs under a `tmp_path` root; run each assertion over the REAL read path. ALL fixtures are SYNTHETIC (0 real operator PII, 0 live-API), on scratch stores.

**Shared fixtures:** reuse `_comprehensive_version(...)` (the real `plan_model` composite spanning ≥1 tracked domain, each a periodized-prescription seven-field program — PF-S131-01) recorded via `plan_model.record_plan_version(version, root)`; a thin decoy via `plan_schema.record_plan("workout", _enriched_workout_plan(...), date, "coach", root)`. The comprehensive prescription (`{"blocks":[{"date",...}]}`) vs the thin plan (`{"exercises":[...]}`) is the distinguishing signal that makes AC-1/AC-2 non-tautological.

- **AC-1 — Horizon re-base, comprehensive periodization (tension-resolution #28, load-bearing; PF-S130-01 PRODUCTION-PATH):**
  - `test_window_block_reads_comprehensive_periodization` — seed a comprehensive version dated the render date covering `workout` whose `prescription.blocks` carry an in-window dated block, PLUS a coexisting flat `plan::workout` DECOY reading dated in-window on a DIFFERENT date; call the REAL `horizons.window_block("workout", root, on_date, horizons.WEEK_SPAN_DAYS)` → the returned block reflects the COMPREHENSIVE periodization (its `plan_date` is the comprehensive block's date), NOT the flat decoy's date. Also assert via the REAL `horizons.read_horizons("workout", <goal>, root, on_date)` that `view["week"]` reflects the comprehensive periodization. RED-capable: the Step-2.5 revert (drop the comprehensive-first branch) turns it RED (returns the flat decoy).
  - `test_window_block_comprehensive_no_flat_history_needed` — seed ONLY the comprehensive version (NO flat `plan::workout` reading at all) → `window_block` returns the in-window comprehensive block (the "0 reads of the retired flat `plan::<domain>` history for periodization" falsifier — the answer does not depend on the flat stream). RED under the Step-2.5 revert (falls to the empty flat range-query → None).
- **AC-2 — Window membership preserved over blocks:** `test_window_block_block_membership_preserved` — seed a comprehensive version whose `prescription.blocks` span an IN-window date AND an OUT-of-window date (before `on_date - (WEEK_SPAN_DAYS - 1)`); `window_block` keeps the in-window block (its date is returned) and DROPS the out-of-window block; a comprehensive version ALL of whose blocks are out-of-window → `window_block` returns None (the `_in_window` boundary-inclusive semantics preserved on block dates, mirroring `test_window_boundary_inclusive`'s far-edge binary). Assert both the 7-day (`WEEK_SPAN_DAYS`) window and confirm the month (`MONTH_SPAN_DAYS`) window admits a block the week window drops.
- **AC-3 — Thin-only back-compat via the mixed-history reader (0 crashes):**
  - `test_window_block_thin_only_backcompat` — a store with ONLY thin `plan::workout` readings (no `plan-model::` version), two in-window dates → `window_block` resolves via the thin fallback (the retained flat range-query) → returns the LATEST-in-window thin block (identical to today's behavior), 0 crashes; a thin store with no in-window plan → None.
  - `test_read_horizons_thin_only_no_crash` — `read_horizons("workout", <goal>, root, on_date)` over a thin-only store returns all four slots (`today`/`week`/`month`/`milestone`) without raising (the pre-migration store composes a horizon — the AC-3 back-compat via T2's mixed-history reader).
- **AC-3b — `read_horizons` today comprehensive-wins (the `today` re-source, PF-S130-01):** `test_read_horizons_today_comprehensive_wins` — a comprehensive version standing for `on_date` covering `workout` → `read_horizons(...)["today"]["state"] is None` and its `plan` carries the comprehensive prescription (`"blocks" in view["today"]["plan"]`), NOT a thin `NO_PLAN`. RED-capable: reverting the `today` re-source (read `plan_schema.read_plan` again) over a comprehensive-only store returns `NO_PLAN` → RED (Step 2.5).
- **AC-3c — `assess_pace` comprehensive-branch verdict (spec `:260` `assess_pace` re-source, transitive-pin — closes the verification gap):** `test_assess_pace_comprehensive_branch_returns_verdict` — `assess_pace` re-sources TRANSITIVELY through `window_block` (Deviation #3), reading the this-week block's `week_expectation` extra; today NO direct test covers it, so a silent key-drop (the comprehensive branch fails to carry `week_expectation` into `window_block`'s `extras`) would make `assess_pace` return None for EVERY comprehensive plan, unobserved. Seed a comprehensive version standing for `on_date` covering `workout` whose in-window `prescription.blocks` block carries a nonzero numeric `week_expectation` (a block-level D2 framing key, e.g. `-0.4`), PLUS two `goal_schema.record_goal` snapshots for the goal (distinct dates, changing `current`) so `actual_weekly_rate(slug, root)` is non-None (mirror `tests/plan/test_horizons.py::test_assess_pace_*`'s two-snapshot fixture). Call the REAL `horizons.assess_pace("workout", <goal-slug>, root, on_date)` and assert it returns a VERDICT — a member of `horizons.CLASSIFICATIONS` (`behind`/`on-track`/`ahead`), NOT None. RED-capable: (i) a comprehensive branch that drops `week_expectation` from the block `extras` → `assess_pace` reads None → returns None → RED (the silent-key-drop catch this pins); (ii) the Step-2.5 comprehensive-first revert → `window_block` falls to the flat thin range-query → with no thin `plan::workout` reading the block is None → `assess_pace` None → RED. This ELEVATES the comprehensive-branch `week_expectation` lift from executor discretion (Deviation #3) to a pinned contract.
- **AC-D1 — Growth-tolerant over the co-wave `PLAN_DOMAINS` growth (Architect RULING D1 — intra-wave coordination):** `test_horizons_growth_tolerant_over_grown_roster` — 0043-T3 (SAME Wave 4, file-disjoint) grows `plan_schema.PLAN_DOMAINS` 4→≥13, the constant `horizons.py:318` (`domain_horizons`) iterates; the Wave-4→Wave-5 checkpoint co-locates both, so this task's committed `horizons.py` must stay correct once the roster grows. `monkeypatch.setattr(plan_schema, "PLAN_DOMAINS", plan_schema.PLAN_DOMAINS + ("sleep",))` (a stand-in grown-roster domain — `sleep` is NOT in `TRACKED_DOMAINS`, which 0043-T3 does NOT grow); seed a comprehensive version STANDING for `on_date` covering the closed four but NOT `sleep`, and NO thin `plan::sleep` reading. Assert the re-based `horizons.window_block("sleep", root, on_date, horizons.WEEK_SPAN_DAYS)` returns None AND `horizons.read_horizons("sleep", <goal>, root, on_date)` returns all four slots WITHOUT raising (`today` a `NO_PLAN`/`NO_PLAN_TODAY` absence, `week`/`month` None) — a grown domain with no comprehensive periodization contributes nothing. Also assert `horizons.domain_horizons(root, on_date)` does not raise over the grown roster and surfaces no `sleep` horizon (skipped by the `TRACKED_DOMAINS` gate). RED-capable: a re-based `window_block` that indexed the standing comprehensive version's `domain_programs[domain]` WITHOUT the coverage guard (rather than composing `read_standing_plan`, which falls through to the thin `read_plan` for an uncovered domain) `KeyError`s on the uncovered `sleep` → RED; the monkeypatch is required because `plan_schema.read_plan` raises `ValueError` for a domain OUTSIDE `PLAN_DOMAINS` (a grown member is INSIDE → resolves `NO_PLAN`, no raise — which is exactly the growth-tolerance being pinned).
- **AC-4 — Suite-local green + 0 real PII (spec AC-4):** `test_horizons_rebase.py` passes against scratch stores — 0 live-API calls, 0 real operator PII (verified by the Step-1→Step-2 RED→GREEN transition + a `grep` that the test tree carries no real operator-identity literal, mirror `tests/store/test_plan_model.py::test_no_operator_identity_literal_in_test_tree`).
- **Regression anchor (existing behavior preserved — the thin-fallback proof):** confirm `tests/plan/test_horizons.py` in FULL stays GREEN post-re-base — the flat-range cases (`test_latest_in_window_selection`, `test_window_boundary_inclusive`, `test_four_horizon_composition` incl. the no-new-stream `items` assertion, `test_today_by_date_equality`) exercise the thin fallback that MUST preserve the exact flat range-query + date-equality semantics. RED-capability: a re-base that routed the thin case through the comprehensive date-equality reader (collapsing the range query) breaks `test_latest_in_window_selection` → RED. This is the proof the re-base did not regress thin-store behavior.

Run: `.venv/bin/python -m pytest tests/plan/test_horizons_rebase.py -q`
Expected: FAIL — the comprehensive-first branch does not exist yet, so `window_block`/`read_horizons` still take the flat/date-equality path and the comprehensive-periodization assertions fail. (If a comprehensive-periodization behavioral test PASSES at this stage, the test is wrong or reads the flat path by coincidence — investigate before GREEN.)

### Step 1 (continued): RED-capability falsification (NON-SKIPPABLE)

Before AND after implementing, prove per-assertion RED-capability (a coincidental pass is a tautology):

1. **AC-1 revert-REDs (PF-S130-01, the load-bearing proof):** after GREEN, revert the comprehensive-first branch in `window_block` (always take the flat fallback); confirm `test_window_block_reads_comprehensive_periodization` (returns the flat decoy) + `test_window_block_comprehensive_no_flat_history_needed` (returns None) + `test_assess_pace_comprehensive_branch_returns_verdict` (AC-3c: the transitive `assess_pace` reads a None block over the comprehensive-only store → returns None) go RED; restore (Step 2.5).
2. **AC-3b revert-RED (the `today` re-source):** after GREEN, revert `read_horizons`'s `today` to `plan_schema.read_plan`; confirm `test_read_horizons_today_comprehensive_wins` goes RED over a comprehensive-only store (returns `NO_PLAN`); restore.

This is a mandatory gate of THIS recipe — do not silently delegate RED-capability to `/write-tests`. Run the falsifier apply→observe-RED→revert on the feature-branch working tree; leave NO edit behind (PF-S132-02 — never mutate-and-restore the main checkout; a reviewer re-verifying via mutation uses an isolated worktree).

### Step 2: GREEN — Implement

Changes to make (behavioral descriptions, NOT literal code) — ALL within `scripts/plan/horizons.py`:

- **Re-base `window_block(domain, root, on_date, span_days)` (satisfies AC-1/AC-2/AC-3):** resolve the standing plan through the ADR-0044-T2 mixed-history reader `plan_model.read_standing_plan(domain, on_date, root)` (comprehensive-wins else thin). When the reader resolves STANDING (state None) AND the resolved `plan` carries a periodization (`domain_program.PRESCRIPTION`'s `blocks` list — the comprehensive branch): read the plan's dated `blocks`, keep those whose `block["date"]` satisfies the EXISTING `_in_window(block["date"], on_date, span_days)` predicate (REUSE it unchanged — the boundary-inclusive semantics carry over to block dates), and return the LATEST in-window block as the existing `{"plan_date": <the block's date>, "extras": <the block's horizon-framing keys>}` contract (mirroring the current max-timepoint latest-wins; None when no block is in-window). OTHERWISE (no comprehensive plan stands, or the standing plan carries no `blocks` — a pre-migration thin store): fall back to the RETAINED flat range-query (`plan_confirm.filter_confirmed(store.read("plan::<domain>"))` + `_in_window` + latest-in-window, the current logic), preserving the exact thin-store behavior (AC-3 back-compat). The comprehensive branch reads 0 flat `plan::<domain>` history for periodization (`read_standing_plan` returns on comprehensive-wins BEFORE reading the thin stream). Add `plan_model` to the `from scripts.store import ...` line.
- **Re-source `read_horizons`'s `today` slot (satisfies AC-3b; spec `:260` "read_horizons re-sources from the comprehensive plan"):** swap the `today` value from `plan_schema.read_plan(domain, on_date, root)` to `plan_model.read_standing_plan(domain, on_date, root)` (comprehensive-wins, same `{state, plan, specialist, plan_date}` shape) — a comprehensive plan standing for the render date surfaces as today's action; a thin-only store preserves the exact date-equality result (`read_standing_plan`'s thin fallback IS `plan_schema.read_plan`). The `week`/`month` slots re-source TRANSITIVELY through the re-based `window_block` (no separate change); `milestone` is unchanged.
- **`assess_pace` re-sources TRANSITIVELY** through the re-based `window_block` (it reads `window_block(...)`'s `week` block extras — no direct edit). Confirm the block's extras carry `week_expectation` when present on the comprehensive branch; the extras source on the comprehensive branch is the in-window block's own framing keys (`phase` and, when the author emits them, `week_intent`/`week_expectation`), with the existing `plan_extras` empty-dict graceful floor when absent (executor discretion — no T3 AC pins the comprehensive-branch extras beyond `plan_date`; see Deviation #3).

Run: `.venv/bin/python -m pytest tests/plan/test_horizons_rebase.py -q`
Expected: PASS.

### Step 2.5: Falsify the re-base (POST-GREEN revert-RED)

The comprehensive-first branch + the `today` re-source now EXIST (GREEN landed), so the falsifiers are meaningful (at Step 1 every comprehensive test fails on the missing branch — nothing to revert). For EACH: apply the revert, run the named test, OBSERVE RED, then RESTORE (never committed). A test that stays green under its revert is tautological and FAILS its AC.

- **(pf-s130 — revert the composition swap):** in `window_block`, drop the comprehensive-first branch (always take the flat fallback) → `test_window_block_reads_comprehensive_periodization` (returns the flat decoy, not the comprehensive block) AND `test_window_block_comprehensive_no_flat_history_needed` (returns None on a comprehensive-only store) AND `test_assess_pace_comprehensive_branch_returns_verdict` (AC-3c: the block is None over the comprehensive-only store → `assess_pace` returns None instead of a verdict) all go RED. Restore.
- **(today re-source):** in `read_horizons`, restore `today = plan_schema.read_plan(...)` → `test_read_horizons_today_comprehensive_wins` goes RED (returns `NO_PLAN` on a comprehensive-only store). Restore.

Run after each restore: `.venv/bin/python -m pytest tests/plan/test_horizons_rebase.py -q`
Expected: PASS (every falsifier reverted; the suite is green again). Confirm NO falsifier edit remains in the working tree before Step 3 (PF-S132-02).

### Step 3: REFACTOR

Review for:
- Duplication between the comprehensive block-window and the flat range-query in `window_block` — both apply the SAME `_in_window` predicate + latest-wins selection over a list of dated items (blocks vs readings); factor the shared "keep in-window, take the max-dated" selection if it reduces the branch to two data sources feeding one selector, WITHOUT changing behavior. Do NOT re-implement `_in_window`.
- The re-base COMPOSES `plan_model.read_standing_plan` — it does NOT re-implement the comprehensive-vs-thin precedence or the store I/O (reached only through the reader / the retained `store.read`). No second copy of the resolution logic.
- No new store stream / no `::`-prefixed id introduced (the `horizons.py:5-12` read-only invariant); the block-extraction reads `blocks` with the graceful empty floor (no raise on a prescription missing `blocks`).
- The import direction stays acyclic (`horizons` imports `plan_model`; `plan_model` does not import `horizons`).
- Remove any Step-1/Step-2.5 temporary stub/mutation residue.

Run: `.venv/bin/python -m pytest tests/plan/test_horizons_rebase.py tests/plan/test_horizons.py -q`
Expected: PASS (the new re-base module + the existing horizon suite both green; no behavior change).

### Step 4: REGRESSION

Run: `.venv/bin/python -m pytest -q`
Expected: PASS at the entry baseline floor — the SAME two pre-existing env-sensitive failures (`test_bind`/`test_server`) + the entry skipped count, PLUS the new `test_horizons_rebase.py` tests passing, with NO new failure. Specifically confirm `tests/plan/test_horizons.py` stays GREEN (the thin fallback preserves the flat range-query + the no-new-stream `items` assertion + `today` date-equality on thin-only stores). A 3rd unexpected failure means the re-base reached outside the manifest or changed thin-store behavior — do NOT fix unrelated tests; investigate whether the change reached outside `horizons.py` and flag per the feedback protocol.

Then confirm the lightweight frozen check (this task supersedes no frozen surface — a numstat sanity, NOT a per-ADR freeze-break AC):
- `git diff --numstat origin/main -- scripts/store/store.py scripts/store/keying.py scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/router.py` → EMPTY (the `<always-frozen>` six untouched — `horizons.py` reads `store.read` unchanged and edits none of them).
- `git diff --numstat PRE_TASK_HEAD -- scripts/plan/horizons.py` → NON-empty (the re-base landed).
- `git diff --name-only PRE_TASK_HEAD -- scripts/ tests/` → exactly `scripts/plan/horizons.py` + `tests/plan/test_horizons_rebase.py` (no scope creep).

## Interface Contracts

This task does NOT create or modify a shared interface consumed by another task (ADR-0044-T3 is a Wave-4 terminal leaf — no downstream dependency, build plan `:280`). It RE-BASES the internal SOURCE of an existing composition while PRESERVING every public signature + return shape; the contract notes below document that preservation + the change-control on the upstream `read_standing_plan` dependency (they are not a new downstream contract).

- `horizons.window_block(domain, root, on_date, span_days) -> dict | None` — SIGNATURE + RETURN SHAPE UNCHANGED (`{"plan_date": str, "extras": dict}` | None). The only change is the SOURCE: the comprehensive periodization (`prescription["blocks"]` via `plan_model.read_standing_plan`) when a comprehensive plan stands and covers the domain; the retained flat `plan::<domain>` range-query otherwise. Consumed WITHIN `horizons.py` by `read_horizons` (`week`/`month`) and `assess_pace` (the `week` block's `week_expectation` extra) — both re-source transitively with 0 signature change.
- `horizons.read_horizons(domain, slug, root, on_date) -> dict` — SIGNATURE + KEYS UNCHANGED (`today`/`week`/`month`/`milestone`). The `today` slot now carries the `plan_model.read_standing_plan` result (comprehensive-wins) instead of `plan_schema.read_plan` — the SAME `{state, plan, specialist, plan_date}` shape, so downstream consumers read the same keys.
- **Behavioral invariants (the read-only + back-compat guarantees):** `horizons.py` WRITES no store stream and defines no `::`-prefixed id (reading `plan-model::` on a missing item creates nothing); a pre-migration thin-only store still composes a horizon via the retained flat range-query (0 crashes); the comprehensive branch reads 0 flat `plan::<domain>` history for periodization; the `<always-frozen>` six stay byte-frozen.
- **Intra-wave coupling — `plan_schema.PLAN_DOMAINS` (Architect RULING D1):** ADR-0043-T3 (the SAME Wave 4, file-disjoint) GROWS `plan_schema.PLAN_DOMAINS` 4→≥13, the constant this file iterates at `domain_horizons` (`:318`, unchanged here). This task neither reads nor asserts a grown roster during its own build (it builds against the closed four — Entry-State note), but its committed `horizons.py` is contractually GROWTH-TOLERANT at the Wave-4→Wave-5 checkpoint: the re-based `window_block`/`read_horizons`/`assess_pace` are absence-tolerant over a grown-roster domain with no comprehensive periodization (a domain OUTSIDE the standing version's `domain_programs` falls through `read_standing_plan` to the thin `read_plan`, which returns `NO_PLAN` for any grown-`PLAN_DOMAINS` member) — never a crash. AC-D1 pins this. A future task that grows `TRACKED_DOMAINS` (0043-T3 does NOT) or re-sources `domain_horizons` onto the comprehensive reader (a tracked follow-on, Deviation #2) must re-confirm the growth-tolerant iteration.
- **Change control:** ADR-0044-T4 (the confirm-hold skip on the comprehensive RESOLVER, a disjoint `plan_model.py` region) must not change `read_standing_plan`'s comprehensive-wins precedence or its return shape — the re-base depends on it. `window_block`/`read_horizons` signatures are stable for any future dashboard/render consumer.

## Verification Checklist

- [ ] AC-1: a horizon window query over a comprehensive plan returns the periodization from the comprehensive model — 0 reads of the retired flat `plan::<domain>` history for periodization — `test_window_block_reads_comprehensive_periodization` (production-path, flat-decoy distinguishing input) + `test_window_block_comprehensive_no_flat_history_needed` (comprehensive-only store); Step-2.5 revert turns both RED (PF-S130-01 non-tautology), restored.
- [ ] AC-2: window membership preserved — `window_block` over the comprehensive periodization keeps the in-window blocks and drops out-of-window (boundary-inclusive on block dates) — `test_window_block_block_membership_preserved`.
- [ ] AC-3: a thin-only pre-migration store still composes a horizon via the mixed-history reader (0 crashes) — `test_window_block_thin_only_backcompat` + `test_read_horizons_thin_only_no_crash`.
- [ ] AC-3b (the `read_horizons` `today` re-source): a comprehensive plan standing for the render date surfaces as today's action — `test_read_horizons_today_comprehensive_wins`; Step-2.5 revert-to-thin turns it RED, restored.
- [ ] AC-3c (the `assess_pace` comprehensive-branch re-source, spec `:260`): a comprehensive in-window block carrying `week_expectation` → `assess_pace` returns a VERDICT (a `CLASSIFICATIONS` member, not None), pinning the transitive re-source so a silent `week_expectation` key-drop (pace None for every comprehensive plan) is caught — `test_assess_pace_comprehensive_branch_returns_verdict`; RED on a `week_expectation` drop AND on the Step-2.5 comprehensive-first revert.
- [ ] AC-D1 (intra-wave coordination, Architect RULING D1): this task's committed `horizons.py` is growth-tolerant over 0043-T3's co-wave `PLAN_DOMAINS` growth — a grown-roster domain with no comprehensive periodization contributes nothing (None/skip), never a crash — `test_horizons_growth_tolerant_over_grown_roster`.
- [ ] AC-4 (spec AC-4): `.venv/bin/python -m pytest tests/plan/test_horizons_rebase.py` passes against scratch stores (0 live-API, 0 real PII — a `grep` confirms no real operator-identity literal in the test tree).
- [ ] Regression: `tests/plan/test_horizons.py` in FULL stays GREEN — the flat-range + date-equality + no-new-stream cases (the thin-fallback behavioral preservation proof).
- [ ] Risk (PF-S130-01 tautological swap) covered by AC-1/AC-3b production-path + the revert falsifier; Risk (PF-S131-01 fixture shape) covered by the reused `_comprehensive_version` periodized-prescription builders; Risk (thin-only back-compat) covered by AC-3; Risk (no new store stream) covered by the `test_four_horizon_composition` `items` regression.
- [ ] Frozen sanity (this task supersedes no frozen surface): `<always-frozen>` six numstat=0 vs `origin/main`; `horizons.py` numstat NON-empty; no per-ADR freeze-break AC applies.
- [ ] No regression: `.venv/bin/python -m pytest -q` holds the entry floor (the two known env-reds, no 3rd) with the new tests passing.
- [ ] Files modified match the 2-file manifest (`scripts/plan/horizons.py`, `tests/plan/test_horizons_rebase.py`) — no scope creep; any Step-2.5 falsifier edit is REVERTED, never committed (PF-S132-02); the `<always-frozen>` six byte-unchanged.
- [ ] Interface contract documented (the `window_block`/`read_horizons` source re-base with signatures + return shapes preserved; the transitive `assess_pace` re-source; the read-only + back-compat invariants).

## Deviations

| # | Spec Says | Recipe Says | Reason | Impact |
|---|----------|------------|--------|--------|
| 1 | AC-3: "a horizon query over a store with only thin `plan::<domain>` history (pre-migration) still resolves via the mixed-history reader — 0 crashes." | `window_block` resolves the STANDING plan via `plan_model.read_standing_plan` (the mixed-history reader) and, on the comprehensive-wins branch, filters the first-class periodization `blocks` by date; on the thin fallback it RETAINS the existing flat `plan_confirm.filter_confirmed(store.read("plan::<domain>"))` range-query (latest-in-window). | `read_standing_plan` does date-EQUALITY (single standing plan), which cannot reproduce the flat multi-reading RANGE query the existing `test_latest_in_window_selection`/`test_window_boundary_inclusive` assert. Routing the thin case through it verbatim would BREAK those tests — and `tests/plan/test_horizons.py` is NOT in the file manifest (so it must stay green). Preserving the flat range-query as the fallback keeps a pre-migration store byte-behavior-identical AND satisfies AC-3's load-bearing clause ("still resolves — 0 crashes") while the comprehensive branch reads the first-class periodization (AC-1). SE Ask-vs-Proceed rule 3 (same public interface, simpler internal behavior that preserves back-compat, state the assumption). | No public-signature change; existing thin-store tests stay green; the comprehensive periodization drives the window only once comprehensive plans are authored (a future live-run state — thin stores 100% backward-compatible). If a reviewer requires literal delegation of the thin case to `read_standing_plan` (collapsing the range to date-equality), that RE-BASES `test_horizons.py`'s flat-range cases and EXPANDS the manifest — flagged as the alternative, not adopted. |
| 2 | Spec `:260`: "`read_horizons`/`assess_pace` re-source from the comprehensive plan." (`domain_horizons` is not named.) | The re-base scopes to `window_block` (the AC-1/2/3 surface) + `read_horizons`'s `today` slot + `assess_pace` (transitive via `window_block`). `domain_horizons` (`:297`, the render-date-plan tracking gate reading `plan_schema.read_plan` date-equality) is LEFT UNCHANGED. | `domain_horizons` is not in the spec's named re-source surfaces, no T3 AC tests it, and re-sourcing it CHANGES behavior no AC pins (scope discipline, TP-06). A comprehensive-only store would have `domain_horizons` skip a domain whose comprehensive plan stands (it reads the thin `read_plan`). **QUALITY-ESCALATION (Call Chain Review Rule):** re-sourcing `domain_horizons` to `read_standing_plan` is a one-line downstream follow-on for the reviewer to route — surfaced, not silently fixed or ignored. | Thin stores keep working unchanged; the `domain_horizons` comprehensive re-source is a tracked follow-on visible to the reviewer, not this task's 4-AC scope. |
| 3 | AC-1/AC-2 pin `window_block`'s `plan_date` + block membership on the comprehensive periodization; the `extras` SOURCE on the comprehensive branch is otherwise unspecified. | On the comprehensive branch, `window_block`'s returned `extras` are the in-window block's own framing keys (`phase`, `week_intent`, `week_expectation`) via the existing `plan_extras` `HORIZON_EXTRAS`-subset lift + its empty-dict graceful floor. The `week_expectation` lift is now REQUIRED (pinned by AC-3c) — a block carrying `week_expectation` MUST surface it in `extras` so `assess_pace` returns a verdict; the other keys ride the same `plan_extras` lift with no additional pin. | The spec (`:260`) names `assess_pace`'s comprehensive re-source, which reads `window_block`'s `week_expectation` extra; AC-3c makes that transitive read a falsifiable contract (a silent key-drop → `assess_pace` None for every comprehensive plan → RED) rather than executor discretion. `plan_extras` lifts `week_expectation` off the block value uniformly (the same D2 seam the thin `_enriched_workout_plan` uses), so a block emitting it is surfaced; a block OMITTING it degrades `assess_pace` to None (honest absence, no fabricated verdict). | `assess_pace` returns a verdict when the block carries `week_expectation`, None when it does not; no downstream contract change (the `extras` shape is unchanged — a `HORIZON_EXTRAS` subset). |

## Commit

```
feat(plan): re-base horizons on comprehensive plan
```
(50 chars — trimmed from the spec-title `…on comprehensive periodization` [59] to satisfy the ≤50-char subject rule; "comprehensive plan" preserves the ADR-family term, and the body/PR carries the full "periodization" framing.)

Stage only: `scripts/plan/horizons.py`, `tests/plan/test_horizons_rebase.py`.

(Stage with `git add <the two paths>` as its OWN command, then `git commit` separately — the PII commit hook denies single-call stage+commit, `git commit -a`, and pathspec `git commit <path>`. Confirm NO Step-2.5 falsifier edit — to the comprehensive-first branch or the `today` re-source — and NO daemon vault-churn / parked `??` file remains staged, and the `<always-frozen>` six are byte-unchanged, before committing.)

## Rollback

If verification fails after 3 fix cycles:
1. `git stash -m "failed-ADR-0044-T3-attempt-{N}"` — preserve the work for diagnosis.
2. `git checkout {commit-before-task}` — revert to the entry-state HEAD (`PRE_TASK_HEAD` recorded in Entry State; also reverts any un-reverted Step-2.5 falsifier edit).
3. Update the issue tracker: flag ADR-0044-T3 as BLOCKED with the failure log.
4. Escalate: "Task ADR-0044-T3 failed verification after 3 attempts. Stash ref: {ref}. Failure log: {summary}." ADR-0044-T3 is a Wave-4 terminal leaf (2 waves slack, no downstream dependency — build plan `:280`); a BLOCK does not halt the critical path, but the Wave 4 → Wave 5 checkpoint's horizon-re-base go/no-go (`build-plan-adr-0041-0046.md:238`) does not pass until it lands.
5. Stash cleanup: after abandonment or re-planning, drop all `failed-ADR-0044-T3` entries from `git stash list`.

Do NOT: silently skip a failing acceptance criterion; weaken a test to make it pass (especially the AC-1/AC-3b production-path revert falsifier — that IS the PF-S130-01 deliverable); edit `tests/plan/test_horizons.py` to make the re-base pass (it is the back-compat regression proof and is NOT in the manifest — if the re-base genuinely requires re-basing its flat-range cases, HALT and flag an incomplete file manifest per the feedback protocol); or proceed to any downstream task with the re-base in a broken state.
