---
task-id: ADR-0033-0035-T4
source-spec: docs/spec/adr-0033-0035-intake-onboarding-spec.md
source-build-plan: docs/build-plan/build-plan-adr-0033-0035-intake.md
wave: 1
assigned-agent: SE
reviewers: [QA, plan-integrity]
created: 2026-06-30
status: approved
depends-on: []
---

# Recipe: The 9-Step Create-Profile Intake Wizard UI — Matching the Signed-Off Mockup, Existing-Seam Uploads + API Key, the Amended Demographics + the `_prefill_form` Consumer Rework; Rendered-HTML-Tested, 0 Live Spend

A Wave-1 entry task (one of two parallel primitives — T1 ∥ T4, disjoint manifests). T4 builds the served-SPA markup for the comprehensive ADR-0034 intake: a NEW 9-step Create-Profile WIZARD added to `vault/design/templates/app_view.html` (mirroring the operator-signed-off `prototype/intake-onboarding-mockup.html` `#mock-wizard`, steps 1-9), the AMENDED demographic capture fields (a full-date `<input type=date name='date-of-birth'>` superseding the born-year text input; a body-weight NUMBER → the local `bodyweight-kg` series superseding the `bodyweight-band` select), and the CONSUMER-INTEGRITY rework of `_prefill_form` in `vault/design/templates/app_shell.py` so the string-replace prefill cannot silently no-op against the removed targets. T4 is a UI/markup task — every AC is verified by `rg`/regex/parse over the rendered SPA HTML + the real `generate.run('app')` inline-asset emit probe, never against a live model call.

T4 edits NO python production engine: `vault/design/templates/app_shell.py` is the served-render layer (NFR-2 EXTEND-NOT-REBUILD scopes the FREEZE to `scripts/plan/{orchestrate,pipeline,assemble,generate_plan,adjudicate,adjust,track}.py` + `scripts/store/*.py` + `scripts/plan/router.py` — none of which T4 touches). T4 adds NO server route (Documents reuse the EXISTING `/upload` → `/confirm-extraction` / DNA-land seam, the API key reuses `/settings/key`). The markup builds to the pinned capture↔form field-name contract T1/T2/T3 define — a FORMAT coupling, NOT a code dependency (independently testable against the rendered HTML; `depends-on: []`).

T4 is THREE edits, all within the served-render surface:

1. **`vault/design/templates/app_view.html` (Modify)** — (a) ADD the 9-step Create-Profile wizard section (mirroring the mockup `#mock-wizard` [prototype/intake-onboarding-mockup.html:480-491]); (b) AMEND the existing `#panel-build` "Your details" demographic sub-form's DOB + weight fields in place (the year-text `date-of-birth` → `<input type=date>`, the `bodyweight-band` `<select>` → a `bodyweight-kg` number + lbs/kg unit select) so `_prefill_form`'s retargeted replace-targets exist on the surface it already operates over and the OLD targets are GONE; (c) GENERIC-ize the upload-status copy [app_view.html:816] — the "an Apple Health .zip/.xml, a labs PDF/CSV, or a 23andMe .zip" message → a generic supported-formats list that does NOT single out one operator source (AC5 / NFR-4). The `#panel-build` Upload doc-cards `<!--DOC_CARDS-->` MARKER + review-panel + `<template id="reading-row-tpl">` markup is UNTOUCHED (the dozens-of-tests ADR-0030/0031 surface; the doc-card LABEL fix lands in `_doc_cards`, edit 2).
2. **`vault/design/templates/app_shell.py` (Modify)** — (a) rework `_prefill_form` [app_shell.py:212-255]: drop `bodyweight-band` from `_DEMOGRAPHIC_SELECTS` [:40]; prefill the `bodyweight-kg` number input from the latest `bodyweight-kg` reading; retarget the `_DEMOGRAPHIC_DOB` prefill from the `placeholder="e.g. 1986"` year-text replace target [:244-245] to the full-date `<input type=date name='date-of-birth'>` `value=`. (b) GENERIC-ize the wearable doc-card LABEL in `_doc_cards` [app_shell.py:66,68] — `"Wearable export (Apple Health)"` → a generic `"Wearable export"` + a render-state-driven `· detected: <source>` suffix appended ONLY when the load-state establishes the source (mirroring the signed-off mockup's "Wearable export · detected: Apple Health" only-when-detected pattern), never a hardcoded single source (AC5 / NFR-4 — the operator flagged this EXACT hardcode as a recurring failure mode). `render` / `_plan_zone` stay byte-frozen (the gate + My-Info regions are T6/T7).
3. **`tests/serve/test_app_shell.py` (Modify)** — add the new wizard/amended-demographics/prefill-consumer assertions AND reconcile the existing demographic-form tests + module constants the amendment supersedes (the blast-radius reconcile — Factory-to-Component Wiring Rule, enumerated in the Risk Pre-Check).

The build is RENDERED-HTML-tested at 0 live spend, 0 network: every assertion runs against `app_shell.render([...])` (a pure string render) or the real `generate.run("app")` into tmp roots [test_app_shell.py:62-70]; no `key_source.resolve()`-gated call, no `converse`, no `aplus-research` dispatch appears anywhere in T4.

## Placement assumptions (stated per SE Ask-vs-Proceed §3 — same rendered-HTML interface, simpler internal choice picked + flagged)

The spec's File Manifest pins the field-name/step contract + the `_prefill_form` retarget but leaves the wizard's DOM placement to implementer discretion (NFR-5). Two interpretations produce the same rendered-HTML interface (the amended fields + the 9 steps + the prefill consumer-integrity all present); the simpler is chosen and stated:

- **A1 — the wizard is a NEW `<section>` in `app_view.html`** (the Create-Profile surface T6/W4 will gate-serve), carrying the full pinned intake field set. At W1 there is no gate yet, so the rendered SPA carries BOTH the wizard AND the existing shell — that is expected; T6 adds the lock/unlock branch.
- **A2 — `_prefill_form`'s W1 operating surface is the AMENDED `#panel-build` "Your details" form** (its existing target surface, amended in place to the date/number fields). Prefilling the WIZARD's own demographics step is T7's concern (when the wizard becomes the gated surface + the editable My-Info reuses the reworked `_prefill_form`) — **[T7 COUPLING, flagged for the wave reviewers + the T7 recipe].** This keeps T4's `_prefill_form` rework a minimal retarget of the existing single-surface prefill, not a dual-surface prefill engine.
- **A3 — `equipment-access-class` stays in `#panel-build`'s amended form + `_DEMOGRAPHIC_SELECTS`** (only `bodyweight-band` is dropped, per the spec's `_prefill_form` note). The wizard's demographics step (mockup step 1) carries birthdate/sex/race/weight/occupation, NOT equipment — equipment is T5's separate access-gated screen (W2).

If a reviewer requires the wizard itself be prefilled at W1 (interpretation B), that is a contract change at the T4↔T7 seam — raise it before GREEN, do not silently widen `_prefill_form`.

## Entry State

**Prerequisites:**
- [ ] On feature branch `feature/intake-build` (the ADR-0033/0034/0035 build branch; carries the ADR commit + the spec/build-plan commits on top of `main` [VERIFIED `git branch --show-current` = `feature/intake-build`]). The fork-point `<wave-base>` is `git merge-base HEAD origin/main` = `12f769f` ([VERIFIED 2026-06-30] — the branch's ADR/spec/build-plan docs commits touch no code, so the frozen-set numstat reads 0 from this base).
- [ ] Working tree clean of code changes: `git status` shows only the basic-memory daemon's `vault/**` frontmatter churn (cosmetic, daemon noise — leave it uncommitted, never `git add vault/` blindly; CLAUDE.md § Vault hygiene). `app_view.html`/`app_shell.py` are NOT vault-entity pages, so they are T4's manifest files staged by explicit path.
- [ ] Full test suite green at the behavior-preservation baseline: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest -q` with port 8765 FREE → 0 failed. T4 changes the served render, so the pytest baseline must stay green across the change (the demographic-form tests T4 reconciles are part of this suite).
- [ ] Loopback default port 8765 FREE — the two socket-binding serve tests bind `DEFAULT_PORT`; a leftover server holding 8765 turns the green baseline RED on EXACTLY `tests/serve/test_bind.py::test_port_collision_exits_nonzero_fail_loud` + `tests/serve/test_server.py::test_main_reads_no_stdin` (the documented ENVIRONMENTAL delta, NOT a regression). Verify: `! lsof -nP -iTCP:8765 -sTCP:LISTEN >/dev/null 2>&1 && echo "8765 FREE"`. Free the port (or accept ONLY those 2 named failures) before the gate.
- [ ] `generate.run("app")` drives `render.emit` over `app_shell.render` and RAISES `ValueError` on any off-file asset reference [render.py:149-176, `_ASSET_ATTRS`=`{src,href,poster}` :37] — the AC6 inline-asset gate. `generate.run` signature carries the tmp-root kwargs `_root`/`_out_dir`/`_dna_root`/`_labs_root` [generate.py:35] the `_emit_app` helper uses [test_app_shell.py:62-70] [VERIFIED].
- [ ] Target files: all three Modify targets exist [VERIFIED] — `vault/design/templates/app_view.html` (916 lines; screens `#screen-dashboard`/`#screen-plan`/`#screen-team`/`#screen-profile`; the `#panel-build` "Your details" form at [:463-471] with the year-text `date-of-birth` [:466] + the `bodyweight-band` 6-option select [:468]; NO wizard section yet — the old wizard is `generate.run('intake')`/`intake.py`, a DIFFERENT template). `vault/design/templates/app_shell.py` (`_DEMOGRAPHIC_SELECTS` [:40] carrying `bodyweight-band`, `_DEMOGRAPHIC_DOB` [:41], `_prefill_form` [:212-255] with the `placeholder="e.g. 1986"` replace target [:244]). `tests/serve/test_app_shell.py` (the demographic-form + prefill suite T4 reconciles).
- [ ] `prototype/intake-onboarding-mockup.html` present (operator-signed-off, gitignored) — the authoritative 9-step wizard structure [`#mock-wizard`:480-491], the Clinical Light design tokens [:3-10], the step set + field controls + the key-save asterisk/greyed/`✓ Saved` affordance [step 9, :488], the generic source labels [step 8 "detected: <source> · parsed locally", :488] [VERIFIED]. `test -f prototype/intake-onboarding-mockup.html`.
- [ ] `plan-integrity` has grounded the plan (Wave-1 entry criterion): every Wave-1 task's inputs confirmed against the LIVE tree (`stat`/`grep`, not plan prose); the dependency map confirmed an acyclic DAG with artifact-named edges; each wave's checkpoint confirmed runnable; the prior-wave checkpoint attested green (N/A — Wave 1 is the entry point, T1 ∥ T4 disjoint manifests).

This entry state doubles as the rollback target — the entry-state commit is the branch HEAD this task is built on top of.

## Context Load List

**Files to read:**
- `vault/design/templates/app_view.html` — the four screens + the topbar nav [:349-353]; the `#screen-team` workspace [:444-498] holding `#panel-build` (the `ws-docs` Upload surface) with the "Your details" demographic form [:463-471], the doc-cards `<!--DOC_CARDS-->` [:475], the review panel [:481-487], and the `reading-row-tpl` template [:488-493]; the `ws-tabs` (`My Plan` / `Build Plan` / `Generate`, T7 relabels) [:455-459]; the inline `<style>` Clinical Light tokens; the inline JS (`go()`/nav [:619-624], the `/upload`/`/confirm-extraction`/`/chat`/`/settings/key`/`/generate-plan` fetch flows). MATCH the existing inline-SVG / `data-lucide` / honest-empty conventions — the wizard adds NO off-file asset (AC6).
- `vault/design/templates/app_shell.py` — `_prefill_form` [:212-255] (the consumer T4 reworks: `_latest_values` read [:230], the `_DEMOGRAPHIC_SELECTS` `<option>` replaces [:234-239], the `_DEMOGRAPHIC_DOB` year-text replace [:241-247], the `<form action='/upload'>` banner inject [:249-255]); `_DEMOGRAPHIC_SELECTS` [:40] + `_DEMOGRAPHIC_DOB` [:41]; `_latest_values` [:185-209] (latest-by-timepoint, an item with no reading is absent — no fabricated default); `_doc_cards` [:56-88] (the wearable card LABEL T4 generic-izes: the loaded branch already names the detected `we['source']` in the `sub` [:64], the hardcoded `"Wearable export (Apple Health)"` title + `"Apple Health export.xml"` unloaded sub [:66,68] are the AC5/NFR-4 fix targets — make the title generic + the source render-state-driven); `render` [:258-288] (the `<!--DOC_CARDS-->`/`<!--PLAN_ZONE-->` substitution + the `_prefill_form` call — UNCHANGED by T4); `_esc` [:44-46]. T4 edits `_prefill_form` + `_doc_cards` ONLY; do NOT modify `render`/`_plan_zone` (the gate + My-Info regions are T6/T7).
- `prototype/intake-onboarding-mockup.html` — `#mock-wizard` [:480-491]: the stepper [:487] (9 `step-item`s), `#wsteps` [:488] (9 `wstep` `data-step` containers, each with its `.h1` heading: Demographics / Goals / Training & activity / Diet / Supplements & peptides / Medications / Health & lifestyle / Documents / API key); step 1 demographics (birthdate `<input type=date>` + the "only your age is used" guidance, sex, race/ethnicity + the "kept on your device, never sent to the planner" note, body-weight number + lbs/kg, occupation); step 3 the exercise-safety chest-pain/dizziness/SOB screen; step 5 supplement-stack/peptide-stack; step 6 raw meds + drug-allergy; step 7 apnea + PHQ-2 + smoker (Yes/Former→years/Never) + alcohol (None/Rarely/Monthly/Weekly/Most-days); step 8 Documents (generic "detected: <source> · parsed locally" cards + the drop affordance); step 9 API key (the empty-vs-saved keystate: asterisks + greyed `disabled` input + the `✓ Saved` pill ~13px). READ to ground the structure + copy; MATCH the field SET + step set, do NOT pin pixel/copy the ADR leaves to discretion (NFR-5).
- `tests/serve/test_app_shell.py` — the assertion idioms T4 mirrors + the tests it reconciles: `_spa_html()` [:73-75] (`app_shell.render([])`), `_emit_app` [:62-70] (the real `generate.run('app')`), `_upload_form_html`/`_form_field_names`/`_select_block` regex helpers [:243-259], the inline-asset emit probe [:83-118], the demographic-form suite [`_DEMOGRAPHIC_NAMES`:220, `_BODYWEIGHT_LB_LABELS`:224, `_RICH_SECTION_NAMES`:231, the round-trip :302-329, the objective-only :332-339], the prefill suite [:865-938]. The new cases MATCH the failing-capable rendered-HTML idiom; the reconciled cases re-key onto the amended contract.

**Do NOT load:** the ADRs (`docs/adr/ADR-003{3,4,5}*.md`) + the dispositions (`docs/spec/.pipeline/adr-0033-0035/`) — the spec/build-plan distilled them; `scripts/serve/capture.py` (T1/T2/T3's surface — T4 makes 0 capture edits; the field-NAME contract is read from the spec's pinned table, not capture.py internals); `scripts/plan/router.py` + the frozen engine + `scripts/store/*` (byte-frozen, NFR-2); `scripts/serve/server.py` (T4 adds no route — the route table is the spec-verified 7 entries, read-to-modify NOT needed); `vault/design/templates/intake.py` (the SUPERSEDED `generate.run('intake')` 6-step flow — NOT the served surface, NOT T4's wizard); any other `vault/**` content file (daemon-churned).

## Risk Pre-Check

This task carries the spec's ADR-0033-0035-T4 Risk Mitigations field + the build plan's Wave-1 hard constraints + the integration-verification mandates (CLAUDE.md). Before proceeding to RED, verify a test exists (or the RED phase adds one) for EACH; a placeholder test (`assert True`) fails this gate. Each gate condition references a live test/command, not a subjective assessment.

- **CONSUMER INTEGRITY — `_prefill_form` cannot silently no-op against a removed target (AC7 / the OQ-5 consumer rework / CLAUDE.md Factory-to-Component Wiring Rule).** `_prefill_form` prefills by STRING REPLACE against literal markup [app_shell.py:238,244]. T4 removes the `placeholder="e.g. 1986"` year-text target [:244] and the `bodyweight-band` `<option>` target — a string-replace against a now-absent literal silently no-ops (a saved demographic would render blank with NO error). The gate: a SEEDED store with a `date-of-birth` full-date + a `bodyweight-kg` reading renders the amended `<input type=date>` carrying `value=<saved-dob>` AND the amended number input carrying `value=<saved-weight>` (prefill WORKS against the new targets), AND `rg` over the rendered HTML returns 0 for both retired targets — the `placeholder="e.g. 1986"` year-text input AND a `bodyweight-band` `<option ... selected>` (the old targets are GONE, so a stale replace cannot pass vacuously). FAILING-CAPABLE: revert the markup amendment (keep the year-text input) while the test asserts the date input's `value=` → the prefill assertion reds; revert the `_prefill_form` retarget (keep targeting the year-text literal) while the markup is the date input → the prefill no-ops → the `value=` assertion reds. The consumer + the markup are updated in the SAME PR (the Wiring Rule), reconciled in Cycle 1.

- **BLAST-RADIUS RECONCILE — the amended demographics supersede the ADR-0018 objective-only-form contract (CLAUDE.md "E2E tests MUST be updated when a later phase changes the expected behavior" + "No tautological tests").** The amendment breaks 8 existing `tests/serve/test_app_shell.py` cases + 3 module constants that encode the OLD 4-field form (year-text DOB, `bodyweight-band` select, objective-only `/upload`). ALL are in T4's manifest file — reconciled IN THIS TASK (no manifest expansion). Each reconcile re-keys onto the amended contract, NEVER weakens to pass:
  - `_DEMOGRAPHIC_NAMES` [:220] `("date-of-birth","sex-for-dosing","bodyweight-band","equipment-access-class")` → drop `bodyweight-band` (weight is now `bodyweight-kg`, a number, not a posted band token); add `bodyweight-kg` if asserting the amended form posts it.
  - `test_demographic_selects_built_from_the_gate_constants` [:274] + `test_bodyweight_band_six_options_with_pinned_pounds_labels` [:289] + `_BODYWEIGHT_LB_LABELS` [:224] → drop the `bodyweight-band` leg (the select is gone; weight is a number input with no `<option>`s). Keep the `sex-for-dosing`/`equipment-access-class` select-vs-gate-constant no-drift checks.
  - `test_demographic_fields_round_trip_through_the_built_capture_seam` [:302] → drop the `first_opt("bodyweight-band")` leg (the select is gone → the helper would assert-fail) and re-key to ONLY the W1-VALID legs that BYPASS the not-yet-built deriver + weight-capture. **[CROSS-WAVE DECOUPLING — the judge-rejected defect, fixed (iter-3)]:** the live case asserts `summarize(...)["training-age-band"] == "born-1980s"` — a DERIVER-OUTPUT token — inside a MARKUP test. Two cross-wave traps: (i) T2/W2 repurposes `_age_band` to the EXACT AGE → `born-1980s` goes wrong at W2; (ii) `bodyweight-kg` CAPTURE routing is T2's W2 work — `capture.py` is NOT in T4's 3-file manifest, so at W1 a posted `bodyweight-kg` field falls to capture's record-only `else` branch [capture.py:291-295] (only `_DOB_FIELD` exists, no `_WEIGHT_FIELD` yet) → `store.read("bodyweight-kg")` is EMPTY → a weight→store round-trip assertion is RED at the W1 full-suite checkpoint with no T4-manifest remedy. The reconcile keeps ONLY: (a) the **DOB capture→store round-trip** — `store.read("date-of-birth")` carries the posted full date via the UNCHANGED `_DOB_FIELD` special-case [capture.py:84,276-281] (W1-valid AND W2-invariant — the DOB raw-source write does not change when T2 repurposes the deriver); (b) the **`sex-for-dosing` WIRED pass-through** (a `WIRED_TOKEN`, not a deriver output — stable across W2). REMOVE every `bodyweight-kg` CAPTURE store round-trip from this test (and any T4-owned test) — that routing is T2's W2 work. T4 covers the WEIGHT only via the two W1-valid surfaces it already owns, both of which BYPASS capture routing: the AC-2 MARKUP assertion (the rendered `bodyweight-kg` number input + unit exists) and the AC-7 PREFILL assertion (a `bodyweight-kg` reading seeded DIRECTLY into the store → `_prefill_form` renders the `value=`; the prefill reads the store directly, not through capture). The deriver-OUTPUT contract (exact age, current-weight+trend) AND the weight capture→`bodyweight-kg` store round-trip are owned + asserted by T2's `test_router.py`/`test_capture.py` (T2 owns `capture.py` + the `bodyweight-kg` capture special-case — it already tests this). T4 now has ZERO dependency on the W2 deriver/weight-capture behavior.
  - `test_upload_form_is_objective_only_zero_rich_section_fields` [:332] + `_RICH_SECTION_NAMES` [:231] → the ADR-0018 "objective-only form vs chat-only rich fields" split is SUPERSEDED by ADR-0034 (the comprehensive intake intentionally captures `nutrition-detail`/`supplement-stack`/`peptide-stack`/`training-detail` + the safety screens through the wizard → `/upload`). Reconcile: scope the "objective-only" assertion to the SPECIFIC `#panel-build` "Your details" form (the amended demographic set), and assert the WIZARD CARRIES the rich-domain field names (the new ADR-0034 contract). Do NOT keep the blanket "no rich field in any `/upload` form" assertion — it encodes the retired ADR-0018 split and would red on the correct ADR-0034 build (a tautological-against-old-behavior test).
  - `test_upload_form_posts_the_four_demographic_names_to_upload` [:265] → re-key to the amended `#panel-build` demographic name set (date-of-birth/sex-for-dosing/bodyweight-kg/equipment-access-class).
  - `test_demographic_form_prefills_each_saved_value` [:865] + `test_prefill_tracks_the_saved_band_and_resolves_latest` [:894] + `test_demographic_form_blank_default_on_empty_store` [:924] → re-key onto the amended date input + number input (the date `value=`, the number `value=`); PRESERVE the non-tautological (A≠B) + latest-wins + honest-blank coverage by re-keying onto the `bodyweight-kg` number value (or the date value), NEVER deleting the differ-by-A/B leg.
  Gate: after the reconcile, `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest -q tests/serve/test_app_shell.py` is green with the reconciled cases asserting the AMENDED contract (failing-capable by construction — each reds if the amended markup/consumer regresses).

- **GENERIC-NOT-OPERATOR-SPECIFIC labels (AC5 / NFR-4 — the operator flagged this EXACT hardcode as a RECURRING failure class, load-bearing; spec AMENDED `[2026-06-30 S103]`).** Three surfaces must be generic-not-operator-specific, ALL folded into T4 (AC5 IS "generic source labels" — it cannot defer while it is a T4 criterion):
  - **The wizard Documents step (step 8)** lists a GENERIC multi-source affordance ("parsed locally", naming Apple Health / Garmin / Whoop / Oura / Fitbit / 23andMe / AncestryDNA) and presents a per-source label ONLY render-state-driven (a source name appears only when DETECTED), never a hardcoded single source as the sole label.
  - **`app_shell._doc_cards` wearable card** [app_shell.py:66,68] — the hardcoded `"Wearable export (Apple Health)"` title + `"Apple Health export.xml"` unloaded sub → a generic `"Wearable export"` title + a `· detected: <source>` suffix appended ONLY when the load-state establishes the source (the loaded branch already carries `we['source']` [:64] — append it to the title; the unloaded branch shows the generic title + a generic format hint, NO single source).
  - **The upload-status copy** [app_view.html:816] — "an Apple Health .zip/.xml, a labs PDF/CSV, or a 23andMe .zip" → a generic supported-formats list that does not single out one operator source.
  Gate (RED-capable): `rg "Wearable export \(Apple Health\)"` over the rendered SPA returns 0; the wearable card title is generic with the source appended ONLY render-state-driven (a SEEDED loaded-wearable status whose `source` is a NON-"Apple Health" value, e.g. `"Garmin"`, renders `· detected: Garmin` — proving the suffix tracks the load-state, not a constant); the wizard Documents step lists the generic multi-source copy (≥ the named sources + "parsed locally"). FAILING-CAPABLE: revert the title to the hardcoded `"Wearable export (Apple Health)"` → the `rg`-returns-0 assertion reds; revert the suffix to a constant → the non-"Apple Health" loaded-source assertion reds. (The loaded-source render assertion mirrors the existing labs-loaded escaped-filename pattern [test_app_shell.py:405-425].) NO EXTRA BLAST RADIUS: no existing test asserts the literal `"Wearable export (Apple Health)"` title [VERIFIED — the `_doc_cards`/wearable assertions key on load-state (`"1 readings"`, `"✓ loaded"`) + labs/dna rendering, not the hardcoded title]; the loaded `sub`'s `we['source']`/`we['count']` render [:64] is byte-unchanged, so `test_server.py:319`'s `"1 readings"` stays green.

- **MATCHES-THE-SIGNED-OFF-MOCKUP — the 9-step structure + the Clinical Light system + the key affordance (AC1/AC2/AC3/AC4).** The wizard MUST carry the mockup's 9 steps in order with their headings, the amended demographic fields, the rich-domain + safety controls, and the API-key empty-vs-saved affordance (asterisks + greyed/`disabled` + `✓ Saved` ~13px). Gate: the rendered HTML parses to 9 `data-step` containers (1-9) with the 9 headings; the demographic + rich-domain + safety field names are present per the pinned contract; the key-saved markup is present. PIN the field-name/step contract (RED-capable); do NOT pin the exact pixel/copy/icon-path the ADR leaves to implementer discretion (NFR-5) — a copy-wording assertion that reds on a synonym is over-pinning.

- **NO NEW ROUTE / NO NEW EGRESS — Documents + key reuse the existing seams (AC4 / ADR-0033 no-new-route).** The Documents step submission targets `/upload` → `/confirm-extraction` / DNA-land; the API-key step targets `/settings/key`; the wizard adds NO `fetch()` to a new path and NO new `<form action>`. Gate: every `fetch(...)`/`<form action=...>` target in the rendered SPA is in the known same-origin loopback set `{/chat,/upload,/settings/key,/confirm-extraction,/generate-plan}` (mirrors [test_app_shell.py:368-390]); `git diff --numstat <wave-base>..HEAD -- scripts/serve/server.py` = 0 rows (T4 touches no server route). FAILING-CAPABLE: a wizard step posting to a new path reds the fetch-target-set assertion.

- **ADR-0029 INLINE-ASSET GATE — `generate.run("app")` returns a written path, no off-file asset (AC6).** The wizard adds inline-SVG icons + inline markup ONLY — no off-file `<script src>`, `<img src="path">`, CDN, or CSS `url()`. Gate: the AUTHORITATIVE real-emit probe — `generate.run("app")` into tmp roots RETURNS a `Path` that EXISTS (does not raise the off-file `ValueError`) [render.py:149-176; test_app_shell.py:83-96]. A substring grep is NOT substituted. FAILING-CAPABLE: the existing negative probe [test_app_shell.py:98-118] proves the gate reds on ONE injected off-file ref.

- **EXTEND-NOT-REBUILD — served-render markup only, no frozen-engine edit (NFR-2).** T4 touches only `vault/design/templates/app_view.html`, `vault/design/templates/app_shell.py`, `tests/serve/test_app_shell.py`. Gate (a SHELL gate, run in REGRESSION; `<wave-base>` a `git merge-base`): `git diff --numstat $(git merge-base HEAD origin/main)..HEAD -- scripts/plan/orchestrate.py scripts/plan/pipeline.py scripts/plan/assemble.py scripts/plan/generate_plan.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/track.py scripts/plan/router.py` → 0 rows; `git diff --numstat $(git merge-base HEAD origin/main)..HEAD -- 'scripts/store/*.py' scripts/serve/server.py scripts/serve/capture.py` → 0 rows (T4 edits no capture/server/store/router).

- **0 LIVE SPEND — rendered-HTML / real-emit tested (NFR-3/NFR-5).** Gate: every T4 case runs against `app_shell.render([...])` (a pure string render) or the real `generate.run("app")` into tmp roots [test_app_shell.py:62-75]; 0 network, 0 key, 0 model call, 0 `converse`, 0 `aplus-research` dispatch appears anywhere in T4. The operator-present LIVE care-agent run is ADR-0035 OQ-1 (operator-gated, out of every wave).

## TDD Steps

Two TDD cycles over `tests/serve/test_app_shell.py` (rendered-HTML + real-emit). Cycle 1 lands the riskiest work first (SE: riskiest-first) — the AMENDED demographic input fields + the `_prefill_form` CONSUMER rework + the blast-radius reconcile (AC2 date/weight inputs, AC7, the 8 reconciled cases) — so the silent-no-op + the supersession reconcile are proven before the wizard scaffold. Cycle 2 lands the 9-step wizard + the rich-domain/safety field names + the generic source labels + the Documents/API-key existing-seam reuse + the key affordance + the inline-asset render gate (AC1, AC3, AC4, AC5, AC6) and the full-suite regression (AC8). All cases are rendered-HTML/real-emit; 0 live spend, 0 network.

**Every Python command in this recipe is prefixed `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES`** (the macOS fork-safety setting the suite runs under) and run as `.venv/bin/python -m pytest` with port 8765 FREE.

### Cycle 1: Steps 1-4 — the amended demographic fields + the `_prefill_form` consumer rework + the blast-radius reconcile

#### Step 1: RED — Write Failing Tests
Edit `tests/serve/test_app_shell.py`:
- **New — AC7 consumer integrity (no silent no-op).** A seeded-store render asserts: the amended `<input type=date name='date-of-birth'>` carries `value=<saved-dob>` (a full date, e.g. `1986-04-12`); the amended `bodyweight-kg` number input carries `value=<saved-weight>`; the saved-state banner (`data-prefill='saved'` + `✓ Saved`) is present; AND `rg` returns 0 for BOTH retired targets — the `placeholder="e.g. 1986"` year-text input AND any `bodyweight-band` `<option ... selected>` (the old targets GONE). Mirror the seed helper [`_seed_demographics`:857-862].
- **New — AC2 (amended demographic input fields, the `_prefill_form`-target subset).** Assert the rendered SPA carries `<input type=date name='date-of-birth'>` (NOT the old year-text input) + a `bodyweight-kg` NUMBER input + a lbs/kg unit select; assert the old `name='date-of-birth' ... placeholder="e.g. 1986"` year-text markup and the 6-option `bodyweight-band` select are ABSENT.
- **New — AC7 non-tautological + honest-blank (re-keyed from [:894]/[:924]).** A DIFFERENT saved `bodyweight-kg` value (A≠B) pre-fills a DIFFERENT number `value=`; latest-by-timepoint wins; an EMPTY store renders the date/number inputs with NO `value=` and NO banner.
- **Reconcile (edit-in-place) the breaking cases + constants** per the Risk Pre-Check enumeration: `_DEMOGRAPHIC_NAMES`/`_BODYWEIGHT_LB_LABELS`/`_RICH_SECTION_NAMES`; `test_demographic_selects_built_from_the_gate_constants`, `test_bodyweight_band_six_options...`, `test_demographic_fields_round_trip...` (drop the bodyweight-band leg; keep ONLY the W1-valid legs — the DOB capture→store round-trip `store.read("date-of-birth")` via `_DOB_FIELD` + the `sex-for-dosing` WIRED pass-through; REMOVE any `bodyweight-kg` capture→store assertion — that routing is T2's W2 work, EMPTY at W1; the weight is covered via AC-2 markup + AC-7 prefill-from-seeded-store, both bypassing capture), `test_upload_form_is_objective_only...` (scope to `#panel-build`; the rich fields move to the wizard contract, asserted in Cycle 2), `test_upload_form_posts_the_four_demographic_names...`, the three prefill cases.

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest -q tests/serve/test_app_shell.py`
Expected: FAIL — the new AC2/AC7 cases red (the date/number markup + the reworked `_prefill_form` do not exist); the reconciled cases red (they now assert the amended contract the markup has not yet adopted).

#### Step 2: GREEN — Implement
Changes to make (describe; the SE writes the markup/python):
- `vault/design/templates/app_view.html` — in `#panel-build`'s "Your details" form [:463-471]: REPLACE the year-text `date-of-birth` input [:466] with `<input type=date name='date-of-birth'>`; REPLACE the `bodyweight-band` `<select>` [:468] with a `bodyweight-kg` NUMBER input + a lbs/kg unit select (the pinned field name is `bodyweight-kg`, store-item-matching like `_DOB_FIELD` [capture.py:84]; the unit handling/conversion is T2's capture-side concern, W2). KEEP the `sex-for-dosing` + `equipment-access-class` selects + the `<form action='/upload'>` action UNCHANGED. Leave the Upload doc-cards / review-panel / `reading-row-tpl` UNTOUCHED.
- `vault/design/templates/app_shell.py` — rework `_prefill_form` [:212-255]: set `_DEMOGRAPHIC_SELECTS` to drop `bodyweight-band` (→ `("sex-for-dosing", "equipment-access-class")`); add `"bodyweight-kg"` to the `_latest_values` read [:230] + a string-replace prefilling the `bodyweight-kg` number input's `value=`; RETARGET the `_DEMOGRAPHIC_DOB` replace [:243-247] from the `placeholder="e.g. 1986"` year-text literal to the `<input type=date name='date-of-birth'>` `value=`. The banner inject [:249-255] + the select-prefill loop pattern [:234-239] are UNCHANGED (the loop now iterates the 2-member tuple). Cycle 1 touches `_prefill_form` ONLY in `app_shell.py` (the `_doc_cards` generic-label fix lands in Cycle 2 with AC5).

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest -q tests/serve/test_app_shell.py`
Expected: PASS — the AC2/AC7 cases green; the reconciled cases green against the amended contract.

#### Step 3: REFACTOR
Review for:
- `_prefill_form` reuses the EXISTING `_latest_values` reader + the select-loop pattern — no bespoke parser, no duplicated latest-by-timepoint logic; the number-input prefill is one string-replace mirroring the DOB replace.
- The amended markup keeps the `<form action='/upload'>` action + the `sex-for-dosing`/`equipment-access-class` select markup byte-identical (only the DOB + weight fields change) — so the banner-inject + select-prefill targets still resolve.
- The retired targets (`placeholder="e.g. 1986"`, the `bodyweight-band` `<option>`) appear NOWHERE in `app_view.html` (the silent-no-op surface is removed, not merely bypassed).
- No fabricated default: an empty store still renders the date/number inputs blank (no `value=`) and no banner (the `if not saved: return html` guard [:231-232] holds).

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest -q tests/serve/test_app_shell.py`
Expected: PASS (no behavior change).

#### Step 4: REGRESSION
Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest -q` (port 8765 free) → green (the amended demographic surface does not regress any non-T4 test; the reconciled demographic-form suite is green). Run the EXTEND-NOT-REBUILD frozen-engine SHELL gate (NFR-2):
```
WAVE_BASE=$(git merge-base HEAD origin/main)
git diff --numstat "$WAVE_BASE"..HEAD -- scripts/plan/orchestrate.py scripts/plan/pipeline.py scripts/plan/assemble.py scripts/plan/generate_plan.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/track.py scripts/plan/router.py
git diff --numstat "$WAVE_BASE"..HEAD -- 'scripts/store/*.py' scripts/serve/server.py scripts/serve/capture.py
```
→ 0 changed rows on every frozen file; capture/server/store/router empty (T4 edits only the render template + its test).

### Cycle 2: Steps 5-8 — the 9-step Create-Profile wizard + the rich-domain/safety field names + generic source labels + existing-seam Documents/API key + the inline-asset render gate

#### Step 5: RED — Write Failing Tests
Edit `tests/serve/test_app_shell.py`: add the wizard cases.
- **Case W1 — the 9 step containers (AC1).** Parse the rendered SPA for 9 `data-step` (or equivalent stable) containers (1-9) each with its heading (Demographics / Goals / Training & activity / Diet / Supplements & peptides / Medications / Health & lifestyle / Documents / API key). RED: the wizard does not exist yet.
- **Case W2 — the amended demographic contract, full set (rest of AC2).** Assert the wizard demographics step carries the `<input type=date name='date-of-birth'>` + the "only your age is used"/"stays on your machine" guidance + the body-weight NUMBER + lbs/kg unit + `sex-for-dosing` + the OPTIONAL race/ethnicity field + its "kept on your device, never sent to the planner" copy + occupation. (The date/weight inputs already exist from Cycle 1 on `#panel-build`; W2 asserts the wizard's demographics step + the race/occupation/guidance copy.)
- **Case W3 — the rich-domain + safety field names (AC3).** Assert the field names `nutrition-detail` / `supplement-stack` / `peptide-stack` / `training-detail` are present, plus the exercise-safety (chest-pain/dizziness/SOB), apnea, PHQ-2, smoker (Yes/Former→years/Never), alcohol (None/Rarely/Monthly/Weekly/Most-days) controls; assert NO `cannabis` control is present (`rg` returns 0).
- **Case W4 — Documents + API key reuse the existing seams + the key affordance (AC4).** Assert the Documents step submission targets `/upload` → `/confirm-extraction` (the existing flow) and the API-key step targets `/settings/key`; assert every `fetch(...)`/`<form action>` target ∈ `{/chat,/upload,/settings/key,/confirm-extraction,/generate-plan}` (no new path); assert the key-saved affordance markup is present (asterisks/masked + greyed/`disabled` + a `✓ Saved` ~13px state).
- **Case W5 — generic source labels, all three surfaces (AC5, the recurring-flag gate).** (a) The wizard Documents step lists the generic multi-source affordance ("parsed locally", naming Apple Health / Garmin / Whoop / Oura / Fitbit / 23andMe / AncestryDNA) AND presents no hardcoded single-operator-source label as the only source affordance. (b) `_doc_cards`: `rg "Wearable export \(Apple Health\)"` over the rendered SPA returns 0; a SEEDED loaded-wearable status with a NON-"Apple Health" `source` (e.g. `"Garmin"`) renders the generic `"Wearable export"` title + a render-state-driven `· detected: Garmin` suffix (the existing labs-loaded render idiom [test_app_shell.py:405-425]); the empty-store wearable card shows the generic title with NO single hardcoded source. (c) The upload-status copy [app_view.html:816] no longer singles out one operator source (a generic supported-formats list). FAILING-CAPABLE: revert any surface to the hardcoded single "Apple Health" → the matching assertion reds (the title-rg-0, the non-"Apple Health" detected-suffix, or the upload-status copy).
- **Case W6 — the rich fields ARE in the wizard /upload contract (the ADR-0034 supersession positive control, pairs with the Cycle-1 objective-only reconcile).** Assert the wizard surface carries the rich-domain field names that the `#panel-build` "Your details" form does NOT (the comprehensive intake routes them through the wizard → `/upload`) — proving the objective-only reconcile re-scoped, not deleted, the coverage.
- **Case W7 — the inline-asset render gate (AC6).** The AUTHORITATIVE real-emit probe: `generate.run("app")` into tmp roots [`_emit_app`:62-70] returns a `Path` that EXISTS (the wizard adds no off-file asset); assert no `<script src` in the emitted file. RED: the wizard does not exist yet (the emit returns a path WITHOUT the wizard markers the case asserts).

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest -q tests/serve/test_app_shell.py`
Expected: FAIL — W1-W7 red (the wizard markup does not exist); the Cycle-1 cases stay green.

#### Step 6: GREEN — Implement
Changes to make:
- `vault/design/templates/app_view.html` — (a) ADD the 9-step Create-Profile wizard section (mirroring `prototype/intake-onboarding-mockup.html` `#mock-wizard` [:480-491]): the 9 `data-step` step containers with their headings, the Clinical Light design tokens (reuse the existing inline `<style>` classes — `.card`/`.field`/`.inp`/`.btn`/`.opt`/`.qcard`/`.note` etc.; add wizard-specific classes inline only, NO off-file CSS), the demographics step (the amended fields + race/occupation/guidance copy), the rich-domain steps named per the pinned contract (`nutrition-detail`/`supplement-stack`/`peptide-stack`/`training-detail`), the safety controls (exercise-safety/apnea/PHQ-2/smoker/alcohol), the Documents step (the generic multi-source affordance + the existing `/upload`→`/confirm-extraction` flow — reuse the existing inline JS fetch flow, NOT a new path), and the API-key step (the `/settings/key` reuse + the empty-vs-saved keystate: masked/asterisks + greyed `disabled` + `✓ Saved` ~13px). All icons inline `<svg>` (the existing convention) — no off-file asset. NO `cannabis` control. (b) GENERIC-ize the upload-status copy [app_view.html:816] — drop the single-sourced "an Apple Health .zip/.xml, a labs PDF/CSV, or a 23andMe .zip" wording to a generic supported-formats list.
- `vault/design/templates/app_shell.py` — GENERIC-ize the `_doc_cards` wearable card LABEL [:66,68]: the hardcoded `"Wearable export (Apple Health)"` title → `"Wearable export"`; on the LOADED branch append a render-state-driven `· detected: {we['source']}` (the source already in scope [:64]); on the UNLOADED branch a generic title + a generic format hint (NO single source). `render`/`_plan_zone`/`_prefill_form` (Cycle 1) are otherwise unchanged.

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest -q tests/serve/test_app_shell.py`
Expected: PASS — W1-W7 green; the Cycle-1 cases stay green; `generate.run('app')` emits a path (no off-file ref).

#### Step 7: REFACTOR
Review for:
- The wizard reuses the existing inline `<style>` design tokens (Clinical Light) — no duplicated CSS, no off-file stylesheet; every icon is inline `<svg>` (matching `_DOC_ICONS`/the existing inline-SVG convention).
- The Documents step reuses the EXISTING `/upload`→`/confirm-extraction` inline JS flow (the same `fetch('/upload'`/`fetch('/confirm-extraction'` the SPA already carries) — no new fetch path, no duplicated upload handler; the API-key step reuses the existing `/settings/key` flow.
- The generic source affordance is render-state-driven for the DETECTED label (no hardcoded single operator source as the only label) across all three surfaces — the wizard Documents step, the `_doc_cards` wearable title (`· detected: <source>` only when loaded), and the upload-status copy; the static supported-sources hint is generic. The `_doc_cards` loaded `sub` (`we['source']`/`we['count']`) is byte-unchanged (the `"1 readings"` server test stays green).
- The field NAMES match the pinned contract exactly (`nutrition-detail`/`supplement-stack`/`peptide-stack`/`training-detail` + `date-of-birth`/`sex-for-dosing`/`bodyweight-kg`) — a typo'd field name silently breaks T1/T2/T3's capture routing (the format coupling), so confirm against the spec's pinned table.

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest -q tests/serve/test_app_shell.py`
Expected: PASS (no behavior change).

#### Step 8: REGRESSION
Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest -q` (port 8765 free) → full suite green (AC8; the wizard addition regresses no non-T4 test). Run the EXTEND-NOT-REBUILD gate + the scope check one final time:
```
WAVE_BASE=$(git merge-base HEAD origin/main)
git diff --numstat "$WAVE_BASE"..HEAD -- scripts/plan/orchestrate.py scripts/plan/pipeline.py scripts/plan/assemble.py scripts/plan/generate_plan.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/track.py scripts/plan/router.py 'scripts/store/*.py' scripts/serve/server.py scripts/serve/capture.py
git diff --name-only "$WAVE_BASE"..HEAD
```
→ 0 frozen-file rows; `git diff --name-only` lists ONLY `vault/design/templates/app_view.html`, `vault/design/templates/app_shell.py`, `tests/serve/test_app_shell.py` (no python production engine, no server route, no capture edit; the daemon `vault/**` content churn is NOT staged).

**TDD step count: 8 (2 cycles).** Within the ≤10-step guidance. T4's 8 ACs (1-8) span the 9-step wizard scaffold (AC1) + the amended demographics (AC2, both cycles) + the rich-domain/safety field names (AC3) + the existing-seam Documents/API-key + key affordance (AC4) + the generic source labels (AC5) + the inline-asset render gate (AC6) + the `_prefill_form` consumer rework (AC7) + the full-suite regression (AC8). Three Modify files; 0 new python production code (the `app_shell.py` edit is the served-render `_prefill_form` consumer, not a frozen-engine edit).

## Interface Contracts

T4 CONSUMES one FORMAT contract (the pinned capture↔form field-name contract T1/T2/T3 define) and PRODUCES the rendered Create-Profile surface T6/T7 reshape. It edits NO python production engine (EXTEND-NOT-REBUILD).

- **The pinned capture↔form field-name contract (CONSUMED — spec § "Pinned capture↔form field-name contract").** The wizard markup builds to these field names: the rich-domain `nutrition-detail`/`supplement-stack`/`peptide-stack`/`training-detail` (the `_CHAT_RAW_SOURCE_FIELDS` keys [capture.py:94-99]); the demographic `date-of-birth` (full-date `<input type=date>`), `sex-for-dosing`, `bodyweight-kg` (a number → the local series; the unit handling is T2's capture-side concern), `equipment-access-class`; the safety-screen + referral markers (T3's capture region — the wizard renders the controls, T3 routes the answers). A change to a pinned field name is a CONTRACT change at the T4↔T1/T2/T3 seam (the format coupling, not a code dependency — T4 imports no capture artifact; each is independently testable against fixtures/rendered-HTML).
- **Test-ownership boundary — what T4 asserts vs what is T2's (no cross-wave deferred-to-a-non-owning-task coupling).** T4 owns three W1-valid demographic assertions, each stable at W1 AND across W2: (i) the **DOB capture→store round-trip** — a posted full-date `date-of-birth` persists to the `date-of-birth` raw store item via the UNCHANGED `_DOB_FIELD` special-case [capture.py:84,276-281] (W1-valid AND W2-invariant — this DOB raw-source write does not change when T2 repurposes the deriver); (ii) the **weight MARKUP** — the rendered `bodyweight-kg` number input + unit exists (a pure render assertion, no capture); (iii) the **weight PREFILL** — a `bodyweight-kg` reading seeded DIRECTLY into the store renders the `_prefill_form` `value=` (the prefill reads the store directly, bypassing capture). T4 asserts NEITHER a deriver-OUTPUT token value (`training-age-band` exact-age, `bodyweight-band` current-weight+trend) NOR the weight's capture→`bodyweight-kg` store round-trip: BOTH are owned + asserted by T2's `test_router.py`/`test_capture.py` (T2's manifest owns `capture.py` + the `bodyweight-kg` capture special-case + the deriver) and change in lockstep with T2's W2 work. This is deliberate and precise: the weight leg is NOT "invariant via T1's capture routing" — T1 does not route `bodyweight-kg`; T2/W2 adds that routing, so at W1 a posted `bodyweight-kg` field is record-only [capture.py:291-295] and `store.read("bodyweight-kg")` is EMPTY. A weight capture→store assertion in T4's `test_app_shell.py` would therefore be RED at W1 with no T4-manifest remedy (T2's manifest excludes `test_app_shell.py` to avoid a T5/W2 collision); the boundary keeps every T4-owned assertion green at W1 and across the integrated W2 build.
- **`_prefill_form` consumer (REWORKED, the AC7 contract).** After T4: `_DEMOGRAPHIC_SELECTS = ("sex-for-dosing", "equipment-access-class")` (bodyweight-band dropped); the prefill targets the `<input type=date name='date-of-birth'>` `value=` + the `bodyweight-kg` number `value=` + the unchanged select-`<option>` prefill + the unchanged banner inject. **Change control:** T7 reuses this reworked `_prefill_form` for the editable My-Info (the full intake set) — extending the prefill to the WIZARD surface is T7's seam (per placement assumption A2); T4 leaves `_prefill_form` operating over the amended `#panel-build` form.
- **The rendered Create-Profile wizard surface (PRODUCED).** The 9-step wizard section + the amended `#panel-build` "Your details" form. T6/W4 adds the completeness-gate render BRANCH that serves the wizard-only body when incomplete; T7/W5 reshapes `#panel-build` (`ws-tabs` `Build Plan` → `My Info`) into the editable My-Info reusing the reworked `_prefill_form`. T4 leaves the `ws-tabs` labels + the `render` branch + the route table UNCHANGED (those are T6/T7's).
- **`_doc_cards` generic-label (REWORKED, the AC5/NFR-4 contract).** The wearable card title is generic (`"Wearable export"`) with a render-state-driven `· detected: <source>` suffix only when the load-state establishes the source; the loaded `sub` (`we['source']`/`we['count']`/range) is byte-unchanged. The labs/medical/dna cards + the `<!--DOC_CARDS-->` substitution seam are unchanged — consumed by the existing ADR-0030/0031 tests + `test_server.py`'s load-state assertions, which stay green.
- **Frozen surface (PRESERVED).** Every `scripts/plan/*.py` (incl. `router.py`), `scripts/store/*.py`, `scripts/serve/server.py` (the 7-entry route table), and `scripts/serve/capture.py` are byte-unchanged (NFR-2) — T4 is the served-render markup + the `_prefill_form` + `_doc_cards` render-helper consumers, not a python-engine/capture/route edit.

## Verification Checklist

- [ ] AC-1 (9-step wizard matching the mockup step set) — verified by Case W1: the rendered SPA parses to 9 `data-step` containers (1-9) with the headings Demographics / Goals / Training & activity / Diet / Supplements & peptides / Medications / Health & lifestyle / Documents / API key (failing-capable: a missing step reds the count/heading assertion).
- [ ] AC-2 (amended demographics: full-date birthdate + weight number + sex + optional race + occupation, NOT the old "Birth year") — verified by the Cycle-1 AC2 case (the date input + `bodyweight-kg` number on `#panel-build`; the old year-text + `bodyweight-band` select ABSENT) + Case W2 (the wizard demographics step + the guidance copy + race/occupation).
- [ ] AC-3 (rich-domain + safety field names per the pinned contract, no cannabis) — verified by Case W3: `nutrition-detail`/`supplement-stack`/`peptide-stack`/`training-detail` present + the exercise-safety/apnea/PHQ-2/smoker(Yes/Former→years/Never)/alcohol(None/Rarely/Monthly/Weekly/Most-days) controls present + `rg` for `cannabis` returns 0.
- [ ] AC-4 (Documents + API key reuse the existing seams, no new route, key affordance) — verified by Case W4: the Documents step targets `/upload`→`/confirm-extraction`, the API-key step targets `/settings/key`, every fetch/form target ∈ the known loopback set, the key-saved markup (masked/greyed/`✓ Saved` ~13px) present; + the NFR-2 `server.py` numstat=0 gate.
- [ ] AC-5 (GENERIC source labels, render-state-driven, not hardcoded single "Apple Health" — spec AMENDED `[2026-06-30 S103]`, FOLDED INTO T4) — verified by Case W5 across ALL THREE surfaces: (a) the wizard Documents step lists the generic multi-source affordance ("parsed locally", the named sources) with no single-source-only label; (b) `_doc_cards` — `rg "Wearable export \(Apple Health\)"` returns 0, the wearable title is generic + the source render-state-driven (a non-"Apple Health" loaded `source` renders `· detected: <that source>`); (c) the upload-status copy [app_view.html:816] is a generic supported-formats list. Failing-capable: revert any surface to the hardcoded single "Apple Health" → the matching assertion reds. No extra blast radius (no existing test asserts the hardcoded title).
- [ ] AC-6 (inline-asset render gate) — verified by Case W7: the AUTHORITATIVE `generate.run("app")` real-emit probe returns an existing `Path` (no off-file `ValueError`) [render.py:149-176] + no `<script src` in the emitted file (the existing negative probe [test_app_shell.py:98-118] proves the gate reds on one injected off-file ref).
- [ ] AC-7 (`_prefill_form` CONSUMER rework, no silent no-op) — verified by the Cycle-1 consumer-integrity case: a seeded `date-of-birth` full-date + `bodyweight-kg` reading prefill the amended date `value=` + number `value=` AND `rg` returns 0 for the retired year-text + `bodyweight-band`-`<option>` targets (the old targets GONE) + the non-tautological (A≠B) + latest-wins + honest-blank re-keyed cases (failing-capable: revert either the markup or the consumer → the prefill assertion reds).
- [ ] AC-8 (pytest passes) — verified by `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest -q` green (port 8765 free; the reconciled demographic-form suite + the new wizard/prefill cases all green; the 2 named env failures accepted ONLY if 8765 is held).
- [ ] BLAST-RADIUS reconcile (CLAUDE.md "E2E tests updated when behavior changes" + "no tautological tests") — verified by the 8 reconciled cases + 3 constants asserting the AMENDED ADR-0034 contract (the objective-only assertion re-scoped to `#panel-build`, NOT deleted; the rich fields asserted in the wizard contract via Case W6); the round-trip case keeps ONLY the W1-valid legs — the DOB capture→store round-trip (`store.read("date-of-birth")` via `_DOB_FIELD`) + the `sex-for-dosing` WIRED pass-through — with NO `bodyweight-kg` capture→store assertion (that routing is T2's W2 work, EMPTY at W1) and the weight covered via AC-2 markup + AC-7 prefill-from-seeded-store (both bypass capture); so every T4-owned assertion is green at W1 AND invariant across T2's W2 deriver/weight-capture work (no cross-wave deferred-to-a-non-owning-task coupling; the deriver-output + weight-capture contracts are T2's `test_router.py`/`test_capture.py`).
- [ ] NFR-2 (EXTEND-NOT-REBUILD frozen engine + no route/capture edit) — verified by the SHELL gate: `WAVE_BASE=$(git merge-base HEAD origin/main); git diff --numstat "$WAVE_BASE"..HEAD -- <7 frozen files> scripts/plan/router.py 'scripts/store/*.py' scripts/serve/server.py scripts/serve/capture.py` → 0 rows.
- [ ] NFR-3/NFR-5 (0 live spend) — every T4 case runs against `app_shell.render([...])` or the real `generate.run("app")` into tmp roots; 0 network, 0 key, 0 model call, 0 `converse`, 0 `aplus-research` dispatch.
- [ ] Wave-1 → Wave-2 checkpoint gates carried (build plan § Wave 1 → Wave 2 Boundary): the 9-step wizard matching the mockup [AC1] + amended demographics (full-date DOB, weight number+unit, sex, optional race) [AC2] + rich-domain + safety field names per the pinned contract, no cannabis [AC3] + Documents/API-key reusing the existing seams, no new route [AC4] + GENERIC source labels [AC5] + inline-asset render gate passes [AC6] + `_prefill_form` reworked, no stale year-text/band target [AC7]; full suite green; frozen-engine numstat 0; 0 live spend.
- [ ] Scope-discipline check: `git diff --name-only "$WAVE_BASE"..HEAD` lists ONLY the 3 manifest files; any other file needs a written justification (the daemon `vault/**` content churn is NOT staged).

## Commit

```
feat(intake): T4 9-step Create-Profile wizard + prefill rework
```
Stage only:
- `vault/design/templates/app_view.html`
- `vault/design/templates/app_shell.py`
- `tests/serve/test_app_shell.py`

Stage by explicit path: run `git add <path>` as its OWN command, then `git commit` SEPARATELY — the `block-pii-commit` PreToolUse hook denies single-call stage+commit, `git commit -a/--all`, and pathspec `git commit <path>` (their content is invisible to the pre-command staged-set scan). `app_view.html`/`app_shell.py` are NOT `vault/{compounds,biomarkers,library}/` entity pages, so `block-ungated-vault-write.sh` does not gate them — but stage by explicit path regardless; never `git add vault/` blindly (the daemon frontmatter churn re-appears every sync). Do NOT stage `.beads/issues.jsonl` / `harvest.jsonl` / `memory/process-failures.md` — those are close-protocol artifacts. Commit on `feature/intake-build` (never `main`).

## Rollback

If verification fails after 3 fix cycles:
1. `git stash -m "failed-ADR-0033-0035-T4-attempt-{N}"` — preserve work for diagnosis.
2. `git checkout {commit-before-task}` — revert to the entry-state commit (the branch HEAD this task was built on top of).
3. Update the bead tracker: flag ADR-0033-0035-T4 as BLOCKED with the failure log.
4. Escalate: "Task ADR-0033-0035-T4 failed verification after 3 attempts. Stash ref: {ref}. Failure log: {summary}."
5. After abandonment or re-planning, drop all `failed-ADR-0033-0035-T4` entries from `git stash list`.

Do NOT: silently skip a failing acceptance criterion; weaken a test to make it pass (especially the AC7 consumer-integrity "old targets GONE" assertion, the AC5 generic-label gate, or the blast-radius reconcile — a test that cannot go RED on the broken markup/consumer is worthless); rework `_prefill_form` to target a literal that no longer exists (the silent-no-op the consumer-integrity check forbids — HALT, retarget to the date/number inputs); add a new server route or a new fetch path for Documents/API-key (ADR-0033 no-new-route — HALT, reuse `/upload`/`/confirm-extraction`/`/settings/key`); present a hardcoded single operator source ("Apple Health") as the only Documents label (NFR-4 recurring-flag breach — HALT, generic/render-state-driven); add an off-file asset / CDN `<script src>` / bare-path `<img>` (the ADR-0029 inline-asset gate reds it — HALT, inline it); edit any `scripts/plan/*.py` / `scripts/store/*` / `scripts/serve/{server,capture}.py` (the EXTEND-NOT-REBUILD breach NFR-2 + the T1/T2/T3 manifest disjointness rejects — HALT, confine to the render-template + its test); or silently extend `_prefill_form` to a dual-surface prefill engine (placement interpretation B — raise it at the T4↔T7 seam first).

## Report

- Recipe: `docs/task-plan/.pipeline/adr-0033-0035/draft-ADR-0033-0035-T4.md` (status: draft).
- Every ADR-0033-0035-T4 acceptance criterion (AC-1..8) maps to a named Verification-Checklist test/command + the Wave-1 → Wave-2 checkpoint gate; the consumer-integrity (AC7), the generic-label (AC5), and the inline-asset (AC6) RED tests are failing-capable by construction — the AC7 prefill reds if the markup or the consumer regresses (the old targets are GONE, so no vacuous pass), the AC5 generic-label reds on a lone hardcoded operator source, the AC6 real-emit probe reds on one off-file ref — never tautological.
- GROUNDED against the live tree (`feature/intake-build`, fork-point `git merge-base HEAD origin/main` = `12f769f`, 2026-06-30): the `#panel-build` "Your details" form's year-text DOB [app_view.html:466] + the 6-option `bodyweight-band` select [:468]; `_prefill_form` [app_shell.py:212-255], `_DEMOGRAPHIC_SELECTS` [:40], the `placeholder="e.g. 1986"` replace target [:244]; `render` [:258-288] + the `_doc_cards` "Apple Health" hardcode [:66-68]; `render.emit` off-file gate [render.py:149-176, `_ASSET_ATTRS`:37] + `generate.run` tmp-root kwargs [generate.py:35]; the demographic-form/prefill suite [test_app_shell.py:220-339, 865-938]; the mockup `#mock-wizard` 9-step structure [prototype/intake-onboarding-mockup.html:480-491] [VERIFIED]; the wizard section ABSENT from `app_view.html` (the old wizard is `generate.run('intake')`/`intake.py`) [VERIFIED]; frozen-set numstat from `12f769f` → 0 rows [VERIFIED via the build plan's grounding].
- ZERO literal implementation code in the recipe — every change is described as behavior + contract + the convention/exemplar to match, with cited `file:line` seams; the SE authors the markup/python under TDD.
- **BLAST-RADIUS reconcile flagged (CLAUDE.md integration mandates).** The amended demographics break 8 existing `test_app_shell.py` cases + 3 module constants encoding the OLD ADR-0018 objective-only-form split — ALL in T4's manifest, reconciled IN-TASK to the AMENDED ADR-0034 contract (the objective-only assertion RE-SCOPED to `#panel-build`, the rich fields asserted in the wizard via Case W6 — re-scoped, not deleted).
- **CROSS-WAVE DECOUPLING (the judge-rejected D6/D10 defect, FIXED — iter-3).** The round-trip case keeps ONLY the W1-valid legs: (a) the DOB capture→store round-trip (`store.read("date-of-birth")` carries the posted full date via the UNCHANGED `_DOB_FIELD` special-case [capture.py:84,276-281] — W1-valid AND W2-invariant) + (b) the `sex-for-dosing` WIRED pass-through. It asserts NEITHER the deriver-OUTPUT token `training-age-band == "born-1980s"` NOR a `bodyweight-kg` capture→store round-trip. Two cross-wave traps closed: (i) T2/W2 repurposes `_age_band` to the exact age, so a `born-1980s` assertion would RED at W2; (ii) `bodyweight-kg` CAPTURE routing is T2's W2 work (`capture.py` is not in T4's manifest), so at W1 a posted `bodyweight-kg` is record-only [capture.py:291-295] and `store.read("bodyweight-kg")` is EMPTY — a weight→store assertion would RED at the W1 checkpoint with no T4-manifest remedy. T4 covers the weight ONLY via the AC-2 markup (the rendered number input) + the AC-7 prefill-from-seeded-store (reads the store directly, bypassing capture) — both W1-valid. The deriver-OUTPUT contract AND the weight capture→`bodyweight-kg` store round-trip are owned by T2's `test_router.py`/`test_capture.py`. T4 now has ZERO dependency on the W2 deriver/weight-capture behavior, green at W1 and across the integrated W2 build.
- **GENERIC-LABEL fix FOLDED INTO T4 (NFR-4 / the recurring operator-specific-label class; spec AMENDED `[2026-06-30 S103]`, operator-adjudicated IN-scope).** AC5 IS "generic source labels" — it cannot defer while it is a T4 criterion. All THREE operator-specific hardcodes are fixed in T4: the `_doc_cards` wearable title `"Wearable export (Apple Health)"` [app_shell.py:66,68] → generic + render-state-driven `· detected: <source>`; the upload-status copy [app_view.html:816] → a generic supported-formats list; the wizard Documents step → a generic multi-source affordance. T4's `app_shell.py` scope is now `_prefill_form` + `_doc_cards` (NOT the gate/My-Info regions — those stay T6/T7). No extra blast radius: no existing test asserts the hardcoded title (the wearable/`_doc_cards` assertions key on load-state, byte-unchanged).
- **T7 COUPLING flagged (placement assumption A2).** `_prefill_form` at W1 operates over the amended `#panel-build` form; prefilling the WIZARD is T7's seam (when the wizard becomes the gated surface + the editable My-Info reuses the reworked `_prefill_form`). The dual-surface prefill (interpretation B) is NOT silently built — it is raised at the T4↔T7 seam.
- NO blocking spec defect found. The weight-input field name (`bodyweight-kg`, store-item-matching) + the unit-conversion split (T2's capture-side concern, W2) + the placement of the amended demographics (panel-build vs wizard) are implementer-discretionary within the pinned contract — resolved by stated assumptions (A1/A2/A3), not a HALT.
