---
task-id: ADR-0052-T1
source-spec: docs/spec/adr-0052-widen-roster-spec.md
source-build-plan: docs/build-plan/build-plan-adr-0052.md
wave: 1
assigned-agent: SE
reviewers: [Architect, QA]
created: 2026-07-19
status: approved
---

# Recipe: Map the six always-on rich domains into `_AUTHOR_CONTRACT_SECTION`

## Entry State

**Prerequisites:**
- [ ] No predecessor tasks — ADR-0052-T1 is a Wave-1 entry point (Dependencies: None). File-disjoint from and fully parallel to ADR-0052-T2 (which owns `scripts/plan/activation.py`, `scripts/serve/plan_loop.py`, and their tests). T1 owns `scripts/model/client.py` + `tests/model/test_client.py`.
- [ ] Full suite green at the session baseline (record the passed/failed counts; the KNOWN env-floor reds `tests/serve/test_bind.py::test_port_collision_exits_nonzero_fail_loud` + `tests/serve/test_server.py::test_main_reads_no_stdin` are not this task's concern). If any OTHER test fails before you change anything, stop and fix that first.
- [ ] Frozen-six + `track.py` byte-frozen (doubles as the rollback anchor): `git diff --numstat 3ab1c3abb6c995fbaaadcb179735759e4a61d73d -- scripts/store/store.py scripts/store/keying.py scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/router.py scripts/plan/track.py` prints NOTHING.
- [ ] Working tree clean for the two manifest files: `git status --short scripts/model/client.py tests/model/test_client.py` prints nothing.
- [ ] On the Wave-1 feature branch (cut from `main`).

Rollback target = this entry state: `client.py` and `test_client.py` unmodified, frozen surface empty.

## Context Load List

**Files to read:**
- `scripts/model/client.py` — the modify target. Read `_AUTHOR_CONTRACT_SECTION` (@513-518, the 4-entry map to extend — workout(1)/nutrition(2)/peptides(3)/supplements(4)); `_contract_section(domain)` (@525-560, the GENERIC heading-slice already built by ADR-0050-T2: reads `_CONTRACTS_PATH`, matches the FULL heading `## <N>. <slug> —`, slices to the next `## ` line, raises a domain-named `ValueError` for an unmapped domain); `_author_system_prompt(domain)` (the prompt builder that calls `_contract_section`). The `_contract_section` extractor is UNCHANGED by this task — T1 only adds map entries; the mechanism already resolves any mapped domain.
- `design/specialist-plan-contracts.md` §5-§10 — the sections the six new entries anchor: `## 5. endocrine-specialist — Hormonal` (@141), `## 6. cardiovascular-specialist — CV / Metabolic` (@167), `## 7. recovery-specialist — Recovery` (@194), `## 8. sleep-coach — Sleep` (@219), `## 9. longevity-strategist — Longevity (cross-cutting synthesis)` (@244), `## 10. mental-performance-coach — Mind` (@269). Each runs from its `## N. <slug> —` heading to the next `## ` heading with its four `### N — …` sub-parts.
- `tests/model/test_client.py` — the modify target. Read the existing `_contract_section` / `_author_system_prompt` tests from ADR-0050-T2 (the four-domain resolution + heading-slice + single-source AC-6 tests) for the assertion style to extend to the ten.

**Do NOT load:** the ADRs (the spec distilled them — TP-02 drift), the frozen surface (this task modifies none of it), `scripts/serve/*` (T2's + T3's surfaces), `design/specialist-plan-contracts.md` §11-§18 as content (the progressive three §11-§13 stay UN-mapped by OQ-5/bead `a-plus-maxing-00kh`; §14-§18 are not plan-authoring domains).

## Risk Pre-Check

- **Risk (ADR-0052 Consequence-Negative — "plan quality now depends on the §5-§10 contract content"):** gated by AC-1/AC-2. Each §5-§10 section must resolve NON-EMPTY and be sliced to its OWN heading. RED-capable: before the map entries exist, `_contract_section` raises `ValueError` for all six (confirmed: `endocrine` raises today).
- **Risk (ADR-0052 "moderate-reversibility domain vocabulary"):** gated by AC-1's `set(_AUTHOR_CONTRACT_SECTION) == {the ten}` pin.
- **Risk (unsourceable-domain — a mapped domain with no contract section → `_author_rich`→None silently):** gated by AC-1/AC-3 — every one of the ten resolves non-empty AND the full `_author_system_prompt` path binds. Scoped to the always-on ten (§11-§13 remain un-mapped by OQ-5 — a dispatched progressive domain still `_author_rich`→None, honest containment, out of scope).
- **Risk (frozen ADR-0032 six + `track.py` byte-frozen):** gated by AC-5 — the change lands ONLY in `client.py` + `test_client.py`.

## TDD Steps

One TDD cycle (AC-1..AC-4 over 2 files); AC-5/AC-6 are the self-verifying terminal probes.

### Step 1: RED — Write Failing Tests
Add to `tests/model/test_client.py`:

- **AC-1 (all ten resolve, no ValueError; map-set pinned):** assert `len(client._contract_section(d)) > 0` for each of the ten (`workout`, `nutrition`, `peptides`, `supplements`, `endocrine`, `cardiovascular`, `recovery`, `sleep`, `longevity`, `mental-performance`) with NO `ValueError`, and `set(client._AUTHOR_CONTRACT_SECTION) == {those ten}`. RED-today: `_contract_section` raises for the six §5-§10 domains (6/10 raise) — the loop/parametrization REDs on the first §5-§10 case.
- **AC-2 (own-heading slice, no bleed):** for each of the six new domains, assert `_contract_section(d)` CONTAINS its own section-unique anchor AND does NOT contain the NEXT domain's heading text. Use these candidate anchors from each section's `**Acts as:**` line — grep-confirm each returns exactly 1 (`grep -c "<anchor>" design/specialist-plan-contracts.md` == 1) before finalizing; if any is not unique, pick another phrase from that section's body:
  - `endocrine` → `HPA/HPG/HPT coupled-axis interpreter` ; must NOT contain `## 6. cardiovascular-specialist —`
  - `cardiovascular` → `recognize-and-route cardiac safety instrument` ; must NOT contain `## 7. recovery-specialist —`
  - `recovery` → `respond when I'm overtrained/primed` ; must NOT contain `## 8. sleep-coach —`
  - `sleep` → `Orthosomnia-aware` ; must NOT contain `## 9. longevity-strategist —`
  - `longevity` → `over-claim control for the longevity domain` ; must NOT contain `## 10. mental-performance-coach —`
  - `mental-performance` → `nootropic over-claim circuit-breaker` ; must NOT contain `## 11. dermatologist —`
- **AC-3 (full `_author_system_prompt` path binds for the six):** for each new domain assert `_author_system_prompt(d)` returns non-empty and contains that domain's section-unique anchor (exercises the whole prompt-build path, not just `_contract_section` — an incidental break in `_author_system_prompt` for a newly-authorable domain FAILs). NOTE: `_AUTHOR_SPECIALIST`/`_AUTHOR_PAYLOAD_GUIDE` carry only the four renderable personas; §5-§10 fall to the generic default — this is functional (the §N contract body names the specialist role) and OUT of scope (persona entries are author-discretion, not the `_AUTHOR_CONTRACT_SECTION`-only ADR scope). Assert the anchor is present; do NOT assert a §5-§10 persona string.
- **AC-4 (entry-removal RED / non-tautological):** a test that asserts the CLEAN direction (all ten resolve) truthy first, then proves RED-capability: removing any one of the six added entries makes `_contract_section` for that domain raise `ValueError` again. Implement via a `monkeypatch`/`copy`-and-delete on `client._AUTHOR_CONTRACT_SECTION` (delete one added key, assert `pytest.raises(ValueError)` on `_contract_section` for that domain), restoring the map after. The test is invalid unless the entry-removal REDs it.

Run: `.venv/bin/python -m pytest tests/model/test_client.py -q`
Expected: FAIL — AC-1/AC-2/AC-3 reference §5-§10 sections the map does not yet resolve (the six raise `ValueError`).

### Step 2: GREEN — Implement
Single change in `scripts/model/client.py`: add the six entries to `_AUTHOR_CONTRACT_SECTION`, mapping each to its `(section-number, specialist-slug)` exactly as the §5-§10 headings read:
```
"endocrine": (5, "endocrine-specialist"),
"cardiovascular": (6, "cardiovascular-specialist"),
"recovery": (7, "recovery-specialist"),
"sleep": (8, "sleep-coach"),
"longevity": (9, "longevity-strategist"),
"mental-performance": (10, "mental-performance-coach"),
```
No other change — `_contract_section`'s heading-slice (`## {number}. {slug} —` → next `## `) is already generic and resolves each new entry. Confirm the slugs match the on-disk headings BYTE-for-byte (`grep -nE '^## (5|6|7|8|9|10)\. ' design/specialist-plan-contracts.md`).

Run: `.venv/bin/python -m pytest tests/model/test_client.py -q`
Expected: PASS.

### Step 3: REFACTOR
- Confirm the map keys match the domain slugs used in `activation.CARD_DOMAINS` / `plan_driver._ROLE_OF_DOMAIN` (same slugs — no key drift, no fifth-through-tenth key typo).
- No new helper, no cache, no config surface — the extractor is unchanged.

Run: `.venv/bin/python -m pytest tests/model/test_client.py -q`
Expected: PASS.

### Step 4: REGRESSION
Run: `.venv/bin/python -m pytest -q`
Expected: PASS — 0 NEW failures vs baseline (only the 2 known env-floor reds). No behavior change to any existing renderable-domain prompt (the four entries are untouched; the six are additive).

## Interface Contracts

`_AUTHOR_CONTRACT_SECTION` gains six keys; `_contract_section` and `_author_system_prompt` keep their signatures. No shared public interface changes. The DOWNSTREAM edge `ADR-0052-T1 → ADR-0052-T3` is semantic: T3's coherence AC reads `client._AUTHOR_CONTRACT_SECTION` (must equal the always-on ten) and drives the real author path — so the six domains must be present and resolvable, which AC-1 pins. No `server.py`/`care_chat.py` change (the rich-author leg already routes any `active − RENDERABLE` domain through `_author_rich`→`_contract_section`).

## Verification Checklist

- [ ] **AC-1** — all ten `_contract_section(d)` non-empty, 0 ValueError; `set(_AUTHOR_CONTRACT_SECTION) == {the ten}`.
- [ ] **AC-2** — each §5-§10 slice contains its own anchor, not the next domain's heading (anchors grep-confirmed unique first).
- [ ] **AC-3** — `_author_system_prompt(d)` binds for the six (contains the section anchor).
- [ ] **AC-4** — entry-removal REDs the mapped domain (RED-capability proven).
- [ ] **AC-5** — `git diff --numstat 3ab1c3ab -- <six + track.py>` prints nothing.
- [ ] **AC-6** — `tests/model/test_client.py` passes; `.venv/bin/python -m pytest -q` 0 NEW failures.
- [ ] Files modified match the manifest — `git diff --name-only` shows ONLY `scripts/model/client.py` + `tests/model/test_client.py`.

## Commit
```
feat(model): map the six always-on rich domains into the author contract
```
Stage only: `scripts/model/client.py`, `tests/model/test_client.py`, `docs/task-plan/adr-0052-t1.md`.
Body (why): the endocrine/cardiovascular/recovery/sleep/longevity/mental-performance domains now source their §5-§10 contract sections through the already-generic `_contract_section` extractor, so the always-on rich domains are authorable (no ValueError→dropped card) — the authoring half of the four→ten roster widening.

## Rollback
If verification fails after 3 fix cycles: `git stash -m "failed-ADR-0052-T1"`, `git checkout main -- scripts/model/client.py tests/model/test_client.py`, confirm the frozen numstat probe still prints nothing, flag ADR-0052-T1 BLOCKED with the failure log. Do NOT weaken a test to pass (e.g. relax AC-2 to a non-unique anchor that a wrong slice would also satisfy) or map §11-§13 (out of scope, OQ-5).

## Deviations
None expected — the `_contract_section` mechanism is proven (ADR-0050-T2), the §5-§10 sections are machine-extractable by heading boundary, and all ACs are testable at $0.
