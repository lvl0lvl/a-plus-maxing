---
task-id: ADR-0043-T3
source-spec: docs/spec/adr-0041-0046-comprehensive-plan-spec.md
source-build-plan: docs/build-plan/build-plan-adr-0041-0046.md
wave: 4
assigned-agent: SE
reviewers: [Architect, QA]
created: 2026-07-12
status: approved
---

# Recipe: Synthesize One Integrated Plan + Front-Door Redefinition + Grow the Dispatch Roster 4→§1-§13 + Record-Path Re-point (FROZEN-SPINE + ATOMIC)

> *[AMENDED 2026-07-12 — Architect, ADR-0046-T1 roster-growth sequencing (Option C — `ruling-0046-t1-sequencing.md`)]:* the `PLAN_DOMAINS` / `_PLAN_TRANSLATORS` / `_DOMAIN_KIND` / `_ROLE_OF_DOMAIN` dispatch-registry growth 4→§1-§13 was RELOCATED from ADR-0046-T1 (Wave 3) into THIS task, to co-land ATOMICALLY with the front-door re-point + the record re-point that make a >4 roster safe. Growing `PLAN_DOMAINS` BEFORE this wiring converts a fail-closed `ValueError` at `record_plan(rich)` into a raw `KeyError` (ruling Ground-truth Claim 1, `plan_schema.py:368`) and breaks 25 out-of-manifest functional/schema tests (Claim 3, F-011-proven) — the reason the growth waits for its consumer here.
>
> Wave-4 task (build-plan Wave 4 = {ADR-0044-T4, ADR-0044-T3, ADR-0043-T3}). This is the LARGEST Wave-4 task and it carries ONE atomic change with SIX co-landing Wave-3 review beads (all `open`, all titled "0043-T3: …"). **See Deviations #1 (expanded manifest) + #2 (sizing flag).** The atomic change:
> - **grow the dispatch roster** (bead `sgur`): `plan_schema.PLAN_DOMAINS` (dispatch-registry role, RT-009 — distinct from the ADR-0044-T1-retired stored-key role) + `generate_plan._PLAN_TRANSLATORS` + `generate_plan._DOMAIN_KIND` + `plan_driver._ROLE_OF_DOMAIN` grow 4→§1-§13, DERIVED from `activation.CARD_DOMAINS` (bead `sgur` A3 note — one source of truth, never two hand-maintained literals asserted equal);
> - **redefine the front door**: `serve/plan_loop.regenerate` re-enters the care-agent Orchestrator (decompose→brief→collect→reconcile→**synthesize**) — the flat `plan_orchestrator.run_orchestrated` composition is retired from the re-entry; the closed `plan_schema.PLAN_DOMAINS` iterations (`plan_loop.py:77,407`, `server.py:784,804`) become `activation.active_domains(operator_surface)`;
> - **synthesize ONE integrated plan**: the care-agent's new synthesize step composes ONE comprehensive plan version (`domain_programs` + integrated `narrative` + ≥1 dated `milestones` + `monitoring_config`) from the reconciled programs — NOT four disjoint per-domain plans;
> - **re-point the record path**: the synthesize step records via `plan_model.record_plan_version` (already roster-agnostic — it validates via `domain_program.validate`, ZERO `PLAN_DOMAINS`/`_PLAN_VALIDATORS` coupling); the thin per-domain `orchestrate._recorded_result → plan_schema.record_plan` path is retired FROM THE RE-ENTRY (rich domains beyond the four record first-class via `plan_model`, never the thin renderable-validator path).
>
> The six co-landing review beads (each becomes LIVE the instant this task wires the seam-emitter + the comprehensive writer): `ncsy` (single-source the seam-conflict safety finding), `qrg4` (reconcile `read_standing_plan`'s renderable shape + reader coverage), `pule` (single-source the seam key-names in `domain_program.py` + correct the divergent fillers), `ubsp` (pin renderable-identity as a top-level prescription field), `61cy` (`care_chat.collect` fail-loud on a duplicate domain), and the `sgur`-recommended `_check_plan_args` fail-closed guard (RECOMMENDED-not-required — see Deviation #4, needs sign-off).
>
> FROZEN-SPINE SUPERSESSION (operator-signed-off, HARD): `plan_schema.PLAN_DOMAINS`'s dispatch-registry growth + `plan_driver._ROLE_OF_DOMAIN`'s growth are NEW Wave-4 superseded surfaces. Per-ADR numstat (AC-5): the `<always-frozen>` six (`store.py`/`keying.py`/`pipeline.py`/`adjudicate.py`/`adjust.py`/`router.py`) stay byte-frozen numstat=0; the growth surfaces are NON-empty. Landing this trips prior byte-guards → the SHARED Wave-4 frozen-guard reconciliation (owned by neither Wave-4 task, F-011 execution-grounded: stage → run the suite → carve EXACTLY the RED guards, do NOT reason the list) carves the sanctioned Wave-4 surfaces — **this task stages NO guard file** (mirror the `adr-0046-t1.md` entry-state note).
>
> PF-S130-01 (THE CRUX, MANDATORY): the front-door rewire, the record-path re-point, the rich-domain first-class record, and the `ncsy` finding are SWAP/call-site changes — EACH needs a PRODUCTION-PATH integration test with an input distinguishing new-from-old behavior that REDs on a revert. A tautological test (green with OR without the wiring) is a gate gap. Every fixture is SYNTHETIC (0 live-API spend, 0 real operator PII; PUBLIC repo; de-id parked, genetics crown-jewel raw-genotypes-never-cross preserved).

## Entry State

**Prerequisites** (ADR-0043-T3 ← ADR-0043-T2 + ADR-0044-T1 + ADR-0046-T1 per the spec Dependency Map; also consumes ADR-0044-T2's `read_standing_plan` and ADR-0045-T1's `monitoring_compiler`, all Wave≤3, all on this branch's history):

- [ ] Predecessor **ADR-0043-T2** present (the reconciled programs it synthesizes + the collect step it extends; both modify `care_chat.py` in DISJOINT regions): `grep -q "def collect" scripts/serve/care_chat.py && grep -q "def reconcile" scripts/plan/orchestrate.py && grep -q "cross_domain_seams pass" scripts/plan/orchestrate.py && echo OK`.
- [ ] Predecessor **ADR-0044-T1** present (the comprehensive model + the roster-agnostic record path): `grep -q "def record_plan_version" scripts/store/plan_model.py && grep -q "def read_plan_version" scripts/store/plan_model.py && grep -q "def validate_plan_version" scripts/store/plan_model.py && echo OK`.
- [ ] Predecessor **ADR-0046-T1** present (the `active_domains` gate the front door dispatches, over its own `CARD_DOMAINS`): `grep -q "def active_domains" scripts/plan/activation.py && grep -q "^CARD_DOMAINS = frozenset" scripts/plan/activation.py && echo OK`.
- [ ] Predecessor **ADR-0044-T2** present (the `read_standing_plan` mixed-history reader `qrg4` reconciles): `grep -q "def read_standing_plan" scripts/store/plan_model.py && grep -q "read_standing_plan" scripts/plan/track.py && echo OK` (the `track.resolve_plan_progress` consumer, `track.py:113`).
- [ ] Predecessor **ADR-0045-T1** present (the `monitoring_compiler` the synthesize step may use to build the version's `monitoring_config`): `test -e scripts/plan/monitoring_compiler.py && grep -q "def compile_config" scripts/plan/monitoring_compiler.py && echo OK`.
- [ ] **Wave-4 intra-wave serialization — build ADR-0044-T4 BEFORE this task (RULING A):** 0043-T3 has 1 wave of slack (build-plan CPM §Slack: successor 0046-T2 is a Wave-5 leaf), so it is scheduled AFTER 0044-T4 in the wave. 0044-T4 and 0043-T3 edit DISJOINT `plan_model.py` regions — 0044-T4 the `read_plan_version` confirm-hold region (`:235-245`, verified via `grep -n "def read_plan_version"` == `:235`), 0043-T3 the `read_standing_plan` comprehensive branch (`:276-282`, verified via `grep -n "def read_standing_plan"` == `:248`) — non-overlapping (28+ lines apart, distinct functions). **CONSEQUENCE for qrg4 (hard):** 0044-T4 adds a confirm-hold filter to `read_plan_version`, which `read_standing_plan` CALLS (`:275`); a HELD (pending-pointer, unconfirmed) comprehensive version is then filtered out BEFORE the standing branch. So every `qrg4` fixture (`test_read_standing_plan_*`, SF-2, LOW-2) MUST seed UN-HELD comprehensive versions (no pending `plan-confirm::` pointer) — else 0044-T4's filter drops them and `read_standing_plan` falls through to the thin branch, and the renderable-shape assertion tests the wrong branch (a tautology). **Import-block co-edit (finding 7 / CM-2):** 0044-T4 adds an import at `plan_model.py:52-54` (the confirm module). 0043-T3's `qrg4` projection reuses the ALREADY-imported `domain_program` (`:52`, IC-2), so 0043-T3 adds NO new `plan_model` import — the CM-2 import-block collision DISSOLVES (one-sided). If a mechanism change forces a new 0043-T3 import here, it co-edits `:52-54` with 0044-T4 (sequential commits, coordinate the region).
- [ ] `PLAN_DOMAINS` is the CLOSED FOUR at entry (the growth is THIS task): `grep -q '^PLAN_DOMAINS = ("workout", "nutrition", "supplements", "peptides")' scripts/store/plan_schema.py && echo OK`.
- [ ] `activation.CARD_DOMAINS` is the §1-§13 card roster at entry (≥13, the source of truth the growth derives from): `.venv/bin/python -c "from scripts.plan import activation; assert len(activation.CARD_DOMAINS) >= 13; print('OK', len(activation.CARD_DOMAINS))"`.
- [ ] Full test suite at the Wave-3-merged floor: `.venv/bin/python -m pytest -q 2>&1 | tail -1` — capture the ACTUAL entry count (≈`2 failed, ~2632 passed, 8 skipped`; 2642 collected as of the recipe-cut HEAD `c1e1f00e`). The two failures (`tests/serve/test_bind.py::test_port_collision_exits_nonzero_fail_loud`, `tests/serve/test_server.py::test_main_reads_no_stdin`) are PRE-EXISTING env-sensitive OS-port/stdin cases, NOT this task's to fix. **The gate is no-NEW-failure**; a Wave-4 sibling (0044-T3/0044-T4) may have merged first and shifted the passed count — do NOT hardcode it; a 3rd distinct failure at entry → fix that first, it is not this task's to introduce.
- [ ] Working tree "clean" for the task scope — **NOTE the daemon caveat:** a bare `git status` is NOT empty (the basic-memory daemon perpetually re-churns `vault/**/*.md` frontmatter; parked `??` files exist — `vault/library/genetics/*.md`, `docs/**/.verify-*.md`). "Clean" here = (a) no STAGED changes (`git diff --cached --name-only` prints nothing); (b) the Create target ABSENT (below); (c) the manifest source files byte-clean vs `PRE_TASK_HEAD`. Do NOT `git restore vault/` (the daemon re-churns within ~1s — CLAUDE.md vault hygiene).
- [ ] On a feature branch cut off `main`: `feature/comprehensive-plan-wave-4` (the current Wave-4 build branch) or an equivalent `feature/*` — never `main`.
- [ ] Rollback target + AC-5 numstat base recorded: `PRE_TASK_HEAD = git rev-parse HEAD` at task entry — BOTH the `{commit-before-task}` the Rollback Protocol reverts to AND the base the per-ADR AC-5 numstat probe diffs against (mirror `tests/plan/test_generate_plan_uniform.py`'s `PRE_TASK_HEAD` constant — the FIXED entry HEAD is pre-commit-runnable + in-flow RED-capability-demonstrable). Record the ACTUAL entry HEAD at execution (a Wave-4 sibling may have committed first; `PRE_TASK_HEAD` is HEAD when THIS task starts).
- [ ] The Create target is ABSENT (Create-only, no stale/duplicate artifact): `test -e tests/serve/test_orchestrator_synthesize.py && { echo "PRESENT (stop)"; exit 1; } || echo "ABSENT (Create OK)"`.
- [ ] The `<always-frozen>` HARD six byte-frozen at entry (numstat==0 vs origin/main): `git diff --numstat origin/main -- scripts/store/store.py scripts/store/keying.py scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/router.py` prints nothing — invariant 1, every wave. (Verified EMPTY at recipe cut.)
- [ ] **Shared Wave-4 frozen-guard reconciliation carved BEFORE this commit** (owned by neither Wave-4 task; ruling §B4/§B6): the roster growth trips (a) the `PLAN_DOMAINS`-tuple guard `tests/store/test_plan_model.py::test_plan_schema_superseded_set_is_exactly_named`; (b) `plan_driver.py`'s byte-guards — `tests/serve/test_plan_loop_hold.py` (the frozen-spine glob at `:72` freezes `scripts/plan/plan_driver.py`; carve `plan_driver.py` from that glob), `tests/plan/test_confirm_pointer_skip.py`, `tests/runner/test_cadence_runner.py`; AND (c) **`tests/plan/test_activation.py::test_per_adr_scoped_freeze_break_numstat`** — a 0046-T1 per-ADR FREEZE probe whose assertion (b) freezes `plan_schema.py`+`plan_driver.py` against 0046-T1's `PRE_TASK_HEAD` and assertion (d) asserts `PLAN_DOMAINS == the closed four`; BOTH RED once this task grows those surfaces, so the reconciliation MUST carve/repoint this probe too (it was ABSENT from the recipe's prior enumeration — grounded here by grep). The shared reconciliation (F-011: stage the growth → run the suite → carve EXACTLY the RED guards, drop ONLY the grown token, leave the `<always-frozen>` HARD set a member) performs these carves; **this task stages NO guard file**, and its Step-4 regression re-runs the carved guards GREEN. A carve without its behavioral replacement (this task's `test_orchestrator_synthesize.py` roster-growth + front-door + record tests) is a gate gap → HALT (ruling §4). **Dual-touch coordination (`test_plan_loop_hold.py`):** the reconciliation carves the `plan_driver.py` glob-freeze (`:72`, a distinct function `test_frozen_spine_and_only_regenerate_changed` at `:365`) in ITS commit; THIS task rewrites the `set(...)==set(PLAN_DOMAINS)` behavioral assertions (`:113` in `test_marker_before_exposure_temporal`, `:300` in `test_regen_re_deriving_held_content_is_held_again`) in ITS commit — disjoint functions in the same file, sequential commits, no region overlap.
- [ ] **Wave-4 reconciliation-owner build-step is an UPSTREAM precondition (43-4, NOTE):** unlike Wave-2 (build-plan lines 85-93 name a dedicated "Wave-2 frozen-guard reconciliation" first-build-step owned by neither task), the Wave-4 build plan has NO such build-step naming the reconciliation's owner. This recipe treats the reconciliation as a wave-level precondition it depends on but does NOT own or stage; the build-plan amendment naming the Wave-4 reconciliation owner + adding the dedicated build-step is handled SEPARATELY (upstream, 43-4 CONTRACT-ESCALATION — flagged, not resolved here). Cross-reference: `docs/build-plan/build-plan-adr-0041-0046.md` §Wave-4 + the frozen-guard-reconciliation ruling (build-plan line 21).

## Context Load List

**Files to read (the EXPANDED manifest — the spec's 6 + the 3 co-landing-review-bead surfaces; see Deviation #1 — plus the interfaces the wiring consumes):**

- `scripts/store/plan_schema.py:49,270-275,356-368` — the growth + hardening surface. Read: `PLAN_DOMAINS` (`:49`, the closed four → grow the dispatch-registry role); `_PLAN_VALIDATORS` (`:270-275`, STAYS the four — RT-009: rich domains record via `plan_model`, never the thin renderable validators, so this map deliberately DIVERGES from the grown `PLAN_DOMAINS`); `_check_plan_args` (`:356-368`), where `_PLAN_VALIDATORS[domain](plan)` at `:368` KeyErrors for a `PLAN_DOMAINS` member absent from `_PLAN_VALIDATORS` (the `sgur` hardening surface — Deviation #4); `record_plan` (`:371`, the thin path retired from the re-entry).
- `scripts/plan/plan_driver.py:117-122` — `_ROLE_OF_DOMAIN` (the closed four → grow to §1-§13; SOLE-writer file, relocated from 0046-T1). Consumed by `plan_orchestrator._dispatch_domains` (`plan_orchestrator.py:381`, `_ROLE_OF_DOMAIN[domain]` — a dispatched domain absent from it KeyErrors), so the growth keeps it coherent with `PLAN_DOMAINS`.
- `scripts/plan/generate_plan.py:128-136,255-297,520-536` — `_DOMAIN_KIND` (`:128`, the four → grow) + `_RENDERABLE_IDENTITY` (`:255`, the `ubsp` top-level-identity table `_project_renderable` reads) + `_project_renderable` (`:262`, the 58z0 projection whose honest-no-plan boundary rests on top-level identity — `ubsp`; reusable by `qrg4`) + `_PLAN_TRANSLATORS` (`:520`, the four → grow; `compute_plan` at `:537` KeyErrors on a member absent from it). Ground how a grown rich-domain member emits its DOMAIN PROGRAM without a thin renderable validator (records via `plan_model`).
- `scripts/serve/care_chat.py:180-297,307-441` — the Orchestrator control flow. Read: `decompose` (`:233`, T1) + `_care_messages` (`:259`) + `respond` (`:307`, the `active_domains` injection at `:308,333,351,362`) + the capture path `extract.persist_extraction` (`:371`, BYTE-UNCHANGED) + `collect` (`:392`, T2 — the `{item["domain"]: …}` dict-comprehension that silently dedupes a duplicate domain, the `61cy` surface). The NEW **synthesize** step is placed AFTER + DISJOINT from the T1/T2 regions.
- `scripts/serve/plan_loop.py:51,77,130,177-203,407` — `regenerate` (`:130`) → `plan_orchestrator.run_orchestrated` (`:177`, the flat composition to retire from the re-entry); the closed `PLAN_DOMAINS` iterations (`:77` default-specialists, `:407` `_last_regen_date`); `LARGE_CHANGE_THRESHOLD_DOMAINS` (`:51`) + its check (`:203`) — READ-ONLY here, the fraction re-base is ADR-0046-T2 (Wave 5, disjoint region).
- `scripts/serve/server.py:710-821` — `_do_generate_plan` (`:710`): the `for domain in plan_schema.PLAN_DOMAINS` author loop (`:784`) + the post-loop results map (`:804`), and `orchestrate.generate_plans` (`:801`) which records thin OUTSIDE the author `try` (`:785` wraps only `self.client.author`, ruling Claim 2) — the record path a rich domain must NOT reach thin.
- `scripts/plan/orchestrate.py:72-78,265-296,492-521,640-663,749-791` — the `ncsy` + `pule` surface. Read: the `SEAM_WITH_DOMAIN`/`SEAM_NATURE`/`SEAM_CONFLICT` constants (`:76-78`, currently LOCAL to orchestrate — `pule` moves the single source to `domain_program.py`); the uniform `cross_domain_seams` pass (`:492-518`) with the explicit `DEFERRED (ADR-0043-T3, bead a-plus-maxing-ncsy)` comment (`:511-516`) — the seam pass adds to `conflict_held` + `report["seams"]` but `_conflict_safety_finding` (`:265`) reads only `report["conflicts"]`; its two callers at `generate_plans:658-659` and `collate_doctor_visit_queue:785-786` (`domain_conflicts = [c for c in report["conflicts"] if c.get("from")==domain]`) → a seam-only held domain distills a DEGENERATE finding.
- `scripts/store/plan_model.py:58-72,115-175,179-283` — the record + reader contract. Read: the version keys (`_PREFIX_MODEL`, `VERSION_DATE="date"`, `DOMAIN_PROGRAMS="domain_programs"`, `NARRATIVE`, `MILESTONES`, `MONITORING_CONFIG`); `validate_plan_version` (`:115`, requires a non-empty `narrative` str + ≥1 dated `milestones` + a non-empty `monitoring_config` mapping + each `domain_program.validate`-conformant) — the synthesize step MUST satisfy all of these; `record_plan_version` (`:179`); `read_standing_plan` (`:248`, the `qrg4` surface — its comprehensive branch at `:277-282` returns the raw `PRESCRIPTION`, but its docstring `:260-261` claims "the SAME shape as `plan_schema.read_plan` … a pure call-site swap" — FALSE once the writer lands).
- `scripts/plan/domain_program.py:29-33,55-77` — the `pule`/`ubsp` contract surface (change-controlled — Architect-reviewed here). The `cross_domain_seams` field docstring (`:29-33`) says only "a paired-domain reference + a seam-nature/conflict token" but pins NO key-name constants (`pule`); the `prescription` field (`:14,68`) says "a periodized/dated representation" but does NOT pin renderable-identity as top-level (`ubsp`).
- `scripts/store/plan_schema.py:469-511` (`resolve_plan`) — the `{state, plan, specialist, plan_date}` shape `read_plan` returns (`plan = reading["value"]`, the RENDERABLE plan document) — the shape `qrg4` must make `read_standing_plan`'s comprehensive branch match.
- `design/specialist-plan-contracts.md` — the §1-§13 card-emitting roster the growth spans (already encoded in `activation.CARD_DOMAINS`); the §14-genetics/§15-labs cross-cutting-input partition (never card domains).
- `docs/checklists/store-adversarial-tests.md` — the four-category battery the record-path re-point (a store-write surface) satisfies (Risk Pre-Check + the QA Tier-2 dispatch).
- `tests/serve/test_orchestrator_brief.py` + `tests/serve/test_orchestrator_reconcile.py` + `tests/plan/test_activation.py` — the T1/T2/0046-T1 fixture conventions to MIRROR (the canonical DOMAIN PROGRAM shape, the `candidate` shape, the `operator_surface` shape). These also carry the `{paired_domain, seam}` divergent fillers `pule` corrects (`test_activation.py:106`, `test_generate_plan_uniform.py:114,277`, `test_domain_program.py:51,82`, `test_monitoring_compiler.py:74,116`).
- **The roster-coupled front-door BREAKER tests (finding 43-1 — read to rewrite, NOT mirror):** the roster growth 4→§1-§13 breaks the `set(...)==set(plan_schema.PLAN_DOMAINS)` all-recorded/all-dispatched behavioral assertions (these are NOT byte-guards — the frozen-guard reconciliation does NOT touch them; they are ABSENT from the prior manifest). Read each named assertion + its companion `for domain in PLAN_DOMAINS: assert recorded` loop (they rewrite as one cluster to the active-subset contract):
  - `tests/serve/test_server.py` — `:1323` (`payload["results"] == {d:"recorded" ...}`) + `:1329` loop + `:1339` (`sorted(client.calls)==sorted(PLAN_DOMAINS)`) in `test_generate_plan_records_all_domains_and_renders`; `:1457` (`set(results)==set(PLAN_DOMAINS)`); `:1795` (`set(payload["results"])==set(PLAN_DOMAINS)`) + `:1797` loop; `:2151` (`payload["results"]=={d:"recorded" ...}`) + `:2153` loop.
  - `tests/serve/test_plan_loop.py` — `:1073` (`set(promoted_plan)==set(PLAN_DOMAINS)`) in `test_post_promote_tailoring_seam_fires_once`; `:1118` (`all(... for d in PLAN_DOMAINS)` all-recorded).
  - `tests/serve/test_plan_loop_hold.py` — `:113` (`set(promoted)==set(PLAN_DOMAINS)`) + `:300` (`set(_promoted(result))==set(PLAN_DOMAINS)`) + their per-domain loops (`:137/:151/:169/:266/:287/:303/:325`).
  - `tests/plan/test_activation.py` — `:267` (`test_registries_coherent_over_current_four`: `PLAN_DOMAINS == the closed four`) is a BEHAVIORAL closed-four assertion this task supersedes with the grown-roster coherence gate (AC-10); this file is ALREADY in-manifest (pule `:106`), so `:267` is a same-file rewrite. (`:376` is the FREEZE probe — reconciliation-owned, per Entry State.)
- **NOT breakers (verified by grep — keep GREEN, do NOT rewrite):** `tests/plan/test_plan_orchestrator.py` (0 `PLAN_DOMAINS` references — `plan_orchestrator.DEFAULT_DOMAINS` is its own literal, decoupled), `tests/serve/test_plan_loop_regen.py:176` (`c["name"] in plan_schema.PLAN_DOMAINS` is a growth-tolerant membership FILTER, not an equality assertion), `tests/plan/test_tailoring.py:636` (`set(_TAILORING_SECTION_KEYS)==set(PLAN_DOMAINS)` stays true — `_TAILORING_SECTION_KEYS = frozenset(PLAN_DOMAINS)` grows in lockstep).

**Do NOT load:**
- The ADRs (`docs/adr/ADR-0041-*.md` … `ADR-0046-*.md`) — the spec already distilled them; re-reading re-opens resolved trade-offs (TP-02 drift).
- `scripts/plan/monitoring_compiler.py` internals beyond `compile_config`'s signature — the synthesize step CALLS it (or a transitional config), it does not re-implement the grammar (ADR-0045-T1).
- `scripts/plan/tiered_executor.py` / `scripts/runner/daily_monitor.py` (ADR-0045-T2/T3, Wave 5/6 — ABSENT/out of scope) and the `LARGE_CHANGE_THRESHOLD_DOMAINS` fraction re-base (ADR-0046-T2, Wave 5 — a disjoint `plan_loop.py` region).
- The frozen four (`store.py`/`keying.py`/`pipeline.py`/`adjudicate.py`) + `adjust.py`/`router.py` — numstat check surfaces, not edits.
- The guard files that freeze `PLAN_DOMAINS`/`plan_driver.py` — carved by the SHARED Wave-4 frozen-guard reconciliation, NOT this task; this task's Step-4 re-runs them GREEN.
- The other Wave-4 task blocks (0044-T3 `horizons.py`, 0044-T4 `plan_model.py` confirm-hold region) — this recipe covers ONLY the `### ADR-0043-T3` block.

## Risk Pre-Check

**BLOCKING GATE — an ATOMIC frozen-spine + store-surface SWAP with six co-landing safety/contract beads.** Do NOT proceed to GREEN until a real (non-`assert True`), RED-capable test exists for every risk below. A placeholder test, or a production-path test that passes with OR without the wiring (tautological), FAILS this gate. **PF-S132-02:** any review/verify agent that MUTATES source during a falsification MUST use an isolated git worktree — never mutate-and-restore the shared main checkout.

- **Risk (PF-S130-01 — the CRUX: the front-door + record-path + roster-growth + `ncsy` are SWAP/call-site changes; a tautological test is a gate gap; the exact Wave-1 gap Tier-3 caught):**
  - Gate (front-door + active-set, AC-3/AC-4): a PRODUCTION-path test drives `regenerate`/`_do_generate_plan` over a data-driven `operator_surface` and asserts (a) the dispatched set == `activation.active_domains(surface)` (a narrow surface dispatches a proper subset, NOT the closed four), and (b) `regenerate`'s call graph REACHES the Orchestrator synthesize step (the flat `run_orchestrated` re-run is retired). RED-capable: reverting the front door to the closed-`PLAN_DOMAINS` flat composition REDs it (Step 2.5).
  - Gate (roster-growth production-path, AC-7, ex-0046-T1 AC-2): a rich domain D beyond the original four (e.g. `sleep`) DISPATCHES via `active_domains` AND records first-class via `plan_model.record_plan_version` → round-trips via `read_plan_version`. RED-capable + non-tautological: against the un-grown tree (`PLAN_DOMAINS` == the four) D is never dispatched/recorded → the test REDs (Step 2.5 revert).
  - Gate (record re-point): the re-entry records ONE comprehensive version via `plan_model.record_plan_version`, NOT the per-domain thin `record_plan`; a rich domain reaching the thin `record_plan` is the failure this closes. RED-capable: reverting the record call to `orchestrate._recorded_result → record_plan` REDs the comprehensive-round-trip test AND a rich domain KeyErrors.
  - Gate (`ncsy` faithful finding, production-path): a seam-sourced conflict (a `SEAM_CONFLICT`-nature `cross_domain_seams` seam on a pair NOT declared via `meta.conflicts`) yields a FAITHFUL `_conflict_safety_finding` (`with_domain` + nature preserved in `finding_id`/`caution`) reaching the liaison adjudication + the doctor-visit queue — NOT the degenerate `finding_id="conflict:<d>:"` empty-caution finding. RED-capable: reverting the single-source (seam pass no longer emits a `report["conflicts"]`-shaped entry) REDs it.
- **Risk (SAFETY — the `ncsy` single-source must NOT weaken the always-on floors; the load-bearing patient-safety property; ADR-0043 Consequences-Negative-2 fail-OPEN class — BLOCKING):** single-sourcing the seam-conflict channel into `report["conflicts"]` must not double-count, drop, or gate any always-on safety floor (RED-S/LEA, additive-AE, Rx-BPMH), nor change the HELD outcome — the hold itself is already correct (per bead `ncsy`, only the finding DETAIL is degenerate).
  - Gate condition: the 0043-T2 always-on-floor suite (`tests/serve/test_orchestrator_reconcile.py`) stays byte-GREEN; a new assertion confirms a seam-sourced hold and a `meta.conflicts`-sourced hold on the same domain BOTH produce exactly one faithful finding (no double-count). RED-capable: a single-source that duplicates the finding REDs the count assertion.
- **Risk (frozen-spine break — `PLAN_DOMAINS`/`_ROLE_OF_DOMAIN` are NEW Wave-4 superseded surfaces; an un-bounded edit bleeds into the frozen engine / a sibling surface; ADR-0032 one-way door):**
  - Gate (per-ADR-scoped numstat, AC-5): RED must include `test_per_adr_scoped_freeze_break_numstat` (subprocess `git diff --numstat`, mirror `test_generate_plan_uniform.py`'s `PRE_TASK_HEAD` pattern): (a) the growth/wiring surfaces are NON-empty vs `PRE_TASK_HEAD`; (b) `git diff --numstat $PRE_TASK_HEAD -- scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/store/store.py scripts/store/keying.py` is EMPTY; (c) `git diff --numstat origin/main -- <always-frozen HARD six>` is EMPTY (over the whole build). RED-capable: a throwaway edit to any forbidden file makes (b)/(c) RED (proven Step 1-continued, reverted).
- **Risk (RULING D1 — roster-growth blast radius; the Call-Chain Review Rule: growing `PLAN_DOMAINS` is a shared-interface change, so EVERY consumer is enumerated, not only the front door + the 3 lookup registries — an un-enumerated iterator KeyErrors or silently mis-behaves on a grown member):** the recipe covers `server.py`/`plan_loop.py` + `_PLAN_TRANSLATORS`/`_DOMAIN_KIND`/`_ROLE_OF_DOMAIN`, but grew from `grep -rn "PLAN_DOMAINS" scripts/` there are ITERATOR consumers it MUST dispose of. Each is either (a) MOVED to `activation.active_domains`, or (b) asserted GROWTH-TOLERANT (absence-tolerant iteration / per-domain gate / validation-widening / derived) WITH a regression assertion over the grown roster:
  - Gate (grep-driven consumer enumeration, AC-D1): `test_grown_roster_consumers_tolerate_growth` seeds a rich domain D beyond the four and asserts each consumer below behaves correctly over the grown roster (no KeyError, no spurious card/record for an inactive-or-absent D). Per-consumer disposition:
    - `scripts/plan/horizons.py:318` (`for domain in plan_schema.PLAN_DOMAINS`) — **(b) absence-tolerant:** the loop filters `if resolved["state"] is not None: continue` and `if domain not in plan_schema.TRACKED_DOMAINS: continue`, so a grown-but-absent D is skipped. **INTRA-WAVE COORDINATION (RULING D1):** this is 0044-T3's file, SAME Wave 4 — 0043-T3 grows the constant `horizons.py:318` READS. 0044-T3 re-bases this loop onto the comprehensive periodization; the two are file-disjoint at the wave (0044-T3 owns `horizons.py`), so 0043-T3 does NOT edit `horizons.py` — it only grows the constant. Regression assertion: a grown-roster `collect_horizons` over a store with only the four active returns exactly the four's horizons (D absent → 0 spurious horizon).
    - `scripts/plan/tailoring.py:33` (`_TAILORING_SECTION_KEYS = frozenset(plan_schema.PLAN_DOMAINS)`) — **(b) derived/validation-widening:** grows in lockstep with `PLAN_DOMAINS`; `test_tailoring.py:636`'s `set(_TAILORING_SECTION_KEYS)==set(PLAN_DOMAINS)` stays GREEN. No edit; regression assertion pins the derivation.
    - `scripts/plan/tailoring.py:181` (`for domain in plan_schema.PLAN_DOMAINS`) — **(b) absence-tolerant:** the emit-gate `if promoted is not None and domain not in promoted: continue` + `if resolved["state"] is not None: continue` skips a grown D absent from the re-gen's `promoted` (active-subset) set. Regression assertion: a grown-roster `tailor` with `promoted` = the active subset emits sections for exactly that subset, 0 for D.
    - `scripts/plan/plan_orchestrator.py:71` — **(b) DECOUPLED:** this is a COMMENT over an INDEPENDENT literal `DEFAULT_DOMAINS = ("workout","nutrition","supplements","peptides")` (`:73`) that does NOT read `PLAN_DOMAINS`; the roster growth does not touch `plan_orchestrator`, and `run_orchestrated` is retired from the re-entry regardless. Regression assertion: `plan_orchestrator.DEFAULT_DOMAINS` stays the four (decoupled); reconcile the stale "all four `PLAN_DOMAINS`" comment wording to "the default four" (a comment, non-load-bearing).
    - `scripts/serve/confirm.py:202` (`if domain not in plan_schema.PLAN_DOMAINS: raise ValueError`) — **(b) validation-widening (CORRECT):** a rich domain IS now recordable, so it SHOULD be a valid confirm target; growth widens the accepted set as intended. Regression assertion: `confirm_plan_change("sleep", ...)` no longer ValueErrors on the unknown-domain check for a grown member.
    - `scripts/serve/confirm.py:240` (`for confirmed_domain in plan_schema.PLAN_DOMAINS if decision_for(...)==CONFIRMED`) — **(b) absence-tolerant:** the `decision_for == CONFIRMED` gate excludes any grown D with no confirm pointer. Regression assertion: the confirmed-union over a store with no D pointer excludes D. **COORDINATION NOTE (not this task's fix):** `:239` reads a CONFIRMED domain's plan via the THIN `plan_schema.read_plan`; a CONFIRMED rich domain that recorded via `plan_model` would read NO_PLAN there — that is 0044-T4's comprehensive confirm-hold re-base territory (serialized after this task per RULING A), NOT 0043-T3's to fix. Flag `CONFIRM-ESCALATION` if a Wave-4/5 test exercises a confirmed rich domain through `confirm.py:239`.
  - RED-capable: on the un-grown tree D is not a `PLAN_DOMAINS` member, so `test_grown_roster_consumers_tolerate_growth` (which asserts grown-roster behavior) cannot be written tautologically — it REDs pre-growth (D absent from the roster the consumers iterate).
- **Risk (43-1 — the roster growth breaks `set(...)==set(PLAN_DOMAINS)` behavioral assertions across 3+ front-door test modules; these are NOT byte-guards, so the frozen-guard reconciliation does NOT fix them, and they are ABSENT from the recipe's manifest/Step-2-GREEN):** the growth makes `set(recorded/dispatched active subset) != set(grown PLAN_DOMAINS)`, RED-ing the all-recorded/all-dispatched assertions enumerated in the Context Load List (`test_server.py` ~`:1323/:1339/:1457/:1795/:2151`, `test_plan_loop.py` ~`:1073/:1118`, `test_plan_loop_hold.py` ~`:113/:300`, `test_activation.py:267`).
  - Gate condition: these modules are ADDED to the manifest + Step-2 GREEN + the commit stage list; each assertion (+ its companion `for domain in PLAN_DOMAINS: assert recorded` loop) is rewritten to the active-subset contract — the front door dispatches `active_domains(surface)` and records the active subset via `plan_model.read_standing_plan`/`read_plan_version` (NOT the thin `read_plan` all-recorded loop, since the re-entry records ONE comprehensive version, not per-domain thin). RED-capable: this is a GREEN-side manifest completeness gate — Step-4 `.venv/bin/python -m pytest -q` REDs (NEW failures) if these modules are NOT rewritten, which is the exact 43-1 gap this closes.
- **Risk (STORE-SURFACE — the record-path re-point writes via `plan_model`; a widened dedupe key / a cross-stream leak is the S37-S41 keying-escape class, bead `pka`):**
  - Gate (store-adversarial battery, all four categories over the `record_plan_version` re-point surface per `docs/checklists/store-adversarial-tests.md`): (a) cross-stream — a `plan-model::` read for one version stream never returns another; (b) same-timepoint — two distinct versions at one date both persist, an identical re-record is idempotent; (c) dedupe-key boundary — the re-point introduces NO new keying (a grep asserts 0 new keying logic; identity stays `keying`'s frozen `(item, timepoint, source)`, the value RIDES `store.append` unchanged); (d) mutation — widening the dedupe key drives a battery test RED (observe RED, revert; a green under the mutation is tautological and FAILS this gate). NOTE this rides the ALREADY-Security-reviewed `plan_model.record_plan_version` (0044-T1) unchanged — the re-point CALLS it, adds no keying.
- **Risk (`qrg4` — `read_standing_plan`'s "pure call-site swap" premise is FALSE once the writer lands; a consumer gets the raw prescription, not `read_plan`'s renderable shape):** `track.resolve_plan_progress` (`track.py:113`) → `adjust.py` → `generate/maintained.py` + the dashboard consume `read_standing_plan` expecting `read_plan`'s `{state, plan (renderable), specialist, plan_date}`.
  - Gate condition: a test asserts `read_standing_plan` on a comprehensive-WRITTEN domain returns the renderable-plan shape its consumers expect (the projected renderable, via the reused `_project_renderable`, OR a docstring+consumer reconciliation — SE discretion, Architect-reviewed) — NOT the raw `PRESCRIPTION`. PLUS SF-2 (stale-comprehensive-boundary: a comprehensive version dated `< on_date` + a thin plan dated `on_date` → the thin plan resolves) + LOW-2 (a ≥2-domain comprehensive version → domain B returns B's prescription, not A's). RED-capable: the current raw-prescription branch REDs the shape assertion.
- **Risk (`pule` — the seam key-names are NOT single-sourced; divergent test fillers are silently inert):** the canonical seam-edge keys `{with_domain, nature}` live only in `orchestrate.py:76-78`; several fixtures use `{paired_domain, seam/seam_nature}` (`test_activation.py:106`, `test_generate_plan_uniform.py:114,277`, `test_domain_program.py:51,82`, `test_monitoring_compiler.py:74,116`) that `orchestrate.reconcile` reads as `None` → silently dropped (SEC-W3-04). Once specialists emit seams as the PRIMARY conflict channel (this task), a malformed/divergent seam is a silent-drop of a real conflict.
  - Gate condition: pin the seam key-name constants in `domain_program.py` as the single source; `orchestrate.SEAM_*` REFERENCE them; a test asserts a seam authored per the `domain_program` contract's key-names RECONCILES (0 silent drop) AND a genuinely malformed seam is documented-inert. RED-capable: a filler diverging from the pinned key-names is caught (the authored-reconciles test REDs if the reader still only accepts the divergent key).
- **Risk (`ubsp` — the 58z0 honest-no-plan boundary rests on an UNSTATED assumption that renderable identity is a top-level prescription field; a specialist emitting identity inside `blocks` → `_project_renderable` silently returns None → the operator gets NO card, the exact ADR-0046 Consequences-Negative-2 false-negative). VACUITY CORRECTION (43-2):** the projection boundary ALREADY EXISTS on the live tree — `_RENDERABLE_IDENTITY` (`generate_plan.py:255`) + the top-level-identity check (`_project_renderable`, `:291-296`: `if identity is not None and "blocks" in projected: … return None`). So the two projection tests (`test_top_level_identity_prescription_projects` / `test_identity_in_blocks_is_documented_no_plan`) PASS on the un-touched tree — they are CHARACTERIZATION tests of pre-existing behavior, NOT PF-S130-01 swap tests (nothing new is reverted to RED them). `ubsp`'s LOAD-BEARING deliverable is the CONTRACT-DOC PIN in `domain_program.py`: the `prescription` contract must STATE the renderable-identity field is TOP-LEVEL (so the boundary is contractually grounded, not fixture-implied).
  - Gate condition: the LOAD-BEARING test `test_prescription_contract_pins_top_level_identity` asserts the PIN itself — greps/asserts `domain_program.py`'s `prescription` contract states top-level renderable-identity. The two projection tests are LABELED characterization (they pin the pre-existing `_project_renderable` boundary; they do NOT RED on a wiring revert). RED-capability: the pin is doc-enforceable — removing the top-level-identity sentence from `domain_program.py` REDs `test_prescription_contract_pins_top_level_identity` (a doc-presence mutation, added to the Step-2.5 list); the two projection tests are explicitly NOT on the Step-2.5 mutation-RED list (characterization, doc-only pin).
- **Risk (`61cy` — `care_chat.collect` silently drops a duplicate domain; a duplicate silently drops a trigger-bearing candidate → a safety floor never fires; fail-OPEN):** `collect`'s `{item["domain"]: … for item in collected}` dict-comprehension last-wins-dedupes.
  - Gate condition: a test asserts `collect` RAISES (fail-loud) on a duplicate domain in `collected` (now that this task wires the emitter that could emit them). RED-capable: the current silent-dedupe comprehension does NOT raise → the test REDs pre-fix.
- **Risk (`_check_plan_args` KeyError at a production boundary — RT-009 deliberately makes `PLAN_DOMAINS` (grown) and `_PLAN_VALIDATORS` (the four) DIVERGE, so `_PLAN_VALIDATORS[domain]` at `plan_schema.py:368` is a reachable raw `KeyError` for a rich domain, OUTSIDE any try/except; ruling §hardening, RECOMMENDED-not-required — Deviation #4):**
  - Gate condition (IF the guard is signed off, Deviation #4): a test asserts `_check_plan_args`/`record_plan` for a grown `PLAN_DOMAINS` member lacking a validator raises a clean `ValueError`, NOT `KeyError` — fail-closed at the record boundary. RED-capable: without the `domain in _PLAN_VALIDATORS` guard the call KeyErrors → the ValueError assertion REDs.
- **Risk (PF-S131-01 — non-canonical fixtures, MANDATORY):** every `domain_program` fixture uses the CANONICAL PERIODIZED shape (`prescription` = `{"blocks":[{"date","load"}]}`, `monitoring_signals` each with a `VALIDITY_TIERS` tier, a compound-kind `required_labs` non-empty, `cross_domain_seams` the pinned edge shape) and passes `domain_program.validate`; every comprehensive version passes `plan_model.validate_plan_version` (non-empty `narrative` + ≥1 dated `milestones` + non-empty `monitoring_config`). A simplified stand-in (a flat `prescription`, a `{paired_domain}` seam, a milestone-less version) FAILS this gate.

## TDD Steps

**Single cycle.** The roster growth, the front-door rewire, the record re-point, and the roster-coupled test updates are CO-DEPENDENT for GREEN — growing `PLAN_DOMAINS` before the front-door + record re-point land breaks 25 tests (ruling Claim 3), so there is no intermediate GREEN and the core cannot be split (the ATOMIC constraint that relocated the growth here). The six co-landing review beads become LIVE with the same wiring. All acceptance criteria + obligations are therefore properties of ONE atomic unit exercised over the SAME seeded synthetic fixtures (a data-driven `operator_surface` spanning ≥2 domains incl. a rich domain D beyond the four; conformant DOMAIN PROGRAMs declaring a seam; a comprehensive version). RED writes every AC + obligation as a specific failing test BEFORE any GREEN; the mutation-RED falsifications run POST-GREEN (Step 2.5), because the wiring being reverted does not exist until GREEN. **Sizing:** this exceeds the template's right-sized envelope — see Deviation #2; the atomicity forbids splitting the core.

### Step 1: RED — Write Failing Tests

Call `/write-tests` for `scripts/serve/care_chat.py`'s synthesize step + `scripts/serve/plan_loop.py`/`scripts/serve/server.py`'s front-door re-point + the grown registries + the six co-landing beads, creating `tests/serve/test_orchestrator_synthesize.py`, targeting each spec AC + obligation. Mirror the T1/T2/0046-T1 fixture conventions (`tests/serve/test_orchestrator_brief.py`, `test_orchestrator_reconcile.py`, `tests/plan/test_activation.py`). ALL fixtures SYNTHETIC (0 real operator PII, 0 live-API). **PF-S131-01 (MANDATORY):** every `domain_program` fixture canonical + `validate`-passing; every version `validate_plan_version`-passing.

**Ordered RED sub-groups (43-3 — write + observe RED per group for intermediate observability, not one batch).** The task is ONE atomic GREEN, but the RED is written + observed in three ordered groups so a mid-build regression localizes:
- **Group A — core-atomic (write + RED first):** AC-1, AC-2, AC-3, AC-4, AC-5, AC-6, AC-7, AC-8, AC-9, AC-10, AC-11, AC-D1 (grown-roster consumer tolerance), `61cy`, `_check_plan_args` (IF signed off), the store-adversarial battery — the roster growth + front-door + record re-point + synthesize + consumer dispositions.
- **Group B — seam-channel (write + RED second):** `ncsy` (faithful seam-sourced finding + no-double-count) + `pule` (seam key-name single-source + authored-reconciles + malformed-inert).
- **Group C — contract-pins (write + RED third):** `ubsp` (the LOAD-BEARING `test_prescription_contract_pins_top_level_identity`; the two projection tests are CHARACTERIZATION, not swap tests) + `qrg4` (reader renderable-shape + SF-2 + LOW-2).
Each group's RED is observed against its own `-k` selection before GREEN (`pytest tests/serve/test_orchestrator_synthesize.py -k "<group tests>" -q`); the numstat AC-5 RED-capability is the Step-1-continued throwaway-edit probe.

- **AC-1 — Single integrated plan (confirmation #2):** seed two programs declaring a seam; assert the synthesize output is ONE integrated plan carrying dated milestones, NOT a per-domain `results` map with no synthesis — a per-domain map with no synthesis → RED. `test_synthesize_emits_one_integrated_plan`.
- **AC-2 — Milestones first-class:** the synthesized version carries dated `milestones` in the `plan_model` representation — `milestone count >= 1` on a multi-domain plan, each dated (passes `validate_plan_version`'s milestone check). `test_synthesized_plan_carries_dated_milestones`.
- **AC-3 — Front-door redefinition (tension-resolution #31):** `regenerate`'s call graph REACHES the Orchestrator synthesize step (decompose→brief→collect→reconcile→synthesize), NOT the flat `run_orchestrated` composition — the flat re-run is retired from the re-entry. `test_regenerate_reaches_orchestrator_synthesize` (a call-graph/structural assertion + a production-path drive).
- **AC-4 — Active-set wiring:** the front door (`regenerate` + `_do_generate_plan`) dispatches `activation.active_domains(operator_surface)`, NOT the closed `plan_schema.PLAN_DOMAINS` tuple — a data-driven surface changes the dispatched set (composes with ADR-0046 without a double-gate). `test_front_door_dispatches_active_domains`.
- **AC-5 — Per-ADR-scoped freeze-break numstat** — `test_per_adr_scoped_freeze_break_numstat`: (a) the growth/wiring surfaces (`plan_schema.py`/`plan_driver.py`/`generate_plan.py`/`care_chat.py`/`plan_loop.py`/`server.py`/`orchestrate.py`/`domain_program.py`/`plan_model.py`) NON-empty vs `PRE_TASK_HEAD`; (b) `git diff --numstat $PRE_TASK_HEAD -- scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/store/store.py scripts/store/keying.py` EMPTY; (c) `git diff --numstat origin/main -- <always-frozen HARD six>` EMPTY. RED-capable via a throwaway forbidden-file edit (Step 1-continued).
- **AC-6 — Module green:** `tests/serve/test_orchestrator_synthesize.py` passes against synthetic fixtures — 0 live-API, 0 real PII (an import grep confirms 0 live-client import, no real operator-identity literal).
- **AC-7 — Roster-growth production-path (ex-0046-T1 AC-2, PF-S130-01, load-bearing):** a rich domain D beyond the four (e.g. `sleep`) DISPATCHES via `active_domains` AND records first-class via `plan_model.record_plan_version` → `read_plan_version` round-trips D's program (0-of-7 dropped). `test_rich_domain_dispatches_and_records_first_class`; RED against the un-grown tree (Step 2.5).
- **AC-8 — Roster grown:** `len(plan_schema.PLAN_DOMAINS) >= 13`. `test_plan_domains_grown_to_card_roster`.
- **AC-9 — Roster-coupled front-door tests updated to the active-domains contract:** the front-door tests assert the active SUBSET dispatches + records via `plan_model`, NOT `set(results) == set(PLAN_DOMAINS)` all-recorded. `test_front_door_records_active_subset_not_all_domains` (the UPDATED contract; the pre-update `set(results)==set(PLAN_DOMAINS)` shape is retired).
- **AC-10 — §1-§13 coherence gate:** `set(PLAN_DOMAINS) <= set(_PLAN_TRANSLATORS) ∩ set(_ROLE_OF_DOMAIN) ∩ set(_DOMAIN_KIND)` — 0 orphan member over the grown roster (`compute_plan`/`_dispatch_domains`/`_lift_program` never KeyError on a `PLAN_DOMAINS` member). `test_registries_coherent_over_grown_roster`. (NOTE `_PLAN_VALIDATORS` is deliberately EXCLUDED — RT-009: rich domains record via `plan_model`, not the thin validators.)
- **AC-11 / `sgur` A3 — Single source of truth (DERIVE, not assert-equal):** `PLAN_DOMAINS`'s card-emitting set is COMPUTED FROM `activation.CARD_DOMAINS` (change `CARD_DOMAINS` → `PLAN_DOMAINS` follows), never two hand-maintained literals asserted equal. `test_plan_domains_derives_from_card_domains` — asserts `set(PLAN_DOMAINS card-emitting) == set(activation.CARD_DOMAINS)` AND the derivation is structural (a mutation to a `CARD_DOMAINS`-derived helper propagates). RED-capable: two independent literals that happen to match pass an equality assert but FAIL the derivation-propagation assertion.
- **AC-D1 — Grown-roster consumer tolerance (RULING D1, load-bearing):** `test_grown_roster_consumers_tolerate_growth` — over a grown roster with a rich D beyond the four active only in a narrow surface, each enumerated iterator consumer behaves correctly: `horizons.collect_horizons` emits 0 horizon for an absent D (absence-tolerant); `tailoring.tailor` with `promoted`=active-subset emits sections for exactly that subset; `tailoring._TAILORING_SECTION_KEYS` grows in lockstep (derived); `plan_orchestrator.DEFAULT_DOMAINS` stays the four (decoupled); `confirm.confirm_plan_change(D, …)` does NOT ValueError on the unknown-domain check (validation-widening); the confirmed-union excludes an un-pointered D (confirm-gate). RED against the un-grown tree (D ∉ `PLAN_DOMAINS`, so the grown-roster behavior cannot be asserted — non-tautological).
- **`ncsy` — Faithful seam-sourced finding (production-path, PF-S130-01):** `test_seam_sourced_conflict_yields_faithful_finding` — a `SEAM_CONFLICT`-nature seam on a pair NOT declared via `meta.conflicts` yields a `_conflict_safety_finding` preserving `with_domain` + nature (a non-empty `finding_id`/`caution`), reaching the liaison adjudication + the doctor-visit queue; PLUS `test_seam_and_meta_conflict_single_finding_no_double_count` (a domain held by BOTH channels → exactly one faithful finding). RED against the pre-fix degenerate `conflict:<d>:` finding (Step 2.5).
- **`qrg4` — Reader shape + coverage:** `test_read_standing_plan_returns_renderable_shape` (a comprehensive-written domain returns `read_plan`'s renderable shape, not the raw `PRESCRIPTION`) + `test_stale_comprehensive_thin_wins_sf2` (SF-2) + `test_two_domain_comprehensive_returns_domain_b_prescription_low2` (LOW-2).
- **`pule` — Seam key-name single source:** `test_seam_keys_pinned_in_domain_program` (the `{with_domain, nature}` constants are the single source in `domain_program.py`, referenced by `orchestrate.SEAM_*`) + `test_authored_seam_reconciles_no_silent_drop` (a seam authored per the pinned key-names reconciles) + `test_malformed_seam_inert_documented` (a genuinely malformed seam is documented-inert).
- **`ubsp` — Top-level renderable-identity contract (43-2 — LOAD-BEARING is the pin, not the projection):** `test_prescription_contract_pins_top_level_identity` (LOAD-BEARING — asserts `domain_program.py`'s `prescription` contract STATES renderable-identity is a TOP-LEVEL field; a doc-presence pin) + the two CHARACTERIZATION tests `test_top_level_identity_prescription_projects` / `test_identity_in_blocks_is_documented_no_plan` (they pin the PRE-EXISTING `_project_renderable` boundary at `generate_plan.py:255/:291-296` — they PASS on the untouched tree, so they are labeled characterization, NOT PF-S130-01 swap tests, and are NOT on the Step-2.5 mutation-RED list).
- **`61cy` — collect fail-loud:** `test_collect_raises_on_duplicate_domain` — `care_chat.collect` RAISES on a duplicate domain in `collected` (no silent last-wins drop). RED against the current dict-comprehension.
- **`_check_plan_args` guard (IF signed off — Deviation #4):** `test_validatorless_domain_raises_valueerror_not_keyerror` — a grown `PLAN_DOMAINS` member absent from `_PLAN_VALIDATORS` raises `ValueError`, not `KeyError`.
- **Store-adversarial battery (record re-point):** `test_record_repoint_store_adversarial` — the four categories over the `record_plan_version` re-point surface (cross-stream / same-timepoint / dedupe-key-boundary grep / category-(d) mutation-RED).
- **Regression pin:** a lightweight assertion (or via Step 4) that `tests/serve/test_orchestrator_reconcile.py` (the 0043-T2 always-on floors) + `tests/serve/test_orchestrator_brief.py` (T1) stay GREEN — the synthesize is ADDITIVE (disjoint from capture). `tests/plan/test_activation.py`'s ACTIVATION-GATE behavioral tests + pule fillers stay GREEN, but its two roster-coupled assertions are NOT additive: `:267` (`test_registries_coherent_over_current_four`) is REWRITTEN by this task to the grown-roster coherence gate, and `:376` (`test_per_adr_scoped_freeze_break_numstat`) is RECONCILED by the pre-commit frozen-guard reconciliation (Entry State) — both green only AFTER those, not as-is.

Run: `.venv/bin/python -m pytest tests/serve/test_orchestrator_synthesize.py -q`
Expected: FAIL — the synthesize step, the front-door re-point, the grown registries, and the six bead fixes do not yet exist. (If a NON-numstat test passes at this stage — especially AC-7 or the `ncsy`/`qrg4` tests — it is TAUTOLOGICAL or wrong; investigate before GREEN.)

### Step 1 (continued): RED-capability falsification pre-check (NON-SKIPPABLE)

Before implementing, prove the numstat probe bites: a throwaway one-line edit to a forbidden file (a comment in `scripts/plan/pipeline.py`), CONFIRM `test_per_adr_scoped_freeze_break_numstat` goes RED, `git checkout` the file. (The front-door/record/roster/`ncsy` revert-REDs are POST-GREEN — Step 2.5 — the wiring does not exist yet.)

### Step 2: GREEN — Implement

Changes to make (behavioral descriptions, NOT literal code):

- `scripts/store/plan_schema.py` (Modify) — grow `PLAN_DOMAINS`'s dispatch-registry role to the §1-§13 card roster, DERIVED from `activation.CARD_DOMAINS` (one source of truth — e.g. `PLAN_DOMAINS` = an ordering over the card-emitting subset of `CARD_DOMAINS`, never a second hand-maintained literal; A3). `_PLAN_VALIDATORS` STAYS the four (RT-009 divergence — rich domains record via `plan_model`). IF the `_check_plan_args` guard is signed off (Deviation #4): add a fail-closed `if domain not in _PLAN_VALIDATORS: raise ValueError(...)` before the `_PLAN_VALIDATORS[domain]` index, converting the reachable raw `KeyError` at the record boundary into the `ValueError` the callers already handle.
- `scripts/plan/plan_driver.py` (Modify — SOLE writer) — grow `_ROLE_OF_DOMAIN` 4→§1-§13, each rich domain mapping to its role slug (per `design/specialist-plan-contracts.md` / the deployed roster). Keeps `_dispatch_domains`'s `_ROLE_OF_DOMAIN[domain]` lookup coherent with the grown `PLAN_DOMAINS`.
- `scripts/plan/generate_plan.py` (Modify) — grow `_PLAN_TRANSLATORS` + `_DOMAIN_KIND` 4→§1-§13. **D2 return-contract PIN (MUST — no SE discretion on a shape a documented consumer depends on):** `_PLAN_TRANSLATORS[domain]` is consumed by `compute_plan` (`:635`: `plan = _PLAN_TRANSLATORS[domain](recommendations, gates)`), which returns that value as `candidate(plan, …)`'s `plan` and `generate_plan` feeds it to `record_plan` (`:685`) — i.e. the translator's contract is a RENDERABLE `plan` dict|None (the existing four return `{"exercises":[…]}` / `{"items":[…]}` / etc.), NOT the seven-field DOMAIN PROGRAM (the program rides ADDITIVELY under `PROGRAM_KEY` via `_with_program`). **The invariant this task ENFORCES:** rich domains BYPASS `compute_plan` in the re-entry — a rich domain's seven-field PROGRAM comes from the Orchestrator specialist dispatch (`care_chat.collect`→`reconcile`→synthesize) and is stored via `plan_model.record_plan_version`, NEVER from a translator. Both front doors route rich domains to `plan_model` (`plan_loop.regenerate` + `server._do_generate_plan`, IC-3 below), so `compute_plan`/`_PLAN_TRANSLATORS` survives ONLY for the single-domain legacy `generate_plan(one-of-the-four, …)` caller + the pre-model thin history. Therefore a GROWN rich-domain `_PLAN_TRANSLATORS` entry is a coherence-gate (AC-10) no-orphan sentinel + a legacy-reach safety: IF ever reached (a legacy single-domain `generate_plan("sleep", …)`) it MUST return `compute_plan`'s renderable `dict|None` contract — and the `_check_plan_args` guard (Deviation #4) then fail-closes the subsequent `_PLAN_VALIDATORS[domain]` `KeyError` at `plan_schema.py:368`. The four existing renderable translators + `_project_renderable` are unchanged.
- `scripts/serve/care_chat.py` (Modify — ADD the synthesize step AFTER + DISJOINT from the T1 decompose + T2 collect regions; the capture path `:371` + `decompose`/`_derive_goals` `:217-256` BYTE-UNCHANGED):
  - Add the **synthesize** step: from the collected+reconciled programs (the T2 `collect` result), compose ONE comprehensive plan version — `domain_programs` (the reconciled per-domain programs), an integrated `narrative`, ≥1 dated `milestones`, and a `monitoring_config` (via `monitoring_compiler.compile_config`, ADR-0045-T1, OR a transitional config — SE discretion) — satisfying `plan_model.validate_plan_version`, and record it via `plan_model.record_plan_version`. The thin per-domain `record_plan` path is NOT reached from the re-entry.
  - **`61cy`:** `collect` FAILS LOUD on a duplicate domain in `collected` (raise a typed error) rather than the silent last-wins dict-comprehension — a duplicate must not silently drop a trigger-bearing candidate.
- `scripts/serve/plan_loop.py` (Modify — the `regenerate` re-entry region ONLY; the `LARGE_CHANGE_THRESHOLD_DOMAINS` region `:51,203` UNCHANGED — ADR-0046-T2's, disjoint):
  - `regenerate` re-enters the Orchestrator (decompose→brief→collect→reconcile→synthesize) instead of the flat `run_orchestrated` composition; the closed `PLAN_DOMAINS` iterations (`:77` default-specialists, `:407` `_last_regen_date`) read `activation.active_domains(operator_surface)`.
- `scripts/serve/server.py` (Modify — the `_do_generate_plan` handler `:710-821`):
  - The `for domain in plan_schema.PLAN_DOMAINS` author loop (`:784`) + the results map (`:804`) iterate `activation.active_domains(operator_surface)`; the record path re-points to `plan_model.record_plan_version` (the synthesize step / Orchestrator), NOT the per-domain thin `orchestrate.generate_plans → record_plan`. The roster-coupled result shape updates to the active-subset contract (AC-9).
  - **IC-3 (finding 5, MUST state explicitly):** `orchestrate.generate_plans`' THIN record leg is RETIRED from `server._do_generate_plan` (`:801` — the SECOND front door, the twin of `plan_loop.regenerate`), so a rich/active domain does NOT reach the thin `record_plan`'s reachable `KeyError` at `plan_schema.py:368` (`_PLAN_VALIDATORS[domain]` for a validator-less grown member). Both front doors (`plan_loop.regenerate` + `server._do_generate_plan`) now route to the Orchestrator synthesize → `plan_model.record_plan_version`; the `_check_plan_args` guard (Deviation #4) is the fail-closed backstop if any residual path reaches thin `record_plan` with a grown member.
- `scripts/plan/orchestrate.py` (Modify — `ncsy` + `pule` single-source; `reconcile`'s public signature UNCHANGED):
  - **`ncsy`:** single-source the seam-conflict channel — the uniform `cross_domain_seams` pass ALSO emits a `report["conflicts"]`-shaped entry (carrying `from`/`with_domain`/nature) for a `SEAM_CONFLICT`-nature held seam (OR generalize `_conflict_safety_finding` + its two callers `:658`/`:785` to read `report["seams"]` — SE discretion). A seam-sourced held domain then distills a FAITHFUL finding (no degenerate `conflict:<d>:`). Remove the `DEFERRED (ADR-0043-T3, bead a-plus-maxing-ncsy)` comment (`:511-516`). Do NOT double-count a domain held via BOTH channels.
  - **`pule`:** `SEAM_WITH_DOMAIN`/`SEAM_NATURE`/`SEAM_CONFLICT` REFERENCE the constants now pinned in `domain_program.py` (single source), not local string literals.
- `scripts/plan/domain_program.py` (Modify — change-controlled, Architect-reviewed here):
  - **`pule`:** pin the `cross_domain_seams` seam-edge key-name constants (`{with_domain, nature}` + the conflict token) as the single source of truth (module constants), documented in the `cross_domain_seams` contract.
  - **`ubsp`:** pin in the `prescription` contract that the renderable-identity field is a TOP-LEVEL prescription field — the 58z0 honest-no-plan boundary (`_project_renderable`) is now contractually grounded, not fixture-implied.
  - **IC-4 (finding 6):** ADD `0043-T3` to `domain_program.py`'s Change-control list (`:56-61`, currently `ADR-0041-T2 / 0044-T1 / 0043-T2 / 0045-T1 / 0046-T1`) — this task edits the change-controlled seam-key + prescription pins, so it must be a named change-controlled editor.
- `scripts/store/plan_model.py` (Modify — the `read_standing_plan` resolver region ONLY; disjoint from 0044-T4's confirm-hold region + 0044-T2's reader region):
  - **`qrg4`:** the comprehensive branch of `read_standing_plan` (`:276-282`) projects the stored `PRESCRIPTION` to `read_plan`'s renderable-plan shape so the `track.resolve_plan_progress` re-point IS a pure call-site swap (its docstring `:260-261` already claims this shape — currently FALSE once the writer lands). **IC-2 (finding 4 — projection home):** put the prescription→renderable projection in `domain_program` (imported by BOTH `plan_model` `:52` AND `generate_plan`), NOT in `plan_model` importing `generate_plan` (a layering inversion, though cycle-safe). Because `domain_program` is ALREADY imported at `plan_model.py:52`, this adds NO new `plan_model` import (dissolving the CM-2 import-block collision with 0044-T4, per Entry State). **None-handling (specify):** when the projection returns None (a degenerate periodized-only prescription with no top-level identity), `read_standing_plan` returns the honest-no-plan on the READ side — fall through to the thin `plan_schema.read_plan(domain, on_date, root)` result (`:283`), never a raw `{"blocks":[…]}` shell. The chosen path makes the SF-2 + LOW-2 reader tests green.
- **Correct the divergent seam fillers (`pule`):** the `{paired_domain, seam/seam_nature}` fillers in `tests/plan/test_activation.py:106`, `tests/plan/test_generate_plan_uniform.py:114,277`, `tests/plan/test_domain_program.py:51,82`, `tests/plan/test_monitoring_compiler.py:74,116` align to the pinned `{with_domain, nature}` key-names (they are inert filler today; aligning them makes them exercise the real reader). See Deviation #1 (out-of-manifest test edits, sanctioned by the `pule` bead).
- **Rewrite the roster-coupled front-door behavioral tests to the active-subset contract (43-1 — REQUIRED for Step-4 GREEN, else NEW failures):** the roster growth REDs the `set(...)==set(plan_schema.PLAN_DOMAINS)` all-recorded/all-dispatched assertions (+ their companion `for domain in PLAN_DOMAINS: assert recorded` loops) enumerated in the Context Load List. Rewrite each cluster to: the front door dispatches `activation.active_domains(surface)` and records the active SUBSET as ONE comprehensive version — assert via `plan_model.read_standing_plan`/`read_plan_version` that the active subset's programs are present, NOT `plan_schema.read_plan`'s thin all-`PLAN_DOMAINS` loop (the re-entry records ONE comprehensive version, not per-domain thin). Files + anchors:
  - `tests/serve/test_server.py` — `:1323`, `:1329`, `:1339`, `:1457`, `:1795`, `:1797`, `:2151`, `:2153`.
  - `tests/serve/test_plan_loop.py` — `:1073`, `:1118`.
  - `tests/serve/test_plan_loop_hold.py` — `:113`, `:300` (+ their per-domain loops) — DISJOINT from the reconciliation's `plan_driver.py` glob-carve (`:72`, distinct function; see Entry State dual-touch).
  - `tests/plan/test_activation.py:267` — supersede `test_registries_coherent_over_current_four`'s `PLAN_DOMAINS == the closed four` assertion with the grown-roster coherence gate (the AC-10 contract; `:376` is the reconciliation-owned FREEZE probe, NOT rewritten here).

Run: `.venv/bin/python -m pytest tests/serve/test_orchestrator_synthesize.py -q`
Expected: PASS.

### Step 2.5: Falsify the load-bearing tests (POST-GREEN mutation-RED)

The wiring the mutations target now EXISTS. **43-3 — this is a PER-MUTATION evidence checklist, NOT a batch:** for EACH row apply the mutation to the GREEN tree (in an ISOLATED git worktree — PF-S132-02, never mutate-and-restore the shared checkout), run ONLY that row's named test, RECORD the observed RED output (the failing assertion + the pytest one-line summary) in the row, then discard the worktree edit and confirm the test is GREEN again before the next row. A test green under its mutation is tautological and FAILS its AC. Do NOT collapse the rows into one run — the individual RED observation is the evidence.

| # | Mutation | Target test(s) | Observed RED (record the failing assertion + summary) |
|---|----------|----------------|-------------------------------------------------------|
| 1 | revert front door to the closed-`PLAN_DOMAINS` flat composition | `test_regenerate_reaches_orchestrator_synthesize` / `test_front_door_dispatches_active_domains` (AC-3/AC-4) | _record_ |
| 2 | revert record re-point to `orchestrate._recorded_result → record_plan` | `test_synthesize_emits_one_integrated_plan` / `test_rich_domain_dispatches_and_records_first_class` (AC-1/AC-7); a rich domain KeyErrors at the thin path | _record_ |
| 3 | revert the roster growth (`PLAN_DOMAINS` back to the four) | AC-7/AC-8/AC-10 (D never dispatched; `len < 13`; the reach test REDs) | _record_ |
| 4 | revert AC-D1 tolerance (e.g. hard-code a consumer to the closed four, or point `_TAILORING_SECTION_KEYS` at a literal) | `test_grown_roster_consumers_tolerate_growth` | _record_ |
| 5 | revert `ncsy` single-source (seam pass no longer emits the `report["conflicts"]`-shaped entry) | `test_seam_sourced_conflict_yields_faithful_finding` (the degenerate `conflict:<d>:` finding returns) | _record_ |
| 6 | revert `qrg4` (return the raw `PRESCRIPTION`) | `test_read_standing_plan_returns_renderable_shape` | _record_ |
| 7 | revert A3 to two independent literals | `test_plan_domains_derives_from_card_domains`'s derivation-propagation assertion | _record_ |
| 8 | remove the `_check_plan_args` guard (IF landed) | `test_validatorless_domain_raises_valueerror_not_keyerror` (KeyError, not ValueError) | _record_ |
| 9 | make `collect` silently dedupe | `test_collect_raises_on_duplicate_domain` | _record_ |
| 10 | revert `pule` (reader accepts only the divergent `{paired_domain}` key) | `test_authored_seam_reconciles_no_silent_drop` | _record_ |
| 11 | remove the `ubsp` top-level-identity sentence from `domain_program.py`'s prescription contract (doc-presence pin) | `test_prescription_contract_pins_top_level_identity` — NOTE the two `ubsp` PROJECTION tests are characterization and are NOT on this list | _record_ |
| 12 | store-adversarial category-(d): widen the dedupe key to include `value` | a `test_record_repoint_store_adversarial` battery case (a green under the mutation is tautological and FAILS the gate) | _record_ |
| 13 | forbidden-file edit (a throwaway comment in `scripts/plan/pipeline.py`) | `test_per_adr_scoped_freeze_break_numstat` | _record_ |

Run after discarding each worktree mutation: `.venv/bin/python -m pytest tests/serve/test_orchestrator_synthesize.py -q`
Expected: PASS (every mutation discarded; the suite green again). Confirm NO mutation edit remains in the working tree before Step 3.

### Step 3: REFACTOR

Review for:
- The roster growth is DERIVED from `activation.CARD_DOMAINS` in ONE place (A3) — not four re-listed literals; the four registries (`PLAN_DOMAINS`/`_PLAN_TRANSLATORS`/`_DOMAIN_KIND`/`_ROLE_OF_DOMAIN`) stay a coherent set (the coherence gate is a real check, not a tautology).
- The synthesize step composes ONE version via ONE path; the seam-conflict single-source (`ncsy`) is ONE emit point, not a per-caller patch; the seam key-name references (`pule`) are the pinned constants, not re-inlined literals.
- `reconcile`'s public signature is unchanged; `read_standing_plan`'s projection (`qrg4`) reuses `_project_renderable` (no duplicated projection); the capture path + decompose/collect regions of `care_chat.py` are byte-unchanged.
- Remove any dead code introduced during GREEN; confirm the frozen four are untouched.

Run: `.venv/bin/python -m pytest tests/serve/test_orchestrator_synthesize.py tests/serve/test_orchestrator_reconcile.py tests/serve/test_orchestrator_brief.py tests/plan/test_activation.py tests/store/test_plan_model_reader.py -q`
Expected: PASS (the new module + the T1/T2/0046-T1/0044-T2 suites green — the synthesize + growth are additive).

### Step 4: REGRESSION

Run: `.venv/bin/python -m pytest -q`
Expected: PASS at the entry floor — the same two known env-sensitive reds, 8 skipped, the passed count RISES by the new module's tests. **The gate is no-NEW-failure.** Specifically confirm:
- The Wave-4-reconciled guards (the `PLAN_DOMAINS`-tuple guard `tests/store/test_plan_model.py::test_plan_schema_superseded_set_is_exactly_named` + `plan_driver.py`'s byte-guards `tests/serve/test_plan_loop_hold.py`/`tests/plan/test_confirm_pointer_skip.py`/`tests/runner/test_cadence_runner.py` + the 0046-T1 freeze probe `tests/plan/test_activation.py::test_per_adr_scoped_freeze_break_numstat`, all carved by the SHARED Wave-4 frozen-guard reconciliation BEFORE this commit) are GREEN — grep each to confirm ONLY the grown token was dropped and the `<always-frozen>` HARD set remains a member (a reconciled guard that also removed a HARD-frozen surface, or a still-RED guard, HALTs).
- The roster-coupled front-door BREAKER tests (43-1 — REWRITTEN by this task's commit) are GREEN on the UPDATED active-domains contract (AC-9 — the active SUBSET records via `plan_model`, NOT `set(results)==set(PLAN_DOMAINS)`): `tests/serve/test_server.py`, `tests/serve/test_plan_loop.py`, `tests/serve/test_plan_loop_hold.py` (the `:113/:300` set-equality functions — disjoint from the reconciliation's `plan_driver` glob-carve), and `tests/plan/test_activation.py::test_registries_coherent_over_current_four` (`:267`, superseded by the grown-roster coherence gate).
- **NOT breakers (verified by grep — stay GREEN UNCHANGED, do NOT rewrite):** `tests/plan/test_plan_orchestrator.py` (0 `PLAN_DOMAINS` references) + `tests/serve/test_plan_loop_regen.py` (`:176` is a growth-tolerant membership FILTER) + `tests/plan/test_tailoring.py` (`:636` derived-equality grows in lockstep). The prior recipe's `test_plan_loop*.py` glob + `test_plan_orchestrator.py` naming was CORRECTED — those are not the breakers.
- `tests/plan/test_orchestrate.py` (the reconcile behavioral suite) + `tests/serve/test_care_chat.py` stay GREEN (the `ncsy` single-source is additive; the synthesize is disjoint from capture).

Then re-check the frozen spine + scope (invariant 1 + AC-5):
- `git diff --numstat $PRE_TASK_HEAD -- scripts/store/plan_schema.py scripts/plan/plan_driver.py scripts/plan/generate_plan.py scripts/serve/care_chat.py scripts/serve/plan_loop.py scripts/serve/server.py scripts/plan/orchestrate.py scripts/plan/domain_program.py scripts/store/plan_model.py` → NON-empty (the sanctioned growth/wiring/bead surfaces).
- `git diff --numstat $PRE_TASK_HEAD -- scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/store/store.py scripts/store/keying.py` → EMPTY.
- `git diff --numstat origin/main -- scripts/store/store.py scripts/store/keying.py scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/router.py` → EMPTY (the `<always-frozen>` HARD six).
- `git diff --name-only $PRE_TASK_HEAD -- scripts/ tests/` → exactly the nine source files + the corrected filler test files (`pule`) + `tests/serve/test_orchestrator_synthesize.py` — no other scope creep, no guard file staged (those are the Wave-4 reconciliation commit's), no `vault/` daemon-churn file.

## Interface Contracts

This task grows the dispatch roster + redefines the front door + re-points the record path + pins two `domain_program` contract facts + reconciles the reader shape — the seams every downstream Wave-4/5/6 task consumes.

- **Front door (redefined):** `regenerate` / `_do_generate_plan` dispatch `activation.active_domains(operator_surface) -> frozenset[str]` (the active card-emitting SUBSET), NOT the closed `plan_schema.PLAN_DOMAINS` tuple; the re-entry reaches the care-agent Orchestrator (decompose→brief→collect→**synthesize**), the flat `run_orchestrated` composition retired from the re-entry. Consumed by ADR-0046-T2 (its `LARGE_CHANGE_THRESHOLD_DOMAINS` fraction computes against the same active set, Wave 5, disjoint `plan_loop.py` region).
- **Record path (re-pointed):** the synthesize step records ONE comprehensive version via `plan_model.record_plan_version(version, root)` — the version = `{VERSION_DATE, DOMAIN_PROGRAMS: {domain: program}, NARRATIVE (non-empty str), MILESTONES: [{date,…}] (>=1 dated), MONITORING_CONFIG (non-empty)}` per `validate_plan_version`. Rich domains beyond the four record first-class here; the thin per-domain `record_plan` is retired FROM THE RE-ENTRY (it survives for the pre-model thin history the ADR-0044-T2 reader consumes).
- **Grown registries (single source of truth, A3):** `plan_schema.PLAN_DOMAINS` (dispatch role, ≥13) + `generate_plan._PLAN_TRANSLATORS` + `generate_plan._DOMAIN_KIND` + `plan_driver._ROLE_OF_DOMAIN` grow to §1-§13, DERIVED from `activation.CARD_DOMAINS`. Invariant: `set(PLAN_DOMAINS) <= set(_PLAN_TRANSLATORS) ∩ set(_ROLE_OF_DOMAIN) ∩ set(_DOMAIN_KIND)` (0 orphan) AND `set(PLAN_DOMAINS card-emitting) == set(activation.CARD_DOMAINS)`. `_PLAN_VALIDATORS` deliberately DIVERGES (stays the four — RT-009); `_check_plan_args` fail-closes on a validator-less member (IF Deviation #4 signed off).
- **Translator return contract (D2 — PINNED, a documented consumer depends on it):** `_PLAN_TRANSLATORS[domain] -> plan dict | None` returns `compute_plan`'s RENDERABLE plan (the existing four's `{"exercises":[…]}` / `{"items":[…]}` / day-plan / single-compound shape), NEVER the seven-field DOMAIN PROGRAM (the program rides additively under `PROGRAM_KEY`). Rich domains BYPASS `compute_plan` in the re-entry (their program comes from the Orchestrator synthesize, stored via `plan_model.record_plan_version`); the grown rich-domain translator is a coherence-gate (AC-10) no-orphan sentinel + a legacy-single-domain-reach safety that, IF reached, returns the renderable `dict|None` contract. Enforcement: both front doors route rich domains to `plan_model` (IC-3), so no re-entry path reaches `compute_plan` for a rich domain; `_check_plan_args` (Deviation #4) fail-closes any residual thin reach.
- **Roster-growth consumer dispositions (D1 — the full enumeration downstream tasks rely on):** the grown `PLAN_DOMAINS` iterators are all growth-TOLERANT (no consumer moves to `active_domains` beyond the two front doors): `horizons.py:318` absence-tolerant (state + `TRACKED_DOMAINS` filter; 0044-T3's file, intra-wave coordination — 0043-T3 grows the constant 0044-T3 reads); `tailoring.py:33` derived (`frozenset(PLAN_DOMAINS)`); `tailoring.py:181` per-domain emit-gate; `plan_orchestrator.py:71` decoupled (independent `DEFAULT_DOMAINS` literal); `confirm.py:202` validation-widening (a rich domain is a valid confirm target); `confirm.py:240` per-domain confirm-gate. Each carries a regression assertion in `test_grown_roster_consumers_tolerate_growth` (AC-D1).
- **Wave-4 serialization (RULING A):** build 0044-T4 FIRST (0043-T3 has wave-slack). The two edit DISJOINT `plan_model.py` regions — 0044-T4 `read_plan_version` (`:235-245`, adds the confirm-hold filter), 0043-T3 `read_standing_plan` (`:276-282`, the `qrg4` renderable projection). Because `read_standing_plan` calls `read_plan_version`, every `qrg4` fixture MUST seed UN-HELD comprehensive versions (no pending pointer) or 0044-T4's filter drops them before the standing branch. The import-block co-edit (`:52-54`) is one-sided (only 0044-T4 adds an import; 0043-T3's projection reuses the already-imported `domain_program`).
- **`domain_program` contract (pinned):** the `cross_domain_seams` seam-edge key-names `{with_domain, nature}` (+ the conflict token) are single-source constants (`pule`), referenced by `orchestrate.SEAM_*`; the renderable-identity field is a TOP-LEVEL prescription field (`ubsp`). Change-controlled — dependent tasks (ADR-0045-T2 Tier-3 reconciler) MUST NOT change them without Architect review.
- **`read_standing_plan` (reconciled, `qrg4`):** returns `read_plan`'s renderable-plan shape (`{state, plan (renderable), specialist, plan_date}`) on a comprehensive-written domain — the `track.resolve_plan_progress` / `adjust.py` / `maintained.py` / dashboard consumers read the renderable plan, not the raw prescription.
- **Seam-conflict finding (single-sourced, `ncsy`):** a seam-sourced held domain distills a FAITHFUL `_conflict_safety_finding` (`with_domain` + nature preserved) reaching the liaison adjudication + the doctor-visit queue — consumed by ADR-0045-T2's Tier-3.

## Verification Checklist

- [ ] AC-1: ONE integrated plan carrying dated milestones (not a per-domain map) — `test_synthesize_emits_one_integrated_plan`.
- [ ] AC-2: milestones first-class, count ≥1 on a multi-domain plan — `test_synthesized_plan_carries_dated_milestones`.
- [ ] AC-3: `regenerate` reaches the Orchestrator synthesize (flat composition retired) — `test_regenerate_reaches_orchestrator_synthesize`; Step-2.5 flat-composition revert REDs it.
- [ ] AC-4: the front door dispatches `activation.active_domains`, not the closed tuple — `test_front_door_dispatches_active_domains`.
- [ ] AC-5: per-ADR numstat — growth/wiring NON-empty, frozen four EMPTY, `<always-frozen>` six EMPTY — `test_per_adr_scoped_freeze_break_numstat` (RED under a throwaway forbidden-file edit, reverted).
- [ ] AC-6: `.venv/bin/python -m pytest tests/serve/test_orchestrator_synthesize.py` passes (0 live-API, 0 real PII — import grep clean).
- [ ] AC-7 (`sgur` production-path, PF-S130-01): a rich D dispatches via `active_domains` + records first-class via `plan_model.record_plan_version`→`read_plan_version` — `test_rich_domain_dispatches_and_records_first_class`; RED against the un-grown tree (Step 2.5).
- [ ] AC-8: `len(plan_schema.PLAN_DOMAINS) >= 13` — `test_plan_domains_grown_to_card_roster`.
- [ ] AC-9: roster-coupled front-door tests assert the active SUBSET records via `plan_model`, NOT `set(results)==set(PLAN_DOMAINS)` — `test_front_door_records_active_subset_not_all_domains` PLUS the 43-1 in-place rewrites of `tests/serve/test_server.py` (`:1323/:1329/:1339/:1457/:1795/:1797/:2151/:2153`), `tests/serve/test_plan_loop.py` (`:1073/:1118`), `tests/serve/test_plan_loop_hold.py` (`:113/:300`), `tests/plan/test_activation.py:267` — all GREEN on the active-subset contract at Step-4.
- [ ] AC-10: `set(PLAN_DOMAINS) <= _PLAN_TRANSLATORS ∩ _ROLE_OF_DOMAIN ∩ _DOMAIN_KIND` (0 orphan) — `test_registries_coherent_over_grown_roster`.
- [ ] AC-11 / `sgur` A3: `PLAN_DOMAINS` DERIVES from `activation.CARD_DOMAINS` (not two literals asserted equal) — `test_plan_domains_derives_from_card_domains`; Step-2.5 two-literals revert REDs the derivation assertion.
- [ ] AC-D1 (RULING D1): every grown-roster iterator consumer disposed of (horizons/tailoring×2/plan_orchestrator/confirm×2) with a per-consumer regression assertion over the grown roster — `test_grown_roster_consumers_tolerate_growth`; RED against the un-grown tree.
- [ ] D2 translator return contract: `_PLAN_TRANSLATORS[domain]` returns `compute_plan`'s renderable `dict|None` (NOT the seven-field program); rich domains bypass `compute_plan` (Orchestrator→`plan_model`) — a contract test asserts a rich-domain translator's return shape matches `compute_plan`'s consumer expectation (or that no re-entry path reaches `compute_plan` for a rich domain).
- [ ] `ncsy`: a seam-sourced conflict yields a FAITHFUL finding (with_domain+nature) at the liaison + doctor-visit queue, no double-count — `test_seam_sourced_conflict_yields_faithful_finding` + `test_seam_and_meta_conflict_single_finding_no_double_count`; Step-2.5 single-source revert REDs; the 0043-T2 always-on-floor suite stays byte-GREEN.
- [ ] `qrg4`: `read_standing_plan` on a comprehensive domain returns `read_plan`'s renderable shape + SF-2 + LOW-2 — `test_read_standing_plan_returns_renderable_shape` + `test_stale_comprehensive_thin_wins_sf2` + `test_two_domain_comprehensive_returns_domain_b_prescription_low2`.
- [ ] `pule`: seam key-names single-sourced in `domain_program.py`, referenced by `orchestrate.SEAM_*`; an authored seam reconciles (0 silent drop); a malformed seam is documented-inert; the divergent fillers corrected — `test_seam_keys_pinned_in_domain_program` + `test_authored_seam_reconciles_no_silent_drop` + `test_malformed_seam_inert_documented`.
- [ ] `ubsp` (43-2): the LOAD-BEARING deliverable is the contract-doc PIN — `test_prescription_contract_pins_top_level_identity` (asserts `domain_program.py` STATES renderable-identity is a top-level prescription field; on the Step-2.5 mutation list). The two projection tests `test_top_level_identity_prescription_projects` + `test_identity_in_blocks_is_documented_no_plan` are LABELED characterization (pre-existing `_project_renderable` boundary; NOT swap tests, NOT on the mutation list).
- [ ] `61cy`: `care_chat.collect` raises on a duplicate domain — `test_collect_raises_on_duplicate_domain`.
- [ ] `_check_plan_args` guard (IF signed off — Deviation #4): a validator-less grown member raises `ValueError`, not `KeyError` — `test_validatorless_domain_raises_valueerror_not_keyerror`.
- [ ] Store-adversarial battery (record re-point): four categories, category-(d) mutation-RED — `test_record_repoint_store_adversarial`.
- [ ] PF-S131-01: every `domain_program` fixture canonical periodized + `validate`-passing; every version `validate_plan_version`-passing (narrative + ≥1 dated milestone + non-empty monitoring_config).
- [ ] PF-S132-02: every Step-2.5 mutation ran in an isolated git worktree, never a mutate-and-restore of the shared checkout.
- [ ] Frozen spine: `git diff --numstat origin/main -- <always-frozen HARD six>` EMPTY — invariant 1. The `PLAN_DOMAINS`-tuple + `plan_driver.py` guard carves are the Wave-4 frozen-guard reconciliation step's (NOT this task's manifest — this task stages NO guard file).
- [ ] No regression: `.venv/bin/python -m pytest -q` holds the entry floor (no 3rd distinct failure) with the new tests passing.
- [ ] Files modified match the expanded manifest (Deviation #1) — no scope creep beyond the nine source + the corrected filler tests + the new test; any Step-2.5 mutation is discarded, never committed.
- [ ] Interface contracts documented (front door / record path / grown registries / `domain_program` pins / `read_standing_plan` shape / single-sourced finding).

## Deviations

| # | Spec Says | Recipe Says | Reason | Impact |
|---|-----------|-------------|--------|--------|
| 1 | ADR-0043-T3 file manifest = `{care_chat.py, plan_loop.py, server.py, plan_schema.py, plan_driver.py, generate_plan.py}` + `tests/serve/test_orchestrator_synthesize.py`. | The manifest EXPANDS to include `scripts/plan/orchestrate.py` (`ncsy`/`pule`), `scripts/plan/domain_program.py` (`pule`/`ubsp`), `scripts/store/plan_model.py` (`qrg4`), the four filler test files `pule` corrects (`test_activation.py`, `test_generate_plan_uniform.py`, `test_domain_program.py`, `test_monitoring_compiler.py`), AND the THREE roster-coupled behavioral BREAKER test files the growth REDs (`tests/serve/test_server.py`, `tests/serve/test_plan_loop.py`, `tests/serve/test_plan_loop_hold.py`; `test_activation.py:267` is a same-file rewrite, already counted). | The orchestrator brief directs SIX co-landing Wave-3 review beads (all `open`, all titled "0043-T3: …") that each become LIVE the instant this task wires the seam-emitter (`ncsy`/`pule`) + the comprehensive writer (`qrg4`). They are NOT new scope — they are the un-shipped consumer-side of the exact wiring this task lands (Call-Chain Review Rule: new producer / un-updated consumer). The three roster-coupled test files (43-1, QA) carry `set(...)==set(PLAN_DOMAINS)` all-recorded assertions that the growth necessarily REDs — NOT byte-guards (the frozen-guard reconciliation does not touch them), so they MUST be rewritten in this commit. `orchestrate.py` is a Wave-3 superseded surface (0043-T2); `domain_program.py`/`plan_model.py` are new modules already excluded from the glob byte-guards; each edit is Architect-reviewed here. **UPSTREAM FLAG (SPEC — incomplete file manifest, non-blocking):** the spec's 6-file manifest predates the Wave-3 Tier-2/Tier-3 review beads; a `/create-spec` pass should fold the surfaces into the 0043-T3 manifest row. | Faithful to the atomic-change intent (the growth's consumers are ready the instant it lands). The `<always-frozen>` HARD six stay EMPTY; the frozen four stay EMPTY; the store-write primitive unchanged (the record re-point rides `plan_model.record_plan_version` unchanged). |
| 2 | Build plan sizes ADR-0043-T3 at 2.5-3.5 days, ONE task. | **SIZING FLAG (non-blocking):** this recipe spans NINE source files + ~11 spec ACs + 6 co-landing obligations — it exceeds the task-planning template's right-sized envelope (>10 TDD assertions, >3 files). It is NOT split: the roster growth + front-door + record re-point are CO-DEPENDENT for GREEN (growing `PLAN_DOMAINS` before the wiring breaks 25 tests — the ATOMIC constraint that relocated the growth here, ruling Claim 3), and the six beads become live with the same wiring. | The build plan defines it as ONE atomic task; splitting the core is forbidden by the F-011-proven breakage. Per the SE brief, the recipe is produced with the sizing concern flagged (not split unilaterally). | The single TDD cycle carries grouped RED tests + one GREEN + worktree mutation-RED falsifiers, mirroring the 0043-T2/0046-T1 single-cycle-many-ACs exemplars. Reviewers (Architect + QA + `plan-integrity`) should weight the atomicity when gating. |
| 3 | Dependency Map edge `ADR-0044-T1 --> ADR-0046-T1  (shared plan_schema.py; stored-key role retired before dispatch-registry role grows)`. | **UPSTREAM FLAG (BUILD-PLAN — stale edge, non-blocking; ARCHITECT'S LANE — do NOT resolve here):** per Option C (`ruling-0046-t1-sequencing.md §B5`) 0046-T1 no longer writes `plan_schema.py` — the shared-`plan_schema.py` relationship moved to `ADR-0044-T1 --> ADR-0043-T3` (which already exists). The `0044-T1 → 0046-T1` edge's rationale is now stale; ruling §B5 implies remove, but bead `sgur`'s note kept it. The Architect must rule whether to remove the edge. | The edge is harmless to 0043-T3 (0044-T1 is Wave 2, well before this Wave-4 node; the ordering holds via other edges), so it does NOT block this recipe — but the dependency map should be reconciled for correctness. | No impact on 0043-T3 execution. Flagged for the Architect to reconcile the Dependency Map. |
| 4 | Ruling §hardening: `_check_plan_args` should guard `if domain not in _PLAN_VALIDATORS: raise ValueError(...)` — RECOMMENDED-not-required, "must carry operator/orchestrator sign-off before landing (per the no-defensive-programming-without-approval rule)". | The recipe SCHEDULES the guard (an AC + a test) but marks it CONDITIONAL on sign-off. | The guard converts a REACHABLE raw `KeyError` (RT-009 deliberately diverges `PLAN_DOMAINS` from `_PLAN_VALIDATORS`, so a validator-less member exists by construction; the index at `plan_schema.py:368` is OUTSIDE any try/except) into the fail-closed `ValueError` callers already handle — NON-speculative, co-located with the change that makes the divergence real. But it is belt-and-suspenders under Option C (rich domains record via `plan_model`, never the thin path), so per the project no-defensive-programming rule it needs explicit sign-off. | If signed off: land the guard + `test_validatorless_domain_raises_valueerror_not_keyerror`. If declined: drop the guard + its test; the divergence is still safe because rich domains never reach the thin `record_plan` (document the decision). |

## Commit

```
feat(plan): grow roster, front door, record path
```
Stage only: `scripts/store/plan_schema.py`, `scripts/plan/plan_driver.py`, `scripts/plan/generate_plan.py`, `scripts/serve/care_chat.py`, `scripts/serve/plan_loop.py`, `scripts/serve/server.py`, `scripts/plan/orchestrate.py`, `scripts/plan/domain_program.py`, `scripts/store/plan_model.py`, the four corrected filler test files (`tests/plan/test_activation.py` — which ALSO carries the `:267` grown-roster-coherence rewrite, `tests/plan/test_generate_plan_uniform.py`, `tests/plan/test_domain_program.py`, `tests/plan/test_monitoring_compiler.py`), the THREE roster-coupled behavioral breaker test files rewritten to the active-subset contract (`tests/serve/test_server.py`, `tests/serve/test_plan_loop.py`, `tests/serve/test_plan_loop_hold.py`), and `tests/serve/test_orchestrator_synthesize.py`.

(Subject is 48 chars — within the ≤50 convention; it names the three atomic halves (roster / front door / record path). The SE brief's suggested `feat(plan): grow roster + front-door + record re-point` is 54 chars, over budget; the three tokens are load-bearing, so this trims to the ≤50 form keeping all three concepts, per the budget-overage-review rule. Stage with `git add <the paths>` as its OWN command, then `git commit` separately — the PII commit hook denies single-call stage+commit, `git commit -a`, and pathspec `git commit <path>`. Confirm NO Step-2.5 worktree mutation leaked into the checkout, `care_chat.py`'s capture path + decompose/collect regions are byte-unchanged, the `<always-frozen>` six are byte-unchanged, and NO guard file / `vault/` daemon-churn file is staged, before committing. The `PLAN_DOMAINS`-tuple + `plan_driver.py` guard carves are the SHARED Wave-4 frozen-guard reconciliation commit — owned by neither Wave-4 task — this task stages NONE of the guard files.)

## Rollback

If verification fails after 3 fix cycles:
1. `git stash -m "failed-ADR-0043-T3-attempt-{N}"` — preserve the work for diagnosis (also stashes any un-discarded Step-2.5 mutation).
2. `git checkout {commit-before-task}` — revert to the entry-state HEAD (`PRE_TASK_HEAD`) recorded in Entry State (restores all nine source files + the filler tests to their pre-task shape, discards this task's edits; the guard carve-out belongs to the separate Wave-4 reconciliation commit and is reverted with it, not here).
3. Update the issue tracker: flag ADR-0043-T3 as BLOCKED with the failure log.
4. Escalate: "Task ADR-0043-T3 failed verification after 3 attempts. Stash ref: {ref}. Failure log: {summary}." ADR-0043-T3 is the ATOMIC roster-growth + front-door + record re-point ADR-0046-T2 (Wave 5, the fraction threshold over the grown active set) depends on, and it carries six co-landing safety/contract review beads — a BLOCK halts the Wave 4→5 boundary; surface it immediately, do not start any downstream task.
5. Stash cleanup: after abandonment or re-planning, drop all `failed-ADR-0043-T3` entries from `git stash list`.

Do NOT: silently skip a failing acceptance criterion; weaken a test to make it pass (ESPECIALLY the PF-S130-01 production-path revert-REDs — the front-door/record/roster/`ncsy` tests — the always-on-floor safety suite, or the store-adversarial category-(d) mutation-RED; these ARE the anti-tautology + fail-OPEN + keying-escape guards); edit any frozen guard here (the `PLAN_DOMAINS`-tuple + `plan_driver.py` carves are the Wave-4 reconciliation commit's, not this task's — this task stages NO guard file); grow `PLAN_DOMAINS` before the front-door + record re-point land in the SAME commit (the ATOMIC constraint — a partial commit KeyErrors + breaks 25 tests); or proceed to any downstream task with the roster/front-door/record in a broken state.
