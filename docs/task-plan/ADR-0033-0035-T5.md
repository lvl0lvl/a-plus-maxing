---
task-id: ADR-0033-0035-T5
source-spec: docs/spec/adr-0033-0035-intake-onboarding-spec.md
source-build-plan: docs/build-plan/build-plan-adr-0033-0035-intake.md
wave: 2
assigned-agent: SE
reviewers: [QA, plan-integrity]
created: 2026-06-30
status: approved
depends-on: []
---

# Recipe: Equipment Access-Gated 29-Item Checklist UI — the 4-Class Access SELECT (feeds `equipment-access-class`) + the Access-Gated Free-weights(7)/Cardio(6)/Resistance(5)/Bodyweight-accessories(11) Checklist + Bodyweight-Only Hide-and-Note + Distinct Inline `<svg>` Icons; Rendered-HTML-Tested, 0 Live Spend

A Wave-2 markup task (∥ T2 on disjoint manifests). It adds the equipment access screen to the served SPA so the operator declares WHERE they train (the bounded `equipment-access-class` planner token) and checks EXACTLY which 29 items they have (a record-only local baseline the plan programs against). The screen is the served form of the operator-signed-off `prototype/intake-onboarding-mockup.html` equipment screen ([prototype/intake-onboarding-mockup.html:492-511] `#mock-equipment` [VERIFIED]) — the 4-class access SELECT → 4-group / 29-item checklist with the specific cardio machines (treadmill / exercise-bike / stair-step / elliptical / rower / air-bike — NOT a generic "cardio machine"), per-item inline icons, and the bodyweight-only hide-and-note.

T5 is TWO Modify files, NO python production edit:

1. **`vault/design/templates/app_view.html` (Modify)** — add the equipment screen as a DISTINCT region (separate from T4's W1 9-step wizard steps; mirroring the mockup's standalone `#mock-equipment` screen): the access `<select name='equipment-access-class'>` carrying exactly the 4 `capture.EQUIPMENT_ACCESS_CLASSES` option values + a gating hook; the access-gated detail container with the four group headings and 29 item cards, each card carrying an inline `<svg>` icon and submitting under the NEW record-only local field `equipment-detail`; the bodyweight-only hide-and-note element with the pinned copy.
2. **`tests/serve/test_app_shell.py` (Modify)** — add the equipment-screen markup assertions (the 4-class no-drift, the 4-group / 29-item count, the access-gating control + bw-note, the access-class-submit / detail-record-only field-name contract, the distinct-inline-`<svg>` + render-gate pass), mirroring the existing rendered-HTML `rg`/parse patterns + the inline-asset `_emit_app` probe.

T5 edits NO python: `scripts/serve/capture.py` (T1/T2/T3's surface — the access value writes the `equipment-access-class` token via the EXISTING wired path; the detail field routes record-only via the EXISTING `persist_capture` default; see Risk Pre-Check), `scripts/plan/router.py` (T2's surface — `equipment-access-class` is already a live pass-through token [router.py:28,110-115,558-559]), `vault/design/templates/app_shell.py` (T4/T6/T7's surface), the frozen engine + store are all untouched (EXTEND-NOT-REBUILD, NFR-2). Per QA-F2, T5 is the MARKUP half — `test_app_shell.py` only — and does NOT add `tests/serve/test_capture.py` (the record-only ROUTING of the detail set is T1's `test_capture.py` contract, the existing default-record-only path; T5 asserts only the field-NAME contract the markup submits). The build is RENDERED-HTML / `render.emit`-tested at 0 live spend, 0 network.

## Entry State

**Prerequisites:**
- [ ] On feature branch `feature/intake-build` ([VERIFIED `git branch --show-current` = `feature/intake-build`]; the branch carries the ADR/spec/build-plan docs commits on top of `main`). The fork-point `<wave-base>` is `git merge-base HEAD origin/main` = `12f769f` ([VERIFIED 2026-06-30] — the docs commits touch no code, so the frozen-engine numstat reads 0 from this base).
- [ ] Wave-1 checkpoint PASSED (build plan § Wave 1 → Wave 2 Boundary): T4 (W1) landed the 9-step Create-Profile wizard markup in `app_view.html` (Demographics / Goals / Training & activity / Diet / Supplements & peptides / Medications / Health & lifestyle / Documents / API key — NONE of these nine steps is "Equipment", per spec T4 AC-1); T1 (W1) landed the capture routing skeleton. T5 builds the equipment screen as a distinct region OVER T4's W1 `app_view.html` (the shared-file sequencing T4(W1)→T5(W2), dependency-ordered across waves, NOT an intra-wave collision — build plan Multi-agent coordination). **Grounding note:** this recipe was grounded against the PRE-T4 live tree (the current `app_view.html` carries `equipment-access-class` only as a legacy select in the pre-rework demographic form [app_view.html:469]); at T5's actual entry that form is T4-reworked. The pinned `equipment-access-class` contract (4 values, /upload submit) is stable across that rework, so the recipe is entry-valid either way — see the Risk Pre-Check single-authoritative-select seam.
- [ ] Working tree clean of code changes beyond the W1 wave: `git status` shows the W1 task files + the basic-memory daemon's `vault/**` frontmatter churn (cosmetic, daemon noise — leave it uncommitted, never `git add vault/` blindly; CLAUDE.md § Vault hygiene). T5's two manifest files carry no un-staged unrelated edit at entry.
- [ ] Full test suite green at the wave-2 entry baseline: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest -q` with port 8765 FREE (0 failed). If the operator's intake server holds 8765, the baseline accepts EXACTLY the two pinned env failures (`tests/serve/test_bind.py::test_port_collision_exits_nonzero_fail_loud` + `tests/serve/test_server.py::test_main_reads_no_stdin`) and NOTHING ELSE — the documented env delta (build plan § Full-suite gate). Free the port first or accept exactly those two.
- [ ] Loopback default port 8765 FREE: `! lsof -nP -iTCP:8765 -sTCP:LISTEN >/dev/null 2>&1 && echo "8765 FREE"`. T5's pytest is pure render/parse (no socket bind in its cases), but the full-suite gate needs the port free for an unambiguous 0-failed.
- [ ] `prototype/intake-onboarding-mockup.html` present (operator-signed-off): `test -f prototype/intake-onboarding-mockup.html && echo OK` ([VERIFIED]). The equipment screen is `#mock-equipment` [:492-511]; the access SELECT [:501], the `#equip-detail` container [:502], the four `seclab` group headings + the 29 `equip` item cards [:504], the `#equip-count` baseline note [:505], the `#equip-bw-note` [:507].
- [ ] `capture.EQUIPMENT_ACCESS_CLASSES` = `("full-home-gym", "commercial-gym", "minimal-equipment", "bodyweight-only")` ([capture.py:119] [VERIFIED]) — the 4 access classes feeding `equipment-access-class`; `equipment-access-class` is a `WIRED_TOKEN` [capture.py:63] + a `_BOUNDED_ENUMS` member ([capture.py:125] `({e.lower() for e in EQUIPMENT_ACCESS_CLASSES}, False)` [VERIFIED]); `persist_capture`'s `else` branch routes "everything else record-only" ([capture.py:291-295] [VERIFIED] — so a NEW field named `equipment-detail`, absent from `WIRED_TOKENS`, routes record-only by construction). T5 reads these to NAME the markup correctly; it modifies none of them.
- [ ] `equipment-access-class` is a live `SUMMARY_FIELD_SET` pass-through token ([router.py:28], with the ADR-0018-T1 note [router.py:110-115,558-559] that the demographic equipment selection is its ONE authoritative source [VERIFIED]) — T5's markup feeds it; router.py is byte-untouched (T2's surface).
- [ ] `tests/serve/test_app_shell.py` harness present: `from scripts.generate import generate, render` [:24]; `_emit_app(tmp_path)` driving the real `generate.run('app')` into tmp roots [:62-70]; `_spa_html()` = `app_shell.render([])` [:73-75]; `_select_block(html, name)` [:255-259], `_form_field_names(form_html)` [:250-252], `_upload_form_html(html)` [:243-247]; the authoritative inline-asset probes `test_generate_run_app_emits_path` [:83-95] + `test_offfile_ref_makes_emit_raise` [:98-118] + the re-run pattern `test_t3_additions_keep_the_spa_inline_asset_clean` [:428-432]; the no-drift select test `test_demographic_selects_built_from_the_gate_constants` [:274-286] ([VERIFIED]).
- [ ] `render.emit` off-file gate present: `scripts/generate/render.py` `emit` [:149] RAISES `ValueError` + writes nothing when the assembled SPA carries any EXTERNAL asset reference (`_is_external` [:46] — http(s)://, protocol-relative, non-`data:`/non-fragment; `_external_references` [:113]); inline `<svg>` + `data:` URIs pass ([VERIFIED]). T5's per-item icons are inline `<svg>` (no off-file `src`), so `generate.run('app')` returns a written Path (AC-4).
- [ ] Target files exist (both Modify): `test -f vault/design/templates/app_view.html && test -f tests/serve/test_app_shell.py && echo OK` ([VERIFIED]).
- [ ] `plan-integrity` has grounded the plan + attested the Wave-1 checkpoint Go/No-Go RAN green: every Wave-2 task's inputs confirmed against the LIVE tree (`stat`/`grep`, not plan prose); the dependency map confirmed an acyclic DAG with artifact-named edges (T5 ← the pinned `equipment-access-class` format contract, a coupling not a code edge — `depends-on: []`); each wave's checkpoint confirmed runnable; the prior-wave (W1) checkpoint attested green (T1 + T4 RAN green).

This entry state doubles as the rollback target — the entry-state commit is the W2 working state (post-W1) this task is built on top of.

## Context Load List

**Files to read:**
- `prototype/intake-onboarding-mockup.html` § `#mock-equipment` [:492-511] — the AUTHORITATIVE design: the access `<select id='equip-access' onchange='onEquipAccess()'>` with the four options Home gym / Commercial gym / Minimal equipment / Bodyweight only [:501]; the `#equip-detail` gated container [:502]; the four `.seclab` group headings — `Free weights &amp; racks`, `Cardio machines`, `Resistance machines`, `Bodyweight &amp; accessories` — and the 29 `.equip` item cards each with an `.eq-ic` inline `<svg>` + an `.eq-name` + an `.eq-chk` check svg [:504]; the `#equip-bw-note` (`display:none`) carrying "No equipment needed — we'll program bodyweight-only." [:507]; the `#equip-count` baseline note [:505]. MATCH the item set + group counts EXACTLY (7+6+5+11=29). Read for the structure + the specific cardio-machine names; the pixel/copy beyond the pinned structure is discretionary (NFR-5).
- `scripts/serve/capture.py` — `EQUIPMENT_ACCESS_CLASSES` [:119] (the 4 access-class tokens the access `<option value=…>`s MUST equal), `WIRED_TOKENS` [:50-65] (carries `equipment-access-class` [:63]; a field NOT here routes record-only), `_BOUNDED_ENUMS` [:120-126] (`equipment-access-class` entry [:125]), `persist_capture`'s record-only `else` branch [:291-295], the `assert set(WIRED_TOKENS) <= set(SUMMARY_FIELD_SET)` tripwire [:193]. READ to NAME the markup; modify NONE of it.
- `scripts/plan/router.py` — `SUMMARY_FIELD_SET` [:23-56] carrying `equipment-access-class` [:28], the ADR-0018-T1 pass-through note [:110-115,558-559] (the demographic equipment selection is `equipment-access-class`'s one authoritative source). READ to confirm the token's planner-input contract; modify NONE of it (T2's surface).
- `tests/serve/test_app_shell.py` — the harness above [:24,:62-70,:73-75,:243-259], the no-drift select pattern `test_demographic_selects_built_from_the_gate_constants` [:274-286] (the MIRROR for AC-1's 4-class no-drift via `_select_block` + `capture.EQUIPMENT_ACCESS_CLASSES`), the inline-asset re-run pattern `test_t3_additions_keep_the_spa_inline_asset_clean` [:428-432] (the MIRROR for AC-4's render-gate re-assert). The new equipment cases MATCH these rendered-HTML parse + `_emit_app` shapes.
- `vault/design/templates/app_view.html` — the current structure: the workspace tabs [:456-458] (still `My Plan | Build Plan | Generate` — T7's rename, NOT T5's), the legacy demographic `equipment-access-class` select [:469] (pre-T4-rework). READ to slot the equipment screen as a distinct region within T4's W1 wizard structure; do NOT touch the tabs (T7) or the T4 wizard steps.

**Do NOT load:** the ADRs (`docs/adr/ADR-003{3,4,5}*.md`) + the dispositions (`docs/spec/.pipeline/adr-0033-0035/`) — the spec/build-plan distilled them; `scripts/serve/capture.py` body internals beyond the symbols above (T1/T2/T3's surface — T5 makes 0 capture edits); `scripts/plan/router.py` deriver bodies (T2's surface); `vault/design/templates/app_shell.py` `render`/`_prefill_form` (T4/T6/T7's surface — T5 makes 0 app_shell edits); every `scripts/plan/*.py` + `scripts/store/*` body — byte-frozen, not read-to-modify (NFR-2); `scripts/serve/care_review.py` / `referral.py` (T8/T3's, sibling tasks); any other `vault/**` content file — daemon-churned.

## Risk Pre-Check

This task carries the spec's ADR-0033-0035-T5 Risk Mitigations + the build plan's Wave-2 hard constraints + QA-F2. Before proceeding to RED, verify a test exists (or the RED phase adds one) for EACH; a placeholder test (`assert True`) fails this gate. Each gate condition references a live test/command, not a subjective assessment.

- **MATCHES-THE-SIGNED-OFF-MOCKUP — the 4-group / 29-item checklist + the specific cardio machines (crit 1,2,3 / NFR-5).** The served equipment screen MUST match `#mock-equipment` [:492-511]: the 4-class access SELECT, the four group headings, the 29 items in the pinned per-group counts (7+6+5+11), the access-gating, and — load-bearing — the SPECIFIC cardio machines (treadmill / exercise-bike / stair-step / elliptical / rower / air-bike), NOT a single generic "cardio machine". Gate: a rendered-HTML parse asserts the 4 group headings present AND the per-group item count is exactly 7/6/5/11 (AC-2); the cardio group enumerates ≥ 5 of the 6 named machines (catches a generic-collapse regression). FAILING-CAPABLE: collapse the cardio group to one generic item → the per-group count (6) and the named-machine assertion both red.
- **PINNED ACCESS-CLASS SET — exactly the 4 classes, no drift from the gate constant (crit 1 / NFR-5).** Gate: the access `<select name='equipment-access-class'>` carries option values EXACTLY equal to `set(capture.EQUIPMENT_ACCESS_CLASSES)` (AC-1) — the no-drift assertion mirroring `test_demographic_selects_built_from_the_gate_constants` [:274-286] via `_select_block(html, "equipment-access-class")`. FAILING-CAPABLE: a 5th option, a dropped class, or a label-not-token value (`Home gym` instead of `full-home-gym`) reds the set-equality.
- **SINGLE AUTHORITATIVE `equipment-access-class` SELECT — the T4/T5 seam (related-code consistency).** `equipment-access-class` is the equipment screen's (T5's) select; T4's 9 wizard steps EXCLUDE equipment (spec T4 AC-1), so the demographics step does NOT carry it. The pre-T4 tree carried a legacy `equipment-access-class` select in the demographic form [app_view.html:469]; T4's W1 rework drops it from the demographics step (it is no longer a demographic field). **Assumption stated (SE Ask-vs-Proceed rule 3 — same interface, simpler implementation):** T5 owns the ONE authoritative `equipment-access-class` select on the equipment screen. Gate: `html.count("name='equipment-access-class'") == 1` (AC-1 guard) — catches a duplicate select surviving the T4 seam (which would make `_select_block`'s first-match parse ambiguous and double-submit the token). FAILING-CAPABLE: a second `equipment-access-class` select (a stray from the T4 demographics form) reds the count. **Flagged for the wave reviewers / plan-integrity:** if T4's landed `app_view.html` still carries an `equipment-access-class` select in the demographics step, T5 RELOCATES it to the equipment screen (one authoritative select), not duplicates it — a coordination note at the T4↔T5 `app_view.html` seam, not a blocking defect.
- **ACCESS-GATING — non-bodyweight-only shows the checklist; `Bodyweight only` HIDES it + shows the note (crit 3).** Gate: the access select carries a gating hook (the `onEquipAccess`-equivalent toggle, e.g. `onchange=`); the `#equip-detail` checklist container is present (shown for a non-bodyweight-only value); the bodyweight-only note element is present, default-hidden (`display:none`), and carries the pinned copy "No equipment needed — we'll program bodyweight-only" (AC-3). The SPA is server-rendered static HTML + client JS, so the test asserts the gating CONTROL + the hidden/note MARKUP (it does not execute JS), per the spec's AC-3 framing. FAILING-CAPABLE: remove the bw-note element or its pinned copy → the note assertion reds; remove the gating hook → the toggle-attribute assertion reds.
- **ADR-0029 INLINE-ASSET GATE — distinct inline `<svg>` per item, no off-file asset (crit 4).** Gate: every one of the 29 item cards carries an inline `<svg>` icon (no `<img src>` / off-file reference); the icons are DISTINCT (the set of icon svgs has many members, not one repeated placeholder); and `generate.run('app')` returns a written `Path` (does NOT raise the `render.emit` off-file `ValueError`) (AC-4). FAILING-CAPABLE: an off-file `<img src='…/dumbbell.png'>` → `render.emit` raises `ValueError` ([render.py:149]) → `_emit_app` reds (the authoritative inline-asset proof, mirroring [:98-118,:428-432]); a single-repeated placeholder icon → the distinct-count assertion reds.
- **EXTEND-NOT-REBUILD — the detail set is a record-only LOCAL field, not a new `SUMMARY_FIELD_SET` token (crit 5 / NFR-2).** The access class writes the EXISTING `equipment-access-class` planner token (via the markup `name=`); the detailed checklist writes a NEW LOCAL field `equipment-detail` that is NOT a `SUMMARY_FIELD_SET`/`WIRED_TOKENS` member, so capture's existing default routes it record-only ([capture.py:291-295]) — the planner gets NO new token from the detailed set. Gate (the MARKUP half, QA-F2): the access select submits `equipment-access-class`; the detail items submit under `equipment-detail`; and `equipment-detail ∉ router.SUMMARY_FIELD_SET` AND `equipment-detail ∉ capture.WIRED_TOKENS` (AC-5) — the record-only-by-construction proof. The record-only ROUTING itself is T1's `test_capture.py` contract (the existing default path), NOT re-asserted here. FAILING-CAPABLE: if the markup ever named the detail field a `SUMMARY_FIELD_SET` token (e.g. `goal-domains`), the `∉` assertions red.
- **NO-PYTHON / FROZEN-ENGINE — T5 edits markup + a test only (NFR-2).** T5 touches ONLY `vault/design/templates/app_view.html` + `tests/serve/test_app_shell.py`; it edits NO `scripts/**` python (not `capture.py`, not `router.py`, not `app_shell.py`, not `server.py`, not the frozen engine/store). Gate (a SHELL gate run in REGRESSION; `<wave-base>` a dynamic `git merge-base`): `git diff --numstat $(git merge-base HEAD origin/main)..HEAD -- scripts/plan/orchestrate.py scripts/plan/pipeline.py scripts/plan/assemble.py scripts/plan/generate_plan.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/track.py 'scripts/store/*.py'` → 0 rows (the cross-cutting NFR-2 invariant, unchanged by T5); AND T5's OWN change set (the staged files / T5's commit) = EXACTLY the two manifest files, with 0 python production hunks (the scope-discipline check below). Sibling-task changes to `capture.py`/`router.py`/`app_shell.py` (T1/T2/T4) appear in a fork-point diff but are NOT T5's — T5's commit touches neither.
- **0 LIVE SPEND — rendered-HTML / `render.emit`-tested (NFR-3).** Gate: every equipment case runs `app_shell.render([])` / `_emit_app(tmp_path)` over the local template (0 network, 0 key, 0 model call); no `key_source.resolve()`-gated call, no `converse`/curation call, no `aplus-research` dispatch appears anywhere in T5. The equipment markup adds no outbound asset reference (the inline-`<svg>` gate enforces it).

## TDD Steps

Two TDD cycles over `tests/serve/test_app_shell.py`. Cycle 1 lands the equipment screen's access CONTROL — the 4-class access SELECT (AC-1), the access-gating + bodyweight-only hide-and-note (AC-3), and the access-class-submit / detail-record-only field-name contract (AC-5). Cycle 2 lands the CHECKLIST CONTENT — the 4-group / 29-item set with the specific cardio machines (AC-2), the distinct inline `<svg>` per item + the `render.emit` off-file gate (AC-4), and the full-suite + EXTEND-NOT-REBUILD regression (AC-6). All cases are rendered-HTML parse / `_emit_app` probes; 0 live spend, 0 network.

**Every Python command in this recipe is prefixed `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES`** (the macOS fork-safety setting the suite runs under), invoked as `.venv/bin/python -m pytest`. `git add` is a SEPARATE command from `git commit`. Commit on `feature/intake-build`.

### Cycle 1: Steps 1-4 — the access SELECT + the access-gating + the submit/record-only field-name contract

#### Step 1: RED — Write Failing Tests
Edit `tests/serve/test_app_shell.py`: add an equipment-screen test section + a small `_equip_screen(html)` parse helper (regex-extract the equipment region — e.g. the container bounded by the access select and the bodyweight-only note, or a stable `id`/marker the GREEN markup carries) so the assertions scope to the equipment screen, not the whole SPA. Pin the contract constants in the test module: the four group headings, the per-group counts `(7, 6, 5, 11)`, the named cardio machines, the bw-note copy, the detail field name `equipment-detail`. Target cases:

- **Case E1 — access SELECT 4-class no-drift (AC-1).** `_spa_html()`; assert `set(re.findall(r"<option value='([^']*)'", _select_block(html, "equipment-access-class"))) - {""} == set(capture.EQUIPMENT_ACCESS_CLASSES)` (mirroring [:274-286]) AND `html.count("name='equipment-access-class'") == 1` (the single-authoritative-select guard). RED: before the GREEN markup the equipment screen's access select does not exist (or, pre-T4, only the legacy demographic one exists with no equipment screen) → `_select_block` assert/count fails.
- **Case E2 — access-gating control + bodyweight-only hide-and-note (AC-3).** `_spa_html()`; assert the access select carries a gating hook (an `onchange=` / `onEquipAccess`-equivalent attribute), the `#equip-detail`-equivalent checklist container is present, and a bodyweight-only note element is present, default-hidden (`display:none`), carrying the pinned copy "No equipment needed — we'll program bodyweight-only". RED: the note + container + hook do not exist yet → reds.
- **Case E3 — access-class submit + detail-record-only field-name contract (AC-5, MARKUP half / QA-F2).** `_spa_html()`; assert the access select submits `name='equipment-access-class'` (within a form whose `action='/upload'` — reuse `_upload_form_html`/`_form_field_names` if the equipment screen is inside the capture form, else assert the access select + detail items carry their `name=` in the equipment region) AND the 29 detail items submit under `name='equipment-detail'` AND `"equipment-detail" not in router.SUMMARY_FIELD_SET` AND `"equipment-detail" not in capture.WIRED_TOKENS` (the record-only-by-construction proof). Do NOT assert routing (T1's `test_capture.py`). RED: the detail items + the `equipment-detail` name do not exist yet → reds.

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/serve/test_app_shell.py -q`
Expected: FAIL — E1/E2/E3 red (the equipment screen markup does not exist yet); the existing app_shell cases stay green.

#### Step 2: GREEN — Implement
Changes to make (describe; the SE writes the markup):
- `vault/design/templates/app_view.html` — add the equipment screen as a DISTINCT region within T4's W1 wizard structure (mirroring the mockup's standalone `#mock-equipment` [:492-511]), carrying the access CONTROL:
  - the access `<select name='equipment-access-class'>` with exactly the four `<option value='…'>`s whose VALUES equal `capture.EQUIPMENT_ACCESS_CLASSES` (`full-home-gym` / `commercial-gym` / `minimal-equipment` / `bodyweight-only`) and the operator-facing labels Home gym / Commercial gym / Minimal equipment / Bodyweight only; the select carries the gating hook (the `onEquipAccess`-equivalent `onchange=`).
  - the access-gated detail container (the `#equip-detail`-equivalent) — the checklist host shown for a non-bodyweight-only value (Cycle 2 fills its content);
  - the bodyweight-only note element (the `#equip-bw-note`-equivalent), default-hidden (`display:none`), carrying the pinned copy "No equipment needed — we'll program bodyweight-only.";
  - the detail items submit under the NEW local field `name='equipment-detail'` (a record-only field — NOT a `SUMMARY_FIELD_SET`/`WIRED_TOKENS` member); each item is a NAMED form control (e.g. a `<label>`-wrapped `<input type='checkbox' name='equipment-detail' value='<slug>'>` preserving the mockup's card look — the exact control shape is SE-discretionary within the pinned field name). The equipment screen's fields sit inside the same Create-Profile capture form that POSTs to `/upload` (so the access value round-trips through the existing `persist_capture` wired path and the detail set through the record-only default).
  Match the mockup's structure; the pixel/copy beyond the pinned access-class set + the bw-note copy + the field names is discretionary (NFR-5). Do NOT touch the workspace tabs [:456-458] (T7) or T4's wizard steps.

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/serve/test_app_shell.py -q`
Expected: PASS — E1 (4-class no-drift + single select), E2 (gating + bw-note), E3 (submit + record-only field name) green; existing app_shell cases green.

#### Step 3: REFACTOR
Review for:
- The access `<option>` VALUES are authored to EQUAL `capture.EQUIPMENT_ACCESS_CLASSES` (the no-drift contract) — not a hand-typed list that could drift; the labels are the operator-facing copy, the values are the tokens.
- ONE authoritative `equipment-access-class` select (the equipment screen's) — confirm the T4 demographics step carries none (the single-select guard E1; relocate, do not duplicate, if the T4 seam left one).
- The bw-note copy + the gating hook match the mockup's intent; the detail container + the bw-note are siblings the gating toggles (one shown XOR the other).
- The `equipment-detail` field name is a single pinned local field, distinct from every `SUMMARY_FIELD_SET`/`WIRED_TOKENS` token (E3) — the record-only-by-construction property.

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/serve/test_app_shell.py -q`
Expected: PASS (no behavior change).

#### Step 4: REGRESSION
Run the full suite + the EXTEND-NOT-REBUILD frozen-engine SHELL gate:
```
OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest -q
WAVE_BASE=$(git merge-base HEAD origin/main)
git diff --numstat "$WAVE_BASE"..HEAD -- scripts/plan/orchestrate.py scripts/plan/pipeline.py scripts/plan/assemble.py scripts/plan/generate_plan.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/track.py 'scripts/store/*.py'
```
Expected: full suite green (port 8765 free; or EXACTLY the two pinned env failures if the port is held — nothing else); 0 frozen-engine rows. T5 added an equipment SCREEN — no demographic/T4-owned case regressed (the equipment region is distinct).

### Cycle 2: Steps 5-7 — the 29-item / 4-group checklist + distinct inline `<svg>` icons + the render-gate

#### Step 5: RED — Write Failing Tests
Edit `tests/serve/test_app_shell.py`: add the checklist-content cases. Target:

- **Case E4 — the 4-group / 29-item checklist matching the mockup (AC-2).** `_equip_screen(_spa_html())`; assert the four group headings present (`Free weights &amp; racks`, `Cardio machines`, `Resistance machines`, `Bodyweight &amp; accessories`) AND the per-group item count is exactly `(7, 6, 5, 11)` (sum 29) — parse each group's item cards between consecutive group headings and count. ALSO assert the cardio group enumerates ≥ 5 of the 6 named machines (`Treadmill`, `Exercise bike`, `Stair`/`step`, `Elliptical`, `Row`, `Air`/`assault bike`) — the not-a-generic-"cardio machine" guard. RED: before the GREEN content the groups/items do not exist → reds.
- **Case E5 — distinct inline `<svg>` icon per item (AC-4, markup leg).** `_equip_screen(_spa_html())`; assert each of the 29 items carries an inline `<svg>` icon (count the per-item icon `<svg>` == 29) AND the icons are DISTINCT (the set of distinct icon-svg blocks has ≥ 20 members — proves they are not one repeated placeholder) AND the equipment region carries no off-file asset (`<img src` / a non-`data:` external `src` returns 0). RED: the items + icons do not exist yet → reds.
- **Case E6 — the inline-asset render gate passes under the equipment additions (AC-4, authoritative leg).** Mirror `test_t3_additions_keep_the_spa_inline_asset_clean` [:428-432]: `path = _emit_app(tmp_path)`; assert `isinstance(path, Path) and path.exists()` AND `"<script src" not in path.read_text()`. RED-capable by construction: an off-file `<img src>`/`<script src>` anywhere in the equipment markup makes `render.emit` raise `ValueError` ([render.py:149]) → `_emit_app` reds (the authoritative proof the AC-1/AC-4 pass rests on the real emit gate, not a substring grep).

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/serve/test_app_shell.py -q`
Expected: FAIL — E4/E5 red (the checklist content does not exist yet); E6 may pass vacuously pre-content (the SPA has no off-file ref yet) — it becomes load-bearing once the icons land (the negative control is the existing `test_offfile_ref_makes_emit_raise` [:98-118] proving the gate reds on an off-file ref).

#### Step 6: GREEN — Implement
Changes to make:
- `vault/design/templates/app_view.html`, inside the equipment screen's detail container — add the four group sections matching the mockup [:504]:
  - **Free weights & racks (7):** Dumbbells, Barbell + plates, Squat / power rack, Flat / adjustable bench, Kettlebells, Smith machine, Weight plates.
  - **Cardio machines (6):** Treadmill, Exercise bike, Stair / step machine, Elliptical, Rowing machine, Air / assault bike (the SPECIFIC machines — never a single generic "cardio machine").
  - **Resistance machines (5):** Cable machine / functional trainer, Lat pulldown, Leg press, Leg curl / extension, Chest press / pec deck.
  - **Bodyweight & accessories (11):** Pull-up bar, Dip station, TRX / suspension straps, Resistance bands, Yoga mat, Foam roller, Medicine ball, Jump rope, Gymnastic rings, Plyo box, Ab wheel.
  Each item is a NAMED form control submitting under `name='equipment-detail'` (record-only), carrying a DISTINCT inline `<svg>` icon (the mockup's per-item `.eq-ic` svgs are inline `viewBox=…` paths — reuse them; NEVER an off-file `<img src>`). 7+6+5+11 = 29 items, four `.seclab`-equivalent group headings.

Run: `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/serve/test_app_shell.py -q`
Expected: PASS — E4 (4 groups / 7-6-5-11 / named cardio), E5 (29 distinct inline svgs, no off-file), E6 (`generate.run('app')` emits a Path) green; E1/E2/E3 + existing cases green.

#### Step 7: REFACTOR + REGRESSION
Review for:
- The 29 items match the mockup's set + per-group counts EXACTLY; the cardio group is the six named machines (no generic collapse); the icons are inline `<svg>` (no off-file `src`), distinct per item.
- The detail items all submit under the single `equipment-detail` record-only field (no per-item `SUMMARY_FIELD_SET` token sneaks in); the access select still the single authoritative `equipment-access-class`.
- `_equip_screen`'s region parse is stable against the surrounding T4 wizard markup (it scopes to the equipment region, not the whole SPA) so the group/item counts are not polluted by other screens' markup.

Run the full suite + the EXTEND-NOT-REBUILD gate + the scope-discipline check:
```
OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest -q
WAVE_BASE=$(git merge-base HEAD origin/main)
git diff --numstat "$WAVE_BASE"..HEAD -- scripts/plan/orchestrate.py scripts/plan/pipeline.py scripts/plan/assemble.py scripts/plan/generate_plan.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/track.py 'scripts/store/*.py'
git diff --name-only "$WAVE_BASE"..HEAD -- scripts/serve/capture.py scripts/plan/router.py vault/design/templates/app_shell.py scripts/serve/server.py
```
Expected: full suite green (port 8765 free; or exactly the two pinned env failures); 0 frozen-engine rows. The third command lists ONLY sibling-task files (T1's `capture.py`, T2's `capture.py`/`router.py`, T4's `app_shell.py`) — confirm T5's OWN commit contributes 0 hunks to any of them (T5's `git show --stat HEAD` after the T5 commit lists exactly `vault/design/templates/app_view.html` + `tests/serve/test_app_shell.py`). The scope-discipline check is "T5's commit = the 2 manifest files", NOT "the fork-point diff is 2 files" (the wave-2 working tree carries the sibling tasks' files).

**TDD step count: 7 (2 cycles).** Within the ≤10-step guidance. T5's 6 ACs (1, 2, 3, 4, 5, 6) span the 4-class access SELECT + the 4-group / 29-item checklist + the access-gating / bw-note + the distinct inline icons + the access-class-submit / detail-record-only field-name contract + the pytest gate, none splittable across waves (T5 is the single Wave-2 equipment-markup task). Two Modify files; 0 new python production code (the equipment screen is pure markup + a test; the access value writes the EXISTING `equipment-access-class` token via the EXISTING `persist_capture` wired path, the detail set via the EXISTING record-only default).

## Interface Contracts

This task CONSUMES one pinned contract and CREATES one local field; it defines NO new python module interface.

- **`equipment-access-class` planner token (CONSUMED, the pinned T1/T2/T4↔T5 format coupling).** The access SELECT submits `name='equipment-access-class'` with option VALUES equal to `capture.EQUIPMENT_ACCESS_CLASSES` (`full-home-gym` / `commercial-gym` / `minimal-equipment` / `bodyweight-only` [capture.py:119]). The value writes the `equipment-access-class` `SUMMARY_FIELD_SET` pass-through token via the EXISTING `persist_capture` wired path ([capture.py:63,267-270]) → `summarize` → `dispatch`; `equipment-access-class`'s one authoritative source is this demographic equipment selection ([router.py:110-115]). T5 builds the markup TO this contract (a format coupling — independently testable against the rendered HTML, not a code dependency; `depends-on: []`). A change to the 4-class set is a capture/router contract change owned by T1/T2, not T5.
- **`equipment-detail` local record field (CREATED, T5-local, record-only).** The 29 detailed checklist items submit under the NEW local field `name='equipment-detail'`. It is NOT a `SUMMARY_FIELD_SET`/`WIRED_TOKENS` member, so `persist_capture`'s default routes it record-only to the gitignored scaffold ([capture.py:291-295]) — the planner receives NO new token from the detailed set (EXTEND-NOT-REBUILD, crit 5). Discretionary within the contract: the per-item control shape (checkbox vs. toggle-backed hidden input) + the item slug values (NFR-5). The record-only ROUTING is verified in T1's `test_capture.py` (the existing default path); T5's `test_app_shell.py` verifies only the field-NAME contract (QA-F2).
- **Frozen surface (PRESERVED).** Every `scripts/**` python file is byte-unchanged by T5 — `scripts/serve/capture.py` / `scripts/plan/router.py` / `vault/design/templates/app_shell.py` / `scripts/serve/server.py` (sibling-task surfaces) and the frozen engine `scripts/plan/{orchestrate,pipeline,assemble,generate_plan,adjudicate,adjust,track}.py` + `scripts/store/*` (NFR-2). T5 is the equipment MARKUP + its rendered-HTML test, not a python edit.

## Verification Checklist

- [ ] AC-1 (access SELECT — exactly the 4 classes feeding `equipment-access-class`) — verified by Case E1: `_select_block(_spa_html(), "equipment-access-class")` option values == `set(capture.EQUIPMENT_ACCESS_CLASSES)` (no-drift, mirroring [:274-286]) AND `html.count("name='equipment-access-class'") == 1` (single authoritative select; failing-capable: a dropped/extra class or a duplicate select reds).
- [ ] AC-2 (29-item checklist in 4 groups matching the mockup) — verified by Case E4: the four group headings present + the per-group count exactly `(7, 6, 5, 11)` (sum 29) + the cardio group enumerates ≥ 5 of the 6 named machines (the not-a-generic-"cardio machine" guard; failing-capable: a generic-collapse or a miscount reds).
- [ ] AC-3 (access-gating — non-bodyweight-only shows the checklist; `Bodyweight only` hides it + shows the note) — verified by Case E2: the access select's gating hook + the `#equip-detail`-equivalent container + the default-hidden (`display:none`) bodyweight-only note carrying the pinned copy "No equipment needed — we'll program bodyweight-only" (the markup-half gating proof, per the spec AC-3 framing; failing-capable: remove the note/copy or the hook → reds).
- [ ] AC-4 (distinct inline icons per item, no off-file asset) — verified by Case E5 (each of the 29 items carries an inline `<svg>`; the icon set has ≥ 20 distinct members; 0 off-file `<img src>` in the equipment region) AND Case E6 (`_emit_app(tmp_path)` returns a written `Path`, `"<script src" not in` the emitted file — the authoritative `render.emit` off-file gate [render.py:149], mirroring [:428-432]; failing-capable: an off-file ref makes `render.emit` raise `ValueError`, proven by the existing negative control [:98-118]).
- [ ] AC-5 (access class writes the `equipment-access-class` token; the detailed set is a record-only local field, not a `SUMMARY_FIELD_SET` token) — verified by Case E3 (the access select submits `equipment-access-class`; the detail items submit under `equipment-detail`; `equipment-detail ∉ router.SUMMARY_FIELD_SET` AND `∉ capture.WIRED_TOKENS` — the record-only-by-construction MARKUP half, QA-F2; the routing itself is T1's `test_capture.py`, NOT in T5's manifest).
- [ ] AC-6 (pytest passes) — verified by `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES .venv/bin/python -m pytest tests/serve/test_app_shell.py -q` green (all equipment cases) AND the full-suite `… -m pytest -q` green (port 8765 free; or exactly the two pinned env failures if held — nothing else).
- [ ] NFR-2 (EXTEND-NOT-REBUILD frozen engine + no python edit) — verified by the SHELL gate (a standing INVARIANT run in REGRESSION; `<wave-base>` a dynamic `git merge-base`): `WAVE_BASE=$(git merge-base HEAD origin/main); git diff --numstat "$WAVE_BASE"..HEAD -- scripts/plan/orchestrate.py scripts/plan/pipeline.py scripts/plan/assemble.py scripts/plan/generate_plan.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/track.py 'scripts/store/*.py'` → 0 rows; AND T5's own commit (`git show --stat HEAD`) lists EXACTLY `vault/design/templates/app_view.html` + `tests/serve/test_app_shell.py` (0 python production hunks).
- [ ] NFR-3 (0 live spend) — every equipment case runs `app_shell.render([])` / `_emit_app(tmp_path)` over the local template; 0 network, 0 key, 0 model call, 0 `aplus-research` dispatch.
- [ ] Wave-2 checkpoint Go/No-Go gates carried (build plan § Wave 2 → Wave 3 Boundary, T5 half): the access SELECT carries exactly the 4 classes feeding `equipment-access-class` [AC-1]; the detailed checklist carries 29 items in the four groups (7+6+5+11) [AC-2]; access-gating hides the checklist + shows the bodyweight-only note [AC-3]; each item has an inline `<svg>` + the render gate passes [AC-4]; the access SELECT submits `equipment-access-class` + the detailed items submit as named form fields (the MARKUP half via `test_app_shell.py`; the record-only ROUTING is T1's `test_capture.py`, QA-F2) [AC-5]; full suite green; frozen-engine numstat 0; 0 live spend.
- [ ] Scope-discipline check: T5's commit lists ONLY the 2 manifest files; any other file needs a written justification (the daemon `vault/**` churn is NOT staged; sibling-task files in the wave-2 working tree carry no T5 hunk).

## Commit

```
feat(intake): T5 equipment access-gated 29-item checklist
```
Stage only:
- `vault/design/templates/app_view.html`
- `tests/serve/test_app_shell.py`

Stage by explicit path: run `git add <path>` as its OWN command, then `git commit` SEPARATELY — the `block-pii-commit` PreToolUse hook denies single-call stage+commit, `git commit -a/--all`, and pathspec `git commit <path>` (their content is invisible to the pre-command staged-set scan). `app_view.html` is NOT a gated `vault/{compounds,biomarkers,library}/` entity page, so `block-ungated-vault-write.sh` does not apply — but stage it by explicit path regardless; never `git add vault/` blindly (the daemon frontmatter churn re-appears every sync). Do NOT stage `.beads/issues.jsonl` / `harvest.jsonl` / `memory/process-failures.md` — those are close-protocol artifacts. Commit on `feature/intake-build` (never `main`).

## Rollback

If verification fails after 3 fix cycles:
1. `git stash -m "failed-ADR-0033-0035-T5-attempt-{N}"` — preserve work for diagnosis.
2. `git checkout {commit-before-task}` — revert to the entry-state commit (the W2 working state this task was built on top of).
3. Update the bead tracker: flag ADR-0033-0035-T5 as BLOCKED with the failure log.
4. Escalate: "Task ADR-0033-0035-T5 failed verification after 3 attempts. Stash ref: {ref}. Failure log: {summary}."
5. After abandonment or re-planning, drop all `failed-ADR-0033-0035-T5` entries from `git stash list`.

Do NOT: silently skip a failing acceptance criterion; weaken a test to make it pass (especially the inline-asset render-gate [AC-4/E6], the 4-group / 29-item count [AC-2/E4], or the access-class no-drift [AC-1/E1] — a check that cannot go RED is worthless); collapse the six named cardio machines to a generic "cardio machine" (the mockup-match HALTs it); add a `SUMMARY_FIELD_SET` token for the detailed equipment set (the EXTEND-NOT-REBUILD crit 5 rejects it — the detail set is record-only `equipment-detail`, HALT); add `tests/serve/test_capture.py` to T5's manifest (the record-only routing is T1's contract — QA-F2, HALT); duplicate the `equipment-access-class` select across the demographics step and the equipment screen (one authoritative select — relocate, do not duplicate; HALT, reconcile the T4 seam); or edit any `scripts/**` python (`capture.py` / `router.py` / `app_shell.py` / `server.py` / the frozen engine — the NFR-2 EXTEND-NOT-REBUILD breach rejects it; HALT, confine to the markup + the test).

## Report

- Recipe: `docs/task-plan/.pipeline/adr-0033-0035/draft-ADR-0033-0035-T5.md` (status: draft).
- Every ADR-0033-0035-T5 acceptance criterion (AC-1..6) maps to a named Verification-Checklist test/command + the Wave-2 checkpoint gate; the inline-asset RED test [AC-4/E6] is failing-capable by the real `render.emit` off-file gate ([render.py:149], with the existing negative control [test_app_shell.py:98-118]), the count/no-drift tests [AC-1/E1, AC-2/E4] red on a miscount/collapse — never tautological.
- GROUNDED against the live tree (`feature/intake-build`, fork-point `git merge-base HEAD origin/main` = `12f769f`, 2026-06-30): `capture.EQUIPMENT_ACCESS_CLASSES` (4 classes) [capture.py:119], `equipment-access-class` WIRED [:63] + `_BOUNDED_ENUMS` [:125] + the record-only `else` default [:291-295]; `router.SUMMARY_FIELD_SET` carrying `equipment-access-class` [router.py:28] + the ADR-0018-T1 pass-through note [:110-115,558-559]; the `test_app_shell.py` harness — `_emit_app`/`_spa_html`/`_select_block`/`_form_field_names`/`_upload_form_html` [:62-75,:243-259], the no-drift select pattern [:274-286], the inline-asset re-run [:428-432] + the off-file negative control [:98-118]; `render.emit` off-file `ValueError` gate [render.py:149,:46,:113]; the mockup `#mock-equipment` screen [prototype/intake-onboarding-mockup.html:492-511] — the access select [:501], the four group headings + the 29 items in 7/6/5/11 (Dumbbells/Barbell/Squat-rack/Bench/Kettlebells/Smith/Plates · Treadmill/Exercise-bike/Stair-step/Elliptical/Rower/Air-bike · Cable/Lat-pulldown/Leg-press/Leg-curl-ext/Chest-press · Pull-up-bar/Dip/TRX/Bands/Yoga-mat/Foam-roller/Med-ball/Jump-rope/Rings/Plyo-box/Ab-wheel) [:504], the `#equip-bw-note` copy [:507] [all VERIFIED].
- ZERO literal implementation code in the recipe — every change is described as markup behavior + the contract/exemplar to match, with cited `file:line` seams; the SE authors the HTML/test under TDD.
- **T4↔T5 `app_view.html` seam flagged (non-blocking, coordination note for the wave reviewers / plan-integrity).** `equipment-access-class` is T5's equipment-screen select (T4's 9 wizard steps EXCLUDE equipment per spec T4 AC-1). The pre-T4 tree carried a legacy `equipment-access-class` select in the demographic form [app_view.html:469]; T4's W1 rework drops it. T5 owns the ONE authoritative select; if T4's landed markup still carries one in the demographics step, T5 RELOCATES it (not duplicates). Case E1's `count(...) == 1` guard catches a surviving duplicate. No blocking spec defect.
- **QA-F2 honored:** T5's manifest is `app_view.html` + `test_app_shell.py` ONLY — the MARKUP half (the access SELECT submits `equipment-access-class`; the detailed items submit under the record-only `equipment-detail` field). `tests/serve/test_capture.py` is NOT in T5's manifest; the record-only ROUTING of the detailed set is T1's existing default-record-only contract.
