---
task-id: ADR-0039-T1
source-spec: docs/spec/adr-0039-scheduled-agent-runner-spec.md
source-build-plan: docs/build-plan/build-plan-adr-0039.md
wave: 1
assigned-agent: SE
reviewers: [Architect, QA, Security]
created: 2026-07-06
status: approved
---

# Recipe: Runner Driver — Raw Read + De-ID in Python, Subscription-Sub-Agent Dispatch, Single `signal(CADENCE_TRIGGER)` Entry (ADR-0039-T1)

> **Wave-1 entry-point task** (the sole Wave-1 task; the crown-jewel containment + extend-not-rebuild gate all three Wave-2 leaves supply to — a failure here HALTS the whole build). It creates the greenfield runner DRIVER (`scripts/runner/cadence_runner.py`) + the subscription-sub-agent dispatch seam (`scripts/runner/subscription_dispatch.py`) + the package marker (`scripts/runner/__init__.py`), and drives the ALREADY-BUILT loop through its SINGLE entry `plan_loop.signal(root, trigger=CADENCE_TRIGGER, dispatch=..., deid_client=..., tailor_client=...)`. The raw `store.read_all` read and the ADR-0027 `deid_in` de-identification run INSIDE the Python driver (`signal → regenerate → run_orchestrated → deid_in`), never in the top-level subscription session's transmitted context — so the subscription lane carries ONLY PII-free orchestration plus the de-identified summary. **EXTEND-NOT-REBUILD is a hard governance floor:** the runner re-hosts NONE of the debounce / fail-closed composition / cross-domain holds, and touches NEITHER the frozen engine NOR the `scripts/serve/` server package it drives (`scripts/serve/plan_loop.py` is IN the numstat freeze, SEC-03). Ships DISABLED BY DEFAULT: importing the runner arms nothing (0 `signal`/de-id/dispatch on import), and the default (non-injected) subscription-session factory RAISES pre-scrub until T2 wires the env-scrub (SEC-04). Every AC is fixture-satisfiable at 0 live spend and 0 real PII.

## Entry State

**Prerequisites** (ADR-0039-T1 is the Wave-1 entry point — the spec Dependency Map lists **NO predecessor task**; Dependencies: None):

- [ ] On a feature branch cut off `main`: `feature/adr-0039-runner-build` (the branch already carries the ADR-0039 spec + build-plan docs commits; verified `git rev-parse --abbrev-ref HEAD == feature/adr-0039-runner-build`) — never `main`.
- [ ] Rollback target recorded: `git rev-parse HEAD` at task entry (currently `5072d1c`; the `{commit-before-task}` the Rollback Protocol reverts to). `git merge-base HEAD origin/main == 4d546e9` (the spec + build-plan are the only commits ahead — docs-only, so the captured suite baseline holds).
- [ ] Full test suite at the captured floor: `.venv/bin/python -m pytest -q` reports **`3 failed, 2312 passed, 7 skipped`** (captured `feature/adr-0039-runner-build @ 67b8389`; unchanged at HEAD since the two commits ahead are docs-only). The three failures are the KNOWN benign floor, NOT this task's to fix: (1) `tests/serve/test_bind.py::test_port_collision_exits_nonzero_fail_loud` + (2) `tests/serve/test_server.py::test_main_reads_no_stdin` (pre-existing env-sensitive OS-port/stdin cases), and (3) `tests/serve/test_plan_loop_hold.py::test_frozen_spine_and_only_regenerate_changed` (ADR-0040's post-merge-VACUOUS merge-artifact — its `current[def] != origin/main[def]` sanity assertion is empty now that ADR-0040 is merged into `origin/main`; ADR-0039 edits none of `plan_loop.py`, so this stays invariant). **The gate is NO NEW 4th distinct failure** (`passed` grows as the runner's tests land); a 4th failure is a regression — HALT.
- [ ] Working tree clean of CODE changes: `git status` shows no uncommitted `scripts/`/`tests/` changes. (The `vault/**` modified lines in `git status` are the known S90 basic-memory daemon frontmatter churn — cosmetic, NOT this task's, leave uncommitted; stage ONLY the manifest by explicit path.)
- [ ] Frozen-spine clean at entry (numstat=EMPTY over the canonical superset, incl. `plan_orchestrator.py` + `plan_driver.py` + `scripts/serve/plan_loop.py`, SEC-03 / Security-M1): `git diff --numstat origin/main -- 'scripts/plan/orchestrate.py' 'scripts/plan/pipeline.py' 'scripts/plan/assemble.py' 'scripts/plan/generate_plan.py' 'scripts/plan/adjudicate.py' 'scripts/plan/adjust.py' 'scripts/plan/track.py' 'scripts/plan/plan_orchestrator.py' 'scripts/plan/plan_driver.py' 'scripts/store/' 'scripts/serve/plan_loop.py'` prints nothing (verified EMPTY 2026-07-06).
- [ ] The Create targets are ABSENT (greenfield, no stale/duplicate artifact): `for f in scripts/runner/__init__.py scripts/runner/cadence_runner.py scripts/runner/subscription_dispatch.py tests/runner/test_cadence_runner.py; do test -e "$f" && { echo "PRESENT $f"; exit 1; }; done && echo "ABSENT (Create OK)"` (verified: `scripts/runner/` and `tests/runner/` trees do not exist).
- [ ] The built-loop seam is present with the seams-unwired no-op guard: `grep -q 'def signal(root, \*, trigger, dispatch=None, deid_client=None, plan_date=None, tailor_client=None)' scripts/serve/plan_loop.py && grep -q '"reason": "seams-unwired"' scripts/serve/plan_loop.py && grep -q 'CADENCE_TRIGGER = "cadence"' scripts/serve/plan_loop.py && echo OK` (verified — `signal` at `plan_loop.py:457`, seams-unwired at `:493`, `CADENCE_TRIGGER` at `:376`).

## Context Load List

**Files to read (this task's file manifest + the built seams it consumes — read the LIVE source, never write a seam signature from memory):**
- `scripts/serve/plan_loop.py` — the SINGLE entry the driver calls and the containment host: `signal(root, *, trigger, dispatch=None, deid_client=None, plan_date=None, tailor_client=None)` (`:457`; `dispatch`/`deid_client` DEFAULT `None` — a gate-pass with them absent returns `{"reason": "seams-unwired"}` at `:493`, so the runner MUST supply them); `CADENCE_TRIGGER = "cadence"` (`:376`), `DATA_EVENT_TRIGGER`/`FREE_TEXT_TRIGGER` (`:377-378`, the labels the cadence tick MUST NOT use); `regenerate(root, *, dispatch, deid_client, ...)` (`:130`); `_read_raw_intake(root)` → `store.read_all(root)` (`:113,127`); `MIN_REGEN_INTERVAL_DAYS = 7` (`:383`); `_last_regen_date` (`:399`). READ-ONLY — the runner drives it, edits nothing under `scripts/serve/`.
- `scripts/plan/plan_orchestrator.py` — the de-id-FIRST orchestration: `run_orchestrated(raw_intake, deid_client, dispatch, store_read, root, *, plan_date, ...)` (`:127`); the `deid_in` sentinel HALT — `if summary.get("deidentified") is False:` → `_honest_no_plan(..., dispatch_count=0)` (`:227-229`), i.e. a `{deidentified: False}` summary yields 0 dispatches (the vacuity source AC-5 must dodge); the dispatch seam shape `dispatch(domain, prompt, summary) -> author envelope` (`:148`) fanned out at `authors[domain] = dispatch(domain, prompt, summary)` (`:382`). READ-ONLY cited input.
- `scripts/plan/deid_in.py` — the de-id-IN boundary: `deid_in(raw_intake, client, identity_config=pii_scan.DEFAULT_IDENTITY_CONFIG)` (`:39`) routes through `client.deidentify(raw_intake)` (`:81`); the discriminated union — a SUCCESS summary keyed ONLY by `router.SUMMARY_FIELD_SET` (never carrying `deidentified`) vs the `{deidentified: False}` no-plan sentinel; the value-scan `DEID_VALUE_PII` sentinel (`:77`) fires on structural PII / operator-identity tokens, NOT on a synthetic name in a free-text value. READ-ONLY cited input.
- `scripts/plan/router.py` — `SUMMARY_FIELD_SET` (`:23`) and its free-text members `goal-targets` (`:35`) + `hard-limits` (`:40`) — the ALLOWED value the faithless AC-5(b) fixture rides a leak in. READ-ONLY.
- `scripts/store/plan_schema.py` — `PLAN_DOMAINS = ("workout", "nutrition", "supplements", "peptides")` (`:49`), the closed 4-set the crown-jewel faithful arm asserts ≥1 payload per member over. READ-ONLY.
- `scripts/model/client.py` — the `deidentify(raw_intake)` method shape (`:78`) the fixture `deid_client` mimics. READ for the fixture contract only.
- `tests/serve/test_plan_loop_regen.py` — **the exact test precedent to MIRROR**: `_SummarizeDeid` (faithful fixture whose `deidentify` returns `router.summarize` over the live store — the SUCCESS arm, `:102-114`), `_LeakDeid` (faithless fixture — a SUMMARY_FIELD_SET-keyed summary carrying a leak in an allowed VALUE that PASSES `deid_in`'s key whitelist, `:117-130`), `_seed_biomarker` (a debounce-passing store, `:65`), `_seed_raw_pii` (a synthetic legal-name + raw drug into the raw intake, `:311`), and the `plan_loop.signal(root, trigger=..., dispatch=dispatch, deid_client=...)` drive pattern (`:212`). The runner's tests reuse these shapes with `trigger=CADENCE_TRIGGER`.

**Do NOT load:**
- `docs/adr/ADR-0039-*.md` and its upstreams (ADR-0036/0026/0027/0001/0005) — the spec already distilled them; re-reading re-opens resolved trade-offs (TP-02 drift).
- The full `docs/spec/adr-0039-scheduled-agent-runner-spec.md` + `docs/build-plan/build-plan-adr-0039.md` beyond the T1 block — already extracted into this recipe.
- The frozen inner engine internals (`scripts/plan/{orchestrate,pipeline,assemble,generate_plan,adjudicate,adjust,track}.py`, `scripts/store/*`) — the runner drives them THROUGH `signal`; reading their bodies invites a re-host (the exact AC-3 violation).
- The T2/T3/T4 manifests (`scripts/runner/auth_isolation.py`, `scripts/runner/schedule/*`, `scripts/runner/store_lock.py`) — other-task scope; T1 exposes the seams they consume, it does not build them.

## Risk Pre-Check

**BLOCKING GATE — this task carries the crown-jewel raw-PII containment + the EXTEND-NOT-REBUILD governance floor + the SEC-04 pre-scrub factory refusal (Security + Architect review).** Do NOT proceed to GREEN until a real (non-vacuous, failing-capable) test exists for every risk below. A placeholder (`assert True`) or a vacuous crown-jewel scan (one that passes because 0 payloads exist) FAILS this gate.

- **Risk — Crown-jewel non-egress (ADR-0039 Decision binding top-level containment + Consequences-Negative-1; the ADR-0001 zero-egress-except-named floor):** raw store PII must never reach a dispatch payload; the de-id-IN call is the SOLE egress. **VACUITY HAZARD (QA-F1):** `run_orchestrated` HALTS with 0 dispatches on the `deid_in` `{deidentified: False}` sentinel (`plan_orchestrator.py:227-229`) — so a single faithful "0 raw-PII in any payload" assertion passes VACUOUSLY whenever the fixture returns the sentinel or the debounce fails (0 payloads exist).
  - Gate condition: RED must carry (a) a **proven-non-empty** faithful arm — over a debounce-PASSING seeded store with a SUCCESS-returning fixture `deid_client`, the recording dispatch captured **≥1 payload PER `PLAN_DOMAINS` member (≥4 payloads)** AND 0 raw-PII tokens across every captured `(domain, prompt, summary)`; AND (b) a **failing-capable de-id-bypass** arm — a FAITHLESS whitelist-passing `deid_client` (a `SUMMARY_FIELD_SET`-keyed summary embedding the seeded legal-name + raw-med token in an ALLOWED value, `_LeakDeid`-style) makes the seeded token REACH ≥1 dispatch payload and the wire-scan detect it (raw-PII hit count ≥ 1). **A naïve raw-intake pass-through `deid_client` is INSUFFICIENT** — `deid_in`'s field-NAME whitelist rejects the raw intake's out-of-set PII fields → the `{deidentified: False}` sentinel → 0 dispatches → the QA-F1 vacuity again. (Covered by AC-5.)
- **Risk — Local-store-only / de-id-IN proven-invoked (ADR-0039 Decision; ADR-0027 `deid_client` sole egress):** the sole-egress claim is trivially true when NOTHING receives raw content.
  - Gate condition: RED must assert (a) the ONLY seam receiving raw store content is `deid_client.deidentify` (count of raw/store-content sends outside the de-id-IN call == 0) AND (b) `deid_client.deidentify` was INVOKED ≥1 over the debounce-passing seeded store (call-count ≥ 1) — the sole-egress claim rests on a de-id call that ACTUALLY FIRED, not an empty run. (Covered by AC-6.)
- **Risk — EXTEND-NOT-REBUILD (SEC-03; ADR-0039 governance floor):** the runner must supply seams, re-host nothing, and edit nothing frozen — including the `scripts/serve/plan_loop.py` host it drives.
  - Gate condition: RED must assert (a) a grep over `cadence_runner.py` + `subscription_dispatch.py` finds 0 occurrences of each re-host token (`_should_regenerate`, `compose_disposition`, `orchestrate.generate_plans`, `plan_driver.drive`, a re-declared `MIN_REGEN_INTERVAL_DAYS`); (b) `git diff --name-only origin/main -- scripts/serve/` is EMPTY (0 runner edits to the server package); (c) the frozen glob — the canonical superset: the 7 plan-engine files + `scripts/plan/plan_orchestrator.py` + `scripts/plan/plan_driver.py` + `scripts/store/` + `scripts/serve/plan_loop.py` (Security-M1 / Architect-F3: `plan_orchestrator.py` holds the `{deidentified: False}` sentinel HALT at `:227-229` + the dispatch fan-out `authors[domain] = dispatch(...)` at `:382` — the exact crown-jewel de-id/dispatch logic — and `plan_driver.drive` is the re-entered driver; both MUST be frozen, and T2 already freezes them) — is `git diff --numstat origin/main` EMPTY. (Covered by AC-3 + AC-4.)
- **Risk — SEC-04 (un-scrubbed session before T2 hardens it, Consequences-Negative-5):** a live subscription session must not be constructible before the auth env-scrub is wired.
  - Gate condition: RED must assert the DEFAULT (non-injected, non-fixture) session factory in `subscription_dispatch.py` RAISES a typed error (a `pytest.raises`) — never returning a live session. This is a CODE-LEVEL invariant; T2 AC-8 lifts it. (Covered by AC-9.)
- **Risk — Import-inertness (disabled-by-default down-payment, governing invariant 3):** building/importing the runner must arm nothing.
  - Gate condition: RED must assert `import scripts.runner.cadence_runner` and `import scripts.runner.subscription_dispatch` fire 0 `signal`, 0 de-id, 0 dispatch calls (spies assert 0). (Covered by AC-7.)

## TDD Steps

Two TDD cycles (8 steps, under the 10-step ceiling). **Cycle 1 = the dispatch seam + the SEC-04 default-factory refusal** (`scripts/runner/subscription_dispatch.py` — the seam-shape + `build_dispatch(session)` + the default-factory-refusal, testable standalone with a fixture session). **Cycle 2 = the driver** (`scripts/runner/cadence_runner.py` — the single `signal(CADENCE_TRIGGER)` binding + crown-jewel non-egress + local-store-only + extend-not-rebuild + import-inertness, driven end-to-end through the built loop with fixture dispatch + fixture `deid_client` over seeded stores). Cycle 2 consumes Cycle 1's seam. All fixtures synthetic PII-free; 0 live spend.

### Cycle 1: The Subscription-Sub-Agent Dispatch Seam + Default-Factory Refusal (SEC-04)

#### Step 1: RED — Write Failing Tests
Call `/write-tests` for `scripts/runner/subscription_dispatch.py`, creating `tests/runner/test_cadence_runner.py` (the single T1 test module per the manifest; Cycle 2 extends it), targeting:
- **AC-9 (SEC-04 default-factory refusal — scoped to a pre-scrub-DETECTABLE condition, Architect-F2):** constructing the DEFAULT (non-injected, non-fixture) subscription-session factory in `subscription_dispatch.py` raises a TYPED error (a named runner error / `NotImplementedError` citing the missing env-scrub) asserted with `pytest.raises`; it NEVER returns a live session. **The raise is GATED on a runtime-detectable "env-scrub-not-applied" predicate the test controls** (e.g. the factory checks for the wired `build_subscription_env` / a scrubbed-env marker and raises when it is absent), NOT on "T2 not yet merged." The T1 test asserts the raise under the env-scrub-ABSENT condition it sets explicitly — so once T2 wires `build_subscription_env`, this same test still exercises the scrub-absent path and stays GREEN with **NO edit to `tests/runner/test_cadence_runner.py`**; T2's AC-8 separately asserts the scrub-WIRED path constructs (no cross-task test mutation). Failing-capable: a default factory that returned a session under the scrub-absent condition would not raise, reddening the `pytest.raises`.
- **AC (seam shape, supports AC-1/AC-5/AC-6):** `build_dispatch(session)` returns a callable `dispatch(domain, prompt, summary)` matching the built seam shape (`plan_orchestrator.py:148`); called with a fixture (recording) session it routes each `(domain, prompt, summary)` to the session and returns the session's author envelope — a direct call `build_dispatch(fixture_session)("workout", "<prompt>", {<summary>})` records exactly that tuple on the fixture and returns the envelope.
- **AC-7 (import-inert, subscription_dispatch arm):** with spies over `plan_loop.signal` + a fixture `deid_client.deidentify` + a recording dispatch installed, freshly importing (`importlib.reload`) `scripts.runner.subscription_dispatch` fires 0 `signal`, 0 de-id, 0 dispatch calls.

Run: `.venv/bin/python -m pytest tests/runner/test_cadence_runner.py -q`
Expected: FAIL — `scripts/runner/subscription_dispatch.py` does not exist, so the module import fails. (If any test passes here, the test is wrong — investigate before GREEN.)

#### Step 2: GREEN — Implement
Changes to make (descriptions, not literal code):
- `scripts/runner/__init__.py` (Create): the cadence-runtime package marker. It imports NOTHING that fires a `signal`, a de-id call, or a dispatch at import time (the disabled-by-default surface). No module-level side effects. (Supports AC-7.)
- `scripts/runner/subscription_dispatch.py` (Create): the subscription-sub-agent dispatch seam builder + the headless Agent-SDK session harness behind an INJECTABLE factory (fixture-substitutable, 0 live spend).
  - `build_dispatch(session)` — returns a `dispatch(domain, prompt, summary) -> author envelope` callable (the exact built seam shape at `plan_orchestrator.py:148`) that routes each specialist/judge/lens call as a subscription sub-agent through the given `session` (Agent SDK per OQ-1) and returns the author envelope. It does NOT read the store, does NOT de-identify, does NOT re-host any loop logic — it only routes the already-de-identified `(domain, prompt, summary)` to the session. (Satisfies the seam-shape AC; consumed by Cycle 2's AC-1/AC-5/AC-6.)
  - The DEFAULT session factory (the non-injected production path) RAISES a typed runner error naming the missing env-scrub WHEN a runtime-detectable "env-scrub-not-applied" predicate holds (the factory checks for the wired `build_subscription_env` / a scrubbed-env marker and refuses when absent) — a live subscription session is NOT constructible while the scrub is unwired. Gating the raise on this detectable predicate (Architect-F2) lets T2 lift the refusal by WIRING the scrub (T2 AC-8) without editing the T1 test. Tests ALWAYS inject a fixture session; the unscrubbed production path stays refused. (Satisfies AC-9 / SEC-04.)
  - **Import constraint:** module import performs 0 side effects (no `signal`/de-id/dispatch). (Satisfies AC-7.)

Run: `.venv/bin/python -m pytest tests/runner/test_cadence_runner.py -q`
Expected: PASS (Cycle-1 tests).

#### Step 3: REFACTOR
Review for:
- Naming consistency of `build_dispatch(session)` + the default-factory symbol with the spec's Interface Contracts (T2 wires the env-scrubbed session under this seam; T4 wraps the driver that consumes it) — these are the stable seam names.
- The default-factory refusal is a SINGLE typed error (one home), not a scatter of ad-hoc raises.
- No frozen-engine symbol (`orchestrate`/`plan_driver`/`_should_regenerate`) crept into the import surface during GREEN (the AC-3 re-host grep will run in Cycle 2, but keep the surface clean now).

Run: `.venv/bin/python -m pytest tests/runner/test_cadence_runner.py -q`
Expected: PASS (no behavior change).

#### Step 4: REGRESSION
Run: `.venv/bin/python -m pytest -q`
Expected: PASS at the floor — `3 failed, 2312 passed, 7 skipped` with NO NEW 4th failure (`passed` grows by the Cycle-1 tests). A 4th distinct failure means the new module's import had a side effect outside the manifest — do NOT fix unrelated tests; investigate per the feedback protocol.

### Cycle 2: The Cadence Runner Driver — Single `signal(CADENCE_TRIGGER)` Entry + Crown-Jewel Containment + Extend-Not-Rebuild

#### Step 5: RED — Write Failing Tests
Call `/write-tests` for `scripts/runner/cadence_runner.py`, EXTENDING `tests/runner/test_cadence_runner.py` (mirror `tests/serve/test_plan_loop_regen.py`'s `_SummarizeDeid` / `_LeakDeid` / `_seed_biomarker` / `_seed_raw_pii` shapes; drive via `cadence_runner.run` with a fixture `dispatch_factory` yielding a recording fixture session + a fixture `deid_client`; 0 live spend, 0 real PII), targeting:
- **AC-1 (single `signal` call):** firing `cadence_runner.run(root, dispatch_factory=<fixture>, deid_client=<fixture>)` over a store whose derived state PASSES the debounce (`_seed_biomarker`-style seeding) invokes `plan_loop.signal` EXACTLY ONCE — a spy over `plan_loop.signal` records call-count == 1.
- **AC-2 (cadence-trigger-label):** the single `signal` call is made with `trigger == plan_loop.CADENCE_TRIGGER` (the constant `"cadence"`, `plan_loop.py:376`) — the spy asserts the `trigger` keyword equals `CADENCE_TRIGGER`, NOT `DATA_EVENT_TRIGGER` / `FREE_TEXT_TRIGGER`. Failing-capable: a driver hardcoding a different trigger reddens this.
- **AC-5(a) — crown-jewel PROVEN-NON-EMPTY faithful arm:** seed a synthetic legal-name + raw meds into the raw intake (`_seed_raw_pii`-style) AND seed a debounce-passing store; fire ONE cadence tick with a recording fixture dispatch, supplying a SUCCESS-returning fixture `deid_client` (its `deidentify` returns a valid PII-free `router.SUMMARY_FIELD_SET`-keyed summary — the SUCCESS arm, NOT the `{deidentified: False}` sentinel; `_SummarizeDeid`-style). Assert the recording dispatch captured **≥1 payload PER `plan_schema.PLAN_DOMAINS` member** (count of captured payloads whose `domain` ∈ the closed 4-set covers all four ⇒ ≥4) **AND 0 raw-PII tokens** (the seeded legal-name + raw-med strings) across every captured `(domain, prompt, summary)`. The 0-raw-PII claim is thereby asserted over a PROVEN-NON-EMPTY payload set. (Seed the store so all 4 specialist domains gate in — a clean summary with no domain-excluding hard-limit.)
- **AC-5(b) — crown-jewel FAILING-CAPABLE de-id-bypass arm (mirrors T4 AC-7(d) mutation-RED, made a permanent falsification test):** swap the faithful fixture for a FAITHLESS `deid_client` whose `deidentify` returns a whitelist-PASSING summary (keyed ONLY by `router.SUMMARY_FIELD_SET`) that embeds the seeded synthetic legal-name + raw-med token in an ALLOWED free-text field's VALUE (`goal-targets` / `hard-limits`; `_LeakDeid`-style). Assert the seeded token REACHES ≥1 dispatch payload and the wire-scan detects it — **raw-PII hit count ≥ 1**. This proves the AC-5(a) scan is NON-VACUOUS (it CAN go RED on a real leak). **Do NOT use a naïve pass-through** `deid_client` — `deid_in`'s field-NAME whitelist fail-closes the raw intake to the `{deidentified: False}` sentinel (0 dispatches, the QA-F1 vacuity); the faithless fixture MUST return a whitelist-passing summary carrying the token in an allowed VALUE. **Why the leak reaches the payload (corrected, QA + Security-L2):** NOT because "the test tree has no operator-identity config" — `vault/meta/operator-identity.txt` IS present on a populated instance and `deid_in`'s value-scan (`pii_scan`, loaded under cwd) reads the REAL operator tokens. The leak reaches the dispatch because the synthetic seed is (i) provably DISJOINT from any real operator-identity token AND (ii) NON-STRUCTURAL — not an email / phone / postal / ZIP shape, so the ALWAYS-ON structural value-scan patterns (which strip those regardless of identity config) do not catch it. **Seed a synthetic legal name provably disjoint from the operator tokens and non-structural — mirror the precedent `_SYNTHETIC_NAME = "Jordan Fakename"` (+ `_SYNTHETIC_DRUG = "Zalenprax-XR 250mg"`), `tests/serve/test_plan_loop_regen.py:58-59` — so the leak reliably reaches the dispatch on EVERY environment (a populated instance with `operator-identity.txt` present AND a fresh clone).**
- **AC-6 (local-store-only, de-id-IN proven-invoked):** a spy over the fixture `deid_client` and every outbound seam asserts (a) the ONLY call receiving raw store content is `deid_client.deidentify` — count of raw/store-content sends outside the de-id-IN call == 0 (the dispatch + every other seam receive 0 bytes of raw store content) — AND (b) `deid_client.deidentify` (the `deid_in` call) was INVOKED ≥1 over the debounce-passing seeded store (call-count ≥ 1), so the sole-egress claim rests on a de-id call that actually fired.
- **AC-7 (import-inert, cadence_runner arm):** with spies over `plan_loop.signal` + `deid_client.deidentify` + a recording dispatch, freshly importing (`importlib.reload`) `scripts.runner.cadence_runner` fires 0 `signal`, 0 de-id, 0 dispatch calls — building/importing the driver arms nothing.
- **AC-3 (extend-not-rebuild, 0 re-hosting + 0 serve edits):** a grep over `scripts/runner/cadence_runner.py` + `scripts/runner/subscription_dispatch.py` finds 0 occurrences of EACH of `_should_regenerate`, `compose_disposition`, `orchestrate.generate_plans`, `plan_driver.drive`, and a re-declared `MIN_REGEN_INTERVAL_DAYS` (count == 0 each); AND `git diff --name-only origin/main -- scripts/serve/` from the runner branch returns EMPTY (0 files — the runner drives `plan_loop.signal` INSIDE `scripts/serve/plan_loop.py` but edits no file under `scripts/serve/`). Encode both as subprocess-backed assertions in the test.
- **AC-4 (frozen glob byte-frozen, the canonical superset incl. `plan_orchestrator.py` + `plan_driver.py` + `scripts/serve/plan_loop.py`, SEC-03 / Security-M1 / Architect-F3):** `git diff --numstat origin/main -- 'scripts/plan/orchestrate.py' 'scripts/plan/pipeline.py' 'scripts/plan/assemble.py' 'scripts/plan/generate_plan.py' 'scripts/plan/adjudicate.py' 'scripts/plan/adjust.py' 'scripts/plan/track.py' 'scripts/plan/plan_orchestrator.py' 'scripts/plan/plan_driver.py' 'scripts/store/' 'scripts/serve/plan_loop.py'` returns EMPTY (0 changed lines) — the freeze includes `plan_orchestrator.py` (the `{deidentified: False}` sentinel HALT `:227-229` + the dispatch fan-out `:382`) and `plan_driver.py` (the re-entered driver), matching the build-plan checkpoint superset. Encode as a subprocess-backed assertion.
- **AC-10 (production `-m` entry wired — Architect-F1, 0-spend + T2-stable):** `scripts/runner/cadence_runner.py` exposes a `main(argv=None)` reachable as `python -m scripts.runner.cadence_runner` (the launchd→module→`run()` path the T3 plist ProgramArguments invoke). RED test — patch `cadence_runner.run` to a spy (and, if the production `deid_client` constructor touches the keychain/env at construct time, patch that constructor to a 0-spend sentinel too) and assert `main()` invokes `run` **EXACTLY ONCE** with the DEFAULT `dispatch_factory` (the `subscription_dispatch` default factory reference — NOT invoked, so no session constructs and no pre-scrub raise), a CONSTRUCTED `deid_client`, and the RESOLVED repo root. Separately assert the module is `-m`-resolvable (exposes `main` + is guarded so it runs only as the module entry) AND that importing it still fires 0 `signal`/0 de-id/0 dispatch (AC-7 holds). **0-spend:** `run` is spied → the default factory is never invoked and `.deidentify()` never fires. **T2-stable:** the test does NOT assert a pre-scrub raise (it spies `run` before the factory is reached), so T2's AC-8 factory-lift needs NO edit to this test.
- **AC-8 (fixture suite passes):** `.venv/bin/python -m pytest tests/runner/test_cadence_runner.py` passes (0 live-API calls, 0 real operator PII in the test tree) — this run also exercises the AC-9 default-factory-refusal and the AC-10 `main()`-entry tests in the same module.

Run: `.venv/bin/python -m pytest tests/runner/test_cadence_runner.py -q`
Expected: FAIL (the newly added Cycle-2 tests reference `cadence_runner.run` + `cadence_runner.main`, which do not yet exist).

#### Step 6: GREEN — Implement
Changes to make (descriptions, not literal code):
- `scripts/runner/cadence_runner.py` (Create): the runner DRIVER.
  - `run(root, *, dispatch_factory, deid_client, tailor_client=None, plan_date=None)` — obtains a subscription session from the injectable `dispatch_factory`, builds the dispatch seam via `subscription_dispatch.build_dispatch(session)`, and calls `plan_loop.signal(root, trigger=plan_loop.CADENCE_TRIGGER, dispatch=dispatch, deid_client=deid_client, tailor_client=tailor_client, plan_date=plan_date)` (`plan_loop.py:457`) **EXACTLY ONCE per tick**. It returns the loop's receipt unchanged. (Satisfies AC-1, AC-2.)
  - `main(argv=None)` — the production `-m` entry the T3 scheduler targets (the plist ProgramArguments invoke `python -m scripts.runner.cadence_runner`; without this the launchd→module→`run()` path is unwired — Architect-F1 "integration point never wired"). It resolves the repo root, constructs the production ADR-0027 `deid_client` (construction is 0-spend — spend is metered ONLY on `.deidentify()`, never on construct), passes `subscription_dispatch`'s DEFAULT session factory as `dispatch_factory` (a reference — `run` invokes it; `main` does not construct the session, so no pre-scrub raise fires here), and calls `run(root, dispatch_factory=<default factory>, deid_client=<constructed>, ...)`. The module is runnable only under `python -m` via the standard `__main__`-guard idiom (it runs `main()` solely when executed as the module entry, NEVER on `import`), so AC-7 import-inertness holds. (Satisfies AC-10 — the launchd→module→`run()` integration point.)
  - It NEVER imports or calls `_should_regenerate`, `regenerate` directly, `orchestrate.generate_plans`, `plan_driver.drive`, and NEVER re-declares `MIN_REGEN_INTERVAL_DAYS` — the raw read + `deid_in` happen INSIDE `signal → regenerate → run_orchestrated` in Python, never in the driver's / top-level session's context. The runner supplies seams; the debounce / fail-closed composition / cross-domain holds stay inside the built loop. (Satisfies AC-3.)
  - The raw `store.read_all` read and the `deid_in` de-identification are reached ONLY through `signal`; the driver hands `signal` the seams and the `deid_client`, so the de-id-IN call is the sole receiver of raw store content and the dispatch payloads carry only the de-identified summary. (Satisfies AC-5, AC-6 — the crown-jewel containment is a property of driving the built path, NOT re-implemented here.)
  - Module import performs 0 side effects — no `signal`/de-id/dispatch fires on `import`. (Satisfies AC-7.)
- No edit to ANY file under `scripts/serve/`, `scripts/plan/`, or `scripts/store/` (AC-3's 0-serve-edits + AC-4's frozen numstat).

Run: `.venv/bin/python -m pytest tests/runner/test_cadence_runner.py -q`
Expected: PASS.

#### Step 7: REFACTOR
Review for:
- Duplication between `run`'s session-obtain + `build_dispatch` wiring and any Cycle-1 seam logic — the driver OBTAINS the session and calls `build_dispatch`; the seam ROUTING lives in `subscription_dispatch.py` (one home each, no re-implementation).
- The single `signal` call site is literally singular (no conditional second call path) — the AC-1 "exactly once" is structural, not incidental.
- Confirm the import surface is `scripts.serve.plan_loop` (for `signal` + `CADENCE_TRIGGER`) + `scripts.runner.subscription_dispatch` ONLY — no frozen-engine symbol crept in during GREEN (the AC-3 grep must stay 0).
- Internal dead code introduced during GREEN (first-time module) — remove any.

Run: `.venv/bin/python -m pytest tests/runner/test_cadence_runner.py -q`
Expected: PASS (no behavior change).

#### Step 8: REGRESSION
Run: `.venv/bin/python -m pytest -q`
Expected: PASS at the floor — `3 failed, 2312 passed, 7 skipped` with NO NEW 4th failure (`passed` grown by the runner's tests). A 4th distinct failure means the change reached outside the manifest — do NOT fix unrelated tests; investigate per the feedback protocol. Re-confirm the frozen glob (AC-4) + `scripts/serve/` (AC-3) are numstat/name EMPTY after all edits.

## Interface Contracts

This task CREATES the runner-driver seams — the stable contract all three Wave-2 leaves supply to (Architect-reviewed; a later change to either signature triggers a contract-update notice to the Architect):

- `cadence_runner.run(root, *, dispatch_factory, deid_client, tailor_client=None, plan_date=None) -> loop receipt` — obtains a subscription session from `dispatch_factory`, builds the dispatch via `subscription_dispatch.build_dispatch(session)`, and calls `plan_loop.signal(root, trigger=plan_loop.CADENCE_TRIGGER, dispatch=..., deid_client=..., tailor_client=..., plan_date=...)` EXACTLY ONCE. The raw read + `deid_in` run inside `signal` (never in the driver). Consumed by **ADR-0039-T4** (wraps this read→regenerate→promote critical section in `store_lock.cadence_lock(root)`) and **ADR-0039-T3** (`activate.enable()` schedules a tick that invokes `run`).
- `cadence_runner.main(argv=None)` — the production `python -m scripts.runner.cadence_runner` entry (resolves root + constructs the ADR-0027 `deid_client` + passes `subscription_dispatch`'s default factory to `run`). This IS the launchd→module→`run()` integration point **ADR-0039-T3's plist ProgramArguments target** (`python -m scripts.runner.cadence_runner`); T3 MUST invoke this module entry, not a re-authored driver. The `__main__`-guard keeps `import` inert.
- `subscription_dispatch.build_dispatch(session) -> dispatch(domain, prompt, summary)` — routes each specialist/judge/lens as a subscription sub-agent through `session`, returning the author envelope (the built `plan_orchestrator.py:148` seam shape). The session comes from an INJECTABLE factory; the DEFAULT (non-injected) factory RAISES a typed error while its env-scrub-not-applied predicate holds (SEC-04, gated on a runtime-detectable condition — Architect-F2). Consumed by **ADR-0039-T2** (constructs the Agent-SDK session under `build_subscription_env(...)`, lifting the AC-9 refusal by WIRING the scrub — T2 AC-8 — with NO edit to the T1 test).
- Behavioral invariants: the runner re-hosts NONE of the loop (0 occurrences of the re-host tokens); it edits NO file under `scripts/serve/` / the frozen engine / the store (numstat EMPTY); import is inert (0 `signal`/de-id/dispatch); the de-id-IN call is the sole receiver of raw store content.
- Change control: ADR-0039-T2/T3/T4 MUST NOT change these signatures without Architect review (the build plan assigns Architect to Wave 1 for exactly this seam).

## Verification Checklist

- [ ] AC-1: `cadence_runner.run` over a debounce-passing store invokes `plan_loop.signal` EXACTLY ONCE — spy call-count == 1 (`test_single_signal_call`).
- [ ] AC-2: the single `signal` call carries `trigger == plan_loop.CADENCE_TRIGGER` (`"cadence"`), not `DATA_EVENT_TRIGGER`/`FREE_TEXT_TRIGGER` — spy asserts the `trigger` kwarg (`test_cadence_trigger_label`).
- [ ] AC-3: 0-re-hosting grep over `cadence_runner.py` + `subscription_dispatch.py` finds 0 of each re-host token AND `git diff --name-only origin/main -- scripts/serve/` is EMPTY — `test_extend_not_rebuild_no_rehost_no_serve_edit` (count == 0 each; 0 serve files).
- [ ] AC-4: `git diff --numstat origin/main -- <7 plan files + scripts/plan/plan_orchestrator.py + scripts/plan/plan_driver.py + scripts/store/ + scripts/serve/plan_loop.py>` returns EMPTY (0 changed lines — the canonical superset incl. the de-id sentinel HALT + dispatch fan-out host `plan_orchestrator.py`, the re-entered `plan_driver.py`, and the plan-loop host, SEC-03 / Security-M1 / Architect-F3) — `test_frozen_glob_numstat_empty`.
- [ ] AC-5: crown-jewel non-egress — (a) faithful SUCCESS-arm fixture over a debounce-passing seeded store captures ≥1 payload PER `PLAN_DOMAINS` member (≥4) with 0 raw-PII tokens (proven-non-empty) AND (b) a FAITHLESS whitelist-passing `deid_client` (leak in an allowed VALUE) makes the seeded token reach ≥1 payload → wire-scan hit count ≥ 1 (failing-capable, NOT the vacuous pass-through) — `test_crownjewel_faithful_zero_raw_pii` + `test_crownjewel_faithless_leak_detected`.
- [ ] AC-6: local-store-only — the ONLY seam receiving raw store content is `deid_client.deidentify` (count outside == 0) AND `deid_client.deidentify` invoked ≥1 over the seeded store — `test_local_store_only_deid_in_invoked`.
- [ ] AC-7: import-inert — importing `cadence_runner` + `subscription_dispatch` fires 0 `signal`/0 de-id/0 dispatch — `test_import_is_inert`.
- [ ] AC-8: `.venv/bin/python -m pytest tests/runner/test_cadence_runner.py` passes (0 live-API calls, 0 real PII), incl. the AC-9 test.
- [ ] AC-9: the DEFAULT (non-injected) subscription-session factory RAISES a typed error under the env-scrub-ABSENT predicate the test sets (SEC-04; gated on a pre-scrub-DETECTABLE condition so T2's AC-8 lift needs NO edit to this test, Architect-F2) — `test_default_session_factory_refuses` (`pytest.raises`).
- [ ] AC-10: `main(argv=None)` is wired as the `python -m scripts.runner.cadence_runner` entry — `main()` calls `run` EXACTLY ONCE with the default `dispatch_factory` + a constructed `deid_client` + the resolved root (spied, 0-spend, no pre-scrub raise ⇒ T2-stable); module is `-m`-resolvable + import stays inert (Architect-F1) — `test_main_entry_wires_run` + `test_module_dash_m_resolvable`.
- [ ] Risk (crown-jewel non-egress, QA-F1) covered by AC-5 (proven-non-empty + failing-capable de-id-bypass).
- [ ] Risk (local-store-only / de-id-IN proven-invoked, QA-F1) covered by AC-6.
- [ ] Risk (extend-not-rebuild, SEC-03) covered by AC-3 (0 re-host + 0 serve edits) + AC-4 (frozen numstat incl. `plan_loop.py`).
- [ ] Risk (SEC-04 un-scrubbed factory) covered by AC-9.
- [ ] Risk (disabled-by-default down-payment) covered by AC-7.
- [ ] No regression: `.venv/bin/python -m pytest -q` holds the `3 failed, 2312 passed, 7 skipped` floor (no 4th failure).
- [ ] Files modified match the manifest (`scripts/runner/__init__.py`, `scripts/runner/cadence_runner.py`, `scripts/runner/subscription_dispatch.py`, `tests/runner/test_cadence_runner.py`) — no scope creep; `scripts/serve/*`, the frozen engine, `scripts/store/*` byte-unchanged.
- [ ] Interface contracts documented (`cadence_runner.run` + `subscription_dispatch.build_dispatch` + the default-factory-refusal seam).

## Commit

```
feat(runner): cadence driver + dispatch seam
```
Stage only: `scripts/runner/__init__.py`, `scripts/runner/cadence_runner.py`, `scripts/runner/subscription_dispatch.py`, `tests/runner/test_cadence_runner.py`.

(Stage with `git add <the four paths>` as its OWN command, then `git commit` separately — the PII commit hook denies single-call stage+commit, `git commit -a`, and pathspec `git commit <path>`. Do NOT `git add vault/` — the modified `vault/**` lines are the S90 daemon frontmatter churn, not this task's; leave them uncommitted. Confirm no frozen-engine/`scripts/serve/` edit remains in the working tree before committing.)

## Rollback

If verification fails after 3 fix cycles:
1. `git stash -m "failed-ADR-0039-T1-attempt-{N}"` — preserve the work for diagnosis.
2. `git checkout {commit-before-task}` — revert to the entry-state HEAD recorded in Entry State (`5072d1c`).
3. Update the issue tracker: flag ADR-0039-T1 as BLOCKED with the failure log.
4. Escalate: "Task ADR-0039-T1 failed verification after 3 attempts. Stash ref: {ref}. Failure log: {summary}." Because ADR-0039-T1 is the Wave-1 entry point that T2/T3/T4 ALL supply to (the runner-driver seam + the crown-jewel containment gate), a BLOCK halts the entire runner build — surface it immediately, do not start any Wave-2 leaf.
5. Stash cleanup: after abandonment or re-planning, drop all `failed-ADR-0039-T1` entries from `git stash list`.

Do NOT: silently skip a failing acceptance criterion; weaken a test to make it pass (especially the AC-5 crown-jewel faithless-arm wire-scan or the AC-9 default-factory-refusal — these ARE the raw-PII-containment + un-scrubbed-session safety deliverable); or proceed to any Wave-2 leaf with the driver in a broken state.
