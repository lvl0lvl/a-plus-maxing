---
task-id: ADR-0046-T1
source-spec: docs/spec/adr-0041-0046-comprehensive-plan-spec.md
source-build-plan: docs/build-plan/build-plan-adr-0041-0046.md
wave: 3
assigned-agent: SE
reviewers: [QA, Architect]
created: 2026-07-12
status: approved
---

# Recipe: Progressive-Activation Gate at Dispatch + the 58z0 Periodized-Prescription Reconciliation (NO ROSTER GROWTH — relocated to Wave 4 / ADR-0043-T3; FROZEN-SPINE + BUG-01 RECONCILIATION)

> *[AMENDED 2026-07-12 — Architect, ADR-0046-T1 roster-growth sequencing (Option C re-sequence)]:* This recipe is RE-SCOPED per the binding Architect ruling (`docs/task-plan/.pipeline/ruling-0046-t1-sequencing.md`, Option C — RE-SEQUENCE). The Wave-3 manifest is the THREE files below — `scripts/plan/activation.py` (Create), `scripts/plan/generate_plan.py` (Modify — the 58z0 `prescription→renderable` projection ONLY), `tests/plan/test_activation.py` (Create). The `PLAN_DOMAINS` / `_PLAN_TRANSLATORS` / `_DOMAIN_KIND` / `_ROLE_OF_DOMAIN` dispatch-registry growth — and its two files `scripts/store/plan_schema.py` + `scripts/plan/plan_driver.py`, the roster-growth production-path AC-2 (dispatch+first-class record), the `plan_driver.py` three-byte-guard carves, and the `PLAN_DOMAINS`-tuple carve — RELOCATE to Wave 4 and co-land ATOMICALLY inside ADR-0043-T3 (grow-roster ∧ rewire-front-door-to-`active_domains` ∧ re-point-record-path-to-`plan_model`), where the growth's consumer is ready the instant it lands. `activation.active_domains()` is built over its OWN §1-§13 `CARD_DOMAINS` constant, DECOUPLED from `plan_schema.PLAN_DOMAINS` (which stays the closed four until Wave 4). KEPT here: AC-1/AC-3/AC-4/AC-5/AC-6/AC-7, the three 58z0 assertions (over the EXISTING four translators), a CURRENT-FOUR coherence gate, and the re-grounded AC-2 (a pure `active_domains`-reach test, no dispatch). `len(activation.CARD_DOMAINS) >= 13` replaces the retired `len(PLAN_DOMAINS) >= 13`.

> Wave-3 Tier-2 task (Dependencies: ADR-0041-T1, ADR-0041-T2, ADR-0044-T1 — all on the branch).
> It CREATES the progressive-activation gate `scripts/plan/activation.py` — `active_domains(operator_surface)`
> computes the active-domain set from the operator's surface (stated goals / tracked-stream data /
> care-conversation mentions / lab-or-genetic-trait touches) over its OWN §1-§13 CARD-EMITTING roster constant
> `activation.CARD_DOMAINS` (grounded against `design/specialist-plan-contracts.md`, disposition #25 — DECOUPLED
> from `plan_schema.PLAN_DOMAINS`, which stays the closed four until Wave 4), and partitions the §1-§13
> CARD-EMITTING domain specialists from the §14-genetics / §15-labs CROSS-CUTTING INPUTS
> (`design/specialist-plan-contracts.md`, disposition #25). The gate is at DISPATCH (disposition #24): don't
> brief an inactive specialist. **The gate is created + unit-tested here as a PURE `frozenset` contract; its
> WIRING into the front door (`server.py:784`, `plan_loop.py:77,407`) AND the dispatch-roster growth a >4 roster
> needs are ADR-0043-T3 (Wave 4) — this task publishes the `frozenset` contract 0043-T3 + 0046-T2 consume, it
> does NOT rewire the front door and it does NOT grow the dispatch registries (`PLAN_DOMAINS`'s dispatch-role,
> `_ROLE_OF_DOMAIN`, `_PLAN_TRANSLATORS`, `_DOMAIN_KIND` — those files are not in this manifest; ruling Option C).**
>
> THE HARD BLOCKER — BUG-01 periodized↔flat reconciliation (bead `a-plus-maxing-58z0`). The EXISTING four
> translators (workout/nutrition/supplements/peptides) consume the ADR-0041-T2 uniform `prescription`, whose
> CANONICAL shape is PERIODIZED (dated `blocks`, per-block `load` — PF-S131-01). `domain_program.prescription`
> is documented PERIODIZED in the frozen contract, yet the ADR-0041-T2 translators consume it FLAT
> (`_to_workout_plan` treats the prescription as a `{name, sets}` exercise dict) and `domain_program.validate`
> checks PRESENCE-not-SHAPE. The Wave-2 `_deep_strip_load` (already on the branch, `generate_plan.py:227`) is the
> PARTIAL fix — it recurses dicts+lists so an un-cleared per-block `load` never leaks (the SAFETY leg). The GAP
> this task completes: a BLOCKS-ONLY prescription (no top-level `name`/`sets`) becomes a `{"blocks":[...]}`
> "exercise" that FAILS `_validate_workout_plan`'s `name`/`sets` requirement at `record_plan` — a hard ValueError
> CRASH (the honest-handling leg) — and a periodized program's blocks must ROUND-TRIP intact (the fidelity leg).
> This is a property of the EXISTING four translators consuming the uniform `prescription` — independent of the
> roster growth (ruling §43), so it lands in Wave 3 on its own value. This recipe SCHEDULES the reconciliation
> via **Option (A)** — a `prescription→renderable` projection in the translator path (extract the renderable flat
> fields + DEEP-STRIP `load` over the FULL periodized structure + ride the full periodized program additively) —
> NOT Option (B) (pinning the shape in `domain_program.validate`), because `domain_program.validate` is the frozen
> change-controlled seam and is NOT in this manifest. The recipe schedules a MANDATORY RED test with a canonical
> periodized-blocks prescription asserting: (1) the recorded plan carries NO `load` at ANY depth, (2) a blocks-only
> prescription does NOT crash plan generation (honest handling — projected renderable or honest no-plan, never a
> raised ValueError), (3) a periodized program round-trips through `generate_plan`→`record_plan` intact (0-of-7
> dropped, the blocks preserved).
>
> FROZEN-SPINE (per-ADR-scoped, this ADR's OWN commit). This task does NOT grow a plan-spine surface —
> `PLAN_DOMAINS` STAYS the closed four (`("workout", "nutrition", "supplements", "peptides")`) until Wave 4; the
> dispatch-registry growth (RT-009 dispatch-registry role, distinct from ADR-0044-T1's stored-key supersession)
> RELOCATES to ADR-0043-T3. The per-ADR numstat probe (AC-4-numstat) proves the HARD-frozen four
> (`store.py`/`keying.py`/`pipeline.py`/`adjudicate.py`) EMPTY in this commit, `PLAN_DOMAINS` STILL == the closed
> four (a grown tuple here → RED, the growth is Wave 4), and the `<always-frozen>` 6-file set EMPTY over the whole
> build, while the sanctioned edits touch exactly `activation.py` (new module) + `generate_plan.py` (the 58z0
> `prescription→renderable` projection region). The ONLY frozen-guard carve this task's surfaces need is
> `activation.py`'s new-module glob exclusion (`test_route.py`/`test_pdf_ingestion_e2e.py` G1/G2) — the SHARED
> Wave-3 frozen-guard reconciliation (Option-A, F-011 — owned by NEITHER Wave-3 task, run BEFORE this task's
> commit); this task stages NO guard file and its Step-4 regression re-runs the carved guards GREEN. The
> `plan_driver.py` three byte-guard carves and the `PLAN_DOMAINS`-tuple carve RELOCATE to Wave 4 (they exist ONLY
> because of the growth — ruling B6).
>
> PF-S130-01 (the Wave-1 Tier-3 catch) is HONORED here by a PURE-FUNCTION reach test, not a dispatch. AC-2 is
> RE-GROUNDED: `active_domains(surface)` RETURNS a §1-§13 `CARD_DOMAINS` domain D BEYOND the original four when D
> is active on the surface — the gate's data-driven reach over the full card roster, a pure-function test needing
> NO dispatch and NO roster growth. The former AC-2 (a rich D DISPATCHES via the grown registries + records
> first-class via `plan_model`, the PF-S130-01 production-path) MOVES to ADR-0043-T3 (Wave 4), where the growth's
> consumer (the front-door + record re-point) lands in the same commit. Every fixture is SYNTHETIC (0 live-API
> spend, 0 real operator PII; PUBLIC repo).

## Entry State

**Prerequisites** (per the spec Dependency Map: ADR-0046-T1 ← ADR-0041-T1, ADR-0041-T2, ADR-0044-T1 — all on the branch):

- [ ] Predecessor **ADR-0041-T1 on the branch** (the seven-field DOMAIN PROGRAM the dispatched specialists emit against): `grep -q "^def validate" scripts/plan/domain_program.py && grep -q "^DOMAIN_KIND_RULES" scripts/plan/domain_program.py && echo OK`.
- [ ] Predecessor **ADR-0041-T2 on the branch** (the migrated `_PLAN_TRANSLATORS` shape the 58z0 projection rebases on — shared `generate_plan.py`, disjoint region): `grep -q "_lift_program" scripts/plan/generate_plan.py && grep -q "^_PLAN_TRANSLATORS = " scripts/plan/generate_plan.py && grep -q "_deep_strip_load" scripts/plan/generate_plan.py && echo OK` (the migration + the Wave-2 deep-strip safety leg are present).
- [ ] Predecessor **ADR-0044-T1 on the branch** (the comprehensive Plan Model + the retired `PLAN_DOMAINS` stored-key role — shared `plan_schema.py`, but this task does NOT edit it): `test -e scripts/store/plan_model.py && grep -q "^PLAN_DOMAINS = " scripts/store/plan_schema.py && echo OK` (the model exists; `PLAN_DOMAINS` survives at the closed four — its dispatch-registry growth is Wave 4 / 0043-T3, NOT this task).
- [ ] **Shared Wave-3 frozen-guard reconciliation carved BEFORE this commit** (owned by neither Wave-3 task) — the ONE carve group THIS task's new module depends on (Option C): **`activation.py` (new module) excluded from BOTH GLOB guards** — added to the `_FROZEN_ENGINE_PATHS` exclusion tuple in `tests/serve/test_route.py` (`:617`, glob `:614`) + `tests/serve/test_pdf_ingestion_e2e.py` (`:111`, glob `:108`), mirroring the `generate_plan.py`/`domain_program.py`/`context_assembler.py` carves. Confirm the reconciliation ran: `.venv/bin/python -m pytest tests/serve/test_route.py tests/serve/test_pdf_ingestion_e2e.py -q` passes with the new `activation.py` module present. This task stages NO guard file; a carve-out without its behavioral replacement (this task's `test_activation.py` activation-gate + partition + 58z0 tests) is a gate gap → HALT. *[AMENDED 2026-07-12 — Architect, Option C]:* the `PLAN_DOMAINS`-tuple carve (`test_plan_model.py:386`) and the `plan_driver.py` three-byte-guard carves (`test_plan_loop_hold.py`/`test_confirm_pointer_skip.py`/`test_cadence_runner.py`) RELOCATE to Wave 4 — they exist ONLY because of the growth, which is now 0043-T3's; this task neither grows `PLAN_DOMAINS`/`_ROLE_OF_DOMAIN` nor carves their guards.
- [ ] Full test suite at the Wave-2-merged floor: `.venv/bin/python -m pytest -q` reports the two PRE-EXISTING env-sensitive reds (`tests/serve/test_bind.py::test_port_collision_exits_nonzero_fail_loud`, `tests/serve/test_server.py::test_main_reads_no_stdin`) + N passed + 8 skipped. **The gate is no-NEW-failure** (the passed count is env-sensitive AND rose from the Wave-2 entry's 2529 by the merged Wave-2 modules — do NOT hardcode it); a 3rd distinct failure at entry → fix that first (not this task's to introduce).
- [ ] Working tree "clean" for the task scope — **NOTE the daemon caveat:** a bare `git status` is NOT empty (the basic-memory daemon perpetually re-churns `vault/**/*.md` frontmatter; parked `??` files exist). "Clean" here = (a) no STAGED changes (`git diff --cached --name-only` prints nothing); (b) the Create targets ABSENT (below); (c) the frozen numstat EMPTY (below). Do NOT `git restore vault/` (the daemon re-churns within ~1s — CLAUDE.md vault hygiene).
- [ ] On a feature branch cut off `main`: `feature/comprehensive-plan-wave-3` (the current Wave-3 build branch) or an equivalent `feature/*` — never `main`.
- [ ] Rollback target + AC-4 numstat base recorded: `PRE_TASK_HEAD = git rev-parse HEAD` at task entry — BOTH the `{commit-before-task}` the Rollback Protocol reverts to AND the base the per-ADR AC-4 numstat probe diffs against (QA-06: mirror `tests/plan/test_generate_plan.py:901`'s `PRE_TASK_HEAD` constant — diffing the working tree against the FIXED entry HEAD is PRE-COMMIT-runnable + in-flow RED-capability-demonstrable, unlike `HEAD^`). Because ADR-0041-T2 / 0044-T1 + the shared Wave-3 reconciliation are already IN this entry HEAD, the working-tree-vs-`PRE_TASK_HEAD` diff isolates ONLY 0046-T1's own delta (`activation.py` + `generate_plan.py`).
- [ ] The Create targets are ABSENT (Create-only, no stale/duplicate artifact): `for f in scripts/plan/activation.py tests/plan/test_activation.py; do test -e "$f" && { echo "PRESENT $f (stop)"; exit 1; }; done && echo "ABSENT (Create OK)"`.
- [ ] **Per-ADR-scoped frozen numstat — entry check (EMPTY at entry because nothing is edited yet).** The HARD-frozen four + `adjust.py`/`router.py` (`<always-frozen>`) are byte-identical to `origin/main`, and this task's one sanctioned MODIFY surface is byte-identical to `PRE_TASK_HEAD` at entry: `git diff --numstat origin/main -- scripts/store/store.py scripts/store/keying.py scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/router.py` prints nothing AND `git diff --numstat PRE_TASK_HEAD -- scripts/plan/generate_plan.py` prints nothing. *[AMENDED 2026-07-12 — Architect, Option C]:* `scripts/store/plan_schema.py` + `scripts/plan/plan_driver.py` are NO LONGER edited by this task — they too must show `git diff --numstat PRE_TASK_HEAD -- scripts/store/plan_schema.py scripts/plan/plan_driver.py` EMPTY at ENTRY AND EXIT (frozen for this task; their growth is Wave 4).
- [ ] The anchor surfaces exist at their pre-task shape, and `PLAN_DOMAINS` is the CLOSED FOUR (the growth is Wave 4): `grep -q '^PLAN_DOMAINS = ("workout", "nutrition", "supplements", "peptides")' scripts/store/plan_schema.py && grep -q "^_PLAN_TRANSLATORS = {" scripts/plan/generate_plan.py && echo OK` (`PLAN_DOMAINS` == the closed four INVARIANT for this task; the translator map present for the 58z0 projection to extend).

## Context Load List

**Files to read (this task's file manifest + the roster contract + the prescription machinery + the renderable schema the projection must satisfy + the guard the shared reconciliation carves):**

- [`design/specialist-plan-contracts.md`](../../design/specialist-plan-contracts.md) — the 18-agent roster + the CARD-EMITTING vs CROSS-CUTTING-INPUT partition the activation gate encodes: §1-§13 are card-emitting domain specialists (personal-trainer/nutritionist/peptide/supplement/endocrine/cardiovascular/recovery/sleep/longevity/mental-performance/dermatologist/gi/lymphatic); §14 genetics-specialist + §15 labs-specialist are CROSS-CUTTING INPUTS ("not a domain card"); §16-18 (medical-liaison, medical-safety-reviewer, care assistant) are safety/orchestration, not domain dispatch. Read the §11-§13 "Progressive-activation design note" (dermatology/gi/lymphatic are empty-state for the operator — the card renders only when a goal/data surfaces) — this IS the activation contract (AC-3 empty-state-no-card). **This is the ground truth for `activation.CARD_DOMAINS` (§1-§13, ≥13), decoupled from `plan_schema.PLAN_DOMAINS`.**
- [`scripts/store/plan_schema.py:49,270-275,356-368`](../../scripts/store/plan_schema.py) **READ-ONLY (not in this task's manifest — the growth is Wave 4)** — `PLAN_DOMAINS` (`:49`, STAYS the closed four here); `_PLAN_VALIDATORS` (270-275) + `_check_plan_args` (356-368); the four renderable validators (`_validate_workout_plan` 178-200 requires `exercises`=list of `{name:nonempty-str, sets:int 1..100}`; nutrition 203-231; supplements 234-249 `{name, dose}`; peptides 252-267 single-compound). The 58z0 projection must emit a shape these ACCEPT for the four renderable path. Do NOT edit this file.
- [`scripts/plan/generate_plan.py:114-153,169-241,251-466,478-580`](../../scripts/plan/generate_plan.py) — `_DOMAIN_KIND` (128-136, READ-ONLY here — its growth is Wave 4); `_lift_program` (169-211, the transitional adapter); `_deep_strip_load` (227-239, the Wave-2 SAFETY leg — recurses dicts+lists) + `_contains_load` (242-248); the four translators (251-432) that consume the prescription FLAT (the 58z0 surface — THIS task's edit) + `_with_program` (214-224, the additive ride); `_PLAN_TRANSLATORS` (461-466, READ-ONLY here — its growth is Wave 4); `compute_plan` (478-580, `if domain not in _PLAN_TRANSLATORS: raise KeyError` at 525) + `generate_plan` (582-630, records via `record_plan`, propagates its ValueError loud — the 58z0 crash surface). Reuse the `_FixedEnvelopeClient` seam (72-89) for 0-spend fixtures. **This task edits ONLY the shared translator projection region (the 58z0 `prescription→renderable` projection); it does NOT grow `_PLAN_TRANSLATORS`/`_DOMAIN_KIND`.**
- [`tests/plan/test_generate_plan_uniform.py:44,61,85,314-374,573`](../../tests/plan/test_generate_plan_uniform.py) — the fixture conventions to MIRROR (`PLAN_DATE` :44, `_seed_store(root)` :61, `_author(*recs)` :85, `_has_load_at_any_depth` :314, `_uniform_program`/`PROGRAM_KEY`) + the EXISTING periodized test `test_clearance_gate_deep_strips_periodized_load` (:346) whose fixture `_periodized_workout_uniform_rec` (:323) carries top-level `name`/`sets` ALONGSIDE `blocks` (so the flat validator passes INCIDENTALLY — the 58z0 test uses a BLOCKS-ONLY canonical shape that does not) + the ADR-0041-T2 per-ADR numstat probe (:573, already drops `plan_schema.py`+`generate_plan.py` for this wave — 0046-T1's edit to `generate_plan.py` does NOT re-trip it).
- [`docs/adr/.pipeline/frozen-guard-reconciliation-ruling.md`](../../docs/adr/.pipeline/frozen-guard-reconciliation-ruling.md) **§4-§5** — the BINDING carve-out authority (context for WHY the shared Wave-3 reconciliation exists + the behavioral-replacement requirement); this task does NOT perform the carve, it references it (only the `activation.py` glob exclusion applies under Option C).

**Do NOT load:**
- `docs/adr/ADR-0041*.md` … `ADR-0046*.md` — the spec already distilled these; re-reading re-opens resolved trade-offs (disposition #23/#24/#25), TP-02 drift.
- The full spec / build plan beyond the ADR-0046-T1 block + the Wave-3 checkpoint already extracted here.
- `scripts/plan/plan_driver.py` — its `_ROLE_OF_DOMAIN` growth is Wave 4 / 0043-T3; NOT in this task's manifest. `test_activation.py`'s current-four coherence gate READS it (`import`), it does NOT edit it.
- `scripts/serve/server.py` / `scripts/serve/plan_loop.py` (the front-door iteration — ADR-0043-T3's manifest, Wave 4; this task publishes the `active_domains` contract it consumes, does NOT rewire it) and `scripts/plan/orchestrate.py` / `scripts/serve/care_chat.py` (ADR-0043-T2, the Wave-3 SIBLING — an AC-4 sibling-excluded surface; do NOT open/edit).
- The frozen four (`store.py`/`keying.py`/`pipeline.py`/`adjudicate.py`) + `adjust.py`/`router.py` — this task edits none; they are a `numstat=0` entry+exit check, not a read.
- `scripts/plan/monitoring_compiler.py` / `scripts/store/plan_model.py`'s reader (ADR-0045-T1 / ADR-0044-T2, other Wave-3 tasks) — out of THIS task's scope.

## Risk Pre-Check

**BLOCKING GATE — the BUG-01 periodized↔flat reconciliation (bead `58z0`) + the activation-gate mis-fire class + the current-four registry-coherence gate + the roster PARTITION class.** Do not proceed to GREEN until a real (non-`assert True`), RED-capable test exists for every risk below. A placeholder fails this gate. This is NOT a `scripts/store/` write-surface task (it adds no keying), so the store-adversarial battery does NOT own here. *[AMENDED 2026-07-12 — Architect, Option C]:* the PF-S130-01 TAUTOLOGICAL-ROSTER-GROWTH risk + the GROWN-roster registry-coherence KeyError class RELOCATE to Wave 4 / 0043-T3 (they are properties of the growth); under Option C this task grows nothing, so its AC-2 is a pure `active_domains`-reach test and its coherence gate is over the CURRENT four.

- **Risk (BUG-01 / bead `58z0` — the four translators consume the uniform `prescription` FLAT; `domain_program.validate` checks presence-not-shape):** a canonical blocks-only prescription (`{"blocks":[{date,load}]}`, no top-level `name`/`sets`) becomes a `{"blocks":[…]}` "exercise" that FAILS `_validate_workout_plan` at `record_plan` — a hard ValueError CRASH; and an un-reconciled periodized program drops its dated blocks on the round-trip.
  - Gate condition (MANDATORY, PF-S131-01 canonical fixtures): RED must include the three 58z0 assertions on the REAL `generate_plan`→`record_plan`→`read_plan` path with a CANONICAL PERIODIZED prescription (dated `blocks`, per-block `load` — NOT a flat exercise dict): **(1)** the read-back plan carries **0 `load` at ANY depth** (deep — the renderable exercise AND the store-bound `program["prescription"]`); **(2)** a BLOCKS-ONLY prescription (no top-level `name`/`sets`) does **NOT crash** `generate_plan` (honest handling — a projected renderable that `record_plan` accepts, OR the honest no-plan state `recorded is False`; a raised `ValueError`/`KeyError` FAILS the assertion); **(3)** a periodized program **round-trips** — all seven program fields present on read-back (0 dropped) AND the periodized `blocks` intact in the stored `program["prescription"]`. RED-capable + non-tautological: run assertion (2) against the PRE-reconciliation translator (the flat-consuming path) — a blocks-only prescription raises ValueError at `record_plan` → the test REDs (the RED against the un-reconciled tree IS the revert-REDs proof).
- **Risk (activation reach — the gate must reason over the FULL §1-§13 card roster, not the closed four):** an `active_domains` computed over `plan_schema.PLAN_DOMAINS` (the closed four) could never return a §1-§13 domain beyond the four — the gate would silently under-cover the roster.
  - Gate condition (pure reach, AC-2, re-grounded): **AC-2** MUST assert `active_domains(surface)` RETURNS a `CARD_DOMAINS` domain **D BEYOND the original four** (e.g. `sleep`/`recovery`/`endocrine`) when D is active on the surface — a pure-function reach test over `activation.CARD_DOMAINS` (≥13), NO dispatch, NO roster growth. RED-capable + non-tautological: an `active_domains` computed over the closed four (or a `CARD_DOMAINS` missing D) never returns D → AC-2 REDs. (The DISPATCH + first-class-record of a rich D — the PF-S130-01 production-path — is ADR-0043-T3's, Wave 4.)
- **Risk (current-four registry-coherence — a `PLAN_DOMAINS` member lacking a translator/role crashes the dispatch path):** even at the closed four, `compute_plan` (`:525`) raises `KeyError` for a `PLAN_DOMAINS` member absent from `_PLAN_TRANSLATORS`; `_dispatch_domains` (`plan_orchestrator.py:381`) for one absent from `_ROLE_OF_DOMAIN`; `_lift_program` (`generate_plan.py:193`) for one absent from `_DOMAIN_KIND`. These three DISPATCH registries are the coherence set.
  - Gate condition (coherence over the CURRENT four): a test asserts `set(PLAN_DOMAINS) <= set(_PLAN_TRANSLATORS)` AND `set(PLAN_DOMAINS) <= set(_ROLE_OF_DOMAIN)` AND `set(PLAN_DOMAINS) <= set(_DOMAIN_KIND)` over the closed four (still a REAL check — a member with no translator reds it), AND `len(activation.CARD_DOMAINS) >= 13` (the CARD roster the gate reasons over is the §1-§13 set, even though the dispatch registries stay the four until Wave 4). The §1-§13 growth-coherence assertion (`len(PLAN_DOMAINS) >= 13` + the grown-registry `<=`) MOVES to Wave 4 / 0043-T3.
- **Risk (activation mis-fire either direction — ADR-0046 Consequences-Negative-2):** a false-positive dispatches an empty-state specialist (cost + a fabricated card); a false-negative drops an active domain (a missing plan).
  - Gate condition (both directions): **AC-3** (empty-state-no-card false-positive: a surface with NO goal/data in D → D not in `active_domains(surface)` → dispatch-for-D == 0 AND personalized-card-for-D == 0, at most the goal-agnostic reference) + **AC-4** (data-present-dispatch false-negative: a surface with a goal/data in D → D in `active_domains(surface)`). Both deterministic on synthetic surfaces. RED-capable: an always-full activation reds AC-3; an always-empty activation reds AC-4.
- **Risk (fan-out unbounded — ADR-0046 Consequences-Negative-1: dispatch cost scales with the active count):** an activation that returns the whole roster regardless of surface defeats progressive activation.
  - Gate condition (fan-out bound): **AC-5** — for a narrow-surface operator, `|active_domains(surface)| < |CARD_DOMAINS|` (the active set is a proper subset); `|active set| == |CARD_DOMAINS|` on a narrow surface → RED. (The `LARGE_CHANGE_THRESHOLD_DOMAINS` re-base that keeps the scaled loop safe is ADR-0046-T2, a hard sequencing precondition BEFORE the loop runs live — disposition #36, out of this task.)
- **Risk (roster partition — disposition #25: §14/§15 are cross-cutting INPUTS, not cards):** treating genetics/labs as card-emitting domains would give them a domain card they must never emit.
  - Gate condition (partition): **AC-6** — a grep/test asserts the §14-genetics / §15-labs specialists are NOT in the card-emitting set (`activation.CARD_DOMAINS`; trivially also absent from the closed-four `PLAN_DOMAINS`) and `active_domains` never returns them as card domains — they are partitioned as cross-cutting inputs. RED-capable: adding `genetics`/`labs` to `CARD_DOMAINS` reds it.

## TDD Steps

Single cycle. AC-1/AC-3/AC-4/AC-5/AC-6 (the activation gate) + AC-2 (the pure reach) + the current-four coherence gate + the three 58z0 assertions are properties of ONE coherent unit — the activation gate over `CARD_DOMAINS` + the `prescription→renderable` projection over the existing four translators — exercised over the SAME seeded fixtures (two operator surfaces, an active card domain D beyond the four, a canonical periodized-blocks program), so they are not split (task-planning: "all criteria test the same component"). RED writes every AC + the three 58z0 assertions as specific failing tests BEFORE any GREEN; the revert-REDs falsification (58z0 assertion-2) runs against the pre-change tree.

### Step 1: RED — Write Failing Tests
Call `/write-tests` for `scripts/plan/activation.py` + `scripts/plan/generate_plan.py`, creating `tests/plan/test_activation.py`, targeting each spec T1 AC + the three 58z0 assertions (mirror `test_generate_plan_uniform.py`'s `_seed_store`/`_author`/`_has_load_at_any_depth`/`PLAN_DATE`/`_FixedEnvelopeClient` conventions — all values SYNTHETIC, no real operator PII, no live client, 0 live spend):

- **AC-1 (data-driven active set — pure gate, load-bearing)** — `test_active_set_is_data_driven`: build two synthetic operator surfaces — one whose signals activate `{A, B}`, one whose signals activate `{A, C, E, G, I}` (goals / tracked-stream data / care mentions / lab-or-genetic-trait touches, per disposition #23); ASSERT `active_domains(surface1) == {A,B}` AND `active_domains(surface2) == {A,C,E,G,I}` AND the two returned sets DIFFER. A gate returning the same closed set regardless of surface → RED.
- **AC-2 (active domain reach — PURE FUNCTION, re-grounded per Option C)** — `test_active_domains_reaches_new_card_domain`: pick a §1-§13 `CARD_DOMAINS` domain **D beyond the original four** (e.g. `sleep`); build a surface where D is active (a goal / data / mention / lab-or-trait touch in D); ASSERT **D ∈ `active_domains(surface)`** — the gate's data-driven reach over the full §1-§13 card roster. RED-capable + NON-tautological: an `active_domains` computed over the closed four (or a `CARD_DOMAINS` missing D) never returns D → this test REDs. *[AMENDED 2026-07-12 — Architect, Option C]:* the former AC-2 (a rich D DISPATCHES via `compute_plan(D)` through the GROWN registries + records first-class via `plan_model.record_plan_version`→`read_plan_version`, the PF-S130-01 production-path with a roster-growth revert-RED) MOVES to ADR-0043-T3 (Wave 4) — it needs the growth + the front-door/record re-point, both 0043-T3's.
- **AC-3 (empty-state-no-card — false-positive falsifier, deterministic)** — `test_empty_state_domain_not_active_no_card`: a surface with NO goal AND NO data in a domain D; ASSERT D ∉ `active_domains(surface)` (dispatch-for-D would be 0 once the gate is wired at the front door by 0043-T3) AND, driving `generate_plan(D, <empty/thin-library envelope>, …)` for one of the FOUR renderable domains, `result["recorded"] is False` with an honest no-plan reason (0 personalized card for D — at most the goal-agnostic "nothing operator-specific" reference). A personalized card on an empty-state domain → RED.
- **AC-4 (data-present-dispatch — false-negative falsifier, deterministic)** — `test_data_present_domain_is_active`: a domain D with a goal OR data present IS in `active_domains(surface)` — **data-present D → D ∈ active set**; a missed active domain (false negative) → RED. (Composes with AC-2's reach on the same D.)
- **AC-5 (fan-out bound — falsifier, deterministic)** — `test_fan_out_bounded_to_active_set`: for a NARROW-surface operator (few goals/data), `len(active_domains(surface)) < len(activation.CARD_DOMAINS)` — the active set is a proper subset. `len(active set) == len(CARD_DOMAINS)` on a narrow surface → RED.
- **AC-6 (roster partition, disposition #25)** — `test_genetics_labs_partitioned_as_cross_cutting_inputs`: the §14-genetics / §15-labs specialists are NOT card-emitting — assert they are absent from `activation.CARD_DOMAINS` (trivially also absent from the closed-four `plan_schema.PLAN_DOMAINS`) AND never returned by `active_domains` as card domains (they feed inputs, per the roster doc). A grep/set assertion; adding `genetics`/`labs` to `CARD_DOMAINS` → RED.
- **AC-registry-coherence (current-four coherence gate, MUST)** — `test_registries_coherent_over_current_four`: `set(plan_schema.PLAN_DOMAINS) <= set(generate_plan._PLAN_TRANSLATORS)` AND `<= set(plan_driver._ROLE_OF_DOMAIN)` AND `<= set(generate_plan._DOMAIN_KIND)` — the DISPATCH registries over the CLOSED FOUR — **0 orphan `PLAN_DOMAINS` member** (else `compute_plan`/`_dispatch_domains`/`_lift_program` KeyErrors). Also assert `len(plan_schema.PLAN_DOMAINS) == 4` (the growth is Wave 4 — a grown tuple here → RED) AND `len(activation.CARD_DOMAINS) >= 13` (the CARD roster the gate reasons over IS the §1-§13 set, decoupled from `PLAN_DOMAINS`). RED-capable: a member missing from any dispatch registry reds the `<=`; a prematurely-grown `PLAN_DOMAINS` reds `== 4`. *[AMENDED 2026-07-12 — Architect, Option C]:* the §1-§13 grown-registry coherence (`len(PLAN_DOMAINS) >= 13` + `set(PLAN_DOMAINS) <= grown registries`) MOVES to Wave 4 / 0043-T3.
- **58z0-(1)+(2)+(3) (BUG-01 periodized reconciliation, PF-S131-01, LOAD-BEARING)** — three assertions over the REAL `generate_plan`→`record_plan`→`read_plan` path with CANONICAL PERIODIZED prescriptions (define the fixtures IN this module, per PF-S131-01 — the prescription is a dated-`blocks` structure with per-block `load`, NOT a flat exercise dict):
  - `test_periodized_program_records_zero_load_at_any_depth`: author a workout program whose prescription carries dated `blocks` each with a per-block `load` (canonical shape); run `generate_plan("workout", …, gates={"clearance_granted": False})`; ASSERT `result["recorded"] is True` AND the read-back plan carries **0 `load` at any depth** in BOTH every renderable exercise AND `program["prescription"]` (reuse `_has_load_at_any_depth`) — the clearance gate + deep-strip hold on the canonical periodized shape.
  - `test_blocks_only_prescription_does_not_crash`: author a BLOCKS-ONLY prescription (dated `blocks`, NO top-level `name`/`sets`); run `generate_plan("workout", …)` and ASSERT it does NOT raise (no `ValueError`/`KeyError` propagating from `record_plan`) — the result is EITHER `recorded is True` with a `record_plan`-valid projected renderable OR `recorded is False` with an honest no-plan reason (honest handling). RED-capable: against the PRE-reconciliation flat-consuming translator, `record_plan` raises `ValueError("workout plan exercise missing required field 'name'")` → this test REDs (the revert-REDs proof).
  - `test_periodized_program_round_trips_intact`: a periodized program (canonical shape carrying renderable identity + dated blocks) round-trips — all seven `domain_program` fields present on read-back (0 dropped) AND the periodized `blocks` intact in the stored `program["prescription"]`. Dropped/flattened blocks → RED.
- **Freeze-break numstat (FROZEN-SPINE, per-ADR-scoped — not a spec-numbered AC; the build-plan Wave-3 checkpoint + Test-Strategy mandate)** — `test_per_adr_scoped_freeze_break_numstat` (subprocess `git diff --numstat`, mirror `test_generate_plan.py:901`'s `PRE_TASK_HEAD` pattern): (a) `git diff --numstat <PRE_TASK_HEAD> -- scripts/plan/activation.py scripts/plan/generate_plan.py` is NON-empty (the sanctioned edits changed exactly these two); (b) `git diff --numstat <PRE_TASK_HEAD> -- scripts/store/plan_schema.py scripts/plan/plan_driver.py scripts/store/store.py scripts/store/keying.py scripts/plan/pipeline.py scripts/plan/adjudicate.py` is EMPTY (the HARD-frozen four untouched in THIS commit AND — per Option C — `plan_schema.py`/`plan_driver.py` frozen for this task, their growth is Wave 4; the sibling-superseded `orchestrate.py`/`care_chat.py`, 0043-T2, are EXCLUDED: they changed before `PRE_TASK_HEAD`); (c) `git diff --numstat origin/main -- scripts/store/store.py scripts/store/keying.py scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/router.py` is EMPTY (the `<always-frozen>` 6-file HARD set over the whole build); (d) `grep -q '^PLAN_DOMAINS = ("workout", "nutrition", "supplements", "peptides")' scripts/store/plan_schema.py` — `PLAN_DOMAINS` STILL the closed four (a grown tuple → RED, the growth is Wave 4). RED-capable: a transient edit to any forbidden file makes (b)/(c) RED; a grown `PLAN_DOMAINS` makes (d) RED.
- **AC-7 (module green, 0 live-API, 0 real PII)** — the whole `tests/plan/test_activation.py` module passes offline; every fixture is a synthetic literal + the `_FixedEnvelopeClient`/`_seed_store` seam (no real `ModelClient`, no real operator PII). Verified by the Step-2 run + an import grep (Verification Checklist) asserting only `pytest` + the manifest modules + `scripts.store` fixtures + the READ-ONLY `plan_schema`/`plan_driver`/`generate_plan` imports the coherence gate needs (0 live-client import).

Run: `.venv/bin/python -m pytest tests/plan/test_activation.py -q`
Expected: FAIL — `activation.py` does not exist (import fails) AND the 58z0 behavior is not yet implemented. (If a NON-numstat test passes at this stage, investigate before GREEN — the freeze-break numstat probe is the exception: `PRE_TASK_HEAD`-based, it is pre-commit-runnable + GREEN whenever the frozen surfaces are untouched; its RED-capability is shown by a throwaway frozen-file edit, not the module RED→GREEN.)

### Step 1 (continued): RED-capability falsification (NON-SKIPPABLE)
Before AND after implementing, prove per-assertion RED-capability against the pre-change tree:

1. **AC-2 reach non-tautology (Option C):** confirm `test_active_domains_reaches_new_card_domain` REDs against a stub `active_domains` that returns only the closed four (or a `CARD_DOMAINS` missing D) — the reach to a §1-§13 D beyond the four is what the test distinguishes. (The former roster-growth dispatch revert-RED is ADR-0043-T3's — not run here.)
2. **58z0-(2) reconciliation revert-REDs (PF-S131-01, load-bearing):** confirm `test_blocks_only_prescription_does_not_crash` is RED against the PRE-reconciliation `generate_plan.py` translators (a blocks-only prescription → `record_plan` ValueError). Re-run POST-GREEN by checking out the pre-reconciliation `generate_plan.py`, confirm RED, restore.
3. **Freeze-break numstat forbidden-file mutation:** make a throwaway one-line edit to a forbidden file (e.g. a comment in `scripts/plan/pipeline.py`, OR a token added to `PLAN_DOMAINS`), CONFIRM `test_per_adr_scoped_freeze_break_numstat` goes RED, `git checkout` the file. Proves the probe bites (both the frozen-four leg and the `PLAN_DOMAINS == 4` leg).

This is a mandatory gate of THIS recipe — do not silently delegate RED-capability to `/write-tests`.

### Step 2: GREEN — Implement
Changes to make (descriptions, NOT literal code):

- `scripts/plan/activation.py` (Create) — the progressive-activation gate. Define an `activation.CARD_DOMAINS` constant (the §1-§13 CARD-EMITTING slugs, grounded against `design/specialist-plan-contracts.md`, ≥13 — DECOUPLED from `plan_schema.PLAN_DOMAINS`, which stays the closed four until Wave 4) and `active_domains(operator_surface) -> frozenset` that computes the active CARD-EMITTING domain set (a subset of `CARD_DOMAINS`) from the operator's surface: a domain is ACTIVE when the surface carries a stated goal in it, OR tracked-stream data/a reading in it, OR a care-conversation mention, OR a lab value / genetic-trait-class touching it (the activation signal, disposition #23). The gate reads the SURFACE (a de-id-safe view of goals / tracked streams / care mentions / lab-or-trait touches — grounded against what the assembled context already carries; it originates no new operator-state source and needs no raw PII). Encode the roster PARTITION (disposition #25): the §1-§13 card-emitting domains are the candidate card set; the §14-genetics / §15-labs specialists are CROSS-CUTTING INPUTS — never in `CARD_DOMAINS`, never returned as card domains (they feed inputs, like the existing `genetic-trait-classes` token). Gate at DISPATCH: the returned frozenset is the set a wired dispatcher briefs (0043-T3 wires it at the front door). Leaf-ish module: import only stdlib + the roster it partitions against (no cycle with `generate_plan`/`plan_loop`). **Do NOT grow `plan_schema.PLAN_DOMAINS` or any dispatch registry — that is Wave 4 / 0043-T3.**
- `scripts/plan/generate_plan.py` (Modify — the 58z0 `prescription→renderable` projection in the translator path ONLY; `compute_plan`/`generate_plan` signatures unchanged; `_PLAN_TRANSLATORS`/`_DOMAIN_KIND` NOT grown) —
  - **The 58z0 `prescription→renderable` projection (Option A, the HARD BLOCKER):** in the shared translator path (the point each of the FOUR existing translators reads `prog.get(domain_program.PRESCRIPTION)`), add a projection that, given a prescription which may be the CANONICAL PERIODIZED shape (dated `blocks`, per-block `load`), (i) DEEP-STRIPS `load` over the FULL periodized structure via the existing `_deep_strip_load` (the Wave-2 safety leg — extend its application so every translator, not only workout, deep-strips before building the renderable AND the store-bound program), (ii) DERIVES the domain's renderable flat fields (workout `{name, sets, …}`; supplements `{name, dose}`; etc.) from the prescription — the top-level flat fields when present, else the periodized blocks' renderable content — so `record_plan`'s frozen per-domain validator ACCEPTS it, and (iii) when NO renderable content can be derived (a degenerate blocks-only prescription with no identity), returns the honest no-plan signal (the translator's existing `None`/skip path) rather than passing a malformed `{"blocks":[…]}` to `record_plan` (the crash). The FULL seven-field periodized program rides ADDITIVELY via `_with_program` (fidelity — the dated blocks round-trip, with `load` stripped when un-cleared). The exact per-domain derivation is executor discretion (SE Ask-vs-Proceed rule 3 — same public interface; Architect reviews the projection per the build-plan Agent Matrix). PRESERVE the workout clearance gate, the nutrition RED-S/LEA veto + aggregation, the peptides single-compound rule.
  - **No edit to `plan_schema.py`'s validators / `PLAN_DOMAINS` / the frozen four / `plan_driver.py` / `orchestrate.py`** — the periodized program rides the UNCHANGED `record_plan`/`store.append` (the frozen numstat keeps those EMPTY); the projection emits a shape the frozen four validators accept. The dispatch-registry growth is Wave 4 / 0043-T3, NOT this task.

Run: `.venv/bin/python -m pytest tests/plan/test_activation.py -q`
Expected: PASS (AC-1..AC-7 + the current-four coherence gate + the three 58z0 assertions). The freeze-break numstat probe is GREEN once `activation.py`+`generate_plan.py` carry the sanctioned edits, the frozen set is untouched, and `PLAN_DOMAINS` is the closed four.

### Step 2.5: Guard carve-out — NOT this task's manifest (performed by the shared Wave-3 frozen-guard reconciliation)
The ONE carve group this task's new module depends on is carved by the SHARED Wave-3 frozen-guard reconciliation (owned by neither Wave-3 task, run BEFORE this task's commit — the Architect Option-A standing rule, `frozen-guard-reconciliation-ruling.md` §4-§5; carved BY EXECUTION per F-011):
- **`activation.py` (new module)** — added to the `_FROZEN_ENGINE_PATHS` exclusion tuple in BOTH GLOB guards: `tests/serve/test_route.py` (`:617`, glob `:614`) + `tests/serve/test_pdf_ingestion_e2e.py` (`:111`, glob `:108`), mirroring the existing `generate_plan.py`/`domain_program.py`/`context_assembler.py` carves — else the `(scripts/plan).glob("*.py")` set emits an add-row for it and both guards RED (QA-W3-03).

`generate_plan.py` is ALREADY carved from the G1-G7 byte-guards (Wave-1) and from the ADR-0041-T2 numstat probe's assertion (this wave's `plan_schema.py`/`generate_plan.py` drop), so its 58z0 projection needs NO new carve. This task stages NO guard file; its Step-4 regression re-runs the carved guards GREEN. This task's behavioral replacement (ruling §4) is `test_activation.py`'s activation-gate + partition + 58z0 tests. *[AMENDED 2026-07-12 — Architect, Option C]:* the `PLAN_DOMAINS`-tuple carve (`test_plan_model.py::test_plan_schema_superseded_set_is_exactly_named`) and the `plan_driver.py` three-byte-guard carves (`test_plan_loop_hold.py`/`test_confirm_pointer_skip.py`/`test_cadence_runner.py`) RELOCATE to Wave 4 — they exist ONLY because of the growth, which is 0043-T3's; `PLAN_DOMAINS` stays byte-frozen at the closed four here, and `plan_driver.py` is unedited, so their guards STAY GREEN unmodified.

### Step 2.6: Revert-REDs re-verification (PF-S131-01 belt-and-suspenders)
`git checkout <PRE_TASK_HEAD> -- scripts/plan/generate_plan.py`; run `.venv/bin/python -m pytest "tests/plan/test_activation.py::test_blocks_only_prescription_does_not_crash" -q`; CONFIRM it goes RED (the un-reconciled translator crashes on the blocks-only prescription); restore the changed file (`git checkout HEAD -- …` / the working copy). A 58z0 test that stays GREEN on the reverted tree is tautological — HALT and fix the distinguishing input. (The AC-2 roster-growth revert-RED is ADR-0043-T3's, Wave 4 — not run here; AC-2's Option-C reach non-tautology is proven in Step 1-continued against a closed-four stub.)

### Step 3: REFACTOR
Review for:
- Duplication across the four translators' projection — one shared `prescription→renderable` projection helper (no per-domain copy of the deep-strip + derive + honest-no-plan logic); the per-domain delta is only the renderable-shape extraction, matching the existing four-translator shape.
- The activation signal is ONE predicate over the surface (goal / data / mention / lab-or-trait), not four copied branches; `CARD_DOMAINS` + the §1-§13 vs §14/§15 partition is a single source of truth (one constant), not a re-listed literal.
- No new import cycle (`activation` imports the roster + stdlib, NOT `plan_loop`/`server`; `generate_plan` keeps its `assemble`/`context_assembler`/`plan_schema`/`domain_program` imports).
- Remove any residue of the Step-1/Step-2.6 temporary stubs/reverts.

Run: `.venv/bin/python -m pytest tests/plan/test_activation.py tests/plan/test_generate_plan_uniform.py tests/plan/test_generate_plan.py -q`
Expected: PASS (no behavior change; the new module + the ADR-0041-T2 translator suite + the compute_plan/generate_plan behavioral suite all green — the 58z0 projection preserves the four existing translators' behavior).

### Step 4: REGRESSION
Run: `.venv/bin/python -m pytest -q`
Expected: PASS at the Wave-2-merged floor — the SAME two pre-existing env-sensitive reds (`test_bind`/`test_server::test_main_reads_no_stdin`) + 8 skipped; the passed count RISES by the new `test_activation.py` count. **The gate is no-NEW-failure.** In particular confirm GREEN:
- The TWO GLOB guards that now exclude the new `activation.py` module: `tests/serve/test_route.py::test_frozen_engine_byte_unchanged` + `tests/serve/test_pdf_ingestion_e2e.py::test_frozen_engine_byte_unchanged` — without the carve the `(scripts/plan).glob("*.py")` add-row for `activation.py` REDs both (QA-W3-03).
- The `PLAN_DOMAINS`-tuple guard `tests/store/test_plan_model.py::test_plan_schema_superseded_set_is_exactly_named` — STILL GREEN UNMODIFIED (this task does NOT grow `PLAN_DOMAINS`; its carve is Wave 4). The three `plan_driver.py` byte-guards (`test_plan_loop_hold.py`/`test_confirm_pointer_skip.py`/`test_cadence_runner.py`) — STILL GREEN UNMODIFIED (this task does NOT edit `plan_driver.py`).
- The roster-coupled front-door tests that read the live `plan_schema.PLAN_DOMAINS` (`tests/serve/test_server.py`, `tests/serve/test_plan_loop.py`, `tests/serve/test_plan_loop_regen.py`, `tests/serve/test_plan_loop_hold.py`, `tests/plan/test_plan_orchestrator.py`, `tests/plan/test_revise_loop.py`) STAY GREEN because `PLAN_DOMAINS` is UNCHANGED at the closed four — there is NO self-adjust to a grown roster in this task (that is Wave 4 / 0043-T3). A 3rd distinct failure here means the 58z0 projection broke a translator — do NOT fix unrelated tests; investigate whether the change bled past the manifest and flag per the feedback protocol.

## Interface Contracts

This task CREATES the `activation` gate contract + the 58z0 `prescription→renderable` projection — the stable seams Wave-4/Wave-5 tasks consume. It does NOT grow the dispatch registries (that is ADR-0043-T3, Wave 4):

- `activation.active_domains(operator_surface) -> frozenset[str]` — the computed active CARD-EMITTING domain set (a subset of `activation.CARD_DOMAINS`, the §1-§13 card roster ≥13, DECOUPLED from `plan_schema.PLAN_DOMAINS`), data-driven from the operator surface (goal / tracked-stream data / care mention / lab-or-genetic-trait touch, disposition #23); the §14-genetics / §15-labs cross-cutting inputs are NEVER in `CARD_DOMAINS` and NEVER returned as card domains (disposition #25). Gate at DISPATCH — a wired dispatcher briefs ONLY this set. Consumed by **ADR-0043-T3** (the front door replaces the closed `plan_schema.PLAN_DOMAINS` iteration at `server.py:784` + `plan_loop.py:77,407` with `active_domains(operator_surface)`, AND grows the dispatch registries to the §1-§13 roster in the SAME commit — ruling Option C) and **ADR-0046-T2** (the `LARGE_CHANGE_THRESHOLD_DOMAINS` re-base is a FRACTION of `|active_domains|`, not a fixed count-of-3). **The gate is created + unit-tested here as a PURE contract; the front-door wiring + the dispatch-roster growth are ADR-0043-T3 (Wave 4) — this task does NOT edit `server.py`/`plan_loop.py`/`plan_schema.py`/`plan_driver.py`.**
- **Current-four coherence invariant (this task's gate):** `set(PLAN_DOMAINS) <= set(_PLAN_TRANSLATORS) ∩ set(_ROLE_OF_DOMAIN) ∩ set(_DOMAIN_KIND)` over the CLOSED FOUR — every current card member is dispatchable, translatable, and kinded — AND `len(PLAN_DOMAINS) == 4` (the growth is Wave 4) AND `len(activation.CARD_DOMAINS) >= 13` (the gate's card roster). The GROWN-registry coherence (`len(PLAN_DOMAINS) >= 13` + `set(PLAN_DOMAINS) <= grown registries` + the single-source `set(PLAN_DOMAINS card-emitting) == set(activation.CARD_DOMAINS)`) is ADR-0043-T3's, Wave 4.
- The `prescription→renderable` projection (the BUG-01 / bead `58z0` reconciliation) — each of the FOUR existing translators projects the DOMAIN PROGRAM's CANONICAL PERIODIZED `prescription` (dated `blocks`, per-block `load`) to (a) the flat renderable shape `plan_schema`'s FROZEN per-domain validator requires (top-level flat fields when present, else derived from the blocks; honest no-plan when neither) + (b) the full seven-field periodized program riding additively via `_with_program`, DEEP-STRIPPING `load` over the FULL structure. **Behavioral invariant:** 0 `load` at any depth in the recorded plan; a blocks-only prescription never crashes `record_plan` (honest handling); a periodized program round-trips 0-of-7-dropped with its blocks intact. `record_plan`/`store.append`/`keying` + the frozen four + `router.py`/`adjust.py` + `plan_schema.py`/`plan_driver.py` byte-unchanged in this commit; the `plan_schema._validate_*_plan` renderable tables byte-unchanged (the projection emits a shape they accept). ADR-0044-T1 (first-class storage) reads the same seven-field periodized shape.
- **Change control:** ADR-0043-T3 / ADR-0046-T2 MUST NOT change the `active_domains` frozenset contract or the `CARD_DOMAINS` roster without Architect review (this wave carries Architect review for exactly the activation contract + the §1-13/§14-15 partition).

## Verification Checklist

- [ ] AC-1: `active_domains` is data-driven — two surfaces yield DIFFERENT active sets equal to their computed active domains — `test_active_set_is_data_driven`.
- [ ] AC-2: `active_domains(surface)` RETURNS a §1-§13 `CARD_DOMAINS` domain D beyond the original four when D is active on the surface (PURE reach; RED against a closed-four stub — the Option-C reach non-tautology) — `test_active_domains_reaches_new_card_domain`. *(The roster-growth dispatch+first-class-record production-path AC-2 is ADR-0043-T3, Wave 4.)*
- [ ] AC-3: an empty-state domain D (no goal/data) is NOT in `active_domains` AND records no personalized card (honest no-plan; false-positive falsifier) — `test_empty_state_domain_not_active_no_card`.
- [ ] AC-4: a data-present domain D IS in `active_domains` (false-negative falsifier) — `test_data_present_domain_is_active`.
- [ ] AC-5: for a narrow surface `|active_domains| < |activation.CARD_DOMAINS|` (fan-out bound) — `test_fan_out_bounded_to_active_set`.
- [ ] AC-6: §14-genetics / §15-labs are partitioned as cross-cutting inputs — absent from `activation.CARD_DOMAINS` (trivially also from the closed-four `PLAN_DOMAINS`) and never returned by `active_domains` as card domains (disposition #25) — `test_genetics_labs_partitioned_as_cross_cutting_inputs`.
- [ ] AC-7: `.venv/bin/python -m pytest tests/plan/test_activation.py` passes against synthetic fixtures (0 live-API, 0 real PII); the test imports only `pytest` + the manifest modules + `scripts.store` fixtures + the READ-ONLY `plan_schema`/`plan_driver`/`generate_plan` imports the coherence gate needs (0 live-client import) — import grep clean.
- [ ] Current-four coherence: `set(PLAN_DOMAINS) <= set(_PLAN_TRANSLATORS) ∩ set(_ROLE_OF_DOMAIN) ∩ set(_DOMAIN_KIND)` over the closed four + `len(PLAN_DOMAINS) == 4` (growth is Wave 4) + `len(activation.CARD_DOMAINS) >= 13` — `test_registries_coherent_over_current_four` (RED under an orphan member OR a prematurely-grown `PLAN_DOMAINS`).
- [ ] BUG-01 / bead `58z0` (the HARD BLOCKER, PF-S131-01 canonical periodized fixtures): (1) 0 `load` at any depth in the recorded plan — `test_periodized_program_records_zero_load_at_any_depth`; (2) a blocks-only prescription does NOT crash `generate_plan` (honest handling; RED against the pre-reconciliation translator — the revert-REDs proof) — `test_blocks_only_prescription_does_not_crash`; (3) a periodized program round-trips 0-of-7-dropped with blocks intact — `test_periodized_program_round_trips_intact`.
- [ ] Freeze-break numstat (FROZEN-SPINE): `git diff --numstat <PRE_TASK_HEAD> -- scripts/plan/activation.py scripts/plan/generate_plan.py` NON-empty; `... -- scripts/store/plan_schema.py scripts/plan/plan_driver.py scripts/store/store.py scripts/store/keying.py scripts/plan/pipeline.py scripts/plan/adjudicate.py` EMPTY (incl. `plan_schema.py`/`plan_driver.py` — frozen for this task under Option C); `git diff --numstat origin/main -- <always-frozen 6-file>` EMPTY; `PLAN_DOMAINS` STILL the closed four — `test_per_adr_scoped_freeze_break_numstat` (RED under a throwaway forbidden-file edit or a grown `PLAN_DOMAINS`, then reverted; sibling `orchestrate.py`/`care_chat.py` excluded).
- [ ] RED-capability falsification (Step 1-continued + Step 2.6, NON-SKIPPABLE): AC-2 observed RED against a closed-four `active_domains` stub; 58z0-(2) observed RED against the pre-reconciliation tree; the freeze-break numstat observed RED under a forbidden-file edit / a grown `PLAN_DOMAINS` — each reverted.
- [ ] Risk (BUG-01 / `58z0`) covered by the three 58z0 assertions (Option-A projection); Risk (activation reach) covered by AC-2's closed-four-stub RED; Risk (current-four coherence) by `test_registries_coherent_over_current_four`; Risk (activation mis-fire) by AC-3 + AC-4; Risk (fan-out) by AC-5; Risk (partition) by AC-6.
- [ ] Frozen-guard reconciliation (ruling §4/§5 — performed by the shared Wave-3 reconciliation, NOT this task's manifest): `activation.py` excluded from the two GLOB guards (`test_route.py`/`test_pdf_ingestion_e2e.py`) — GREEN post-carve; this task's behavioral-replacement deliverable (`test_activation.py` activation-gate + partition + 58z0) present + green (no gate gap). *(The `PLAN_DOMAINS`-tuple carve + the `plan_driver.py` three-byte-guard carves are Wave 4 — not performed here; those guards stay GREEN unmodified.)*
- [ ] No regression: `.venv/bin/python -m pytest -q` holds the floor — the same two known reds, 8 skipped, passed rises by the new module's count; the roster-coupled front-door tests STAY GREEN (`PLAN_DOMAINS` unchanged at the closed four — no self-adjust needed); no 3rd distinct failure.
- [ ] Files modified match the 3-file manifest: `scripts/plan/activation.py` (Create), `scripts/plan/generate_plan.py` (Modify — 58z0 projection ONLY), `tests/plan/test_activation.py` (Create) — NO `plan_schema.py`/`plan_driver.py` edit (their growth is Wave 4); the `activation.py` glob carve is the shared reconciliation commit's, NOT this task's; no other scope creep; temporary stubs/reverts undone.
- [ ] Interface contract documented (the `active_domains` frozenset seam over `CARD_DOMAINS` + the current-four coherence invariant + the 58z0 `prescription→renderable` projection contract + the 0043-T3 / 0046-T2 consumers + the front-door-wiring-AND-roster-growth-are-0043-T3 note).

## Commit

```
feat(plan): activation gate + 58z0 periodized reconciliation
```
Stage only: `scripts/plan/activation.py`, `scripts/plan/generate_plan.py`, `tests/plan/test_activation.py` (the `activation.py` glob carve-out is the shared Wave-3 frozen-guard reconciliation commit's, owned by neither Wave-3 task — this task stages NO guard file; and it stages NO `plan_schema.py`/`plan_driver.py` — the roster growth is Wave 4 / 0043-T3).

(Stage with `git add <the three paths>` as its OWN command, then `git commit` separately — the PII commit hook denies single-call stage+commit, `git commit -a`, and pathspec `git commit <path>`. Confirm the working tree has no file outside the three-path manifest — in particular that the Step-1/Step-2.6 temporary stubs/reverts were undone, that NO `vault/` daemon-churn or parked `??` file is staged, that `plan_schema.py`/`plan_driver.py` are byte-unchanged (`PLAN_DOMAINS` still the closed four), and that `store.py`/`keying.py`/`pipeline.py`/`adjudicate.py` are byte-unchanged — before committing. The per-ADR AC-4 numstat probes (`git diff --numstat PRE_TASK_HEAD …`, the fixed entry HEAD) are pre-commit-runnable and re-confirmed against the final commit.)

## Deviations

| # | Spec Says | Recipe Says | Reason | Impact |
|---|-----------|-------------|--------|--------|
| 1 | ADR-0046-T1 test manifest = `tests/plan/test_activation.py` ("data-driven active set, empty-state-no-card, data-present-dispatch, fan-out-bound"). | `tests/plan/test_activation.py` ALSO carries the re-grounded reach test (AC-2, pure `active_domains` reach — Option C), the current-four registry-coherence test, and the three BUG-01 / bead `58z0` periodized-reconciliation tests (production-path through `generate_plan`→`record_plan`). | The spec names exactly ONE test file for the task; the 58z0 reconciliation is intrinsic to the existing four translators (why the periodized shape reaches them), so its production-path tests belong to this task's single test module — not a new file (which would be a manifest-expansion). The 58z0 fixtures are defined in-module (canonical periodized shape, PF-S131-01). *[AMENDED 2026-07-12 — Architect, Option C]:* the ROSTER-GROWTH production-path AC-2 (a rich D DISPATCHES + records first-class) is REMOVED from this module and MOVES to ADR-0043-T3 (Wave 4) — it needs the growth + the front-door/record re-point. | `test_activation.py` is the ADR-0046-T1 integration module (activation gate + BUG-01 58z0), importing `generate_plan`/`plan_schema`/`activation` (the last two READ-ONLY). No new test file; the manifest stays at three paths. |
| 2 | "GROWS `PLAN_DOMAINS` … 4→15+"; the roster is "15+ specialists". | *[AMENDED 2026-07-12 — Architect, Option C]:* the roster growth is RELOCATED to Wave 4 / ADR-0043-T3 (it co-lands atomically with the front-door + record re-point that make a >4 roster safe — ruling Option C). This task defines the §1-§13 CARD roster as `activation.CARD_DOMAINS` (≥13, in `activation.py`), DECOUPLED from `plan_schema.PLAN_DOMAINS` (which STAYS the closed four here). §14-genetics + §15-labs are partitioned as CROSS-CUTTING INPUTS, never in `CARD_DOMAINS`. | The Architect ruling (`ruling-0046-t1-sequencing.md`, Option C) split 0046-T1: growing `PLAN_DOMAINS` in Wave 3 (before its front-door/record consumer lands in 0043-T3) breaks a production contract + 25 out-of-manifest tests. The `active_domains` gate reasons over `CARD_DOMAINS` (a decoupled constant), so it needs no dispatch-registry growth to be authored/tested (ruling §5 condition (b)). Genetics/labs emit NO card (disposition #25). | The dispatch registries (`PLAN_DOMAINS`/`_PLAN_TRANSLATORS`/`_DOMAIN_KIND`/`_ROLE_OF_DOMAIN`) STAY at the four here; `activation.CARD_DOMAINS` (≥13) is the gate's roster. The single-source assertion `set(PLAN_DOMAINS card-emitting) == set(activation.CARD_DOMAINS)` is asserted in Wave 4 when `PLAN_DOMAINS` grows. AC-6 pins the partition. |
| 3 | *[AMENDED 2026-07-12 — Architect, Option C]:* DELETED. | The former Deviation #3 asserted "the interim front door iterates the GROWN `PLAN_DOMAINS` (over-dispatching the full card roster) until 0043-T3, and coherent growth keeps the roster-coupled front-door tests self-adjusting GREEN via the per-domain `try/except` at `server.py:786`." | The Architect ruling re-executed this premise against the live tree and found it EMPIRICALLY FALSE (ruling Ground-truth Claim 2): the front-door record path (`orchestrate.generate_plans` → `_recorded_result` → `record_plan`) is OUTSIDE the `try/except` at `server.py:785` (which wraps only the author call), so a grown-roster rich domain reaches `record_plan(rich)` → raw `KeyError` → the WHOLE `/generate-plan` run aborts; the "self-adjusts green" premise does NOT hold. Under Option C there is NO interim over-dispatch — `PLAN_DOMAINS` stays the four in Wave 3, and the growth co-lands with its consumer in 0043-T3. | The interim-over-dispatch transient is REMOVED. `PLAN_DOMAINS` unchanged at the four → the roster-coupled front-door tests STAY GREEN with no self-adjust. |

## Rollback

If verification fails after 3 fix cycles:
1. `git stash -m "failed-ADR-0046-T1-attempt-{N}"` — preserve the work for diagnosis.
2. `git checkout {commit-before-task}` — revert to the entry-state HEAD recorded in Entry State (restores `activation.py` absence + `generate_plan.py` + discards this task's edits + any residual Step-1/Step-2.6 temporary stub/revert; the shared-reconciliation `activation.py` glob carve belongs to the separate Wave-3 reconciliation commit and is reverted with it, not here). `plan_schema.py`/`plan_driver.py` were never edited by this task.
3. Update the issue tracker: flag ADR-0046-T1 as BLOCKED with the failure log.
4. Escalate: "Task ADR-0046-T1 failed verification after 3 attempts. Stash ref: {ref}. Failure log: {summary}." ADR-0046-T1 is a Wave-3 task that ADR-0043-T3 (the front door dispatches its `active_domains` AND grows the roster in the same commit) and ADR-0046-T2 (the fraction threshold is computed against its active set) both depend on, AND it carries the BUG-01 / bead `58z0` HARD blocker (the four translators must not mis-handle a periodized program) — a BLOCK halts the Wave 3→4 boundary; surface it immediately, do not start ADR-0043-T3.
5. Stash cleanup: after abandonment or re-planning, drop all `failed-ADR-0046-T1` entries from `git stash list`.

Do NOT: silently skip a failing acceptance criterion; weaken a test to make it pass (especially the three BUG-01 / `58z0` periodized assertions, AC-2's closed-four-stub reach RED, or the current-four registry-coherence gate — these ARE the reconciliation + non-tautological gate safety deliverable); edit any frozen guard here (the `activation.py` glob carve is the shared Wave-3 reconciliation commit's, not this task's — this task stages NO guard file; the `PLAN_DOMAINS`-tuple + `plan_driver.py` carves are Wave 4); grow `PLAN_DOMAINS`/`_PLAN_TRANSLATORS`/`_DOMAIN_KIND`/`_ROLE_OF_DOMAIN` (Wave 4 / 0043-T3); or proceed to any downstream task (ADR-0043-T3 / ADR-0046-T2) with the 58z0 reconciliation in a broken state.
