---
task-id: ADR-0044-T1
source-spec: docs/spec/adr-0041-0046-comprehensive-plan-spec.md
source-build-plan: docs/build-plan/build-plan-adr-0041-0046.md
wave: 2
assigned-agent: SE
reviewers: [Architect, QA, Security]
created: 2026-07-11
status: approved
---

# Recipe: The Comprehensive Plan Model + Resolver — Value Rides the Frozen `store.append` (STORE SURFACE + FROZEN-SPINE)

> Wave-2 Tier-2 foundation task (Dependencies: ADR-0041-T1, on `main`). It CREATES the comprehensive
> Plan Model — `scripts/store/plan_model.py` — ONE composite object per plan version (per active domain:
> the seven-field DOMAIN PROGRAM intact + integrated narrative + dated milestones + compiled monitoring
> config + plan-level adjustment rules) + the comprehensive resolver + the record path — and SUPERSEDES
> the thin `plan_schema.py` record spine (the four `_validate_*_plan` validators `178-275`, `record_plan`
> `371-396`, `resolve_plan` `462-498`, and `PLAN_DOMAINS`'s stored-plan-domain-KEY role `49` — routed to
> `plan_model`). `PLAN_DOMAINS`'s dispatch-roster-registry role SURVIVES (RT-009 — grown later by
> ADR-0046-T1); `correct_plan` / `record_plan_tracking` / `resolve_tracking` / `read_plan_tracking` stay
> BYTE-UNCHANGED (not superseded here).
>
> THE VALUE RIDES THE FROZEN PRIMITIVE (disposition #14). The comprehensive plan is a richer VALUE, not a
> new store primitive: the record path appends through the UNCHANGED `store.append(item, _reading(...), root)`
> and reuses `keying`'s frozen `(item, timepoint, source)` dedupe identity with `value` EXCLUDED. The
> HARD-frozen WRITE primitive stays byte-frozen — `store.py` / `keying.py` numstat=0 for the whole build,
> AND `pipeline.py` / `adjudicate.py` numstat=0 in this commit (per-ADR AC-4). The model defines NO second
> dedupe key.
>
> STORE SURFACE — the four-part store-adversarial battery (bead `pka`, `docs/checklists/store-adversarial-tests.md`)
> is a BLOCKING Tier-1 gate; category (d) run RED-then-revert under a NAMED store-keying mutation. A
> placeholder / `assert True` test fails the gate. The S37–S41 five-wave escape pattern (cross-stream,
> same-timepoint dedupe) is the exact failure class this battery moves upstream of Tier-3.
>
> PF-S130-01 (the Wave-1 Tier-3 catch). AC-1 comprehensive round-trip AND AC-3 standing resolution are
> PRODUCTION-PATH tests — the REAL `plan_model` record → `store.append` → `plan_model` resolve path over a
> scratch store — with a distinguishing input (narrative + dated milestones + compiled monitoring config +
> ≥2 domains in ONE version) that the old thin plan CANNOT carry, so a revert to the thin
> `plan_schema.record_plan` / `resolve_plan` path drives them RED (Step 2.5). A round-trip test that passes
> whether or not the comprehensive model is used is tautological and does NOT satisfy the AC.
>
> FROZEN-GUARD RECONCILIATION (binding — performed by the Wave-2 frozen-guard reconciliation step, NOT
> this task's manifest). This is the FIRST task to supersede `plan_schema.py`'s record spine, so it trips
> the two prior-feature byte-guards that freeze `resolve_plan`/`record_plan` at 0 lines AND — because it
> CREATES `plan_model.py` + MODIFIES `plan_schema.py` under `scripts/store/` — the two whole-`scripts/store/`-dir
> freezes (`test_cadence_runner.py::test_frozen_glob_numstat_empty` :297, `tests/runner/test_store_concurrency.py::test_no_new_store_stream` :297).
> Per the Architect Option-(A) arbitration (`docs/task-plan/.pipeline/review-architect.md` §3) + the ruling
> §2 addendum + build-plan-adr-0041-0046's Wave-2 frozen-guard reconciliation step, ALL these carve-outs are
> owned by a SINGLE wave-level reconciliation commit (neither 0044-T1 nor 0041-T2's manifest): the record-spine
> tuples narrow to `("correct_plan", "record_plan_tracking")` (dropping ONLY `resolve_plan`+`record_plan`),
> and each `scripts/store/` whole-dir freeze narrows to an enumerated frozen-store set MINUS `plan_model.py`+`plan_schema.py`
> — leaving the `<always-frozen>` HARD set frozen throughout. The behavioral replacement (ruling §4) this task
> DELIVERS is `test_plan_model.py`'s round-trip fidelity + resolver + store-adversarial battery + the per-ADR
> AC-4 numstat. This task stages NO guard file; its Step-4 regression runs AFTER the reconciliation. A carve-out
> WITHOUT a behavioral replacement is a gate gap — HALT.

## Entry State

**Prerequisites** (ADR-0044-T1 depends on ADR-0041-T1, on `main` via Wave-1 merge #330 — the seven-field DOMAIN PROGRAMs the model stores):

- [ ] Task ADR-0041-T1 complete: `scripts/plan/domain_program.py` present and exports `validate`, `REQUIRED_FIELDS`, `CONDITIONAL_FIELDS`, `VALIDITY_TIERS` — `grep -q "^def validate" scripts/plan/domain_program.py && grep -q "^REQUIRED_FIELDS" scripts/plan/domain_program.py && echo OK` (the schema the composite validates each domain program against).
- [ ] Full test suite at the baseline floor: `.venv/bin/python -m pytest -q` reports `2 failed, 2529 passed, 7 skipped` (2538 collected). The two failures — `tests/serve/test_bind.py::test_port_collision_exits_nonzero_fail_loud` and `tests/serve/test_server.py::test_main_reads_no_stdin` — are PRE-EXISTING, environment-sensitive OS-port/stdin cases, NOT this task's to fix; a 3rd failure is a regression, HALT. The gate is no-NEW-failure, not an absolute count (the passed count is env-sensitive).
- [ ] Working tree clean for the task scope: `git status --short` shows no STAGED changes and no uncommitted changes to `scripts/store/` / `tests/store/` / `tests/plan/`. ("Clean" = no STAGED path + the Create targets absent + the frozen-spine numstat=0 below. The basic-memory daemon perpetually re-churns `vault/**/*.md` frontmatter — S90; that churn + any parked `??` files are EXPECTED noise, NOT this task's changes. Do NOT stage them.)
- [ ] On a feature branch cut off `main`: `feature/comprehensive-plan-wave-2` (or equivalent `feature/*`) — never `main`.
- [ ] Rollback target + AC-4 numstat base recorded: `PRE_TASK_HEAD = git rev-parse HEAD` at task entry — BOTH the `{commit-before-task}` the Rollback Protocol reverts to AND the base the per-ADR AC-4 numstat probe diffs against (QA-06: mirror `tests/plan/test_generate_plan.py:901`'s `PRE_TASK_HEAD` constant — diffing against the FIXED entry HEAD is PRE-COMMIT-runnable + in-flow RED-capability-demonstrable, unlike `HEAD^`, which exists only post-commit and forces committing a knowingly-RED probe). Because 0041-T2 + the Wave-2 frozen-guard reconciliation (if they land first on the branch) are already IN this entry HEAD, the working-tree-vs-`PRE_TASK_HEAD` diff still isolates ONLY 0044-T1's own delta over `pipeline.py`/`adjudicate.py`/`store.py`/`keying.py` (none of which they touch), so the per-ADR scoping is preserved AND the probe runs in-flow without a committed RED test.
- [ ] The Create targets are ABSENT (Create-only, no stale/duplicate artifact): `for f in scripts/store/plan_model.py tests/store/test_plan_model.py; do test -e "$f" && { echo "PRESENT $f"; exit 1; }; done && echo "ABSENT (Create OK)"`.
- [ ] The `<always-frozen>` HARD set byte-frozen at entry (numstat==0 vs `origin/main`): `git diff --numstat origin/main -- scripts/store/store.py scripts/store/keying.py scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/router.py` prints nothing (empty). The comprehensive plan rides `store.append`/`keying`'s `(item, timepoint, source)` unchanged as a richer VALUE (disposition #14).
- [ ] The superseded anchor surfaces exist (present to be superseded): `grep -q "^def record_plan" scripts/store/plan_schema.py && grep -q "^def resolve_plan" scripts/store/plan_schema.py && grep -q "_PLAN_VALIDATORS" scripts/store/plan_schema.py && grep -q "^PLAN_DOMAINS = " scripts/store/plan_schema.py && echo OK`.
- [ ] The two plan_schema record-spine byte-guards are present at their pre-carve shape (the frozen-function tuples the Wave-2 reconciliation narrows — NOT this task): `grep -q 'test_frozen_spine_numstat_and_read_plan_only_hunk' tests/plan/test_confirm_pointer_skip.py && grep -q 'test_record_plan_byte_unchanged_vs_pretask' tests/plan/test_generate_plan.py && echo OK` — both currently pin `resolve_plan` + `record_plan` byte-frozen (only `read_plan`'s ADR-0040 hunk sanctioned); the Wave-2 frozen-guard reconciliation step (owned by neither task) extends the carve-out to the 0044-superseded surfaces AND narrows the two `scripts/store/` whole-dir freezes before this task's commit. This task stages NO guard file.

## Context Load List

**Files to read (this task's file manifest + the frozen sinks it writes through + the guards it carves + the store-adversarial precedent):**
- `scripts/store/plan_schema.py` — the thin surfaces this task SUPERSEDES + the shape to mirror. `PLAN_DOMAINS` (`:49`, its stored-plan-domain-KEY role retired, its tuple + dispatch-roster role SURVIVING); the four `_validate_*_plan` validators + `_PLAN_VALIDATORS` + `_check_plan_args` (`178-275`, `356-368`); `record_plan` → `store.append(item, _reading(item, plan_date, f"{_PREFIX_PLAN}{specialist}", plan), root)` (`371-396`, the append-ride shape to reuse); `resolve_plan` (`462-498`, the reversed-scan latest-wins-per-`on_date` resolution shape the comprehensive resolver mirrors); `read_plan` (`501-520`, the public `store.read` + `filter_confirmed` + `resolve_plan` wrapper). The `_PREFIX_PLAN = "plan::"` / `_PREFIX_TRACK = "plan-track::"` namespacing convention (`56-57`) — the comprehensive model gets a NEW disjoint prefix. `correct_plan` / `record_plan_tracking` / `resolve_tracking` / `read_plan_tracking` are NOT superseded (stay byte-unchanged).
- `scripts/store/store.py` — the FROZEN WRITE primitive the value rides UNCHANGED: `append(item, reading, root)` idempotent on the `(item, timepoint, source)` dedupe identity (`134`; re-append of a same-identity reading is a no-op, value included or not — the normal-ingest path NEVER overrides a stored value), `read(item, root)` latest-wins-per-identity + timepoint-sorted (`214`). Do NOT edit (byte-frozen — entry-state numstat only). The model appends through this, never a new sink.
- `scripts/store/keying.py` — the dedupe identity `(item, timepoint, source)` with `value` EXCLUDED: `LINE_FIELDS = ("item","timepoint","source","value")` + `DEDUPE_FIELDS` (`11-16`) + `dedupe_key` (`19`). Do NOT edit (byte-frozen). `DEDUPE_FIELDS` is computed ONCE at import from `_DEDUPE_EXCLUDED` and `dedupe_key` reads `DEDUPE_FIELDS`, so the category-(d)-2 mutation patches `keying.DEDUPE_FIELDS` DIRECTLY (patching `_DEDUPE_EXCLUDED` at test time is INERT — the frozen tuple never recomputes).
- `scripts/store/loop_schema.py` — the `_reading(item, timepoint, source, value)` reading-builder (`81`) `record_plan` already uses (the 4-field `keying.LINE_FIELDS` dict) + `_content_tag(prefix, value)` (`62`, the content-hashed source tag on the `plan-track::` prefix — the precedent for the model's VERSION-KEY source discriminator: two distinct versions at one timepoint get distinct sources and both persist, an identical re-record is an idempotent store dedupe no-op).
- `scripts/plan/domain_program.py` — the ADR-0041-T1 seven-field DOMAIN PROGRAM contract each per-domain program in the composite is validated against: `validate(program)` (`134`), `REQUIRED_FIELDS`/`CONDITIONAL_FIELDS` (`76-77`), the field-name constants (`PRESCRIPTION`…`CROSS_DOMAIN_SEAMS`, `68-74`), `VALIDITY_TIERS`/`SIGNAL_TIER_KEY` (`93-94`). READ-only reuse (do NOT edit — the frozen schema seam).
- `docs/checklists/store-adversarial-tests.md` — the four required categories AC-5 embeds (cross-stream namespace collision, same-timepoint dedupe, dedupe-key boundary, mutation-RED), the enforcement points, and the S37–S41 escape pattern this battery moves upstream.
- `tests/store/test_correction.py` and `tests/store/test_plan_schema.py` — the existing `tmp_path`-root store-fixture pattern to MIRROR (seed via `store.append` / the `plan_schema` API under a `tmp_path` root, build a per-item `store_read` callable, run assertions over the result) + the store-adversarial precedent (`test_correction_never_surfaces_in_another_stream`-style cross-stream isolation) the AC-5 battery parallels. No new fixture machinery.
- `tests/plan/test_confirm_pointer_skip.py` (READ-ONLY — carved by the Wave-2 reconciliation, NOT this task) — `test_frozen_spine_numstat_and_read_plan_only_hunk` (`211`): its frozen-function tuple at `236` (`"resolve_plan", "record_plan", "correct_plan", "record_plan_tracking"`) is narrowed to `("correct_plan", "record_plan_tracking")` by the reconciliation (dropping the two 0044-superseded surfaces); its `FROZEN_GLOB` numstat + `care_team_rollup.py` byte-check stay intact. This task's Step-4 regression re-runs it GREEN — it does NOT edit it.
- `tests/plan/test_generate_plan.py` (READ-ONLY — carved by the Wave-2 reconciliation, NOT this task) — `test_record_plan_byte_unchanged_vs_pretask` (`1003`): the frozen-function tuple at `1014` (`"record_plan", "correct_plan", "record_plan_tracking", "resolve_plan"`) is narrowed the same way by the reconciliation; `PRE_TASK_HEAD` (`901`) is its base — and this task's AC-4 numstat base (QA-06).
- `docs/adr/.pipeline/frozen-guard-reconciliation-ruling.md` — §4 (behavioral-replacement sufficiency) + §5 (the standing per-wave carve-out rule) — the BINDING authority for the two guard edits; read §4's table row for the replacement-guard obligation.

**Do NOT load:**
- The ADRs (`docs/adr/ADR-0041-*.md` … `ADR-0046-*.md`) — the spec already distilled them; re-reading re-opens resolved trade-offs (TP-02 drift).
- The frozen primitives beyond the entry-state numstat check: `scripts/store/store.py` / `scripts/store/keying.py` are READ above for the append/dedupe CONTRACT the model rides, never edited; `pipeline.py` / `adjudicate.py` / `adjust.py` / `router.py` are not read at all (byte-frozen, not this task's surface).
- The other ADR-0044/0041/0043/0045/0046 task blocks + their manifests (`plan_model.py`'s mixed-history reader = 0044-T2; the confirm-hold skip = 0044-T4; `horizons.py` re-base = 0044-T3; `track.py` re-point = 0044-T2; `orchestrate.py`/`care_chat.py`/`plan_loop.py`/`server.py`/`activation.py`/`monitoring_compiler.py` = other tasks) — out of THIS task's scope. This recipe covers ONLY the `### ADR-0044-T1` block.

## Risk Pre-Check

**BLOCKING GATE — STORE SURFACE (bead `pka`) + the FROZEN-SPINE record-spine supersession (the biggest one-way door).** Do NOT proceed to GREEN until a real (non-`assert True`), RED-capable test exists for every risk below. The task is NOT complete until the AC-5 category-(d) battery + the PF-S130-01 revert falsifier are proven mutation-RED against the GREEN implementation (Step 2.5 — the store-keying / thin-path logic being mutated does not exist until GREEN). A placeholder test fails this gate.

- **Risk (ADR-0044 Consequences-Negative-1 — breaks the ADR-0032 record-spine freeze, the biggest one-way door; tension-resolution #27):** the supersession must be deliberate + bounded, and the store WRITE primitive must stay byte-frozen (the value rides the primitive, it is not a new primitive).
  - Gate condition: RED must include the **AC-4 per-ADR numstat probe** — `git diff --numstat PRE_TASK_HEAD -- scripts/plan/pipeline.py scripts/plan/adjudicate.py` == 0 changed lines AND `git diff --numstat PRE_TASK_HEAD -- scripts/store/store.py scripts/store/keying.py` == 0 changed lines (the value rides the primitive unchanged, disposition #14); the `scripts/store/plan_schema.py` delta touches ONLY the four named surfaces (the 4 `_validate_*_plan` validators + their `_PLAN_VALIDATORS`/`_check_plan_args` dispatch, `record_plan`, `resolve_plan`, and the stored-key-role reroute of `read_plan`), with `correct_plan` / `record_plan_tracking` / `resolve_tracking` / `read_plan_tracking` byte-UNCHANGED (a `_func_src` AST check); AND the `<always-frozen>` 6-file glob == 0 vs `origin/main`. **0 unnamed frozen edits.** EXPLICITLY EXCLUDED from THIS ADR's forbidden set (sibling supersessions, so probes don't contradict): `generate_plan.py`+`assemble.py` (0041), `orchestrate.py` (0043), `adjust.py`/`track.py` (0045/consumer) — they simply do not appear in 0044-T1's OWN commit delta.
- **Risk (S37–S41 store-keying escape pattern, bead `pka`):** the builder + Tier-2 systematically under-test store cross-stream / same-timepoint-dedupe / dedupe-key cases; five consecutive waves shipped a store-keying escape caught only at Tier-3.
  - Gate condition: RED must include all four adversarial categories as real tests over the `plan_model` record/read surface — (a) cross-stream namespace collision (a `plan-model` read for one plan-version stream never returns another version's OR a thin `plan::<domain>` value — seed a thin `plan::` decoy + a sibling version), (b) same-timepoint dedupe (two DISTINCT plan versions sharing a timepoint both persist under distinct version-key source tags; an identical re-record is an idempotent no-op, 0 duplicate lines), (c) dedupe-key boundary + no-new-keying (a grep/AST asserts `plan_model.py` defines 0 new dedupe key / keying logic — identity stays `keying`'s frozen `(item, timepoint, source)`; AND same `(item, timepoint, source)` + a CHANGED value is a store-dedupe no-op, second write dropped, first value wins), (d) **mutation — a NAMED battery test must go RED under a store-keying mutation, then revert.** Two named mutations (Step 2.5): **(d)-1** blank the `plan-model::` item-namespace prefix (`_PREFIX_MODEL=""`) → the write+read RELOCATE to item `""` and collide with the item-`""` cross-stream decoy → the cross-stream test (a) goes RED (the read returns the foreign item-`""` value) — SEC-01: the decoy MUST be seeded at item `""` (a bare-lookalike decoy does NOT bite; blanking the prefix relocates write+read together), re-run and confirm RED; **(d)-2** widen the store dedupe key with `monkeypatch.setattr(keying, "DEDUPE_FIELDS", keying.LINE_FIELDS)` (re-include `"value"`) → the dedupe-key-boundary test (c) goes RED — SEC-02: for the real composite DICT value the widened key is UNHASHABLE, so `store.append` raises `TypeError` at the SECOND append (before any `store.read`); a scalar value would instead persist a second line — assert the second append FAILS vs the NORMAL no-op, accepting EITHER a raised `TypeError` OR a second persisted line (do NOT assert `store.read` returns 2 for the composite). Patching `keying._DEDUPE_EXCLUDED` is INERT (`DEDUPE_FIELDS` is import-frozen); patch `DEDUPE_FIELDS` directly. A battery green under either mutation is tautological and FAILS this gate.
- **Risk (PF-S130-01 — tautological round-trip; the Wave-1 gap Tier-3 caught):** a round-trip test that passes whether or not the comprehensive model is used encodes broken behavior.
  - Gate condition: AC-1 (round-trip) + AC-3 (standing resolution) MUST run the REAL `plan_model` record → `store.append` → `plan_model` resolve path over a scratch store, with an input carrying the comprehensive-ONLY elements (integrated narrative + dated milestones + compiled monitoring config + ≥2 domains in ONE version). The **PF-S130-01 revert falsifier** (Step 2.5): reverting the model's record/resolve path to the thin `plan_schema.record_plan` / `resolve_plan` (single-domain thin `plan` dict, no narrative/milestones/config) drives `test_comprehensive_round_trip_production_path` (AC-1) AND `test_dated_comprehensive_plan_resolves_standing` (AC-3) RED — proving both distinguish the comprehensive model from the thin path (non-tautological). A test green under this revert FAILS the AC.
- **Risk (round-trip fidelity, full variant space — ADR-0044 falsification #1):** a combined "everything survives" assertion masks a single dropped element.
  - Gate condition: AC-2 is THREE INDEPENDENT falsifiers — `test_periodization_survives_round_trip`, `test_milestones_survive_round_trip`, `test_monitoring_config_survives_round_trip` — each asserting its element survives store→load and REDs when THAT element (alone) is dropped/flattened. Not one combined test.
- **Risk (frozen-guard reconciliation — a gate gap, ruling §4):** superseding `resolve_plan`/`record_plan` REDs the two prior record-spine byte-guards AND (via the `plan_model.py` create + `plan_schema.py` modify under `scripts/store/`) the two `scripts/store/` whole-dir freezes; carving them without a behavioral replacement opens the exact regression class they guard. The carve is the **Wave-2 frozen-guard reconciliation step's** (owned by neither task); the behavioral replacement is THIS task's deliverable.
  - Gate condition: the Wave-2 reconciliation removes ONLY `resolve_plan` + `record_plan` from each record-spine tuple (leaving `correct_plan` + `record_plan_tracking` + the `<always-frozen>` HARD set frozen) AND narrows each `scripts/store/` whole-dir freeze to exclude ONLY `plan_model.py`+`plan_schema.py` (keeping `store.py`/`keying.py`/`loop_schema.py`/… frozen); the behavioral replacement THIS task delivers is `test_plan_model.py` (round-trip fidelity + resolver + store-adversarial battery) + the per-ADR AC-4 numstat (bounds the `plan_schema.py` delta to the sanctioned surfaces). This task's Step-4 REGRESSION re-runs the carved guards GREEN. A reconciled guard that also un-freezes any store/engine primitive, or a missing behavioral replacement, FAILS this gate.
- **Risk (ADR-0044 Consequences-Negative-4 — larger stored plans / denser store, disposition #35):** NO gate — an accepted standing cost, documented. The comprehensive plan is a richer VALUE per version (the whole point); the store stays append-only local NDJSON, keyed unchanged. Recorded here so the spec's Risk-Mitigations field is fully covered, not silently dropped.

## TDD Steps

Single cycle. All six acceptance criteria are properties of ONE coherent unit — the `plan_model` module (composite object + record path + comprehensive resolver) + the `plan_schema.py` supersession — exercised over the same seeded scratch-store fixtures, so they are not split (task-planning: "all criteria test the same component"). RED writes every AC as a specific failing test BEFORE any GREEN; the category-(d) store-mutation FALSIFICATION and the PF-S130-01 revert falsifier run POST-GREEN (Step 2.5), because the store-keying / thin-path logic being mutated does not exist until GREEN.

### Step 1: RED — Write Failing Tests

Call `/write-tests` for `scripts/store/plan_model.py`, creating `tests/store/test_plan_model.py`, targeting each spec AC. Mirror `tests/store/test_correction.py` / `tests/store/test_plan_schema.py`: seed via the `plan_model` API (and direct `store.append` / `plan_schema.record_plan` for adversarial decoys) under a `tmp_path` root; build a per-item `store_read`/root closure; run each assertion over the REAL record→store→resolve path. ALL fixtures are SYNTHETIC (0 real operator PII, 0 live-API), on scratch stores.

**Shared fixture (a comprehensive plan version):** a `_comprehensive_version(...)` builder that assembles ONE composite object per plan version spanning ≥2 active domains (a training-domain program AND a compound-domain program, each a seven-field DOMAIN PROGRAM that `domain_program.validate` accepts) + an integrated narrative string + ≥1 dated milestone + a compiled monitoring config field + plan-level adjustment rules. This is the input that distinguishes the comprehensive model from the thin plan (the thin `plan_schema.record_plan` stores a single-domain `plan` dict with none of narrative/milestones/config).

- **AC-1 — Comprehensive round-trip (confirmation #1, PRODUCTION-PATH, PF-S130-01 load-bearing):** `test_comprehensive_round_trip_production_path` — record a `_comprehensive_version` (≥2 domains + narrative + milestones + monitoring config) via the REAL `plan_model` record entry through `store.append` under a `tmp_path` root; load it back via the REAL `plan_model` resolver/read entry from the SAME store; assert **missing-element count == 0** — every seven-field domain program field (for each of the ≥2 domains), the narrative, the milestones, and the monitoring config are present AND well-typed on load. The path is the real record→`store.append`→resolve (not a hand-built dict fed to the resolver).
- **AC-1b — Fail-closed rejection of a partial version (QA-04, MUST — mirrors 0041-T2's tested fail-closed AC-3):** `test_partial_version_rejected_fail_closed` — a version MISSING a domain program, OR the narrative, OR milestones, OR the monitoring config, OR carrying a NON-conformant per-domain program (one `domain_program.validate` rejects) → `validate_plan_version` / `record_plan_version` RAISES a typed error that NAMES the missing/non-conformant element, and the partial version is NOT silently stored (0 partial versions persisted). Parametrize over each element (≥1 case per required element + 1 non-conformant-program case). RED-capability PROVEN by stubbing the model-level required-element check to a no-op (`monkeypatch` the `validate_plan_version` model-level branch to return without raising) → the partial version records/stands → the test goes RED; observe RED, revert. WITHOUT this test a no-op `validate_plan_version` passes AC-1/AC-2/AC-3/AC-5/AC-6 (all seed CONFORMANT `_comprehensive_version` fixtures) — the exact untested-fail-closed class on a STORE-WRITE surface (a partial version silently stored then resolved "standing" surfaces a broken plan on the product's core surface).
- **AC-2 — Round-trip fidelity, FULL variant space (falsification #1):** THREE independent falsifiers over the real round-trip —
  - `test_periodization_survives_round_trip` — a plan whose prescription carries dated periodization blocks loses **0** blocks store→load (each dated block present + its dates intact); dropped/flattened → RED.
  - `test_milestones_survive_round_trip` — the dated milestones survive store→load with **0** dropped (count + dates intact); dropped/flattened → RED.
  - `test_monitoring_config_survives_round_trip` — the compiled monitoring config survives store→load intact (**0** config entries dropped); dropped/flattened → RED.
  - Each drops ONLY its element in the assertion focus, so a single missing element is caught independently (not masked by a combined "everything survives" check).
- **AC-3 — Standing resolution (confirmation #2, PRODUCTION-PATH load-bearing):** `test_dated_comprehensive_plan_resolves_standing` — record (real path) a `_comprehensive_version` dated the render date under a `tmp_path` root; the REAL comprehensive resolver over the stored readings resolves it as **standing** (the returned state carries the periodized program + narrative + milestones + config), NOT `NO_PLAN` / `NO_PLAN_TODAY` — assert the state is the standing state AND the standing payload carries the comprehensive elements. An absence state on a dated plan → RED.
- **AC-3b — Resolver absence branches (QA-05; mirror `plan_schema.resolve_plan`, `scripts/store/plan_schema.py:462-499`):** `test_resolve_comprehensive_absence_states` — (i) ZERO readings → the resolver returns the `NO_PLAN` analog; (ii) readings present but NONE dated `on_date` → the `NO_PLAN_TODAY` analog carrying `max(timepoint)` (the latest on-file date). This mirrors `plan_schema.resolve_plan`'s three-state vocabulary (`NO_PLAN` :484 / `NO_PLAN_TODAY` :494 carrying `max(r["timepoint"] for r in readings)` :497 / standing). RED-capable: a resolver that always returns standing (or omits `max(timepoint)` on the `NO_PLAN_TODAY` branch) REDs. (Read-path — ADR-0044-T2's mixed-history reader + ADR-0044-T4's confirm-hold both consume this absence shape, so it is a stated seam.)
- **AC-4 — Per-ADR freeze-break numstat (verbatim ADR threshold):** two probes.
  - *Frozen store primitive + engine (store.py/keying.py/pipeline.py/adjudicate.py == 0):* `test_frozen_store_primitive_numstat_zero` — `git diff --numstat PRE_TASK_HEAD -- scripts/plan/pipeline.py scripts/plan/adjudicate.py` prints nothing AND `git diff --numstat PRE_TASK_HEAD -- scripts/store/store.py scripts/store/keying.py` prints nothing (the value rides the primitive unchanged); AND `git diff --numstat origin/main -- scripts/store/store.py scripts/store/keying.py scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/router.py` prints nothing (the `<always-frozen>` 6-file HARD glob).
  - *plan_schema.py delta == exactly the four named surfaces:* `test_plan_schema_superseded_set_is_exactly_named` — read `origin/main:scripts/store/plan_schema.py` (git show) vs the current file; assert `correct_plan`, `record_plan_tracking`, `resolve_tracking`, `read_plan_tracking` are byte-IDENTICAL (`_func_src` AST equality — the NON-superseded functions untouched); assert `PLAN_DOMAINS`'s tuple literal is byte-identical (the constant survives; only its stored-KEY USE is rerouted). Failing-capable: an edit to any non-superseded plan_schema function reds it. (The superseded `record_plan`/`resolve_plan`/`read_plan`/validators DO change — they are NOT asserted frozen here; the two carved guards below stop asserting them frozen.)
  - *[base convention, QA-06]:* `PRE_TASK_HEAD` = the FIXED entry HEAD recorded at task entry (the per-ADR scoped delta base — isolates ONLY 0044-T1's own delta, since 0041-T2 + the Wave-2 reconciliation are already IN it); `origin/main` = the pre-build trunk (the always-frozen assertion). Diffing the working tree against `PRE_TASK_HEAD` is PRE-COMMIT-runnable + in-flow RED-capability-demonstrable (mirror `tests/plan/test_generate_plan.py:901`'s `PRE_TASK_HEAD` constant + 0041-T2's AC-4) — the freeze probes' RED-capability is shown by a throwaway edit to a frozen file (observe RED, revert), NOT deferred to a committed knowingly-RED test.
- **AC-5 — Store-adversarial battery (all four, per `docs/checklists/store-adversarial-tests.md`):**
  - (a) **cross-stream namespace collision:** `test_cross_stream_disjoint` — a `plan_model` resolve/read for the `plan-model::` stream never returns a thin `plan::<domain>` value (seed a thin `plan::workout` reading via `plan_schema.record_plan` as a decoy) AND never returns a sibling plan-version stream's value (distinct item files → disjoint). **SEC-01 fix — the (d)-1-biting decoy:** additionally seed a decoy reading at the EXACT item the blanked prefix resolves to — item `""` (file `.ndjson`, which `store.py:30-34` documents as an allowed empty item), dated the render date, carrying a DISTINCT foreign value; assert the `plan-model::` read returns ONLY the real comprehensive value and NEVER that item-`""` foreign value. NORMALLY (`_PREFIX_MODEL="plan-model::"`) the real stream is item `"plan-model::"`, disjoint from item `""`, so the item-`""` decoy is never returned (GREEN). This item-`""` decoy is what makes mutation (d)-1 BITE — SEC-01 (empirically confirmed): a bare-`plan-model`-lookalike decoy does NOT bite, because blanking `_PREFIX_MODEL` RELOCATES the whole plan-model stream (write AND read together) to item `""`, so without a decoy seeded AT item `""` the read still returns only the real value (GREEN — a non-falsifying mutation).
  - (b) **same-timepoint dedupe:** `test_same_timepoint_two_versions_persist` — two DISTINCT comprehensive plan versions recorded at ONE timepoint (distinct version-key source tags) both persist (`store.read` of the `plan-model::` item returns 2); an identical re-record of one version stays an idempotent no-op (append count unchanged, 0 duplicate lines).
  - (c) **dedupe-key boundary + no-new-keying:** `test_dedupe_key_boundary_and_no_new_keying` — (i) a grep/AST over `scripts/store/plan_model.py` asserts **0 new dedupe-key / keying logic** (no local `DEDUPE_FIELDS`/`dedupe_key`/`LINE_FIELDS` redefinition; identity stays `keying`'s frozen `(item, timepoint, source)`, reached only through `store.append`), count == 0; (ii) same `(item, timepoint, source)` + a CHANGED value is a store-dedupe no-op — the second write is DROPPED, the first value resolves, `store.read` returns 1 line (the value-excluded key; a re-record with a changed value is a no-op, never a silent overwrite — the `record_plan` idempotency contract).
  - (d) **mutation-style (run RED, observe, revert — Step 2.5):** name the two mutations + the battery test each turns RED — (d)-1 blank the `plan-model::` item-namespace prefix in `plan_model.py` (`_PREFIX_MODEL=""`) → the write+read RELOCATE to item `""`, colliding with the item-`""` cross-stream decoy → `test_cross_stream_disjoint` (a) goes RED (the read returns the foreign item-`""` value) — **SEC-01: the decoy MUST be seeded at item `""`** (a bare-`plan-model`-lookalike decoy does NOT bite — relocation moves write+read together); re-run and CONFIRM RED. (d)-2 widen the store dedupe key with `monkeypatch.setattr(keying, "DEDUPE_FIELDS", keying.LINE_FIELDS)` → `test_dedupe_key_boundary_and_no_new_keying` (c-ii) goes RED — **SEC-02: for the real composite DICT value the widened key is UNHASHABLE, so `store.append` raises `TypeError` at the SECOND append (before any `store.read`); a scalar value would instead persist a second line** — assert the second append FAILS vs the NORMAL no-op, accepting EITHER a raised `TypeError` OR a second persisted line (do NOT assert `store.read` returns 2 for the composite — that line is unreachable, the append raises first); both are RED. Then revert. **Patching `keying._DEDUPE_EXCLUDED` is INERT** — `DEDUPE_FIELDS` is import-frozen and `dedupe_key` reads it, so patch `DEDUPE_FIELDS` directly. A battery green under either mutation is tautological and FAILS AC-5.
- **AC-6 — Suite-local green:** `test_plan_model.py` passes against scratch stores — 0 live-API calls, 0 real operator PII (verified by the Step-1→Step-2 RED→GREEN transition + a `grep` that the test tree carries no real operator-identity literal).

Run: `.venv/bin/python -m pytest tests/store/test_plan_model.py -q`
Expected: FAIL — `scripts/store/plan_model.py` does not exist, so the module import fails. (If a NON-numstat test passes at this stage, the test is wrong — investigate before GREEN. The AC-4 freeze-numstat probes are the exception: based on `PRE_TASK_HEAD` (QA-06) they are pre-commit-runnable and GREEN whenever the frozen surfaces are untouched — their RED-capability is demonstrated by a throwaway frozen-file edit, not by the module RED→GREEN transition; re-confirmed at Step 4.)

### Step 2: GREEN — Implement

Changes to make (behavioral descriptions, NOT literal code):

- `scripts/store/plan_model.py` (Create):
  - **The comprehensive composite (satisfies AC-1/AC-2):** define the composite object contract — ONE object per plan version carrying, per active domain, the seven-field DOMAIN PROGRAM intact (each validated via `domain_program.validate` — reuse it, do NOT re-implement conformance), plus an integrated narrative, dated milestones (each with its date), a compiled monitoring config, and plan-level adjustment rules. Add a `validate_plan_version(version)`-style conformance that runs `domain_program.validate` per domain program and checks the model-level required elements (≥1 domain program, narrative, milestones, config present + well-typed) — fail-closed (a partial version raises a typed error naming the missing element), never a silent thin fallback.
  - **The record path (satisfies AC-1; rides the frozen primitive, disposition #14):** add a `record_plan_version(version, root)`-style entry that builds the reading via `loop_schema._reading(item, timepoint, source, value)` and appends through the UNCHANGED `store.append(item, reading, root)`. `item` = a NEW plan-model namespace prefix (e.g. `_PREFIX_MODEL = "plan-model::"`, disjoint from `plan::` / `plan-track::`); `timepoint` = the plan version's declared YYYY-MM-DD date (reuse `plan_schema._check_date`'s validation posture — a malformed date fails loud at the writer boundary, since date equality is the standing-resolution mechanism); `source` = the VERSION-KEY discriminator, derived as a content-tag over the composite value via `loop_schema._content_tag(_PREFIX_MODEL, version)` (so two DISTINCT versions at one timepoint get distinct sources and both persist, and an identical re-record hashes to the same tag → an idempotent `store.append` dedupe no-op — the `plan-track::` precedent); `value` = the composite object. The record path RIDES `store.append` unchanged: NO new store primitive, NO `store.correct` (append-only comprehensive versions; a revision is a new version), NO second dedupe key (identity stays `keying`'s `(item, timepoint, source)`).
  - **The comprehensive resolver (satisfies AC-3):** add a `resolve_comprehensive(readings, on_date)`-style pure function over `store.read`'s timepoint-sorted, append-order-stable readings (mirror `plan_schema.resolve_plan`'s reversed-scan-for-`on_date` latest-wins shape): the LAST reading in file (append) order dated `on_date` wins → a STANDING state carrying the composite object; zero readings → the `NO_PLAN` analog; readings none dated `on_date` → the `NO_PLAN_TODAY` analog carrying the latest on-file date. Add a `read_plan_version(on_date, root)`-style public entry that `store.read`s the `plan-model::` item and resolves (the `read_plan` shape). A dated comprehensive plan resolves standing carrying the periodized program + narrative + milestones + config (NOT an absence state).
  - **Import surface:** `scripts.store.store` (append/read), `scripts.store.loop_schema` (`_reading`, `_content_tag`), `scripts.plan.domain_program` (validate + constants) — all READ-only reuse. Do NOT import or redefine `keying` internals (the identity is reached only through `store.append`); do NOT edit `store.py` / `keying.py`.
- `scripts/store/plan_schema.py` (Modify — the four named surfaces, disjoint from 0046-T1's tuple-growth region): supersede the four `_validate_*_plan` validators + their `_PLAN_VALIDATORS` / `_check_plan_args` dispatch, `record_plan` → `store.append`, `resolve_plan`, and `PLAN_DOMAINS`'s stored-plan-domain-KEY role — routed to `plan_model`. Re-point the public `record_plan` / `resolve_plan` / `read_plan` entries to `plan_model`'s record/resolve (delegating, or retired with callers re-pointed — the executor's choice, bounded by AC-4's "exactly the four named surfaces"). `PLAN_DOMAINS` the TUPLE stays (its surviving dispatch-roster-registry role — RT-009; grown by ADR-0046-T1 in Wave 3); `correct_plan`, `record_plan_tracking`, `resolve_tracking`, `read_plan_tracking` stay BYTE-UNCHANGED (not superseded by 0044-T1). Do NOT touch the tracking streams or the `PLAN_DOMAINS` tuple literal.
- The `tests/plan/*` + `tests/runner/*` frozen-guard carve-outs are NOT this task's manifest — the two record-spine tuples (`test_confirm_pointer_skip.py:236`, `test_generate_plan.py:1014`, each narrowed to `("correct_plan", "record_plan_tracking")`) AND the two `scripts/store/` whole-dir freezes (`test_cadence_runner.py:297`, `tests/runner/test_store_concurrency.py:297`, each narrowed to a frozen-store set MINUS `plan_model.py`+`plan_schema.py`) are carved by the **Wave-2 frozen-guard reconciliation step** (build plan Wave 2, owned by neither task — Architect Option-(A) arbitration `docs/task-plan/.pipeline/review-architect.md` §3), which runs BEFORE this task's commit. This task edits ONLY `plan_model.py` (Create) + `plan_schema.py` (Modify) + `test_plan_model.py` (Create); its Step-4 regression re-runs the carved guards GREEN.

Run: `.venv/bin/python -m pytest tests/store/test_plan_model.py -q`
Expected: PASS (the AC-4 `PRE_TASK_HEAD`-based freeze-numstat probes are pre-commit-runnable and GREEN here — the frozen primitives are untouched; re-confirmed at Step 4).

### Step 2.5: Falsify the store-adversarial battery + PF-S130-01 (POST-GREEN mutation-RED / revert)

The store-keying + thin-path logic the falsifiers target now EXISTS (GREEN landed), so they are meaningful here (at Step 1 every test fails on import — nothing to mutate). For EACH: apply, run the named test, OBSERVE RED, then REVERT (never committed). A test that stays green under its falsifier is tautological and FAILS its AC.

- **(d)-1 — blank the plan-model prefix (SEC-01):** set `_PREFIX_MODEL = ""` in `plan_model.py` → the write+read RELOCATE to item `""`, colliding with the item-`""` cross-stream decoy → `test_cross_stream_disjoint` (AC-5a) goes RED (the read returns the foreign item-`""` value). Re-run and CONFIRM RED — the decoy MUST be seeded at item `""` (a bare-`plan-model`-lookalike decoy does NOT falsify: blanking the prefix moves write+read together, so the read still returns only the real value = GREEN). Revert.
- **(d)-2 — widen the store dedupe key (SEC-02):** in the test, `monkeypatch.setattr(keying, "DEDUPE_FIELDS", keying.LINE_FIELDS)` → `test_dedupe_key_boundary_and_no_new_keying` (AC-5c-ii) goes RED. For the REAL composite DICT value the widened key is UNHASHABLE → `store.append` raises `TypeError: unhashable type: 'dict'` at the SECOND append (before any `store.read`); a scalar value would instead persist a second line. Assert the CHANGED-value re-record FAILS vs the NORMAL no-op — accept EITHER a raised `TypeError` at append OR a second persisted line; do NOT assert `store.read` returns 2 for the composite (unreachable — the append raises first). Both are RED (pytest counts the error as a failure). Revert. (Patch `DEDUPE_FIELDS` directly; `_DEDUPE_EXCLUDED` is INERT.)
- **(pf-s130) — revert the model to the thin path:** make the model's record/resolve delegate to `plan_schema.record_plan` / `resolve_plan` (the single-domain thin `plan` dict, no narrative/milestones/config) → BOTH `test_comprehensive_round_trip_production_path` (AC-1: the comprehensive elements are absent on load) AND `test_dated_comprehensive_plan_resolves_standing` (AC-3: the standing payload lacks narrative/milestones/config, or resolves absent on a multi-domain version the thin path can't key) go RED — proving neither is tautological (both distinguish comprehensive from thin). Revert.
- **(fail-closed, QA-04) — stub the model-level check to a no-op:** `monkeypatch` `validate_plan_version`'s model-level required-element branch to return without raising → the partial version records/stands → `test_partial_version_rejected_fail_closed` (AC-1b) goes RED — proving the fail-closed rejection is non-tautological (a no-op `validate_plan_version` would otherwise pass every conformant-fixture AC). Revert.

Run after each revert: `.venv/bin/python -m pytest tests/store/test_plan_model.py -q`
Expected: PASS (every falsifier reverted; the suite is green again). Confirm NO falsifier edit remains in the working tree before Step 3.

### Step 3: REFACTOR

Review for:
- Duplication between `resolve_comprehensive` and `plan_schema.resolve_plan`'s reversed-scan resolution — the comprehensive resolver reuses the SHAPE (reversed scan, latest-wins-per-`on_date`, the `NO_PLAN`/`NO_PLAN_TODAY` state vocabulary) but over the composite value; do not carry a second copy of the store I/O (append/read reached only through `store` / `loop_schema`).
- The record path builds the reading via `loop_schema._reading` (not a hand-rolled 4-field dict) and derives the source via `loop_schema._content_tag` (not a second content-hash) — a divergent copy is the store-keying escape vector. No local `keying` redefinition.
- Naming consistency of the `plan-model::` prefix + the record/resolve/read entry names + the composite field contract with the Interface Contracts below (0044-T2's reader extends the resolver; 0043-T3 stores via this model; 0045-T1's config lives in it) — the names are the stable seam.
- Internal dead code introduced during GREEN (first-time module) — remove any.
- Confirm the `plan_schema.py` edit touched ONLY the four named surfaces (validators + `record_plan` + `resolve_plan` + the `read_plan` stored-key reroute); `correct_plan` / `record_plan_tracking` / the tracking resolvers / the `PLAN_DOMAINS` tuple literal are byte-unchanged.

Run: `.venv/bin/python -m pytest tests/store/test_plan_model.py -q`
Expected: PASS (no behavior change).

### Step 4: REGRESSION

Run: `.venv/bin/python -m pytest -q`
Expected: PASS at the baseline floor — `2 failed, 2529 passed, 7 skipped` PLUS the new `test_plan_model.py` tests passing, with NO new failure beyond the two known env-sensitive `test_bind`/`test_server` reds. Specifically confirm the reconciled guards are GREEN (the behavioral replacement holds — they are carved by the Wave-2 reconciliation step, NOT this task): `.venv/bin/python -m pytest tests/plan/test_confirm_pointer_skip.py::test_frozen_spine_numstat_and_read_plan_only_hunk tests/plan/test_generate_plan.py::test_record_plan_byte_unchanged_vs_pretask tests/runner/test_cadence_runner.py::test_frozen_glob_numstat_empty tests/runner/test_store_concurrency.py::test_no_new_store_stream -q` passes (the record-spine tuples now freeze `correct_plan` + `record_plan_tracking` only; the `scripts/store/` whole-dir freezes now exclude `plan_model.py`+`plan_schema.py`). A 3rd unexpected failure means the `plan_schema.py` supersession reached outside the manifest or a shared-fixture touch had a side effect — do NOT fix unrelated tests; investigate whether the change reached outside the manifest and flag per the feedback protocol.

Then re-check the frozen spine + the per-ADR numstat (based on `PRE_TASK_HEAD`, the fixed entry HEAD — pre-commit-runnable per QA-06, re-confirmed here against the final working tree/commit):
- `git diff --numstat PRE_TASK_HEAD -- scripts/plan/pipeline.py scripts/plan/adjudicate.py` → empty AND `git diff --numstat PRE_TASK_HEAD -- scripts/store/store.py scripts/store/keying.py` → empty (the value rides the primitive; AC-4).
- `git diff --numstat origin/main -- scripts/store/store.py scripts/store/keying.py scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/router.py` → empty (the `<always-frozen>` 6-file HARD glob).
- `git diff --name-only PRE_TASK_HEAD -- scripts/ tests/` → exactly `scripts/store/plan_model.py`, `scripts/store/plan_schema.py`, `tests/store/test_plan_model.py` (no scope creep — the guard carve-outs are the Wave-2 reconciliation commit's, not this task's).
- A `_func_src` AST check confirms `plan_schema.py`'s `correct_plan` / `record_plan_tracking` / `resolve_tracking` / `read_plan_tracking` + the `PLAN_DOMAINS` tuple are byte-identical to `origin/main` (0 unnamed frozen edits within `plan_schema.py`).

## Interface Contracts

This task CREATES the `plan_model` comprehensive storage contract — the stable seam every downstream comprehensive-plan task consumes.

- `plan_model.record_plan_version(version, root) -> None` — validates the composite (per-domain `domain_program.validate` + the model-level required-element check) and appends ONE reading to item `f"{_PREFIX_MODEL}"` (a fixed `plan-model::` namespace), timepoint = the version's declared date, source = the content-tag version key, value = the composite, via the UNCHANGED `store.append`. Inherits the store dedupe: two distinct versions at one timepoint both persist (distinct version keys); an identical re-record is an idempotent no-op; a CHANGED value at the same `(item, timepoint, source)` is a store-dedupe no-op (never a silent overwrite). Consumed by **ADR-0043-T3** (the orchestrator synthesizes ONE integrated plan + milestones and stores it via this entry) and, transitively, **ADR-0045-T3** (a Tier-1 auto-apply records a bounded adjustment as a version).
- `plan_model.resolve_comprehensive(readings, on_date) -> dict` — pure over `store.read`'s timepoint-sorted readings; the LAST version dated `on_date` resolves STANDING carrying the composite (periodized program + narrative + milestones + config); zero readings → `NO_PLAN`; none dated `on_date` → `NO_PLAN_TODAY` with the latest on-file date. Consumed by **ADR-0044-T2** (the mixed-history reader EXTENDS this resolver to also read pre-existing thin `plan::<domain>` readings, comprehensive-wins) and **ADR-0044-T4** (the confirm-hold skip re-bases on this resolver — an unconfirmed large-change version resolves not-standing).
- `plan_model.read_plan_version(on_date, root) -> dict` — the public `store.read` + `resolve_comprehensive` wrapper (the `read_plan` shape). Consumed by **ADR-0044-T3** (`horizons.window_block` re-sources the first-class periodization from the comprehensive model) and **ADR-0044-T2** (`track.resolve_plan_progress` re-points onto the mixed-history reader).
- The composite object contract: per active domain the seven-field DOMAIN PROGRAM intact (ADR-0041-T1's shape) + integrated narrative + dated milestones + compiled monitoring config (the ADR-0045-T1 `monitoring_compiler` output lives HERE — a field of the model, not a side stream) + plan-level adjustment rules. **ADR-0045-T1**'s compiled config round-trips through this model.
- **Behavioral invariants (the store-surface + frozen-spine guarantees):** the record path rides the UNCHANGED `store.append` / `keying` `(item, timepoint, source)` identity (the value grows, the key does not — disposition #14); `plan_model.py` defines NO second dedupe key; the `plan-model::` stream is namespace-disjoint from `plan::` / `plan-track::` (0 cross-stream reads); the store WRITE primitive + inner engine (`store.py`/`keying.py`/`pipeline.py`/`adjudicate.py`) stay byte-frozen.
- **Change control:** ADR-0044-T2/T3/T4, ADR-0043-T3, ADR-0045-T1/T3 MUST NOT change the record/resolve/read signatures, the composite contract, or the `plan-model::` identity without Architect + Security review (this task carries Architect — the storage contract — AND Security — the store record surface + the ride-the-frozen-primitive property).

## Verification Checklist

- [ ] AC-1: comprehensive round-trip is a PRODUCTION-PATH test (real record→`store.append`→resolve), missing-element count == 0 with narrative + milestones + config + ≥2 domains present — `test_comprehensive_round_trip_production_path`; Step-2.5 revert-to-thin turns it RED (PF-S130-01 non-tautology), reverted.
- [ ] AC-1b (QA-04, MUST): a partial version (missing a domain program / narrative / milestones / config, OR a non-conformant per-domain program) → `validate_plan_version`/`record_plan_version` raises a typed error NAMING the missing element; 0 partial versions persisted — `test_partial_version_rejected_fail_closed`; RED-capability proven by stubbing the model-level check to a no-op (observe RED, revert).
- [ ] AC-2: round-trip fidelity across the FULL variant space — periodization, milestones, monitoring config each survive independently (0 dropped) — `test_periodization_survives_round_trip` + `test_milestones_survive_round_trip` + `test_monitoring_config_survives_round_trip`.
- [ ] AC-3: a dated comprehensive plan resolves STANDING (not `NO_PLAN`/`NO_PLAN_TODAY`) carrying the comprehensive elements, via the real resolver — `test_dated_comprehensive_plan_resolves_standing`; Step-2.5 revert-to-thin turns it RED, reverted.
- [ ] AC-3b (QA-05): resolver absence branches — zero readings → `NO_PLAN`; readings none dated `on_date` → `NO_PLAN_TODAY` carrying `max(timepoint)` — `test_resolve_comprehensive_absence_states` (mirrors `plan_schema.resolve_plan`).
- [ ] AC-4: per-ADR numstat — `pipeline.py`/`adjudicate.py` == 0 AND `store.py`/`keying.py` == 0 (value rides the primitive) + the `<always-frozen>` 6-file glob == 0 vs origin/main + the `plan_schema.py` delta == exactly the four named surfaces (`correct_plan`/`record_plan_tracking`/`resolve_tracking`/`read_plan_tracking` + the `PLAN_DOMAINS` tuple byte-identical), 0 unnamed frozen edits — `test_frozen_store_primitive_numstat_zero` + `test_plan_schema_superseded_set_is_exactly_named` (based on `PRE_TASK_HEAD`/`origin/main` — pre-commit-runnable, QA-06).
- [ ] AC-5: four-part store-adversarial battery — (a) cross-stream disjoint (incl. the SEC-01 item-`""` decoy), (b) same-timepoint two versions persist + idempotent re-record, (c) dedupe-key boundary + 0 new keying (grep) + changed-value-is-a-no-op, (d) run RED under both named mutations then reverted — `test_cross_stream_disjoint` + `test_same_timepoint_two_versions_persist` + `test_dedupe_key_boundary_and_no_new_keying` + the documented (d)-1 (blank prefix → write+read relocate to item `""`, collide with the item-`""` decoy → (a) RED — SEC-01) / (d)-2 (`DEDUPE_FIELDS`=`LINE_FIELDS` → (c) RED: the composite dict makes the key unhashable → `TypeError` at the 2nd append, OR a scalar persists a 2nd line — SEC-02, accept either vs the normal no-op) procedure.
- [ ] AC-6: `.venv/bin/python -m pytest tests/store/test_plan_model.py` passes (0 live-API, 0 real PII — a `grep` confirms no real operator-identity literal in the test tree).
- [ ] Risk (ADR-0044 Neg-1 record-spine freeze break / tension #27) covered by AC-4; Risk (S37–S41 store-keying escape, bead `pka`) covered by AC-5 (all four, (d) run RED); Risk (PF-S130-01 tautological round-trip) covered by AC-1/AC-3 production-path + the revert falsifier; Risk (fidelity variant space) covered by AC-2's three independent falsifiers.
- [ ] Frozen-guard reconciliation (ruling §4/§5 — performed by the Wave-2 reconciliation step, NOT this task's manifest): the two record-spine guards carved to `("correct_plan", "record_plan_tracking")` ONLY + the two `scripts/store/` whole-dir freezes narrowed to exclude ONLY `plan_model.py`+`plan_schema.py`, all GREEN post-carve; this task's behavioral-replacement deliverable (`test_plan_model.py` + the per-ADR AC-4 numstat) present; the `<always-frozen>` HARD set + `correct_plan`/`record_plan_tracking` + `store.py`/`keying.py`/`loop_schema.py` still frozen in every reconciled guard.
- [ ] No regression: `.venv/bin/python -m pytest -q` holds the `2 failed, 2529 passed, 7 skipped` floor (no 3rd unexpected failure) with the new tests passing.
- [ ] Files modified match the 3-file manifest (`scripts/store/plan_model.py`, `scripts/store/plan_schema.py`, `tests/store/test_plan_model.py`) — the guard carve-outs are the Wave-2 reconciliation commit's, NOT this task's; no other scope creep; any Step-2.5 falsifier edit is REVERTED, never committed; `store.py`/`keying.py`/`pipeline.py`/`adjudicate.py` byte-unchanged.
- [ ] Interface contract documented (the `plan_model` record/resolve/read seam + the composite contract + the ride-the-frozen-primitive invariant + the 0044-T2/T3/T4, 0043-T3, 0045-T1/T3 consumers).

## Deviations

| # | Spec Says | Recipe Says | Reason | Impact |
|---|----------|------------|--------|--------|
| 1 | The sibling Wave-1 recipe (`adr-0042-t1.md`) cites `source-build-plan: docs/build-plan/build-plan-v1-full.md`. | This recipe cites `source-build-plan: docs/build-plan/build-plan-adr-0041-0046.md`. | The load-bearing wave-2 schedule, the invariant-1 frozen-guard amendment, and the Wave-2→3 checkpoint that gate THIS task live in `build-plan-adr-0041-0046.md`, not the original V1 umbrella plan. Citing the accurate source is the faithful traceability. | Traceability accuracy only; no behavioral impact. |

*(The prior Deviation #1 — this task carving the two `plan_schema.py` record-spine byte-guards into its own manifest — was REMOVED under the Architect Option-(A) arbitration (`docs/task-plan/.pipeline/review-architect.md` §3): the guard carve-outs are owned by the single Wave-2 frozen-guard reconciliation commit, not either Wave-2 task's manifest. This recipe's manifest is now exactly the spec's three files — no manifest-expansion deviation remains.)*

## Commit

```
feat(store): comprehensive Plan Model + resolver
```
Stage only: `scripts/store/plan_model.py`, `scripts/store/plan_schema.py`, `tests/store/test_plan_model.py` (the prior-feature guard carve-outs — the two record-spine tuples + the two `scripts/store/` whole-dir freezes — are the Wave-2 frozen-guard reconciliation commit's, owned by neither task; this task stages NO guard file).

(Stage with `git add <the three paths>` as its own command, then `git commit` separately — the PII commit hook denies single-call stage+commit, `git commit -a`, and pathspec `git commit <path>`. Confirm NO Step-2.5 falsifier edit — to the `plan-model::` prefix, the dedupe key, or the thin-path delegation — and NO daemon vault-churn / parked `??` file remains staged, and `store.py`/`keying.py`/`pipeline.py`/`adjudicate.py` are byte-unchanged, before committing. The per-ADR AC-4 numstat probes (`git diff --numstat PRE_TASK_HEAD …`, the fixed entry HEAD) are pre-commit-runnable and re-confirmed against the final commit.)

## Rollback

If verification fails after 3 fix cycles:
1. `git stash -m "failed-ADR-0044-T1-attempt-{N}"` — preserve the work for diagnosis.
2. `git checkout {commit-before-task}` — revert to the entry-state HEAD recorded in Entry State (also reverts any un-reverted Step-2.5 falsifier edit + any un-reverted `store.py`/`keying.py` dedupe-widen mutation).
3. Update the issue tracker: flag ADR-0044-T1 as BLOCKED with the failure log.
4. Escalate: "Task ADR-0044-T1 failed verification after 3 attempts. Stash ref: {ref}. Failure log: {summary}." Because ADR-0044-T1 is the Wave-2 comprehensive-model foundation that ADR-0044-T2/T3/T4, ADR-0043-T3, and ADR-0045-T1/T3 ALL depend on (the storage seam every consumer reads), a BLOCK halts the comprehensive-plan build at the record spine — surface it immediately, do not start any downstream task.
5. Stash cleanup: after abandonment or re-planning, drop all `failed-ADR-0044-T1` entries from `git stash list`.

Do NOT: silently skip a failing acceptance criterion; weaken a test to make it pass (especially the AC-5 store-adversarial mutation-RED battery, the AC-4 per-ADR numstat, or the PF-S130-01 production-path round-trip — these ARE the store-surface + frozen-spine deliverable); edit any frozen guard here (the record-spine + `scripts/store/` carve-outs are the Wave-2 reconciliation commit's, not this task's — this task stages NO guard file); or proceed to any downstream task with the comprehensive model in a broken state.
