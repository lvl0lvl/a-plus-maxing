---
task-id: ADR-0041-T2
source-spec: docs/spec/adr-0041-0046-comprehensive-plan-spec.md
source-build-plan: docs/build-plan/build-plan-adr-0041-0046.md
wave: 2
assigned-agent: SE
reviewers: [QA, Architect]
created: 2026-07-11
status: approved
---

# Recipe: Transitional Additive Adapter — Migrate the Four Translators + `assemble._is_complete` to the Uniform DOMAIN PROGRAM (FROZEN-SPINE)

> Wave-2 task. It SUPERSEDES the four thin translators `_to_workout/nutrition/supplements/peptides_plan`
> + `_PLAN_TRANSLATORS` (`generate_plan.py:123-291`) and `assemble._is_complete` + `_compose_claim`
> (`assemble.py:116-124,250-275`) so they accept/emit the uniform seven-field DOMAIN PROGRAM
> (`domain_program.py`, ADR-0041-T1, on main) via a TRANSITIONAL ADDITIVE ADAPTER — both the legacy
> thin payload AND the full uniform program valid during migration. The adapter lifts today's `payload`
> into the program's `prescription` while the other six fields ramp; it is bounded by the fail-closed
> `domain_program.validate` — a program that cannot be made conformant is REJECTED at the collection
> boundary, never a silent thin fallback. The migrated `_is_complete` supersedes the thin
> `source/confidence_tier/reversibility` check with seven-field completeness. The seven-field program
> rides the FROZEN `plan_schema.record_plan` → `store.append` ADDITIVELY (the ADR-0006/0010
> "closed on required fields, OPEN ON EXTRAS" seam, `plan_schema.py:33-36,113-139`) — the thin
> renderable shape still records so `record_plan`'s frozen validator accepts it, and the seven fields
> ride along as extras so the round-trip drops 0 of 7. ADR-0044-T1 (Wave 2, disjoint file) later
> supersedes `record_plan` to store the program first-class. This task is FROZEN-SPINE: the HARD-frozen
> four (`store.py`/`keying.py`/`pipeline.py`/`adjudicate.py`) + `adjust.py`/`router.py` stay byte-frozen;
> the per-ADR numstat (AC-4) proves the break is bounded to `generate_plan.py`+`assemble.py`; and — because
> `assemble.py` is byte-frozen by prior-feature guards — the `assemble.py` scoped carve-out is performed by
> the **Wave-2 frozen-guard reconciliation step** (build plan Wave 2, owned by neither Wave-2 task — the
> Architect Option-(A) arbitration `docs/task-plan/.pipeline/review-architect.md` §3), NOT this task's
> manifest; this task's Step-4 regression runs AFTER the reconciliation, so the `assemble.py` guards are
> already carved. Two load-bearing safety deliverables:
> (1) FAIL-CLOSED — a non-liftable output never becomes a plan (AC-3); (2) NON-TAUTOLOGICAL round-trip —
> AC-1 drives the PRODUCTION path with a distinguishing seven-field input that REDs on a revert of the
> migration (PF-S130-01, the exact gap Tier-3 caught in Wave 1).

## Entry State

**Prerequisites** (ADR-0041-T2 is a Wave-2 task — the spec Dependency Map lists ADR-0041-T1 + ADR-0042-T1 as predecessors, BOTH merged to `main`):

- [ ] Predecessor **ADR-0041-T1 complete on `main`** (the schema + validator the migrated code emits against): `scripts/plan/domain_program.py` present + `grep -q "def validate" scripts/plan/domain_program.py && grep -q "DOMAIN_KINDS" scripts/plan/domain_program.py && grep -q "VALIDITY_TIERS" scripts/plan/domain_program.py && echo OK`.
- [ ] Predecessor **ADR-0042-T1 complete on `main`** (the `:356` assembler swap this task rebases on — disjoint region): `grep -q "context_assembler.assemble_context" scripts/plan/generate_plan.py && echo OK` (the swap is already in `origin/main`; the translator migration is a DIFFERENT region of `generate_plan.py`).
- [ ] Full test suite at the baseline floor: `.venv/bin/python -m pytest -q` reports **`2 failed, 2529 passed, 7 skipped`** (Wave 1 merged: this floor SUPERSEDES the build plan's pre-Wave-1 `2 failed, 2489 passed` figure — the +40 are the merged `test_domain_program.py` + `test_context_assembler.py` cases). The two failures (`tests/serve/test_bind.py::test_port_collision_exits_nonzero_fail_loud`, `tests/serve/test_server.py::test_main_reads_no_stdin`) are PRE-EXISTING env-sensitive OS-port/stdin cases, NOT this task's to fix; the gate is **no-NEW-failure**, a 3rd distinct failure is a regression → HALT.
- [ ] Working tree "clean" — **NOTE the daemon caveat:** a bare `git status` is NOT empty (the basic-memory daemon perpetually re-churns `vault/**/*.md` frontmatter, and parked untracked files exist — `vault/library/genetics/*.md`, `docs/**/.verify-*.md`). "Clean" for THIS task means: (a) **no STAGED changes** (`git diff --cached --name-only` prints nothing); (b) the Create target ABSENT (below); (c) the frozen numstat EMPTY (below). Do NOT `git restore vault/` or try to zero `git status` — the daemon re-churns within ~1s (CLAUDE.md vault hygiene).
- [ ] On a feature branch cut off `main`: `feature/comprehensive-plan-wave-2` (the current Wave-2 build branch, HEAD == `main`) or an equivalent `feature/*` — never `main`.
- [ ] Rollback target recorded: `git rev-parse HEAD` at task entry — the `{commit-before-task}` the Rollback Protocol reverts to AND the `PRE_TASK_HEAD` base the AC-4 per-ADR numstat probe diffs against (mirror `tests/plan/test_generate_plan.py:901`'s `PRE_TASK_HEAD` constant pattern).
- [ ] The Create target is ABSENT (Create-only, no stale/duplicate artifact): `test -e tests/plan/test_generate_plan_uniform.py && echo "PRESENT (stop)" || echo "ABSENT (Create OK)"`.
- [ ] **Per-ADR-scoped frozen numstat — entry check (`origin/main` base; everything EMPTY at entry because nothing is edited yet).** The HARD-frozen four + `adjust.py`/`router.py` (`<always-frozen>`) AND the AC-4 sibling-superseded surfaces are all byte-identical to `origin/main`, AND `generate_plan.py`+`assemble.py` are byte-identical to `origin/main` at entry (Wave-1's `:356` swap is already IN `origin/main`; THIS task's supersession is scoped by the AC-4 probe to `PRE_TASK_HEAD`, not `origin/main`): `git diff --numstat origin/main -- scripts/store/store.py scripts/store/keying.py scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/router.py scripts/store/plan_schema.py scripts/plan/orchestrate.py scripts/plan/generate_plan.py scripts/plan/assemble.py` prints nothing (verified EMPTY 2026-07-11).
- [ ] **`assemble.py` byte-guard inventory (carved by the Wave-2 reconciliation step, NOT this task's manifest).** `assemble.py` is byte-frozen by prior-feature guards: `[ "$(rg -l 'scripts/plan/assemble.py|(REPO_ROOT / .scripts. / .plan.).glob' tests/serve/test_route.py tests/serve/test_pdf_ingestion_e2e.py tests/serve/test_intake_onboarding_e2e.py tests/genetics/test_dna_aware_plan_e2e.py tests/serve/test_plan_loop_hold.py tests/plan/test_confirm_pointer_skip.py tests/runner/test_cadence_runner.py | wc -l | tr -d ' ')" = "7" ] && echo "≥7 guards freeze assemble.py — carved by the Wave-2 frozen-guard reconciliation step (build plan Wave 2)"`. NOTE this static grep counts 7; the reconciliation enumerates the FULL set by executing the suite (F-011), catching guards this grep misses — notably **G8 `tests/plan/test_generate_plan.py::test_assemble_byte_unchanged_vs_pretask` (:990)** (a single-file byte assertion, not a glob/list). `generate_plan.py` is ALREADY carved out of all 7 (Wave-1 reconciliation, on `main`). This task's contribution to the behavioral replacement (ruling §4) is `tests/plan/test_assemble.py` staying green + the new `tests/plan/test_generate_plan_uniform.py`; confirm `tests/plan/test_assemble.py` PRESENT (a carve-out without a behavioral replacement is a gate gap → HALT).

## Context Load List

**Files to read (this task's file manifest + the one existing convention it mirrors + the frozen record path it rides):**

- [`scripts/plan/generate_plan.py:123-291`](../../scripts/plan/generate_plan.py) — the FOUR translators (`_to_workout_plan` 124-154, `_to_nutrition_plan` 160-204 AGGREGATES, `_to_supplements_plan` 207-233, `_to_peptides_plan` 236-258 single-compound) + `_PLAN_TRANSLATORS` (287-292) the migration supersedes. Also `compute_plan` (304) + `generate_plan` (408) — the PRODUCTION callers AC-1/AC-3 drive; note the workout **clearance gate** (`load` dropped unless `clearance_granted`, 149-150 — a LOAD-BEARING safety gate to PRESERVE), the nutrition aggregation, the peptides single-compound rule, and the `_FixedEnvelopeClient`/`_FixtureBackend` seam (`_self_test` 459-542) the AC-1 fixture reuses at 0 live spend.
- [`scripts/plan/assemble.py:116-124,250-275`](../../scripts/plan/assemble.py) — `_is_complete` (116-124, the thin `source`+`confidence_tier`+`reversibility` + per-number `units`+`reference_range` check the seven-field completeness supersedes) + `_compose_claim` (250-275, runs the four filters; the sourcing filter drops incomplete recs — the drop-behavior to PRESERVE). Note `assemble` imports `re` + stdlib only today; it must NOT import `generate_plan` (cycle — `generate_plan` imports `assemble`), so the shared conformance check is `domain_program.validate` (leaf, importable by both).
- [`scripts/plan/domain_program.py`](../../scripts/plan/domain_program.py) — `validate(program) -> None|raises DomainProgramError` + `missing_required_fields` + the seven-field constants (`REQUIRED_FIELDS`, `CONDITIONAL_FIELDS`), `KIND_FIELD`/`DOMAIN_KINDS`/`DOMAIN_KIND_RULES` (training vs compound), `SIGNAL_TIER_KEY`/`VALIDITY_TIERS` — the contract the migrated code emits/validates against (ADR-0041-T1, on `main`; change-control frozen — do NOT edit it).
- [`scripts/store/plan_schema.py:33-36,113-139,371-396`](../../scripts/store/plan_schema.py) — **READ-ONLY** (frozen at 0041-T2, an AC-4 forbidden surface): confirm `record_plan` (371-396) stores the WHOLE `plan` value via `store.append`, and `_check_fields` (113-139) is "closed on required fields, OPEN ON EXTRAS: unknown extra keys are permitted and ignored" (module docstring 33-36). This is WHY the seven-field program rides the frozen record path additively + round-trips (AC-1). Do NOT edit — the migration adds NO line here.
- [`tests/plan/test_assemble.py:210-251,370-378`](../../tests/plan/test_assemble.py) — the behavioral guard to PRESERVE (the `assemble.py` carve-out's ruling-§4 replacement): `_count_incomplete` (210-216) calls production `assemble_mod._is_complete` DIRECTLY (one source of truth); `test_recommendations_fully_sourced` (219-251) + `test_specialist_all_recs_incomplete_renders_gap` (370-378) assert a rec missing `reversibility` (or a number missing `reference_range`) is DROPPED. These are the REGRESSION proof that the migrated completeness gate does NOT silently loosen drop-semantics. Also the fixture convention (`_rec(...)`, `_specialist(...)`, `_summary(...)`) to mirror.
- [`tests/plan/test_generate_plan.py:901,980-1010`](../../tests/plan/test_generate_plan.py) — the `compute_plan`/`generate_plan` behavioral guard (25+ tests; also the carve-out's replacement) + the `PRE_TASK_HEAD` per-task-scoped `git diff --numstat` pattern to MIRROR for the AC-4 probe (record HEAD at entry, diff `<PRE_TASK_HEAD> -- <path>`).
- [`docs/adr/.pipeline/frozen-guard-reconciliation-ruling.md`](../../docs/adr/.pipeline/frozen-guard-reconciliation-ruling.md) **§2 (incl. the §2 addendum) + §4 + §5** — the BINDING carve-out authority (context for WHY the Wave-2 reconciliation exists; this task does NOT perform the carve): §2 + its Wave-2 per-surface addendum enumerate the `assemble.py` EIGHT-guard set (G1–G8); §4 the behavioral-replacement-guard requirement; §5 the standing per-wave carve-out rule. The per-guard EDITS are the Wave-2 frozen-guard reconciliation step's manifest (build plan Wave 2), NOT this task's — read the ruling to understand the freeze, do NOT edit the guard files here.
- The guard files that freeze `assemble.py` are carved by the **Wave-2 frozen-guard reconciliation step**, not this task (`tests/serve/test_route.py`, `tests/serve/test_pdf_ingestion_e2e.py`, `tests/serve/test_intake_onboarding_e2e.py`, `tests/genetics/test_dna_aware_plan_e2e.py`, `tests/serve/test_plan_loop_hold.py`, `tests/plan/test_confirm_pointer_skip.py`, `tests/runner/test_cadence_runner.py`, and G8 `tests/plan/test_generate_plan.py::test_assemble_byte_unchanged_vs_pretask`). This task's Step-4 regression re-runs them GREEN (already carved) — it does NOT edit them.

**Do NOT load:**
- `docs/adr/ADR-0041*.md` … `ADR-0046*.md` — the spec already distilled these; re-reading re-opens resolved trade-offs (OQ-2 migration-sequencing disposition #2), TP-02 drift.
- The full `docs/spec/adr-0041-0046-comprehensive-plan-spec.md` / `docs/build-plan/build-plan-adr-0041-0046.md` beyond the ADR-0041-T2 block already extracted here.
- `scripts/plan/context_assembler.py` (ADR-0042-T1, landed), `scripts/store/plan_model.py` (ADR-0044-T1), `scripts/plan/orchestrate.py` (ADR-0043-T2), `scripts/plan/activation.py`/`plan_driver.py` (ADR-0046-T1) — other-task manifests, out of THIS task's scope (`orchestrate.py` is an AC-4 forbidden surface — read its call relationship only via `generate_plan.py`, do NOT open/edit it).
- The frozen four (`scripts/store/store.py`/`scripts/store/keying.py`/`scripts/plan/pipeline.py`/`scripts/plan/adjudicate.py`) + `adjust.py`/`router.py` — this task edits none of them; they are a `numstat=0` entry+exit check, not a read.

## Risk Pre-Check

**BLOCKING GATE — FROZEN-SPINE supersession + a de-id-adjacent completeness gate + the PF-S130-01 tautological-swap failure class.** Do not proceed to GREEN until a real (non-`assert True`) test exists for every risk below. A placeholder test fails this gate. This is NOT a `scripts/store/` write-surface task (it rides the FROZEN `record_plan`/`store.append` unchanged — no new keying), so the store-adversarial battery (bead `pka`) does NOT own here; the store-ride property is asserted read-only via AC-1's round-trip.

- **Risk (ADR-0041 Consequences-Negative-2 — migration cost of the four translators + `_is_complete`):** a big-bang cutover would break every existing caller at once.
  - Gate condition (transitional additive, bounds the cost, disposition #2): RED must include **AC-2** — the adapter accepts BOTH shapes (a legacy thin rec lifts + validates; a full uniform program validates), **0 conformant inputs rejected** across the two shapes. Proven RED-capable against a stub that rejects one shape.
- **Risk (ADR-0041 Consequences-Negative-1 — breaks the ADR-0032 freeze on the specialist-output surface):** an un-bounded edit could bleed into the frozen engine / a sibling ADR's surface.
  - Gate condition (per-ADR-scoped numstat, tension-resolution #27): RED must include **AC-4** — `git diff --numstat <PRE_TASK_HEAD> -- scripts/plan/generate_plan.py scripts/plan/assemble.py` is NON-empty (the sanctioned supersession) AND `git diff --numstat <PRE_TASK_HEAD> -- scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/store/store.py scripts/store/keying.py scripts/store/plan_schema.py scripts/plan/orchestrate.py scripts/plan/router.py` is EMPTY (the HARD-frozen four + the sibling-superseded surfaces 0044/`plan_schema.py`, 0043/`orchestrate.py`, 0042/`router.py` untouched in THIS commit), AND the `<always-frozen>` set (`origin/main` base, incl. `adjust.py`) is EMPTY. RED-capable: a transient edit to any forbidden file makes the probe RED.
- **Risk (PF-S130-01 — the tautological component-SWAP test, the EXACT Wave-1 gap Tier-3 caught):** the Wave-1 `:356` swap was tested tautologically — a revert of the swap kept the suite green because every `compute_plan` fixture seeded only `SUMMARY_FIELD_SET` band tokens, so no test drove the production path with a DISTINGUISHING input. A recipe for a component migration that repeats this ships a plan that passes whether or not the migration happened.
  - Gate condition (production-path + revert-REDs, MANDATORY): **AC-1** MUST be an integration test on the REAL production path (`generate_plan`/`compute_plan` → `assemble` → the migrated translator → `record_plan` → `read_plan`/`resolve_plan` read-back), seeded with a uniform DOMAIN PROGRAM whose seven-field content includes fields the OLD thin path DROPS (`monitoring_signals` with a validity tier, `adjustment_rules`, the GRADE `rationale`, `cross_domain_seams`). The read-back MUST carry all seven (0 dropped). RED-capability is proven by running AC-1 against the PRE-migration tree (the un-migrated translator drops the seven fields → AC-1 RED) — the un-migrated tree IS the "reverted" state, so "REDs against pre-migration" == "REDs on a revert of the migration." Re-verified POST-GREEN by re-checking out the pre-migration `generate_plan.py`+`assemble.py`, confirming AC-1 RED, restoring (Step 2.6). A test that passes against the pre-migration tree is tautological and FAILS this gate.
- **Risk (fail-closed / silent thin fallback — the ADR-0041 "never a silent thin fallback" property):** if a non-liftable output silently fell back to the old thin accept, a malformed/under-delivered specialist output would surface as a degraded plan instead of the honest no-plan state, AND a non-uniform output would reach the reconcile step un-validated (falsification #2).
  - Gate condition (non-liftable rejection at the collection boundary): **AC-3** — feeding an OLD bespoke non-uniform shape that is NOT adapter-liftable produces `plan == None` (the honest no-plan state; 0 non-uniform outputs become a plan/reach `orchestrate.reconcile`). RED-capable: a SILENT-THIN-FALLBACK mutation (on validate-fail, accept the payload anyway) makes the non-liftable shape produce a plan → AC-3 RED; observe RED, revert (Step 1-continued).
- **Risk (legacy drop-semantics regression — the completeness gate must not silently LOOSEN):** if the adapter always ramped every field, a legacy rec missing `reversibility` (or a number missing `reference_range`) would now lift-and-validate, silently ADMITTING a rec the thin gate dropped — a de-id-adjacent completeness regression, and `test_assemble.py` (the carve-out's ruling-§4 replacement) would break.
  - Gate condition (adapter liftability SUBSUMES the old completeness gate, Deviation #2): the migrated adapter's liftability precondition requires the legacy completeness metadata (`source`+`confidence_tier`+`reversibility` + per-number `units`+`reference_range`) to construct a conformant `rationale`/`prescription`; a legacy rec missing any is NON-liftable → dropped. Proven by the REGRESSION gate: `tests/plan/test_assemble.py::test_recommendations_fully_sourced` + `::test_specialist_all_recs_incomplete_renders_gap` stay GREEN post-migration (a rec missing `reversibility` is still dropped). If either goes RED, the adapter loosened the gate — HALT before commit.
- **Risk (guard carve-out without a behavioral replacement = gate gap, ruling §4/§5 "HALT"):** the Wave-2 reconciliation carves `assemble.py` out of the eight guards that freeze it (G1–G8), removing the byte-freeze; without a behavioral guarantor the supersession is unguarded.
  - Gate condition (behavioral replacement present + green): the `assemble.py` carve-out's replacement — THIS task's deliverable — = `tests/plan/test_assemble.py` (existing behavioral suite, PRESERVED green) + the new `tests/plan/test_generate_plan_uniform.py` (the migration behavioral suite) + the AC-4 per-ADR numstat (bounds `assemble.py`'s delta to the sanctioned supersession, replacing the removed "0-line" tripwire). This task's Step-4 regression MUST run the reconciled guards GREEN — they are carved by the Wave-2 frozen-guard reconciliation step, which runs BEFORE this task's commit; grep each guard's frozen set to confirm ONLY `assemble.py` was removed and the `<always-frozen>` HARD members remain. A reconciled guard that also removed any HARD-frozen surface, or a still-RED guard, HALTs.

## TDD Steps

Single migration unit: the transitional additive adapter + the `_is_complete`/`_compose_claim` completeness supersession + the four translators' emit are ONE coherent change to the plan-authoring path — exercised over the SAME seeded fixture programs (a training-kind and a compound-kind DOMAIN PROGRAM; a legacy thin rec; a non-liftable bespoke rec), so they are not split (tdd-planning: "all criteria test the same component"). The `assemble.py` guard carve-out is NOT a step here — it is performed by the Wave-2 frozen-guard reconciliation step (build plan Wave 2), owned by neither task, which runs BEFORE this task's commit; this task's Step-4 regression re-runs those guards GREEN (already carved).

### Step 1: RED — Write Failing Tests
Call `/write-tests` for `scripts/plan/generate_plan.py` + `scripts/plan/assemble.py`, creating `tests/plan/test_generate_plan_uniform.py`, targeting each spec T2 AC (mirror `test_assemble.py`'s `_rec(...)`/`_specialist(...)`/`_summary(...)` inline fixture convention and reuse the `_FixedEnvelopeClient`/`_FixtureBackend` client seam from `generate_plan._self_test` — all values SYNTHETIC, no real operator PII, no live client, 0 live spend):

- **AC-1 (uniform program round-trips, PRODUCTION-PATH integration test — PF-S130-01 LOAD-BEARING)** — `test_uniform_program_round_trips_zero_fields_dropped`: seed a scratch store (per `_self_test`'s `tempfile.TemporaryDirectory` + `store.append` intake pattern); author, through the `_FixedEnvelopeClient` seam, an envelope whose recommendation carries a full uniform DOMAIN PROGRAM (all seven `domain_program` fields, including `monitoring_signals` with a `validity_tier`, `adjustment_rules`, a GRADE `rationale`, and `cross_domain_seams` — fields the OLD thin path drops); run the REAL `generate_plan(domain, ...)` for a training-kind domain (`workout`) AND a compound-kind domain (`peptides` or `supplements`); read the recorded plan back via `plan_schema.read_plan(domain, plan_date, root)` (or `resolve_plan`). ASSERT the read-back `plan` value carries **all seven program fields (missing-from-read-back count == 0)** AND the thin renderable shape still rendered (record accepted it — e.g. `exercises` present for workout, the single-compound regimen for peptides), AND the workout **clearance gate still stripped `load`** (safety preserved). RED-capable + NON-tautological: run this test against the PRE-migration tree (Step 1-continued) — the un-migrated translator emits only the renderable payload, so the seven non-renderable fields are ABSENT from read-back → the test REDs. That RED against pre-migration IS the revert-REDs proof.
- **AC-2 (transitional adapter accepts BOTH shapes, 0 conformant rejected)** — `test_adapter_accepts_full_uniform_program`: an envelope carrying a full uniform program flows through `compute_plan`/`assemble` and produces a recorded-able plan (`plan is not None`, the program validates). `test_adapter_lifts_legacy_thin_payload`: a LEGACY thin rec (the current `{claim, source, confidence_tier, reversibility, category, payload}` shape, WITH the completeness metadata) flows through and produces a plan — the adapter lifted `payload` → `prescription`, derived `rationale` from the legacy metadata, set the domain kind, and ramped the remaining fields to a conformant program (`domain_program.validate` passes on the lifted result). **Compound-lift reading (QA-01, pinned):** a CLEAN legacy rec (one carrying the full completeness metadata) is ALWAYS liftable — for a compound-kind domain the adapter ramps `required_labs` + `monitoring_signals` + `adjustment_rules` to conformant NON-EMPTY transitional floors, so the lift SUCCEEDS and yields `plan is not None`; it does NOT fail closed. The fail-closed path is reserved for AC-3's genuinely-non-liftable BESPOKE shapes, NOT AC-2's clean legacy recs. ASSERT **0 conformant inputs rejected** across the two shapes (both yield `plan is not None`). Parametrize the legacy-lift over the four translators (`workout`/`nutrition`/`supplements`/`peptides`) — ALL FOUR params yield `plan is not None` (the two compound params `supplements`/`peptides` lift via the always-ramp, never fail closed here). RED-capable: fails if either shape is rejected (a one-shape-only migration).
- **AC-3 (non-uniform-output rejection — falsification #2, fail-closed, LOAD-BEARING)** — `test_non_liftable_bespoke_shape_rejected_at_collection`: an envelope whose recommendation is an OLD bespoke NON-uniform shape that is NOT adapter-liftable (its lift cannot be made to pass `domain_program.validate` — e.g. no `payload`/prescription source, or a shape that yields a partial program) flows through `compute_plan(domain, ...)` → ASSERT `result["plan"] is None` AND `result["reason"]` is an honest no-plan kind (coverage-gap / `no-actionable-recommendation`) — **0 non-uniform outputs become a plan** (so 0 reach `orchestrate.reconcile`, which consumes only non-`None` `compute_plan` candidates). RED-capable via the SILENT-THIN-FALLBACK mutation (Step 1-continued): if the adapter falls back to the old thin accept on validate-fail, the non-liftable shape yields `plan is not None` → this test REDs.
- **AC-4 (per-ADR-scoped freeze-break numstat)** — `test_per_adr_scoped_freeze_break_numstat` (subprocess `git diff --numstat`, mirror `test_generate_plan.py`'s `PRE_TASK_HEAD` pattern): (a) `git diff --numstat <PRE_TASK_HEAD> -- scripts/plan/generate_plan.py scripts/plan/assemble.py` is NON-empty (the sanctioned supersession changed exactly these two); (b) `git diff --numstat <PRE_TASK_HEAD> -- scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/store/store.py scripts/store/keying.py scripts/store/plan_schema.py scripts/plan/orchestrate.py scripts/plan/router.py` is EMPTY (the HARD-frozen four + the three sibling-superseded surfaces untouched in THIS commit); (c) `git diff --numstat origin/main -- scripts/store/store.py scripts/store/keying.py scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/plan/adjust.py scripts/plan/router.py` is EMPTY (the `<always-frozen>` HARD set over the whole build). RED-capable: a transient edit to any forbidden file makes (b)/(c) RED (proven in Step 1-continued by a throwaway edit, observed, reverted).
- **AC-5 (module test green, 0 live-API, 0 real PII)** — the whole `tests/plan/test_generate_plan_uniform.py` module passes offline; every fixture is a synthetic literal + the `_FixedEnvelopeClient`/`_FixtureBackend` seam (no real `ModelClient`, no real operator PII). Verified by the module run in Step 2 + an import grep (Verification Checklist) asserting the test imports only `pytest` + the manifest modules + `scripts.store` fixtures (0 live-client import).

Run: `.venv/bin/python -m pytest tests/plan/test_generate_plan_uniform.py -q`
Expected: FAIL — the migrated behavior does not yet exist (AC-1 REDs: seven fields dropped by the thin translator; AC-2 REDs: the uniform-program shape is not yet accepted; AC-3 may pass-for-the-wrong-reason against the thin gate — see Step 1-continued; AC-4 REDs: no supersession delta yet). If AC-1 PASSES at this stage, the test is TAUTOLOGICAL (it does not distinguish migrated from un-migrated) — fix it before GREEN.

### Step 1 (continued): RED-capability falsification (NON-SKIPPABLE)
Module presence + a thin-gate coincidence can make a rejection/round-trip test pass for the wrong reason. Before AND after implementing, prove per-assertion RED-capability:

1. **AC-1 revert-REDs (PF-S130-01, the load-bearing proof):** confirm `test_uniform_program_round_trips_zero_fields_dropped` is RED against the PRE-migration `generate_plan.py`+`assemble.py` (the un-migrated tree). This is the tautology falsifier — a round-trip test that stays GREEN on the un-migrated tree does not distinguish the migration from its absence (the Wave-1 failure). Re-run POST-GREEN by `git stash`/`git checkout <PRE_TASK_HEAD> -- scripts/plan/generate_plan.py scripts/plan/assemble.py`, confirm AC-1 RED, then restore the migrated files.
2. **AC-3 silent-thin-fallback mutation:** after GREEN, add a temporary SILENT-THIN-FALLBACK to the adapter (on `domain_program.validate` failure, accept the legacy `payload` as the plan anyway — the exact anti-behavior the ADR forbids). CONFIRM `test_non_liftable_bespoke_shape_rejected_at_collection` goes RED (the non-liftable shape now yields a plan). REVERT the mutation. A rejection test that stays GREEN under this mutation is tautological.
3. **AC-4 forbidden-file mutation:** make a throwaway one-line edit to a forbidden file (e.g. a comment in `scripts/plan/pipeline.py`), CONFIRM `test_per_adr_scoped_freeze_break_numstat` goes RED, `git checkout` the file. Proves the numstat probe bites.

This is a mandatory gate of THIS recipe — do not silently delegate RED-capability to `/write-tests`.

### Step 2: GREEN — Implement
Changes to make (descriptions, not literal code):

- `scripts/plan/assemble.py` (Modify — `_is_complete` + `_compose_claim` only; the four filters + `assemble`'s structure otherwise unchanged):
  - **`_is_complete(rec)` supersede:** replace the thin `source`+`confidence_tier`+`reversibility` (+ per-number `units`+`reference_range`) check with SEVEN-FIELD completeness — the rec carries (or the transitional adapter lifts it to) a uniform DOMAIN PROGRAM that passes `domain_program.validate`. Import `domain_program` (a leaf module — no cycle; `assemble.py` must NOT import `generate_plan`). A rec that is neither a conformant uniform program NOR adapter-liftable → incomplete → False (dropped by the existing `_compose_claim` sourcing filter — the honest no-plan state, fail-closed, no silent thin fallback). **The adapter's liftability precondition SUBSUMES the old thin completeness criteria** (a legacy rec missing `source`/`confidence_tier`/`reversibility`, or a number missing `units`/`reference_range`, is NON-liftable → dropped) so `test_assemble.py`'s drop-semantics are preserved (Deviation #2, satisfies the legacy-drop-regression gate).
  - **`_compose_claim` accept the uniform program:** the sourcing filter now gates on the seven-field completeness (`_is_complete` above); the drop-behavior (an incomplete rec is not emitted) is UNCHANGED; the composed claim carries the normalized uniform program (the lifted-or-passed-through program) so the downstream translator reads it uniformly. The other three filters (attribution, population-mismatch, HALT) are untouched.
- `scripts/plan/generate_plan.py` (Modify — the four translators + `_PLAN_TRANSLATORS` region; `compute_plan`/`generate_plan` callers unchanged in signature):
  - **The transitional additive adapter** (a helper the migrated path uses; home is executor discretion within the two manifest files subject to the no-cycle constraint — `assemble` may own the lift and `generate_plan`'s translators consume the normalized program, OR the lift lives in `generate_plan` and `assemble._is_complete` calls `domain_program.validate` on the rec's program; either yields the same public behavior — SE Ask-vs-Proceed rule 3, state the assumption): given a recommendation, PRODUCE a uniform DOMAIN PROGRAM — pass a full uniform program through (validate), OR lift a legacy thin rec (map `payload` → `prescription`; derive the GRADE `rationale` from the legacy `source`+`confidence_tier`+`reversibility`; set the domain `KIND` from the domain per `domain_program.DOMAIN_KINDS` — training for movement/nutrition, compound for supplements/peptides, executor-grounded against `DOMAIN_KIND_RULES`; ramp `monitoring_signals`/`adjustment_rules`/`required_labs`/`refusal_escalation`/`cross_domain_seams` to conformant transitional values — for a compound kind, a CLEAN legacy rec ALWAYS ramps `required_labs` + `monitoring_signals` + `adjustment_rules` to NON-EMPTY floors so the lift succeeds (AC-2, QA-01); the fail-closed path applies only to a rec that CANNOT be made conformant — a non-liftable bespoke shape lacking the legacy completeness metadata (AC-3)). Bound the result by `domain_program.validate` — a non-conformant lift is REJECTED (returns the incomplete signal), NEVER a silent thin fallback.
  - **The four translators emit the uniform program additively:** each translator (`_to_workout_plan` 1:1, `_to_nutrition_plan` AGGREGATE, `_to_supplements_plan` 1:1, `_to_peptides_plan` single-compound) reads the surviving recommendations' normalized uniform program and emits a plan carrying (a) the thin renderable shape `record_plan`'s frozen per-domain validator requires (extracted from the program's `prescription`) SO `record_plan` accepts it unchanged, PLUS (b) the full seven-field DOMAIN PROGRAM attached ADDITIVELY (as extras — the `plan_schema.py:33-36` open-on-extras seam) so the round-trip drops 0 of 7. PRESERVE every existing per-domain safety/shape rule: the workout **clearance gate** (`load` dropped unless `clearance_granted`), the nutrition day-target aggregation + the RED-S/LEA `_DOMAIN_GATES` veto, the supplements 1:1, the peptides single-compound rule. `_PLAN_TRANSLATORS` keeps its four-domain mapping (the 4→15+ roster growth is ADR-0046-T1, disjoint).
  - **No new import cycle, no edit to `plan_schema.py`/`orchestrate.py`/the frozen four** — the seven fields ride the UNCHANGED `record_plan`/`store.append` (AC-4 keeps those EMPTY).

Run: `.venv/bin/python -m pytest tests/plan/test_generate_plan_uniform.py -q`
Expected: PASS (AC-1/AC-2/AC-3/AC-5). AC-4 needs the commit's `PRE_TASK_HEAD` delta — it is asserted once the two files carry the supersession (green after the edits).

### Step 2.5: Guard carve-out — NOT this task's manifest (performed by the Wave-2 frozen-guard reconciliation step)
The `assemble.py` guard carve-out is performed by the Wave-2 frozen-guard reconciliation step (build plan Wave 2), not this task's manifest; this task's Step-4 regression runs AFTER the reconciliation, so the `assemble.py` guards are already carved. (Option (A), Architect arbitration `docs/task-plan/.pipeline/review-architect.md` §3 — the reconciliation commit is owned by neither Wave-2 task; neither task stages the guard files. The reconciliation enumerates the FULL `assemble.py` guard set — the eight G1–G8, including G8 `tests/plan/test_generate_plan.py::test_assemble_byte_unchanged_vs_pretask` — by executing the suite (F-011) and carving exactly the RED guards, surgically, removing ONLY `assemble.py` and leaving the `<always-frozen>` HARD set frozen.) This task's deliverable toward the behavioral replacement (ruling §4) is `tests/plan/test_assemble.py` staying green + the new `tests/plan/test_generate_plan_uniform.py`.

### Step 2.6: AC-1 revert-REDs re-verification (PF-S130-01 belt-and-suspenders)
`git stash push -- scripts/plan/generate_plan.py scripts/plan/assemble.py` (or `git checkout <PRE_TASK_HEAD> -- scripts/plan/generate_plan.py scripts/plan/assemble.py`); run `.venv/bin/python -m pytest tests/plan/test_generate_plan_uniform.py::test_uniform_program_round_trips_zero_fields_dropped -q`; CONFIRM it goes RED (the reverted thin translator drops the seven fields); restore the migrated files (`git stash pop` / `git checkout HEAD -- ...`). A round-trip that stays GREEN on the reverted tree is tautological — HALT and fix the test's distinguishing input.

### Step 3: REFACTOR
Review for:
- Duplication between `_is_complete`'s conformance check and the translators' program extraction — the single conformance authority is `domain_program.validate`; the lift/adapter is ONE helper (no per-domain copy of the lift logic); the per-domain translators differ ONLY in the renderable-shape extraction (workout 1:1 / nutrition aggregate / supplements 1:1 / peptides single-compound), matching the existing 4-translator shape.
- The additive-emit path is uniform across the four translators (one "attach the seven-field program as extras" step, not four copies).
- No new import crept in that forms a cycle (`assemble` imports `domain_program` + stdlib only, NOT `generate_plan`; `generate_plan` keeps its `assemble`/`context_assembler`/`plan_schema` imports).
- Remove any residue of the Step-1/Step-2.6 temporary stubs/mutations/reverts.

Run: `.venv/bin/python -m pytest tests/plan/test_generate_plan_uniform.py tests/plan/test_assemble.py tests/plan/test_generate_plan.py -q`
Expected: PASS (no behavior change; the two behavioral guards + the new module green).

### Step 4: REGRESSION
Run: `.venv/bin/python -m pytest -q`
Expected: PASS at the baseline floor — the SAME two pre-existing failures (`test_bind`/`test_server`) + 7 skipped; the passed count RISES from 2529 by the new `test_generate_plan_uniform.py` count. **The gate is no-NEW-failure.** In particular: `tests/plan/test_assemble.py` (drop-semantics preserved) + the reconciled guards (G1–G8, carved by the Wave-2 frozen-guard reconciliation step — incl. G8 `tests/plan/test_generate_plan.py::test_assemble_byte_unchanged_vs_pretask`) + `tests/plan/test_generate_plan.py` + `tests/genetics/test_dna_aware_plan_e2e.py` + `tests/serve/test_plan_loop_hold.py` are GREEN. A 3rd distinct failure means the migration reached outside the manifest (e.g. an unintended `orchestrate`/`plan_schema` behavior change, or the adapter loosened drop-semantics) — do NOT fix unrelated tests; investigate whether the change bled past the scoped supersession and flag per the feedback protocol.

## Interface Contracts

This task SUPERSEDES the plan-authoring completeness + translation surface. The migrated contract is the shared seam downstream tasks consume:

- **The transitional additive adapter** — produces a uniform `domain_program`-validated program from a recommendation: pass a full uniform program through (validate); lift a legacy thin rec (`payload`→`prescription`; `source`+`confidence_tier`+`reversibility`→GRADE `rationale`; domain→`KIND`; the remaining fields ramp to conformant transitional values). BOUNDED by `domain_program.validate` — a non-conformant result is REJECTED (the incomplete signal), NEVER a silent thin fallback. **Liftability precondition SUBSUMES the old thin completeness gate** (a legacy rec missing `source`/`confidence_tier`/`reversibility` or a number missing `units`/`reference_range` is non-liftable → dropped), so drop-semantics are preserved (behavior-preserving migration).
- `assemble._is_complete(rec: Mapping) -> bool` — UNCHANGED signature; now returns True iff `rec` carries (or the adapter lifts it to) a `domain_program.validate`-conformant program. Consumed by `_compose_claim` (unchanged drop-behavior). `assemble.py` imports `domain_program` (leaf), NOT `generate_plan` (no cycle).
- **The four translators** (`_to_workout/nutrition/supplements/peptides_plan`) + `_PLAN_TRANSLATORS` — emit a plan carrying (a) the thin renderable shape `plan_schema.record_plan`'s FROZEN per-domain validator requires (from `prescription`) + (b) the full seven-field DOMAIN PROGRAM as ADDITIVE extras. The seven fields ride the UNCHANGED `record_plan`→`store.append` (the `plan_schema.py:33-36` open-on-extras seam) and round-trip 0-of-7-dropped. Per-domain safety/shape rules PRESERVED (workout clearance gate, nutrition aggregation + RED-S/LEA veto, peptides single-compound).
- **The additive-ride is TRANSITIONAL:** ADR-0044-T1 (Wave 2, disjoint `plan_model.py`/`plan_schema.py`) SUPERSEDES `record_plan` to store the seven-field program FIRST-CLASS; until then the program rides the frozen record path as extras (disposition #14 "value rides `store.append`"). ADR-0044-T1 must read the same seven-field shape this task emits.
- **Dependent tasks** (per the spec Dependency Map's outgoing edges from ADR-0041-T2):
  - **ADR-0046-T1** — grows `_PLAN_TRANSLATORS` 4→15+; each grown translator MUST emit the migrated uniform shape via this adapter (the shape migration lands first, the roster growth rebases; disjoint `generate_plan.py` region).
  - **ADR-0043-T2** — the orchestrator `reconcile` reads each emitted program's `cross_domain_seams` (the ADR-0041-T1-pinned edge shape this migration now carries into the composed output).
  - **ADR-0044-T1** — stores the seven-field program intact (supersedes the additive-ride with first-class storage).
- **Behavioral invariants:** both shapes valid during migration (0 conformant rejected); a non-liftable output is rejected at the collection boundary (0 reach `orchestrate.reconcile`); the seven fields round-trip 0-dropped; drop-semantics preserved; `record_plan`/`store.append`/`keying` + the frozen four + `router.py`/`orchestrate.py`/`plan_schema.py` byte-unchanged in this commit; no `assemble`↔`generate_plan` cycle.
- **Change control:** ADR-0046-T1 / 0043-T2 / 0044-T1 MUST NOT change the adapter contract, the seven-field-rides-`record_plan`-additively property, or the `_is_complete` signature without Architect review (this wave carries Architect review for exactly this migration seam).

## Verification Checklist

- [ ] AC-1: a uniform DOMAIN PROGRAM authored through the `_FixedEnvelopeClient` seam flows `generate_plan`→`assemble`→migrated translator→`record_plan`→`read_plan` and the read-back carries all seven program fields (missing-count == 0) for a training-kind AND a compound-kind domain, with the renderable shape recorded + the workout clearance gate preserved — `test_uniform_program_round_trips_zero_fields_dropped` (PRODUCTION PATH; RED against the pre-migration tree — the revert-REDs proof, PF-S130-01).
- [ ] AC-2: the adapter accepts BOTH a full uniform program AND a legacy thin rec (lifted `payload`→`prescription`, validated) with 0 conformant inputs rejected, parametrized across the four translators — `test_adapter_accepts_full_uniform_program` + `test_adapter_lifts_legacy_thin_payload`.
- [ ] AC-3: an OLD bespoke non-uniform, non-adapter-liftable shape yields `plan is None` (honest no-plan; 0 non-uniform outputs become a plan / reach `orchestrate.reconcile`) — `test_non_liftable_bespoke_shape_rejected_at_collection`; RED under the silent-thin-fallback mutation (Step 1-continued), then reverted.
- [ ] AC-4: `git diff --numstat <PRE_TASK_HEAD> -- scripts/plan/generate_plan.py scripts/plan/assemble.py` NON-empty; `... -- scripts/plan/pipeline.py scripts/plan/adjudicate.py scripts/store/store.py scripts/store/keying.py scripts/store/plan_schema.py scripts/plan/orchestrate.py scripts/plan/router.py` EMPTY; `git diff --numstat origin/main -- <always-frozen>` (incl. `adjust.py`) EMPTY — `test_per_adr_scoped_freeze_break_numstat` (RED under a throwaway forbidden-file edit, then reverted).
- [ ] AC-5: `.venv/bin/python -m pytest tests/plan/test_generate_plan_uniform.py` passes against synthetic fixtures (0 live-API calls, 0 real operator PII); the test imports only `pytest` + the manifest modules + `scripts.store`/`scripts.model` fixture seams (0 real-client import) — import grep clean.
- [ ] RED-capability falsification (Step 1-continued + Step 2.6, NON-SKIPPABLE): AC-1 observed RED against the reverted (pre-migration) tree (PF-S130-01 tautology falsifier); AC-3 observed RED under the silent-thin-fallback mutation; AC-4 observed RED under a forbidden-file edit — each reverted.
- [ ] Risk (ADR-0041 Neg-2 migration cost) covered by AC-2 (transitional additive, no big-bang).
- [ ] Risk (ADR-0041 Neg-1 freeze break) covered by AC-4 (per-ADR numstat) + the Wave-2 frozen-guard reconciliation step's surgical `assemble.py` carve-out (store/engine primitives stay frozen; carve owned by the reconciliation, not this task).
- [ ] Risk (PF-S130-01 tautological swap) covered by AC-1's production-path revert-REDs design + Step 2.6.
- [ ] Risk (fail-closed / silent thin fallback) covered by AC-3.
- [ ] Legacy drop-semantics preserved (Deviation #2): `tests/plan/test_assemble.py::test_recommendations_fully_sourced` + `::test_specialist_all_recs_incomplete_renders_gap` GREEN post-migration (a rec missing `reversibility` still dropped).
- [ ] `assemble.py` guard carve-out (ruling §4/§5 — performed by the Wave-2 frozen-guard reconciliation step, NOT this task's manifest): the reconciled guards G1–G8 GREEN with ONLY `assemble.py` removed; a grep of each guard's frozen set confirms the `<always-frozen>` HARD members (`store.py`/`keying.py`/`pipeline.py`/`adjudicate.py` + `adjust.py`) remain frozen; this task's behavioral-replacement deliverable (`test_assemble.py` + `test_generate_plan_uniform.py`) present + green (no gate gap).
- [ ] No regression: `.venv/bin/python -m pytest -q` holds the floor — the same two known reds, 7 skipped, passed rises by the new module's count; no 3rd failure.
- [ ] Files modified match the 3-file manifest: `scripts/plan/generate_plan.py`, `scripts/plan/assemble.py`, `tests/plan/test_generate_plan_uniform.py` (Create) — the guard files are the Wave-2 reconciliation commit's, NOT this task's; no other scope creep; temporary stubs/mutations reverted.
- [ ] Interface contract documented (the transitional adapter + the seven-field-rides-`record_plan`-additively property + `_is_complete` signature + the no-cycle constraint + the three dependent task IDs).

## Commit

```
feat(plan): migrate translators to uniform program
```
Stage only: `scripts/plan/generate_plan.py`, `scripts/plan/assemble.py`, `tests/plan/test_generate_plan_uniform.py` (the `assemble.py` guard carve-out is the Wave-2 frozen-guard reconciliation commit, owned by neither Wave-2 task — this task stages NONE of the guard files).

(Stage with `git add <the three paths>` as its OWN command, then `git commit` separately — the PII commit hook denies single-call stage+commit, `git commit -a`, and pathspec `git commit <path>`. Confirm the working tree has no file outside the three-path manifest — in particular that the Step-1/Step-2.6 temporary stubs/mutations/reverts were undone, and that NO `vault/` daemon-churn file is staged — before committing.)

## Deviations

| # | Spec Says | Recipe Says | Reason | Impact |
|---|-----------|-------------|--------|--------|
| 1 | Build-plan Agent Matrix (line 165): ADR-0041-T2 reviewers = `QA` only ("no new cross-module contract → no Architect") | Frontmatter reviewers = `[QA, Architect]` | The task dispatch directs `[QA, Architect]`. Architect is warranted here: the transitional-adapter contract and the seven-field-rides-`record_plan`-additively property (the emit shape ADR-0044-T1 must store + ADR-0046-T1 must emit) are contract-adjacent cross-wave seams. | Adds an Architect review lens on the adapter/emit contract; no change to the ACs or the file set. |
| 2 | "the adapter lifts today's `payload` into `prescription` while the other six fields ramp" | The adapter's LIFTABILITY PRECONDITION subsumes the old thin completeness gate — a legacy rec missing `source`/`confidence_tier`/`reversibility` (or a number missing `units`/`reference_range`) is NON-liftable → dropped; only the SEVEN-FIELD fields the legacy rec lacks (`monitoring_signals`/`adjustment_rules`/`required_labs`/`refusal_escalation`/`cross_domain_seams`) ramp | If the adapter ALWAYS ramped every field, a legacy rec the thin gate dropped would silently lift-and-validate — a completeness regression that breaks `test_assemble.py`'s drop-semantics (`_count_incomplete` calls production `_is_complete` directly). Subsuming the old gate preserves drop-semantics AND satisfies AC-2 (0 conformant rejected) + AC-3 (non-liftable rejected). SE Ask-vs-Proceed rule 3: same public interface, behavior-preserving implementation, assumption stated. | `test_assemble.py` stays GREEN (no manifest expansion for it); the migration is behavior-preserving for legacy recs; downstream emitters (0046-T1) that emit full uniform programs are unaffected. |
| 3 | The adapter is bounded by `domain_program.validate`; the six fields "ramp" | For a COMPOUND-kind domain (supplements/peptides) the adapter ramps `required_labs` + `monitoring_signals` + `adjustment_rules` to conformant NON-EMPTY transitional floors. **QA-01 reconciliation:** a CLEAN legacy compound rec (carrying the full completeness metadata) ALWAYS ramps successfully → lifts to `plan is not None` (AC-2, all four params); the FAIL-CLOSED path is reserved for a genuinely-non-liftable BESPOKE shape lacking the metadata (AC-3), NOT a clean legacy rec. The domain→KIND mapping + the exact ramp values are executor discretion, grounded against `domain_program.DOMAIN_KINDS`/`DOMAIN_KIND_RULES`. | `domain_program.validate` rejects a compound program with empty `required_labs`/autoregulation; a CLEAN legacy compound rec is always ramped to conformant transitional values (lifts, AC-2), only a non-liftable bespoke shape fails closed (AC-3). The recipe pins the STRUCTURE (clean-rec-ramps vs non-liftable-rejects), not the concrete ramp tokens. | AC-2's four-param legacy-lift ALL yield `plan is not None` (clean compound recs ramp, never fail closed here); the fail-closed behavior is exercised only by AC-3's non-liftable bespoke shape — no downstream contract change. |

## Rollback

If verification fails after 3 fix cycles:
1. `git stash -m "failed-ADR-0041-T2-attempt-{N}"` — preserve the work for diagnosis.
2. `git checkout {commit-before-task}` — revert to the entry-state HEAD recorded in Entry State (also restores `generate_plan.py`/`assemble.py` and discards this task's edits + any residual Step-1/Step-2.6 temporary stub/mutation; the guard carve-out belongs to the separate Wave-2 reconciliation commit and is reverted with it, not here).
3. Update the issue tracker: flag ADR-0041-T2 as BLOCKED with the failure log.
4. Escalate: "Task ADR-0041-T2 failed verification after 3 attempts. Stash ref: {ref}. Failure log: {summary}." ADR-0041-T2 is a Wave-2 FROZEN-SPINE task that ADR-0046-T1 depends on (`_PLAN_TRANSLATORS` roster growth rebases on the migrated shape) and it shares the Wave-2 checkpoint with ADR-0044-T1/ADR-0043-T1 — a BLOCK halts the Wave 2→3 boundary; surface it immediately, do not start ADR-0046-T1.
5. Stash cleanup: after abandonment or re-planning, drop all `failed-ADR-0041-T2` entries from `git stash list`.

Do NOT: silently skip a failing acceptance criterion; weaken a test to make it pass (especially AC-1's production-path round-trip / the revert-REDs proof, AC-3's non-liftable rejection / the silent-thin-fallback mutation, or `test_assemble.py`'s preserved drop-semantics — these ARE the fail-closed + non-tautological migration safety deliverable); edit any frozen guard here (the `assemble.py` carve-out is the Wave-2 reconciliation commit's, not this task's — this task stages NO guard file); or proceed to any downstream task (ADR-0046-T1) with the migration in a broken state.
