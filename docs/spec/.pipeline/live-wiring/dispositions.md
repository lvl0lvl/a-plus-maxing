# Unresolved Concerns Gate — Live-Wiring Spec (ADR-0026 + ADR-0027)

Dispositioned autonomously per the S93 standing directive (the operator delegated these engineering dispositions; none requires a research spike — the build is mock/fixture-tested and the open items are either in-task design choices or post-build operator-side/operational items already scoped out by "everything up to the live test").

| # | Source | Summary | Disposition | Rationale |
|---|--------|---------|-------------|-----------|
| 1 | ADR-0026 OQ-1 | SHAPE of the shared control-inversion driver (generator/coroutine vs step state-machine) + the exact extract boundary in run_orchestrated | **PROCEED** | The no-fork-via-SHARED-DRIVER decision is FIXED; only the realization shape is open, resolved in the keystone build task during TDD, bounded by no-fork + frozen-inner-engine + behavior-preservation. Documented as an in-task design decision (a generator/`.send()` coroutine is the recommended default; the task may choose the step state-machine if cleaner). |
| 2 | ADR-0026 OQ-2 | The composed gate_dispatch topology (one gate pass vs two, ordering, re-trigger, joint cap) | **PROCEED** | The disposition OUTPUT KEYS (`accept`/`safety_passed`/`revise_domains`) are FIXED by the loop; only the adapter's INTERNAL composition is open, resolved in the gate_dispatch task. Documented as assumption: the composer emits exactly the 3-key disposition; the one-pass-vs-two internal choice is a build decision. |
| 3 | ADR-0026 OQ-3 | Subscription rate/concurrency ceiling that bounds per-plan aggregate dispatch volume (target 2026-07-15) | **DEFER** | Operational/billing question about the LIVE subscription path — does NOT block the mock-tested build (the built `dispatch_cap` is the in-run fail-closed bound). Relevant to S94 + the future all-API cutover. Out-of-scope for this build spec; recorded as a post-build operator/operational concern. |
| 4 | ADR-0026 OQ-4 | What core-capability-audit.sh CAN mechanically assert of the skill-driven A′ path | **PROCEED** | Already resolved in the ADR: the audit asserts the deterministic Python SPINE via a fixture-`dispatch` `--self-test` exercising the A′ inversion (promotion-on-accept, 0-plans-on-safety-not-True); the live subscription dispatch is the S94 attestation. Encoded as the audit-repoint task's ACs. |
| 5 | ADR-0027 OQ-1 | Per-request no-train/ZDR knob on messages.create, or key-property only | **PROCEED** | Finding stands: no per-request flag in the SDK surface; no-train is a key/account property the operator guarantees. The build proceeds with that contract (key resolved from env at call time, never tracked/printed/committed, agent never reads the keychain). Documented as assumption; if a knob is later confirmed, add it as defense-in-depth. |
| 6 | ADR-0027 OQ-2 | Exact no-train retention window for raw plan-intake PII (target 2026-06-30) | **DEFER** | Operator-side / published-policy question; does NOT change the decision (accepts bounded retention) — only quantifies a negative consequence. Verified at/before the S94 live test. Out-of-scope for the mock-tested build. |
| 7 | ADR-0027 OQ-3 | Local-residue handling for raw plan-intake during the live de-id call (LOAD-BEARING for ADR-0005) | **PROCEED** | Resolved to the most-rigorous crown-jewel posture and encoded as a HARD AC on the deidentify backend task: the raw plan-intake is held IN MEMORY ONLY during the call, never written to ANY path (tracked or gitignored — no scaffold replay for the raw PII), and discarded after the boundary emits the de-identified summary. The block-pii-commit + pre-push backstop remains the defense-in-depth. |
| 8 | ADR-0027 OQ-4 | Exact retry/backoff/timeout mechanics before ModelCallError | **PROCEED** | The fail-closed PRINCIPLE is decided (any failure → ModelCallError → DEID_HALTED); only the mechanics are a spec-stage concern. The deidentify task picks bounded retries + a timeout (mock-tested via injected SDK exceptions). Documented as an in-task decision. |

## Summary: 6 PROCEED (documented as assumptions/ACs), 2 DEFER (operator-side/operational, post-build), 0 BLOCK (no research spike needed — build is well-grounded and mock-tested).

## Dispositions Status: COMPLETE
