# a-plus-maxing — .venv runtime dependencies (the gitignored, per-instance
# Python 3.14 runtime). Provision a fresh clone with:
#   python3 -m venv .venv && .venv/bin/python -m pip install -r requirements.txt
#
# pytest     — the V1 Python test baseline (.venv/bin/python -m pytest -q)
# jsonschema — gate_attest.py verify-chain, used by the research-provenance audit
#              (scripts/audit-research-provenance.sh) + the aplus-research gates
#
# NOTE: the `anthropic` SDK is intentionally NOT listed here. The test suite runs with
# the SDK ABSENT so no test path can ever make a live model call (the 0-live-spend
# guard — the guard tests skip when the SDK is present). Install it ONLY for the
# operator-present LIVE run: `.venv/bin/python -m pip install anthropic==0.112.0`
# (see scripts/model/keychain-setup.md).
pytest>=9.0
jsonschema>=4.26
